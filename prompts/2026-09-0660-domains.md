[ ]

[✨🦩] The app is running on a domain `ptbk.io` but some subpages should have their own domains.

- The `/ai-ta-krajta` should became `ai-ta-krajta.cz`
- The `/cs/pavol` should became `pavolhejny.cz`
- The `/en/pavol` should became `pavolhejny.com`
- Keep the `/ai-ta-krajta`, `/cs/pavol` and `/en/pavol` reirect them to corresponding domains.
- In the file `DOMAINS.md`, write instructions on what I need to configure on Cloudflare and Vercel to get it working
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)
