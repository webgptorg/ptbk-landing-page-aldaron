[x] by OpenAI Codex `gpt-5.6-terra` thinking `max` (ChatGPT account) - Implementation ~$0.3815 13 minutes; Testing 5 minutes

[✨🍔] Allow to do discount codes with wildcards

- For example, a discount code like `SUMMER*` would apply to all codes starting with `SUMMER`.
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

---

[x] by OpenAI Codex `gpt-5.6-terra` thinking `max` (ChatGPT account) - Implementation ~$0.2415 9 minutes; Testing 6 minutes

[✨🍔] Allow to do discount codes with wildcards the wildcards to be anywhere in the code and also allow any amount of wildcards

- For example, a discount code like `SUMMER*` would apply to all codes starting with `SUMMER`.
- For example, a discount code like `*SUMMER` would apply to all codes ending with `SUMMER`.
- For example, a discount code like `*SUMMER*` would apply to all codes containing `SUMMER`.
- For example, a discount code like `*SUMMER*2026` would apply to all codes containing `SUMMER` and ending with `2026`.
- etc.
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

