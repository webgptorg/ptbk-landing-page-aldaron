# Run trace of `prompts/2026-09-0240-wrap-up-pdf.md#1`

-   **Prompt:** After the workshop has ended, allow to download a wrap-up PDF
-   **Outcome:** Succeeded
-   **Runner:** OpenAI Codex `gpt-5.6-terra` thinking `max` (ChatGPT account)
-   **Attempts:** 1
-   **Steps:** Implementation ~$0.3039 10 minutes; Testing 8 minutes
-   **Verification command:** `npm run test-for-ptbk-coder`
-   **Started:** 2026-09-16T07:14:12.810Z
-   **Finished:** 2026-09-16T07:58:44.255Z
-   **Duration:** an hour

## Runtime log

```text
=== runner shell started at 2026-09-16T07:14:13.022Z ===
Script path: /c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/.promptbook/coder-prompts/2026-09-0240-wrap-up-pdf.sh

--- raw input ---
if [ -n "${PTBK_AGENTS_SERVER_ENV_FILE:-}" ] && [ -f "${PTBK_AGENTS_SERVER_ENV_FILE}" ]; then
set -a
source "${PTBK_AGENTS_SERVER_ENV_FILE}"
set +a
elif [ -f .env ]; then
set -a
source .env
set +a
fi

CODEX_LOGIN_STATUS="$(
    unset OPENAI_API_KEY OPENAI_BASE_URL CODEX_API_KEY
    # 'codex login status' prints the "Logged in using ChatGPT" line to stderr, so merge stderr into stdout (2>&1) to capture it
    codex login status 2>&1 || true
)"
case "$CODEX_LOGIN_STATUS" in
    *"Logged in using ChatGPT"*)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=1
        ;;
    *)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=0
        ;;
esac

CODEX_LOGIN_METHOD=chatgpt
CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=chatgpt)
unset CODEX_API_KEY
if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" != "1" ] &&
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" = "1" ] &&
    [ -n "${OPENAI_API_KEY:-}" ]; then
    CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=api)
    CODEX_LOGIN_METHOD=api
    CODEX_API_KEY="${OPENAI_API_KEY}"
    export CODEX_API_KEY
fi

if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" = "1" ] ||
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" != "1" ] ||
    [ -z "${OPENAI_API_KEY:-}" ]; then
unset OPENAI_API_KEY
unset OPENAI_BASE_URL
fi

printf '%s %s\n' 'ptbk-codex-login-method:' "${CODEX_LOGIN_METHOD}"

codex \
    "${CODEX_LOGIN_METHOD_ARGUMENTS[@]}" \
    -c model_reasoning_effort="max" \
    --ask-for-approval never \
    exec --model gpt-5.6-terra \
    --local-provider none \
    --sandbox danger-full-access \
    -C /c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron \
    --skip-git-repo-check \
    <<'CODEX_PROMPT'

## Your Task

After the workshop has ended, allow to download a wrap-up PDF 

- The wrap-up PDF should include a summary of the workshop, key takeaways, and any materials shared during the session.
- Do not add anything into the admin or database. The wrap-up PDF should be generated from information already there. 
- You are working with page `/cs/online-workshop/participant?workshop=`
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

## Your Behavior

You are Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

## Context

### Context

This repository contains Promptbook landing pages for different businesses,
use cases, and audiences. Keep these rules current when behavior changes.

#### Public routes

- `/` redirects to `/cs` or `/en` using `Accept-Language`.
- `/cs` is the Czech homepage and source of truth for homepage structure and copy.
- `/en` is its English localization.
- `/pro-mesta`, `/pro-firmy`, `/for-agro`, `/for-industry`, `/ai-supervize`,
  `/hackathon-factory`, and `/pavol` are specialized landing pages. `/pavol`
  redirects to `/cs/pavol` or `/en/pavol`; the localized pages are Pavol Hejny's
  personal pages.
- `/ai-supervize-mini` is the Czech one-day AI Supervize page. Published terms,
  prices, capacities, places, FAQs, registration, and participant information
  come from `/admin/workshops`; with no published term it shows a notice.
  `/skoleni` redirects there.
- `/cs/online-workshop` lists free 60-minute online workshops about writing
  production code with AI agents. Each term has its own subject and description,
  but all use one registration form. `/cs/online-workshop/dekujeme` is the
  full-load conversion page; `/participant` is the live room. Its waiting room
  offers every published term as the same term cards the landing page registers
  with: running and upcoming ones first, finished ones behind a disclosure.
  Picking one changes the room being connected to and the `workshop` parameter,
  without losing the name and e-mail already typed.
- `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
  merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
  the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
  `application/rss+xml`.
  Each episode lists every credited person by name, and every person any source
  credits has a card in `aiTaKrajtaPeople.ts`, which a test keeps in step, so a
  credited person never misses their portrait and the filter cannot lose them.
  It keeps exact episode counts but labels subscriptions and listening hours as
  estimates. The fixed mini-player, newest-episode header button, person/search/
  episode/play/archive/collaboration filters, and section hash are shareable as
  query/hash state; the snake game is local state. Person clicks filter episodes.
  Everyone in the roster shows a normalized, transparent PNG portrait. The source
  crop is recorded by `scripts/_cutAiTaKrajtaPeoplePortraits.mjs`, from the
  episode cover the show itself published them on or from the picture they publish
  of themselves when no cover carries them; the final alpha PNG is named by the
  roster and a test verifies its format and transparent pixels. Portrait frames
  supply subtle, stable neutral-gradient variations and a restrained hover cue,
  so photographed backgrounds never compete with the page. The one people list
  is drawn anew
  for every visit, weighted by how many episodes of the archive name each
  person, so somebody heard often comes up high far more often than someone in
  one díl without the list ever becoming a ranking. The draw is local state and
  is made in the browser; the page itself is built in the order the draw leans
  towards, from the most often named person to the least often named one, so the
  browser hydrates into the list it was sent.
  Collaboration submissions use `/admin/contacts`. Its tab icon is the page's
  own snake drawing in `/ai-ta-krajta/logo.svg` and `.png`; SVG corners are
  rounded and transparent, while the raster fills its square. That drawing is
  traced off the cover artwork of the show and recorded once in
  `businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.ts`, together with the
  measurements of the animal along its own length. The snake of the minigame
  starts in exactly that shape, in the frame the still logo occupied, and only
  then eases into the proportions, colours and speed of a game snake.
  A seven-cell Promptbook coder terminal floats in the bottom-right corner and
  clears the player and cookie controls. It types `$ ptbk`, then animates an
  ASCII octopus in response to pointer, focus, scrolling, and the snake terrarium.
  It stays still for reduced motion and pauses its clock in hidden tabs. The
  badge and the shared footer credit link to `https://coder.ptbk.io/`.
- `/ai-ta-krajta/media-kit` and `/ai-ta-krajta/branding` are the two pages beside
  the podcast. They are named once, in `AI_TA_KRAJTA_SUBPAGES`, which the footer
  lists and which they point at each other through, so no link can name a page
  differently than the page names itself. Both wear the same compact header,
  section heading and footer as each other. The brand kit hands out the logo the
  site already serves at `/ai-ta-krajta/logo.svg`, `.png` and the cover artwork,
  publishes no colour outside `AI_TA_KRAJTA_COLORS`, and says how the name is
  written; it asks for nothing and sends visitors to the media-kit form.
- `/cs/komunita` is the permanent Czech community room. It has chat, polls,
  projects, materials, and published terms, but no schedule, stage, or live
  updates. Terms show event kind, format/place, price, and status. A term with a
  live room links there; otherwise it links to its landing page. The calendar
  opens on the member's month, can filter by day, and uses the same terms and
  statuses as the cards. Empty days cannot be selected; an empty month is only
  selected when the member's month has no terms. The room offers Google Calendar
  and `webcal:` subscriptions.
- `/cs/komunita/projects` is the full project gallery, ordered by upvotes.
  `/cs/komunita/projects/<project_id>` shows scraped project details and a
  moderated discussion. Project-room sessions derive from the community session;
  the author moderates their own discussion.
- `/cs/komunita/clenstvi` offers free community access and the 199 Kč monthly
  membership. Webinars remain free; paid access adds recordings, archive,
  materials, extra content, priority questions, and Discord/community features.
  The page is prefilled from `fullname` and `email`, supports community discount
  codes, has no new annual plan or trial, and allows cancellation.
- `/cs/komunita/calendar.ics` publishes the same terms by stable slug, so moved
  or renamed terms update existing subscriptions. It contains only event terms
  and public destinations, never subscriber identity.
- `/admin/workshops` manages terms of every event kind, participants, comments,
  reactions, content, attached community polls, and settings. Every term says both
  of its audiences: the people registered on the landing page of its event, and
  the people who entered its room. A room which is no term of an event says
  nothing about registrations at all. The registration block opens the exact
  filtered `/admin/contacts` list and its shared CSV, vCard, and Book exports;
  it never creates a second contact table or serializer in workshop administration.
- `/admin/community` manages the permanent community, including polls, project
  moderation, participants, memberships, payments, and room analytics.
- `/admin/shortener` manages public short links, QR/UTM output, destinations,
  notes, search/filter/sort state, and private click history. Links are served
  by `/[shortcode]`; `/shortener` redirects to the admin page.
- `/admin/login` authenticates the single `admin` account with `ADMIN_PASSWORD`
  and a signed session cookie. All `/admin/*` pages and APIs require
  `requireAdminSignedIn`. `/admin` is the post-login dashboard.
- Other public/legal routes include `/contact`, `/data-deletion`, `/privacy`,
  `/terms`, `/dekujeme`, `/branding`, and the routes under `/k`, `/old`, and
  `/test`.

#### Shared community and workshop behavior

- Rooms lead to each other in both directions, through one identity hand-off. The
  community lists the terms and opens the room of each; an invited workshop room
  places its community link beside its materials as a special material for every
  participant, regardless of paid membership. Where a special material sits is
  decided once, in `lib/workshops/workshopSpecialMaterials.ts`: a card says where
  among the ordinary materials it belongs, and the invitation opens the material
  list of a participant who does not pay while it closes the list of a paying
  member. A membership which is not loaded yet leaves it where a participant who
  does not pay reads it. Both carry the connected member's
  name and email on, and an incomplete identity carries nothing rather than half
  of it. Which rooms invite is answered by
  `lib/workshops/workshopKindCapabilities.ts`; the community and project
  discussions do not. The invitation names the community's sections by the names
  the community itself uses.
- Community polls attached to workshops are shared. A normalized email gives a
  member one vote across the community and all attached workshops. Workshops
  may display and accept votes, but the community owns administration.
- The participant room owns the membership badge and popup. Community and live
  workshop rooms use the same membership for the connecting email, and checkout
  returns to the room where it started. Membership is offered only by room kinds
  listed in `lib/workshops/workshopKindCapabilities.ts`; project discussions do
  not offer it.
- Members can open Stripe Customer Portal from the popup. In-app cancellation
  stops only the next renewal, preserves access through the paid period, appears
  in the badge, and can be reversed before that period ends.
- A full-price-for-the-whole-term discount is a voucher: atomically consume it,
  create membership immediately, ask for no card, and create no Stripe renewal.
  A limited-month discount still opens checkout because the regular price returns.
  Stripe webhooks update completed, cancelled, failed, and late payments. With no
  Stripe key, hide membership; with test keys, identify the test payment gate.
- Community projects use a URL-first metadata wizard. Ordinary submissions await
  moderation; trusted members and moderators are approved immediately. Pending
  projects remain visible to their author and moderators.
- A live workshop room has countdown, YouTube stage, reactions, watching count,
  moderated chat, timed materials, and attached poll aggregates. An open-ended
  term runs until its recorded end; its stage does not end automatically. Admins
  can select, replace, clear, or create the displayed comment through the same
  private realtime channel used by reactions.
- A term can be about a project. The connection is one value — GitHub repository,
  the default branch, and one or more branch patterns such as `main`, `client-*`,
  `feature/*`, or `*` for all branches, plus an optional deployment URL — so it is
  set, changed, and unset at once and neither branch selection nor deployment
  outlives its repository. Room kinds which offer it are named in
  `lib/workshops/workshopKindCapabilities.ts`. The room shows the repository as a
  special material beside its ordinary materials for every participant, with its
  links and newest commits, marking those which arrive while a participant watches;
  a newly found commit is broadcast to active rooms and appears on the stage for
  ten seconds. A selection resolving to multiple branches is shown as a commit
  graph. Commits come from the public commit feed or keyless API, pooled once per
  server and cached through the revalidation window; an unreadable feed still
  leaves the room naming its project.
- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
  GitHub Markdown page. The room renders it beside ordinary materials through the
  shared material card, primary action, and QR code, for every participant without
  making it timed or membership-gated content.
- Paid-only materials are decided on the server in one pass. Members receive
  unlocked material; others receive only the published titles as an offer. An
  untitled item is not named, items are not named before their unlock time, and a
  room without membership hides paid-only items without naming them.
- After a workshop ends, its recording is server-gated to members. Others receive
  the published teaser, or a generic offer when no teaser exists; a term without
  a recording offers nothing.
- Trusted participants remain invisible and their messages are auto-approved.
  Moderators see pending messages, can approve/reject/correct/pin them, and can
  trust or silence authors. Workshop and community moderators are appointed in
  `/admin/workshops`; project authors moderate their own discussions.
- The room records whether each open browser is actively or passively attended
  from pointer, typing, scroll, and touch activity. Admin analytics distinguish
  active computer users from merely open tabs with dashed audience lines.
- The community's compact gallery shows its five highest-upvoted approved projects
  and links to the full gallery. Community landing content is read from the live
  room: expose only anonymous totals, approved messages, and shared projects;
  show member first names only. If the room cannot be read, show less content,
  not an error. Landing-page reactions are estimates based on recent room data.

#### Administration and data rules

- Event kinds are defined once in `lib/events/eventTypes.ts`; adding one should
  not require database migration or page-specific duplication. Terms ask for kind,
  online/place format, price, and capacity. Each kind also names where its landing
  page records registrations, which is the `placeName` of the contacts it gathers.
- Which term a registration belongs to is decided once, in
  `lib/workshops/workshopRegistrations.ts`. A term is recognised by its slug, by
  its Prague day, and by the moment it begins at, so registrations written before
  terms had slugs keep counting. Registration forms write the term through the
  same line prefixes that rule reads back. Contacts are still gathered, recorded
  and shown only by `/admin/contacts`; counting reads nothing but their notes.
  The registration actions in workshop administration carry that same term filter
  into the contacts list and its exports.
- Workshop polls are read-only in workshop administration. Creating a new event
  publishes it by default; duplicating an event keeps it published unless the
  source is unpublished. Duplicating a workshop preserves its connections to
  the already existing attached polls; it creates no polls, options, votes, or room history. Stage settings contain
  the live stream and recording teaser, presentation settings the public presentation
  address, and project settings the repository the term is about, written as an
  address or as `owner/name`, together with its branch patterns. Deleting a workshop is a soft deletion: its room
  data and attached
  community polls stay stored, but the term leaves normal public and administrative
  lists and no longer holds its slug for a replacement. An end may be empty;
  admins can record, adjust, clear, and reopen it. Overview analytics are zoomable and share their
  room, section, lines, reaction, zoom, and keyword metrics through query params.
- Community administration is the workshop dashboard restricted to the
  `community` room kind: no room picker, schedule, stage, reactions, or address.
  Its membership view filters Stripe lifecycle, prices, discounts, identifiers,
  dates, test/live mode, and links to the exact Stripe checkout/subscription.
  Community analytics cover all measured time and member details show seen times.
- Shortener search, provenance filters, sorting, and selected click history live
  in GET parameters. Editing changes only shortcode, destinations, note, and
  landing page; deletion also deletes recorded clicks.

#### Database and verification

- Put database changes in `migrations/*.sql`. Startup and
  `npm run migrate-database` run `_initialize.sql`, then migrations in filename
  order, recording immutable filenames and checksums in `public."Migration"`.
  Changed or missing migrations fail, as do queries requiring an unapplied one.
- `npm run test-types` must build before `tsc` to refresh `.next/types`.
- `npm run test-e2e` uses the Next.js development server; first-request compile
  headroom belongs in `playwright.config.ts`, not individual tests. E2E tests are
  independent and each archives one recording in `tests/e2e/videos/`, retaining
  only recent runs.

CODEX_PROMPT

--- raw output ---
.env: line 27: app: command not found
ptbk-codex-login-method: chatgpt
Reading prompt from stdin...
OpenAI Codex v0.154.0
--------
workdir: C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: max
reasoning summaries: none
session id: 01a0a911-0d13-7830-90ae-ee311708b6eb
--------
user

## Your Task

After the workshop has ended, allow to download a wrap-up PDF 

- The wrap-up PDF should include a summary of the workshop, key takeaways, and any materials shared during the session.
- Do not add anything into the admin or database. The wrap-up PDF should be generated from information already there. 
- You are working with page `/cs/online-workshop/participant?workshop=`
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

## Your Behavior

You are Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

## Context

### Context

This repository contains Promptbook landing pages for different businesses,
use cases, and audiences. Keep these rules current when behavior changes.

#### Public routes

- `/` redirects to `/cs` or `/en` using `Accept-Language`.
- `/cs` is the Czech homepage and source of truth for homepage structure and copy.
- `/en` is its English localization.
- `/pro-mesta`, `/pro-firmy`, `/for-agro`, `/for-industry`, `/ai-supervize`,
  `/hackathon-factory`, and `/pavol` are specialized landing pages. `/pavol`
  redirects to `/cs/pavol` or `/en/pavol`; the localized pages are Pavol Hejny's
  personal pages.
- `/ai-supervize-mini` is the Czech one-day AI Supervize page. Published terms,
  prices, capacities, places, FAQs, registration, and participant information
  come from `/admin/workshops`; with no published term it shows a notice.
  `/skoleni` redirects there.
- `/cs/online-workshop` lists free 60-minute online workshops about writing
  production code with AI agents. Each term has its own subject and description,
  but all use one registration form. `/cs/online-workshop/dekujeme` is the
  full-load conversion page; `/participant` is the live room. Its waiting room
  offers every published term as the same term cards the landing page registers
  with: running and upcoming ones first, finished ones behind a disclosure.
  Picking one changes the room being connected to and the `workshop` parameter,
  without losing the name and e-mail already typed.
- `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
  merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
  the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
  `application/rss+xml`.
  Each episode lists every credited person by name, and every person any source
  credits has a card in `aiTaKrajtaPeople.ts`, which a test keeps in step, so a
  credited person never misses their portrait and the filter cannot lose them.
  It keeps exact episode counts but labels subscriptions and listening hours as
  estimates. The fixed mini-player, newest-episode header button, person/search/
  episode/play/archive/collaboration filters, and section hash are shareable as
  query/hash state; the snake game is local state. Person clicks filter episodes.
  Everyone in the roster shows a normalized, transparent PNG portrait. The source
  crop is recorded by `scripts/_cutAiTaKrajtaPeoplePortraits.mjs`, from the
  episode cover the show itself published them on or from the picture they publish
  of themselves when no cover carries them; the final alpha PNG is named by the
  roster and a test verifies its format and transparent pixels. Portrait frames
  supply subtle, stable neutral-gradient variations and a restrained hover cue,
  so photographed backgrounds never compete with the page. The one people list
  is drawn anew
  for every visit, weighted by how many episodes of the archive name each
  person, so somebody heard often comes up high far more often than someone in
  one díl without the list ever becoming a ranking. The draw is local state and
  is made in the browser; the page itself is built in the order the draw leans
  towards, from the most often named person to the least often named one, so the
  browser hydrates into the list it was sent.
  Collaboration submissions use `/admin/contacts`. Its tab icon is the page's
  own snake drawing in `/ai-ta-krajta/logo.svg` and `.png`; SVG corners are
  rounded and transparent, while the raster fills its square. That drawing is
  traced off the cover artwork of the show and recorded once in
  `businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.ts`, together with the
  measurements of the animal along its own length. The snake of the minigame
  starts in exactly that shape, in the frame the still logo occupied, and only
  then eases into the proportions, colours and speed of a game snake.
  A seven-cell Promptbook coder terminal floats in the bottom-right corner and
  clears the player and cookie controls. It types `$ ptbk`, then animates an
  ASCII octopus in response to pointer, focus, scrolling, and the snake terrarium.
  It stays still for reduced motion and pauses its clock in hidden tabs. The
  badge and the shared footer credit link to `https://coder.ptbk.io/`.
- `/ai-ta-krajta/media-kit` and `/ai-ta-krajta/branding` are the two pages beside
  the podcast. They are named once, in `AI_TA_KRAJTA_SUBPAGES`, which the footer
  lists and which they point at each other through, so no link can name a page
  differently than the page names itself. Both wear the same compact header,
  section heading and footer as each other. The brand kit hands out the logo the
  site already serves at `/ai-ta-krajta/logo.svg`, `.png` and the cover artwork,
  publishes no colour outside `AI_TA_KRAJTA_COLORS`, and says how the name is
  written; it asks for nothing and sends visitors to the media-kit form.
- `/cs/komunita` is the permanent Czech community room. It has chat, polls,
  projects, materials, and published terms, but no schedule, stage, or live
  updates. Terms show event kind, format/place, price, and status. A term with a
  live room links there; otherwise it links to its landing page. The calendar
  opens on the member's month, can filter by day, and uses the same terms and
  statuses as the cards. Empty days cannot be selected; an empty month is only
  selected when the member's month has no terms. The room offers Google Calendar
  and `webcal:` subscriptions.
- `/cs/komunita/projects` is the full project gallery, ordered by upvotes.
  `/cs/komunita/projects/<project_id>` shows scraped project details and a
  moderated discussion. Project-room sessions derive from the community session;
  the author moderates their own discussion.
- `/cs/komunita/clenstvi` offers free community access and the 199 Kč monthly
  membership. Webinars remain free; paid access adds recordings, archive,
  materials, extra content, priority questions, and Discord/community features.
  The page is prefilled from `fullname` and `email`, supports community discount
  codes, has no new annual plan or trial, and allows cancellation.
- `/cs/komunita/calendar.ics` publishes the same terms by stable slug, so moved
  or renamed terms update existing subscriptions. It contains only event terms
  and public destinations, never subscriber identity.
- `/admin/workshops` manages terms of every event kind, participants, comments,
  reactions, content, attached community polls, and settings. Every term says both
  of its audiences: the people registered on the landing page of its event, and
  the people who entered its room. A room which is no term of an event says
  nothing about registrations at all. The registration block opens the exact
  filtered `/admin/contacts` list and its shared CSV, vCard, and Book exports;
  it never creates a second contact table or serializer in workshop administration.
- `/admin/community` manages the permanent community, including polls, project
  moderation, participants, memberships, payments, and room analytics.
- `/admin/shortener` manages public short links, QR/UTM output, destinations,
  notes, search/filter/sort state, and private click history. Links are served
  by `/[shortcode]`; `/shortener` redirects to the admin page.
- `/admin/login` authenticates the single `admin` account with `ADMIN_PASSWORD`
  and a signed session cookie. All `/admin/*` pages and APIs require
  `requireAdminSignedIn`. `/admin` is the post-login dashboard.
- Other public/legal routes include `/contact`, `/data-deletion`, `/privacy`,
  `/terms`, `/dekujeme`, `/branding`, and the routes under `/k`, `/old`, and
  `/test`.

#### Shared community and workshop behavior

- Rooms lead to each other in both directions, through one identity hand-off. The
  community lists the terms and opens the room of each; an invited workshop room
  places its community link beside its materials as a special material for every
  participant, regardless of paid membership. Where a special material sits is
  decided once, in `lib/workshops/workshopSpecialMaterials.ts`: a card says where
  among the ordinary materials it belongs, and the invitation opens the material
  list of a participant who does not pay while it closes the list of a paying
  member. A membership which is not loaded yet leaves it where a participant who
  does not pay reads it. Both carry the connected member's
  name and email on, and an incomplete identity carries nothing rather than half
  of it. Which rooms invite is answered by
  `lib/workshops/workshopKindCapabilities.ts`; the community and project
  discussions do not. The invitation names the community's sections by the names
  the community itself uses.
- Community polls attached to workshops are shared. A normalized email gives a
  member one vote across the community and all attached workshops. Workshops
  may display and accept votes, but the community owns administration.
- The participant room owns the membership badge and popup. Community and live
  workshop rooms use the same membership for the connecting email, and checkout
  returns to the room where it started. Membership is offered only by room kinds
  listed in `lib/workshops/workshopKindCapabilities.ts`; project discussions do
  not offer it.
- Members can open Stripe Customer Portal from the popup. In-app cancellation
  stops only the next renewal, preserves access through the paid period, appears
  in the badge, and can be reversed before that period ends.
- A full-price-for-the-whole-term discount is a voucher: atomically consume it,
  create membership immediately, ask for no card, and create no Stripe renewal.
  A limited-month discount still opens checkout because the regular price returns.
  Stripe webhooks update completed, cancelled, failed, and late payments. With no
  Stripe key, hide membership; with test keys, identify the test payment gate.
- Community projects use a URL-first metadata wizard. Ordinary submissions await
  moderation; trusted members and moderators are approved immediately. Pending
  projects remain visible to their author and moderators.
- A live workshop room has countdown, YouTube stage, reactions, watching count,
  moderated chat, timed materials, and attached poll aggregates. An open-ended
  term runs until its recorded end; its stage does not end automatically. Admins
  can select, replace, clear, or create the displayed comment through the same
  private realtime channel used by reactions.
- A term can be about a project. The connection is one value — GitHub repository,
  the default branch, and one or more branch patterns such as `main`, `client-*`,
  `feature/*`, or `*` for all branches, plus an optional deployment URL — so it is
  set, changed, and unset at once and neither branch selection nor deployment
  outlives its repository. Room kinds which offer it are named in
  `lib/workshops/workshopKindCapabilities.ts`. The room shows the repository as a
  special material beside its ordinary materials for every participant, with its
  links and newest commits, marking those which arrive while a participant watches;
  a newly found commit is broadcast to active rooms and appears on the stage for
  ten seconds. A selection resolving to multiple branches is shown as a commit
  graph. Commits come from the public commit feed or keyless API, pooled once per
  server and cached through the revalidation window; an unreadable feed still
  leaves the room naming its project.
- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
  GitHub Markdown page. The room renders it beside ordinary materials through the
  shared material card, primary action, and QR code, for every participant without
  making it timed or membership-gated content.
- Paid-only materials are decided on the server in one pass. Members receive
  unlocked material; others receive only the published titles as an offer. An
  untitled item is not named, items are not named before their unlock time, and a
  room without membership hides paid-only items without naming them.
- After a workshop ends, its recording is server-gated to members. Others receive
  the published teaser, or a generic offer when no teaser exists; a term without
  a recording offers nothing.
- Trusted participants remain invisible and their messages are auto-approved.
  Moderators see pending messages, can approve/reject/correct/pin them, and can
  trust or silence authors. Workshop and community moderators are appointed in
  `/admin/workshops`; project authors moderate their own discussions.
- The room records whether each open browser is actively or passively attended
  from pointer, typing, scroll, and touch activity. Admin analytics distinguish
  active computer users from merely open tabs with dashed audience lines.
- The community's compact gallery shows its five highest-upvoted approved projects
  and links to the full gallery. Community landing content is read from the live
  room: expose only anonymous totals, approved messages, and shared projects;
  show member first names only. If the room cannot be read, show less content,
  not an error. Landing-page reactions are estimates based on recent room data.

#### Administration and data rules

- Event kinds are defined once in `lib/events/eventTypes.ts`; adding one should
  not require database migration or page-specific duplication. Terms ask for kind,
  online/place format, price, and capacity. Each kind also names where its landing
  page records registrations, which is the `placeName` of the contacts it gathers.
- Which term a registration belongs to is decided once, in
  `lib/workshops/workshopRegistrations.ts`. A term is recognised by its slug, by
  its Prague day, and by the moment it begins at, so registrations written before
  terms had slugs keep counting. Registration forms write the term through the
  same line prefixes that rule reads back. Contacts are still gathered, recorded
  and shown only by `/admin/contacts`; counting reads nothing but their notes.
  The registration actions in workshop administration carry that same term filter
  into the contacts list and its exports.
- Workshop polls are read-only in workshop administration. Creating a new event
  publishes it by default; duplicating an event keeps it published unless the
  source is unpublished. Duplicating a workshop preserves its connections to
  the already existing attached polls; it creates no polls, options, votes, or room history. Stage settings contain
  the live stream and recording teaser, presentation settings the public presentation
  address, and project settings the repository the term is about, written as an
  address or as `owner/name`, together with its branch patterns. Deleting a workshop is a soft deletion: its room
  data and attached
  community polls stay stored, but the term leaves normal public and administrative
  lists and no longer holds its slug for a replacement. An end may be empty;
  admins can record, adjust, clear, and reopen it. Overview analytics are zoomable and share their
  room, section, lines, reaction, zoom, and keyword metrics through query params.
- Community administration is the workshop dashboard restricted to the
  `community` room kind: no room picker, schedule, stage, reactions, or address.
  Its membership view filters Stripe lifecycle, prices, discounts, identifiers,
  dates, test/live mode, and links to the exact Stripe checkout/subscription.
  Community analytics cover all measured time and member details show seen times.
- Shortener search, provenance filters, sorting, and selected click history live
  in GET parameters. Editing changes only shortcode, destinations, note, and
  landing page; deletion also deletes recorded clicks.

#### Database and verification

- Put database changes in `migrations/*.sql`. Startup and
  `npm run migrate-database` run `_initialize.sql`, then migrations in filename
  order, recording immutable filenames and checksums in `public."Migration"`.
  Changed or missing migrations fail, as do queries requiring an unapplied one.
- `npm run test-types` must build before `tsc` to refresh `.next/types`.
- `npm run test-e2e` uses the Next.js development server; first-request compile
  headroom belongs in `playwright.config.ts`, not individual tests. E2E tests are
  independent and each archives one recording in `tests/e2e/videos/`, retaining
  only recent runs.


codex
I’ll first trace how the participant room determines that a workshop has ended and how it already exposes materials, then I’ll add a reusable PDF export based only on that existing data and document it in the changelog.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Location; rg --files -g AGENTS.md -g '"'!node_modules'"' -g '"'!'"\\.next' .; git status --short; rg -n --glob '"'!node_modules'"' --glob '"'!'"\\.next' \"online-workshop|participant|WorkshopParticipant|wrap-up|wrap up|materials\" app components lib tests changelog 2>"'$null' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 505ms:

.\AGENTS.md
 M prompts/2026-09-0240-wrap-up-pdf.md
changelog\_current-preversion.md:4:  ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
changelog\_current-preversion.md:8:- Placed the community material of `/cs/online-workshop/participant?workshop=` by what the member reading it has: a
changelog\_current-preversion.md:9:  participant who does not pay for the membership is invited into the community before the materials of the workshop,
changelog\_current-preversion.md:12:  card which shares the material list now says where among the ordinary materials it belongs, so one rule places them
changelog\_current-preversion.md:16:  Markdown page, and `/cs/online-workshop/participant?workshop=` shows it beside ordinary materials with the same
changelog\_current-preversion.md:18:  to free and paid participants alike without becoming timed or membership-gated content.
changelog\_current-preversion.md:20:- Moved a connected GitHub repository in `/cs/online-workshop/participant` into the materials list as a special
changelog\_current-preversion.md:21:  material. Its existing repository, deployment, and live-commit links now stay with ordinary workshop materials,
changelog\_current-preversion.md:23:  participants without turning the repository into timed or membership-gated content.
changelog\_current-preversion.md:25:- Moved the community hand-off in `/cs/online-workshop/participant` into the materials list as a dedicated
changelog\_current-preversion.md:27:  available to free and paid members alike, while carrying the participant identity through the existing shared
changelog\_current-preversion.md:29:  ordinary materials without becoming timed or membership-gated workshop content.
changelog\_current-preversion.md:36:- Let `/admin/workshops` reopen a workshop by clearing its recorded end, and give an open-ended workshop quick choices to end now, one hour after its start, or two hours after its start; each choice uses the existing live update, so `/cs/online-workshop/participant` immediately keeps or replaces the stream accordingly.
changelog\_current-preversion.md:52:  the community or scrolling past its materials. A return from Stripe opens that modal as well, preserving the clear
changelog\_current-preversion.md:61:  participant history, so the two administration views stay directly connected without pretending one room session is
changelog\_current-preversion.md:64:- Show every visible community poll attached to an occurrence inside that occurrence's participant room, including its
changelog\_current-preversion.md:98:  view, yet keeps the native scrollbar as a fallback. The participant, contact, discount-code and short-link tables
changelog\_current-preversion.md:99:  all use it, and their useful identity column — the participant or contact name, a discount code, or a shortcode —
changelog\_current-preversion.md:101:- Said on every term of `/cs/online-workshop` which workshop it is. Each card of the picker now names the workshop
changelog\_current-preversion.md:141:- Replaced the stack of repeated registration forms on `/cs/online-workshop` with one clear term picker and one shared
changelog\_current-preversion.md:148:  question which has not been rejected can now be sent above the stream of `/cs/online-workshop/participant`, replaced
changelog\_current-preversion.md:157:  `/cs/online-workshop/participant` now watches whether somebody is at their computer or phone at all — whether they
changelog\_current-preversion.md:170:  participant is listed with is now named `Čas v místnosti` throughout the administration and its exports, because it
changelog\_current-preversion.md:174:  as long as it takes: `/cs/online-workshop/participant` keeps its stream on the stage and its room open indefinitely
changelog\_current-preversion.md:177:  the room replaces the stage with the closing wrap-up and its feedback the moment the workshop really ends, and the
changelog\_current-preversion.md:191:  the schedule, the capacity and the price, the registration form and the participant information page all read the
changelog\_current-preversion.md:205:  existing membership page with the benefits of the paid membership, while the shared participant-room header now
changelog\_current-preversion.md:210:  webinar recordings and their archive, practical materials, additional content, priority questions and the existing
changelog\_current-preversion.md:242:  the workshop links and above the materials, ordered by upvotes. Its home view shows the five strongest projects and a
changelog\_current-preversion.md:262:- Let moderators and administration-created artificial messages of `/cs/online-workshop/participant` and `/cs/komunita` carry active links while keeping every normal participant's URLs inert text. Eligible links now follow the same persisted ad hoc `ptbk.io` shortener path as materials, including their UTM source record and shortener redirect click measurement; the safe chat renderer opens only those already-shortened URLs in a new tab, so a raw destination cannot become an untracked active link.
changelog\_current-preversion.md:270:- Put a branded QR code beside every linked material on the desktop layouts of `/cs/online-workshop/participant` and `/cs/komunita`. Each code carries the persisted public short URL prepared when `/admin/workshops` saves the material, so scanning it on a phone follows the exact same redirect and click tracking as opening, copying, or forwarding the material link; mobile layouts keep the panel out of the way. The shortener and the shared participant room now both use one client-only Promptbook QR renderer instead of loading separate implementations.
changelog\_current-preversion.md:272:- Routed every HTTP(S) material link of `/cs/online-workshop/participant` and `/cs/komunita` through a persisted ad hoc `ptbk.io` short link, including ordinary Markdown, HTML, reference, autolink and bare-URL forms. The participant room no longer posts browser click events: opening, copying or forwarding the same short address is measured only by the shortener redirect, and `/admin/workshops` now derives material totals and its activity graph from those redirect records. Per-participant material-click figures were removed because a shareable public link cannot honestly identify the workshop participant who opened it. The shortener now records whether a link was manually created or ad hoc and which application created it, and `/admin/shortener` can display, filter and sort by both properties.
changelog\_current-preversion.md:274:- Made `/cs/online-workshop/participant` survive a temporary overloaded or unavailable backend after it has been opened once: every successful room snapshot is now kept in the browser under its own workshop slug, for no longer than the participant session, and a failed refresh leaves the stream, countdown, chat and materials on the last known version instead of replacing the room with an error. A small shared status dot in the upper-right corner now quietly changes from green “Připojeno” to amber while the saved version is shown, keeps the detailed explanation in its tooltip and accessible label, and lets a participant retry immediately; it also serves `/cs/komunita` through the common participant room. The room keeps trying with a jittered exponential backoff rather than sending a whole audience back every half minute, and pauses nonessential presence reports until the service answers again; an authoritative missing-room or missing-session answer removes the private saved copy. The `state-changed` broadcasts already sent for every content and setting change in `/admin/workshops` make the next successful refresh replace that copy, so no second cache or migration is needed there.
changelog\_current-preversion.md:276:- Added a small service worker for the online-workshop funnel: the landing page, confirmation and room prepare the already loaded Next.js assets, and every canonical participant document (`?workshop=…`) is saved network-first under that exact workshop selection. When the application server answers with an overload error or cannot be reached, a visitor who has opened that term before receives their local application shell and its matching room snapshot; prefilled e-mail/name URLs and legacy links without a selected workshop are deliberately never reused as a cached page.
changelog\_current-preversion.md:278:- Added the third, post-workshop stage to `/cs/online-workshop/participant`: when an occurrence ends, the live video is replaced with a thank-you wrap-up, a progressively saved star review (low scores ask what to improve first; high scores ask what helped first), and a direct link to the chosen follow-up material. Chat and reactions remain available throughout. One ordinary Markdown material can now be marked as the follow-up; it is highlighted in the material list before the end and is enforced as a single selection per workshop in the database.
changelog\_current-preversion.md:282:- Verified `npm run test-for-ptbk-coder` locally: ESLint, TypeScript, the production build, and the public Playwright suite complete successfully. The two published-online-workshop scenarios remain intentionally skipped when no published workshop is available, while the other nine public scenarios pass.
changelog\_current-preversion.md:284:- Added Playwright end-to-end coverage for the public site without touching `/admin`: the smoke test opens the public landing and information routes, while real `@example.com` submissions cover the footer newsletter, generic and business lead dialogs, qualification flow, podcast subscription, Pavol contact form, online workshop registration, AI Supervize Mini registration and its future-term interest form. The public workshop-room connection is covered too when a published room is available. `npm run test-e2e` runs headlessly, `npm run test-e2e:headed` opens the browser, both terminal shortcuts are available, and every run archives timestamped `.webm` recordings under the git-ignored `tests/e2e/videos/` directory. `npm run delete-test-data` is the separate, deliberate garbage collector: it removes `@example.com` Contact and workshop-participant records through `DATABASE_URL`, letting database cascades remove their dependent workshop activity without deleting data at the end of a test run.
changelog\_current-preversion.md:294:- Replaced the stack of coloured bars in the "Přehled" of `/admin/workshops` and `/admin/community` with a real graph of the workshop in time: the audience is drawn across the hour the way a video draws its viewers, next to the messages, reactions, votes and clicks on the materials, all against one axis so that no two scales can invent a relation which is not in the data. Hovering anywhere reads out every line at that moment, dragging over the graph zooms into that span and the wheel zooms back out, and the graph opens exactly on the time of the workshop, from where it can be opened up to everything which happened before it started and after it ended.
changelog\_current-preversion.md:299:- Started remembering who was watching a live room and when, as one row per participant and minute of the presence the room already reports, which is what the audience of the graph is counted from. A workshop which was held before this says so instead of drawing a room nobody watched.
changelog\_current-preversion.md:300:- Made the workshop links of `/cs/komunita` carry the member who clicks them: a room now tells the participant reading it their own e-mail address, so every link it offers arrives at `/cs/online-workshop/participant` with `email` and `fullname` filled in and the workshop welcomes a community member without asking them to type either again. Until now only somebody who had just typed their e-mail into the connection form was handed on, and a member returning to the community on nothing but their session cookie was sent to an empty form — which the calendar invitation of a workshop room, built from the very same identity, was sending to an empty form as well.
changelog\_current-preversion.md:301:- Took the subtitles off the stream of `/cs/online-workshop/participant`: asking the embed address not to turn them on was never enough for a participant whose own YouTube account keeps them on, or for a live broadcast which writes them by itself, so the room now unloads the subtitle module of the player, and keeps asking for the first few seconds because a player which has just appeared on the page is not listening yet.
changelog\_current-preversion.md:302:- Fixed the participants of `/admin/workshops` and `/admin/community` still answering `column reference "fullname" is ambiguous` instead of listing anybody, which took their CSV and vCard exports down with them again: naming the row source of every ordering column mended the migration files but not a database which still held an older body of the paging function, so that function now settles the conflict for the whole of itself at once and drops every signature it ever had, leaving no older copy of it to be called instead.
changelog\_current-preversion.md:304:- Gave the live rooms moderators next to their invisible trusted participants: a moderator of `/cs/online-workshop/participant` or `/cs/komunita` is announced by a badge on their messages and in the header of the room, is shown every message which waits for a decision instead of only their own, and approves, rejects, corrects, or pins any message straight from the chat, while a trusted participant stays invisible with nothing but their own messages being approved as they write them.
changelog\_current-preversion.md:305:- Let that moderator also trust the author of a message or take their interactions away, from the very same message, so a helpful participant stops waiting for moderation and a disruptive one stops reaching the room without anybody leaving the chat.
changelog\_current-preversion.md:306:- Kept the appointment of a moderator with `/admin/workshops` alone, where an administrator makes and unmakes both trusted participants and moderators, filters the audience by either of them, and exports who moderates; a moderator of a room can hand on trust but never their own moderation, and cannot reach a fellow moderator whom a ban would dismiss, which both the room and the administration read from one description of what each moderating role may do.
changelog\_current-preversion.md:311:- Fixed the participants of `/admin/workshops` answering `column reference "fullname" is ambiguous` instead of listing anybody, which took their CSV and vCard exports down with them: the paging function now says which row source each of its ordering columns comes from, so the database no longer has to choose between a column and the returned value of the very same name.
changelog\_current-preversion.md:312:- Made `/cs/komunita` the permanent room it is: no stage, no countdown towards a start it never had, no participant reactions and no watching count, and no realtime channel or fast poll behind them, so the community catches up calmly instead of pretending to be a live occurrence.
changelog\_current-preversion.md:314:- Described every room kind in one place — whether it is the only one of its kind, keeps a fixed URL, has a schedule, a stage, and live updates — so the participant room, its administration, the routes, and the data loading all ask the same question instead of naming the community, and a room which does not show a live panel neither counts nor broadcasts for it.
changelog\_current-preversion.md:315:- Replaced the workshop selector of `/admin/workshops` with a card for every occurrence, saying when the term is, whether it is upcoming, ongoing or already over, and how many participants it gathered; a running room leads the list, the prepared terms follow it, and the history closes it.
changelog\_current-preversion.md:316:- Generalized discount codes from an AI Supervize Mini-only feature into a shared, RLS-protected database-backed system: `/admin/discount-codes` can now make a code valid everywhere or only in selected offer places, optionally limit its total uses, and generate `?code=...` links which prefill and scroll to the matching registration form; previews show remaining uses and registration consumes limited codes atomically, while the old single online-workshop follow-up selection was removed.
changelog\_current-preversion.md:317:- Added the Czech-only `/cs/komunita` member room with the workshop waiting-room flow, GET-prefilled name and e-mail, moderated chat, timed materials, and identity-prefilled links to every published workshop.
changelog\_current-preversion.md:318:- Added `/admin/community`, reusing the workshop moderation, participants, content, analytics, and settings dashboard while keeping its stable community URL read-only.
changelog\_current-preversion.md:320:- Extended the live-room data model with a database-enforced singleton community kind, keeping it separate from workshop terms and their public/admin lists without duplicating the secure participant-room infrastructure.
changelog\_current-preversion.md:321:- Rebuilt `/admin/workshops` around focused overview, participant, comment, reaction, content, and settings sections; large audiences now have server-side filtering, sorting, paging, per-person activity histories, a workshop-wide activity timeline, and CSV exports for every section plus filtered participant vCards.
changelog\_current-preversion.md:322:- Made `/cs/online-workshop` load and present every upcoming published workshop from the shared workshop data, with a separate registration form and selected-term contact note for each date instead of one hardcoded occurrence.
changelog\_current-preversion.md:323:- Carried the selected workshop as `?workshop=<slug>` through registration confirmation, direct room links, and calendar invitations; `/cs/online-workshop/participant` and `/cs/online-workshop/dekujeme` now resolve it, while old links without it use the most recent published workshop.
changelog\_current-preversion.md:332:- Generated on-brand sharing preview images for every landing page (`/cs`, `/en`, `/pro-mesta`, `/for-agro`, `/for-industry`, `/ai-supervize`, `/ai-supervize-mini`, `/cs/online-workshop`, `/hackathon-factory`, `/cs/pavol`, `/en/pavol`) from a derived palette, and dropped the duplicated `twitter-image` routes.
changelog\_current-preversion.md:337:- Moved the online workshop registration confirmation onto its own `/cs/online-workshop/dekujeme` url reached by a full page load, so the Meta Pixel reports a `PageView` of it and an ad campaign can optimize on real registrations instead of clicks.
changelog\_current-preversion.md:340:- Joined the private contact and workshop-participant views by normalized e-mail, including case-insensitive and `+tag` aliases: `/admin/contacts` now groups duplicate Contact rows and shows workshop attendance, while workshop participant lists, timelines, CSV exports, and vCards include the matching Contact details such as phone numbers without changing stored data.
changelog\_current-preversion.md:346:- Added the reusable `/cs/online-workshop/participant` live room with participant sessions, a server-synchronized countdown and YouTube stream, timed Markdown materials, moderated chat, upvotes, animated reactions, an RLS-secured Supabase model, and workshop administration from the new `/admin` dashboard.
changelog\_current-preversion.md:347:- Offered the workshop to the calendar of a participant from the `/cs/online-workshop/participant` room as long as it has not started, as a `Karel <> Pavol - Produkční kód s AI agenty` event built from the live workshop details and carrying a prefilled link back into the room, and moved the registration confirmation onto the very same shared calendar code.
changelog\_current-preversion.md:348:- Let a participant of the `/cs/online-workshop/participant` room rename themselves straight from the room header, which also renames the author of the comments they already wrote, and validates the new name by the very same rule as the connection form.
changelog\_current-preversion.md:349:- Let a participant of the `/cs/online-workshop/participant` room answer a message of the chat, so a question and its answers are read as one conversation which the newest-first order lifts by its newest answer, while an answer is moderated, voted, and rate limited exactly like any other message and the moderation of it shows the question it answers.
changelog\_current-preversion.md:350:- Gave every reaction of the `/cs/online-workshop/participant` room a celebration of its own: a thumbs-up hops, a heart beats its way up an S-curve, applause claps just above the edge, fire flickers up a draught, an idea glows and drifts, laughter rolls away, `</>` climbs line by line behind a blinking cursor, sparkles twinkle, a snake slithers, eyes dart around, a party popper throws confetti and a firework explodes, while any other emoji still flies with the animation the room had before and any text an admin sends flies as a readable chip instead of a huge word.
changelog\_current-preversion.md:351:- Composed those animations in one registry out of a flight, a flourish and a decoration, which the stylesheet of the room answers with keyframes the browser runs on the compositor, so a new reaction is one entry and a full stage costs no JavaScript, keeps at most two dozen reactions in the air however many participants react at once, and stops travelling altogether for anyone who asked the system for less motion.
changelog\_current-preversion.md:353:- Kept every participant action in the online workshop attributable to its server-side timestamp: joining the room, comments, likes, reactions, and material-link clicks are recorded for the administration without duplicating the existing event records.
tests\e2e\public-submissions.spec.ts:96:test('submits a published online-workshop registration', async ({ page }) => {
tests\e2e\public-submissions.spec.ts:97:    await page.goto('/cs/online-workshop');
tests\e2e\public-submissions.spec.ts:105:    await registrationForm.locator('input[name="email"]').fill(createE2eTestEmail('online-workshop-registration'));
tests\e2e\public-submissions.spec.ts:108:    const thankYouNavigation = page.waitForURL(/\/cs\/online-workshop\/dekujeme/);
tests\e2e\public-submissions.spec.ts:113:    await expect(page).toHaveURL(/\/cs\/online-workshop\/dekujeme/);
tests\e2e\public-submissions.spec.ts:280:test('connects a public online-workshop participant', async ({ page }) => {
tests\e2e\public-submissions.spec.ts:281:    await page.goto('/cs/online-workshop/participant');
tests\e2e\public-submissions.spec.ts:283:    const fullname = page.locator('#participant-fullname');
tests\e2e\public-submissions.spec.ts:287:            'The configured database has no published workshop room available for participant registration.',
tests\e2e\public-submissions.spec.ts:293:    await page.locator('#participant-email').fill(createE2eTestEmail('workshop-participant'));
tests\e2e\public-pages.spec.ts:32:    '/cs/online-workshop',
lib\community-membership\communityMembershipTypes.ts:79:    readonly participantId: string;
lib\community-membership\communityMembershipCheckout.test.ts:15:    participantId: '00000000-0000-0000-0000-000000000001',
lib\community-membership\communityMembershipCheckout.test.ts:128:const WORKSHOP_ROOM_URL = 'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty';
lib\community-membership\communityMembershipCheckout.test.ts:142:            'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty' +
lib\community-membership\communityMembershipCheckout.test.ts:146:            'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty&membership=cancelled',
lib\community-membership\communityMembershipAdminQuery.ts:103: * administration, so this list never competes with the participant list or overview graph for a shared-link value.
app\sitemap.test.ts:11:        expect(entries.map((entry) => entry.url)).not.toContain('https://ptbk.io/cs/online-workshop/participant');
lib\github\githubCommitApi.ts:60: * Reads the small part of one GitHub REST commit response which the participant room can safely show
lib\community-membership\communityMembershipRedemption.ts:74:        requestedByParticipantId: member.participantId,
lib\github\fetchGithubRepository.ts:139: * repository has many active branches. Each URL is still cached by `fetchCachedText`, so participants do not multiply
lib\community-membership\communityMembershipRedemption.test.ts:26:    participantId: '00000000-0000-0000-0000-000000000001',
lib\community-membership\communityMembershipRedemption.test.ts:70:            requestedByParticipantId: MEMBER.participantId,
lib\community-membership\communityMembershipDatabase.ts:211: * Loads all paid-membership states needed by one page of community participants in one query. The membership is keyed
lib\community-membership\communityMembershipDatabase.ts:235:            errorMessage: reportSupabaseError('the membership statuses of community participants', error),
lib\community-membership\communityMembershipDatabase.ts:312:        requested_by_participant_id: values.requestedByParticipantId,
lib\community-membership\communityMembershipCheckout.ts:52:    participantId: 'communityParticipantId',
lib\community-membership\communityMembershipCheckout.ts:101:        [COMMUNITY_MEMBERSHIP_METADATA_KEYS.participantId]: member.participantId,
lib\community-membership\communityMembershipCheckout.ts:163:        client_reference_id: member.participantId,
components\workshops\workshopReactionAnimations.css:11: * workshop with thousands of participants. `will-change` is deliberately not used: a running transform animation is
components\workshops\workshopReactionAnimations.css:890: * The safety net of a participant who asked the operating system for less motion. The room already resolves such a
components\workshops\WorkshopLinksPanel.tsx:8:import type { WorkshopParticipantIdentity } from '@/lib/workshops/workshopParticipantLink';
components\workshops\WorkshopLinksPanel.tsx:34:    readonly participantIdentity: WorkshopParticipantIdentity;
components\workshops\WorkshopLinksPanel.tsx:56: * A list of the terms of every kind of event, which preserves the already verified identity of the participant in each
components\workshops\WorkshopLinksPanel.tsx:65:    participantIdentity,
components\workshops\WorkshopLinksPanel.tsx:78:        () => createEventListings({ workshops, participantIdentity, currentTimeMilliseconds, timeZone }),
components\workshops\WorkshopLinksPanel.tsx:79:        [workshops, participantIdentity, currentTimeMilliseconds, timeZone],
components\workshops\WorkshopLinksPanel.test.tsx:63:            participantIdentity={PARTICIPANT_IDENTITY}
components\workshops\WorkshopLinksPanel.test.tsx:150:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
components\workshops\WorkshopFlyingReaction.tsx:19:     * Whether the participant asked the operating system to move as little as possible
components\workshops\useWorkshopReactionStream.ts:9: * Note: A workshop of thousands of participants sends far more than that, so the oldest reactions make room for the
components\workshops\useWorkshopReactionStream.ts:10: *       newest ones. The stage stays readable and, more importantly, the browser of every single participant animates
components\workshops\useWorkshopReactionStream.ts:47: *       participants clap at once therefore re-renders the stage a few times per second instead of a hundred times.
lib\events\eventTypes.ts:6:} from '@/businesses/online-workshop/config';
lib\events\eventTypes.ts:12:export const EVENT_TYPE_VALUES = ['online-workshop', 'ai-supervize-mini'] as const;
lib\events\eventTypes.ts:48:    readonly participantPath: string | null;
lib\events\eventTypes.ts:58:    'online-workshop': {
lib\events\eventTypes.ts:59:        id: 'online-workshop',
lib\events\eventTypes.ts:63:        participantPath: ONLINE_WORKSHOP_PARTICIPANT_PATH,
lib\events\eventTypes.ts:70:        participantPath: null,
lib\conversations-data.ts:19:    participants: ConversationParticipant[];
lib\events\eventListing.ts:9:import type { WorkshopParticipantIdentity } from '@/lib/workshops/workshopParticipantLink';
lib\events\eventListing.ts:45:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\events\eventListing.ts:66:    participantIdentity,
lib\events\eventListing.ts:71:        const link = createEventLinkOrNull(workshop, participantIdentity);
lib\events\eventListing.test.ts:54:        participantIdentity: PARTICIPANT_IDENTITY,
lib\events\eventListing.test.ts:72:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
lib\events\eventLinks.ts:2:import { createWorkshopRoomLink, type WorkshopParticipantIdentity } from '@/lib/workshops/workshopParticipantLink';
lib\events\eventLinks.ts:8:const ANONYMOUS_WORKSHOP_PARTICIPANT_IDENTITY: WorkshopParticipantIdentity = { email: '', fullname: '' };
lib\events\eventLinks.ts:21:    participantIdentity: WorkshopParticipantIdentity,
lib\events\eventLinks.ts:27:    const { participantPath, landingPagePath } = getEventTypeDefinition(workshop.event.type);
lib\events\eventLinks.ts:29:    return participantPath === null
lib\events\eventLinks.ts:31:        : createWorkshopRoomLink(participantPath, participantIdentity, workshop.slug);
lib\events\event.ts:37:    type: 'online-workshop',
lib\contacts\serializeContactsAsBook.test.ts:21:    url: 'https://ptbk.io/cs/online-workshop',
lib\contacts\serializeContactsAsBook.test.ts:25:    participantId: 'participant-1',
lib\contacts\contacts.test.ts:81:    slug: 'online-workshop-2026-08-26',
lib\contacts\contacts.test.ts:100:    url: 'https://ptbk.io/cs/online-workshop',
lib\contacts\contacts.test.ts:233:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
lib\contacts\contacts.test.ts:238:                userNote: JSON.stringify({ selectedDateId: '2026-08-26', participantCount: 3 }),
lib\contacts\contacts.test.ts:248:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
lib\contacts\contacts.test.ts:253:                userNote: JSON.stringify({ selectedDateId: 'online-workshop-2026-09-02', participantCount: 1 }),
lib\contacts\contacts.test.ts:277:                                participantCount: 1,
lib\contacts\contacts.test.ts:432:            href: 'https://ptbk.io/cs/online-workshop',
lib\contacts\contacts.test.ts:806:        expect(vcard).not.toContain('https://ptbk.io/cs/online-workshop');
app\cs\online-workshop\participant\page.tsx:1:import { ONLINE_WORKSHOP_EVENT_TYPE } from '@/businesses/online-workshop/config';
app\cs\online-workshop\participant\page.tsx:2:import { OnlineWorkshopSelectedTermRoom } from '@/businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom';
app\cs\online-workshop\participant\page.tsx:3:import { ONLINE_WORKSHOP_PARTICIPANT_METADATA } from '@/businesses/online-workshop/onlineWorkshopMetadata';
app\cs\online-workshop\participant\page.tsx:5:    readWorkshopParticipantIdentity,
app\cs\online-workshop\participant\page.tsx:11:type OnlineWorkshopParticipantRouteProps = {
app\cs\online-workshop\participant\page.tsx:22:export default async function OnlineWorkshopParticipantRoute({ searchParams }: OnlineWorkshopParticipantRouteProps) {
app\cs\online-workshop\participant\page.tsx:24:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\online-workshop\participant\page.tsx:44:            initialEmail={participantIdentity.email}
app\cs\online-workshop\participant\page.tsx:45:            initialFullname={participantIdentity.fullname}
lib\community-projects\communityProjectTypes.ts:11: * `pending`, while the shared participant-submission policy may auto-approve a trusted user or a moderator.
lib\community-projects\communityProjectTypes.ts:37: * participant identity; members only need the display name of its author and their own vote. A pending card reaches
app\cs\online-workshop\participant\opengraph-image.tsx:1:import { ONLINE_WORKSHOP_PARTICIPANT_SOCIAL_PREVIEW_OPTIONS } from '@/businesses/online-workshop/onlineWorkshopMetadata';
lib\community-projects\communityProjectService.ts:13:import { getWorkshopParticipantSubmissionStatus } from '@/lib/workshops/workshopParticipantInteraction';
lib\community-projects\communityProjectService.ts:40:    readonly author_community_participant_id: string;
lib\community-projects\communityProjectService.ts:61:    readonly discussion_participant_id: string;
lib\community-projects\communityProjectService.ts:70:    'not-found' | 'member-invalid' | 'community-participant-missing' | 'discussion-participant-invalid';
lib\community-projects\communityProjectService.ts:75:    'key-share': 'FOR KEY SHARE OF community_participant',
lib\community-projects\communityProjectService.ts:76:    update: 'FOR UPDATE OF community_participant',
lib\community-projects\communityProjectService.ts:157:                community_participant.id,
lib\community-projects\communityProjectService.ts:158:                community_participant.fullname,
lib\community-projects\communityProjectService.ts:159:                community_participant.email,
lib\community-projects\communityProjectService.ts:160:                community_participant.is_interaction_banned,
lib\community-projects\communityProjectService.ts:161:                community_participant.is_trusted,
lib\community-projects\communityProjectService.ts:162:                community_participant.is_moderator
lib\community-projects\communityProjectService.ts:163:            FROM ${WORKSHOP_PARTICIPANT_TABLE_REFERENCE} AS community_participant
lib\community-projects\communityProjectService.ts:165:                ON community_room.id = community_participant.workshop_id
lib\community-projects\communityProjectService.ts:166:            WHERE community_participant.id = $1
lib\community-projects\communityProjectService.ts:198:                community_project.author_community_participant_id
lib\community-projects\communityProjectService.ts:265:              AND community_participant_id = $2
lib\community-projects\communityProjectService.ts:288:            WHERE community_project_vote.community_participant_id = $1
lib\community-projects\communityProjectService.ts:305:            SELECT discussion_participant_id
lib\community-projects\communityProjectService.ts:308:              AND community_participant_id = $2
lib\community-projects\communityProjectService.ts:385:                community_participant_id,
lib\community-projects\communityProjectService.ts:386:                discussion_participant_id,
lib\community-projects\communityProjectService.ts:428:    const status = getWorkshopParticipantSubmissionStatus({
lib\community-projects\communityProjectService.ts:458:                author_community_participant_id,
lib\community-projects\communityProjectService.ts:520: * two browser tabs cannot create two discussion participants for the same member.
lib\community-projects\communityProjectService.ts:529:    const isProjectAuthor = projectDiscussion.author_community_participant_id === communityParticipantId;
lib\community-projects\communityProjectService.ts:535:            existingMapping.discussion_participant_id,
lib\community-projects\communityProjectService.ts:540:                'discussion-participant-invalid',
lib\community-projects\communityProjectService.ts:625:        throw new CommunityProjectMutationError('community-participant-missing', 'COMMUNITY_PROJECT_MEMBER_NOT_FOUND');
lib\community-projects\communityProjectService.ts:635:            WHERE community_participant_id = $1;
lib\community-projects\communityProjectService.ts:662:        throw new CommunityProjectMutationError('community-participant-missing', 'COMMUNITY_PROJECT_MEMBER_NOT_FOUND');
lib\community-projects\communityProjectService.ts:669:    readonly participantId: string | null;
lib\community-projects\communityProjectService.ts:674:        const participantId = await runDatabaseTransaction(COMMUNITY_PROJECT_OPERATION_NAME, (transaction) =>
lib\community-projects\communityProjectService.ts:678:        return { participantId, errorMessage: null, isParticipantMissing: false };
lib\community-projects\communityProjectService.ts:681:            error instanceof CommunityProjectMutationError && error.kind === 'community-participant-missing';
lib\community-projects\communityProjectService.ts:682:        return { participantId: null, errorMessage: getMutationErrorMessage(error), isParticipantMissing };
lib\community-projects\communityProjectService.ts:707:                    community_participant_id,
lib\community-projects\communityProjectService.ts:721:                  AND community_participant_id = $2;
lib\community-projects\communityProjectService.ts:732:                  AND community_participant_id = $2;
app\cs\online-workshop\page.tsx:1:import { ONLINE_WORKSHOP_EVENT_TYPE } from '@/businesses/online-workshop/config';
app\cs\online-workshop\page.tsx:2:import { OnlineWorkshopPage } from '@/businesses/online-workshop/_OnlineWorkshopPage';
app\cs\online-workshop\page.tsx:6:} from '@/businesses/online-workshop/onlineWorkshopMetadata';
app\cs\online-workshop\opengraph-image.tsx:1:import { ONLINE_WORKSHOP_SOCIAL_PREVIEW_OPTIONS } from '@/businesses/online-workshop/onlineWorkshopMetadata';
lib\community-projects\communityProjectService.test.ts:15:const COMMUNITY_PARTICIPANT_QUERY_FRAGMENT = 'FROM public.workshop_participants AS community_participant';
lib\community-projects\communityProjectService.test.ts:112:        const participantInsert = findQueryCall(queryCalls, 'INSERT INTO public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:113:        expect(participantInsert.values[2]).toBe('Ada Lovelace');
lib\community-projects\communityProjectService.test.ts:114:        expect(participantInsert.values[3]).toBe('ada@example.com');
lib\community-projects\communityProjectService.test.ts:115:        expect(participantInsert.values[4]).toMatch(/^[a-f0-9]{64}$/);
lib\community-projects\communityProjectService.test.ts:116:        expect(participantInsert.values[5]).toBe(true);
lib\community-projects\communityProjectService.test.ts:120:            'INSERT INTO public.community_project_discussion_participants',
lib\community-projects\communityProjectService.test.ts:124:        expect(discussionMappingInsert.values[2]).toBe(participantInsert.values[0]);
lib\community-projects\communityProjectService.test.ts:151:    it('refuses to create a project for a participant outside the permanent community', async () => {
lib\community-projects\communityProjectService.test.ts:180:                        author_community_participant_id: communityParticipantId,
lib\community-projects\communityProjectService.test.ts:184:            if (queryText.includes('SELECT discussion_participant_id')) {
lib\community-projects\communityProjectService.test.ts:185:                return [{ discussion_participant_id: discussionParticipantId }];
lib\community-projects\communityProjectService.test.ts:202:        const participantUpdate = findQueryCall(queryCalls, 'UPDATE public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:203:        expect(participantUpdate.values).toEqual([
lib\community-projects\communityProjectService.test.ts:211:        expect(queryCalls.some(({ queryText }) => queryText.includes('INSERT INTO public.workshop_participants'))).toBe(
lib\community-projects\communityProjectService.test.ts:234:                        author_community_participant_id: '55555555-5555-4555-8555-555555555555',
lib\community-projects\communityProjectService.test.ts:247:        const participantInsert = findQueryCall(queryCalls, 'INSERT INTO public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:250:            'INSERT INTO public.community_project_discussion_participants',
lib\community-projects\communityProjectService.test.ts:253:        expect(connection.discussionParticipantId).toBe(participantInsert.values[0]);
lib\community-projects\communityProjectService.test.ts:254:        expect(participantInsert.values[5]).toBe(false);
lib\community-projects\communityProjectService.test.ts:325:    it('reconciles every vote before deleting a community participant', async () => {
lib\community-projects\communityProjectService.test.ts:339:            if (queryText.includes('DELETE FROM public.workshop_participants')) {
lib\community-projects\communityProjectService.test.ts:350:        expect(findQueryCall(queryCalls, 'FOR UPDATE OF community_participant')).toBeDefined();
lib\community-projects\communityProjectService.test.ts:363:        expect(findQueryCall(queryCalls, 'DELETE FROM public.workshop_participants').values).toEqual([
app\cs\online-workshop\dekujeme\page.tsx:1:import { ONLINE_WORKSHOP_EVENT_TYPE } from '@/businesses/online-workshop/config';
app\cs\online-workshop\dekujeme\page.tsx:2:import { OnlineWorkshopThankYouPage } from '@/businesses/online-workshop/_OnlineWorkshopThankYouPage';
app\cs\online-workshop\dekujeme\page.tsx:3:import { ONLINE_WORKSHOP_THANK_YOU_METADATA } from '@/businesses/online-workshop/onlineWorkshopMetadata';
app\cs\online-workshop\dekujeme\page.tsx:4:import { readWorkshopParticipantIdentity, readWorkshopSlug } from '@/lib/workshops/workshopParticipantLink';
app\cs\online-workshop\dekujeme\page.tsx:21:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\online-workshop\dekujeme\page.tsx:33:    return <OnlineWorkshopThankYouPage workshop={workshop} participantIdentity={participantIdentity} />;
lib\community-projects\communityProjectDatabase.ts:12:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
lib\community-projects\communityProjectDatabase.ts:13:import type { WorkshopParticipant, WorkshopSubmissionStatus } from '@/lib/workshops/workshopTypes';
lib\community-projects\communityProjectDatabase.ts:17:    'id, author_community_participant_id, discussion_workshop_id, url, title, description, preview_image_url, status, upvote_count, downvote_count, created_at';
lib\community-projects\communityProjectDatabase.ts:23:    readonly author_community_participant_id: string;
lib\community-projects\communityProjectDatabase.ts:66:    communityParticipant: WorkshopParticipant | null,
lib\community-projects\communityProjectDatabase.ts:75:        (project.author_community_participant_id === communityParticipant.id ||
lib\community-projects\communityProjectDatabase.ts:76:            isWorkshopParticipantModerating(communityParticipant))
lib\community-projects\communityProjectDatabase.ts:81:    communityParticipant: WorkshopParticipant,
lib\community-projects\communityProjectDatabase.ts:83:    return isWorkshopParticipantModerating(communityParticipant) ? null : communityParticipant.id;
lib\community-projects\communityProjectDatabase.ts:100:            query = query.eq('author_community_participant_id', authorCommunityParticipantId);
lib\community-projects\communityProjectDatabase.ts:123:    communityParticipant: WorkshopParticipant | null,
lib\community-projects\communityProjectDatabase.ts:187:            .eq('community_participant_id', communityParticipantId)
lib\community-projects\communityProjectDatabase.ts:239:            projectRows.map((projectRow) => projectRow.author_community_participant_id),
lib\community-projects\communityProjectDatabase.ts:271:                    authorName: authorsResult.authorNameById.get(projectRow.author_community_participant_id) ?? 'Člen komunity',
lib\community-projects\communityProjectDatabase.ts:285: * Lists approved cards by their positive vote total. A participant receives their own pending project as well, while
lib\community-projects\communityProjectDatabase.ts:290:    communityParticipant: WorkshopParticipant | null,
lib\community-projects\communityProjectDatabase.ts:320:    communityParticipant: WorkshopParticipant | null,
lib\community-projects\communityProjectConstants.ts:3:export const COMMUNITY_PROJECT_DISCUSSION_PARTICIPANT_TABLE_NAME = 'community_project_discussion_participants';
lib\workshops\workshopCalendar.ts:5:    createWorkshopParticipantLink,
lib\workshops\workshopCalendar.ts:7:    type WorkshopParticipantIdentity,
lib\workshops\workshopCalendar.ts:19: * Note: A workshop loaded from the database satisfies this shape without carrying any participant data.
lib\workshops\workshopCalendar.ts:41:     * Details of the participant the event is built for, which may still be incomplete
lib\workshops\workshopCalendar.ts:43:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\workshops\workshopCalendar.ts:46:     * Site-relative path of the participant room of the workshop
lib\workshops\workshopCalendar.ts:48:    readonly participantPath: string;
lib\workshops\workshopCalendar.ts:52: * Names the event as a meeting of the participant with the host, so that both recognize it among the rest of their day
lib\workshops\workshopCalendar.ts:59:    participantFullname: string,
lib\workshops\workshopCalendar.ts:61:    const meetingTitle = [getFirstName(participantFullname), getFirstName(hostFullname)]
lib\workshops\workshopCalendar.ts:72: *       the calendar of a participant with the length such a workshop usually takes.
lib\workshops\workshopCalendar.ts:79: * Turns a workshop into the event which a participant can put into their calendar
lib\workshops\workshopCalendar.ts:81: * Note: The event points to the participant room with the details of the participant prefilled, so that the link inside
lib\workshops\workshopCalendar.ts:88:    participantIdentity,
lib\workshops\workshopCalendar.ts:89:    participantPath,
lib\workshops\workshopCalendar.ts:91:    const participantLink = createWorkshopParticipantLink(participantPath, participantIdentity, occurrence.slug);
lib\workshops\workshopCalendar.ts:95:        title: createWorkshopCalendarEventTitle(occurrence.title, hostFullname, participantIdentity.fullname),
lib\workshops\workshopCalendar.ts:99:        url: createAbsoluteUrl(participantLink ?? createWorkshopSelectionPath(participantPath, occurrence.slug)),
lib\workshops\workshopCalendar.ts:114: * @param currentTime moment to compare against, as an ISO 8601 string - the server time keeps every participant in sync
lib\workshops\workshopCalendar.test.ts:11:    slug: 'online-workshop-2026-08-20',
lib\workshops\workshopCalendar.test.ts:18:const PARTICIPANT_PATH = '/cs/online-workshop/participant';
lib\workshops\workshopCalendar.test.ts:21:    it('names the event as a meeting of the participant with the host', () => {
lib\workshops\workshopCalendar.test.ts:27:    it('leaves out an unknown participant instead of naming an empty side of the meeting', () => {
lib\workshops\workshopCalendar.test.ts:34:    it('points the event to the participant room with the details of the participant prefilled', () => {
lib\workshops\workshopCalendar.test.ts:38:            participantIdentity: { email: 'karel@firma.cz', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:39:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopCalendar.test.ts:52:    it('falls back to the plain participant room when the identity cannot prefill the connection form', () => {
lib\workshops\workshopCalendar.test.ts:56:            participantIdentity: { email: '', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:57:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopCalendar.test.ts:68:            participantIdentity: { email: 'karel@firma.cz', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:69:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopCalendar.test.ts:76:        expect(createWorkshopCalendarFileName('online-workshop-2026-08-20')).toBe('online-workshop-2026-08-20.ics');
lib\workshops\workshopAttendance.ts:8: * it sends and the graph which draws it all mean the same thing by an active participant.
lib\workshops\workshopAttendance.ts:15: *       moving a hand for half a minute never makes an attentive participant passive. It is short enough that somebody
lib\workshops\workshopAttendance.ts:37: * Was this participant at their computer, as far as the room could tell at this moment?
lib\community-membership\communityMembershipAdminLinks.ts:6:export type CommunityMembershipAdminTab = 'participants' | 'memberships';
lib\community-membership\communityMembershipAdminLinks.ts:9: * Builds the two-way handoff between a community participant and their durable membership. A participant is a room
lib\community-membership\communityMembershipAdminLinks.test.ts:8:    it('uses the durable normalized e-mail to open a membership from its community participant', () => {
lib\community-membership\communityMembershipAdminLinks.test.ts:14:    it('opens the participant side without inventing a member target when one was not selected', () => {
lib\community-membership\communityMembershipAdminLinks.test.ts:15:        expect(createCommunityMembershipAdminPath('participants')).toBe('/admin/community?tab=participants');
lib\workshops\workshopAdminSections.ts:9:    'participants',
lib\workshops\workshopAdminParticipantQuery.ts:92: * Reads the participant list view out of an API or export URL.
lib\workshops\workshopAdminParticipantQuery.ts:125: * Writes the filters and sort in a form shared by the paged API and participant exports.
components\shortener\ShortcodeLinkAdmin.test.tsx:38:        sourceApp: 'online-workshop',
lib\workshops\workshopAdminParticipantQuery.test.ts:8:describe('workshop admin participant query', () => {
lib\workshops\workshopAdminExports.ts:21:    'participants',
lib\workshops\workshopAdminExports.ts:22:    'participants-vcard',
lib\workshops\workshopAdminExports.ts:35:    readonly participantFullname: string | null;
lib\workshops\workshopAdminExports.ts:36:    readonly participantEmail: string | null;
lib\workshops\workshopAdminExports.ts:130:export function serializeWorkshopAdminParticipantsAsCsv(participants: readonly WorkshopAdminParticipant[]): string {
lib\workshops\workshopAdminExports.ts:131:    return serializeRowsAsCsv(participants, [
lib\workshops\workshopAdminExports.ts:132:            { header: 'Jméno', getValue: (participant) => participant.fullname },
lib\workshops\workshopAdminExports.ts:133:            { header: 'E-mail', getValue: (participant) => participant.email },
lib\workshops\workshopAdminExports.ts:134:            { header: 'Registrace', getValue: (participant) => participant.connectedAt },
lib\workshops\workshopAdminExports.ts:135:            { header: 'Naposledy aktivní', getValue: (participant) => participant.lastSeenAt },
lib\workshops\workshopAdminExports.ts:136:            { header: 'Čas v místnosti (s)', getValue: (participant) => participant.activeDurationSeconds },
lib\workshops\workshopAdminExports.ts:137:            { header: 'Komentáře', getValue: (participant) => participant.commentCount },
lib\workshops\workshopAdminExports.ts:138:            { header: 'Reakce', getValue: (participant) => participant.reactionCount },
lib\workshops\workshopAdminExports.ts:139:            { header: 'Hlasy', getValue: (participant) => participant.upvoteCount },
lib\workshops\workshopAdminExports.ts:140:            { header: 'Důvěryhodný', getValue: (participant) => formatBoolean(participant.isTrusted) },
lib\workshops\workshopAdminExports.ts:141:            { header: 'Moderátor', getValue: (participant) => formatBoolean(participant.isModerator) },
lib\workshops\workshopAdminExports.ts:142:            { header: 'Interakce zakázány', getValue: (participant) => formatBoolean(participant.isInteractionBanned) },
lib\workshops\workshopAdminExports.ts:145:                getValue: (participant) => getAdminContactPhoneNumbers(participant.contactGroup).join(', '),
lib\workshops\workshopAdminExports.ts:149:                getValue: (participant) => formatAdminContactRecords(participant.contactGroup),
lib\workshops\workshopAdminExports.ts:153:                getValue: (participant) => formatAdminWorkshopParticipations(participant.contactGroup),
lib\workshops\workshopAdminExports.ts:160:    participants: readonly WorkshopAdminParticipant[],
lib\workshops\workshopAdminExports.ts:163:        participants.map((participant) => {
lib\workshops\workshopAdminExports.ts:164:            const phoneNumbers = getAdminContactPhoneNumbers(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:165:            const contactRecords = formatAdminContactRecords(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:166:            const workshopParticipations = formatAdminWorkshopParticipations(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:169:                uid: `workshop-participant-${participant.id}`,
lib\workshops\workshopAdminExports.ts:170:                fullname: participant.fullname,
lib\workshops\workshopAdminExports.ts:171:                email: participant.email,
lib\workshops\workshopAdminExports.ts:175:                    `Registrace: ${participant.connectedAt}`,
lib\workshops\workshopAdminExports.ts:176:                    `Čas v místnosti: ${participant.activeDurationSeconds} s`,
lib\workshops\workshopAdminExports.ts:182:                revision: participant.lastSeenAt,
lib\workshops\workshopAdminExports.ts:192:            { header: 'ID účastníka', getValue: (comment) => comment.participantId },
lib\workshops\workshopAdminExports.ts:210:            { header: 'Jméno účastníka', getValue: (reaction) => reaction.participantFullname },
lib\workshops\workshopAdminExports.ts:211:            { header: 'E-mail účastníka', getValue: (reaction) => reaction.participantEmail },
lib\workshops\workshopAdminExports.ts:235:            { header: 'Nově připojení', getValue: (point) => point.participantCount },
lib\workshops\workshopAdminExports.ts:243:                    point.participantCount +
lib\workshops\workshopAdminExports.ts:263:    const fileExtension = exportKind === 'participants-vcard' ? 'vcf' : 'csv';
lib\workshops\workshopAdminExports.ts:271:        readonly participants?: readonly WorkshopAdminParticipant[];
lib\workshops\workshopAdminExports.ts:285:        case 'participants':
lib\workshops\workshopAdminExports.ts:287:                content: serializeWorkshopAdminParticipantsAsCsv(options.participants ?? []),
lib\workshops\workshopAdminExports.ts:291:        case 'participants-vcard':
lib\workshops\workshopAdminExports.ts:293:                content: serializeWorkshopAdminParticipantsAsVcard(options.workshop, options.participants ?? []),
lib\workshops\workshopAdminExports.test.ts:34:    id: 'participant-id',
lib\workshops\workshopAdminExports.test.ts:73:                participantId: PARTICIPANT.id,
lib\workshops\workshopAdminExports.test.ts:99:            'participants',
lib\workshops\workshopAdminExports.test.ts:100:            'participants-vcard',
lib\workshops\workshopAdminExports.test.ts:108:    it('writes the full participant activity summary as a spreadsheet-friendly CSV', () => {
lib\workshops\workshopAdminExports.test.ts:117:    it('writes a useful contact card for each filtered participant', () => {
lib\workshops\workshopAdminExports.test.ts:123:        expect(vcard).toContain('UID:workshop-participant-participant-id');
lib\workshops\workshopAdminExports.test.ts:127:    it('joins Contact-table values and workshop history into participant CSV and vCard exports', () => {
lib\workshops\workshopAdminExports.test.ts:182:    it('uses the matching MIME type and filename for participant vCards', () => {
lib\workshops\workshopAdminExports.test.ts:183:        const exportFile = createWorkshopAdminExportFile('participants-vcard', {
lib\workshops\workshopAdminExports.test.ts:185:            participants: [PARTICIPANT],
lib\workshops\workshopAdminExports.test.ts:190:        expect(buildWorkshopAdminExportFileName(WORKSHOP, 'participants-vcard')).toBe(
lib\workshops\workshopAdminExports.test.ts:191:            'production-code-with-agents-participants-vcard.vcf',
components\promptbook-qr-code.tsx:18: * shortener and participant rooms use exactly the same QR-code implementation and canvas layout.
lib\community\communityPreviewValues.ts:40:        isAuthorModerator: row.participant_id !== null && moderatorParticipantIds.has(row.participant_id),
lib\community\communityPreviewValues.test.ts:67:                participant_id: 'participant-id',
lib\community\communityPreviewValues.test.ts:72:            new Set(['participant-id']),
lib\community\communityPreviewValues.test.ts:84:                participant_id: null,
lib\community\communityPreviewValues.test.ts:89:            new Set(['participant-id']),
lib\community\communityPreviewValues.test.ts:99:                participant_id: null,
lib\tracking\trackingExclusions.ts:1:import { ONLINE_WORKSHOP_PARTICIPANT_PATH } from '@/businesses/online-workshop/config';
lib\tracking\trackingExclusions.ts:6:function isWorkshopParticipantPath(pathname: string): boolean {
lib\tracking\trackingExclusions.ts:17: * Admin URLs carry a bearer token and the participant URL can carry personal
lib\tracking\trackingExclusions.ts:21:    return !isPathExcluded(pathname, ADMIN_TRACKING_EXCLUDED_PATH_PREFIXES) && !isWorkshopParticipantPath(pathname);
lib\tracking\trackingExclusions.ts:25: * The participant room sends only explicit anonymized interaction events to
lib\tracking\trackingExclusions.ts:34:    return isGoogleAnalyticsAllowed(pathname) && !isWorkshopParticipantPath(pathname);
lib\tracking\trackingExclusions.test.ts:10:    it('excludes admin and workshop participant pages', () => {
lib\tracking\trackingExclusions.test.ts:13:        expect(isThirdPartyTrackingAllowed('/cs/online-workshop/participant')).toBe(false);
lib\tracking\trackingExclusions.test.ts:14:        expect(isThirdPartyTrackingAllowed('/cs/online-workshop')).toBe(true);
lib\tracking\trackingExclusions.test.ts:17:    it('allows anonymized Google Analytics events from the participant room without a page view', () => {
lib\tracking\trackingExclusions.test.ts:19:        expect(isGoogleAnalyticsAllowed('/cs/online-workshop/participant')).toBe(true);
lib\tracking\trackingExclusions.test.ts:20:        expect(isGoogleAnalyticsPageViewAllowed('/cs/online-workshop/participant')).toBe(false);
lib\tracking\trackingExclusions.test.ts:21:        expect(isGoogleAnalyticsPageViewAllowed('/cs/online-workshop')).toBe(true);
lib\community\communityPreviewDatabase.ts:18:    readonly participant_id: string | null;
lib\community\communityPreviewDatabase.ts:66: * Note: A message the administration created without a participant is deliberately left out, so this total says what
lib\community\communityPreviewDatabase.ts:148:        .select('id, participant_id, author_name, body, created_at')
lib\community\communityPreviewDatabase.ts:172:    participantIds: readonly string[],
lib\community\communityPreviewDatabase.ts:174:    if (participantIds.length === 0) {
lib\community\communityPreviewDatabase.ts:183:        .in('id', participantIds);
components\mocked-chat-section.tsx:18:    const participants = useMemo(
components\mocked-chat-section.tsx:20:            conversation.participants
components\mocked-chat-section.tsx:21:                .filter((participant: any) => participant.name !== 'user')
components\mocked-chat-section.tsx:22:                .map((participant: any) => ({
components\mocked-chat-section.tsx:23:                    ...participant,
components\mocked-chat-section.tsx:24:                    name: participant.name,
components\mocked-chat-section.tsx:25:                    isMe: participant.isMe,
components\mocked-chat-section.tsx:26:                    fullname: participant.fullname || participant.name,
components\mocked-chat-section.tsx:27:                    color: participant.color || '#6B7280',
components\mocked-chat-section.tsx:28:                    avatarSrc: participant.avatar! /* || generatePlaceholderAgentProfileImageUrl(participant.name)*/,
components\mocked-chat-section.tsx:30:        [conversation.participants],
components\mocked-chat-section.tsx:57:                participants={participants}
lib\community\communityPreview.ts:29:const COMMUNITY_WEBINAR_EVENT_TYPE: EventType = 'online-workshop';
lib\community\communityPreview.ts:99:            .map((discussionRow) => discussionRow.participant_id)
lib\community\communityPreview.ts:100:            .filter((participantId): participantId is string => participantId !== null),
lib\supabase\reportSupabaseError.test.ts:28:        expect(reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR)).toBe(
lib\supabase\reportSupabaseError.test.ts:36:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR);
lib\supabase\reportSupabaseError.test.ts:39:        expect(report).toContain('`get_workshop_admin_participant_page`');
lib\supabase\reportSupabaseError.test.ts:48:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR);
lib\supabase\reportSupabaseError.test.ts:67:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR, {
lib\supabase\reportSupabaseError.test.ts:81:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR, {
lib\calendar\create-calendar-links.test.ts:10:    id: 'online-workshop-2026-08-20',
lib\calendar\create-calendar-links.test.ts:15:    url: 'https://ptbk.io/cs/online-workshop/participant?email=karel%40firma.cz&fullname=Karel+Nov%C3%A1k',
lib\calendar\create-calendar-links.test.ts:45:        expect(icalendarLines).toContain('UID:online-workshop-2026-08-20@ptbk.io');
lib\calendar\create-calendar-links.test.ts:55:            [WORKSHOP_EVENT, { ...WORKSHOP_EVENT, id: 'online-workshop-2026-09-10' }],
lib\workshops\workshopValues.ts:20:type WorkshopParticipantUpdateValues = z.infer<typeof workshopParticipantUpdateSchema>;
lib\workshops\workshopValues.ts:49:        maximum_participant_count: values.maximumParticipantCount,
lib\workshops\workshopValues.ts:50:        artificial_watching_participant_count: values.artificialWatchingParticipantCount,
lib\workshops\workshopValues.ts:75:            : { maximum_participant_count: values.maximumParticipantCount }),
lib\workshops\workshopValues.ts:78:            : { artificial_watching_participant_count: values.artificialWatchingParticipantCount }),
lib\workshops\workshopValues.ts:119: * Database names for the feedback fields which may be independently saved by the participant.
lib\workshops\workshopValues.ts:157: * The written moderation state of one participant, whether the administration or a moderator of the room wrote it
lib\workshops\workshopValues.ts:159:export function createWorkshopParticipantUpdateDatabaseValues(values: WorkshopParticipantUpdateValues) {
lib\workshops\workshopValues.ts:169:        participant_id: null,
app\cs\komunita\page.tsx:3:import { readWorkshopParticipantIdentity } from '@/lib/workshops/workshopParticipantLink';
app\cs\komunita\page.tsx:31:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\komunita\page.tsx:40:            initialEmail={participantIdentity.email}
app\cs\komunita\page.tsx:41:            initialFullname={participantIdentity.fullname}
lib\workshops\workshopTypes.ts:21: * The lifecycle of something a participant submits for moderation. Chat messages and community projects deliberately
lib\workshops\workshopTypes.ts:65: * Note: The participant count deliberately stays out of `WorkshopSummary`, so no public list of terms exposes how
lib\workshops\workshopTypes.ts:69:    readonly participantCount: number;
lib\workshops\workshopTypes.ts:75:     * Note: This is a different audience from `participantCount`, which counts the people who really entered the room.
lib\workshops\workshopTypes.ts:106:     * published. It is a workshop setting rather than an ordinary content block, so every participant can open it
lib\workshops\workshopTypes.ts:131:export type WorkshopParticipant = {
lib\workshops\workshopTypes.ts:136:     * Contact address this participant connected with
lib\workshops\workshopTypes.ts:138:     * Note: A room only ever describes the very participant reading it, so this address never reaches anybody else. It
lib\workshops\workshopTypes.ts:146:     * Whether the messages of this participant are approved as they are written
lib\workshops\workshopTypes.ts:148:     * Note: Trust stays invisible in the room. Nothing but the approval of their own messages tells a participant or
lib\workshops\workshopTypes.ts:154:     * Whether this participant moderates the room, which the room says with a badge
lib\workshops\workshopTypes.ts:159:export type WorkshopAdminParticipant = WorkshopParticipant &
lib\workshops\workshopTypes.ts:168:     * Present when the participant is listed inside the community administration. A community membership belongs to
lib\workshops\workshopTypes.ts:169:     * their e-mail address rather than to this particular room session, so ordinary workshop participant data does
lib\workshops\workshopTypes.ts:176: * One server-paged slice of participants, keeping large workshops responsive in the administration.
lib\workshops\workshopTypes.ts:179:    readonly participants: readonly WorkshopAdminParticipant[];
lib\workshops\workshopTypes.ts:184: * One timestamped activity attributable to a participant.
lib\workshops\workshopTypes.ts:189:export type WorkshopParticipantTimelineEvent =
lib\workshops\workshopTypes.ts:218:    readonly participant: WorkshopAdminParticipant;
lib\workshops\workshopTypes.ts:219:    readonly events: readonly WorkshopParticipantTimelineEvent[];
lib\workshops\workshopTypes.ts:249:    readonly participantCount: number;
lib\workshops\workshopTypes.ts:334: * A participant's progressively saved reflection after one workshop.
lib\workshops\workshopTypes.ts:349: * The admin-only form of feedback, attributable to its participant and joinable to the private contact projection.
lib\workshops\workshopTypes.ts:354:        readonly participantId: string;
lib\workshops\workshopTypes.ts:362: * Note: Only a moderator receives this, so an ordinary participant never learns which invisible moderation state the
lib\workshops\workshopTypes.ts:366:    readonly participantId: string;
lib\workshops\workshopTypes.ts:387:     * Whether the administration created this message without a participant
lib\workshops\workshopTypes.ts:428: * Note: This counts reaction actions, rather than distinct people. A participant who reacts twice therefore adds two
lib\workshops\workshopTypes.ts:437: * One answer a member can choose in a poll. The room only receives aggregate counts and whether its own participant
lib\workshops\workshopTypes.ts:488:    readonly participant: WorkshopParticipant;
lib\workshops\workshopTypes.ts:491:     * How many participants had the room open recently, including the one this state was loaded for
lib\workshops\workshopTypes.ts:498:     * The materials which only paid members may see, named for a member who is not one of them
lib\workshops\workshopTypes.ts:500:     * Note: The materials themselves stay out of `contentBlocks`, so this list is what lets the room say where the
lib\workshops\workshopTypes.ts:501:     *       paid materials are, what they are called and how to unlock them. It is empty for a member who already paid
lib\workshops\workshopTypes.ts:542:    readonly participantId: string | null;
lib\workshops\workshopTypes.ts:571:    readonly participants: readonly WorkshopAdminParticipant[];
lib\workshops\workshopTypes.ts:572:    readonly participantCount: number;
lib\workshops\workshopSubmissionStatus.ts:4: * Recognizes the shared moderation lifecycle of a participant submission without tying project routes to chat-only
lib\workshops\workshopSpecialMaterials.ts:4: * Where a card which shares the material list belongs among the ordinary materials
lib\workshops\workshopSpecialMaterials.ts:11:     * After the ordinary materials, which is where a card belongs that adds to what the room already handed over
lib\workshops\workshopSpecialMaterials.ts:13:    | 'after-materials'
lib\workshops\workshopSpecialMaterials.ts:16:     * Before the ordinary materials while the member reading them does not pay for the community membership, and after
lib\workshops\workshopSpecialMaterials.ts:19:    | 'before-materials-until-paid';
lib\workshops\workshopSpecialMaterials.ts:22: * A participant-facing card which belongs in the material list without being an administrator-managed material.
lib\workshops\workshopSpecialMaterials.ts:26: *       material list still gives every participant the same predictable place to find them.
lib\workshops\workshopSpecialMaterials.ts:33:     * Where this card belongs in the material list, which is after the ordinary materials unless it says otherwise
lib\workshops\workshopSpecialMaterials.ts:49: * The special materials of a room divided around its ordinary materials, each group in the order it was supplied in
lib\workshops\workshopSpecialMaterials.ts:57: * Divides the special materials of a room into the ones which open its material list and the ones which close it.
lib\workshops\workshopSpecialMaterials.ts:70:        specialMaterial.placement === 'before-materials-until-paid' && !isPaidMember;
lib\workshops\workshopSpecialMaterials.test.ts:12:    placement: 'before-materials-until-paid',
lib\workshops\workshopSpecialMaterials.test.ts:21:describe('placement of the special materials of a room', () => {
lib\workshops\workshopSpecialMaterials.test.ts:42:    it('leaves a card which says nothing about its placement after the ordinary materials of either member', () => {
lib\workshops\workshopSession.ts:9:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopSession.ts:14:export type WorkshopParticipantRow = {
lib\workshops\workshopSession.ts:25: * Columns every query needs to describe a participant to themselves
lib\workshops\workshopSession.ts:27: * Note: These are only ever read for the participant making the request, so their own contact address is among them.
lib\workshops\workshopSession.ts:70: * Takes the session of one room out of the browser, which is what really signs a participant out of it: the cookie is
lib\workshops\workshopSession.ts:80:export function mapWorkshopParticipantRow(row: WorkshopParticipantRow): WorkshopParticipant {
lib\workshops\workshopSession.ts:92:export async function createWorkshopParticipant(
lib\workshops\workshopSession.ts:98:): Promise<{ readonly participant: WorkshopParticipant; readonly sessionToken: string } | null> {
lib\workshops\workshopSession.ts:114:        console.error('Failed to connect a workshop participant:', error?.message ?? 'No participant returned');
lib\workshops\workshopSession.ts:118:    return { participant: mapWorkshopParticipantRow(data as WorkshopParticipantRow), sessionToken };
lib\workshops\workshopSession.ts:122: * Ends the room session of one participant, leaving everything they did in that room exactly where it is.
lib\workshops\workshopSession.ts:124: * Note: A participant row is the author of every message, reaction and measured minute they left behind, so signing
lib\workshops\workshopSession.ts:126: *       every participant rather than none at all, the token is replaced by a fresh one which no browser was ever
lib\workshops\workshopSession.ts:129:export async function endWorkshopParticipantSession(
lib\workshops\workshopSession.ts:132:    participantId: string,
lib\workshops\workshopSession.ts:137:        .eq('id', participantId)
lib\workshops\workshopSession.ts:141:        console.error('Failed to end the session of a workshop participant:', error.message);
lib\workshops\workshopSession.ts:148:export async function authenticateWorkshopParticipant(
lib\workshops\workshopSession.ts:153:): Promise<WorkshopParticipant | null> {
lib\workshops\workshopSession.ts:168:        console.error('Failed to authenticate a workshop participant:', error.message);
lib\workshops\workshopSession.ts:182:        console.error('Failed to update workshop participant activity:', activityError.message);
lib\workshops\workshopSession.ts:185:    return mapWorkshopParticipantRow(data as WorkshopParticipantRow);
lib\workshops\workshopSession.test.ts:4:describe('workshop participant sessions', () => {
lib\workshops\workshopSession.test.ts:14:        const sessionToken = 'participant-session-token';
components\events\EventTermOptionCard.tsx:10: * participant connects to
lib\workshops\workshopSchemas.ts:32:    isWorkshopParticipantFullnameValid,
lib\workshops\workshopSchemas.ts:33:    normalizeWorkshopParticipantFullname,
lib\workshops\workshopSchemas.ts:35:import { normalizeWorkshopParticipantEmail } from '@/lib/workshops/workshopParticipantEmail';
lib\workshops\workshopSchemas.ts:47:    .transform(normalizeWorkshopParticipantFullname)
lib\workshops\workshopSchemas.ts:48:    .refine(isWorkshopParticipantFullnameValid, 'A participant name is required');
lib\workshops\workshopSchemas.ts:178:        .transform(normalizeWorkshopParticipantEmail),
lib\workshops\workshopSchemas.ts:317: * Every change a moderating role can make to one participant of a room
lib\workshops\workshopSchemas.ts:329:        message: 'At least one participant field is required',
lib\workshops\workshopSchemas.ts:475: * A participant can first submit a rating and then update each optional answer on its own request.
lib\workshops\workshopSchemas.test.ts:31:    eventType: 'online-workshop',
lib\workshops\workshopSchemas.test.ts:39:    it('normalizes participant identity before it is stored', () => {
lib\workshops\workshopSchemas.test.ts:46:    it('renames a participant under the very same rule the connection form uses', () => {
lib\workshops\workshopSchemas.test.ts:260:            eventType: 'online-workshop',
lib\workshops\workshopSchemas.test.ts:327:    it('writes which materials only paid members may see and refuses a content request without that answer', () => {
lib\workshops\workshopSchemas.test.ts:442:    it('validates artificial workshop actions independently from participant actions', () => {
lib\workshops\workshopSchemas.test.ts:466:    it('accepts trusted participant changes and bounded active-time reports', () => {
lib\workshops\workshopRequest.ts:7:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
lib\workshops\workshopRequest.ts:8:import { authenticateWorkshopParticipant } from '@/lib/workshops/workshopSession';
lib\workshops\workshopRequest.ts:9:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopRequest.ts:16:    readonly participant: WorkshopParticipant;
lib\workshops\workshopRequest.ts:33:    const participant = await authenticateWorkshopParticipant(supabase, request, workshopRow.id, workshopRow.slug);
lib\workshops\workshopRequest.ts:34:    if (participant === null) {
lib\workshops\workshopRequest.ts:38:    return { supabase, workshopRow, participant };
lib\workshops\workshopRequest.ts:55:    return isWorkshopParticipantModerating(authenticatedRequest.participant)
lib\workshops\workshopRequest.test.ts:2:    getWorkshopParticipantSubmissionStatus,
lib\workshops\workshopRequest.test.ts:5:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopRequest.test.ts:8:function createParticipant(isInteractionBanned: boolean, isTrusted = false, isModerator = false): WorkshopParticipant {
lib\workshops\workshopRequest.test.ts:10:        id: 'participant-1',
lib\workshops\workshopRequest.test.ts:20:describe('workshop participant interaction bans', () => {
lib\workshops\workshopRequest.test.ts:21:    it('allows watching participants and refuses chat interactions for banned participants', async () => {
lib\workshops\workshopRequest.test.ts:31:        expect(getWorkshopParticipantSubmissionStatus(createParticipant(false))).toBe('pending');
lib\workshops\workshopRequest.test.ts:32:        expect(getWorkshopParticipantSubmissionStatus(createParticipant(false, true))).toBe('approved');
lib\workshops\workshopRequest.test.ts:33:        expect(getWorkshopParticipantSubmissionStatus(createParticipant(true, true))).toBe('rejected');
lib\workshops\workshopRequest.test.ts:37:        expect(getWorkshopParticipantSubmissionStatus(createParticipant(false, false, true))).toBe('approved');
lib\workshops\workshopRequest.test.ts:38:        expect(getWorkshopParticipantSubmissionStatus(createParticipant(true, false, true))).toBe('rejected');
lib\workshops\workshopRepositoryMonitor.ts:122:    // Progress is newest-first. Sending oldest-first means that a participant who receives several commits in one
lib\shortener\shortcodeLink.ts:8:export const SHORTCODE_LINK_SOURCE_APP_VALUES = ['admin-shortener', 'online-workshop', 'community'] as const;
lib\shortener\shortcodeLink.ts:16:    'online-workshop': 'Online workshop',
lib\workshops\workshopPaidMembersContent.ts:4: * What decides whether the materials reserved for paid members reach the member reading the room
lib\workshops\workshopPaidMembersContent.ts:19: * The materials of a room as one member is given them: the ones they may read, and the ones they are merely told about
lib\workshops\workshopPaidMembersContent.ts:31: * Divides the materials a paid member would read into the ones this member receives and the ones which are only named
components\branding-components-demo.tsx:62:                        participants={SAMPLE_PARTICIPANTS}
lib\workshops\workshopRegistrationTiming.ts:2:    createWorkshopParticipantSearchParameters,
lib\workshops\workshopRegistrationTiming.ts:4:    type WorkshopParticipantIdentity,
lib\workshops\workshopRegistrationTiming.ts:39:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\workshops\workshopRegistrationTiming.ts:44: * Carries participant details to a registration confirmation page only when the direct-room-link window is open.
lib\workshops\workshopRegistrationTiming.ts:50:    participantIdentity,
lib\workshops\workshopRegistrationTiming.ts:57:    const searchParameters = createWorkshopParticipantSearchParameters(participantIdentity);
lib\workshops\workshopPaidMembersContent.test.ts:29:describe('the materials one member of a room is given', () => {
lib\workshops\workshopPaidMembersContent.test.ts:71:    it('keeps the order the materials were read in', () => {
lib\workshops\workshopRegistrationTiming.test.ts:10:const WORKSHOP_SLUG = 'online-workshop-2026-08-20';
lib\workshops\workshopRegistrationTiming.test.ts:12:describe('workshop direct participant links', () => {
lib\workshops\workshopRegistrationTiming.test.ts:38:    it('carries valid participant details to the thank-you path only inside the direct-link window', () => {
lib\workshops\workshopRegistrationTiming.test.ts:39:        const participantIdentity = { email: 'jana@example.com', fullname: 'Jana Nováková' } as const;
lib\workshops\workshopRegistrationTiming.test.ts:43:                thankYouPath: '/cs/online-workshop/dekujeme',
lib\workshops\workshopRegistrationTiming.test.ts:46:                participantIdentity,
lib\workshops\workshopRegistrationTiming.test.ts:50:            '/cs/online-workshop/dekujeme?workshop=online-workshop-2026-08-20&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
lib\workshops\workshopRegistrationTiming.test.ts:54:                thankYouPath: '/cs/online-workshop/dekujeme',
lib\workshops\workshopRegistrationTiming.test.ts:57:                participantIdentity,
lib\workshops\workshopRegistrationTiming.test.ts:60:        ).toBe('/cs/online-workshop/dekujeme?workshop=online-workshop-2026-08-20');
lib\workshops\workshopOverviewSeriesPoints.ts:185:            joinedParticipants: point.participantCount,
lib\workshops\workshopRegistrations.ts:7: *       who entered the live room are counted from the room's own participants instead.
lib\workshops\workshopRegistrations.ts:18:    readonly participantCount: number;
lib\workshops\workshopRegistrations.ts:43:const REGISTRATION_PARTICIPANT_COUNT_FIELD_NAME = 'participantCount';
lib\workshops\workshopRegistrations.ts:115:    const participantCount = registrationPayload[REGISTRATION_PARTICIPANT_COUNT_FIELD_NAME];
lib\workshops\workshopRegistrations.ts:120:        typeof participantCount !== 'number' ||
lib\workshops\workshopRegistrations.ts:121:        !Number.isSafeInteger(participantCount) ||
lib\workshops\workshopRegistrations.ts:122:        participantCount < 1
lib\workshops\workshopRegistrations.ts:127:    return { termId, participantCount };
lib\workshops\workshopRegistrations.ts:156:            return { termId, participantCount: SINGLE_PARTICIPANT_COUNT };
lib\workshops\workshopRegistrations.ts:229:            registeredParticipantCount + registration.participantCount,
components\analytics.tsx:15: * participant room initializes Google Analytics without a page view and sends
lib\workshops\workshopOverviewSeriesPoints.test.ts:27:        participantCount: 0,
lib\workshops\workshopOverviewSeriesPoints.test.ts:93:            createTimelinePoint('2026-08-20T08:00:00.000Z', { participantCount: 3 }),
lib\workshops\workshopRegistrations.test.ts:14:    slug: 'online-workshop-2026-08-26',
lib\workshops\workshopRegistrations.test.ts:45:    slug: 'online-workshop-2026-08-20',
lib\workshops\workshopRegistrations.test.ts:53:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 4 }),
lib\workshops\workshopRegistrations.test.ts:55:        ).toEqual({ termId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 4 });
lib\workshops\workshopRegistrations.test.ts:63:            JSON.stringify({ selectedDateId: '2026-09-09', participantCount: 2 }),
lib\workshops\workshopRegistrations.test.ts:66:        expect(readWorkshopRegistration(contactNote)).toEqual({ termId: '2026-09-09', participantCount: 2 });
lib\workshops\workshopRegistrations.test.ts:72:            participantCount: 1,
lib\workshops\workshopRegistrations.test.ts:79:            participantCount: 1,
lib\workshops\workshopRegistrations.test.ts:113:                JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 0 }),
lib\workshops\workshopRegistrations.test.ts:126:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 3 }),
lib\workshops\workshopRegistrations.test.ts:138:            readWorkshopRegistration(JSON.stringify({ selectedDateId: '2026-09-09', participantCount: 4 })),
lib\workshops\workshopRegistrations.test.ts:140:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 1 }),
lib\workshops\workshopRegistrations.test.ts:176:                { termId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 },
lib\workshops\workshopRegistrations.test.ts:181:            isWorkshopRegistrationForTerm({ termId: '2026-08-26', participantCount: 1 }, ONLINE_WORKSHOP_TERM),
lib\workshops\workshopRegistrations.test.ts:185:                { termId: 'středa 26. 8. 2026 19:00', participantCount: 1 },
lib\workshops\workshopRegistrations.test.ts:191:                { termId: 'online-workshop-2026-09-02', participantCount: 1 },
lib\workshops\workshopReactionAnimations.ts:14:     * What the participant sent, an emoji or any other short text
lib\workshops\workshopReactionAnimations.ts:109:     * The reactions which are celebrated this way, already in the form participants send them
lib\workshops\workshopReactionAnimations.ts:329: * The reaction as the registry knows it, no matter how the keyboard of the participant spelled it
lib\workshops\workshopReactionAnimations.ts:374: * The same reaction told without any travel, for a participant who asked the system for less motion
lib\workshops\workshopReactionAnimations.ts:444: *       recognize what a participant sent, not to be listed twice in the administration.
lib\workshops\workshopModeration.ts:1:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopModeration.ts:30:     * Whether this role has the messages of a participant approved as they are written from now on
lib\workshops\workshopModeration.ts:35:     * Whether this role takes the interactions of a participant away
lib\workshops\workshopModeration.ts:82: * Whether a participant moderates the room they are connected to
lib\workshops\workshopModeration.ts:89:export function isWorkshopParticipantModerating(participant: WorkshopParticipant): boolean {
lib\workshops\workshopModeration.ts:90:    return participant.isModerator && !participant.isInteractionBanned;
lib\workshops\workshopModeration.ts:94: * Whether this moderating role may change the moderation state of one participant at all
lib\workshops\workshopModeration.ts:100:export function isWorkshopParticipantModeratedBy(
lib\workshops\workshopModeration.ts:102:    participant: { readonly isModerator: boolean },
lib\workshops\workshopModeration.ts:104:    return !participant.isModerator || getWorkshopModerationCapabilities(moderationRole).isModeratorAppointmentOffered;
lib\workshops\workshopModeration.ts:127: * The written participant fields which this moderating role may not change
lib\workshops\workshopModeration.ts:129:export function getUnofferedWorkshopParticipantModerationFieldNames(
lib\workshops\workshopModeration.test.ts:3:    getUnofferedWorkshopParticipantModerationFieldNames,
lib\workshops\workshopModeration.test.ts:5:    isWorkshopParticipantModeratedBy,
lib\workshops\workshopModeration.test.ts:6:    isWorkshopParticipantModerating,
lib\workshops\workshopModeration.test.ts:9:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopModeration.test.ts:12:function createParticipant(values: Partial<WorkshopParticipant> = {}): WorkshopParticipant {
lib\workshops\workshopModeration.test.ts:14:        id: 'participant-1',
lib\workshops\workshopModeration.test.ts:44:        expect(getUnofferedWorkshopParticipantModerationFieldNames('moderator', { isModerator: true })).toEqual([
lib\workshops\workshopModeration.test.ts:48:            getUnofferedWorkshopParticipantModerationFieldNames('moderator', {
lib\workshops\workshopModeration.test.ts:53:        expect(getUnofferedWorkshopParticipantModerationFieldNames('admin', { isModerator: true })).toEqual([]);
lib\workshops\workshopModeration.test.ts:57:        expect(isWorkshopParticipantModeratedBy('moderator', { isModerator: false })).toBe(true);
lib\workshops\workshopModeration.test.ts:58:        expect(isWorkshopParticipantModeratedBy('moderator', { isModerator: true })).toBe(false);
lib\workshops\workshopModeration.test.ts:59:        expect(isWorkshopParticipantModeratedBy('admin', { isModerator: true })).toBe(true);
lib\workshops\workshopModeration.test.ts:75:        expect(isWorkshopParticipantModerating(createParticipant({ isModerator: true }))).toBe(true);
lib\workshops\workshopModeration.test.ts:76:        expect(isWorkshopParticipantModerating(createParticipant())).toBe(false);
lib\workshops\workshopModeration.test.ts:77:        expect(isWorkshopParticipantModerating(createParticipant({ isTrusted: true }))).toBe(false);
lib\workshops\workshopModeration.test.ts:79:            isWorkshopParticipantModerating(createParticipant({ isModerator: true, isInteractionBanned: true })),
lib\workshops\workshopMigration.test.ts:32:    'migrations/2026-08-0100-online-workshop-multiple-terms.sql',
lib\workshops\workshopMigration.test.ts:44:    'migrations/2026-08-0800-workshop-admin-participant-page-ordering.sql',
lib\workshops\workshopMigration.test.ts:51:    'migrations/2026-08-1100-workshop-admin-participant-page-variable-conflict.sql',
lib\workshops\workshopMigration.test.ts:59:    'migrations/2026-08-1600-online-workshop-wrap-up-and-feedback.sql',
lib\workshops\workshopMigration.test.ts:64:    'migrations/2026-08-1700-online-workshop-shortcode-material-links.sql',
lib\workshops\workshopMigration.test.ts:69:    'migrations/2026-08-2500-online-workshop-chat-shortcode-links.sql',
lib\workshops\workshopMigration.test.ts:178:const PARTICIPANT_PAGE_ROW_SOURCE = 'participant_page';
lib\workshops\workshopMigration.test.ts:182:    'workshop_participants',
lib\workshops\workshopMigration.test.ts:189: * Names the database column behind one sortable column of the participant administration.
lib\workshops\workshopMigration.test.ts:208:    it('records each participant action with a server timestamp and identity', () => {
lib\workshops\workshopMigration.test.ts:209:        expect(MIGRATION_SQL).toMatch(/workshop_participants[\s\S]*connected_at timestamptz NOT NULL DEFAULT now\(\)/);
lib\workshops\workshopMigration.test.ts:212:            /workshop_comment_upvotes[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:215:            /workshop_reactions[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:218:            /workshop_content_link_clicks[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:231:            'CONSTRAINT workshop_comment_upvotes_one_per_participant UNIQUE (comment_id, participant_id)',
lib\workshops\workshopMigration.test.ts:233:        expect(MIGRATION_SQL).toContain('workshop_comment_upvotes_participant_fk');
lib\workshops\workshopMigration.test.ts:248:    it('marks artificial actions and participant interaction bans in the follow-up migration', () => {
lib\workshops\workshopMigration.test.ts:252:        expect(MODERATION_MIGRATION_SQL).toContain('ALTER COLUMN participant_id DROP NOT NULL');
lib\workshops\workshopMigration.test.ts:261:        expect(ANALYTICS_MIGRATION_SQL).toContain('record_workshop_participant_presence');
lib\workshops\workshopMigration.test.ts:262:        expect(ANALYTICS_MIGRATION_SQL).toContain('get_workshop_participant_activity_totals');
lib\workshops\workshopMigration.test.ts:265:    it('indexes the participants seen recently so the watching count stays cheap', () => {
lib\workshops\workshopMigration.test.ts:267:            'CREATE INDEX IF NOT EXISTS workshop_participants_watching_idx',
lib\workshops\workshopMigration.test.ts:270:            'public.workshop_participants (workshop_id, last_seen_at DESC)',
lib\workshops\workshopMigration.test.ts:338:        expect(REACTION_COUNT_MIGRATION_SQL).toContain('target_participant_id IS NULL');
lib\workshops\workshopMigration.test.ts:364:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('get_workshop_admin_participant_page');
lib\workshops\workshopMigration.test.ts:368:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_admin_filter_idx');
lib\workshops\workshopMigration.test.ts:369:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_fullname_trigram_idx');
lib\workshops\workshopMigration.test.ts:370:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_email_trigram_idx');
lib\workshops\workshopMigration.test.ts:371:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_comments_participant_timeline_idx');
lib\workshops\workshopMigration.test.ts:372:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_reactions_participant_timeline_idx');
lib\workshops\workshopMigration.test.ts:377:            'CREATE OR REPLACE FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:396:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain("'participant'::text AS event_kind");
lib\workshops\workshopMigration.test.ts:408:        expect(MODERATOR_MIGRATION_SQL).toContain('CREATE INDEX IF NOT EXISTS workshop_participants_moderator_idx');
lib\workshops\workshopMigration.test.ts:412:    it('returns and filters the moderator in the paged participant administration, without a second overload', () => {
lib\workshops\workshopMigration.test.ts:415:        expect(MODERATOR_MIGRATION_SQL).toContain('DROP FUNCTION IF EXISTS public.get_workshop_admin_participant_page');
lib\workshops\workshopMigration.test.ts:418:            'AND (target_is_moderator IS NULL OR workshop_participant.is_moderator = target_is_moderator)',
lib\workshops\workshopMigration.test.ts:420:        expect(MODERATOR_MIGRATION_SQL).toContain('workshop_participant.is_moderator,');
lib\workshops\workshopMigration.test.ts:430:    it('settles the ambiguous participant ordering for the whole function, whatever the database still holds', () => {
lib\workshops\workshopMigration.test.ts:435:            'CREATE FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:441:                `DROP FUNCTION IF EXISTS public.get_workshop_admin_participant_page( ${functionSignature} );`,
lib\workshops\workshopMigration.test.ts:453:            'GRANT EXECUTE ON FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:477:            'CONSTRAINT workshop_poll_votes_one_per_participant UNIQUE (poll_id, participant_id)',
lib\workshops\workshopMigration.test.ts:552:            'REFERENCES public.workshop_participants(id) ON DELETE SET NULL',
lib\workshops\workshopMigration.test.ts:593:            'CREATE TABLE IF NOT EXISTS public.community_project_discussion_participants',
lib\workshops\workshopMigration.test.ts:595:        expect(COMMUNITY_PROJECT_MIGRATION_SQL).toContain('PRIMARY KEY (project_id, community_participant_id)');
lib\workshops\workshopMigration.test.ts:628:            'DROP CONSTRAINT IF EXISTS community_project_votes_community_participant_id_fkey',
lib\workshops\workshopMigration.test.ts:631:            'REFERENCES public.workshop_participants(id) ON DELETE RESTRICT',
lib\workshops\workshopMigration.test.ts:648:            'CONSTRAINT workshop_feedback_one_per_participant UNIQUE (workshop_id, participant_id)',
lib\workshops\workshopMigration.test.ts:650:        expect(WRAP_UP_AND_FEEDBACK_MIGRATION_SQL).toContain('workshop_feedback_participant_fk');
lib\workshops\workshopMigration.test.ts:675:    it('routes material analytics through persisted ad hoc short links rather than a participant browser event', () => {
lib\workshops\workshopMigration.test.ts:785:            'DROP FUNCTION IF EXISTS public.record_workshop_participant_presence(uuid, uuid, integer);',
lib\workshops\workshopMigration.test.ts:789:            'ON CONFLICT (workshop_id, bucket_starts_at, participant_id) DO UPDATE',
lib\workshops\workshopMigration.test.ts:791:        expect(ATTENDANCE_MIGRATION_SQL).toContain('NOT workshop_participant_presence_samples.is_actively_attending');
lib\workshops\workshopMigration.test.ts:793:            'GRANT EXECUTE ON FUNCTION public.record_workshop_participant_presence(uuid, uuid, integer, boolean) TO service_role;',
lib\workshops\workshopMigration.test.ts:799:        expect(ATTENDANCE_MIGRATION_SQL).toContain('actively_watching_participant_count bigint');
lib\workshops\workshopMigration.test.ts:800:        expect(ATTENDANCE_MIGRATION_SQL).toContain('passively_watching_participant_count bigint');
lib\workshops\workshopMaterialLinks.ts:479:    return workshopKind === 'workshop' ? 'online-workshop' : 'community';
lib\workshops\workshopMaterialLinks.ts:695: * ad hoc-shortcode path as materials. A normal participant never calls this:
lib\workshops\workshopReactionAnimations.test.ts:97:    it('recognizes the same reaction however the keyboard of the participant spelled it', () => {
lib\workshops\workshopReactionAnimations.test.ts:124:    it('leaves a reaction where it was sent when the participant asked for less motion', () => {
lib\workshops\workshopMaterialLinks.test.ts:35:            'online-workshop-2026-08-20',
lib\workshops\workshopMaterialLinks.test.ts:43:        expect(parsedUrl.searchParams.get('utm_campaign')).toBe('online-workshop-2026-08-20');
lib\workshops\workshopMaterialLinks.test.ts:110:        expect(getWorkshopMaterialShortcodeSourceApp('workshop')).toBe('online-workshop');
lib\workshops\workshopMaterialLinks.test.ts:156:                sourceApp: 'online-workshop',
lib\workshops\workshopMaterialLinks.test.ts:180:            sourceApp: 'online-workshop',
lib\workshops\workshopMaterialLinks.test.ts:239:                sourceApp: 'online-workshop',
lib\workshops\workshopMaterialLinks.test.ts:264:            sourceApp: 'online-workshop',
lib\workshops\workshopKindCapabilities.ts:30:     * Whether a room gathers its participants around a shared stage with a video stream
lib\workshops\workshopKindCapabilities.ts:40:    /** Whether a room can give every participant a presentation beside its ordinary materials. */
lib\workshops\workshopKindCapabilities.ts:65:     * Whether a room leads its participants on into the permanent community, which is where everything outlasting one
lib\workshops\workshopKindCapabilities.ts:75:     * Whether a participant can end their own session in this room and be offered its connection form again, which
lib\workshops\workshopKindCapabilities.ts:78:     * Note: Only a room a participant connected to themselves is left this way. The session of a project discussion is
components\admin\AdminContactDetails.tsx:30:                    <li key={workshopParticipation.participantId} className="border-l-2 border-cyan-200 pl-2">
lib\workshops\workshopKindCapabilities.test.ts:89:    it('lets a participant sign out of the rooms they connected to themselves', () => {
lib\workshops\workshopKindCapabilities.test.ts:134:                eventType: 'online-workshop',
lib\workshops\workshopDatabase.ts:39:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
lib\workshops\workshopDatabase.ts:40:import { normalizeWorkshopParticipantEmail } from '@/lib/workshops/workshopParticipantEmail';
lib\workshops\workshopDatabase.ts:68:    WorkshopParticipant,
lib\workshops\workshopDatabase.ts:69:    WorkshopParticipantTimelineEvent,
lib\workshops\workshopDatabase.ts:125:    readonly maximum_participant_count: number | null;
lib\workshops\workshopDatabase.ts:126:    readonly artificial_watching_participant_count?: number;
lib\workshops\workshopDatabase.ts:129:     * The keys of the panels this workshop switched off for its participants
lib\workshops\workshopDatabase.ts:160:    | 'maximum_participant_count'
lib\workshops\workshopDatabase.ts:168:    'id, room_kind, slug, title, description, starts_at, ends_at, is_published, event_type, location_kind, location_label, price_czk, maximum_participant_count';
lib\workshops\workshopDatabase.ts:190: * The one record a participant progressively fills after a workshop.
lib\workshops\workshopDatabase.ts:195:    readonly participant_id: string;
lib\workshops\workshopDatabase.ts:205:    'id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at';
lib\workshops\workshopDatabase.ts:211:    readonly participant_id: string | null;
lib\workshops\workshopDatabase.ts:226:    'id, participant_id, parent_comment_id, author_name, body, status, upvote_count, artificial_upvote_count, is_artificial, created_at';
lib\workshops\workshopDatabase.ts:279: * Everything the administration lists about one participant, so a new participant field is selected everywhere at once
lib\workshops\workshopDatabase.ts:287:const WORKSHOP_ADMIN_PARTICIPANT_PAGE_FUNCTION_NAME = 'get_workshop_admin_participant_page';
lib\workshops\workshopDatabase.ts:290:    WorkshopParticipantActivityTotalsRow & {
lib\workshops\workshopDatabase.ts:294:type WorkshopParticipantActivityTotalsRow = {
lib\workshops\workshopDatabase.ts:295:    readonly participant_id: string;
lib\workshops\workshopDatabase.ts:306:type WorkshopParticipantCountRow = {
lib\workshops\workshopDatabase.ts:308:    readonly participant_count: number | string;
lib\workshops\workshopDatabase.ts:317:type WorkshopParticipantTimelineCommentRow = Pick<WorkshopCommentRow, 'id' | 'body' | 'status' | 'created_at'>;
lib\workshops\workshopDatabase.ts:319:type WorkshopParticipantTimelineCommentReferenceRow = Pick<WorkshopCommentRow, 'id' | 'author_name' | 'body'>;
lib\workshops\workshopDatabase.ts:323:    readonly watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:324:    readonly actively_watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:325:    readonly passively_watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:326:    readonly participant_count: number | string;
lib\workshops\workshopDatabase.ts:343:    readonly participant_id: string | null;
lib\workshops\workshopDatabase.ts:388:type WorkshopParticipantPollVoteRow = {
lib\workshops\workshopDatabase.ts:410: * otherwise complete participant timeline or a large reaction export.
lib\workshops\workshopDatabase.ts:451:        // to a participant browser.
lib\workshops\workshopDatabase.ts:463:        artificialWatchingParticipantCount: row.artificial_watching_participant_count ?? 0,
lib\workshops\workshopDatabase.ts:484:            maximumParticipantCount: row.maximum_participant_count,
lib\workshops\workshopDatabase.ts:495:    participantCount: number,
lib\workshops\workshopDatabase.ts:502:        participantCount,
lib\workshops\workshopDatabase.ts:539:    participantById: ReadonlyMap<string, WorkshopFeedbackParticipantRow>,
lib\workshops\workshopDatabase.ts:541:    const participant = participantById.get(row.participant_id);
lib\workshops\workshopDatabase.ts:542:    if (participant === undefined) {
lib\workshops\workshopDatabase.ts:548:        participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:549:        fullname: participant.fullname,
lib\workshops\workshopDatabase.ts:550:        email: participant.email,
lib\workshops\workshopDatabase.ts:560:    const author = row.participant_id === null ? undefined : roomContext.authorByParticipantId.get(row.participant_id);
lib\workshops\workshopDatabase.ts:580:        participantId: row.id,
lib\workshops\workshopDatabase.ts:588: * The author of a message which the very participant who wrote it just sent, so it needs no second read of them
lib\workshops\workshopDatabase.ts:590:export function createWorkshopCommentAuthor(participant: WorkshopParticipant): WorkshopCommentAuthor {
lib\workshops\workshopDatabase.ts:592:        participantId: participant.id,
lib\workshops\workshopDatabase.ts:593:        isTrusted: participant.isTrusted,
lib\workshops\workshopDatabase.ts:594:        isInteractionBanned: participant.isInteractionBanned,
lib\workshops\workshopDatabase.ts:595:        isModerator: participant.isModerator,
lib\workshops\workshopDatabase.ts:602: * Note: An artificial message of the administration has no author at all, and a deleted participant leaves their
lib\workshops\workshopDatabase.ts:613:        .map((commentRow) => commentRow.participant_id)
lib\workshops\workshopDatabase.ts:614:        .filter((participantId): participantId is string => participantId !== null);
lib\workshops\workshopDatabase.ts:664:    activityTotals: WorkshopParticipantActivityTotalsRow | undefined,
lib\workshops\workshopDatabase.ts:732:        watchingParticipantCount: getNonNegativeWholeNumber(row.watching_participant_count),
lib\workshops\workshopDatabase.ts:733:        activelyWatchingParticipantCount: getNonNegativeWholeNumber(row.actively_watching_participant_count),
lib\workshops\workshopDatabase.ts:734:        passivelyWatchingParticipantCount: getNonNegativeWholeNumber(row.passively_watching_participant_count),
lib\workshops\workshopDatabase.ts:735:        participantCount: getNonNegativeWholeNumber(row.participant_count),
lib\workshops\workshopDatabase.ts:818:type WorkshopPollVoteErrorKind = 'not-found' | 'closed' | 'invalid-participant' | 'database';
lib\workshops\workshopDatabase.ts:831:    WORKSHOP_POLL_PARTICIPANT_INVALID: 'invalid-participant',
lib\workshops\workshopDatabase.ts:832:    WORKSHOP_POLL_VOTER_EMAIL_INVALID: 'invalid-participant',
lib\workshops\workshopDatabase.ts:844:    participant: Pick<WorkshopParticipant, 'id' | 'email'>,
lib\workshops\workshopDatabase.ts:852:        target_participant_id: participant.id,
lib\workshops\workshopDatabase.ts:853:        target_voter_email: normalizeWorkshopParticipantEmail(participant.email),
lib\workshops\workshopDatabase.ts:870: *       room correct when several participants react at once, when a room reconnects, or when its own broadcast comes
lib\workshops\workshopDatabase.ts:876:    participantId: string | null,
lib\workshops\workshopDatabase.ts:884:        target_participant_id: participantId,
lib\workshops\workshopDatabase.ts:1107:async function countWorkshopParticipantsByWorkshop(
lib\workshops\workshopDatabase.ts:1111:    const { data, error } = await supabase.rpc('get_workshop_participant_counts', { target_room_kind: workshopKind });
lib\workshops\workshopDatabase.ts:1113:        console.error('Failed to count the participants of the listed workshops:', error.message);
lib\workshops\workshopDatabase.ts:1118:        ((data ?? []) as WorkshopParticipantCountRow[]).map(
lib\workshops\workshopDatabase.ts:1119:            (participantCounts) =>
lib\workshops\workshopDatabase.ts:1121:                    participantCounts.workshop_id,
lib\workshops\workshopDatabase.ts:1122:                    getNonNegativeWholeNumber(participantCounts.participant_count),
lib\workshops\workshopDatabase.ts:1139:    const [workshopsResult, participantCountByWorkshopId, registeredParticipantCountByTermId] = await Promise.all([
lib\workshops\workshopDatabase.ts:1153:        countWorkshopParticipantsByWorkshop(supabase, workshopKind),
lib\workshops\workshopDatabase.ts:1167:                participantCountByWorkshopId.get(workshopRow.id) ?? 0,
lib\workshops\workshopDatabase.ts:1181: * Counts the participants whose browser talked to the room within the watching window
lib\workshops\workshopDatabase.ts:1186: *       participant would otherwise count the whole audience for nobody to read.
lib\workshops\workshopDatabase.ts:1189:export async function countWatchingWorkshopParticipants(
lib\workshops\workshopDatabase.ts:1205:        console.error('Failed to count the participants watching a workshop:', error.message);
lib\workshops\workshopDatabase.ts:1209:    return (count ?? 0) + Math.max(0, workshopRow.artificial_watching_participant_count ?? 0);
lib\workshops\workshopDatabase.ts:1422:            ? Promise.resolve({ data: [] as readonly WorkshopParticipantPollVoteRow[], error: null })
lib\workshops\workshopDatabase.ts:1456:                ((selectedVotesResult.data ?? []) as readonly WorkshopParticipantPollVoteRow[]).map(
lib\workshops\workshopDatabase.ts:1488: * participant-specific lookup contains only that participant's own selection, so no room response can infer who
lib\workshops\workshopDatabase.ts:1502:    const normalizedVoterEmail = voterEmail === null ? null : normalizeWorkshopParticipantEmail(voterEmail);
lib\workshops\workshopDatabase.ts:1576: * Loads one page of participants together with the activity totals used for its sortable columns.
lib\workshops\workshopDatabase.ts:1579: * every participant to the browser just to display one page.
lib\workshops\workshopDatabase.ts:1586:    const participantPageParameters = {
lib\workshops\workshopDatabase.ts:1601:        participantPageParameters,
lib\workshops\workshopDatabase.ts:1609:                participantPageParameters,
lib\workshops\workshopDatabase.ts:1614:    const participantRows = (data ?? []) as WorkshopAdminParticipantPageRow[];
lib\workshops\workshopDatabase.ts:1617:            participants: participantRows.map(mapWorkshopAdminParticipantPageRow),
lib\workshops\workshopDatabase.ts:1618:            totalCount: getNonNegativeWholeNumber(participantRows[0]?.total_count),
lib\workshops\workshopDatabase.ts:1625: * Loads the exact actions of one participant from their existing source records.
lib\workshops\workshopDatabase.ts:1627: * Note: Registration and last activity are participant fields rather than action rows, so they are added as two
lib\workshops\workshopDatabase.ts:1630: * aggregate instead of this participant-attributed timeline.
lib\workshops\workshopDatabase.ts:1635:    participantId: string,
lib\workshops\workshopDatabase.ts:1637:    const [participantResult, commentsResult, reactionsResult, upvotesResult] = await Promise.all([
lib\workshops\workshopDatabase.ts:1642:            .eq('id', participantId)
lib\workshops\workshopDatabase.ts:1644:        loadAllSupabaseRows<WorkshopParticipantTimelineCommentRow>((fromIndex, toIndex) =>
lib\workshops\workshopDatabase.ts:1649:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1659:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1669:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1677:        participantResult.error?.message ??
lib\workshops\workshopDatabase.ts:1686:    if (participantResult.data === null) {
lib\workshops\workshopDatabase.ts:1690:    const participant = participantResult.data as WorkshopAdminParticipantRow;
lib\workshops\workshopDatabase.ts:1696:    const upvotedCommentsResult = await loadWorkshopRowsByIds<WorkshopParticipantTimelineCommentReferenceRow>(
lib\workshops\workshopDatabase.ts:1709:    const upvotedCommentById = new Map<string, WorkshopParticipantTimelineCommentReferenceRow>(
lib\workshops\workshopDatabase.ts:1712:    const events: WorkshopParticipantTimelineEvent[] = [
lib\workshops\workshopDatabase.ts:1713:        { kind: 'joined' as const, id: `joined-${participant.id}`, occurredAt: participant.connected_at },
lib\workshops\workshopDatabase.ts:1714:        { kind: 'last-seen' as const, id: `last-seen-${participant.id}`, occurredAt: participant.last_seen_at },
lib\workshops\workshopDatabase.ts:1715:        ...commentRows.map((comment): WorkshopParticipantTimelineEvent => ({
lib\workshops\workshopDatabase.ts:1722:        ...reactionRows.map((reaction): WorkshopParticipantTimelineEvent => ({
lib\workshops\workshopDatabase.ts:1728:        ...upvoteRows.map((upvote): WorkshopParticipantTimelineEvent => {
lib\workshops\workshopDatabase.ts:1746:            participant: mapWorkshopAdminParticipantRow(participant, {
lib\workshops\workshopDatabase.ts:1747:                participant_id: participant.id,
lib\workshops\workshopDatabase.ts:1847: * Loads every participant which matches the administration filter for a file export.
lib\workshops\workshopDatabase.ts:1849: * Note: The export keeps the exact same filter and sort as the participant table, while intentionally ignoring its
lib\workshops\workshopDatabase.ts:1857:    readonly participants: readonly WorkshopAdminParticipant[] | null;
lib\workshops\workshopDatabase.ts:1867:        return { participants: null, errorMessage: firstPageResult.errorMessage };
lib\workshops\workshopDatabase.ts:1870:    const participants = [...firstPageResult.page.participants];
lib\workshops\workshopDatabase.ts:1878:            return { participants: null, errorMessage: pageResult.errorMessage };
lib\workshops\workshopDatabase.ts:1881:        participants.push(...pageResult.page.participants);
lib\workshops\workshopDatabase.ts:1884:    return { participants, errorMessage: null };
lib\workshops\workshopDatabase.ts:1918:            participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:1929: * Reads enough participant identity to make exported reactions understandable, including artificial reactions whose
lib\workshops\workshopDatabase.ts:1930: * participant is intentionally absent.
lib\workshops\workshopDatabase.ts:1932:async function loadWorkshopParticipantIdentities(
lib\workshops\workshopDatabase.ts:1935:    participantIds: readonly string[],
lib\workshops\workshopDatabase.ts:1941:        participantIds,
lib\workshops\workshopDatabase.ts:1954:        identityByParticipantId: new Map(rows.map((participant) => [participant.id, participant] as const)),
lib\workshops\workshopDatabase.ts:1971:              readonly participantFullname: string | null;
lib\workshops\workshopDatabase.ts:1972:              readonly participantEmail: string | null;
lib\workshops\workshopDatabase.ts:1982:            .select('id, participant_id, emoji, created_at, is_artificial')
lib\workshops\workshopDatabase.ts:1992:    const participantIds = rows
lib\workshops\workshopDatabase.ts:1993:        .map((reaction) => reaction.participant_id)
lib\workshops\workshopDatabase.ts:1994:        .filter((participantId): participantId is string => participantId !== null);
lib\workshops\workshopDatabase.ts:1995:    const { identityByParticipantId, errorMessage: identityErrorMessage } = await loadWorkshopParticipantIdentities(
lib\workshops\workshopDatabase.ts:1998:        participantIds,
lib\workshops\workshopDatabase.ts:2006:            const participant =
lib\workshops\workshopDatabase.ts:2007:                reaction.participant_id === null ? undefined : identityByParticipantId.get(reaction.participant_id);
lib\workshops\workshopDatabase.ts:2012:                participantFullname: participant?.fullname ?? null,
lib\workshops\workshopDatabase.ts:2013:                participantEmail: participant?.email ?? null,
lib\workshops\workshopDatabase.ts:2062: * Feedback and participants stay separate source records, just as comments and reactions do. The small identity read
lib\workshops\workshopDatabase.ts:2063: * keeps this list attributable without ever exposing an e-mail address through the participant-facing state route.
lib\workshops\workshopDatabase.ts:2084:    const participantResult = await loadWorkshopRowsByIds<WorkshopFeedbackParticipantRow>(
lib\workshops\workshopDatabase.ts:2085:        feedbackResult.rows.map((feedback) => feedback.participant_id),
lib\workshops\workshopDatabase.ts:2086:        (participantIds) =>
lib\workshops\workshopDatabase.ts:2091:                .in('id', participantIds),
lib\workshops\workshopDatabase.ts:2093:    if (participantResult.rows === null) {
lib\workshops\workshopDatabase.ts:2094:        return { feedbacks: null, errorMessage: participantResult.errorMessage };
lib\workshops\workshopDatabase.ts:2097:    const participantById = new Map<string, WorkshopFeedbackParticipantRow>(
lib\workshops\workshopDatabase.ts:2098:        participantResult.rows.map((participant) => [participant.id, participant] as const),
lib\workshops\workshopDatabase.ts:2102:            .map((feedback) => mapWorkshopAdminFeedbackRow(feedback, participantById))
lib\workshops\workshopDatabase.ts:2211:    participant: WorkshopParticipant,
lib\workshops\workshopDatabase.ts:2228:    // Note: The membership decides which materials a member sees, so the room asks about it before it assembles what
lib\workshops\workshopDatabase.ts:2230:    //       not arrive keeps the paid materials hidden rather than letting them escape.
lib\workshops\workshopDatabase.ts:2235:            participant.email,
lib\workshops\workshopDatabase.ts:2238:            console.error('Failed to load the community membership of a workshop participant:', membershipErrorMessage);
lib\workshops\workshopDatabase.ts:2245:    const isModerationOffered = isWorkshopParticipantModerating(participant);
lib\workshops\workshopDatabase.ts:2271:    // Note: The materials are read as a paid member would read them, whoever is reading the room. Which of them reach
lib\workshops\workshopDatabase.ts:2310:        // wrap-up starts it must be available for the stage link even if its ordinary unlock time is later; unrelated
lib\workshops\workshopDatabase.ts:2311:        // scheduled materials keep their own timing.
lib\workshops\workshopDatabase.ts:2318:                  .eq('participant_id', participant.id)
lib\workshops\workshopDatabase.ts:2322:        isModerationOffered ? pendingCommentsQuery : pendingCommentsQuery.eq('participant_id', participant.id),
lib\workshops\workshopDatabase.ts:2332:        countWatchingWorkshopParticipants(supabase, workshopRow),
lib\workshops\workshopDatabase.ts:2337:        loadWorkshopPolls(supabase, workshopRow, participant.email),
lib\workshops\workshopDatabase.ts:2418:            .eq('participant_id', participant.id)
lib\workshops\workshopDatabase.ts:2437:                commentRow.participant_id === null ? undefined : authorByParticipantId.get(commentRow.participant_id);
lib\workshops\workshopDatabase.ts:2473:            participant,
lib\workshops\workshopDatabase.ts:2555:        participantCountResult,
lib\workshops\workshopDatabase.ts:2609:        participantCountResult.error ??
lib\workshops\workshopDatabase.ts:2637:        participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:2664:            participants: [],
lib\workshops\workshopDatabase.ts:2665:            participantCount: participantCountResult.count ?? 0,
lib\workshops\workshopConstants.ts:4:export const WORKSHOP_PARTICIPANT_TABLE_NAME = 'workshop_participants';
lib\workshops\workshopConstants.ts:58: * Note: The commits are read through one cached server-side answer however many participants have the room open, so
lib\workshops\workshopConstants.ts:59: *       the room can follow the project without turning every participant into a GitHub client.
lib\workshops\workshopConstants.ts:79: * How long after their last request a participant still counts as watching the workshop
lib\workshops\workshopConstants.ts:82: *       so this window stays above both to never drop an attentive participant out of the count.
lib\workshops\workshopConstants.ts:87: * How long one presence sample of a participant lasts, which is what the audience of the administration graph is
app\api\workshops\[workshopSlug]\state\route.ts:22:        authenticatedRequest.participant,
app\api\workshops\[workshopSlug]\repository\route.ts:12: * How far the project of this workshop has come, read for the participants who are in its room
app\api\workshops\[workshopSlug]\repository\route.ts:15: *       delays nothing but the panel which is about GitHub. Every participant reads one and the same server-side
app\api\workshops\[workshopSlug]\reactions\route.ts:45:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\reactions\route.ts:57:        authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\presence\route.ts:3:import { countWatchingWorkshopParticipants } from '@/lib/workshops/workshopDatabase';
app\api\workshops\[workshopSlug]\presence\route.ts:30:    const { error } = await authenticatedRequest.supabase.rpc('record_workshop_participant_presence', {
app\api\workshops\[workshopSlug]\presence\route.ts:32:        target_participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\presence\route.ts:37:        console.error('Failed to record workshop participant presence:', error.message);
app\api\workshops\[workshopSlug]\presence\route.ts:42:    const watchingParticipantCount = await countWatchingWorkshopParticipants(
lib\admin\adminSession.test.ts:90:        expect(getAdminRedirectPath('/cs/online-workshop')).toBe('/admin');
lib\admin\adminContactJoin.ts:7: * Note: This is a read model assembled only for the administration. The workshop participant and Contact rows stay
lib\admin\adminContactJoin.ts:11:    readonly participantId: string;
lib\admin\adminContactJoin.ts:32: * The feedback remains stored against its workshop participant. This is only the admin read model which lets a
lib\admin\adminContactJoin.ts:42:    readonly participantId: string;
lib\admin\adminContactJoin.ts:347:                `Participant ID: ${workshopParticipation.participantId}`,
lib\admin\adminContactJoin.ts:377:                `Participant ID: ${workshopFeedback.participantId}`,
lib\admin\adminContactJoin.test.ts:40:    participantId: 'participant-1',
lib\admin\adminContactJoin.test.ts:65:    participantId: WORKSHOP_PARTICIPATION.participantId,
lib\admin\adminContactJoin.test.ts:127:            [{ ...WORKSHOP_PARTICIPATION, participantId: 'participant-without-email', email: '' }],
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:44:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:52:        authenticatedRequest.participant,
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:71:        authenticatedRequest.participant.email,
lib\admin\adminContactDatabase.ts:23:    'id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at';
lib\admin\adminContactDatabase.ts:33:type AdminContactWorkshopParticipantRow = {
lib\admin\adminContactDatabase.ts:48:    readonly participant_id: string;
lib\admin\adminContactDatabase.ts:57:type AdminContactWorkshopParticipantActivityTotalsRow = {
lib\admin\adminContactDatabase.ts:59:    readonly participant_id: string;
lib\admin\adminContactDatabase.ts:94:function createWorkshopParticipantActivityTotalsKey(workshopId: string, participantId: string): string {
lib\admin\adminContactDatabase.ts:95:    return `${workshopId}:${participantId}`;
lib\admin\adminContactDatabase.ts:99:    participantRow: AdminContactWorkshopParticipantRow,
lib\admin\adminContactDatabase.ts:101:    activityTotalsByParticipationKey: ReadonlyMap<string, AdminContactWorkshopParticipantActivityTotalsRow>,
lib\admin\adminContactDatabase.ts:103:    const workshopRow = workshopById.get(participantRow.workshop_id);
lib\admin\adminContactDatabase.ts:109:        createWorkshopParticipantActivityTotalsKey(participantRow.workshop_id, participantRow.id),
lib\admin\adminContactDatabase.ts:113:        participantId: participantRow.id,
lib\admin\adminContactDatabase.ts:119:        fullname: participantRow.fullname,
lib\admin\adminContactDatabase.ts:120:        email: participantRow.email,
lib\admin\adminContactDatabase.ts:121:        connectedAt: participantRow.connected_at,
lib\admin\adminContactDatabase.ts:122:        lastSeenAt: participantRow.last_seen_at,
lib\admin\adminContactDatabase.ts:123:        activeDurationSeconds: participantRow.active_duration_seconds,
lib\admin\adminContactDatabase.ts:127:        isInteractionBanned: participantRow.is_interaction_banned,
lib\admin\adminContactDatabase.ts:128:        isTrusted: participantRow.is_trusted,
lib\admin\adminContactDatabase.ts:135:    participantById: ReadonlyMap<string, AdminContactWorkshopParticipantRow>,
lib\admin\adminContactDatabase.ts:138:    const participantRow = participantById.get(feedbackRow.participant_id);
lib\admin\adminContactDatabase.ts:139:    if (workshopRow === undefined || participantRow === undefined) {
lib\admin\adminContactDatabase.ts:150:        participantId: participantRow.id,
lib\admin\adminContactDatabase.ts:151:        fullname: participantRow.fullname,
lib\admin\adminContactDatabase.ts:152:        email: participantRow.email,
lib\admin\adminContactDatabase.ts:163: * Read the already-aggregated activity of every workshop participant in one stable, paged query.
lib\admin\adminContactDatabase.ts:165: * The workshop participant page uses the very same database aggregate for one workshop. This multi-workshop variant
lib\admin\adminContactDatabase.ts:168:async function loadAdminWorkshopParticipantActivityTotals(
lib\admin\adminContactDatabase.ts:172:    readonly activityTotals: readonly AdminContactWorkshopParticipantActivityTotalsRow[] | null;
lib\admin\adminContactDatabase.ts:179:    const { rows, errorMessage } = await loadAllSupabaseRows<AdminContactWorkshopParticipantActivityTotalsRow>(
lib\admin\adminContactDatabase.ts:182:                .rpc('get_workshop_participant_activity_totals_for_workshops', {
lib\admin\adminContactDatabase.ts:186:                .order('participant_id', { ascending: true })
lib\admin\adminContactDatabase.ts:188:        '`get_workshop_participant_activity_totals_for_workshops`',
lib\admin\adminContactDatabase.ts:212:    const [workshopRowsResult, participantRowsResult, feedbackRowsResult] = await Promise.all([
lib\admin\adminContactDatabase.ts:222:        loadAllSupabaseRows<AdminContactWorkshopParticipantRow>(
lib\admin\adminContactDatabase.ts:245:        workshopRowsResult.errorMessage ?? participantRowsResult.errorMessage ?? feedbackRowsResult.errorMessage;
lib\admin\adminContactDatabase.ts:246:    if (workshopRowsResult.rows === null || participantRowsResult.rows === null || feedbackRowsResult.rows === null) {
lib\admin\adminContactDatabase.ts:253:    const participantById = new Map<string, AdminContactWorkshopParticipantRow>(
lib\admin\adminContactDatabase.ts:254:        participantRowsResult.rows.map((participantRow) => [participantRow.id, participantRow] as const),
lib\admin\adminContactDatabase.ts:257:        ? await loadAdminWorkshopParticipantActivityTotals(
lib\admin\adminContactDatabase.ts:266:    const activityTotalsByParticipationKey = new Map<string, AdminContactWorkshopParticipantActivityTotalsRow>(
lib\admin\adminContactDatabase.ts:270:                    createWorkshopParticipantActivityTotalsKey(
lib\admin\adminContactDatabase.ts:272:                        activityTotals.participant_id,
lib\admin\adminContactDatabase.ts:279:        ? participantRowsResult.rows
lib\admin\adminContactDatabase.ts:280:              .map((participantRow) =>
lib\admin\adminContactDatabase.ts:281:                  mapAdminWorkshopParticipation(participantRow, workshopById, activityTotalsByParticipationKey),
lib\admin\adminContactDatabase.ts:289:        .map((feedbackRow) => mapAdminWorkshopFeedback(feedbackRow, workshopById, participantById))
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:48:const WORKSHOP_SLUG = 'online-workshop-git-ai-2026-09-07';
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:53:const PARTICIPANT = { id: 'participant-id', email: 'jana@example.com', isInteractionBanned: false };
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:97:            participant: PARTICIPANT,
lib\admin\adminContactDatabase.test.ts:68:        const participantQuery = createPagedQuery([
lib\admin\adminContactDatabase.test.ts:70:                id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:84:                participant_id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:94:                participant_id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:110:                return participantQuery;
lib\admin\adminContactDatabase.test.ts:133:        expect(rpc).toHaveBeenCalledWith('get_workshop_participant_activity_totals_for_workshops', {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:4:    getUnofferedWorkshopParticipantModerationFieldNames,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:5:    isWorkshopParticipantModeratedBy,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:8:    findModeratedWorkshopParticipant,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:9:    updateModeratedWorkshopParticipant,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:16:type WorkshopParticipantModerationRouteContext = {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:17:    readonly params: Promise<{ readonly workshopSlug: string; readonly participantId: string }>;
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:21: * Trusts one participant of the room or takes their interactions away, straight from the chat
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:26:export async function PATCH(request: NextRequest, context: WorkshopParticipantModerationRouteContext) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:38:    const { workshopSlug, participantId } = await context.params;
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:44:    const unofferedFieldNames = getUnofferedWorkshopParticipantModerationFieldNames('moderator', parsedResult.data);
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:49:    const moderatedParticipant = await findModeratedWorkshopParticipant(
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:52:        participantId,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:55:        console.error('Failed to read a moderated workshop participant:', moderatedParticipant.errorMessage);
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:58:    if (moderatedParticipant.participant === null) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:61:    if (!isWorkshopParticipantModeratedBy('moderator', moderatedParticipant.participant)) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:68:    const { participant, errorMessage } = await updateModeratedWorkshopParticipant(
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:71:        participantId,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:75:        console.error('Failed to moderate a workshop participant from the room:', errorMessage);
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:78:    if (participant === null) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:86:    return NextResponse.json(participant, { headers: { 'Cache-Control': 'no-store' } });
app\api\workshops\[workshopSlug]\participant\route.ts:4:import { renameWorkshopParticipant } from '@/lib/workshops/workshopParticipantRename';
app\api\workshops\[workshopSlug]\participant\route.ts:8:import { clearWorkshopSessionCookie, endWorkshopParticipantSession } from '@/lib/workshops/workshopSession';
app\api\workshops\[workshopSlug]\participant\route.ts:11:type WorkshopParticipantRouteContext = {
app\api\workshops\[workshopSlug]\participant\route.ts:15:export async function PATCH(request: NextRequest, context: WorkshopParticipantRouteContext) {
app\api\workshops\[workshopSlug]\participant\route.ts:33:    // A banned participant keeps the name they were moderated under, because renaming would also rewrite the author
app\api\workshops\[workshopSlug]\participant\route.ts:35:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\participant\route.ts:40:    const renameResult = await renameWorkshopParticipant(
app\api\workshops\[workshopSlug]\participant\route.ts:43:        authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\participant\route.ts:50:    // Nobody else needs a fresh snapshot when the new name stayed inside the room of the renamed participant.
app\api\workshops\[workshopSlug]\participant\route.ts:57:    const response = NextResponse.json({ participant: renameResult.participant });
app\api\workshops\[workshopSlug]\participant\route.ts:63: * Signs the participant out of this room, which is the way back out of a room they connected to themselves.
app\api\workshops\[workshopSlug]\participant\route.ts:65: * Note: A participant who may not interact is signed out like anybody else. Their ban holds their name and their
app\api\workshops\[workshopSlug]\participant\route.ts:68:export async function DELETE(request: NextRequest, context: WorkshopParticipantRouteContext) {
app\api\workshops\[workshopSlug]\participant\route.ts:81:        const isSessionEnded = await endWorkshopParticipantSession(
app\api\workshops\[workshopSlug]\participant\route.ts:84:            authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\participant\route.ts:87:            // The cookie is deliberately left alone, so a participant told that signing out failed is really still in.
lib\metadata\structured-data.ts:196: * for the private participant room.
app\api\workshops\[workshopSlug]\membership\route.ts:30:        authenticatedRequest.participant.email,
app\api\workshops\[workshopSlug]\membership\portal\route.ts:44:    const { participant, supabase, workshopRow } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\portal\route.ts:45:    const membershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:52:    maximum_participant_count: null,
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:60:    event_type: 'online-workshop',
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:66:    return { participant: { email: 'jana@example.com' }, supabase: SUPABASE, workshopRow };
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:149:            return_url: 'https://promptbook.studio/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty',
lib\metadata\page-registry.ts:13:import { ONLINE_WORKSHOP_PAGE_DEFINITION } from '@/businesses/online-workshop/onlineWorkshopMetadata';
app\cs\komunita\clenstvi\page.tsx:8:import { readWorkshopParticipantIdentity } from '@/lib/workshops/workshopParticipantLink';
app\cs\komunita\clenstvi\page.tsx:19:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\komunita\clenstvi\page.tsx:35:            initialFullname={participantIdentity.fullname}
app\cs\komunita\clenstvi\page.tsx:36:            initialEmail={participantIdentity.email}
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:162:            requestedByParticipantId: member.participantId,
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:207:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:208:    const currentMembership = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:232:        member: { participantId: participant.id, fullname: participant.fullname, email: participant.email },
lib\workshops\workshopChatInteractivity.test.ts:5:    it('offers writing and voting in a chat which belongs to the participants', () => {
lib\workshops\workshopChatInteractivity.test.ts:15:    it('leaves the form to a banned participant, so nothing tells them about their ban', () => {
lib\workshops\workshopChatInteractivity.test.ts:42:    it('keeps a moderator moderating a chat which the participants of the room cannot write into', () => {
app\api\workshops\[workshopSlug]\membership\checkout\confirmation\route.ts:52:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\checkout\confirmation\route.ts:62:    if (checkoutMemberEmail !== normalizeCommunityMemberEmail(participant.email)) {
app\cs\komunita\calendar.ics\route.test.ts:17:        id: 'online-workshop-id',
app\cs\komunita\calendar.ics\route.test.ts:95:            'URL:https://ptbk.io/cs/online-workshop/participant?workshop=production-ai-2026-09-10',
lib\legal\termsAndConditionsDocuments.tsx:192:                    'The membership page describes the free community and the current paid membership. Live online AI webinars remain free; paid membership primarily gives access to recordings, materials, and additional content.',
lib\legal\termsAndConditionsDocuments.tsx:218:                    'The content of the website, the workshop materials, texts, graphics, and software are protected by copyright and belong to the operator or to its partners.',
lib\legal\termsAndConditionsDocuments.tsx:219:                    'You may use the materials you receive from us for your own needs and for the needs of your company. You must not distribute, sell, or build your own training on them without our written consent.',
lib\workshops\workshopClientState.test.ts:123:    it('uses server time so an incorrect participant clock cannot delay content', () => {
lib\workshops\workshopParticipantModeration.ts:3:import { createWorkshopParticipantUpdateDatabaseValues } from '@/lib/workshops/workshopValues';
lib\workshops\workshopParticipantModeration.ts:7:type WorkshopParticipantUpdateValues = z.infer<typeof workshopParticipantUpdateSchema>;
lib\workshops\workshopParticipantModeration.ts:10: * The moderation state of one participant after it was written
lib\workshops\workshopParticipantModeration.ts:12:export type ModeratedWorkshopParticipant = {
lib\workshops\workshopParticipantModeration.ts:13:    readonly participantId: string;
lib\workshops\workshopParticipantModeration.ts:19:type ModeratedWorkshopParticipantRow = {
lib\workshops\workshopParticipantModeration.ts:28:function mapModeratedWorkshopParticipantRow(row: ModeratedWorkshopParticipantRow): ModeratedWorkshopParticipant {
lib\workshops\workshopParticipantModeration.ts:30:        participantId: row.id,
lib\workshops\workshopParticipantModeration.ts:38: * Reads the moderation state one participant of one room carries right now
lib\workshops\workshopParticipantModeration.ts:43:export async function findModeratedWorkshopParticipant(
lib\workshops\workshopParticipantModeration.ts:46:    participantId: string,
lib\workshops\workshopParticipantModeration.ts:47:): Promise<{ readonly participant: ModeratedWorkshopParticipant | null; readonly errorMessage: string | null }> {
lib\workshops\workshopParticipantModeration.ts:51:        .eq('id', participantId)
lib\workshops\workshopParticipantModeration.ts:56:        return { participant: null, errorMessage: error.message };
lib\workshops\workshopParticipantModeration.ts:60:        participant: data === null ? null : mapModeratedWorkshopParticipantRow(data as ModeratedWorkshopParticipantRow),
lib\workshops\workshopParticipantModeration.ts:66: * Writes the moderation state of one participant of one room and reads it back
lib\workshops\workshopParticipantModeration.ts:70: * Note: The participant is looked for inside its own workshop, so no request can moderate somebody of another room.
lib\workshops\workshopParticipantModeration.ts:72:export async function updateModeratedWorkshopParticipant(
lib\workshops\workshopParticipantModeration.ts:75:    participantId: string,
lib\workshops\workshopParticipantModeration.ts:76:    values: WorkshopParticipantUpdateValues,
lib\workshops\workshopParticipantModeration.ts:77:): Promise<{ readonly participant: ModeratedWorkshopParticipant | null; readonly errorMessage: string | null }> {
lib\workshops\workshopParticipantModeration.ts:80:        .update(createWorkshopParticipantUpdateDatabaseValues(values))
lib\workshops\workshopParticipantModeration.ts:81:        .eq('id', participantId)
lib\workshops\workshopParticipantModeration.ts:87:        return { participant: null, errorMessage: error.message };
lib\workshops\workshopParticipantModeration.ts:91:        participant: data === null ? null : mapModeratedWorkshopParticipantRow(data as ModeratedWorkshopParticipantRow),
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:60:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:61:    const membershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:87:    const refreshedMembershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
lib\workshops\workshopChatInteractivity.ts:2: * What the chat still offers to one participant
lib\workshops\workshopChatInteractivity.ts:4: * Note: Both the switch of the whole panel and the ban of a single participant end here, so every part of the chat
lib\workshops\workshopChatInteractivity.ts:29:     * Whether this participant moderates the room, see `isWorkshopParticipantModerating`
lib\workshops\workshopChatInteractivity.ts:35: * Note: A banned participant keeps the form, because they must not learn about their ban. Their messages are rejected
lib\workshops\workshopChatInteractivity.ts:37: * Note: A switched-off chat is switched off for the participants of the room. A moderator keeps moderating it, exactly
app\api\workshops\[workshopSlug]\membership\cancellation\route.test.ts:50:    participant: { email: 'jana@example.com' },
lib\legal\privacyPolicyDocuments.tsx:169:                    'Data about an event registration: the chosen date, the number of participants, the format, and any discount code.',
lib\legal\privacyPolicyDocuments.tsx:179:                    'To register you for a workshop or another event, issue an invoice, and send you the materials - based on the performance of the contract and our legal obligations.',
lib\workshops\workshopParticipantFullname.ts:4: * Cleans a participant name the same way it is stored, so that a form never validates something else than the server
lib\workshops\workshopParticipantFullname.ts:6:export function normalizeWorkshopParticipantFullname(fullname: string): string {
lib\workshops\workshopParticipantFullname.ts:11: * The single rule for a participant name, shared by the connection form, the rename form and the API schemas
lib\workshops\workshopParticipantFullname.ts:13:export function isWorkshopParticipantFullnameValid(fullname: string): boolean {
lib\workshops\workshopParticipantFullname.ts:14:    const normalizedFullname = normalizeWorkshopParticipantFullname(fullname);
lib\workshops\workshopParticipantEmail.ts:2: * The one form of a participant address used to decide durable identity across rooms.
lib\workshops\workshopParticipantEmail.ts:4:export function normalizeWorkshopParticipantEmail(email: string): string {
app\api\workshops\[workshopSlug]\feedback\route.ts:16: * Persists one step of the participant's post-workshop feedback.
app\api\workshops\[workshopSlug]\feedback\route.ts:18: * A score initializes the one row for a participant. The optional text fields deliberately update that row afterwards,
app\api\workshops\[workshopSlug]\feedback\route.ts:19: * allowing each answer to survive independently if the participant does not continue through the whole form.
app\api\workshops\[workshopSlug]\feedback\route.ts:56:                  .eq('participant_id', authenticatedRequest.participant.id)
app\api\workshops\[workshopSlug]\feedback\route.ts:62:                          participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\feedback\route.ts:65:                      { onConflict: 'workshop_id,participant_id' },
app\api\workshops\[workshopSlug]\feedback\route.ts:67:    const { data, error } = await feedbackQuery.select('id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at').maybeSingle();
lib\workshops\workshopPanels.ts:5: * One part of the participant room which an admin can take away from the participants of a single workshop
lib\workshops\workshopPanels.ts:29: * Every switchable panel of the participant room, in the order the administration offers them
lib\workshops\workshopPanels.ts:82: * Whether the room still offers a panel to its participants
app\branding\page.tsx:103:                                    presentations, event materials, partner pages, and articles.
lib\workshops\workshopParticipantInteraction.ts:3:import type { WorkshopParticipant, WorkshopSubmissionStatus } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopParticipantInteraction.ts:10:export function getWorkshopParticipantSubmissionStatus(
lib\workshops\workshopParticipantInteraction.ts:11:    participant: Pick<WorkshopParticipant, 'isInteractionBanned' | 'isTrusted' | 'isModerator'>,
lib\workshops\workshopParticipantInteraction.ts:13:    if (participant.isInteractionBanned) {
lib\workshops\workshopParticipantInteraction.ts:17:    return participant.isTrusted || participant.isModerator ? 'approved' : 'pending';
lib\workshops\workshopParticipantInteraction.ts:20:export function getWorkshopInteractionBanResponseOrNull(participant: WorkshopParticipant): NextResponse | null {
lib\workshops\workshopParticipantInteraction.ts:21:    return participant.isInteractionBanned
lib\workshops\workshopParticipantInteraction.ts:30: * Refuses an action of a participant which the room does not offer anymore
lib\workshops\workshopParticipantInteraction.ts:34: * Note: Only participants are held back here. The administration writes through its own routes and therefore keeps
app\api\workshops\[workshopSlug]\connect\route.ts:10:import { createWorkshopParticipant, setWorkshopSessionCookie } from '@/lib/workshops/workshopSession';
app\api\workshops\[workshopSlug]\connect\route.ts:46:    const connection = await createWorkshopParticipant(
app\api\workshops\[workshopSlug]\connect\route.ts:58:    // participant authentication request when many attendees join at once.
app\api\workshops\[workshopSlug]\connect\route.ts:62:        connection.participant,
app\api\workshops\[workshopSlug]\connect\route.ts:67:        // endpoint without creating a duplicate participant row.
lib\workshops\workshopParticipantLink.test.ts:2:    createWorkshopParticipantLink,
lib\workshops\workshopParticipantLink.test.ts:11:    it('keeps the selected occurrence in a participant link together with the prefilled identity', () => {
lib\workshops\workshopParticipantLink.test.ts:13:            createWorkshopParticipantLink(
lib\workshops\workshopParticipantLink.test.ts:14:                '/cs/online-workshop/participant',
lib\workshops\workshopParticipantLink.test.ts:19:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
lib\workshops\workshopParticipantLink.test.ts:23:    it('keeps the selected occurrence even without a prefilled participant identity', () => {
lib\workshops\workshopParticipantLink.test.ts:24:        expect(createWorkshopSelectionPath('/cs/online-workshop/participant', WORKSHOP_SLUG)).toBe(
lib\workshops\workshopParticipantLink.test.ts:25:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10',
lib\workshops\workshopParticipantLink.test.ts:28:            createWorkshopParticipantLink(
lib\workshops\workshopParticipantLink.test.ts:29:                '/cs/online-workshop/participant',
lib\workshops\workshopParticipantRename.ts:3:    mapWorkshopParticipantRow,
lib\workshops\workshopParticipantRename.ts:5:    type WorkshopParticipantRow,
lib\workshops\workshopParticipantRename.ts:7:import type { WorkshopParticipant } from '@/lib/workshops/workshopTypes';
lib\workshops\workshopParticipantRename.ts:10:type WorkshopParticipantRenameResult = {
lib\workshops\workshopParticipantRename.ts:11:    readonly participant: WorkshopParticipant;
lib\workshops\workshopParticipantRename.ts:24: * Renames a participant everywhere their name is shown: in the room header and on every comment they wrote.
lib\workshops\workshopParticipantRename.ts:28: * Note: The comments are renamed after the participant, because a participant renamed with stale comments still leads
lib\workshops\workshopParticipantRename.ts:29: *       to the name they asked for, while renamed comments of a participant who kept their old name would not.
lib\workshops\workshopParticipantRename.ts:31:export async function renameWorkshopParticipant(
lib\workshops\workshopParticipantRename.ts:34:    participantId: string,
lib\workshops\workshopParticipantRename.ts:36:): Promise<WorkshopParticipantRenameResult | null> {
lib\workshops\workshopParticipantRename.ts:37:    const { data: participantRow, error: participantError } = await supabase
lib\workshops\workshopParticipantRename.ts:40:        .eq('id', participantId)
lib\workshops\workshopParticipantRename.ts:45:    if (participantError || participantRow === null) {
lib\workshops\workshopParticipantRename.ts:47:            'Failed to rename a workshop participant:',
lib\workshops\workshopParticipantRename.ts:48:            participantError?.message ?? 'No participant returned',
lib\workshops\workshopParticipantRename.ts:53:    const participant = mapWorkshopParticipantRow(participantRow as WorkshopParticipantRow);
lib\workshops\workshopParticipantRename.ts:58:        .eq('participant_id', participantId)
lib\workshops\workshopParticipantRename.ts:63:        // The participant already carries their new name, so the rename is reported as done and only the older
lib\workshops\workshopParticipantRename.ts:65:        console.error('Failed to rename the comments of a workshop participant:', commentsError.message);
lib\workshops\workshopParticipantRename.ts:66:        return { participant, isVisibleCommentRenamed: false };
lib\workshops\workshopParticipantRename.ts:70:        participant,
app\api\workshops\[workshopSlug]\comments\[commentId]\upvotes\route.ts:33:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\comments\[commentId]\upvotes\route.ts:56:        participant_id: authenticatedRequest.participant.id,
lib\workshops\workshopPollSharing.test.ts:16:    slug: 'online-workshop-git-ai-2026-09-07',
lib\workshops\workshopPollSharing.test.ts:27:    event_type: 'online-workshop',
lib\workshops\workshopPollSharing.test.ts:31:    maximum_participant_count: null,
lib\workshops\workshopPollSharing.test.ts:170:    it('passes the verified e-mail and current participant to the one shared vote procedure', async () => {
lib\workshops\workshopPollSharing.test.ts:177:            { id: 'workshop-participant-id', email: MEMBER_EMAIL },
lib\workshops\workshopPollSharing.test.ts:187:            target_participant_id: 'workshop-participant-id',
app\api\workshops\[workshopSlug]\comments\route.ts:12:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
app\api\workshops\[workshopSlug]\comments\route.ts:15:    getWorkshopParticipantSubmissionStatus,
app\api\workshops\[workshopSlug]\comments\route.ts:97:    const commentStatus = getWorkshopParticipantSubmissionStatus(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\comments\route.ts:103:            participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\comments\route.ts:105:            author_name: authenticatedRequest.participant.fullname,
app\api\workshops\[workshopSlug]\comments\route.ts:120:    const { participant } = authenticatedRequest;
app\api\workshops\[workshopSlug]\comments\route.ts:122:    if (areWorkshopCommentLinksEnabled({ isArtificial: false, isAuthorModerator: participant.isModerator })) {
app\api\workshops\[workshopSlug]\comments\route.ts:138:    // The participant who just wrote this message is the only author it can have, so it is described from them.
app\api\workshops\[workshopSlug]\comments\route.ts:141:        authorByParticipantId: new Map([[participant.id, createWorkshopCommentAuthor(participant)]]),
app\api\workshops\[workshopSlug]\comments\route.ts:142:        isModerationOffered: isWorkshopParticipantModerating(participant),
lib\language\pageLanguage.test.ts:15:        expect(getLanguageFromPathname('/cs/online-workshop/participant')).toBe('cs');
lib\language\pageLanguage.test.ts:16:        expect(getLanguageFromPathname('/ai-supervize-mini/participant')).toBe('cs');
lib\workshops\workshopParticipantLink.ts:12:export type WorkshopParticipantIdentity = {
lib\workshops\workshopParticipantLink.ts:21:function readWorkshopParticipantValue(value: SearchParameterValue, maximalLength: number): string {
lib\workshops\workshopParticipantLink.ts:26: * Reads participant details from URL search parameters using the same limits as the participant connection form.
lib\workshops\workshopParticipantLink.ts:28:export function readWorkshopParticipantIdentity(
lib\workshops\workshopParticipantLink.ts:31:): WorkshopParticipantIdentity {
lib\workshops\workshopParticipantLink.ts:33:        email: readWorkshopParticipantValue(email, MAXIMAL_WORKSHOP_PARTICIPANT_EMAIL_LENGTH),
lib\workshops\workshopParticipantLink.ts:34:        fullname: readWorkshopParticipantValue(fullname, MAXIMAL_WORKSHOP_PARTICIPANT_FULLNAME_LENGTH),
lib\workshops\workshopParticipantLink.ts:72: * Creates the query parameters used to prefill a workshop participant connection form.
lib\workshops\workshopParticipantLink.ts:74: * Invalid or incomplete values do not produce a link. The participant room still validates its form server-side, but
lib\workshops\workshopParticipantLink.ts:77:export function createWorkshopParticipantSearchParameters(
lib\workshops\workshopParticipantLink.ts:78:    identity: WorkshopParticipantIdentity,
lib\workshops\workshopParticipantLink.ts:80:    const normalizedIdentity = readWorkshopParticipantIdentity(identity.email, identity.fullname);
lib\workshops\workshopParticipantLink.ts:99: * Note: This is what leads a participant into the one permanent room of the application, whose address is decided once
lib\workshops\workshopParticipantLink.ts:103:export function createParticipantIdentityPath(path: string, identity: WorkshopParticipantIdentity): string {
lib\workshops\workshopParticipantLink.ts:104:    const searchParameters = createWorkshopParticipantSearchParameters(identity);
lib\workshops\workshopParticipantLink.ts:110: * Creates a site-relative participant URL with prefilled connection details.
lib\workshops\workshopParticipantLink.ts:112:export function createWorkshopParticipantLink(
lib\workshops\workshopParticipantLink.ts:113:    participantPath: string,
lib\workshops\workshopParticipantLink.ts:114:    identity: WorkshopParticipantIdentity,
lib\workshops\workshopParticipantLink.ts:117:    const searchParameters = createWorkshopParticipantSearchParameters(identity);
lib\workshops\workshopParticipantLink.ts:122:    return createWorkshopSelectionPath(participantPath, workshopSlug, searchParameters);
lib\workshops\workshopParticipantLink.ts:132:    participantPath: string,
lib\workshops\workshopParticipantLink.ts:133:    identity: WorkshopParticipantIdentity,
lib\workshops\workshopParticipantLink.ts:137:        createWorkshopParticipantLink(participantPath, identity, workshopSlug) ??
lib\workshops\workshopParticipantLink.ts:138:        createWorkshopSelectionPath(participantPath, workshopSlug)
app\api\workshops\komunita\projects\[projectId]\vote\route.ts:27:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\komunita\projects\[projectId]\vote\route.ts:43:    const savedVote = await setCommunityProjectVote(projectId, authenticatedRequest.participant.id, parsedResult.data.vote);
app\api\workshops\komunita\projects\[projectId]\route.ts:12:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
app\api\workshops\komunita\projects\[projectId]\route.ts:33:    if (!isWorkshopParticipantModerating(authenticatedRequest.participant)) {
app\api\contacts\export\[formatId]\route.test.ts:49:    slug: 'online-workshop-2026-08-26',
app\api\contacts\export\[formatId]\route.test.ts:127:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
app\api\contacts\export\[formatId]\route.test.ts:133:                userNote: JSON.stringify({ selectedDateId: 'online-workshop-2026-09-02', participantCount: 1 }),
app\api\contacts\export\[formatId]\route.test.ts:139:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
app\api\ai-supervize-mini\registration\route.ts:62:    const participantCount = readParticipantCount(body.participantCount);
app\api\ai-supervize-mini\registration\route.ts:73:        participantCount === null ||
app\api\ai-supervize-mini\registration\route.ts:88:        participantCount,
app\api\ai-supervize-mini\registration\route.ts:138:    if (maximumParticipantCount !== null && registrationRequest.participantCount > maximumParticipantCount) {
app\api\ai-supervize-mini\registration\route.ts:165:        registrationRequest.participantCount,
app\api\ai-supervize-mini\registration\route.ts:239:            registrationRequest.participantCount,
app\api\ai-supervize-mini\registration\route.ts:244:            registrationRequest.participantCount,
lib\workshops\workshopPublic.ts:37: * Note: This is what a room offers a participant to choose from, so it deliberately says more than the landing page
lib\workshops\workshopPublic.ts:78: * Loads the one published community room. Its data model is shared with workshops so participants get the same
app\api\ai-supervize-mini\registration\route.test.ts:88:            participantCount: 2,
app\api\ai-supervize-mini\registration\route.test.ts:170:            participantCount: 2,
app\api\ai-supervize-mini\registration\route.test.ts:180:        const response = await POST(createRegistrationRequest({ participantCount: 9 }));
app\api\workshops\komunita\projects\route.ts:14:import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
app\api\workshops\komunita\projects\route.ts:43:        authenticatedRequest.participant,
app\api\workshops\komunita\projects\route.ts:54:            isModerationOffered: isWorkshopParticipantModerating(authenticatedRequest.participant),
app\api\workshops\komunita\projects\route.ts:95:        communityParticipantId: authenticatedRequest.participant.id,
app\api\workshops\komunita\projects\route.ts:109:        authenticatedRequest.participant,
app\api\workshops\komunita\projects\[projectId]\connect\route.ts:41:    const connectedDiscussion = await connectCommunityProjectDiscussion(projectId, authenticatedRequest.participant.id);
app\api\admin\workshops\[workshopId]\stage-comment\route.test.ts:64:            workshopRow: { room_kind: 'workshop', slug: 'online-workshop-2026-08-20' },
app\api\admin\workshops\[workshopId]\stage-comment\route.test.ts:80:            { room_kind: 'workshop', slug: 'online-workshop-2026-08-20' },
app\api\admin\workshops\[workshopId]\stage-comment\route.test.ts:94:            { room_kind: 'workshop', slug: 'online-workshop-2026-08-20' },
app\api\admin\workshops\route.test.ts:56:            eventType: 'online-workshop',
app\api\admin\workshops\[workshopId]\content\route.ts:63:        // The source Markdown was persisted safely. The participant-state load
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:56:        case 'participants':
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:57:        case 'participants-vcard': {
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:59:            const [participantsResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:66:            const { participants, errorMessage } = participantsResult;
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:67:            if (participants === null) {
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:76:                participants: participants.map((participant) =>
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:77:                    joinAdminContactGroup(participant, contactGroupsResult.groups ?? []),
app\api\admin\workshops\[workshopId]\feedback\route.test.ts:34:    participantId: 'participant-1',
app\api\admin\workshops\[workshopId]\participants\route.ts:14:type AdminWorkshopParticipantsRouteContext = {
app\api\admin\workshops\[workshopId]\participants\route.ts:21:export async function GET(request: NextRequest, context: AdminWorkshopParticipantsRouteContext) {
app\api\admin\workshops\[workshopId]\participants\route.ts:34:    const [participantPageResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\participants\route.ts:41:    const { page, errorMessage } = participantPageResult;
app\api\admin\workshops\[workshopId]\participants\route.ts:56:              page.participants.map((participant) => participant.email),
app\api\admin\workshops\[workshopId]\participants\route.ts:66:            participants: page.participants.map((participant) => {
app\api\admin\workshops\[workshopId]\participants\route.ts:67:                const participantWithContactGroup = joinAdminContactGroup(participant, contactGroupsResult.groups ?? []);
app\api\admin\workshops\[workshopId]\participants\route.ts:71:                          ...participantWithContactGroup,
app\api\admin\workshops\[workshopId]\participants\route.ts:74:                              participant.email,
app\api\admin\workshops\[workshopId]\participants\route.ts:77:                    : participantWithContactGroup;
app\api\admin\workshops\[workshopId]\participants\route.test.ts:46:    id: 'participant-1',
app\api\admin\workshops\[workshopId]\participants\route.test.ts:63:    return new NextRequest('https://promptbook.studio/api/admin/workshops/community-id/participants');
app\api\admin\workshops\[workshopId]\participants\route.test.ts:66:describe('admin community participants membership projection', () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:77:        joinAdminContactGroupMock.mockImplementation((participant) => ({ ...participant, contactGroup: null }));
app\api\admin\workshops\[workshopId]\participants\route.test.ts:80:            page: { participants: [PARTICIPANT], totalCount: 1 },
app\api\admin\workshops\[workshopId]\participants\route.test.ts:85:    it('adds the membership state to each community participant in one batched membership lookup', async () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:98:        const responseBody = (await response.json()) as { readonly participants: unknown };
app\api\admin\workshops\[workshopId]\participants\route.test.ts:99:        expect(responseBody.participants).toEqual([
app\api\admin\workshops\[workshopId]\participants\route.test.ts:106:    it('does not fetch paid membership information for an ordinary workshop participant list', async () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:118:    it('rejects a request before querying either participant or payment data when the administrator is not signed in', async () => {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:8:import { updateModeratedWorkshopParticipant } from '@/lib/workshops/workshopParticipantModeration';
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:11:type AdminWorkshopParticipantRouteContext = {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:12:    readonly params: Promise<{ readonly workshopId: string; readonly participantId: string }>;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:15:export async function PATCH(request: NextRequest, context: AdminWorkshopParticipantRouteContext) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:27:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:34:    const { participant, errorMessage } = await updateModeratedWorkshopParticipant(
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:37:        participantId,
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:43:    if (participant === null) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:48:    return NextResponse.json(participant);
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:51:export async function DELETE(request: NextRequest, context: AdminWorkshopParticipantRouteContext) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:57:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:65:        const deletedCommunityParticipant = await deleteCommunityParticipantWithProjectVotes(participantId);
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:66:        if (deletedCommunityParticipant.participantId === null) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:69:                    'Failed to delete a community participant and reconcile their project votes:',
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:84:        deletedParticipantId = deletedCommunityParticipant.participantId;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:89:            .eq('id', participantId)
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:104:    return NextResponse.json({ participantId: deletedParticipantId });
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:8:type AdminWorkshopParticipantTimelineRouteContext = {
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:9:    readonly params: Promise<{ readonly workshopId: string; readonly participantId: string }>;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:13: * Serves the full event history only after an administrator opens a participant's detail.
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:15:export async function GET(request: NextRequest, context: AdminWorkshopParticipantTimelineRouteContext) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:21:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:27:    const [participantTimelineResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:28:        loadWorkshopAdminParticipantTimeline(workshopData.supabase, workshopData.workshopRow, participantId),
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:31:    const { timeline, errorMessage } = participantTimelineResult;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:47:            participant: joinAdminContactGroup(timeline.participant, contactGroupsResult.groups ?? []),
Path
----
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files app components lib tests | rg "(online-workshop|WorkshopParticipant|workshop.*(Room|State|Material|Public)|participant.*workshop|Workshop.*Participant)"; Get-Content -Raw AGENTS.md; Get-Content -TotalCount 45 changelog/_current-preversion.md' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 324ms:
app\cs\online-workshop\participant\page.tsx
app\cs\online-workshop\participant\opengraph-image.tsx
app\cs\online-workshop\page.tsx
app\cs\online-workshop\opengraph-image.tsx
app\cs\online-workshop\dekujeme\page.tsx
lib\workshops\workshopSpecialMaterials.ts
lib\workshops\workshopSpecialMaterials.test.ts
lib\workshops\workshopPublic.ts
lib\workshops\workshopOverviewGraphState.ts
lib\workshops\workshopMaterialLinks.ts
lib\workshops\workshopMaterialLinks.test.ts
lib\workshops\workshopClientState.ts
lib\workshops\workshopClientState.test.ts
lib\workshops\workshopAdminViewState.ts
lib\workshops\workshopAdminViewState.test.ts
## Context

This repository contains Promptbook landing pages for different businesses,
use cases, and audiences. Keep these rules current when behavior changes.

### Public routes

- `/` redirects to `/cs` or `/en` using `Accept-Language`.
- `/cs` is the Czech homepage and source of truth for homepage structure and copy.
- `/en` is its English localization.
- `/pro-mesta`, `/pro-firmy`, `/for-agro`, `/for-industry`, `/ai-supervize`,
  `/hackathon-factory`, and `/pavol` are specialized landing pages. `/pavol`
  redirects to `/cs/pavol` or `/en/pavol`; the localized pages are Pavol Hejny's
  personal pages.
- `/ai-supervize-mini` is the Czech one-day AI Supervize page. Published terms,
  prices, capacities, places, FAQs, registration, and participant information
  come from `/admin/workshops`; with no published term it shows a notice.
  `/skoleni` redirects there.
- `/cs/online-workshop` lists free 60-minute online workshops about writing
  production code with AI agents. Each term has its own subject and description,
  but all use one registration form. `/cs/online-workshop/dekujeme` is the
  full-load conversion page; `/participant` is the live room. Its waiting room
  offers every published term as the same term cards the landing page registers
  with: running and upcoming ones first, finished ones behind a disclosure.
  Picking one changes the room being connected to and the `workshop` parameter,
  without losing the name and e-mail already typed.
- `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
  merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
  the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
  `application/rss+xml`.
  Each episode lists every credited person by name, and every person any source
  credits has a card in `aiTaKrajtaPeople.ts`, which a test keeps in step, so a
  credited person never misses their portrait and the filter cannot lose them.
  It keeps exact episode counts but labels subscriptions and listening hours as
  estimates. The fixed mini-player, newest-episode header button, person/search/
  episode/play/archive/collaboration filters, and section hash are shareable as
  query/hash state; the snake game is local state. Person clicks filter episodes.
  Everyone in the roster shows a normalized, transparent PNG portrait. The source
  crop is recorded by `scripts/_cutAiTaKrajtaPeoplePortraits.mjs`, from the
  episode cover the show itself published them on or from the picture they publish
  of themselves when no cover carries them; the final alpha PNG is named by the
  roster and a test verifies its format and transparent pixels. Portrait frames
  supply subtle, stable neutral-gradient variations and a restrained hover cue,
  so photographed backgrounds never compete with the page. The one people list
  is drawn anew
  for every visit, weighted by how many episodes of the archive name each
  person, so somebody heard often comes up high far more often than someone in
  one díl without the list ever becoming a ranking. The draw is local state and
  is made in the browser; the page itself is built in the order the draw leans
  towards, from the most often named person to the least often named one, so the
  browser hydrates into the list it was sent.
  Collaboration submissions use `/admin/contacts`. Its tab icon is the page's
  own snake drawing in `/ai-ta-krajta/logo.svg` and `.png`; SVG corners are
  rounded and transparent, while the raster fills its square. That drawing is
  traced off the cover artwork of the show and recorded once in
  `businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.ts`, together with the
  measurements of the animal along its own length. The snake of the minigame
  starts in exactly that shape, in the frame the still logo occupied, and only
  then eases into the proportions, colours and speed of a game snake.
  A seven-cell Promptbook coder terminal floats in the bottom-right corner and
  clears the player and cookie controls. It types `$ ptbk`, then animates an
  ASCII octopus in response to pointer, focus, scrolling, and the snake terrarium.
  It stays still for reduced motion and pauses its clock in hidden tabs. The
  badge and the shared footer credit link to `https://coder.ptbk.io/`.
- `/ai-ta-krajta/media-kit` and `/ai-ta-krajta/branding` are the two pages beside
  the podcast. They are named once, in `AI_TA_KRAJTA_SUBPAGES`, which the footer
  lists and which they point at each other through, so no link can name a page
  differently than the page names itself. Both wear the same compact header,
  section heading and footer as each other. The brand kit hands out the logo the
  site already serves at `/ai-ta-krajta/logo.svg`, `.png` and the cover artwork,
  publishes no colour outside `AI_TA_KRAJTA_COLORS`, and says how the name is
  written; it asks for nothing and sends visitors to the media-kit form.
- `/cs/komunita` is the permanent Czech community room. It has chat, polls,
  projects, materials, and published terms, but no schedule, stage, or live
  updates. Terms show event kind, format/place, price, and status. A term with a
  live room links there; otherwise it links to its landing page. The calendar
  opens on the member's month, can filter by day, and uses the same terms and
  statuses as the cards. Empty days cannot be selected; an empty month is only
  selected when the member's month has no terms. The room offers Google Calendar
  and `webcal:` subscriptions.
- `/cs/komunita/projects` is the full project gallery, ordered by upvotes.
  `/cs/komunita/projects/<project_id>` shows scraped project details and a
  moderated discussion. Project-room sessions derive from the community session;
  the author moderates their own discussion.
- `/cs/komunita/clenstvi` offers free community access and the 199 Kč monthly
  membership. Webinars remain free; paid access adds recordings, archive,
  materials, extra content, priority questions, and Discord/community features.
  The page is prefilled from `fullname` and `email`, supports community discount
  codes, has no new annual plan or trial, and allows cancellation.
- `/cs/komunita/calendar.ics` publishes the same terms by stable slug, so moved
  or renamed terms update existing subscriptions. It contains only event terms
  and public destinations, never subscriber identity.
- `/admin/workshops` manages terms of every event kind, participants, comments,
  reactions, content, attached community polls, and settings. Every term says both
  of its audiences: the people registered on the landing page of its event, and
  the people who entered its room. A room which is no term of an event says
  nothing about registrations at all. The registration block opens the exact
  filtered `/admin/contacts` list and its shared CSV, vCard, and Book exports;
  it never creates a second contact table or serializer in workshop administration.
- `/admin/community` manages the permanent community, including polls, project
  moderation, participants, memberships, payments, and room analytics.
- `/admin/shortener` manages public short links, QR/UTM output, destinations,
  notes, search/filter/sort state, and private click history. Links are served
  by `/[shortcode]`; `/shortener` redirects to the admin page.
- `/admin/login` authenticates the single `admin` account with `ADMIN_PASSWORD`
  and a signed session cookie. All `/admin/*` pages and APIs require
  `requireAdminSignedIn`. `/admin` is the post-login dashboard.
- Other public/legal routes include `/contact`, `/data-deletion`, `/privacy`,
  `/terms`, `/dekujeme`, `/branding`, and the routes under `/k`, `/old`, and
  `/test`.

### Shared community and workshop behavior

- Rooms lead to each other in both directions, through one identity hand-off. The
  community lists the terms and opens the room of each; an invited workshop room
  places its community link beside its materials as a special material for every
  participant, regardless of paid membership. Where a special material sits is
  decided once, in `lib/workshops/workshopSpecialMaterials.ts`: a card says where
  among the ordinary materials it belongs, and the invitation opens the material
  list of a participant who does not pay while it closes the list of a paying
  member. A membership which is not loaded yet leaves it where a participant who
  does not pay reads it. Both carry the connected member's
  name and email on, and an incomplete identity carries nothing rather than half
  of it. Which rooms invite is answered by
  `lib/workshops/workshopKindCapabilities.ts`; the community and project
  discussions do not. The invitation names the community's sections by the names
  the community itself uses.
- Community polls attached to workshops are shared. A normalized email gives a
  member one vote across the community and all attached workshops. Workshops
  may display and accept votes, but the community owns administration.
- The participant room owns the membership badge and popup. Community and live
  workshop rooms use the same membership for the connecting email, and checkout
  returns to the room where it started. Membership is offered only by room kinds
  listed in `lib/workshops/workshopKindCapabilities.ts`; project discussions do
  not offer it.
- Members can open Stripe Customer Portal from the popup. In-app cancellation
  stops only the next renewal, preserves access through the paid period, appears
  in the badge, and can be reversed before that period ends.
- A full-price-for-the-whole-term discount is a voucher: atomically consume it,
  create membership immediately, ask for no card, and create no Stripe renewal.
  A limited-month discount still opens checkout because the regular price returns.
  Stripe webhooks update completed, cancelled, failed, and late payments. With no
  Stripe key, hide membership; with test keys, identify the test payment gate.
- Community projects use a URL-first metadata wizard. Ordinary submissions await
  moderation; trusted members and moderators are approved immediately. Pending
  projects remain visible to their author and moderators.
- A live workshop room has countdown, YouTube stage, reactions, watching count,
  moderated chat, timed materials, and attached poll aggregates. An open-ended
  term runs until its recorded end; its stage does not end automatically. Admins
  can select, replace, clear, or create the displayed comment through the same
  private realtime channel used by reactions.
- A term can be about a project. The connection is one value — GitHub repository,
  the default branch, and one or more branch patterns such as `main`, `client-*`,
  `feature/*`, or `*` for all branches, plus an optional deployment URL — so it is
  set, changed, and unset at once and neither branch selection nor deployment
  outlives its repository. Room kinds which offer it are named in
  `lib/workshops/workshopKindCapabilities.ts`. The room shows the repository as a
  special material beside its ordinary materials for every participant, with its
  links and newest commits, marking those which arrive while a participant watches;
  a newly found commit is broadcast to active rooms and appears on the stage for
  ten seconds. A selection resolving to multiple branches is shown as a commit
  graph. Commits come from the public commit feed or keyless API, pooled once per
  server and cached through the revalidation window; an unreadable feed still
  leaves the room naming its project.
- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
  GitHub Markdown page. The room renders it beside ordinary materials through the
  shared material card, primary action, and QR code, for every participant without
  making it timed or membership-gated content.
- Paid-only materials are decided on the server in one pass. Members receive
  unlocked material; others receive only the published titles as an offer. An
  untitled item is not named, items are not named before their unlock time, and a
  room without membership hides paid-only items without naming them.
- After a workshop ends, its recording is server-gated to members. Others receive
  the published teaser, or a generic offer when no teaser exists; a term without
  a recording offers nothing. An administrator can set the recording's start
  offset in seconds, which applies only to that paid replay and never to the
  countdown or live stream.
- Trusted participants remain invisible and their messages are auto-approved.
  Moderators see pending messages, can approve/reject/correct/pin them, and can
  trust or silence authors. Workshop and community moderators are appointed in
  `/admin/workshops`; project authors moderate their own discussions.
- The room records whether each open browser is actively or passively attended
  from pointer, typing, scroll, and touch activity. Admin analytics distinguish
  active computer users from merely open tabs with dashed audience lines.
- The community's compact gallery shows its five highest-upvoted approved projects
  and links to the full gallery. Community landing content is read from the live
  room: expose only anonymous totals, approved messages, and shared projects;
  show member first names only. If the room cannot be read, show less content,
  not an error. Landing-page reactions are estimates based on recent room data.

### Administration and data rules

- Event kinds are defined once in `lib/events/eventTypes.ts`; adding one should
  not require database migration or page-specific duplication. Terms ask for kind,
  online/place format, price, and capacity. Each kind also names where its landing
  page records registrations, which is the `placeName` of the contacts it gathers.
- Which term a registration belongs to is decided once, in
  `lib/workshops/workshopRegistrations.ts`. A term is recognised by its slug, by
  its Prague day, and by the moment it begins at, so registrations written before
  terms had slugs keep counting. Registration forms write the term through the
  same line prefixes that rule reads back. Contacts are still gathered, recorded
  and shown only by `/admin/contacts`; counting reads nothing but their notes.
  The registration actions in workshop administration carry that same term filter
  into the contacts list and its exports.
- Workshop polls are read-only in workshop administration. Creating a new event
  publishes it by default; duplicating an event keeps it published unless the
  source is unpublished. Duplicating a workshop preserves its connections to
  the already existing attached polls; it creates no polls, options, votes, or room history. Stage settings contain
  the live stream, paid-recording start offset, and recording teaser; presentation settings the public presentation
  address, and project settings the repository the term is about, written as an
  address or as `owner/name`, together with its branch patterns. Deleting a workshop is a soft deletion: its room
  data and attached
  community polls stay stored, but the term leaves normal public and administrative
  lists and no longer holds its slug for a replacement. An end may be empty;
  admins can record, adjust, clear, and reopen it. Overview analytics are zoomable and share their
  room, section, lines, reaction, zoom, and keyword metrics through query params.
- Community administration is the workshop dashboard restricted to the
  `community` room kind: no room picker, schedule, stage, reactions, or address.
  Its membership view filters Stripe lifecycle, prices, discounts, identifiers,
  dates, test/live mode, and links to the exact Stripe checkout/subscription.
  Community analytics cover all measured time and member details show seen times.
- Shortener search, provenance filters, sorting, and selected click history live
  in GET parameters. Editing changes only shortcode, destinations, note, and
  landing page; deletion also deletes recorded clicks.

### Database and verification

- Put database changes in `migrations/*.sql`. Startup and
  `npm run migrate-database` run `_initialize.sql`, then migrations in filename
  order, recording immutable filenames and checksums in `public."Migration"`.
  Changed or missing migrations fail, as do queries requiring an unapplied one.
- `npm run test-types` must build before `tsc` to refresh `.next/types`.
- `npm run test-e2e` uses the Next.js development server; first-request compile
  headroom belongs in `playwright.config.ts`, not individual tests. E2E tests are
  independent and each archives one recording in `tests/e2e/videos/`, retaining
  only recent runs.

# Current preversion

- Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
  ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
  uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
  unchanged; members without replay access still receive only the existing teaser or membership offer.

- Placed the community material of `/cs/online-workshop/participant?workshop=` by what the member reading it has: a
  participant who does not pay for the membership is invited into the community before the materials of the workshop,
  while a paying member keeps that invitation at the end of the list as one more way on. The card itself, its link and
  the identity it carries are unchanged, and the presentation and the project of a workshop stay where they were. Every
  card which shares the material list now says where among the ordinary materials it belongs, so one rule places them
  all instead of each room deciding its own order.

- Added a presentation setting to `/admin/workshops`. A workshop can now link a PDF, PowerPoint file, or public GitHub
  Markdown page, and `/cs/online-workshop/participant?workshop=` shows it beside ordinary materials with the same
  open action and desktop QR code. The presentation is a shared workshop-level material, so it is immediately visible
  to free and paid participants alike without becoming timed or membership-gated content.

- Moved a connected GitHub repository in `/cs/online-workshop/participant` into the materials list as a special
  material. Its existing repository, deployment, and live-commit links now stay with ordinary workshop materials,
  are visible even when no ordinary material has been unlocked, and remain available to both free and paid
  participants without turning the repository into timed or membership-gated content.

- Moved the community hand-off in `/cs/online-workshop/participant` into the materials list as a dedicated
  community material. It is shown even before an administrator has unlocked any ordinary material and remains
  available to free and paid members alike, while carrying the participant identity through the existing shared
  community link. The material list now has one reusable special-material slot for room features that belong beside
  ordinary materials without becoming timed or membership-gated workshop content.

- Let an administrator make a prefix discount code by ending it in `*`: `SUMMER*` now gives its configured discount
  to every submitted code starting with `SUMMER`. The same database resolver supplies form previews and the atomic
  registration consumption, so one path cannot accept a code which the other would reject; an exact code takes
  precedence over a pattern, then the more specific prefix wins.

- Let `/admin/workshops` reopen a workshop by clearing its recorded end, and give an open-ended workshop quick choices to end now, one hour after its start, or two hours after its start; each choice uses the existing live update, so `/cs/online-workshop/participant` immediately keeps or replaces the stream accordingly.
- Let a paid community member open their Stripe Customer Portal from the membership popup of `/cs/komunita`, through
  a new short-lived, authenticated link which belongs only to that member and returns them to the community. The
  portal is now available alongside, rather than instead of, the confirmed in-app cancellation and reactivation of
  the next renewal, so card and invoice management stays with Stripe while the simple cancellation remains where the
  member already sees their membership.

- Let a paid member manage renewal directly in the membership popup of `/cs/komunita`: after an explicit confirmation,
  they can stop the next Stripe payment without losing the days they have already paid for. The modal and its header
  badge say that the membership is ending and name the last paid day, then offer one-click reactivation until that day.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content app/cs/online-workshop/participant/page.tsx; rg -n --glob '"'!node_modules'"' --glob '"'!'"\\.next' \"function WorkshopParticipant|WorkshopParticipantRoom|ParticipantRoom|Closing|wrap-up|wrapUp|isWorkshopEnded|endedAt|endsAt\" app components lib | Select-Object -First 500" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 355ms:
import { ONLINE_WORKSHOP_EVENT_TYPE } from '@/businesses/online-workshop/config';
import { OnlineWorkshopSelectedTermRoom } from '@/businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom';
import { ONLINE_WORKSHOP_PARTICIPANT_METADATA } from '@/businesses/online-workshop/onlineWorkshopMetadata';
import {
    readWorkshopParticipantIdentity,
    readWorkshopSlug,
} from '@/lib/workshops/workshopParticipantLink';
import { loadPublishedEventSummaries, loadSelectedPublishedWorkshop } from '@/lib/workshops/workshopPublic';
import { notFound } from 'next/navigation';

type OnlineWorkshopParticipantRouteProps = {
    readonly searchParams: Promise<{
        readonly email?: string | string[];
        readonly fullname?: string | string[];
        readonly workshop?: string | string[];
    }>;
};

export const metadata = ONLINE_WORKSHOP_PARTICIPANT_METADATA;
export const dynamic = 'force-dynamic';

export default async function OnlineWorkshopParticipantRoute({ searchParams }: OnlineWorkshopParticipantRouteProps) {
    const resolvedSearchParams = await searchParams;
    const participantIdentity = readWorkshopParticipantIdentity(
        resolvedSearchParams.email,
        resolvedSearchParams.fullname,
    );

    // Note: The term of the address decides which room is opened, while every published term of this very event is
    //       what the waiting room offers instead of it.
    const [openedWorkshop, workshops] = await Promise.all([
        loadSelectedPublishedWorkshop(readWorkshopSlug(resolvedSearchParams.workshop), ONLINE_WORKSHOP_EVENT_TYPE),
        loadPublishedEventSummaries(ONLINE_WORKSHOP_EVENT_TYPE),
    ]);
    if (openedWorkshop === null) {
        notFound();
    }

    return (
        <OnlineWorkshopSelectedTermRoom
            openedWorkshop={openedWorkshop}
            workshops={workshops}
            currentTime={new Date().toISOString()}
            initialEmail={participantIdentity.email}
            initialFullname={participantIdentity.fullname}
        />
    );
}
lib\calendar\create-calendar-links.ts:66:    readonly endsAt: string;
lib\calendar\create-calendar-links.ts:123:        dates: `${formatUtcTimestamp(event.startsAt)}/${formatUtcTimestamp(event.endsAt)}`,
lib\calendar\create-calendar-links.ts:145:        `DTEND:${formatUtcTimestamp(event.endsAt)}`,
lib\discounts\discountCodeSchema.ts:59:        endsAt: z.string().datetime({ offset: true }),
lib\discounts\discountCodeSchema.ts:65:    .refine((discountCode) => Date.parse(discountCode.endsAt) >= Date.parse(discountCode.startsAt), {
lib\discounts\discountCodeSchema.ts:67:        path: ['endsAt'],
lib\calendar\create-calendar-links.test.ts:14:    endsAt: '2026-08-20T20:30:00+02:00',
lib\discounts\discountCodeSchema.test.ts:8:    endsAt: '2026-08-21T00:00:00.000Z',
lib\discounts\discountCodeDatabase.ts:103:        endsAt: discountCodeRow.ends_at,
lib\discounts\discountCodeDatabase.ts:121:        ends_at: discountCode.endsAt,
lib\discounts\discountCode.ts:29:    readonly endsAt: string;
lib\discounts\discountCode.ts:67:    discountCode: Pick<DiscountCodeValues, 'startsAt' | 'endsAt' | 'isEnabled'>,
lib\discounts\discountCode.ts:75:    const endsAtMilliseconds = Date.parse(discountCode.endsAt);
lib\discounts\discountCode.ts:78:    return currentMilliseconds >= startsAtMilliseconds && currentMilliseconds <= endsAtMilliseconds;
lib\discounts\discountCode.test.ts:25:        endsAt: '2026-08-21T00:00:00.000Z',
components\workshops\WorkshopLinksPanel.test.tsx:28:    endsAt: '2026-09-10T20:30:00+02:00',
components\workshops\WorkshopLinksPanel.test.tsx:45:    endsAt: '2026-09-24T16:00:00+02:00',
components\workshops\WorkshopLinksPanel.test.tsx:54:    endsAt: '2026-07-10T20:30:00+02:00',
lib\workshops\workshopAdminExports.ts:69:                      { header: 'Končí', getValue: (item: WorkshopDetails) => item.endsAt },
lib\workshops\workshopAdminExports.test.ts:20:    endsAt: '2026-08-20T18:30:00.000Z',
lib\workshops\workshopAdminExports.test.ts:78:                workshopEndsAt: WORKSHOP.endsAt,
lib\workshops\workshopValues.ts:44:        ends_at: values.endsAt,
lib\workshops\workshopValues.ts:68:        ...(values.endsAt === undefined ? {} : { ends_at: values.endsAt }),
lib\workshops\workshopTypes.ts:50:    readonly endsAt: string | null;
lib\workshops\workshopSchemas.ts:399:        endsAt: nullableTimestampSchema.default(null),
lib\workshops\workshopSchemas.ts:422:    .refine(({ startsAt, endsAt }) => endsAt === null || Date.parse(endsAt) > Date.parse(startsAt), {
lib\workshops\workshopSchemas.ts:424:        path: ['endsAt'],
lib\workshops\workshopSchemas.ts:437:        endsAt: nullableTimestampSchema.optional(),
lib\workshops\workshopSchemas.test.ts:30:    endsAt: '2026-08-20T20:30:00+02:00',
lib\workshops\workshopSchemas.test.ts:251:                endsAt: '2026-08-20T18:59:59+02:00',
components\events\EventTermOptionCard.tsx:180:                {formatCzechWorkshopTimeRange(occurrence.startsAt, occurrence.endsAt)}
app\cs\komunita\calendar.ics\route.test.ts:24:        endsAt: '2026-09-10T20:30:00+02:00',
app\cs\komunita\calendar.ics\route.test.ts:41:        endsAt: '2026-09-12T16:00:00+02:00',
app\cs\komunita\calendar.ics\route.test.ts:52:        endsAt: null,
components\admin\DiscountCodeForm.tsx:33:    const endsAt = new Date(startsAt.getTime() + DEFAULT_DISCOUNT_VALIDITY_MILLISECONDS);
components\admin\DiscountCodeForm.tsx:39:        endsAt: endsAt.toISOString(),
components\admin\DiscountCodeForm.tsx:56:        endsAt: discountCode.endsAt,
components\admin\DiscountCodeForm.tsx:108:        const endsAt = fromDateTimeLocalValue(toDateTimeLocalValue(values.endsAt));
components\admin\DiscountCodeForm.tsx:109:        if (startsAt === null || endsAt === null || !values.code.trim()) {
components\admin\DiscountCodeForm.tsx:113:        if (Date.parse(endsAt) < Date.parse(startsAt)) {
components\admin\DiscountCodeForm.tsx:138:            endsAt,
components\admin\DiscountCodeForm.tsx:216:                        value={toDateTimeLocalValue(values.endsAt)}
components\admin\DiscountCodeForm.tsx:218:                            const endsAt = fromDateTimeLocalValue(event.target.value);
components\admin\DiscountCodeForm.tsx:219:                            if (endsAt !== null) {
components\admin\DiscountCodeForm.tsx:220:                                updateValue('endsAt', endsAt);
components\admin\DiscountCodeAdmin.tsx:255:                                                    až {formatDiscountCodeDateTime(discountCode.endsAt)}
lib\events\eventSummary.ts:86:        formatCzechWorkshopTimeRange(occurrence.startsAt, occurrence.endsAt),
lib\events\eventListing.test.ts:22:    endsAt: '2026-09-10T20:30:00+02:00',
lib\events\eventListing.test.ts:31:    endsAt: '2026-10-08T20:30:00+02:00',
lib\events\eventListing.test.ts:39:    endsAt: '2026-07-10T20:30:00+02:00',
lib\workshops\workshopMigration.test.ts:59:    'migrations/2026-08-1600-online-workshop-wrap-up-and-feedback.sql',
lib\events\eventCalendar.ts:48:        endsAt: getWorkshopCalendarEndsAt(workshop),
lib\workshops\workshopKindCapabilities.ts:162:const WORKSHOP_SCHEDULE_FIELD_NAMES = ['startsAt', 'endsAt'] as const;
lib\workshops\workshopKindCapabilities.test.ts:101:                endsAt: null,
lib\workshops\workshopKindCapabilities.test.ts:110:            'endsAt',
lib\workshops\workshopKindCapabilities.test.ts:153:                endsAt: '2026-08-21T20:30:00+02:00',
lib\workshops\workshopDate.ts:105:export function formatCzechWorkshopTimeRange(startsAt: string, endsAt: string | null): string {
lib\workshops\workshopDate.ts:107:    return endsAt === null ? startTime : `${startTime}–${formatCzechWorkshopTime(endsAt)}`;
lib\workshops\workshopDate.ts:122:export function formatCzechWorkshopDuration(startsAt: string, endsAt: string | null): string {
lib\workshops\workshopDate.ts:126:              (getWorkshopExpectedEndsAtMilliseconds({ startsAt, endsAt }) - startsAtMilliseconds) /
lib\workshops\workshopDatabase.ts:477:        endsAt: row.ends_at,
lib\workshops\workshopDatabase.ts:697:    const endsAtMilliseconds = Math.max(startsAtMilliseconds, configuredEndsAtMilliseconds);
lib\workshops\workshopDatabase.ts:698:    const durationMilliseconds = endsAtMilliseconds - startsAtMilliseconds;
lib\workshops\workshopDatabase.ts:721:    const endsAtMilliseconds = workshopRow.ends_at === null ? Date.now() : Date.parse(workshopRow.ends_at);
lib\workshops\workshopDatabase.ts:723:    return new Date(Math.max(startsAtMilliseconds, endsAtMilliseconds)).toISOString();
lib\workshops\workshopDatabase.ts:2310:        // wrap-up starts it must be available for the stage link even if its ordinary unlock time is later; unrelated
lib\workshops\workshopCalendar.ts:26:    readonly endsAt: string | null;
lib\workshops\workshopCalendar.ts:75:    return occurrence.endsAt ?? new Date(getWorkshopExpectedEndsAtMilliseconds(occurrence)).toISOString();
lib\workshops\workshopCalendar.ts:98:        endsAt: getWorkshopCalendarEndsAt(occurrence),
lib\workshops\workshopCalendar.test.ts:15:    endsAt: '2026-08-20T20:30:00+02:00',
lib\workshops\workshopCalendar.test.ts:49:        expect(event.endsAt).toBe(ONLINE_WORKSHOP_OCCURRENCE.endsAt);
lib\workshops\workshopCalendar.test.ts:66:            occurrence: { ...ONLINE_WORKSHOP_OCCURRENCE, endsAt: null },
lib\workshops\workshopCalendar.test.ts:72:        expect(Date.parse(event.endsAt) - Date.parse(event.startsAt)).toBe(60 * 60 * 1000);
lib\metadata\structured-data.ts:189:    readonly endsAt: string | null;
lib\metadata\structured-data.ts:210:        ...(options.endsAt === null ? {} : { endDate: options.endsAt }),
lib\workshops\workshopPhase.ts:22:    readonly endsAt: string | null;
lib\workshops\workshopPhase.ts:42:    const endsAtMilliseconds = occurrence.endsAt === null ? Number.NaN : Date.parse(occurrence.endsAt);
lib\workshops\workshopPhase.ts:44:    return endsAtMilliseconds > Date.parse(occurrence.startsAt) ? endsAtMilliseconds : null;
lib\workshops\workshopPhase.test.ts:16:    endsAt: '2026-07-10T20:30:00+02:00',
lib\workshops\workshopPhase.test.ts:20:    endsAt: '2026-06-10T20:30:00+02:00',
lib\workshops\workshopPhase.test.ts:24:    endsAt: '2026-08-21T20:30:00+02:00',
lib\workshops\workshopPhase.test.ts:28:    endsAt: '2026-09-10T20:30:00+02:00',
lib\workshops\workshopPhase.test.ts:32:    endsAt: '2026-10-10T20:30:00+02:00',
lib\workshops\workshopPhase.test.ts:36:    endsAt: null,
lib\workshops\workshopPhase.test.ts:58:            endsAt: '2026-08-21T20:12:00+02:00',
lib\workshops\workshopOverviewSeriesPoints.ts:291:    const endsAtMilliseconds = Date.parse(analytics.timelineEndsAt);
lib\workshops\workshopOverviewSeriesPoints.ts:293:    if (Number.isNaN(startsAtMilliseconds) || Number.isNaN(endsAtMilliseconds)) {
lib\workshops\workshopOverviewSeriesPoints.ts:299:        toMilliseconds: Math.max(endsAtMilliseconds, startsAtMilliseconds + 1_000),
components\promptbook-coder\PromptbookCoderBadge.tsx:33:                <span className={styles.cell}>{pose.headClosing}</span>
components\promptbook-coder\promptbookCoderAnimation.ts:10:    readonly headClosing: string;
components\promptbook-coder\promptbookCoderAnimation.ts:20:    headClosing: '>',
components\promptbook-coder\promptbookCoderAnimation.ts:48:            { leftTentacle: '=', headOpening: '[', headClosing: ']', rightTentacle: '|' },
components\promptbook-coder\promptbookCoderAnimation.ts:61:            { headOpening: '(', headClosing: ')', rightTentacle: '♥' },
app\api\ai-supervize-mini\registration\route.test.ts:51:        endsAt: '2026-09-04T16:00:00+02:00',
app\api\ai-supervize-mini\registration\route.test.ts:67:        endsAt: '2026-09-09T17:00:00+02:00',
app\api\admin\workshops\[workshopId]\route.ts:95:    const endsAt = parsedResult.data.endsAt === undefined ? existingWorkshop.ends_at : parsedResult.data.endsAt;
app\api\admin\workshops\[workshopId]\route.ts:96:    if (endsAt !== null && Date.parse(endsAt) <= Date.parse(startsAt)) {
app\api\admin\workshops\route.test.ts:55:            endsAt: '2026-09-13T15:00:00.000Z',

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files businesses/online-workshop components/workshops lib/workshops | Sort-Object; Get-Content -Raw lib/workshops/workshopPhase.ts; Get-Content -Raw lib/workshops/workshopClientState.ts; Get-Content -Raw lib/workshops/workshopPublic.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 328ms:
businesses/online-workshop\_OnlineWorkshopPage.tsx
businesses/online-workshop\_OnlineWorkshopThankYouPage.tsx
businesses/online-workshop\config.ts
businesses/online-workshop\onlineWorkshopContent.tsx
businesses/online-workshop\onlineWorkshopMetadata.test.ts
businesses/online-workshop\onlineWorkshopMetadata.ts
businesses/online-workshop\OnlineWorkshopRegistrationForm.test.tsx
businesses/online-workshop\OnlineWorkshopRegistrationForm.tsx
businesses/online-workshop\onlineWorkshopTerms.ts
businesses/online-workshop\onlineWorkshopTestimonials.ts
businesses/online-workshop\participant\OnlineWorkshopParticipantPage.test.tsx
businesses/online-workshop\participant\OnlineWorkshopParticipantPage.tsx
businesses/online-workshop\participant\OnlineWorkshopRoomTermPicker.tsx
businesses/online-workshop\participant\OnlineWorkshopSelectedTermRoom.test.tsx
businesses/online-workshop\participant\OnlineWorkshopSelectedTermRoom.tsx
businesses/online-workshop\participant\useWorkshopAttendanceTracker.ts
businesses/online-workshop\participant\useWorkshopParticipant.test.tsx
businesses/online-workshop\participant\useWorkshopParticipant.ts
businesses/online-workshop\participant\useWorkshopParticipantOfflineSupport.ts
businesses/online-workshop\participant\useWorkshopReactionAnimations.ts
businesses/online-workshop\participant\useWorkshopRepositoryCommitNotification.ts
businesses/online-workshop\participant\useWorkshopRepositoryProgress.ts
businesses/online-workshop\participant\WorkshopCalendarInvitation.test.tsx
businesses/online-workshop\participant\WorkshopCalendarInvitation.tsx
businesses/online-workshop\participant\WorkshopChat.test.tsx
businesses/online-workshop\participant\WorkshopChat.tsx
businesses/online-workshop\participant\WorkshopChatComposer.tsx
businesses/online-workshop\participant\WorkshopChatMessage.tsx
businesses/online-workshop\participant\WorkshopChatMessageModeration.tsx
businesses/online-workshop\participant\WorkshopChatThread.tsx
businesses/online-workshop\participant\WorkshopConnectionForm.tsx
businesses/online-workshop\participant\WorkshopContent.test.tsx
businesses/online-workshop\participant\WorkshopContent.tsx
businesses/online-workshop\participant\WorkshopFeedback.test.tsx
businesses/online-workshop\participant\WorkshopFeedback.tsx
businesses/online-workshop\participant\WorkshopPaidMembersVideoNotice.tsx
businesses/online-workshop\participant\workshopPanelAppearance.ts
businesses/online-workshop\participant\workshopParticipantApi.ts
businesses/online-workshop\participant\WorkshopParticipantBadge.test.tsx
businesses/online-workshop\participant\WorkshopParticipantBadge.tsx
businesses/online-workshop\participant\workshopParticipantStateCache.test.ts
businesses/online-workshop\participant\workshopParticipantStateCache.ts
businesses/online-workshop\participant\WorkshopPolls.test.tsx
businesses/online-workshop\participant\WorkshopPolls.tsx
businesses/online-workshop\participant\WorkshopPresentationMaterial.tsx
businesses/online-workshop\participant\WorkshopReactions.test.tsx
businesses/online-workshop\participant\WorkshopReactions.tsx
businesses/online-workshop\participant\WorkshopRepositoryCommitCard.tsx
businesses/online-workshop\participant\WorkshopRepositoryCommitNotification.tsx
businesses/online-workshop\participant\WorkshopRepositoryGraph.tsx
businesses/online-workshop\participant\WorkshopRepositoryPanel.tsx
businesses/online-workshop\participant\workshopRoomBadge.ts
businesses/online-workshop\participant\WorkshopServerConnectionStatus.tsx
businesses/online-workshop\participant\WorkshopStage.test.tsx
businesses/online-workshop\participant\WorkshopStage.tsx
businesses/online-workshop\participant\WorkshopStageComment.tsx
businesses/online-workshop\participant\WorkshopWatchingBadge.test.tsx
businesses/online-workshop\participant\WorkshopWatchingBadge.tsx
businesses/online-workshop\participant\workshopWatchingCountLabel.test.ts
businesses/online-workshop\participant\workshopWatchingCountLabel.ts
businesses/online-workshop\participant\WorkshopWrapUp.tsx
components/workshops\useWorkshopReactionStream.ts
components/workshops\WorkshopCalendarDay.tsx
components/workshops\WorkshopCalendarMonth.tsx
components/workshops\WorkshopEventCard.tsx
components/workshops\WorkshopEventCardList.tsx
components/workshops\WorkshopFlyingReaction.tsx
components/workshops\WorkshopLinksPanel.test.tsx
components/workshops\WorkshopLinksPanel.tsx
components/workshops\workshopPhaseAppearance.ts
components/workshops\WorkshopPhaseBadge.tsx
components/workshops\workshopReactionAnimations.css
components/workshops\WorkshopReactionStream.test.tsx
components/workshops\WorkshopReactionStream.tsx
lib/workshops\fetchWorkshopRepositoryProgress.test.ts
lib/workshops\fetchWorkshopRepositoryProgress.ts
lib/workshops\workshopActiveStatusQuery.test.ts
lib/workshops\workshopActiveStatusQuery.ts
lib/workshops\workshopAdminExports.test.ts
lib/workshops\workshopAdminExports.ts
lib/workshops\workshopAdminParticipantQuery.test.ts
lib/workshops\workshopAdminParticipantQuery.ts
lib/workshops\workshopAdminRequest.ts
lib/workshops\workshopAdminSections.ts
lib/workshops\workshopAdminViewState.test.ts
lib/workshops\workshopAdminViewState.ts
lib/workshops\workshopAttendance.test.ts
lib/workshops\workshopAttendance.ts
lib/workshops\workshopCalendar.test.ts
lib/workshops\workshopCalendar.ts
lib/workshops\workshopChatInteractivity.test.ts
lib/workshops\workshopChatInteractivity.ts
lib/workshops\workshopClientState.test.ts
lib/workshops\workshopClientState.ts
lib/workshops\workshopCommentLinks.ts
lib/workshops\workshopCommentModeration.ts
lib/workshops\workshopCommentStatus.ts
lib/workshops\workshopCommentThreads.test.ts
lib/workshops\workshopCommentThreads.ts
lib/workshops\workshopCommentValues.ts
lib/workshops\workshopConstants.ts
lib/workshops\workshopDatabase.ts
lib/workshops\workshopDate.ts
lib/workshops\workshopKindCapabilities.test.ts
lib/workshops\workshopKindCapabilities.ts
lib/workshops\workshopMaterialLinks.test.ts
lib/workshops\workshopMaterialLinks.ts
lib/workshops\workshopMigration.test.ts
lib/workshops\workshopModeration.test.ts
lib/workshops\workshopModeration.ts
lib/workshops\workshopOverviewCsv.ts
lib/workshops\workshopOverviewGraphState.ts
lib/workshops\workshopOverviewRangeNavigation.test.ts
lib/workshops\workshopOverviewRangeNavigation.ts
lib/workshops\workshopOverviewSeries.ts
lib/workshops\workshopOverviewSeriesPoints.test.ts
lib/workshops\workshopOverviewSeriesPoints.ts
lib/workshops\workshopPaidMembersContent.test.ts
lib/workshops\workshopPaidMembersContent.ts
lib/workshops\workshopPaidMembersVideo.test.ts
lib/workshops\workshopPaidMembersVideo.ts
lib/workshops\workshopPanels.test.ts
lib/workshops\workshopPanels.ts
lib/workshops\workshopParticipantEmail.ts
lib/workshops\workshopParticipantFullname.ts
lib/workshops\workshopParticipantInteraction.ts
lib/workshops\workshopParticipantLink.test.ts
lib/workshops\workshopParticipantLink.ts
lib/workshops\workshopParticipantModeration.ts
lib/workshops\workshopParticipantRename.ts
lib/workshops\workshopPhase.test.ts
lib/workshops\workshopPhase.ts
lib/workshops\workshopPollAttachmentErrors.ts
lib/workshops\workshopPollSharing.test.ts
lib/workshops\workshopPollValues.test.ts
lib/workshops\workshopPollValues.ts
lib/workshops\workshopPublic.ts
lib/workshops\workshopReactionAnimations.test.ts
lib/workshops\workshopReactionAnimations.ts
lib/workshops\workshopReactionAnimationStyle.test.ts
lib/workshops\workshopReactionAnimationStyle.ts
lib/workshops\workshopRealtime.ts
lib/workshops\workshopRegistrationDatabase.ts
lib/workshops\workshopRegistrations.test.ts
lib/workshops\workshopRegistrations.ts
lib/workshops\workshopRegistrationTiming.test.ts
lib/workshops\workshopRegistrationTiming.ts
lib/workshops\workshopRepository.test.ts
lib/workshops\workshopRepository.ts
lib/workshops\workshopRepositoryGraph.test.ts
lib/workshops\workshopRepositoryGraph.ts
lib/workshops\workshopRepositoryMonitor.test.ts
lib/workshops\workshopRepositoryMonitor.ts
lib/workshops\workshopRepositoryProgress.test.ts
lib/workshops\workshopRepositoryProgress.ts
lib/workshops\workshopRequest.test.ts
lib/workshops\workshopRequest.ts
lib/workshops\workshopSchemas.test.ts
lib/workshops\workshopSchemas.ts
lib/workshops\workshopSession.test.ts
lib/workshops\workshopSession.ts
lib/workshops\workshopSpecialMaterials.test.ts
lib/workshops\workshopSpecialMaterials.ts
lib/workshops\workshopSubmissionStatus.ts
lib/workshops\workshopTypes.ts
lib/workshops\workshopValues.test.ts
lib/workshops\workshopValues.ts
import { DEFAULT_WORKSHOP_DURATION_MINUTES } from '@/lib/workshops/workshopConstants';

const MILLISECONDS_PER_MINUTE = 60 * 1000;

/**
 * Where one occurrence currently stands in time
 */
export const WORKSHOP_PHASE_VALUES = ['ongoing', 'upcoming', 'past'] as const;

export type WorkshopPhase = (typeof WORKSHOP_PHASE_VALUES)[number];

/**
 * As much of a workshop as it takes to decide when it happens
 *
 * Note: Both a summary and the full details of a workshop satisfy this shape, so nothing has to be loaded just to
 *       place an occurrence in time.
 * Note: An occurrence without an end has not ended. It runs for as long as it takes and is only over once its end is
 *       recorded, which is what the administration does when the workshop is really finished.
 */
export type WorkshopOccurrenceTiming = {
    readonly startsAt: string;
    readonly endsAt: string | null;
};

/**
 * The order in which an administrator needs the phases: what runs right now, then what is being prepared, and
 * finally the history.
 */
const WORKSHOP_PHASE_ORDER: Readonly<Record<WorkshopPhase, number>> = {
    ongoing: 0,
    upcoming: 1,
    past: 2,
};

/**
 * Moment an occurrence was really given as its end, or `null` while that end is left open
 *
 * Note: This is the single rule for whether an occurrence has an end at all, so nothing decides on its own that an
 *       unwritten or nonsensical end means the workshop is over.
 */
export function getWorkshopRecordedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number | null {
    const endsAtMilliseconds = occurrence.endsAt === null ? Number.NaN : Date.parse(occurrence.endsAt);

    return endsAtMilliseconds > Date.parse(occurrence.startsAt) ? endsAtMilliseconds : null;
}

/**
 * Whether an occurrence still has no end, so it runs until the administration ends it
 */
export function isWorkshopEndOpen(occurrence: WorkshopOccurrenceTiming): boolean {
    return getWorkshopRecordedEndsAtMilliseconds(occurrence) === null;
}

/**
 * Moment an occurrence is expected to end, taken from the usual length of a workshop while its end is left open
 *
 * Note: A calendar invitation and a duration label have to name a length before an occurrence has one, which is what
 *       this expectation is for. It deliberately never decides whether an occurrence is over — only a recorded end
 *       does that.
 */
export function getWorkshopExpectedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number {
    return (
        getWorkshopRecordedEndsAtMilliseconds(occurrence) ??
        Date.parse(occurrence.startsAt) + DEFAULT_WORKSHOP_DURATION_MINUTES * MILLISECONDS_PER_MINUTE
    );
}

/**
 * Decides whether an occurrence is still ahead, running right now, or already over
 *
 * Note: An occurrence whose end is open never becomes past by itself. It keeps running — and keeps its stage on —
 *       until an administrator records the end of it.
 *
 * @param currentTimeMilliseconds moment to compare against, so a list places every occurrence against the same instant
 */
export function getWorkshopPhase(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds = Date.now(),
): WorkshopPhase {
    if (Date.parse(occurrence.startsAt) > currentTimeMilliseconds) {
        return 'upcoming';
    }

    const recordedEndsAtMilliseconds = getWorkshopRecordedEndsAtMilliseconds(occurrence);
    return recordedEndsAtMilliseconds === null || recordedEndsAtMilliseconds > currentTimeMilliseconds
        ? 'ongoing'
        : 'past';
}

/**
 * The one phase which speaks for a group of occurrences, which is the most pressing among them
 *
 * Note: A day of a calendar carries whatever terms fall on it, and it can only be coloured by one of them. It is
 *       coloured by the very same ranking which puts a running term on top of a list, so a day where something is
 *       happening right now never looks like history.
 */
export function getMostProminentWorkshopPhase(phases: readonly WorkshopPhase[]): WorkshopPhase {
    return phases.reduce(
        (mostProminentPhase, phase) =>
            WORKSHOP_PHASE_ORDER[phase] < WORKSHOP_PHASE_ORDER[mostProminentPhase] ? phase : mostProminentPhase,
        'past',
    );
}

/**
 * The two keys which order one occurrence among the others
 *
 * Note: The history is ranked by its negated date, so the term which ended last leads it while the future is led by
 *       the term which starts next. The most relevant occurrence of every group therefore stays on top of it.
 */
function getWorkshopPhaseOrder(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds: number,
): { readonly phaseRank: number; readonly dateRank: number } {
    const phase = getWorkshopPhase(occurrence, currentTimeMilliseconds);
    const startsAtMilliseconds = Date.parse(occurrence.startsAt);
    const dateRank = Number.isFinite(startsAtMilliseconds) ? startsAtMilliseconds : 0;

    return { phaseRank: WORKSHOP_PHASE_ORDER[phase], dateRank: phase === 'past' ? -dateRank : dateRank };
}

/**
 * Orders occurrences by their phase first and by their date within every phase
 *
 * @param currentTimeMilliseconds moment which decides the phases, so one list is never sorted against a moving instant
 */
export function sortWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): readonly TWorkshop[] {
    return workshops
        .map((workshop) => ({ workshop, order: getWorkshopPhaseOrder(workshop, currentTimeMilliseconds) }))
        .sort(
            (firstEntry, secondEntry) =>
                firstEntry.order.phaseRank - secondEntry.order.phaseRank ||
                firstEntry.order.dateRank - secondEntry.order.dateRank,
        )
        .map(({ workshop }) => workshop);
}

/**
 * Splits occurrences into the same ordered phases which administration and public listings use.
 *
 * Keeping the ordering and the grouping together means a picker can progressively disclose an archive without
 * teaching a second place which workshop belongs before, during, or after the current moment.
 */
export function groupWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): Readonly<Record<WorkshopPhase, readonly TWorkshop[]>> {
    const workshopsByPhase: Record<WorkshopPhase, TWorkshop[]> = {
        ongoing: [],
        upcoming: [],
        past: [],
    };

    for (const workshop of sortWorkshopsByPhase(workshops, currentTimeMilliseconds)) {
        workshopsByPhase[getWorkshopPhase(workshop, currentTimeMilliseconds)].push(workshop);
    }

    return workshopsByPhase;
}

import { isGithubCommit } from '@/lib/github/githubCommitFeed';
import type { WorkshopReaction, WorkshopRealtimeEvent } from '@/lib/workshops/workshopTypes';

export { sortWorkshopComments } from '@/lib/workshops/workshopCommentValues';

export const MAXIMAL_BROWSER_TIMEOUT_MILLISECONDS = 2_147_000_000;
const CONTENT_UNLOCK_REFRESH_PADDING_MILLISECONDS = 250;

function isObject(value: unknown): value is Readonly<Record<string, unknown>> {
    return typeof value === 'object' && value !== null;
}

function isWorkshopReaction(value: unknown): value is WorkshopReaction {
    return (
        isObject(value) &&
        typeof value.id === 'string' &&
        typeof value.emoji === 'string' &&
        typeof value.createdAt === 'string'
    );
}

function isWorkshopCommentReference(value: unknown): boolean {
    return isObject(value) && typeof value.id === 'string' && typeof value.authorName === 'string' && typeof value.body === 'string';
}

export function isWorkshopRealtimeEvent(value: unknown): value is WorkshopRealtimeEvent {
    if (!isObject(value) || typeof value.kind !== 'string') {
        return false;
    }

    if (value.kind === 'state-changed') {
        return true;
    }

    if (value.kind === 'reaction') {
        return (
            isWorkshopReaction(value.reaction) &&
            typeof value.reactionCount === 'number' &&
            Number.isSafeInteger(value.reactionCount) &&
            value.reactionCount >= 0
        );
    }

    if (value.kind === 'stage-comment') {
        return value.stageComment === null || isWorkshopCommentReference(value.stageComment);
    }

    if (value.kind === 'repository-commit') {
        return isGithubCommit(value.commit);
    }

    return (
        value.kind === 'upvote' &&
        typeof value.commentId === 'string' &&
        typeof value.upvoteCount === 'number' &&
        Number.isSafeInteger(value.upvoteCount) &&
        value.upvoteCount >= 0
    );
}

export function getContentUnlockRefreshDelay(
    unlockAt: string,
    serverTime: string,
    clientTimeMilliseconds: number,
): number {
    const serverClockOffsetMilliseconds = Date.parse(serverTime) - clientTimeMilliseconds;
    const unlockDelayMilliseconds =
        Date.parse(unlockAt) -
        (clientTimeMilliseconds + serverClockOffsetMilliseconds) +
        CONTENT_UNLOCK_REFRESH_PADDING_MILLISECONDS;

    return Math.min(MAXIMAL_BROWSER_TIMEOUT_MILLISECONDS, Math.max(0, unlockDelayMilliseconds));
}

import { selectEventOccurrences, type EventOccurrence } from '@/lib/events/eventOccurrence';
import type { EventType } from '@/lib/events/eventTypes';
import {
    findPublishedCommunity,
    findPublishedWorkshops,
    findMostRecentPublishedWorkshop,
    findUpcomingPublishedWorkshops,
    findWorkshopBySlug,
    getWorkshopDatabaseOrNull,
    mapWorkshopRow,
    mapWorkshopSummaryRow,
} from '@/lib/workshops/workshopDatabase';
import type { WorkshopDetails, WorkshopSummary } from '@/lib/workshops/workshopTypes';

/**
 * Loads every term of one kind of event which visitors can still register for. A missing workshop database
 * deliberately looks like no listed terms instead of exposing internal configuration details on the public landing
 * page.
 */
export async function loadUpcomingPublishedEventSummaries(
    eventType: EventType,
): Promise<readonly EventOccurrence[]> {
    const supabase = getWorkshopDatabaseOrNull();
    if (supabase === null) {
        return [];
    }

    const workshopRows = await findUpcomingPublishedWorkshops(supabase, eventType);
    return selectEventOccurrences(workshopRows.map(mapWorkshopSummaryRow));
}

/**
 * Loads every published term of one kind of event, including the ones which are running right now and the ones which
 * are already over. A missing workshop database deliberately looks like no listed terms, exactly as it does on the
 * public landing page of that event.
 *
 * Note: This is what a room offers a participant to choose from, so it deliberately says more than the landing page
 *       of the same event does. A term is worth entering while it runs and long after it ended, while only a term
 *       which has not started yet is worth registering for.
 */
export async function loadPublishedEventSummaries(eventType: EventType): Promise<readonly EventOccurrence[]> {
    const supabase = getWorkshopDatabaseOrNull();
    if (supabase === null) {
        return [];
    }

    const workshopRows = await findPublishedWorkshops(supabase, eventType);
    return selectEventOccurrences(workshopRows.map(mapWorkshopSummaryRow));
}

/**
 * Resolves a published occurrence of one kind of event selected by the URL. Legacy links without a selection
 * deliberately enter the most recent published occurrence of that event, while an explicitly unknown slug never
 * silently opens a different workshop.
 */
export async function loadSelectedPublishedWorkshop(
    requestedWorkshopSlug: string | null,
    eventType: EventType,
): Promise<WorkshopDetails | null> {
    const supabase = getWorkshopDatabaseOrNull();
    if (supabase === null) {
        return null;
    }

    const workshopRow =
        requestedWorkshopSlug === null
            ? await findMostRecentPublishedWorkshop(supabase, eventType)
            : await findWorkshopBySlug(supabase, requestedWorkshopSlug, true);

    if (workshopRow === null || workshopRow.room_kind !== 'workshop' || workshopRow.event_type !== eventType) {
        return null;
    }

    return mapWorkshopRow(workshopRow);
}

/**
 * Loads the one published community room. Its data model is shared with workshops so participants get the same
 * secure live-room capabilities without a second copy of the session and moderation model.
 */
export async function loadPublishedCommunity(): Promise<WorkshopDetails | null> {
    const supabase = getWorkshopDatabaseOrNull();
    if (supabase === null) {
        return null;
    }

    const communityRow = await findPublishedCommunity(supabase);
    return communityRow === null ? null : mapWorkshopRow(communityRow);
}

/**
 * Lists every published term of every kind of event for the community, including past terms but never the community
 * room itself or unpublished drafts.
 */
export async function loadPublishedWorkshopSummaries(): Promise<readonly WorkshopSummary[]> {
    const supabase = getWorkshopDatabaseOrNull();
    if (supabase === null) {
        return [];
    }

    const workshopRows = await findPublishedWorkshops(supabase);
    return workshopRows.map(mapWorkshopSummaryRow);
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom.tsx; Get-Content -Raw businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopContent.tsx; Get-Content -Raw lib/workshops/workshopTypes.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 246ms:
'use client';

import {
    ONLINE_WORKSHOP_HOST_FULLNAME,
    ONLINE_WORKSHOP_PARTICIPANT_PATH,
} from '@/businesses/online-workshop/config';
import { createOnlineWorkshopConnectionDetails } from '@/businesses/online-workshop/onlineWorkshopTerms';
import { OnlineWorkshopParticipantPage } from '@/businesses/online-workshop/participant/OnlineWorkshopParticipantPage';
import { OnlineWorkshopRoomTermPicker } from '@/businesses/online-workshop/participant/OnlineWorkshopRoomTermPicker';
import type { EventOccurrence } from '@/lib/events/eventOccurrence';
import { createWorkshopSelectionPath } from '@/lib/workshops/workshopParticipantLink';
import type { WorkshopSummary } from '@/lib/workshops/workshopTypes';
import { useState } from 'react';

/**
 * How many terms it takes for a participant to be offered a choice at all
 *
 * Note: One term is no choice, and a room which offers a list of exactly the workshop it already describes would only
 *       ask a participant to confirm what they have been told.
 */
const MINIMAL_PICKED_ONLINE_WORKSHOP_COUNT = 2;

type OnlineWorkshopSelectedTermRoomProps = {
    /**
     * The term this address opened, which is the one a participant connects to unless they pick another
     */
    readonly openedWorkshop: WorkshopSummary;

    /**
     * Every published term of the online workshop, which is what a participant picks from
     */
    readonly workshops: readonly EventOccurrence[];

    /**
     * Moment the server built this page at, which places every offered term in time
     */
    readonly currentTime: string;
    readonly initialEmail: string;
    readonly initialFullname: string;
};

/**
 * Keeps the address of the room on the term which is really being entered, so reloading the waiting room or sharing
 * its address opens the workshop a participant picked rather than the one they were offered first.
 */
function rememberPickedWorkshopInAddress(workshopSlug: string): void {
    const roomPath = `${window.location.pathname}${window.location.search}${window.location.hash}`;
    window.history.replaceState(window.history.state, '', createWorkshopSelectionPath(roomPath, workshopSlug));
}

/**
 * The room of one term of the online workshop, together with the choice of which term that is
 *
 * Note: The choice lives here rather than in the shared room, because only this business surface knows which terms
 *       belong to the online workshop. The room below is the very same audited participant room every other kind of
 *       event uses.
 */
export function OnlineWorkshopSelectedTermRoom({
    openedWorkshop,
    workshops,
    currentTime,
    initialEmail,
    initialFullname,
}: OnlineWorkshopSelectedTermRoomProps) {
    const [pickedWorkshopSlug, setPickedWorkshopSlug] = useState(openedWorkshop.slug);

    // Note: A term which is not among the published ones leaves the room exactly where the address put it, so an
    //       address opening a term this list does not carry still enters that very term.
    const selectedWorkshop =
        workshops.find((workshop) => workshop.slug === pickedWorkshopSlug) ?? openedWorkshop;
    const isChoiceOffered = workshops.length >= MINIMAL_PICKED_ONLINE_WORKSHOP_COUNT;

    const pickWorkshop = (workshop: EventOccurrence) => {
        setPickedWorkshopSlug(workshop.slug);
        rememberPickedWorkshopInAddress(workshop.slug);
    };

    return (
        <OnlineWorkshopParticipantPage
            workshopSlug={selectedWorkshop.slug}
            connectionDetails={createOnlineWorkshopConnectionDetails(selectedWorkshop)}
            calendarDetails={{
                hostFullname: ONLINE_WORKSHOP_HOST_FULLNAME,
                participantPath: ONLINE_WORKSHOP_PARTICIPANT_PATH,
            }}
            initialEmail={initialEmail}
            initialFullname={initialFullname}
            connectionTermPicker={
                isChoiceOffered ? (
                    <OnlineWorkshopRoomTermPicker
                        terms={workshops}
                        selectedTermSlug={selectedWorkshop.slug}
                        onSelectTerm={pickWorkshop}
                        currentTime={currentTime}
                    />
                ) : undefined
            }
        />
    );
}

'use client';

import { CommunityRoomInvitation } from '@/businesses/community/CommunityRoomInvitation';
import { CommunityMembershipBadge } from '@/businesses/community/membership/CommunityMembershipBadge';
import { CommunityMembershipModal } from '@/businesses/community/membership/CommunityMembershipModal';
import { CommunityMembershipRoomProvider } from '@/businesses/community/membership/CommunityMembershipRoomProvider';
import { WorkshopCalendarInvitation } from '@/businesses/online-workshop/participant/WorkshopCalendarInvitation';
import { WorkshopChat } from '@/businesses/online-workshop/participant/WorkshopChat';
import {
    WorkshopConnectionForm,
    type WorkshopConnectionDetails,
} from '@/businesses/online-workshop/participant/WorkshopConnectionForm';
import { WorkshopContent } from '@/businesses/online-workshop/participant/WorkshopContent';
import { WorkshopParticipantBadge } from '@/businesses/online-workshop/participant/WorkshopParticipantBadge';
import { WorkshopPolls } from '@/businesses/online-workshop/participant/WorkshopPolls';
import { WorkshopPresentationMaterial } from '@/businesses/online-workshop/participant/WorkshopPresentationMaterial';
import { WorkshopReactions } from '@/businesses/online-workshop/participant/WorkshopReactions';
import { WorkshopRepositoryPanel } from '@/businesses/online-workshop/participant/WorkshopRepositoryPanel';
import { WorkshopStage } from '@/businesses/online-workshop/participant/WorkshopStage';
import { WorkshopServerConnectionStatus } from '@/businesses/online-workshop/participant/WorkshopServerConnectionStatus';
import { WorkshopWatchingBadge } from '@/businesses/online-workshop/participant/WorkshopWatchingBadge';
import { useWorkshopParticipantOfflineSupport } from '@/businesses/online-workshop/participant/useWorkshopParticipantOfflineSupport';
import { useWorkshopParticipant } from '@/businesses/online-workshop/participant/useWorkshopParticipant';
import { useWorkshopRepositoryProgress } from '@/businesses/online-workshop/participant/useWorkshopRepositoryProgress';
import { WorkshopLinksPanel } from '@/components/workshops/WorkshopLinksPanel';
import { getWorkshopKindCapabilities, isWorkshopPollVisibleInRoom } from '@/lib/workshops/workshopKindCapabilities';
import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
import { isWorkshopPanelOffered, type WorkshopPanelKey } from '@/lib/workshops/workshopPanels';
import { WORKSHOP_SEARCH_PARAMETER_NAME } from '@/lib/workshops/workshopParticipantLink';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type { WorkshopSpecialMaterial } from '@/lib/workshops/workshopSpecialMaterials';
import type { WorkshopSummary } from '@/lib/workshops/workshopTypes';
import { RefreshCw, Radio } from 'lucide-react';
import Image from 'next/image';
import { useCallback, useEffect, type ReactNode } from 'react';

/**
 * What it takes to offer this workshop to the calendar of a participant
 */
export type WorkshopCalendarDetails = {
    readonly hostFullname: string;
    readonly participantPath: string;
};

/**
 * Optional hand-off from a persistent room to the terms of every kind of event. The shared participant room only needs
 * the data required to keep a participant identity in those links, never business-specific UI knowledge.
 *
 * Note: Where one term leads is decided by the kind of event it is a term of, so this hand-off never names a path and
 *       a kind of event added later is led to without changing anything here.
 */
export type WorkshopNavigationDetails = {
    readonly workshops: readonly WorkshopSummary[];
    readonly title: string;
    readonly description: string;
    readonly emptyMessage: string;
    readonly locale: string;
    readonly timeZone: string;

    /**
     * Absolute url of the published calendar of these terms, which a room without one leaves out
     */
    readonly calendarFeedUrl?: string;
};

type OnlineWorkshopParticipantPageProps = {
    readonly workshopSlug: string;
    readonly connectionDetails: WorkshopConnectionDetails;
    readonly calendarDetails: WorkshopCalendarDetails | null;

    /**
     * Identity carried here by the link which opened the room, offered to the connection form
     *
     * Note: This only ever prefills that form. A connected participant is described by the loaded room itself, so a
     *       returning member who opened no such link is known just as well.
     */
    readonly initialEmail: string;
    readonly initialFullname: string;
    readonly roomSubtitle?: string;
    readonly isWorkshopSelectionInUrl?: boolean;
    readonly workshopNavigation?: WorkshopNavigationDetails;
    readonly materialsTitle?: string;
    readonly unavailableConnectionMessage?: string;

    /**
     * Optional room-specific member information shown next to the connected participant. This keeps the shared room
     * layout unaware of the business surface that supplied the information.
     *
     * Note: The membership of the member is deliberately not supplied this way. It belongs to every room which offers
     *       it rather than to one business surface, so the room draws it itself just above this supplement.
     */
    readonly participantHeaderSupplement?: ReactNode;

    /**
     * Optional community-specific content placed after room navigation and before materials. It lets the permanent
     * community add a surface without copying the secured participant-room layout.
     */
    readonly mainContentAfterWorkshopNavigation?: ReactNode;

    /**
     * A project discussion has no materials of its own, while the shared community room continues to show them.
     */
    readonly isMaterialsShown?: boolean;

    /**
     * A room may require a domain-specific reconnection flow instead of the general name-and-email form.
     */
    readonly connectionRequiredContent?: ReactNode;

    /**
     * Optional choice of the term to connect to, offered by the connection form of this room
     *
     * Note: Which terms can be entered and what picking one changes is known by the business surface which owns them,
     *       so the room only carries that choice to the one screen where a participant is choosing at all.
     */
    readonly connectionTermPicker?: ReactNode;
};

export function OnlineWorkshopParticipantPage({
    workshopSlug,
    connectionDetails,
    calendarDetails,
    initialEmail,
    initialFullname,
    roomSubtitle = 'Online workshop · Promptbook',
    isWorkshopSelectionInUrl = true,
    workshopNavigation,
    materialsTitle,
    unavailableConnectionMessage = 'Připojení k workshopu se nepodařilo ověřit.',
    participantHeaderSupplement,
    mainContentAfterWorkshopNavigation,
    isMaterialsShown = true,
    connectionRequiredContent,
    connectionTermPicker,
}: OnlineWorkshopParticipantPageProps) {
    const controller = useWorkshopParticipant(workshopSlug);
    useWorkshopParticipantOfflineSupport();
    const isRepositoryProgressEnabled =
        controller.state !== null &&
        getWorkshopKindCapabilities(controller.state.workshop.kind).isRepositoryOffered &&
        controller.state.workshop.repository !== null;
    const repositoryProgressController = useWorkshopRepositoryProgress(workshopSlug, isRepositoryProgressEnabled);
    const subscribeToRepositoryCommits = useCallback<SubscribeToWorkshopRepositoryCommits>(
        (listener) => {
            const unsubscribeFromRealtime = controller.subscribeToRepositoryCommits?.(listener) ?? (() => undefined);
            const unsubscribeFromPolling =
                repositoryProgressController.subscribeToNewCommits?.(listener) ?? (() => undefined);

            return () => {
                unsubscribeFromRealtime();
                unsubscribeFromPolling();
            };
        },
        [controller.subscribeToRepositoryCommits, repositoryProgressController.subscribeToNewCommits],
    );

    useEffect(() => {
        if (controller.state === null) {
            return;
        }

        const sanitizedUrl = new URL(window.location.href);
        const isWorkshopSelectionChanged =
            sanitizedUrl.searchParams.get(WORKSHOP_SEARCH_PARAMETER_NAME) !== workshopSlug;
        const isUrlChanged =
            (isWorkshopSelectionInUrl && isWorkshopSelectionChanged) ||
            (!isWorkshopSelectionInUrl && sanitizedUrl.searchParams.has(WORKSHOP_SEARCH_PARAMETER_NAME)) ||
            sanitizedUrl.searchParams.has('email') ||
            sanitizedUrl.searchParams.has('fullname');
        sanitizedUrl.searchParams.delete('email');
        sanitizedUrl.searchParams.delete('fullname');
        if (isWorkshopSelectionInUrl) {
            sanitizedUrl.searchParams.set(WORKSHOP_SEARCH_PARAMETER_NAME, workshopSlug);
        } else {
            sanitizedUrl.searchParams.delete(WORKSHOP_SEARCH_PARAMETER_NAME);
        }
        if (isUrlChanged) {
            window.history.replaceState(
                window.history.state,
                '',
                `${sanitizedUrl.pathname}${sanitizedUrl.search}${sanitizedUrl.hash}`,
            );
        }
    }, [controller.state, isWorkshopSelectionInUrl, workshopSlug]);

    if (controller.isCheckingConnection && controller.state === null) {
        return (
            <main className="flex min-h-screen items-center justify-center bg-[#07151d] text-cyan-200">
                <RefreshCw className="h-7 w-7 animate-spin" aria-label="Ověřuji připojení" />
            </main>
        );
    }

    if (controller.isConnectionRequired) {
        return (
            connectionRequiredContent ?? (
                <WorkshopConnectionForm
                    connectionDetails={connectionDetails}
                    initialEmail={initialEmail}
                    initialFullname={initialFullname}
                    errorMessage={controller.errorMessage}
                    onConnect={controller.connect}
                    connectionTermPicker={connectionTermPicker}
                />
            )
        );
    }

    if (controller.state === null) {
        return (
            <main className="flex min-h-screen items-center justify-center bg-[#07151d] px-6 text-center text-slate-200">
                <div className="max-w-md rounded-2xl border border-white/10 bg-white/[0.04] p-8 shadow-2xl">
                    <Radio className="mx-auto h-9 w-9 text-cyan-300" />
                    <h1 className="mt-5 text-2xl font-bold text-white">Místnost teď není dostupná</h1>
                    <p className="mt-3 text-sm leading-6 text-slate-400">
                        {controller.errorMessage ?? unavailableConnectionMessage}
                    </p>
                    <button
                        type="button"
                        onClick={() => void controller.refresh()}
                        className="mt-6 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-bold text-slate-950 hover:bg-cyan-200"
                    >
                        <RefreshCw className="h-4 w-4" /> Zkusit znovu
                    </button>
                </div>
            </main>
        );
    }

    const { state } = controller;
    const roomCapabilities = getWorkshopKindCapabilities(state.workshop.kind);
    const isWorkshopPollVisible = isWorkshopPollVisibleInRoom(state.workshop.kind);
    const isModerating = isWorkshopParticipantModerating(state.participant);
    const followUpContentBlock = state.contentBlocks.find((contentBlock) => contentBlock.isFollowUp) ?? null;
    const connectedRepository = roomCapabilities.isRepositoryOffered ? state.workshop.repository : null;
    const presentationUrl = roomCapabilities.isPresentationOffered ? state.workshop.presentationUrl : null;
    const specialMaterials: readonly WorkshopSpecialMaterial[] = [
        ...(presentationUrl === null
            ? []
            : [
                  {
                      id: 'presentation',
                      content: <WorkshopPresentationMaterial presentationUrl={presentationUrl} />,
                  },
              ]),
        ...(connectedRepository === null
            ? []
            : [
                  {
                      id: 'repository',
                      content: (
                          <WorkshopRepositoryPanel
                              repository={connectedRepository}
                              progressController={repositoryProgressController}
                          />
                      ),
                  },
              ]),
        ...(roomCapabilities.isCommunityInvitationOffered
            ? [
                  {
                      id: 'community',
                      content: <CommunityRoomInvitation participantIdentity={state.participant} />,
                      // Note: This is an invitation rather than something the workshop hands over, so a member who
                      //       has not joined the community yet reads it before the materials, while a member who
                      //       already pays for it keeps it at the end as one more way on. It is written as the one
                      //       placement it is, because a spread condition widens it into any string otherwise.
                      placement: 'before-materials-until-paid' as const,
                  },
              ]
            : []),
    ];

    // Note: What the kind of this room has and what an administrator switched off is asked here once, so a new panel
    //       is one line of the room.
    const isPanelOffered = (panelKey: WorkshopPanelKey) =>
        isWorkshopPanelOffered(state.workshop.kind, state.workshop.disabledPanels, panelKey);

    const roomLayout = (
        <div className="min-h-screen bg-[#06131b] text-slate-200">
            <header className="border-b border-white/[0.07] bg-[#071820]/90 backdrop-blur">
                <div className="mx-auto flex max-w-[1500px] flex-col items-stretch gap-3 px-4 py-3.5 sm:flex-row sm:items-center sm:justify-between sm:gap-4 sm:px-8">
                    <div className="flex min-w-0 flex-1 items-center gap-3">
                        <Image
                            src="/logo/promptbook-logo-blue-white-256.png"
                            alt="Promptbook"
                            width={36}
                            height={36}
                            className="h-9 w-9 rounded-lg"
                        />
                        <div className="min-w-0">
                            <p className="break-words text-sm font-bold text-white">{state.workshop.title}</p>
                            <p className="hidden text-xs text-slate-500 sm:block">{roomSubtitle}</p>
                        </div>
                    </div>
                    <div className="flex min-w-0 flex-col items-stretch gap-2 sm:flex-row sm:items-center sm:gap-3">
                        {isPanelOffered('watching-count') && (
                            <WorkshopWatchingBadge watchingParticipantCount={state.watchingParticipantCount} />
                        )}
                        {/*
                          * Note: Who the participant is connected as, and both ways of changing that — renaming
                          *       themselves and signing out — belong to the room they are connected to, so every room
                          *       offers them from this one badge.
                          */}
                        <WorkshopParticipantBadge
                            fullname={state.participant.fullname}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            isModerating={isModerating}
                            isRefreshing={controller.isRefreshing}
                            onChangeFullname={controller.changeFullname}
                            onDisconnect={roomCapabilities.isDisconnectionOffered ? controller.disconnect : undefined}
                        />
                        {/*
                          * Note: Every room which offers the membership says which one its member has and lets them
                          *       buy or manage it with the very same badge and modal instead of a surface of its own.
                          */}
                        <CommunityMembershipBadge />
                        <CommunityMembershipModal />
                        {participantHeaderSupplement}
                        <WorkshopServerConnectionStatus
                            isUsingCachedState={controller.isUsingCachedState}
                            isRefreshing={controller.isRefreshing}
                            unavailableMessage={controller.isUsingCachedState ? controller.errorMessage : null}
                            onRefresh={() => void controller.refresh()}
                        />
                    </div>
                </div>
            </header>

            <main className="mx-auto grid w-full min-w-0 max-w-[1500px] grid-cols-1 gap-5 px-4 py-4 sm:px-8 sm:py-5 lg:grid-cols-[minmax(0,1fr)_390px]">
                <div className="min-w-0 lg:col-start-1 lg:row-start-1">
                    {controller.errorMessage && !controller.isUsingCachedState ? (
                        <div className="mb-4 flex flex-wrap items-start justify-between gap-3 rounded-xl border border-rose-400/20 bg-rose-400/[0.08] px-4 py-3 text-sm text-rose-200">
                            <span className="min-w-0 flex-1 break-words">{controller.errorMessage}</span>
                            <button
                                type="button"
                                onClick={() => void controller.refresh()}
                                className="shrink-0 font-semibold underline underline-offset-4"
                            >
                                Zkusit znovu
                            </button>
                        </div>
                    ) : null}

                    {roomCapabilities.isStageOffered && (
                        <WorkshopStage
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            subscribeToReactions={controller.subscribeToReactions}
                            repository={state.workshop.repository}
                            subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                            feedback={state.feedback}
                            followUpContentBlock={followUpContentBlock}
                            stageComment={state.stageComment}
                            paidMembersOnlyVideo={state.paidMembersOnlyVideo}
                            onSaveFeedback={controller.saveFeedback}
                        />
                    )}
                    {calendarDetails !== null && (
                        <WorkshopCalendarInvitation
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            hostFullname={calendarDetails.hostFullname}
                            participantPath={calendarDetails.participantPath}
                            participantIdentity={state.participant}
                        />
                    )}
                    {isWorkshopPollVisible && (
                        <WorkshopPolls
                            polls={state.polls}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onVote={controller.voteOnPoll}
                        />
                    )}
                    {workshopNavigation !== undefined && (
                        <WorkshopLinksPanel
                            workshops={workshopNavigation.workshops}
                            participantIdentity={state.participant}
                            title={workshopNavigation.title}
                            description={workshopNavigation.description}
                            emptyMessage={workshopNavigation.emptyMessage}
                            locale={workshopNavigation.locale}
                            timeZone={workshopNavigation.timeZone}
                            serverTime={state.serverTime}
                            calendarFeedUrl={workshopNavigation.calendarFeedUrl}
                        />
                    )}
                    {mainContentAfterWorkshopNavigation}
                    {isPanelOffered('reactions') && (
                        <WorkshopReactions
                            emojis={state.workshop.allowedReactions}
                            reactionCounts={state.reactionCounts}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onReact={controller.react}
                        />
                    )}
                </div>

                <WorkshopChat
                    className="min-w-0 lg:col-start-2 lg:row-span-2 lg:row-start-1"
                    comments={state.comments}
                    commentSort={controller.commentSort}
                    isEnabled={isPanelOffered('chat')}
                    isInteractionBanned={state.participant.isInteractionBanned}
                    isModerating={isModerating}
                    onChangeSort={controller.changeCommentSort}
                    onSubmitComment={controller.submitComment}
                    onUpvoteComment={controller.upvoteComment}
                    onModerateComment={controller.moderateComment}
                    onModerateAuthor={controller.moderateAuthor}
                />

                {isMaterialsShown && (
                    <div className="min-w-0 lg:col-start-1 lg:row-start-2">
                        <WorkshopContent
                            contentBlocks={state.contentBlocks}
                            nextContentUnlockAt={state.nextContentUnlockAt}
                            newlyUnlockedContentBlockIds={controller.newlyUnlockedContentBlockIds}
                            paidMembersOnlyContentPreviews={state.paidMembersOnlyContentPreviews}
                            specialMaterials={specialMaterials}
                            title={materialsTitle}
                        />
                    </div>
                )}
            </main>
        </div>
    );

    /*
     * Note: The membership of a member is one membership wherever they read it from, and every surface of the room
     *       which needs to know it — the badge and the modal in the header, the paid materials and the video which a
     *       paid member unlocks after the workshop — asks this one controller instead of loading a membership of its
     *       own.
     */
    return (
        <CommunityMembershipRoomProvider
            workshopSlug={workshopSlug}
            isMembershipOffered={roomCapabilities.isMembershipOffered}
        >
            {roomLayout}
        </CommunityMembershipRoomProvider>
    );
}

'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}

'use client';

import { CommunityPaidMembersNotice } from '@/businesses/community/membership/CommunityPaidMembersNotice';
import { useCommunityMembershipPurchaseOffer } from '@/businesses/community/membership/useCommunityMembershipPurchaseOffer';
import { useIsPaidCommunityMember } from '@/businesses/community/membership/useIsPaidCommunityMember';
import { MarkdownContent } from '@/components/markdown-content';
import { PromptbookQrCode } from '@/components/promptbook-qr-code';
import {
    selectWorkshopSpecialMaterialsByPlacement,
    type WorkshopSpecialMaterial,
} from '@/lib/workshops/workshopSpecialMaterials';
import type { WorkshopContentBlock, WorkshopContentPreview } from '@/lib/workshops/workshopTypes';
import { motion, useReducedMotion } from 'framer-motion';
import { Clock3, Crown, ExternalLink, Lock, Sparkles } from 'lucide-react';
import { Fragment, useEffect, useRef, useState } from 'react';

type WorkshopContentProps = {
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly nextContentUnlockAt: string | null;
    readonly newlyUnlockedContentBlockIds: ReadonlySet<string>;

    /**
     * The paid materials the room hides from the member reading it, by their titles alone. The materials themselves
     * never reach this component for such a member; this is only the place where the room names them and offers the
     * key.
     */
    readonly paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[];

    /**
     * Cards which share the materials placement but have their own source and access rules.
     *
     * Note: The room which knows whether a card applies supplies it here. This component only owns the common list,
     *       so it cannot accidentally make a special material conditional on paid-material visibility.
     * Note: Each card says where among the ordinary materials it belongs, while the membership of the member reading
     *       them is known here, so a card whose placement depends on that membership is placed without the room which
     *       supplied it having to ask what its reader pays for.
     */
    readonly specialMaterials?: readonly WorkshopSpecialMaterial[];
    readonly title?: string;
};

type WorkshopMaterialBodyProps = {
    readonly bodyMarkdown: string;
    readonly callToActionLabel?: string;
};

/**
 * The portion of a material card which is shared by an administrator-managed material and a room-level material.
 */
export type WorkshopMaterialCardContent = Pick<
    WorkshopContentBlock,
    'id' | 'title' | 'bodyMarkdown' | 'isFollowUp' | 'isPaidMembersOnly'
>;

type WorkshopMaterialCardProps = {
    readonly contentBlock: WorkshopMaterialCardContent;
    readonly isNewlyUnlocked?: boolean;
    readonly callToActionLabel?: string;
    readonly ariaLabel?: string;
};

type WorkshopMaterialLink = {
    readonly href: string;
    readonly label: string;
};

const CZECH_DATE_TIME_FORMAT = new Intl.DateTimeFormat('cs-CZ', {
    dateStyle: 'medium',
    timeStyle: 'short',
    timeZone: 'Europe/Prague',
});
const MATERIAL_CALL_TO_ACTION_LABEL = 'Otevřít materiál';
const MATERIAL_LINK_SELECTOR = 'a[href]:not([data-workshop-material-call-to-action])';
const MATERIAL_QR_CODE_SIZE = 144;

function configureMaterialLink(linkElement: HTMLAnchorElement): void {
    // The server has already replaced the href with a persisted short link.
    // This only preserves the existing new-tab behavior; it never observes or
    // reports a click, so copied and forwarded links use the same tracking path.
    linkElement.target = '_blank';
    linkElement.rel = 'noopener noreferrer';
}

function getWorkshopMaterialLinks(linkElements: readonly HTMLAnchorElement[]): readonly WorkshopMaterialLink[] {
    return linkElements.map((linkElement) => ({
        href: linkElement.href,
        label: linkElement.textContent?.trim() || MATERIAL_CALL_TO_ACTION_LABEL,
    }));
}

function areWorkshopMaterialLinkListsEqual(
    currentMaterialLinks: readonly WorkshopMaterialLink[],
    nextMaterialLinks: readonly WorkshopMaterialLink[],
): boolean {
    return (
        currentMaterialLinks.length === nextMaterialLinks.length &&
        currentMaterialLinks.every(
        (currentMaterialLink, index) =>
            currentMaterialLink.href === nextMaterialLinks[index]?.href &&
            currentMaterialLink.label === nextMaterialLinks[index]?.label,
        )
    );
}

/**
 * Every QR code carries the persisted short link already present in a material. On a desktop it gives the person
 * reading the room the same tracked destination on their phone without sending mobile layouts through an extra panel.
 */
function WorkshopMaterialQrCodes({ materialLinks }: { readonly materialLinks: readonly WorkshopMaterialLink[] }) {
    return (
        <aside aria-label="QR kódy materiálů" className="hidden shrink-0 lg:flex lg:flex-col lg:items-center lg:gap-4">
            {materialLinks.map((materialLink, index) => (
                <figure
                    key={`${materialLink.href}-${index}`}
                    aria-label={`QR kód materiálu: ${materialLink.label}`}
                    className="w-44"
                >
                    <PromptbookQrCode
                        value={materialLink.href}
                        size={MATERIAL_QR_CODE_SIZE}
                        className="mx-auto overflow-hidden rounded-xl bg-white shadow-lg shadow-cyan-300/10"
                    />
                    {materialLinks.length > 1 && (
                        <figcaption
                            className="mt-2 truncate text-center text-xs font-semibold leading-5 text-slate-400"
                            title={materialLink.label}
                        >
                            {materialLink.label}
                        </figcaption>
                    )}
                </figure>
            ))}
        </aside>
    );
}

function WorkshopMaterialBody({
    bodyMarkdown,
    callToActionLabel = MATERIAL_CALL_TO_ACTION_LABEL,
}: WorkshopMaterialBodyProps) {
    const materialBodyReference = useRef<HTMLDivElement>(null);
    const [materialLinks, setMaterialLinks] = useState<readonly WorkshopMaterialLink[]>([]);
    const singleMaterialLink = materialLinks.length === 1 ? materialLinks[0] : null;

    useEffect(() => {
        const materialBodyElement = materialBodyReference.current;
        if (materialBodyElement === null) {
            return;
        }

        const configureMaterialLinks = () => {
            const linkElements = Array.from(
                materialBodyElement.querySelectorAll<HTMLAnchorElement>(MATERIAL_LINK_SELECTOR),
            );
            linkElements.forEach((linkElement) => {
                configureMaterialLink(linkElement);
            });

            const nextMaterialLinks = getWorkshopMaterialLinks(linkElements);
            setMaterialLinks((currentMaterialLinks) =>
                areWorkshopMaterialLinkListsEqual(currentMaterialLinks, nextMaterialLinks)
                    ? currentMaterialLinks
                    : nextMaterialLinks,
            );
        };

        configureMaterialLinks();
        const observer = new MutationObserver(configureMaterialLinks);
        observer.observe(materialBodyElement, { childList: true, subtree: true });
        return () => observer.disconnect();
    }, [bodyMarkdown]);

    return (
        <div className="lg:grid lg:grid-cols-[minmax(0,1fr)_auto] lg:items-start lg:gap-8">
            <div ref={materialBodyReference} className="min-w-0 break-words">
                <MarkdownContent
                    content={bodyMarkdown}
                    theme="DARK"
                    className="max-w-none leading-7 text-slate-200 [--chat-md-link-color:#f1f5f9] [&_a]:font-semibold [&_code]:break-words [&_code]:text-cyan-100 [&_h1]:text-white [&_h2]:text-white [&_h3]:text-white [&_img]:max-w-full [&_pre]:max-w-full [&_pre]:overflow-x-auto [&_table]:block [&_table]:max-w-full [&_table]:overflow-x-auto"
                />
                {singleMaterialLink && (
                    <div className="mt-5 flex flex-wrap items-center gap-3">
                        <a
                            href={singleMaterialLink.href}
                            target="_blank"
                            rel="noopener noreferrer"
                            data-workshop-material-call-to-action
                            aria-label={`${callToActionLabel}: ${singleMaterialLink.label}`}
                            className="inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-cyan-300/10 transition hover:bg-cyan-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#07151d]"
                        >
                            {callToActionLabel}
                            <ExternalLink className="h-4 w-4" aria-hidden="true" />
                        </a>
                    </div>
                )}
            </div>
            {materialLinks.length > 0 && <WorkshopMaterialQrCodes materialLinks={materialLinks} />}
        </div>
    );
}

/**
 * The shared card for every material-shaped item in a room.
 *
 * Note: A presentation is configured with the workshop rather than stored as scheduled content, but it deliberately
 *       uses this card so its link, primary action, and desktop QR code never drift from ordinary materials.
 */
export function WorkshopMaterialCard({
    contentBlock,
    isNewlyUnlocked = false,
    callToActionLabel,
    ariaLabel,
}: WorkshopMaterialCardProps) {
    const isReducedMotionPreferred = useReducedMotion() === true;
    const isFollowUp = contentBlock.isFollowUp;
    const isPaidMembersOnly = contentBlock.isPaidMembersOnly;

    return (
        <motion.article
            id={`workshop-material-${contentBlock.id}`}
            aria-label={ariaLabel}
            initial={isNewlyUnlocked && !isReducedMotionPreferred ? { opacity: 0, y: 24, scale: 0.97 } : false}
            animate={{ opacity: 1, y: 0, scale: 1 }}
            transition={{ duration: isReducedMotionPreferred ? 0 : 0.55, ease: 'easeOut' }}
            className={`relative scroll-mt-5 overflow-hidden rounded-2xl border bg-white/[0.045] p-5 text-slate-200 shadow-lg transition-colors sm:p-8 ${isNewlyUnlocked ? 'border-cyan-300/60 pt-16 shadow-cyan-300/10 sm:pt-8' : isFollowUp || isPaidMembersOnly ? 'border-amber-300/60 shadow-amber-300/10' : 'border-white/10'}`}
        >
            {isNewlyUnlocked && (
                <span className="absolute right-4 top-4 rounded-full bg-cyan-300 px-3 py-1 text-xs font-bold text-slate-950 shadow-lg">
                    Právě odemčeno
                </span>
            )}
            {(isFollowUp || isPaidMembersOnly) && (
                <div className="mb-4 flex flex-wrap gap-2">
                    {isFollowUp && (
                        <span className="inline-flex items-center gap-1.5 rounded-full border border-amber-200/30 bg-amber-300/10 px-3 py-1 text-xs font-bold text-amber-100">
                            <Sparkles className="h-3.5 w-3.5" aria-hidden="true" /> Navazující materiál
                        </span>
                    )}
                    {isPaidMembersOnly && (
                        <span className="inline-flex items-center gap-1.5 rounded-full border border-amber-200/30 bg-amber-300/10 px-3 py-1 text-xs font-bold text-amber-100">
                            <Crown className="h-3.5 w-3.5" aria-hidden="true" /> Pro placené členy
                        </span>
                    )}
                </div>
            )}
            {contentBlock.title && <h3 className="mb-5 text-xl font-bold text-white">{contentBlock.title}</h3>}
            <WorkshopMaterialBody bodyMarkdown={contentBlock.bodyMarkdown} callToActionLabel={callToActionLabel} />
        </motion.article>
    );
}

/**
 * Where the paid materials are, for the members who cannot see them. The materials themselves stay on the server;
 * this card only names them, says that they are here and opens the very same membership popup the badge in the header
 * opens.
 */
function WorkshopPaidMembersContentNotice({
    contentPreviews,
    onUnlockPaidMaterials,
}: {
    readonly contentPreviews: readonly WorkshopContentPreview[];
    readonly onUnlockPaidMaterials: () => void;
}) {
    // A material an administrator left untitled has nothing to tease with, so it keeps this card without being named
    // in it.
    const namedContentPreviews = contentPreviews.filter((contentPreview) => contentPreview.title.trim() !== '');

    return (
        <CommunityPaidMembersNotice
            title="Materiály pro placené členy"
            description="Na tomto místě jsou materiály dostupné jen pro placené členy komunity. Odemknete je měsíčním placeným členstvím."
            onUnlockPaidMembership={onUnlockPaidMaterials}
            unlockedLabel="Co odemknete"
            unlockedContent={
                namedContentPreviews.length === 0 ? undefined : (
                    <ul aria-label="Náhled materiálů pro placené členy" className="space-y-2">
                        {namedContentPreviews.map((contentPreview) => (
                            <li key={contentPreview.id} className="flex items-start gap-2">
                                <Lock className="mt-0.5 h-4 w-4 shrink-0 text-amber-200/70" aria-hidden="true" />
                                <span className="min-w-0 break-words text-sm font-semibold leading-6 text-amber-50">
                                    {contentPreview.title}
                                </span>
                            </li>
                        ))}
                    </ul>
                )
            }
        />
    );
}

export function WorkshopContent({
    contentBlocks,
    nextContentUnlockAt,
    newlyUnlockedContentBlockIds,
    paidMembersOnlyContentPreviews,
    specialMaterials = [],
    title = 'Materiály z workshopu',
}: WorkshopContentProps) {
    // Note: The purchase is only offered while a gate is configured and the member has not paid yet, which is exactly
    //       when the server keeps the paid materials hidden, so the notice and the hidden materials cannot disagree.
    const membershipPurchaseOffer = useCommunityMembershipPurchaseOffer();
    const isPaidMembersContentNoticeShown =
        paidMembersOnlyContentPreviews.length > 0 && membershipPurchaseOffer !== null;
    const isSpecialMaterialShown = specialMaterials.length > 0;
    // Note: The community is invited to at the head of the list of a member who has not joined it yet and stays at
    //       the end of the list of a member who already pays, which is the one difference the membership makes here.
    const isPaidMember = useIsPaidCommunityMember();
    const { specialMaterialsBeforeContentBlocks, specialMaterialsAfterContentBlocks } =
        selectWorkshopSpecialMaterialsByPlacement(specialMaterials, { isPaidMember });

    if (
        contentBlocks.length === 0 &&
        nextContentUnlockAt === null &&
        !isPaidMembersContentNoticeShown &&
        !isSpecialMaterialShown
    ) {
        return null;
    }

    return (
        <section className="mt-8" aria-labelledby="workshop-materials-title">
            <div className="mb-4 flex items-center gap-2">
                <Sparkles className="h-5 w-5 text-cyan-300" />
                <h2 id="workshop-materials-title" className="text-xl font-bold text-white">
                    {title}
                </h2>
            </div>

            <div className="space-y-4">
                {specialMaterialsBeforeContentBlocks.map((specialMaterial) => (
                    <Fragment key={specialMaterial.id}>{specialMaterial.content}</Fragment>
                ))}

                {contentBlocks.map((contentBlock) => (
                    <WorkshopMaterialCard
                            key={contentBlock.id}
                                contentBlock={contentBlock}
                        isNewlyUnlocked={newlyUnlockedContentBlockIds.has(contentBlock.id)}
                            />
                ))}

                {specialMaterialsAfterContentBlocks.map((specialMaterial) => (
                    <Fragment key={specialMaterial.id}>{specialMaterial.content}</Fragment>
                ))}

                {nextContentUnlockAt && (
                    <div className="flex items-start gap-3 rounded-xl border border-dashed border-cyan-300/20 bg-cyan-300/[0.04] px-5 py-4 text-sm text-slate-400">
                        <Clock3 className="h-5 w-5 shrink-0 text-cyan-300" />
                        <span className="min-w-0">
                            Další materiál se automaticky odemkne{' '}
                            {CZECH_DATE_TIME_FORMAT.format(new Date(nextContentUnlockAt))}.
                        </span>
                    </div>
                )}

                {isPaidMembersContentNoticeShown && membershipPurchaseOffer !== null && (
                    <WorkshopPaidMembersContentNotice
                        contentPreviews={paidMembersOnlyContentPreviews}
                        onUnlockPaidMaterials={membershipPurchaseOffer.openMembershipModal}
                    />
                )}
            </div>
        </section>
    );
}

import type { AdminContactJoin } from '@/lib/admin/adminContactJoin';
import type { CommunityMembershipStatus } from '@/lib/community-membership/communityMembershipTypes';
import type { EventDetails } from '@/lib/events/event';
import type { GithubCommit } from '@/lib/github/githubCommitFeed';
import type { WorkshopPanelKey } from '@/lib/workshops/workshopPanels';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';

/**
 * A live room is normally one workshop occurrence. The community uses the same resilient room infrastructure, but
 * remains a single, separately administered room across all occurrences.
 */
export const WORKSHOP_KIND_VALUES = ['workshop', 'community', 'project'] as const;

export type WorkshopKind = (typeof WORKSHOP_KIND_VALUES)[number];

export function isWorkshopKind(value: string): value is WorkshopKind {
    return WORKSHOP_KIND_VALUES.includes(value as WorkshopKind);
}

/**
 * The lifecycle of something a participant submits for moderation. Chat messages and community projects deliberately
 * use these same states, so trust and moderator privileges never approve one kind of submission differently from the
 * other.
 */
export const WORKSHOP_SUBMISSION_STATUS_VALUES = ['pending', 'approved', 'rejected'] as const;

export type WorkshopSubmissionStatus = (typeof WORKSHOP_SUBMISSION_STATUS_VALUES)[number];

/**
 * A chat message keeps this alias because it is the public type used by the existing room API.
 */
export type WorkshopCommentStatus = WorkshopSubmissionStatus;
export type WorkshopCommentSort = 'recent' | 'upvotes';

export type WorkshopSummary = {
    readonly id: string;
    readonly kind: WorkshopKind;
    readonly slug: string;
    readonly title: string;

    /**
     * What this very term is about, in the words an administrator wrote about it
     *
     * Note: Terms of one event each have a subject of their own, so a list of them says what every single one is
     *       about rather than describing them all by the event they belong to. The room and the list read the very
     *       same words, so a term is never described in two different ways.
     */
    readonly description: string;
    readonly startsAt: string;
    readonly endsAt: string | null;
    readonly isPublished: boolean;

    /**
     * The event this room is a term of, or `null` for a permanent room which is no event at all
     *
     * Note: Every kind of event is administered in the very same place and stored in the very same table, so a page
     *       listing terms only ever asks which kind of event it wants to list.
     */
    readonly event: EventDetails | null;
};

/**
 * One occurrence as the administration lists it, together with the audience it gathered
 *
 * Note: The participant count deliberately stays out of `WorkshopSummary`, so no public list of terms exposes how
 *       many people registered for them.
 */
export type WorkshopAdminSummary = WorkshopSummary & {
    readonly participantCount: number;

    /**
     * How many people registered for this term on the landing page of its event, or `null` for a room which is no term
     * of an event and therefore has no registration form at all
     *
     * Note: This is a different audience from `participantCount`, which counts the people who really entered the room.
     *       Somebody can register and never come, and somebody can be handed a link into a room they never registered
     *       for, so neither number can be derived from the other.
     */
    readonly registeredParticipantCount: number | null;
};

export type WorkshopDetails = WorkshopSummary & {
    /** Number of artificial viewers added to the live watching badge by an administrator. */
    readonly artificialWatchingParticipantCount?: number;
    readonly youtubeVideoId: string | null;

    /**
     * Where paid members begin an ended workshop's recording, in seconds from the beginning of its stream.
     *
     * Note: This has no effect on the countdown or the live stream. It only removes the waiting-room portion from the
     *       replay which the membership unlocks after the workshop ends.
     */
    readonly recordingStartOffsetSeconds: number;

    /**
     * The teaser of that stream which stands in for it once the workshop is over, or `null` while none was published
     *
     * Note: The recording of an ended workshop is unlocked by the paid membership, and this is the snippet of it which
     *       an administrator lets everybody else watch instead. It is deliberately a second video rather than a part
     *       of the first one, so nothing of the recording itself has to reach a member who has not unlocked it.
     */
    readonly previewYoutubeVideoId: string | null;

    /**
     * A public PDF, presentation file, or Markdown page which belongs to this workshop, or `null` while none was
     * published. It is a workshop setting rather than an ordinary content block, so every participant can open it
     * without waiting for a scheduled unlock or buying a membership.
     */
    readonly presentationUrl: string | null;

    /**
     * The project this workshop is about, or `null` while no project is connected to it
     *
     * Note: This is the whole connection — the repository, its branch selection, and the address the project runs at
     *       — so the administration sets, changes, and unsets it as one thing. What has been committed in it is
     *       deliberately not part of it: that is read from GitHub, see `fetchWorkshopRepositoryProgress`.
     */
    readonly repository: WorkshopRepository | null;
    readonly allowedReactions: readonly string[];

    /**
     * The panels of the room an admin switched off for this workshop
     *
     * Note: Everything not listed here is offered, so a panel added later starts switched on for every workshop.
     */
    readonly disabledPanels: readonly WorkshopPanelKey[];
    readonly createdAt: string;
    readonly updatedAt: string;
};

export type WorkshopParticipant = {
    readonly id: string;
    readonly fullname: string;

    /**
     * Contact address this participant connected with
     *
     * Note: A room only ever describes the very participant reading it, so this address never reaches anybody else. It
     *       is what lets a room hand a verified identity on, for example into the link leading to another room.
     */
    readonly email: string;
    readonly connectedAt: string;
    readonly isInteractionBanned: boolean;

    /**
     * Whether the messages of this participant are approved as they are written
     *
     * Note: Trust stays invisible in the room. Nothing but the approval of their own messages tells a participant or
     *       anybody else that they were trusted.
     */
    readonly isTrusted: boolean;

    /**
     * Whether this participant moderates the room, which the room says with a badge
     */
    readonly isModerator: boolean;
};

export type WorkshopAdminParticipant = WorkshopParticipant &
    AdminContactJoin & {
    readonly lastSeenAt: string;
    readonly activeDurationSeconds: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly upvoteCount: number;

    /**
     * Present when the participant is listed inside the community administration. A community membership belongs to
     * their e-mail address rather than to this particular room session, so ordinary workshop participant data does
     * not fetch or expose it.
     */
    readonly communityMembershipStatus?: CommunityMembershipStatus;
};

/**
 * One server-paged slice of participants, keeping large workshops responsive in the administration.
 */
export type WorkshopAdminParticipantPage = {
    readonly participants: readonly WorkshopAdminParticipant[];
    readonly totalCount: number;
};

/**
 * One timestamped activity attributable to a participant.
 *
 * Note: These are assembled from the existing audited source records. They deliberately do not introduce a second
 * event store which could disagree with comments, reactions, or votes.
 */
export type WorkshopParticipantTimelineEvent =
    | {
          readonly kind: 'joined' | 'last-seen';
          readonly id: string;
          readonly occurredAt: string;
      }
    | {
          readonly kind: 'comment';
          readonly id: string;
          readonly occurredAt: string;
          readonly body: string;
          readonly status: WorkshopCommentStatus;
      }
    | {
          readonly kind: 'reaction';
          readonly id: string;
          readonly occurredAt: string;
          readonly emoji: string;
      }
    | {
          readonly kind: 'upvote';
          readonly id: string;
          readonly occurredAt: string;
          readonly commentId: string;
          readonly commentAuthorName: string | null;
          readonly commentBody: string | null;
      };

export type WorkshopAdminParticipantTimeline = {
    readonly participant: WorkshopAdminParticipant;
    readonly events: readonly WorkshopParticipantTimelineEvent[];
};

/**
 * Activity totals inside one compact bucket of the workshop-wide timeline.
 */
export type WorkshopAdminTimelinePoint = {
    readonly startsAt: string;

    /**
     * How many people had the room open during this bucket, which is the audience rather than an action
     *
     * Note: This is counted from the presence the room reports while it is open, so a workshop which was held before
     *       the room started reporting it has an audience of nobody however many people really watched it.
     */
    readonly watchingParticipantCount: number;

    /**
     * How many of the watching people were really at their computer during this bucket, and how many only had the room
     * open, which together are the whole audience of it
     *
     * Note: How that is told apart is decided by `workshopAttendance`, and a bucket which was measured before the
     *       attendance of a room was measured at all is passive throughout.
     */
    readonly activelyWatchingParticipantCount: number;
    readonly passivelyWatchingParticipantCount: number;

    /**
     * How many people registered into the room during this bucket
     */
    readonly participantCount: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly upvoteCount: number;
    readonly linkClickCount: number;

    /**
     * How many times each reaction was sent during this bucket, so a graph can draw one of them alone
     */
    readonly reactionCountsByEmoji: Readonly<Record<string, number>>;
};

/**
 * One message with the moment it was written, which is everything a metric counting words in the chat needs
 *
 * Note: The administration counts the matches of a regular expression in the browser, so that an expression which is
 *       still being typed answers immediately and never reaches the database.
 */
export type WorkshopAdminCommentSample = {
    readonly occurredAt: string;
    readonly body: string;
};

export type WorkshopAdminAnalytics = {
    readonly timelineStartsAt: string;
    readonly timelineEndsAt: string;
    readonly bucketDurationSeconds: number;
    readonly timeline: readonly WorkshopAdminTimelinePoint[];
    readonly reactionCounts: readonly WorkshopReactionCount[];
    readonly commentSamples: readonly WorkshopAdminCommentSample[];

    /**
     * Whether every message of the room could be sampled, or the oldest ones had to be left out of a very busy room
     */
    readonly isCommentSampleComplete: boolean;
};

export type WorkshopContentBlock = {
    readonly id: string;
    readonly title: string;
    readonly bodyMarkdown: string;
    readonly unlockAt: string;
    readonly sortOrder: number;
    readonly isPublished: boolean;

    /**
     * The one ordinary material selected to lead the post-workshop follow-up.
     */
    readonly isFollowUp: boolean;

    /**
     * Whether this material is reserved for the members who pay for the community membership
     *
     * Note: A room which offers the membership keeps it out of the state of everybody else and only says that it is
     *       there, so the material itself never reaches a member who has not unlocked it.
     */
    readonly isPaidMembersOnly: boolean;
    readonly createdAt: string;
    readonly updatedAt: string;
    readonly linkClickCount: number;
};

/**
 * As much of a withheld material as the member who may not read it is told
 *
 * Note: Nothing but the title of such a material ever leaves the server, so the room names what is waiting behind the
 *       membership without handing over any of it.
 */
export type WorkshopContentPreview = {
    readonly id: string;
    readonly title: string;
};

/**
 * As much of a withheld recording as the member who may not play it is given
 *
 * Note: The recording itself never leaves the server for such a member. Only the teaser an administrator published for
 *       it does, which is what the room shows on the closing stage instead, and which is `null` while none was
 *       published at all.
 */
export type WorkshopPaidMembersVideo = {
    readonly previewYoutubeVideoId: string | null;
};

/**
 * A participant's progressively saved reflection after one workshop.
 *
 * The score creates the record; every written answer is optional and can then arrive independently, so somebody who
 * only answers the first question is still represented faithfully.
 */
export type WorkshopFeedback = {
    readonly rating: number;
    readonly whatWasGood: string | null;
    readonly whatWasBad: string | null;
    readonly note: string | null;
    readonly createdAt: string;
    readonly updatedAt: string;
};

/**
 * The admin-only form of feedback, attributable to its participant and joinable to the private contact projection.
 */
export type WorkshopAdminFeedback = WorkshopFeedback &
    AdminContactJoin & {
        readonly id: string;
        readonly participantId: string;
        readonly fullname: string;
        readonly email: string;
    };

/**
 * Who wrote a message, as far as somebody moderating the room may know them
 *
 * Note: Only a moderator receives this, so an ordinary participant never learns which invisible moderation state the
 *       author of a message carries, nor the identity behind their name.
 */
export type WorkshopCommentAuthor = {
    readonly participantId: string;
    readonly isTrusted: boolean;
    readonly isInteractionBanned: boolean;
    readonly isModerator: boolean;
};

export type WorkshopComment = {
    readonly id: string;
    readonly authorName: string;
    readonly body: string;
    readonly status: WorkshopCommentStatus;
    readonly upvoteCount: number;
    readonly isUpvotedByParticipant: boolean;
    readonly createdAt: string;

    /**
     * Whether a moderator of the room wrote this message, which the whole room sees on it
     */
    readonly isAuthorModerator: boolean;

    /**
     * Whether the administration created this message without a participant
     */
    readonly isArtificial: boolean;

    /**
     * The author as a moderator of the room may act on them, or `null` for everybody else
     */
    readonly moderatedAuthor: WorkshopCommentAuthor | null;

    /**
     * The comment this one answers, or `null` when it opens its own thread
     */
    readonly parentCommentId: string | null;

    /**
     * Whether an admin pinned this message to the top of the chat
     *
     * Note: A room has at most one pinned message, because the pin is remembered by the workshop itself.
     */
    readonly isPinned: boolean;
};

/**
 * A comment together with the answers it received
 *
 * Note: The chat is exactly one level deep, so a reply never carries replies of its own.
 */
export type WorkshopCommentThread = {
    readonly comment: WorkshopComment;
    readonly replies: readonly WorkshopComment[];
};

export type WorkshopReaction = {
    readonly id: string;
    readonly emoji: string;
    readonly createdAt: string;
};

/**
 * The number of times one exact reaction has been sent in a workshop
 *
 * Note: This counts reaction actions, rather than distinct people. A participant who reacts twice therefore adds two
 *       to the total, exactly as the room celebrated two reactions.
 */
export type WorkshopReactionCount = {
    readonly emoji: string;
    readonly count: number;
};

/**
 * One answer a member can choose in a poll. The room only receives aggregate counts and whether its own participant
 * chose this option, never the identities behind any other vote.
 */
export type WorkshopPollOption = {
    readonly id: string;
    readonly label: string;
    readonly sortOrder: number;
    readonly voteCount: number;
    readonly isVotedByParticipant: boolean;
};

/**
 * The administrative view additionally separates the member votes from the explicitly seeded aggregate. The member
 * room deliberately keeps receiving only `WorkshopPollOption`, so an artificial starting count does not expose its
 * origin to the people taking part in the poll.
 */
export type WorkshopAdminPollOption = WorkshopPollOption & {
    readonly realVoteCount: number;
    readonly artificialVoteCount: number;
};

/**
 * A community question prepared by an administrator. Poll infrastructure is shared with the room model, while the
 * room-kind capability decides which kinds offer it.
 */
export type WorkshopPoll = {
    readonly id: string;
    readonly question: string;
    readonly isClosed: boolean;
    readonly isVisible: boolean;
    readonly createdAt: string;
    readonly updatedAt: string;
    readonly options: readonly WorkshopPollOption[];

    /**
     * The workshop occurrences this poll is about
     *
     * Note: A poll keeps belonging to the community which administers it. An attached occurrence is the subject of the
     *       question, which is what lets its room receive the poll and what lets the administration of that occurrence
     *       see the question asked about it. Members are only ever told about published occurrences.
     */
    readonly attachedWorkshops: readonly WorkshopSummary[];
};

export type WorkshopAdminPoll = Omit<WorkshopPoll, 'options'> & {
    readonly options: readonly WorkshopAdminPollOption[];
};

export type WorkshopPublicState = {
    readonly serverTime: string;
    readonly workshop: WorkshopDetails;
    readonly participant: WorkshopParticipant;

    /**
     * How many participants had the room open recently, including the one this state was loaded for
     */
    readonly watchingParticipantCount: number;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly nextContentUnlockAt: string | null;

    /**
     * The materials which only paid members may see, named for a member who is not one of them
     *
     * Note: The materials themselves stay out of `contentBlocks`, so this list is what lets the room say where the
     *       paid materials are, what they are called and how to unlock them. It is empty for a member who already paid
     *       and in a room which offers no membership at all.
     */
    readonly paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[];

    /**
     * The recording which the paid membership would unlock for this member, or `null` while nothing of the video is
     * being withheld from them
     *
     * Note: The stream of the occurrence stays out of `workshop` while this is set, so the room cannot play a recording
     *       it also offers to sell. It is `null` for everybody who may watch it — during the workshop itself, for a
     *       member who paid, and in a room which offers no membership — and for an occurrence which carries no video
     *       at all, because there would be nothing for a purchase to unlock.
     */
    readonly paidMembersOnlyVideo: WorkshopPaidMembersVideo | null;
    readonly feedback: WorkshopFeedback | null;
    readonly comments: readonly WorkshopComment[];

    /**
     * The one question the host selected for the shared stage, or `null` while no question is being answered
     *
     * Note: This is a reference to the ordinary comment which supplied the question. Keeping that comment as the
     *       source of truth lets an attendee and the host read the same words without copying a second message body
     *       into a stage-only record.
     */
    readonly stageComment: WorkshopCommentReference | null;
    readonly recentReactions: readonly WorkshopReaction[];
    readonly reactionCounts: readonly WorkshopReactionCount[];
    readonly polls: readonly WorkshopPoll[];
};

/**
 * As much of an answered comment as the moderation of a reply needs to judge it
 */
export type WorkshopCommentReference = {
    readonly id: string;
    readonly authorName: string;
    readonly body: string;
};

export type WorkshopAdminComment = Omit<WorkshopComment, 'isUpvotedByParticipant'> & {
    readonly participantId: string | null;
    readonly realUpvoteCount: number;
    readonly artificialUpvoteCount: number;
    readonly parentComment: WorkshopCommentReference | null;
};

export type WorkshopAdminSnapshot = {
    readonly workshop: WorkshopDetails;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly polls: readonly WorkshopAdminPoll[];

    /**
     * The community polls asked about this room, which a room administering polls of its own never has
     *
     * Note: These are read-only here. They are administered where they belong, in the administration of the community
     *       which owns them, rather than being editable from two places at once.
     */
    readonly attachedPolls: readonly WorkshopAdminPoll[];
    readonly comments: readonly WorkshopAdminComment[];

    /**
     * The message pinned on top of the chat, whatever moderation state the administration is listing
     */
    readonly pinnedComment: WorkshopCommentReference | null;

    /**
     * The question currently shown over the shared stage, if the room has one
     */
    readonly stageComment: WorkshopCommentReference | null;
    readonly participants: readonly WorkshopAdminParticipant[];
    readonly participantCount: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly artificialReactionCount: number;
};

export type WorkshopRealtimeEvent =
    | { readonly kind: 'state-changed' }
    | { readonly kind: 'reaction'; readonly reaction: WorkshopReaction; readonly reactionCount: number }
    | { readonly kind: 'upvote'; readonly commentId: string; readonly upvoteCount: number }
    | { readonly kind: 'stage-comment'; readonly stageComment: WorkshopCommentReference | null }
    | { readonly kind: 'repository-commit'; readonly commit: GithubCommit };


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '(Get-Content businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx | Measure-Object -Line).Lines; Get-Content businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx | Select-Object -First 260; Get-Content businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx | Select-Object -Skip 260 -First 280; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopContent.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 560ms:
417
'use client';

import { CommunityRoomInvitation } from '@/businesses/community/CommunityRoomInvitation';
import { CommunityMembershipBadge } from '@/businesses/community/membership/CommunityMembershipBadge';
import { CommunityMembershipModal } from '@/businesses/community/membership/CommunityMembershipModal';
import { CommunityMembershipRoomProvider } from '@/businesses/community/membership/CommunityMembershipRoomProvider';
import { WorkshopCalendarInvitation } from '@/businesses/online-workshop/participant/WorkshopCalendarInvitation';
import { WorkshopChat } from '@/businesses/online-workshop/participant/WorkshopChat';
import {
    WorkshopConnectionForm,
    type WorkshopConnectionDetails,
} from '@/businesses/online-workshop/participant/WorkshopConnectionForm';
import { WorkshopContent } from '@/businesses/online-workshop/participant/WorkshopContent';
import { WorkshopParticipantBadge } from '@/businesses/online-workshop/participant/WorkshopParticipantBadge';
import { WorkshopPolls } from '@/businesses/online-workshop/participant/WorkshopPolls';
import { WorkshopPresentationMaterial } from '@/businesses/online-workshop/participant/WorkshopPresentationMaterial';
import { WorkshopReactions } from '@/businesses/online-workshop/participant/WorkshopReactions';
import { WorkshopRepositoryPanel } from '@/businesses/online-workshop/participant/WorkshopRepositoryPanel';
import { WorkshopStage } from '@/businesses/online-workshop/participant/WorkshopStage';
import { WorkshopServerConnectionStatus } from '@/businesses/online-workshop/participant/WorkshopServerConnectionStatus';
import { WorkshopWatchingBadge } from '@/businesses/online-workshop/participant/WorkshopWatchingBadge';
import { useWorkshopParticipantOfflineSupport } from '@/businesses/online-workshop/participant/useWorkshopParticipantOfflineSupport';
import { useWorkshopParticipant } from '@/businesses/online-workshop/participant/useWorkshopParticipant';
import { useWorkshopRepositoryProgress } from '@/businesses/online-workshop/participant/useWorkshopRepositoryProgress';
import { WorkshopLinksPanel } from '@/components/workshops/WorkshopLinksPanel';
import { getWorkshopKindCapabilities, isWorkshopPollVisibleInRoom } from '@/lib/workshops/workshopKindCapabilities';
import { isWorkshopParticipantModerating } from '@/lib/workshops/workshopModeration';
import { isWorkshopPanelOffered, type WorkshopPanelKey } from '@/lib/workshops/workshopPanels';
import { WORKSHOP_SEARCH_PARAMETER_NAME } from '@/lib/workshops/workshopParticipantLink';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type { WorkshopSpecialMaterial } from '@/lib/workshops/workshopSpecialMaterials';
import type { WorkshopSummary } from '@/lib/workshops/workshopTypes';
import { RefreshCw, Radio } from 'lucide-react';
import Image from 'next/image';
import { useCallback, useEffect, type ReactNode } from 'react';

/**
 * What it takes to offer this workshop to the calendar of a participant
 */
export type WorkshopCalendarDetails = {
    readonly hostFullname: string;
    readonly participantPath: string;
};

/**
 * Optional hand-off from a persistent room to the terms of every kind of event. The shared participant room only needs
 * the data required to keep a participant identity in those links, never business-specific UI knowledge.
 *
 * Note: Where one term leads is decided by the kind of event it is a term of, so this hand-off never names a path and
 *       a kind of event added later is led to without changing anything here.
 */
export type WorkshopNavigationDetails = {
    readonly workshops: readonly WorkshopSummary[];
    readonly title: string;
    readonly description: string;
    readonly emptyMessage: string;
    readonly locale: string;
    readonly timeZone: string;

    /**
     * Absolute url of the published calendar of these terms, which a room without one leaves out
     */
    readonly calendarFeedUrl?: string;
};

type OnlineWorkshopParticipantPageProps = {
    readonly workshopSlug: string;
    readonly connectionDetails: WorkshopConnectionDetails;
    readonly calendarDetails: WorkshopCalendarDetails | null;

    /**
     * Identity carried here by the link which opened the room, offered to the connection form
     *
     * Note: This only ever prefills that form. A connected participant is described by the loaded room itself, so a
     *       returning member who opened no such link is known just as well.
     */
    readonly initialEmail: string;
    readonly initialFullname: string;
    readonly roomSubtitle?: string;
    readonly isWorkshopSelectionInUrl?: boolean;
    readonly workshopNavigation?: WorkshopNavigationDetails;
    readonly materialsTitle?: string;
    readonly unavailableConnectionMessage?: string;

    /**
     * Optional room-specific member information shown next to the connected participant. This keeps the shared room
     * layout unaware of the business surface that supplied the information.
     *
     * Note: The membership of the member is deliberately not supplied this way. It belongs to every room which offers
     *       it rather than to one business surface, so the room draws it itself just above this supplement.
     */
    readonly participantHeaderSupplement?: ReactNode;

    /**
     * Optional community-specific content placed after room navigation and before materials. It lets the permanent
     * community add a surface without copying the secured participant-room layout.
     */
    readonly mainContentAfterWorkshopNavigation?: ReactNode;

    /**
     * A project discussion has no materials of its own, while the shared community room continues to show them.
     */
    readonly isMaterialsShown?: boolean;

    /**
     * A room may require a domain-specific reconnection flow instead of the general name-and-email form.
     */
    readonly connectionRequiredContent?: ReactNode;

    /**
     * Optional choice of the term to connect to, offered by the connection form of this room
     *
     * Note: Which terms can be entered and what picking one changes is known by the business surface which owns them,
     *       so the room only carries that choice to the one screen where a participant is choosing at all.
     */
    readonly connectionTermPicker?: ReactNode;
};

export function OnlineWorkshopParticipantPage({
    workshopSlug,
    connectionDetails,
    calendarDetails,
    initialEmail,
    initialFullname,
    roomSubtitle = 'Online workshop · Promptbook',
    isWorkshopSelectionInUrl = true,
    workshopNavigation,
    materialsTitle,
    unavailableConnectionMessage = 'Připojení k workshopu se nepodařilo ověřit.',
    participantHeaderSupplement,
    mainContentAfterWorkshopNavigation,
    isMaterialsShown = true,
    connectionRequiredContent,
    connectionTermPicker,
}: OnlineWorkshopParticipantPageProps) {
    const controller = useWorkshopParticipant(workshopSlug);
    useWorkshopParticipantOfflineSupport();
    const isRepositoryProgressEnabled =
        controller.state !== null &&
        getWorkshopKindCapabilities(controller.state.workshop.kind).isRepositoryOffered &&
        controller.state.workshop.repository !== null;
    const repositoryProgressController = useWorkshopRepositoryProgress(workshopSlug, isRepositoryProgressEnabled);
    const subscribeToRepositoryCommits = useCallback<SubscribeToWorkshopRepositoryCommits>(
        (listener) => {
            const unsubscribeFromRealtime = controller.subscribeToRepositoryCommits?.(listener) ?? (() => undefined);
            const unsubscribeFromPolling =
                repositoryProgressController.subscribeToNewCommits?.(listener) ?? (() => undefined);

            return () => {
                unsubscribeFromRealtime();
                unsubscribeFromPolling();
            };
        },
        [controller.subscribeToRepositoryCommits, repositoryProgressController.subscribeToNewCommits],
    );

    useEffect(() => {
        if (controller.state === null) {
            return;
        }

        const sanitizedUrl = new URL(window.location.href);
        const isWorkshopSelectionChanged =
            sanitizedUrl.searchParams.get(WORKSHOP_SEARCH_PARAMETER_NAME) !== workshopSlug;
        const isUrlChanged =
            (isWorkshopSelectionInUrl && isWorkshopSelectionChanged) ||
            (!isWorkshopSelectionInUrl && sanitizedUrl.searchParams.has(WORKSHOP_SEARCH_PARAMETER_NAME)) ||
            sanitizedUrl.searchParams.has('email') ||
            sanitizedUrl.searchParams.has('fullname');
        sanitizedUrl.searchParams.delete('email');
        sanitizedUrl.searchParams.delete('fullname');
        if (isWorkshopSelectionInUrl) {
            sanitizedUrl.searchParams.set(WORKSHOP_SEARCH_PARAMETER_NAME, workshopSlug);
        } else {
            sanitizedUrl.searchParams.delete(WORKSHOP_SEARCH_PARAMETER_NAME);
        }
        if (isUrlChanged) {
            window.history.replaceState(
                window.history.state,
                '',
                `${sanitizedUrl.pathname}${sanitizedUrl.search}${sanitizedUrl.hash}`,
            );
        }
    }, [controller.state, isWorkshopSelectionInUrl, workshopSlug]);

    if (controller.isCheckingConnection && controller.state === null) {
        return (
            <main className="flex min-h-screen items-center justify-center bg-[#07151d] text-cyan-200">
                <RefreshCw className="h-7 w-7 animate-spin" aria-label="Ověřuji připojení" />
            </main>
        );
    }

    if (controller.isConnectionRequired) {
        return (
            connectionRequiredContent ?? (
                <WorkshopConnectionForm
                    connectionDetails={connectionDetails}
                    initialEmail={initialEmail}
                    initialFullname={initialFullname}
                    errorMessage={controller.errorMessage}
                    onConnect={controller.connect}
                    connectionTermPicker={connectionTermPicker}
                />
            )
        );
    }

    if (controller.state === null) {
        return (
            <main className="flex min-h-screen items-center justify-center bg-[#07151d] px-6 text-center text-slate-200">
                <div className="max-w-md rounded-2xl border border-white/10 bg-white/[0.04] p-8 shadow-2xl">
                    <Radio className="mx-auto h-9 w-9 text-cyan-300" />
                    <h1 className="mt-5 text-2xl font-bold text-white">Místnost teď není dostupná</h1>
                    <p className="mt-3 text-sm leading-6 text-slate-400">
                        {controller.errorMessage ?? unavailableConnectionMessage}
                    </p>
                    <button
                        type="button"
                        onClick={() => void controller.refresh()}
                        className="mt-6 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-bold text-slate-950 hover:bg-cyan-200"
                    >
                        <RefreshCw className="h-4 w-4" /> Zkusit znovu
                    </button>
                </div>
            </main>
        );
    }

    const { state } = controller;
    const roomCapabilities = getWorkshopKindCapabilities(state.workshop.kind);
    const isWorkshopPollVisible = isWorkshopPollVisibleInRoom(state.workshop.kind);
    const isModerating = isWorkshopParticipantModerating(state.participant);
    const followUpContentBlock = state.contentBlocks.find((contentBlock) => contentBlock.isFollowUp) ?? null;
    const connectedRepository = roomCapabilities.isRepositoryOffered ? state.workshop.repository : null;
    const presentationUrl = roomCapabilities.isPresentationOffered ? state.workshop.presentationUrl : null;
    const specialMaterials: readonly WorkshopSpecialMaterial[] = [
        ...(presentationUrl === null
            ? []
            : [
                  {
                      id: 'presentation',
                      content: <WorkshopPresentationMaterial presentationUrl={presentationUrl} />,
                  },
              ]),
        ...(connectedRepository === null
            ? []
            : [
                  {
                      id: 'repository',
                      content: (
                          <WorkshopRepositoryPanel
                              repository={connectedRepository}
                              progressController={repositoryProgressController}
                          />
                      ),
                  },
              ]),
        ...(roomCapabilities.isCommunityInvitationOffered
            ? [
                  {
                      id: 'community',
                      content: <CommunityRoomInvitation participantIdentity={state.participant} />,
                      // Note: This is an invitation rather than something the workshop hands over, so a member who
                      //       has not joined the community yet reads it before the materials, while a member who
                      //       already pays for it keeps it at the end as one more way on. It is written as the one
                      //       placement it is, because a spread condition widens it into any string otherwise.
                      placement: 'before-materials-until-paid' as const,
                  },
              ]
            : []),
    ];

    // Note: What the kind of this room has and what an administrator switched off is asked here once, so a new panel
    //       is one line of the room.
    const isPanelOffered = (panelKey: WorkshopPanelKey) =>
        isWorkshopPanelOffered(state.workshop.kind, state.workshop.disabledPanels, panelKey);

    const roomLayout = (
        <div className="min-h-screen bg-[#06131b] text-slate-200">
            <header className="border-b border-white/[0.07] bg-[#071820]/90 backdrop-blur">
                <div className="mx-auto flex max-w-[1500px] flex-col items-stretch gap-3 px-4 py-3.5 sm:flex-row sm:items-center sm:justify-between sm:gap-4 sm:px-8">
                    <div className="flex min-w-0 flex-1 items-center gap-3">
                        <Image
                            src="/logo/promptbook-logo-blue-white-256.png"
                            alt="Promptbook"
                            width={36}
                            height={36}
                            className="h-9 w-9 rounded-lg"
                        />
                        <div className="min-w-0">
                            <p className="break-words text-sm font-bold text-white">{state.workshop.title}</p>
                            <p className="hidden text-xs text-slate-500 sm:block">{roomSubtitle}</p>
                        </div>
                    </div>
                    <div className="flex min-w-0 flex-col items-stretch gap-2 sm:flex-row sm:items-center sm:gap-3">
                        {isPanelOffered('watching-count') && (
                            <WorkshopWatchingBadge watchingParticipantCount={state.watchingParticipantCount} />
                        )}
                        {/*
                          * Note: Who the participant is connected as, and both ways of changing that — renaming
                          *       themselves and signing out — belong to the room they are connected to, so every room
                          *       offers them from this one badge.
                          */}
                        <WorkshopParticipantBadge
                            fullname={state.participant.fullname}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            isModerating={isModerating}
                            isRefreshing={controller.isRefreshing}
                            onChangeFullname={controller.changeFullname}
                            onDisconnect={roomCapabilities.isDisconnectionOffered ? controller.disconnect : undefined}
                        />
                        {/*
                          * Note: Every room which offers the membership says which one its member has and lets them
                          *       buy or manage it with the very same badge and modal instead of a surface of its own.
                          */}
                        <CommunityMembershipBadge />
                        <CommunityMembershipModal />
                        {participantHeaderSupplement}
                        <WorkshopServerConnectionStatus
                            isUsingCachedState={controller.isUsingCachedState}
                            isRefreshing={controller.isRefreshing}
                            unavailableMessage={controller.isUsingCachedState ? controller.errorMessage : null}
                            onRefresh={() => void controller.refresh()}
                        />
                    </div>
                </div>
            </header>

            <main className="mx-auto grid w-full min-w-0 max-w-[1500px] grid-cols-1 gap-5 px-4 py-4 sm:px-8 sm:py-5 lg:grid-cols-[minmax(0,1fr)_390px]">
                <div className="min-w-0 lg:col-start-1 lg:row-start-1">
                    {controller.errorMessage && !controller.isUsingCachedState ? (
                        <div className="mb-4 flex flex-wrap items-start justify-between gap-3 rounded-xl border border-rose-400/20 bg-rose-400/[0.08] px-4 py-3 text-sm text-rose-200">
                            <span className="min-w-0 flex-1 break-words">{controller.errorMessage}</span>
                            <button
                                type="button"
                                onClick={() => void controller.refresh()}
                                className="shrink-0 font-semibold underline underline-offset-4"
                            >
                                Zkusit znovu
                            </button>
                        </div>
                    ) : null}

                    {roomCapabilities.isStageOffered && (
                        <WorkshopStage
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            subscribeToReactions={controller.subscribeToReactions}
                            repository={state.workshop.repository}
                            subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                            feedback={state.feedback}
                            followUpContentBlock={followUpContentBlock}
                            stageComment={state.stageComment}
                            paidMembersOnlyVideo={state.paidMembersOnlyVideo}
                            onSaveFeedback={controller.saveFeedback}
                        />
                    )}
                    {calendarDetails !== null && (
                        <WorkshopCalendarInvitation
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            hostFullname={calendarDetails.hostFullname}
                            participantPath={calendarDetails.participantPath}
                            participantIdentity={state.participant}
                        />
                    )}
                    {isWorkshopPollVisible && (
                        <WorkshopPolls
                            polls={state.polls}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onVote={controller.voteOnPoll}
                        />
                    )}
                    {workshopNavigation !== undefined && (
                        <WorkshopLinksPanel
                            workshops={workshopNavigation.workshops}
                            participantIdentity={state.participant}
                            title={workshopNavigation.title}
                            description={workshopNavigation.description}
                            emptyMessage={workshopNavigation.emptyMessage}
                            locale={workshopNavigation.locale}
                            timeZone={workshopNavigation.timeZone}
                            serverTime={state.serverTime}
                            calendarFeedUrl={workshopNavigation.calendarFeedUrl}
                        />
                    )}
                    {mainContentAfterWorkshopNavigation}
                    {isPanelOffered('reactions') && (
                        <WorkshopReactions
                            emojis={state.workshop.allowedReactions}
                            reactionCounts={state.reactionCounts}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onReact={controller.react}
                        />
                    )}
                </div>

                <WorkshopChat
                    className="min-w-0 lg:col-start-2 lg:row-span-2 lg:row-start-1"
                    comments={state.comments}
                    commentSort={controller.commentSort}
                    isEnabled={isPanelOffered('chat')}
                    isInteractionBanned={state.participant.isInteractionBanned}
                    isModerating={isModerating}
                    onChangeSort={controller.changeCommentSort}
                    onSubmitComment={controller.submitComment}
                    onUpvoteComment={controller.upvoteComment}
                    onModerateComment={controller.moderateComment}
                    onModerateAuthor={controller.moderateAuthor}
                />

                {isMaterialsShown && (
                    <div className="min-w-0 lg:col-start-1 lg:row-start-2">
                        <WorkshopContent
                            contentBlocks={state.contentBlocks}
                            nextContentUnlockAt={state.nextContentUnlockAt}
                            newlyUnlockedContentBlockIds={controller.newlyUnlockedContentBlockIds}
                            paidMembersOnlyContentPreviews={state.paidMembersOnlyContentPreviews}
                            specialMaterials={specialMaterials}
                            title={materialsTitle}
                        />
                    </div>
                )}
            </main>
        </div>
    );

    /*
     * Note: The membership of a member is one membership wherever they read it from, and every surface of the room
     *       which needs to know it — the badge and the modal in the header, the paid materials and the video which a
     *       paid member unlocks after the workshop — asks this one controller instead of loading a membership of its
     *       own.
     */
    return (
        <CommunityMembershipRoomProvider
            workshopSlug={workshopSlug}
            isMembershipOffered={roomCapabilities.isMembershipOffered}
        >
            {roomLayout}
        </CommunityMembershipRoomProvider>
    );
}
'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}

'use client';

import { CommunityPaidMembersNotice } from '@/businesses/community/membership/CommunityPaidMembersNotice';
import { useCommunityMembershipPurchaseOffer } from '@/businesses/community/membership/useCommunityMembershipPurchaseOffer';
import { useIsPaidCommunityMember } from '@/businesses/community/membership/useIsPaidCommunityMember';
import { MarkdownContent } from '@/components/markdown-content';
import { PromptbookQrCode } from '@/components/promptbook-qr-code';
import {
    selectWorkshopSpecialMaterialsByPlacement,
    type WorkshopSpecialMaterial,
} from '@/lib/workshops/workshopSpecialMaterials';
import type { WorkshopContentBlock, WorkshopContentPreview } from '@/lib/workshops/workshopTypes';
import { motion, useReducedMotion } from 'framer-motion';
import { Clock3, Crown, ExternalLink, Lock, Sparkles } from 'lucide-react';
import { Fragment, useEffect, useRef, useState } from 'react';

type WorkshopContentProps = {
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly nextContentUnlockAt: string | null;
    readonly newlyUnlockedContentBlockIds: ReadonlySet<string>;

    /**
     * The paid materials the room hides from the member reading it, by their titles alone. The materials themselves
     * never reach this component for such a member; this is only the place where the room names them and offers the
     * key.
     */
    readonly paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[];

    /**
     * Cards which share the materials placement but have their own source and access rules.
     *
     * Note: The room which knows whether a card applies supplies it here. This component only owns the common list,
     *       so it cannot accidentally make a special material conditional on paid-material visibility.
     * Note: Each card says where among the ordinary materials it belongs, while the membership of the member reading
     *       them is known here, so a card whose placement depends on that membership is placed without the room which
     *       supplied it having to ask what its reader pays for.
     */
    readonly specialMaterials?: readonly WorkshopSpecialMaterial[];
    readonly title?: string;
};

type WorkshopMaterialBodyProps = {
    readonly bodyMarkdown: string;
    readonly callToActionLabel?: string;
};

/**
 * The portion of a material card which is shared by an administrator-managed material and a room-level material.
 */
export type WorkshopMaterialCardContent = Pick<
    WorkshopContentBlock,
    'id' | 'title' | 'bodyMarkdown' | 'isFollowUp' | 'isPaidMembersOnly'
>;

type WorkshopMaterialCardProps = {
    readonly contentBlock: WorkshopMaterialCardContent;
    readonly isNewlyUnlocked?: boolean;
    readonly callToActionLabel?: string;
    readonly ariaLabel?: string;
};

type WorkshopMaterialLink = {
    readonly href: string;
    readonly label: string;
};

const CZECH_DATE_TIME_FORMAT = new Intl.DateTimeFormat('cs-CZ', {
    dateStyle: 'medium',
    timeStyle: 'short',
    timeZone: 'Europe/Prague',
});
const MATERIAL_CALL_TO_ACTION_LABEL = 'Otevřít materiál';
const MATERIAL_LINK_SELECTOR = 'a[href]:not([data-workshop-material-call-to-action])';
const MATERIAL_QR_CODE_SIZE = 144;

function configureMaterialLink(linkElement: HTMLAnchorElement): void {
    // The server has already replaced the href with a persisted short link.
    // This only preserves the existing new-tab behavior; it never observes or
    // reports a click, so copied and forwarded links use the same tracking path.
    linkElement.target = '_blank';
    linkElement.rel = 'noopener noreferrer';
}

function getWorkshopMaterialLinks(linkElements: readonly HTMLAnchorElement[]): readonly WorkshopMaterialLink[] {
    return linkElements.map((linkElement) => ({
        href: linkElement.href,
        label: linkElement.textContent?.trim() || MATERIAL_CALL_TO_ACTION_LABEL,
    }));
}

function areWorkshopMaterialLinkListsEqual(
    currentMaterialLinks: readonly WorkshopMaterialLink[],
    nextMaterialLinks: readonly WorkshopMaterialLink[],
): boolean {
    return (
        currentMaterialLinks.length === nextMaterialLinks.length &&
        currentMaterialLinks.every(
        (currentMaterialLink, index) =>
            currentMaterialLink.href === nextMaterialLinks[index]?.href &&
            currentMaterialLink.label === nextMaterialLinks[index]?.label,
        )
    );
}

/**
 * Every QR code carries the persisted short link already present in a material. On a desktop it gives the person
 * reading the room the same tracked destination on their phone without sending mobile layouts through an extra panel.
 */
function WorkshopMaterialQrCodes({ materialLinks }: { readonly materialLinks: readonly WorkshopMaterialLink[] }) {
    return (
        <aside aria-label="QR kódy materiálů" className="hidden shrink-0 lg:flex lg:flex-col lg:items-center lg:gap-4">
            {materialLinks.map((materialLink, index) => (
                <figure
                    key={`${materialLink.href}-${index}`}
                    aria-label={`QR kód materiálu: ${materialLink.label}`}
                    className="w-44"
                >
                    <PromptbookQrCode
                        value={materialLink.href}
                        size={MATERIAL_QR_CODE_SIZE}
                        className="mx-auto overflow-hidden rounded-xl bg-white shadow-lg shadow-cyan-300/10"
                    />
                    {materialLinks.length > 1 && (
                        <figcaption
                            className="mt-2 truncate text-center text-xs font-semibold leading-5 text-slate-400"
                            title={materialLink.label}
                        >
                            {materialLink.label}
                        </figcaption>
                    )}
                </figure>
            ))}
        </aside>
    );
}

function WorkshopMaterialBody({
    bodyMarkdown,
    callToActionLabel = MATERIAL_CALL_TO_ACTION_LABEL,
}: WorkshopMaterialBodyProps) {
    const materialBodyReference = useRef<HTMLDivElement>(null);
    const [materialLinks, setMaterialLinks] = useState<readonly WorkshopMaterialLink[]>([]);
    const singleMaterialLink = materialLinks.length === 1 ? materialLinks[0] : null;

    useEffect(() => {
        const materialBodyElement = materialBodyReference.current;
        if (materialBodyElement === null) {
            return;
        }

        const configureMaterialLinks = () => {
            const linkElements = Array.from(
                materialBodyElement.querySelectorAll<HTMLAnchorElement>(MATERIAL_LINK_SELECTOR),
            );
            linkElements.forEach((linkElement) => {
                configureMaterialLink(linkElement);
            });

            const nextMaterialLinks = getWorkshopMaterialLinks(linkElements);
            setMaterialLinks((currentMaterialLinks) =>
                areWorkshopMaterialLinkListsEqual(currentMaterialLinks, nextMaterialLinks)
                    ? currentMaterialLinks
                    : nextMaterialLinks,
            );
        };

        configureMaterialLinks();
        const observer = new MutationObserver(configureMaterialLinks);
        observer.observe(materialBodyElement, { childList: true, subtree: true });
        return () => observer.disconnect();
    }, [bodyMarkdown]);

    return (
        <div className="lg:grid lg:grid-cols-[minmax(0,1fr)_auto] lg:items-start lg:gap-8">
            <div ref={materialBodyReference} className="min-w-0 break-words">
                <MarkdownContent
                    content={bodyMarkdown}
                    theme="DARK"
                    className="max-w-none leading-7 text-slate-200 [--chat-md-link-color:#f1f5f9] [&_a]:font-semibold [&_code]:break-words [&_code]:text-cyan-100 [&_h1]:text-white [&_h2]:text-white [&_h3]:text-white [&_img]:max-w-full [&_pre]:max-w-full [&_pre]:overflow-x-auto [&_table]:block [&_table]:max-w-full [&_table]:overflow-x-auto"
                />
                {singleMaterialLink && (
                    <div className="mt-5 flex flex-wrap items-center gap-3">
                        <a
                            href={singleMaterialLink.href}
                            target="_blank"
                            rel="noopener noreferrer"
                            data-workshop-material-call-to-action
                            aria-label={`${callToActionLabel}: ${singleMaterialLink.label}`}
                            className="inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-cyan-300/10 transition hover:bg-cyan-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#07151d]"
                        >
                            {callToActionLabel}
                            <ExternalLink className="h-4 w-4" aria-hidden="true" />
                        </a>
                    </div>
                )}
            </div>
            {materialLinks.length > 0 && <WorkshopMaterialQrCodes materialLinks={materialLinks} />}
        </div>
    );
}

/**
 * The shared card for every material-shaped item in a room.
 *
 * Note: A presentation is configured with the workshop rather than stored as scheduled content, but it deliberately
 *       uses this card so its link, primary action, and desktop QR code never drift from ordinary materials.
 */
export function WorkshopMaterialCard({
    contentBlock,
    isNewlyUnlocked = false,
    callToActionLabel,
    ariaLabel,
}: WorkshopMaterialCardProps) {
    const isReducedMotionPreferred = useReducedMotion() === true;
    const isFollowUp = contentBlock.isFollowUp;
    const isPaidMembersOnly = contentBlock.isPaidMembersOnly;

    return (
        <motion.article
            id={`workshop-material-${contentBlock.id}`}
            aria-label={ariaLabel}
            initial={isNewlyUnlocked && !isReducedMotionPreferred ? { opacity: 0, y: 24, scale: 0.97 } : false}
            animate={{ opacity: 1, y: 0, scale: 1 }}
            transition={{ duration: isReducedMotionPreferred ? 0 : 0.55, ease: 'easeOut' }}
            className={`relative scroll-mt-5 overflow-hidden rounded-2xl border bg-white/[0.045] p-5 text-slate-200 shadow-lg transition-colors sm:p-8 ${isNewlyUnlocked ? 'border-cyan-300/60 pt-16 shadow-cyan-300/10 sm:pt-8' : isFollowUp || isPaidMembersOnly ? 'border-amber-300/60 shadow-amber-300/10' : 'border-white/10'}`}
        >
            {isNewlyUnlocked && (
                <span className="absolute right-4 top-4 rounded-full bg-cyan-300 px-3 py-1 text-xs font-bold text-slate-950 shadow-lg">
                    Právě odemčeno
                </span>
            )}
            {(isFollowUp || isPaidMembersOnly) && (
                <div className="mb-4 flex flex-wrap gap-2">
                    {isFollowUp && (
                        <span className="inline-flex items-center gap-1.5 rounded-full border border-amber-200/30 bg-amber-300/10 px-3 py-1 text-xs font-bold text-amber-100">
                            <Sparkles className="h-3.5 w-3.5" aria-hidden="true" /> Navazující materiál
                        </span>
                    )}
                    {isPaidMembersOnly && (
                        <span className="inline-flex items-center gap-1.5 rounded-full border border-amber-200/30 bg-amber-300/10 px-3 py-1 text-xs font-bold text-amber-100">
                            <Crown className="h-3.5 w-3.5" aria-hidden="true" /> Pro placené členy
                        </span>
                    )}
                </div>
            )}
            {contentBlock.title && <h3 className="mb-5 text-xl font-bold text-white">{contentBlock.title}</h3>}
            <WorkshopMaterialBody bodyMarkdown={contentBlock.bodyMarkdown} callToActionLabel={callToActionLabel} />
        </motion.article>
    );
}

/**
 * Where the paid materials are, for the members who cannot see them. The materials themselves stay on the server;
 * this card only names them, says that they are here and opens the very same membership popup the badge in the header
 * opens.
 */
function WorkshopPaidMembersContentNotice({
    contentPreviews,
    onUnlockPaidMaterials,
}: {
    readonly contentPreviews: readonly WorkshopContentPreview[];
    readonly onUnlockPaidMaterials: () => void;
}) {
    // A material an administrator left untitled has nothing to tease with, so it keeps this card without being named
    // in it.
    const namedContentPreviews = contentPreviews.filter((contentPreview) => contentPreview.title.trim() !== '');

    return (
        <CommunityPaidMembersNotice
            title="Materiály pro placené členy"
            description="Na tomto místě jsou materiály dostupné jen pro placené členy komunity. Odemknete je měsíčním placeným členstvím."
            onUnlockPaidMembership={onUnlockPaidMaterials}
            unlockedLabel="Co odemknete"
            unlockedContent={
                namedContentPreviews.length === 0 ? undefined : (
                    <ul aria-label="Náhled materiálů pro placené členy" className="space-y-2">
                        {namedContentPreviews.map((contentPreview) => (
                            <li key={contentPreview.id} className="flex items-start gap-2">
                                <Lock className="mt-0.5 h-4 w-4 shrink-0 text-amber-200/70" aria-hidden="true" />
                                <span className="min-w-0 break-words text-sm font-semibold leading-6 text-amber-50">
                                    {contentPreview.title}
                                </span>
                            </li>
                        ))}
                    </ul>
                )
            }
        />
    );
}

export function WorkshopContent({
    contentBlocks,
    nextContentUnlockAt,
    newlyUnlockedContentBlockIds,
    paidMembersOnlyContentPreviews,
    specialMaterials = [],
    title = 'Materiály z workshopu',
}: WorkshopContentProps) {
    // Note: The purchase is only offered while a gate is configured and the member has not paid yet, which is exactly
    //       when the server keeps the paid materials hidden, so the notice and the hidden materials cannot disagree.
    const membershipPurchaseOffer = useCommunityMembershipPurchaseOffer();
    const isPaidMembersContentNoticeShown =
        paidMembersOnlyContentPreviews.length > 0 && membershipPurchaseOffer !== null;
    const isSpecialMaterialShown = specialMaterials.length > 0;
    // Note: The community is invited to at the head of the list of a member who has not joined it yet and stays at
    //       the end of the list of a member who already pays, which is the one difference the membership makes here.
    const isPaidMember = useIsPaidCommunityMember();
    const { specialMaterialsBeforeContentBlocks, specialMaterialsAfterContentBlocks } =
        selectWorkshopSpecialMaterialsByPlacement(specialMaterials, { isPaidMember });

    if (
        contentBlocks.length === 0 &&
        nextContentUnlockAt === null &&
        !isPaidMembersContentNoticeShown &&
        !isSpecialMaterialShown
    ) {
        return null;
    }

    return (
        <section className="mt-8" aria-labelledby="workshop-materials-title">
            <div className="mb-4 flex items-center gap-2">
                <Sparkles className="h-5 w-5 text-cyan-300" />
                <h2 id="workshop-materials-title" className="text-xl font-bold text-white">
                    {title}
                </h2>
            </div>

            <div className="space-y-4">
                {specialMaterialsBeforeContentBlocks.map((specialMaterial) => (
                    <Fragment key={specialMaterial.id}>{specialMaterial.content}</Fragment>
                ))}

                {contentBlocks.map((contentBlock) => (
                    <WorkshopMaterialCard
                            key={contentBlock.id}
                                contentBlock={contentBlock}
                        isNewlyUnlocked={newlyUnlockedContentBlockIds.has(contentBlock.id)}
                            />
                ))}

                {specialMaterialsAfterContentBlocks.map((specialMaterial) => (
                    <Fragment key={specialMaterial.id}>{specialMaterial.content}</Fragment>
                ))}

                {nextContentUnlockAt && (
                    <div className="flex items-start gap-3 rounded-xl border border-dashed border-cyan-300/20 bg-cyan-300/[0.04] px-5 py-4 text-sm text-slate-400">
                        <Clock3 className="h-5 w-5 shrink-0 text-cyan-300" />
                        <span className="min-w-0">
                            Další materiál se automaticky odemkne{' '}
                            {CZECH_DATE_TIME_FORMAT.format(new Date(nextContentUnlockAt))}.
                        </span>
                    </div>
                )}

                {isPaidMembersContentNoticeShown && membershipPurchaseOffer !== null && (
                    <WorkshopPaidMembersContentNotice
                        contentPreviews={paidMembersOnlyContentPreviews}
                        onUnlockPaidMaterials={membershipPurchaseOffer.openMembershipModal}
                    />
                )}
            </div>
        </section>
    );
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx | Select-Object -Skip 220 -First 230; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 418ms:
                        className="mt-6 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-5 py-2.5 text-sm font-bold text-slate-950 hover:bg-cyan-200"
                    >
                        <RefreshCw className="h-4 w-4" /> Zkusit znovu
                    </button>
                </div>
            </main>
        );
    }

    const { state } = controller;
    const roomCapabilities = getWorkshopKindCapabilities(state.workshop.kind);
    const isWorkshopPollVisible = isWorkshopPollVisibleInRoom(state.workshop.kind);
    const isModerating = isWorkshopParticipantModerating(state.participant);
    const followUpContentBlock = state.contentBlocks.find((contentBlock) => contentBlock.isFollowUp) ?? null;
    const connectedRepository = roomCapabilities.isRepositoryOffered ? state.workshop.repository : null;
    const presentationUrl = roomCapabilities.isPresentationOffered ? state.workshop.presentationUrl : null;
    const specialMaterials: readonly WorkshopSpecialMaterial[] = [
        ...(presentationUrl === null
            ? []
            : [
                  {
                      id: 'presentation',
                      content: <WorkshopPresentationMaterial presentationUrl={presentationUrl} />,
                  },
              ]),
        ...(connectedRepository === null
            ? []
            : [
                  {
                      id: 'repository',
                      content: (
                          <WorkshopRepositoryPanel
                              repository={connectedRepository}
                              progressController={repositoryProgressController}
                          />
                      ),
                  },
              ]),
        ...(roomCapabilities.isCommunityInvitationOffered
            ? [
                  {
                      id: 'community',
                      content: <CommunityRoomInvitation participantIdentity={state.participant} />,
                      // Note: This is an invitation rather than something the workshop hands over, so a member who
                      //       has not joined the community yet reads it before the materials, while a member who
                      //       already pays for it keeps it at the end as one more way on. It is written as the one
                      //       placement it is, because a spread condition widens it into any string otherwise.
                      placement: 'before-materials-until-paid' as const,
                  },
              ]
            : []),
    ];

    // Note: What the kind of this room has and what an administrator switched off is asked here once, so a new panel
    //       is one line of the room.
    const isPanelOffered = (panelKey: WorkshopPanelKey) =>
        isWorkshopPanelOffered(state.workshop.kind, state.workshop.disabledPanels, panelKey);

    const roomLayout = (
        <div className="min-h-screen bg-[#06131b] text-slate-200">
            <header className="border-b border-white/[0.07] bg-[#071820]/90 backdrop-blur">
                <div className="mx-auto flex max-w-[1500px] flex-col items-stretch gap-3 px-4 py-3.5 sm:flex-row sm:items-center sm:justify-between sm:gap-4 sm:px-8">
                    <div className="flex min-w-0 flex-1 items-center gap-3">
                        <Image
                            src="/logo/promptbook-logo-blue-white-256.png"
                            alt="Promptbook"
                            width={36}
                            height={36}
                            className="h-9 w-9 rounded-lg"
                        />
                        <div className="min-w-0">
                            <p className="break-words text-sm font-bold text-white">{state.workshop.title}</p>
                            <p className="hidden text-xs text-slate-500 sm:block">{roomSubtitle}</p>
                        </div>
                    </div>
                    <div className="flex min-w-0 flex-col items-stretch gap-2 sm:flex-row sm:items-center sm:gap-3">
                        {isPanelOffered('watching-count') && (
                            <WorkshopWatchingBadge watchingParticipantCount={state.watchingParticipantCount} />
                        )}
                        {/*
                          * Note: Who the participant is connected as, and both ways of changing that — renaming
                          *       themselves and signing out — belong to the room they are connected to, so every room
                          *       offers them from this one badge.
                          */}
                        <WorkshopParticipantBadge
                            fullname={state.participant.fullname}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            isModerating={isModerating}
                            isRefreshing={controller.isRefreshing}
                            onChangeFullname={controller.changeFullname}
                            onDisconnect={roomCapabilities.isDisconnectionOffered ? controller.disconnect : undefined}
                        />
                        {/*
                          * Note: Every room which offers the membership says which one its member has and lets them
                          *       buy or manage it with the very same badge and modal instead of a surface of its own.
                          */}
                        <CommunityMembershipBadge />
                        <CommunityMembershipModal />
                        {participantHeaderSupplement}
                        <WorkshopServerConnectionStatus
                            isUsingCachedState={controller.isUsingCachedState}
                            isRefreshing={controller.isRefreshing}
                            unavailableMessage={controller.isUsingCachedState ? controller.errorMessage : null}
                            onRefresh={() => void controller.refresh()}
                        />
                    </div>
                </div>
            </header>

            <main className="mx-auto grid w-full min-w-0 max-w-[1500px] grid-cols-1 gap-5 px-4 py-4 sm:px-8 sm:py-5 lg:grid-cols-[minmax(0,1fr)_390px]">
                <div className="min-w-0 lg:col-start-1 lg:row-start-1">
                    {controller.errorMessage && !controller.isUsingCachedState ? (
                        <div className="mb-4 flex flex-wrap items-start justify-between gap-3 rounded-xl border border-rose-400/20 bg-rose-400/[0.08] px-4 py-3 text-sm text-rose-200">
                            <span className="min-w-0 flex-1 break-words">{controller.errorMessage}</span>
                            <button
                                type="button"
                                onClick={() => void controller.refresh()}
                                className="shrink-0 font-semibold underline underline-offset-4"
                            >
                                Zkusit znovu
                            </button>
                        </div>
                    ) : null}

                    {roomCapabilities.isStageOffered && (
                        <WorkshopStage
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            subscribeToReactions={controller.subscribeToReactions}
                            repository={state.workshop.repository}
                            subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                            feedback={state.feedback}
                            followUpContentBlock={followUpContentBlock}
                            stageComment={state.stageComment}
                            paidMembersOnlyVideo={state.paidMembersOnlyVideo}
                            onSaveFeedback={controller.saveFeedback}
                        />
                    )}
                    {calendarDetails !== null && (
                        <WorkshopCalendarInvitation
                            workshop={state.workshop}
                            serverTime={state.serverTime}
                            hostFullname={calendarDetails.hostFullname}
                            participantPath={calendarDetails.participantPath}
                            participantIdentity={state.participant}
                        />
                    )}
                    {isWorkshopPollVisible && (
                        <WorkshopPolls
                            polls={state.polls}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onVote={controller.voteOnPoll}
                        />
                    )}
                    {workshopNavigation !== undefined && (
                        <WorkshopLinksPanel
                            workshops={workshopNavigation.workshops}
                            participantIdentity={state.participant}
                            title={workshopNavigation.title}
                            description={workshopNavigation.description}
                            emptyMessage={workshopNavigation.emptyMessage}
                            locale={workshopNavigation.locale}
                            timeZone={workshopNavigation.timeZone}
                            serverTime={state.serverTime}
                            calendarFeedUrl={workshopNavigation.calendarFeedUrl}
                        />
                    )}
                    {mainContentAfterWorkshopNavigation}
                    {isPanelOffered('reactions') && (
                        <WorkshopReactions
                            emojis={state.workshop.allowedReactions}
                            reactionCounts={state.reactionCounts}
                            isInteractionBanned={state.participant.isInteractionBanned}
                            onReact={controller.react}
                        />
                    )}
                </div>

                <WorkshopChat
                    className="min-w-0 lg:col-start-2 lg:row-span-2 lg:row-start-1"
                    comments={state.comments}
                    commentSort={controller.commentSort}
                    isEnabled={isPanelOffered('chat')}
                    isInteractionBanned={state.participant.isInteractionBanned}
                    isModerating={isModerating}
                    onChangeSort={controller.changeCommentSort}
                    onSubmitComment={controller.submitComment}
                    onUpvoteComment={controller.upvoteComment}
                    onModerateComment={controller.moderateComment}
                    onModerateAuthor={controller.moderateAuthor}
                />

                {isMaterialsShown && (
                    <div className="min-w-0 lg:col-start-1 lg:row-start-2">
                        <WorkshopContent
                            contentBlocks={state.contentBlocks}
                            nextContentUnlockAt={state.nextContentUnlockAt}
                            newlyUnlockedContentBlockIds={controller.newlyUnlockedContentBlockIds}
                            paidMembersOnlyContentPreviews={state.paidMembersOnlyContentPreviews}
                            specialMaterials={specialMaterials}
                            title={materialsTitle}
                        />
                    </div>
                )}
            </main>
        </div>
    );

    /*
     * Note: The membership of a member is one membership wherever they read it from, and every surface of the room
     *       which needs to know it — the badge and the modal in the header, the paid materials and the video which a
     *       paid member unlocks after the workshop — asks this one controller instead of loading a membership of its
     *       own.
     */
    return (
        <CommunityMembershipRoomProvider
            workshopSlug={workshopSlug}
            isMembershipOffered={roomCapabilities.isMembershipOffered}
        >
            {roomLayout}
        </CommunityMembershipRoomProvider>
    );
}
'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}


codex
The existing room already has the right post-event boundary: one shared phase rule treats a term as finished only after its recorded end, and the stage then swaps to the existing closing wrap-up. Materials are already loaded as the participant is allowed to see them, so the PDF can safely reuse that same room state without any new admin fields or storage.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content -Raw businesses/online-workshop/participant/WorkshopStage.tsx; Get-Content -Raw package.json; rg -n --glob '"'!node_modules'"' --glob '"'!'"\\.next' \"pdf|PDF|jsPDF|react-pdf|pdf-lib|window.print|print\\(\" . | Select-Object -First 300; Get-Content -Raw businesses/online-workshop/participant/WorkshopStage.test.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.test.tsx" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 831ms:
'use client';

import { WorkshopWrapUp } from '@/businesses/online-workshop/participant/WorkshopWrapUp';
import { WorkshopStageComment } from '@/businesses/online-workshop/participant/WorkshopStageComment';
import { WorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/WorkshopRepositoryCommitNotification';
import { useWorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/useWorkshopRepositoryCommitNotification';
import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import { useWorkshopReactionStream } from '@/components/workshops/useWorkshopReactionStream';
import { WorkshopReactionStream } from '@/components/workshops/WorkshopReactionStream';
import { trackGoogleAnalyticsEvent } from '@/lib/tracking/track-google-analytics-event';
import { createYoutubeEmbedUrl } from '@/lib/youtube/youtubeEmbed';
import { keepYoutubeVideoSubtitlesHidden, unmuteYoutubeVideo } from '@/lib/youtube/youtubePlayerCommands';
import { getWorkshopPhase } from '@/lib/workshops/workshopPhase';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type {
    WorkshopCommentReference,
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { motion, useReducedMotion } from 'framer-motion';
import { ArrowDownLeft, ArrowLeft, Maximize, Play, Radio, Volume2 } from 'lucide-react';
import { useEffect, useMemo, useRef, useState } from 'react';

const CLOCK_TICK_MILLISECONDS = 1000;

type WorkshopStageProps = {
    readonly workshop: WorkshopDetails;
    readonly serverTime: string;

    /**
     * Where the reactions of the room come from
     *
     * Note: The stage keeps the flying reactions itself, so a busy room re-renders nothing but the stage they fly over.
     */
    readonly subscribeToReactions: SubscribeToWorkshopReactions;

    /** The project connected to this workshop, if there is one */
    readonly repository?: WorkshopRepository | null;

    /** Offers the stage commits found by the repository monitor or its polling fallback */
    readonly subscribeToRepositoryCommits?: SubscribeToWorkshopRepositoryCommits;

    /**
     * All of these values are supplied by the participant room in production. Defaults keep this low-level stage usable
     * in isolation while it is still responsible for choosing the correct temporal phase.
     */
    readonly feedback?: WorkshopFeedback | null;
    readonly followUpContentBlock?: WorkshopContentBlock | null;
    readonly stageComment?: WorkshopCommentReference | null;

    /**
     * The recording which this member has not unlocked, or `null` while nothing of the video is withheld from them
     *
     * Note: The server decides this together with the video it hands over, so the stage plays whatever video it was
     *       given and offers the membership exactly when it was given none.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;
    readonly onSaveFeedback?: (values: WorkshopFeedbackValues) => Promise<boolean>;
};

function getRemainingSegments(remainingMilliseconds: number) {
    const remainingSeconds = Math.max(0, Math.floor(remainingMilliseconds / 1000));
    return [
        { label: 'dní', value: Math.floor(remainingSeconds / 86400) },
        { label: 'hodin', value: Math.floor((remainingSeconds % 86400) / 3600) },
        { label: 'minut', value: Math.floor((remainingSeconds % 3600) / 60) },
        { label: 'sekund', value: remainingSeconds % 60 },
    ];
}

function requestVideoFullscreen(videoFrame: HTMLIFrameElement | null): void {
    const requestFullscreen = videoFrame?.requestFullscreen;
    if (requestFullscreen === undefined || videoFrame === null) {
        return;
    }

    void requestFullscreen.call(videoFrame).catch(() => undefined);
}

const refuseStandaloneFeedbackSave = async (): Promise<boolean> => false;

export function WorkshopStage({
    workshop,
    serverTime,
    subscribeToReactions,
    repository = null,
    subscribeToRepositoryCommits,
    feedback = null,
    followUpContentBlock = null,
    stageComment = null,
    paidMembersOnlyVideo = null,
    onSaveFeedback = refuseStandaloneFeedbackSave,
}: WorkshopStageProps) {
    const isReducedMotionPreferred = useReducedMotion() === true;
    const serverClockOffset = useMemo(() => Date.parse(serverTime) - Date.now(), [serverTime]);
    const [currentTime, setCurrentTime] = useState(() => Date.now() + serverClockOffset);
    const [isVideoUnmuted, setIsVideoUnmuted] = useState(false);
    const videoFrameReference = useRef<HTMLIFrameElement>(null);
    const { flyingReactions, launchReaction } = useWorkshopReactionStream();

    const phase = getWorkshopPhase(workshop, currentTime);
    const isWorkshopOngoing = phase === 'ongoing';
    const isWorkshopPast = phase === 'past';
    const remainingMilliseconds = Date.parse(workshop.startsAt) - currentTime;
    const newRepositoryCommit = useWorkshopRepositoryCommitNotification({
        workshopSlug: workshop.slug,
        isWorkshopOngoing,
        isEnabled: repository !== null,
        subscribeToRepositoryCommits,
    });

    // Note: Once the workshop is over, the room only holds its video for the members whose membership pays for it, and
    //       the server is what decides that. The wrap-up therefore keeps its feedback for everybody and gains either
    //       the button which plays the video again or the offer of the membership which unlocks it.
    const isVideoRewatchOffered = isWorkshopPast && workshop.youtubeVideoId !== null;
    const [isVideoRewatchShown, setIsVideoRewatchShown] = useState(false);
    useEffect(() => {
        if (!isVideoRewatchOffered) {
            setIsVideoRewatchShown(false);
        }
    }, [isVideoRewatchOffered]);

    useEffect(() => subscribeToReactions(launchReaction), [launchReaction, subscribeToReactions]);

    useEffect(() => {
        setCurrentTime(Date.now() + serverClockOffset);
        const intervalId = window.setInterval(
            () => setCurrentTime(Date.now() + serverClockOffset),
            CLOCK_TICK_MILLISECONDS,
        );
        return () => window.clearInterval(intervalId);
    }, [serverClockOffset]);

    useEffect(() => setIsVideoUnmuted(false), [workshop.youtubeVideoId]);

    useEffect(() => {
        if (workshop.youtubeVideoId === null || !isWorkshopOngoing) {
            return;
        }

        return keepYoutubeVideoSubtitlesHidden(videoFrameReference.current);
    }, [workshop.youtubeVideoId, isWorkshopOngoing]);

    const countdownSegments = getRemainingSegments(remainingMilliseconds);
    const handleVideoUnmute = () => {
        unmuteYoutubeVideo(videoFrameReference.current);
        setIsVideoUnmuted(true);
        trackGoogleAnalyticsEvent('workshop_video_unmuted', { workshop_slug: workshop.slug });
    };
    const handleVideoFullscreen = () => requestVideoFullscreen(videoFrameReference.current);

    return (
        <section className="relative overflow-hidden rounded-2xl border border-white/10 bg-[#081a24] shadow-2xl">
            {isWorkshopPast && isVideoRewatchShown && workshop.youtubeVideoId !== null ? (
                <div>
                    <div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 px-5 py-3">
                        <span className="inline-flex items-center gap-2 text-sm font-bold text-white">
                            <Play className="h-4 w-4 text-amber-300" aria-hidden="true" /> Video z workshopu
                        </span>
                        <button
                            type="button"
                            onClick={() => setIsVideoRewatchShown(false)}
                            className="inline-flex items-center gap-2 rounded-full border border-white/20 bg-slate-950/60 px-3 py-1.5 text-xs font-semibold text-slate-200 transition hover:border-cyan-200/70 hover:bg-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200"
                        >
                            <ArrowLeft className="h-3.5 w-3.5" aria-hidden="true" /> Zpět na závěrečné shrnutí
                        </button>
                    </div>
                    <div className="relative aspect-video">
                        <iframe
                            className="absolute inset-0 h-full w-full"
                            src={createYoutubeEmbedUrl(workshop.youtubeVideoId, {
                                isAutoplayed: true,
                                isInlinePlayback: true,
                                isRelatedVideoEnabled: false,
                                isControlsVisible: true,
                                isJavaScriptApiEnabled: false,
                                startAtSeconds: workshop.recordingStartOffsetSeconds,
                            })}
                            title={workshop.title}
                            allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                            referrerPolicy="strict-origin-when-cross-origin"
                            allowFullScreen
                        />
                    </div>
                </div>
            ) : isWorkshopPast ? (
                <WorkshopWrapUp
                    feedback={feedback}
                    followUpContentBlock={followUpContentBlock}
                    paidMembersOnlyVideo={paidMembersOnlyVideo}
                    onSaveFeedback={onSaveFeedback}
                    onRewatchVideo={isVideoRewatchOffered ? () => setIsVideoRewatchShown(true) : undefined}
                />
            ) : (
                <div
                    className={`relative min-w-0 w-full max-w-full aspect-video ${isWorkshopOngoing ? 'min-h-[220px] sm:min-h-[260px]' : 'min-h-[280px] sm:min-h-[260px]'}`}
                >
                    {isWorkshopOngoing && workshop.youtubeVideoId ? (
                        <iframe
                            ref={videoFrameReference}
                            className="absolute inset-0 h-full w-full"
                            src={createYoutubeEmbedUrl(workshop.youtubeVideoId, {
                                isAutoplayed: true,
                                isMuted: true,
                                isInlinePlayback: true,
                                isRelatedVideoEnabled: false,
                                isControlsVisible: false,
                                isCaptionsEnabled: false,
                                isJavaScriptApiEnabled: true,
                            })}
                            title={workshop.title}
                            allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                            referrerPolicy="strict-origin-when-cross-origin"
                            allowFullScreen
                        />
                    ) : isWorkshopOngoing ? (
                        <div className="absolute inset-0 flex flex-col items-center justify-center bg-[radial-gradient(circle_at_center,rgba(48,168,189,.24),transparent_52%)] px-8 text-center">
                            <Radio className="h-11 w-11 animate-pulse text-cyan-300" />
                            <h2 className="mt-5 text-2xl font-bold text-white">Stream právě připravujeme</h2>
                            <p className="mt-2 max-w-md text-sm text-slate-400">
                                Video se zde objeví automaticky, jakmile administrátor vloží YouTube stream.
                            </p>
                        </div>
                    ) : (
                        <div className="absolute inset-0 flex flex-col items-center justify-center bg-[radial-gradient(circle_at_50%_35%,rgba(122,235,255,.16),transparent_42%)] px-4 py-5 text-center sm:px-5">
                            <span className="inline-flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/10 px-3 py-1 text-[11px] font-semibold uppercase leading-5 tracking-[0.16em] text-cyan-200 sm:text-xs sm:tracking-[0.18em]">
                                <span className="h-2 w-2 animate-pulse rounded-full bg-cyan-300" /> Začínáme za
                            </span>
                            <div className="mt-5 grid w-full max-w-[19rem] grid-cols-2 gap-2 sm:mt-7 sm:w-auto sm:max-w-none sm:grid-cols-4 sm:gap-4">
                                {countdownSegments.map((segment) => (
                                    <div
                                        key={segment.label}
                                        className="min-w-0 rounded-xl border border-white/10 bg-white/5 px-2 py-2.5 text-center sm:min-w-[82px] sm:px-4 sm:py-4"
                                    >
                                        <div className="font-mono text-3xl font-bold tabular-nums text-white sm:text-4xl">
                                            {String(segment.value).padStart(2, '0')}
                                        </div>
                                        <div className="mt-1 text-[10px] uppercase tracking-wider text-slate-500 sm:text-xs">
                                            {segment.label}
                                        </div>
                                    </div>
                                ))}
                            </div>
                            <p className="mt-4 w-full max-w-[25rem] px-2 text-sm leading-6 text-slate-400 sm:mt-6">
                                Stránku nemusíte obnovovat. Stream se spustí automaticky.
                            </p>
                        </div>
                    )}
                </div>
            )}

            <WorkshopReactionStream reactions={flyingReactions} />
            {isWorkshopOngoing && newRepositoryCommit !== null && repository !== null && (
                <WorkshopRepositoryCommitNotification
                    key={newRepositoryCommit.sha}
                    repository={repository}
                    commit={newRepositoryCommit}
                />
            )}
            {isWorkshopOngoing && <WorkshopStageComment stageComment={stageComment} />}

            {isWorkshopOngoing && workshop.youtubeVideoId && (
                <button
                    type="button"
                    onClick={handleVideoFullscreen}
                    aria-label="Přehrát video na celé obrazovce"
                    className="absolute right-3 top-3 z-20 inline-flex items-center gap-2 rounded-full border border-white/20 bg-slate-950/90 px-3 py-2 text-xs font-semibold text-white shadow-lg transition hover:border-cyan-200/70 hover:bg-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 sm:right-5 sm:top-5"
                >
                    <Maximize className="h-4 w-4" aria-hidden="true" />
                    <span className="hidden sm:inline">Celá obrazovka</span>
                </button>
            )}

            {isWorkshopOngoing && workshop.youtubeVideoId && !isVideoUnmuted && (
                <motion.div
                    initial={isReducedMotionPreferred ? false : { opacity: 0, y: 12 }}
                    animate={{ opacity: 1, y: 0 }}
                    className="absolute bottom-3 left-3 right-3 z-20 rounded-2xl border border-cyan-200/40 bg-slate-950/95 p-3 shadow-2xl backdrop-blur sm:bottom-5 sm:left-6 sm:right-auto sm:max-w-xs sm:p-4"
                >
                    <div className="flex items-start gap-3">
                        <ArrowDownLeft className="mt-1 h-8 w-8 shrink-0 animate-bounce text-cyan-300" aria-hidden="true" />
                        <div>
                            <p className="text-sm font-bold text-white">Zapněte si zvuk</p>
                            <p className="mt-1 text-xs leading-5 text-slate-300">
                                Klikněte na tlačítko níže – stream se kvůli automatickému spuštění otevírá ztlumený.
                            </p>
                            <button
                                type="button"
                                onClick={handleVideoUnmute}
                                className="mt-3 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-4 py-2 text-sm font-bold text-slate-950 transition hover:bg-cyan-200"
                            >
                                <Volume2 className="h-4 w-4" /> Zapnout zvuk
                            </button>
                        </div>
                    </div>
                </motion.div>
            )}
        </section>
    );
}

{
    "name": "promptbook-landing-page",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "npx kill-port 4009 && next dev -p 4009",
        "build": "next build",
        "start": "next start",
        "lint": "next lint",
        "test": "vitest run",
        "test-e2e": "playwright test",
        "test-e2e:headed": "playwright test --headed",
        "test-types": "npm run build && tsc",
        "migrate-database": "tsx scripts/migrate-database.ts",
        "backup-database": "tsx scripts/backup-database.ts",
        "backup-database:verify": "tsx scripts/backup-database-verify.ts",
        "delete-test-data": "tsx scripts/delete-test-data.ts",
        "test-for-ptbk-coder": "npm run lint && npx kill-port 4009 && npm run test-types && npm run test-e2e && npm run delete-test-data",
        "export": "next build",
        "coder:generate-boilerplates": "npx ptbk coder generate-boilerplates --template ./prompts/templates/common.md",
        "coder:run": "npx ptbk coder run --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-push --auto-pull",
        "coder:run:openai": "npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-push --auto-pull",
        "coder:find-refactor-candidates": "npx ptbk coder find-refactor-candidates",
        "coder:verify": "npx ptbk coder verify --order from-latest --commit --auto-pull --auto-push",
        "coder:add": "npx ptbk coder add --template ./prompts/templates/common.md"
    },
    "dependencies": {
        "@hookform/resolvers": "^5.0.1",
        "@radix-ui/react-accordion": "^1.2.3",
        "@radix-ui/react-alert-dialog": "^1.1.6",
        "@radix-ui/react-aspect-ratio": "^1.1.2",
        "@radix-ui/react-avatar": "^1.1.3",
        "@radix-ui/react-checkbox": "^1.1.4",
        "@radix-ui/react-collapsible": "^1.1.3",
        "@radix-ui/react-context-menu": "^2.2.6",
        "@radix-ui/react-dialog": "^1.1.6",
        "@radix-ui/react-dropdown-menu": "^2.1.6",
        "@radix-ui/react-hover-card": "^1.1.6",
        "@radix-ui/react-label": "^2.1.2",
        "@radix-ui/react-menubar": "^1.1.6",
        "@radix-ui/react-navigation-menu": "^1.2.5",
        "@radix-ui/react-popover": "^1.1.6",
        "@radix-ui/react-progress": "^1.1.2",
        "@radix-ui/react-radio-group": "^1.2.3",
        "@radix-ui/react-scroll-area": "^1.2.3",
        "@radix-ui/react-select": "^2.1.6",
        "@radix-ui/react-separator": "^1.1.2",
        "@radix-ui/react-slider": "^1.2.3",
        "@radix-ui/react-slot": "^1.1.2",
        "@radix-ui/react-switch": "^1.1.3",
        "@radix-ui/react-tabs": "^1.1.3",
        "@radix-ui/react-toast": "^1.2.6",
        "@radix-ui/react-toggle": "^1.1.2",
        "@radix-ui/react-toggle-group": "^1.1.2",
        "@radix-ui/react-tooltip": "^1.1.8",
        "@supabase/ssr": "^0.7.0",
        "@supabase/supabase-js": "^2.57.2",
        "class-variance-authority": "^0.7.1",
        "clsx": "^2.1.1",
        "cmdk": "^1.1.1",
        "date-fns": "^3.6.0",
        "dotenv": "^16.4.7",
        "embla-carousel-react": "^8.5.2",
        "framer-motion": "^12.6.3",
        "input-otp": "^1.4.2",
        "js-yaml": "^4.1.0",
        "leaflet": "^1.9.4",
        "logrocket": "^12.1.1",
        "lucide-react": "^0.487.0",
        "moment": "^2.30.1",
        "next": "15.2.6",
        "next-themes": "^0.4.6",
        "pg": "^8.16.3",
        "react": "^18.3.1",
        "react-day-picker": "^8.10.1",
        "react-dom": "^18.3.1",
        "react-hook-form": "^7.55.0",
        "react-qr-code": "^2.0.18",
        "react-resizable-panels": "^2.1.7",
        "react-use-measure": "^2.1.7",
        "recharts": "^2.15.1",
        "sharp": "^0.34.4",
        "sonner": "^2.0.3",
        "spacetrim": "^0.11.60",
        "stripe": "^22.6.0",
        "tailwind-merge": "^3.1.0",
        "tw-animate-css": "^1.2.5",
        "vaul": "^1.1.2",
        "zod": "^3.24.2"
    },
    "devDependencies": {
        "@babel/eslint-parser": "^7.27.1",
        "@eslint/eslintrc": "^3",
        "@playwright/test": "^1.62.1",
        "@testing-library/jest-dom": "^6.6.3",
        "@testing-library/react": "^16.3.0",
        "@total-typescript/ts-reset": "^0.6.1",
        "@types/js-yaml": "^4.0.9",
        "@types/lodash": "^4.17.20",
        "@types/node": "^20",
        "@types/pg": "^8.15.5",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "autoprefixer": "^10.4.21",
        "eslint": "^9",
        "eslint-config-next": "15.2.4",
        "jsdom": "^26.0.0",
        "postcss": "^8.5.3",
        "ptbk": "0.114.0-38",
        "raw-loader": "^4.0.2",
        "tailwindcss": "^3.4.17",
        "tailwindcss-animate": "^1.0.7",
        "tsx": "^4.19.4",
        "typescript": "^5",
        "vitest": "^3.1.1"
    }
}

.\lib\exports\svgChartExport.ts:31: * The printable page, in the points a PDF measures everything in, and the margin the picture keeps from its edges
.\lib\exports\svgChartExport.ts:33:const PDF_PAGE_WIDTH_POINTS = 842;
.\lib\exports\svgChartExport.ts:34:const PDF_PAGE_HEIGHT_POINTS = 595;
.\lib\exports\svgChartExport.ts:35:const PDF_PAGE_MARGIN_POINTS = 24;
.\lib\exports\svgChartExport.ts:201:    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
.\lib\exports\svgChartExport.ts:202:    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
.\lib\exports\svgChartExport.ts:210:        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
.\lib\exports\svgChartExport.ts:211:        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
.\lib\exports\svgChartExport.ts:218: * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
.\lib\exports\svgChartExport.ts:230:        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
.\lib\exports\svgChartExport.ts:235:    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
.\lib\exports\svgChartExport.ts:268:    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
.\lib\exports\svgChartExport.test.ts:9:async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
.\lib\exports\svgChartExport.test.ts:10:    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
.\lib\exports\svgChartExport.test.ts:17:        const pdfText = await readPdfAsLatin1Text(
.\lib\exports\svgChartExport.test.ts:21:        expect(pdfText.startsWith('%PDF-1.4')).toBe(true);
.\lib\exports\svgChartExport.test.ts:22:        expect(pdfText.endsWith('%%EOF\n')).toBe(true);
.\lib\exports\svgChartExport.test.ts:23:        expect(pdfText).toContain('/Filter /DCTDecode');
.\lib\exports\svgChartExport.test.ts:24:        expect(pdfText).toContain(`/Length ${PICTURE_BYTES.length}`);
.\lib\exports\svgChartExport.test.ts:28:        const pdfText = await readPdfAsLatin1Text(
.\lib\exports\svgChartExport.test.ts:32:        const crossReferenceOffset = Number(/startxref\n(\d+)\n/.exec(pdfText)?.[1]);
.\lib\exports\svgChartExport.test.ts:33:        expect(pdfText.startsWith('xref', crossReferenceOffset)).toBe(true);
.\lib\exports\svgChartExport.test.ts:35:        const objectOffsets = Array.from(pdfText.matchAll(/^(\d{10}) 00000 n $/gm)).map((match) => Number(match[1]));
.\lib\exports\svgChartExport.test.ts:38:            expect(pdfText.startsWith(`${objectIndex + 1} 0 obj`, objectOffset)).toBe(true);
.\lib\exports\svgChartExport.test.ts:43:        const pdfText = await readPdfAsLatin1Text(
.\lib\exports\svgChartExport.test.ts:48:        expect(pdfText).toContain('/Title <FEFF005001590020002800320030003200360029>');
.\lib\exports\svgChartExport.test.ts:49:        expect(pdfText).not.toContain('/Title (');
.\lib\exports\svgChartExport.test.ts:53:        const pdfText = await readPdfAsLatin1Text(
.\lib\exports\svgChartExport.test.ts:57:        const [, pictureWidth, pictureHeight] = /\n(\d+\.\d+) 0 0 (\d+\.\d+) /.exec(pdfText) ?? [];
.\package-lock.json:5549:                "jspdf": "4.1.0",
.\package-lock.json:5931:            "integrity": "sha512-xla6AOfz68a6kq3gRQccWEvFC/VRGJmA/QuSLENSO7CZX5WIEkSz7r1FdXUjtGCQ1c2M+ndUAH7opdfLK1PQbw==",
.\package-lock.json:7020:                "jspdf": "4.1.0",
.\package-lock.json:11419:        "node_modules/@promptbook/pdf": {
.\package-lock.json:11421:            "resolved": "https://registry.npmjs.org/@promptbook/pdf/-/pdf-0.114.0-35.tgz",
.\package-lock.json:15599:            "integrity": "sha512-jGTk8UD/RdjsNZW8qq10r0RBvxL8OWtoT+kImlzPDFilmozzM+9QmIJsmze9UiSBrFU45ZxhTYBypn9q9z/VfQ==",
.\package-lock.json:17122:            "integrity": "sha512-/HjUqcV8XpUQET/P5fqVCsm9ZXpeYwHpKVJtv+pIPDF1Zk5thHCKgyC3qIM+/ZGBDqt6MVrhpyrYIvEHFyd3dQ==",
.\package-lock.json:25186:        "node_modules/jspdf": {
.\package-lock.json:25188:            "resolved": "https://registry.npmjs.org/jspdf/-/jspdf-4.1.0.tgz",
.\package-lock.json:25910:                "pdf-parse": "^2.4.5",
.\package-lock.json:27640:        "node_modules/pdf-parse": {
.\package-lock.json:27642:            "resolved": "https://registry.npmjs.org/pdf-parse/-/pdf-parse-2.4.5.tgz",
.\package-lock.json:27648:                "pdfjs-dist": "5.4.296"
.\package-lock.json:27651:                "pdf-parse": "bin/cli.mjs"
.\package-lock.json:27661:        "node_modules/pdfjs-dist": {
.\package-lock.json:27663:            "resolved": "https://registry.npmjs.org/pdfjs-dist/-/pdfjs-dist-5.4.296.tgz",
.\package-lock.json:28220:                "@promptbook/pdf": "0.114.0-35",
.\lib\workshops\workshopValues.test.ts:60:            createWorkshopUpdateDatabaseValues({ presentationUrl: 'https://files.example.com/workshop.pdf' }),
.\lib\workshops\workshopValues.test.ts:62:            presentation_url: 'https://files.example.com/workshop.pdf',
.\lib\workshops\workshopTypes.ts:105:     * A public PDF, presentation file, or Markdown page which belongs to this workshop, or `null` while none was
.\migrations\2026-09-1500-workshop-presentation.sql:2:-- public address: PDFs, PowerPoint files, and GitHub Markdown pages all remain where their author publishes them.
.\lib\workshops\workshopSchemas.test.ts:147:    it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\lib\workshops\workshopSchemas.test.ts:149:            workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\lib\workshops\workshopSchemas.test.ts:151:        ).toBe('https://files.example.com/ai-agents.pdf');
.\components\faq-section.tsx:32:        answer: 'Ne. Promptbook automatizuje tu pěnu dní - rutinní dotazy, hledání PDFek, opakované odpovídání na to samé dokola. Vaši lidé díky tomu mají čas na smysluplnou práci a skutečnou spolupráci. Nenahrazujeme lidskou komunikaci - odstraňujeme tu její část, která všechny štve.',
.\public\integrations\default.svg:50:     xlink:href="data:image/png;base64,"
.\changelog\_current-preversion.md:15:- Added a presentation setting to `/admin/workshops`. A workshop can now link a PDF, PowerPoint file, or public GitHub
.\changelog\_current-preversion.md:295:- Let that graph be taken apart and taken away: every line is switched on and off by its own name, which says its number in the shown span as well; the reaction line counts either every reaction or one chosen emoji; and the whole thing is exported as SVG, PNG, PDF or as a CSV of the very numbers which are drawn, zoom, chosen lines and all.
.\public\backgrounds\pro-mesta.svg:156:       xlink:href="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAABAAAAAQACAYAAAB/HSuDAAAABHNCSVQICAgIfAhkiAAAIABJREFU&#10;eJzs3XmwLdt9F/Zvn+EO7z0NT7NlSWjwBLKxDS6wgAQZHCibhKnApOIQh1DYIQkJUyoUFcqmGKpC&#10;CkgqCUFiCmWm2NghMWBIESyCq7DLgDEegoVsyYNsS0LW8KT33r33nNP5Y/V6u+959559zundp7t3&#10;fz6vbt37zrB3d+/Vq9f6rd9aq0lynHE1I78+67atfLU3chQwjm3lV/0KAMujfcqYLixfBzd1FAAA&#10;AMB0jqY+ABhIBBUAAKC4MENUBgAAAACsgAwAAACA3ZGhymzJAAAAAIAVEAAAAACAFRAAAAAAgBWw&#10;BgCwZNvm2F24CuoMDJ0jOPc5hkOv/1XP76rvN/X1G7t8Tn1+20x9fw69PmOX76mvz1D7fn5c7Pzn&#10;v7TPe2j53fb9udfP7DEZAAAAALACAgAAAACwAgIAAAAAsALWAACWbGlzCgGA+dO+YG/JAAAAAIAV&#10;EAAAAACAFRAAAAAAgBVokhzfwHsAAAAwXDv1AbBcMgAAAABgBQQAAAAAYAUEAAAAAGAFji7xM9vm&#10;8JuDAgAAcDOGrrGm/7ZiMgAAAABgBQQAAAAAYAUEAAAAAGAFLrMGgDkiAAAAy6D/xmPJAAAAAIAV&#10;EAAAAACAFRAAAAAAgBW4zBoAAAAA7Idm5Ne3BsGMyQAAAACAFZABAKzZtgi4CDYAAHtDBgAAAACs&#10;wGUyAMaeIwK8WL3vjECPy/UFAPaNOf48lgwAAAAAWAEBAAAAAFgBAQCGMD0EAABgIewCwHXo+AMA&#10;wDoN7QtsW0PALk0XG3R9ZABwVTr/AAAACyQDYDsRrouNfX2mtvTPZ+n2vXwN5fpczPVhSp4fwFyp&#10;n1ZMBgBXYfQfAABgoZokt6Y+CBZt7iNs9fhEMgEAYPm06y92Yf9MBgBrIXsBAABYNWsAMHfmKAEA&#10;AOyAAMD05t7BHXvk/CZH
entationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\businesses\_generic\default.book:9:KNOWLEDGE https://company.com/company-policies.pdf
.\lib\workshops\workshopKindCapabilities.test.ts:105:                presentationUrl: 'https://files.example.com/workshop.pdf',
.\lib\workshops\workshopKindCapabilities.test.ts:126:                presentationUrl: 'https://files.example.com/workshop.pdf',
.\businesses\workshop-admin\WorkshopSettingsForm.tsx:319:                            placeholder="https://…/prezentace.pdf"
.\businesses\workshop-admin\WorkshopSettingsForm.tsx:322:                            Nepovinné. Může vést na PDF, PowerPoint nebo veřejný Markdown na GitHubu. Všichni účastníci
.\businesses\workshop-admin\WorkshopOverviewExportButtons.tsx:16:const WORKSHOP_OVERVIEW_EXPORT_FORMATS = ['CSV', 'SVG', 'PNG', 'PDF'] as const;
.\prompts\2026-09-0110-presentation-as-special-material.md:7:- Presentation can be both a PDF, PPT or link to the GitHub Markdown.
.\prompts\2026-09-0110-presentation-as-special-material.md:24:- Presentation can be both a PDF, PPT or link to the GitHub Markdown.
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:281:- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:576:- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:1828:     * A public PDF, presentation file, or Markdown page which belongs to this workshop, or `null` while none was
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:6894:                            placeholder="https://…/prezentace.pdf"
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:6897:                            Nepovinné. Může vést na PDF, PowerPoint nebo veřejný Markdown na GitHubu. Všichni účastníci
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:7602:     * The public PDF, presentation file, or Markdown page participants receive as a shared workshop material.
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:10278:-- public address: PDFs, PowerPoint files, and GitHub Markdown pages all remain where their author publishes them.
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11298:lib\exports\svgChartExport.ts:210:        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11299:lib\exports\svgChartExport.ts:211:        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11310:lib\exports\svgChartExport.test.ts:32:        const crossReferenceOffset = Number(/startxref\n(\d+)\n/.exec(pdfText)?.[1]);
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11311:lib\exports\svgChartExport.test.ts:33:        expect(pdfText.startsWith('xref', crossReferenceOffset)).toBe(true);
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11312:lib\exports\svgChartExport.test.ts:35:        const objectOffsets = Array.from(pdfText.matchAll(/^(\d{10}) 00000 n $/gm)).map((match) => Number(match[1]));
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11315:lib\exports\svgChartExport.test.ts:38:            expect(pdfText.startsWith(`${objectIndex + 1} 0 obj`, objectOffset)).toBe(true);
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11455:            createWorkshopUpdateDatabaseValues({ presentationUrl: 'https://files.example.com/workshop.pdf' }),
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11457:            presentation_url: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11678:    it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11680:            workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:11682:        ).toBe('https://files.example.com/ai-agents.pdf');
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:12115:    presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:12689:    presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:13015:                presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:13035:                presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:14337:- Added a presentation setting to `/admin/workshops`. A workshop can now link a PDF, PowerPoint file, or public GitHub
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:14617:- Let that graph be taken apart and taken away: every line is switched on and off by its own name, which says its number in the shown span as well; the reaction line counts either every reaction or one chosen emoji; and the whole thing is exported as SVG, PNG, PDF or as a CSV of the very numbers which are drawn, zoom, chosen lines and all.
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:14861:- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18130:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18209:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18258:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18434:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18436:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18842:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18921:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:18970:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19146:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19148:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19656:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19735:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19784:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19960:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:19962:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:20831:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:20910:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:20959:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:21135:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:21137:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:21647:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:21726:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:21791:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22002:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22004:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22508:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22587:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22652:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22863:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:22865:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:23684:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:23763:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:23828:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24039:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24041:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24550:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24629:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24694:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24905:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:24907:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:25411:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:25490:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:25555:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:25766:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:25768:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:26043:  Name "URL prezentace Nepovinné. Může vést na PDF, PowerPoint nebo veřejný Markdown na GitHubu. Všichni účastníci ji najdou mezi materiály bez čekání a bez placeného členství.":
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:26047:    placeholder="https://…/prezentace.pdf"
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:26519:  Name "URL prezentace Nepovinné. Může vést na PDF, PowerPoint nebo veřejný Markdown na GitHubu. Všichni účastníci ji najdou mezi materiály bez čekání a bez placeného členství.":
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:26523:    placeholder="https://…/prezentace.pdf"
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:27266:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:27345:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:27410:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:27621:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:27623:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28130:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28209:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28274:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28485:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28487:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:28991:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29070:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29135:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29346:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29348:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29870:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:29949:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30014:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30225:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30227:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30735:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30814:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:30879:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31090:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31092:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31616:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31695:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31760:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31971:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:31973:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:32556:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:32635:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:32700:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:32911:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:32913:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:33419:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:33498:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:33563:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:33774:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:33776:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:34289:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:34368:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:34433:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:34644:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:34646:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:35175:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:35254:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:35319:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:35530:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:35532:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:36225:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:36304:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:36369:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:36580:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:36582:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37113:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37192:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37257:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37468:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37470:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:37979:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38058:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38123:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38334:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38336:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38844:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38923:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:38988:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:39199:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:39201:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:39706:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:39785:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:39850:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40061:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40063:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40571:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40650:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40715:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40926:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:40928:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:41437:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:41516:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:41581:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:41792:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:41794:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:42309:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:42388:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:42453:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:42664:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:42666:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:43176:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:43255:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:43320:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:43531:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:43533:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:44958:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45037:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45102:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45313:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45315:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45824:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45903:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:45968:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:46179:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:46181:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:46696:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:46775:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:46840:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47051:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47053:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47563:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47642:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47707:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47918:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:47920:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:52580:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:52659:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:52724:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:52935:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:52937:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:53871:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:53950:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54015:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54226:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54228:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54807:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54886:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:54951:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:55162:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:55164:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:55684:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:55763:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:55844:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56056:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56058:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56584:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56663:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56744:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56956:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:56958:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:57506:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:57585:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:57666:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:57878:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:57880:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:58409:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:58488:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:58569:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:58781:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:58783:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:59333:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:59412:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:59493:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:59705:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:59707:             workshopUpdateSchema.parse({ presentationUrl: ' https://files.example.com/ai-agents.pdf#introduction ' })
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:60254:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:60333:     presentationUrl: 'https://files.example.com/production-ai-workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:60414:                 presentationUrl: 'https://files.example.com/workshop.pdf',
.\prompts\traces\2026-09-0220-starting-minute-of-video.md:60626:     it('accepts a presentation as a PDF, PowerPoint file, or GitHub Markdown page and normalizes its public URL', () => {
/**
 * @vitest-environment jsdom
 */

import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
import { WorkshopStage } from '@/businesses/online-workshop/participant/WorkshopStage';
import type { CommunityMembershipRoomState } from '@/lib/community-membership/communityMembershipTypes';
import { DEFAULT_EVENT_DETAILS } from '@/lib/events/event';
import type { GithubCommit } from '@/lib/github/githubCommitFeed';
import type { FlyingWorkshopReaction } from '@/lib/workshops/workshopReactionAnimations';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type { WorkshopCommentReference, WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
import { act, cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
import { afterEach, describe, expect, it, vi } from 'vitest';

/**
 * The room as far as the stage is concerned: the membership it already loaded for the member watching it
 */
const membershipRoomMock = vi.hoisted(() => ({
    membershipRoom: null as null | {
        membership: CommunityMembershipRoomState | null;
        openMembershipModal: () => void;
    },
}));

vi.mock('@/businesses/community/membership/CommunityMembershipRoomProvider', () => ({
    useCommunityMembershipRoom: () => membershipRoomMock.membershipRoom,
}));

const PAID_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'active',
    monthlyPriceCzk: 199,
    currentPeriodEndsAt: '2026-09-30T10:00:00.000Z',
    isCancellationScheduled: false,
    isPurchaseOffered: false,
    isSubscriptionManagementOffered: true,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const FREE_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'none',
    monthlyPriceCzk: null,
    currentPeriodEndsAt: null,
    isCancellationScheduled: false,
    isPurchaseOffered: true,
    isSubscriptionManagementOffered: false,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const WORKSHOP: WorkshopDetails = {
    id: '5a7eb2ad-2583-4e98-9640-50bc773b5fde',
    kind: 'workshop',
    event: DEFAULT_EVENT_DETAILS,
    slug: 'online-workshop-2026-08-20',
    title: 'Produkční kód s AI agenty',
    description: 'Online workshop s Pavolem Hejným a Jiřím Jahnem.',
    startsAt: '2026-08-20T19:00:00+02:00',
    endsAt: '2026-08-20T20:30:00+02:00',
    youtubeVideoId: null,
    recordingStartOffsetSeconds: 0,
    previewYoutubeVideoId: null,
    presentationUrl: null,
    repository: null,
    isPublished: true,
    allowedReactions: ['👍'],
    disabledPanels: [],
    createdAt: '2026-08-01T10:00:00+02:00',
    updatedAt: '2026-08-01T10:00:00+02:00',
};

const WORKSHOP_WITH_VIDEO: WorkshopDetails = {
    ...WORKSHOP,
    youtubeVideoId: 'dQw4w9WgXcQ',
    recordingStartOffsetSeconds: 75,
};

const OPEN_ENDED_WORKSHOP_WITH_VIDEO: WorkshopDetails = {
    ...WORKSHOP_WITH_VIDEO,
    endsAt: null,
};

/**
 * The teaser an administrator published for the recording of a workshop
 */
const PREVIEW_YOUTUBE_VIDEO_ID = 'M7lc1UVf-VE';

/**
 * The ended workshop as the server hands it to a member whose membership does not unlock its recording: the stream is
 * gone from the room itself and only what is offered to them comes with it.
 */
const WORKSHOP_WITHOUT_ITS_RECORDING: WorkshopDetails = {
    ...WORKSHOP_WITH_VIDEO,
    youtubeVideoId: null,
};

const FOLLOW_UP_CONTENT: WorkshopContentBlock = {
    id: 'follow-up-material',
    title: 'Materiály pro další krok',
    bodyMarkdown: '[Otevřít](https://example.com/materialy)',
    unlockAt: '2026-08-20T19:00:00+02:00',
    sortOrder: 0,
    isPublished: true,
    isFollowUp: true,
    isPaidMembersOnly: false,
    createdAt: '2026-08-20T18:00:00+02:00',
    updatedAt: '2026-08-20T18:00:00+02:00',
    linkClickCount: 0,
};

const STAGE_COMMENT: WorkshopCommentReference = {
    id: 'stage-question',
    authorName: 'Jana Nováková',
    body: 'Jak poznám, že agent opravdu běží v produkci?',
};

/**
 * The room as far as the stage is concerned: something which tells it about a reaction
 */
function createReactionSource() {
    const listeners = new Set<(reaction: FlyingWorkshopReaction) => void>();
    const subscribeToReactions: SubscribeToWorkshopReactions = (listener) => {
        listeners.add(listener);
        return () => {
            listeners.delete(listener);
        };
    };

    return {
        subscribeToReactions,
        listenerCount: () => listeners.size,
        sendReaction: (reaction: FlyingWorkshopReaction) =>
            act(() => {
                listeners.forEach((listener) => listener(reaction));
            }),
    };
}

function createRepositoryCommitSource() {
    const listeners = new Set<(commit: GithubCommit) => void>();
    const subscribeToRepositoryCommits: SubscribeToWorkshopRepositoryCommits = (listener) => {
        listeners.add(listener);
        return () => {
            listeners.delete(listener);
        };
    };

    return {
        subscribeToRepositoryCommits,
        sendCommit: (commit: GithubCommit) =>
            act(() => {
                listeners.forEach((listener) => listener(commit));
            }),
    };
}

afterEach(() => {
    cleanup();
    membershipRoomMock.membershipRoom = null;
});

describe('workshop stage', () => {
    it('sends a reaction of the room over the stage', async () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(reactionSource.listenerCount()).toBe(1);
        await reactionSource.sendReaction({ flightId: 'first', reactionText: '🎉' });
        await waitFor(() => expect(container.querySelectorAll('.workshop-reaction')).toHaveLength(1));
        expect(container.querySelector('.workshop-reaction')?.className).toContain('workshop-reaction-flight--launch');
    });

    it('shows a new repository commit on the stage for ten seconds', () => {
        vi.useFakeTimers();
        try {
            const reactionSource = createReactionSource();
            const repositoryCommitSource = createRepositoryCommitSource();
            const commit: GithubCommit = {
                sha: '6dcb09b5b57875f334f61aebed695e2e4193db5b',
                message: 'Přidat oznámení commitů',
                authorName: 'Pavol Hejný',
                committedAt: '2026-08-20T19:09:00.000Z',
            };

            render(
                <WorkshopStage
                    workshop={WORKSHOP}
                    serverTime="2026-08-20T19:10:00+02:00"
                    subscribeToReactions={reactionSource.subscribeToReactions}
                    repository={{ owner: 'hejny', name: 'promptbook', branch: 'main', deploymentUrl: null }}
                    subscribeToRepositoryCommits={repositoryCommitSource.subscribeToRepositoryCommits}
                />,
            );

            repositoryCommitSource.sendCommit(commit);
            expect(screen.getByRole('status').textContent).toContain('Nový commit na projektu');
            expect(screen.getByText(commit.message)).not.toBeNull();

            act(() => vi.advanceTimersByTime(9_999));
            expect(screen.getByText(commit.message)).not.toBeNull();

            act(() => vi.advanceTimersByTime(1));
            expect(screen.queryByText(commit.message)).toBeNull();
        } finally {
            vi.useRealTimers();
        }
    });

    it('shows the question the host selected over the stream', () => {
        const reactionSource = createReactionSource();
        render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                stageComment={STAGE_COMMENT}
            />,
        );

        expect(screen.getByRole('status').textContent).toContain('Otázka na stage');
        expect(screen.getByRole('status').textContent).toContain(STAGE_COMMENT.authorName);
        expect(screen.getByRole('status').textContent).toContain(STAGE_COMMENT.body);
    });

    it('stops listening once the room leaves the stage', () => {
        const reactionSource = createReactionSource();
        const { unmount } = render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        unmount();
        expect(reactionSource.listenerCount()).toBe(0);
    });

    it('offers the active video in fullscreen mode', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );
        const videoFrame = container.querySelector('iframe');
        const requestFullscreen = vi.fn().mockResolvedValue(undefined);

        expect(videoFrame).not.toBeNull();
        Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });

        expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
        expect(videoFrame?.allowFullscreen).toBe(true);

        fireEvent.click(screen.getByRole('button', { name: 'Přehrát video na celé obrazovce' }));

        expect(requestFullscreen).toHaveBeenCalledOnce();
    });

    it('takes the subtitles away from the video it plays', () => {
        const reactionSource = createReactionSource();
        const postMessage = vi.fn();
        const contentWindowSpy = vi
            .spyOn(HTMLIFrameElement.prototype, 'contentWindow', 'get')
            .mockReturnValue({ postMessage } as unknown as Window);

        render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(postMessage.mock.calls.map(([message]) => JSON.parse(message as string))).toEqual([
            { event: 'command', func: 'unloadModule', args: ['captions'] },
            { event: 'command', func: 'unloadModule', args: ['cc'] },
        ]);

        contentWindowSpy.mockRestore();
    });

    it('keeps the configured replay offset out of the live stream', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(container.querySelector('iframe')?.getAttribute('src')).not.toContain('start=75');
    });

    it('says nothing to a player of a workshop which has not started', () => {
        const reactionSource = createReactionSource();
        const postMessage = vi.fn();
        const contentWindowSpy = vi
            .spyOn(HTMLIFrameElement.prototype, 'contentWindow', 'get')
            .mockReturnValue({ postMessage } as unknown as Window);

        render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T18:50:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(postMessage).not.toHaveBeenCalled();

        contentWindowSpy.mockRestore();
    });

    it('keeps the video of a workshop without an end on the stage however long it runs', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={OPEN_ENDED_WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-21T09:00:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
            />,
        );

        expect(container.querySelector('iframe')).not.toBeNull();
        expect(screen.queryByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).toBeNull();
    });

    it('wraps a workshop without an end up as soon as the administration records its end', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={{ ...OPEN_ENDED_WORKSHOP_WITH_VIDEO, endsAt: '2026-08-20T21:12:00+02:00' }}
                serverTime="2026-08-20T21:13:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
    });

    it('replaces the video with the wrap-up while reactions keep their stage stream', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                feedback={null}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
        expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
            '#workshop-material-follow-up-material',
        );
        expect(reactionSource.listenerCount()).toBe(1);
    });

    it('keeps the wrap-up for a member who has not paid and offers them the withheld video instead of playing it', () => {
        const reactionSource = createReactionSource();
        membershipRoomMock.membershipRoom = { membership: FREE_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITHOUT_ITS_RECORDING}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                paidMembersOnlyVideo={{ previewYoutubeVideoId: null }}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
        expect(screen.queryByRole('button', { name: /Přehrát video znovu/ })).toBeNull();
        expect(screen.getByText('Záznam workshopu je pro placené členy')).not.toBeNull();
    });

    it('plays the published teaser of the withheld video and opens the membership which unlocks the whole of it', () => {
        const reactionSource = createReactionSource();
        const openMembershipModal = vi.fn();
        membershipRoomMock.membershipRoom = { membership: FREE_MEMBERSHIP, openMembershipModal };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITHOUT_ITS_RECORDING}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                paidMembersOnlyVideo={{ previewYoutubeVideoId: PREVIEW_YOUTUBE_VIDEO_ID }}
                onSaveFeedback={async () => true}
            />,
        );
        const previewFrame = container.querySelector('iframe');

        expect(previewFrame?.getAttribute('src')).toContain(
            `https://www.youtube-nocookie.com/embed/${PREVIEW_YOUTUBE_VIDEO_ID}`,
        );
        expect(previewFrame?.getAttribute('src')).toContain('autoplay=0');
        expect(screen.getByText('Ukázka ze záznamu')).not.toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Koupit placené členství/ }));

        expect(openMembershipModal).toHaveBeenCalledOnce();
    });

    it('keeps the wrap-up of a paid member and lets them play the video of the ended workshop again', () => {
        const reactionSource = createReactionSource();
        membershipRoomMock.membershipRoom = { membership: PAID_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Přehrát video znovu/ }));

        const rewatchFrame = container.querySelector('iframe');
        expect(rewatchFrame).not.toBeNull();
        expect(rewatchFrame?.getAttribute('src')).toContain('https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ');
        expect(rewatchFrame?.getAttribute('src')).toContain('controls=1');
        expect(rewatchFrame?.getAttribute('src')).toContain('start=75');
        expect(screen.queryByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Zpět na závěrečné shrnutí/ }));

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
    });
});

Get-Content: 
Line |
   2 |  … e.test.tsx; Get-Content -Raw businesses/online-workshop/participant/W …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUp.test.tsx' because it does not exist.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$package = Get-Content -Raw package.json | ConvertFrom-Json; $package.dependencies.psobject.Properties | Where-Object { $_.Name -match '"'pdf|html|canvas' } | ForEach-Object { \""'$($_.Name)=$($_.Value)" }; $package.devDependencies.psobject.Properties | Where-Object { $_.Name -match '"'pdf|html|canvas' } | ForEach-Object { \""'$($_.Name)=$($_.Value)" }; rg -n --glob '"'"'!node_modules'"' --glob '"'!'"\\.next' --glob '"'!prompts/**'"' --glob '"'!coverage/**'"' \"pdf-lib|jsPDF|@react-pdf|application/pdf|window\\.print|print\\(\" app businesses components lib tests package.json; Get-Content -Raw lib/workshops/workshopMaterialLinks.ts; Get-Content -Raw lib/workshops/workshopSpecialMaterials.ts; Get-Content -Raw businesses/online-workshop/participant/WorkshopPresentationMaterial.tsx" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 420ms:
lib\exports\svgChartExport.ts:268:    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';
const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
        }

        const destination = markdown.slice(destinationStart, destinationEnd);
        if (destination !== '') {
            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
        }
    }

    return ranges;
}

function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const openingAnchorPattern = /<a\b[^>]*>/gi;
    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;

    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
        const openingAnchor = openingAnchorMatch[0];
        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
        if (isInsideCode(markdown, openingAnchorIndex)) {
            continue;
        }

        const hrefMatch = hrefPattern.exec(openingAnchor);
        if (hrefMatch === null) {
            continue;
        }

        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
        if (!destination) {
            continue;
        }

        const hrefText = hrefMatch[0];
        const destinationIndexInHref = hrefText.lastIndexOf(destination);
        if (destinationIndexInHref === -1) {
            continue;
        }

        const hrefIndexInAnchor = hrefMatch.index;
        ranges.push({
            destination,
            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;

    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
        const destination = autolinkMatch[1];
        const matchIndex = autolinkMatch.index ?? 0;
        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
            ranges.push({
                destination,
                start: matchIndex,
                end: matchIndex + autolinkMatch[0].length,
                isTitleRequired: true,
            });
        }
    }

    return ranges;
}

/**
 * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
 * bare HTTP(S) URL too. Materialization must therefore match that rendered
 * anchor as well as explicit Markdown and HTML links.
 */
function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
        const leadingWhitespace = bareUrlMatch[1] ?? '';
        const rawDestination = bareUrlMatch[2];
        const matchIndex = bareUrlMatch.index ?? 0;
        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
        const start = matchIndex + leadingWhitespace.length;

        if (destination !== '' && !isInsideCode(markdown, start)) {
            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
        }
    }

    return ranges;
}

function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;

    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
        const destination = definitionMatch[1] ?? definitionMatch[2];
        if (!destination) {
            continue;
        }

        const matchIndex = definitionMatch.index ?? 0;
        if (isInsideCode(markdown, matchIndex)) {
            continue;
        }

        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
        ranges.push({
            destination,
            start: matchIndex + destinationIndexInDefinition,
            end: matchIndex + destinationIndexInDefinition + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    try {
        return await fetchPublicWebPageTitle(trackableUrl);
    } catch {
        // A remote page may reject our bounded metadata request, but that must
        // never prevent a room from handing out its already-safe short link.
        return getWorkshopShortcodeLinkFallbackTitle(destinationUrl);
    }
}

/**
 * Replaces a source address with its persisted short URL. An authored Markdown,
 * HTML, or reference label remains its author's wording; raw URLs and
 * autolinks receive the fetched page title in ordinary Markdown link syntax.
 */
export function replaceWorkshopShortcodeLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    let replacedMarkdown = bodyMarkdown;
    const ranges = collectWorkshopMaterialLinkRanges(bodyMarkdown);

    for (const range of [...ranges].reverse()) {
        const shortcodeLink = shortcodeLinkByDestination.get(range.destination);
        if (shortcodeLink !== undefined) {
            const replacement =
                typeof shortcodeLink === 'string'
                    ? shortcodeLink
                    : range.isTitleRequired
                      ? createMarkdownShortcodeLink(shortcodeLink.title, shortcodeLink.shortUrl)
                      : shortcodeLink.shortUrl;
            replacedMarkdown =
                replacedMarkdown.slice(0, range.start) + replacement + replacedMarkdown.slice(range.end);
        }
    }

    return replacedMarkdown;
}

export function replaceWorkshopMaterialLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    return replaceWorkshopShortcodeLinkDestinations(bodyMarkdown, shortcodeLinkByDestination);
}

export function getWorkshopShortcodeLinkSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return workshopKind === 'workshop' ? 'online-workshop' : 'community';
}

export function getWorkshopMaterialShortcodeSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return getWorkshopShortcodeLinkSourceApp(workshopKind);
}

function getShortcodeLinkId(value: number | string): number | null {
    const shortcodeLinkId = Number(value);

    return Number.isSafeInteger(shortcodeLinkId) && shortcodeLinkId > 0 ? shortcodeLinkId : null;
}

async function loadWorkshopShortcodeLinks(
    supabase: SupabaseClient,
    linkOwner: WorkshopShortcodeLinkOwner,
): Promise<LoadedWorkshopShortcodeLinks> {
    const { data: mappingData, error: mappingError } = await supabase
        .from(linkOwner.mappingTableName)
        .select('destination_url, destination_title, shortcode_link_id')
        .eq(linkOwner.mappingOwnerColumnName, linkOwner.id);
    if (mappingError) {
        return { shortcodeLinkByDestination: null, errorMessage: mappingError.message };
    }

    const mappings = (mappingData ?? []) as WorkshopShortcodeLinkMappingRow[];
    const shortcodeLinkIds = Array.from(
        new Set(
            mappings
                .map((mapping) => getShortcodeLinkId(mapping.shortcode_link_id))
                .filter((shortcodeLinkId): shortcodeLinkId is number => shortcodeLinkId !== null),
        ),
    );
    if (shortcodeLinkIds.length === 0) {
        return { shortcodeLinkByDestination: new Map(), errorMessage: null };
    }

    const { data: shortcodeLinkData, error: shortcodeLinkError } = await supabase
        .from(SHORTCODE_LINK_TABLE_NAME)
        .select('id, shortcode')
        .in('id', shortcodeLinkIds);
    if (shortcodeLinkError) {
        return { shortcodeLinkByDestination: null, errorMessage: shortcodeLinkError.message };
    }

    const shortcodeById = new Map<number, string>(
        ((shortcodeLinkData ?? []) as ShortcodeLinkReferenceRow[])
            .map((shortcodeLink) => {
                const shortcodeLinkId = getShortcodeLinkId(shortcodeLink.id);

                return shortcodeLinkId === null ? null : ([shortcodeLinkId, shortcodeLink.shortcode] as const);
            })
            .filter((shortcodeLink): shortcodeLink is readonly [number, string] => shortcodeLink !== null),
    );
    const shortcodeLinkByDestination = new Map<string, LoadedWorkshopShortcodeLink>();

    for (const mapping of mappings) {
        const shortcodeLinkId = getShortcodeLinkId(mapping.shortcode_link_id);
        const shortcode = shortcodeLinkId === null ? undefined : shortcodeById.get(shortcodeLinkId);
        if (shortcode !== undefined) {
            const storedTitle = getStoredWorkshopShortcodeLinkTitle(mapping.destination_title);
            shortcodeLinkByDestination.set(mapping.destination_url, {
                shortUrl: createPublicShortcodeLinkUrl(shortcode),
                title: storedTitle ?? getWorkshopShortcodeLinkFallbackTitle(mapping.destination_url),
                isTitleStored: storedTitle !== null,
            });
        }
    }

    return { shortcodeLinkByDestination, errorMessage: null };
}

async function persistWorkshopShortcodeLinkTitle(
    supabase: SupabaseClient,
    linkOwner: WorkshopShortcodeLinkOwner,
    destination: string,
    title: string,
): Promise<string | null> {
    const { error } = await supabase
        .from(linkOwner.mappingTableName)
        .update({ destination_title: title })
        .eq(linkOwner.mappingOwnerColumnName, linkOwner.id)
        .eq('destination_url', destination);

    return error?.message ?? null;
}

/**
 * Makes sure every trackable URL of one persisted workshop record has one ad
 * hoc short link, then returns a copy of its Markdown with public short URLs.
 * The original text remains in its source table, so a changed destination or a
 * deleted shortcode can be safely prepared again.
 */
async function materializeWorkshopShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly bodyMarkdown: string;
    },
    linkOwner: WorkshopShortcodeLinkOwner,
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    const destinations = getWorkshopShortcodeLinkDestinations(context.bodyMarkdown);
    if (destinations.length === 0) {
        return { bodyMarkdown: context.bodyMarkdown, errorMessage: null };
    }
    const destinationsRequiringTitle = new Set(getWorkshopShortcodeLinkDestinationsRequiringTitle(context.bodyMarkdown));

    const loadedShortcodeLinks = await loadWorkshopShortcodeLinks(supabase, linkOwner);
    if (loadedShortcodeLinks.shortcodeLinkByDestination === null) {
        return { bodyMarkdown: null, errorMessage: loadedShortcodeLinks.errorMessage };
    }

    const missingDestinations = destinations.filter(
        (destination) => !loadedShortcodeLinks.shortcodeLinkByDestination.has(destination),
    );
    for (const destination of missingDestinations) {
        const destinationTitle = destinationsRequiringTitle.has(destination)
            ? await resolveWorkshopShortcodeLinkTitle(destination)
            : null;
        const trackedDestination = createWorkshopShortcodeLinkTrackingUrl(
            destination,
            context.workshopSlug,
            linkOwner.id,
        );
        const createdShortcodeLink = await createAdHocShortcodeLink(supabase, {
            urls: [trackedDestination],
            note: linkOwner.note,
            sourceApp: getWorkshopShortcodeLinkSourceApp(context.workshopKind),
        });
        if (createdShortcodeLink.shortcodeLink === null) {
            return { bodyMarkdown: null, errorMessage: createdShortcodeLink.errorMessage };
        }

        // A room can be opened by many people at once. Upsert lets the source
        // record retain whichever equivalent shortcode reached the mapping
        // first, and a reload below makes every concurrent response use it.
        const { error: mappingError } = await supabase
            .from(linkOwner.mappingTableName)
            .upsert(
                {
                    [linkOwner.mappingOwnerColumnName]: linkOwner.id,
                    destination_url: destination,
                    shortcode_link_id: createdShortcodeLink.shortcodeLink.id,
                    ...(destinationTitle === null ? {} : { destination_title: destinationTitle }),
                },
                { onConflict: `${linkOwner.mappingOwnerColumnName},destination_url`, ignoreDuplicates: true },
            );
        if (mappingError) {
            return { bodyMarkdown: null, errorMessage: mappingError.message };
        }
    }

    const resolvedShortcodeLinks =
        missingDestinations.length === 0
            ? loadedShortcodeLinks
            : await loadWorkshopShortcodeLinks(supabase, linkOwner);
    const resolvedShortcodeLinkByDestination = resolvedShortcodeLinks.shortcodeLinkByDestination;
    if (resolvedShortcodeLinkByDestination === null) {
        return { bodyMarkdown: null, errorMessage: resolvedShortcodeLinks.errorMessage };
    }

    const destinationsWithoutStoredTitle = Array.from(destinationsRequiringTitle).filter(
        (destination) => !resolvedShortcodeLinkByDestination.get(destination)?.isTitleStored,
    );
    for (const destination of destinationsWithoutStoredTitle) {
        const titleErrorMessage = await persistWorkshopShortcodeLinkTitle(
            supabase,
            linkOwner,
            destination,
            await resolveWorkshopShortcodeLinkTitle(destination),
        );
        if (titleErrorMessage !== null) {
            return { bodyMarkdown: null, errorMessage: titleErrorMessage };
        }
    }

    let shortcodeLinkByDestination = resolvedShortcodeLinkByDestination;
    if (destinationsWithoutStoredTitle.length > 0) {
        const reloadedShortcodeLinks = await loadWorkshopShortcodeLinks(supabase, linkOwner);
        const reloadedShortcodeLinkByDestination = reloadedShortcodeLinks.shortcodeLinkByDestination;
        if (reloadedShortcodeLinkByDestination === null) {
            return { bodyMarkdown: null, errorMessage: reloadedShortcodeLinks.errorMessage };
        }
        shortcodeLinkByDestination = reloadedShortcodeLinkByDestination;
    }

    return {
        bodyMarkdown: replaceWorkshopShortcodeLinkDestinations(context.bodyMarkdown, shortcodeLinkByDestination),
        errorMessage: null,
    };
}

/**
 * Makes sure every trackable URL in one material has one ad hoc short link,
 * then returns a copy of its Markdown which contains the public short URLs.
 */
export async function materializeWorkshopMaterialShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly contentBlockId: string;
        readonly bodyMarkdown: string;
    },
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    return materializeWorkshopShortLinks(supabase, context, {
        id: context.contentBlockId,
        mappingTableName: WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
        mappingOwnerColumnName: 'content_block_id',
        note: `Ad hoc material link for ${context.workshopSlug}`,
    });
}

/**
 * Materializes the links of an eligible chat message through the same persisted
 * ad hoc-shortcode path as materials. A normal participant never calls this:
 * their links deliberately stay inert text in the room.
 */
export async function materializeWorkshopCommentShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly commentId: string;
        readonly bodyMarkdown: string;
    },
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    return materializeWorkshopShortLinks(supabase, context, {
        id: context.commentId,
        mappingTableName: WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
        mappingOwnerColumnName: 'comment_id',
        note: `Ad hoc chat link for ${context.workshopSlug}`,
    });
}

/**
 * Prepares a material as soon as an administrator saves it. Participant-facing
 * reads call the same materializer as a recovery path for links which existed
 * before this migration or whose first preparation was interrupted.
 */
export async function ensureWorkshopMaterialShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly contentBlockId: string;
        readonly bodyMarkdown: string;
    },
): Promise<string | null> {
    const { errorMessage } = await materializeWorkshopMaterialShortLinks(supabase, context);

    return errorMessage;
}

/**
 * Prepares a moderator or artificial message as soon as it is written. Public
 * state loading repeats this safely for old messages and interrupted writes.
 */
export async function ensureWorkshopCommentShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly commentId: string;
        readonly bodyMarkdown: string;
    },
): Promise<string | null> {
    const { errorMessage } = await materializeWorkshopCommentShortLinks(supabase, context);

    return errorMessage;
}

import type { ReactNode } from 'react';

/**
 * Where a card which shares the material list belongs among the ordinary materials
 *
 * Note: A card is placed by what it is rather than by a number written next to it, so the room which supplies one says
 *       what it means and this one rule decides where every member reading it sees it.
 */
export type WorkshopSpecialMaterialPlacement =
    /**
     * After the ordinary materials, which is where a card belongs that adds to what the room already handed over
     */
    | 'after-materials'

    /**
     * Before the ordinary materials while the member reading them does not pay for the community membership, and after
     * them once they do
     */
    | 'before-materials-until-paid';

/**
 * A participant-facing card which belongs in the material list without being an administrator-managed material.
 *
 * Note: A room can lead someone to a durable place such as the community without turning that link into content that
 *       is stored, timed, paid-only, or measured as an ordinary workshop material. Keeping these cards in the one
 *       material list still gives every participant the same predictable place to find them.
 */
export type WorkshopSpecialMaterial = {
    readonly id: string;
    readonly content: ReactNode;

    /**
     * Where this card belongs in the material list, which is after the ordinary materials unless it says otherwise
     */
    readonly placement?: WorkshopSpecialMaterialPlacement;
};

/**
 * What decides where a card belongs whose placement depends on the member reading it
 */
export type WorkshopSpecialMaterialMembership = {
    /**
     * Whether the room knows that the member reading it pays for the community membership
     */
    readonly isPaidMember: boolean;
};

/**
 * The special materials of a room divided around its ordinary materials, each group in the order it was supplied in
 */
export type WorkshopSpecialMaterialPlacementSelection = {
    readonly specialMaterialsBeforeContentBlocks: readonly WorkshopSpecialMaterial[];
    readonly specialMaterialsAfterContentBlocks: readonly WorkshopSpecialMaterial[];
};

/**
 * Divides the special materials of a room into the ones which open its material list and the ones which close it.
 *
 * Note: An invitation is the first thing a member who has not accepted it yet reads and one more way on for a member
 *       who is already inside, so one card is placed by what the member reading it has instead of two cards being
 *       written for the two of them.
 * Note: A member is only ever moved past an invitation once the room knows that they pay for the membership, so a
 *       membership which is still being loaded leaves the invitation where a member who does not pay reads it.
 */
export function selectWorkshopSpecialMaterialsByPlacement(
    specialMaterials: readonly WorkshopSpecialMaterial[],
    { isPaidMember }: WorkshopSpecialMaterialMembership,
): WorkshopSpecialMaterialPlacementSelection {
    const isPlacedBeforeContentBlocks = (specialMaterial: WorkshopSpecialMaterial): boolean =>
        specialMaterial.placement === 'before-materials-until-paid' && !isPaidMember;

    return {
        specialMaterialsBeforeContentBlocks: specialMaterials.filter(isPlacedBeforeContentBlocks),
        specialMaterialsAfterContentBlocks: specialMaterials.filter(
            (specialMaterial) => !isPlacedBeforeContentBlocks(specialMaterial),
        ),
    };
}

'use client';

import {
    WorkshopMaterialCard,
    type WorkshopMaterialCardContent,
} from '@/businesses/online-workshop/participant/WorkshopContent';

const WORKSHOP_PRESENTATION_MATERIAL_ID = 'presentation';
const WORKSHOP_PRESENTATION_TITLE = 'Prezentace';
const WORKSHOP_PRESENTATION_CALL_TO_ACTION_LABEL = 'Otevřít prezentaci';

type WorkshopPresentationMaterialProps = {
    /** A normalized public address supplied by the workshop setting. */
    readonly presentationUrl: string;
};

/**
 * Makes the one static link whose title stays stable across PDF, PowerPoint, and Markdown presentations.
 */
function createWorkshopPresentationMarkdown(presentationUrl: string): string {
    // An angle-bracket destination keeps an otherwise valid public URL with parentheses from ending the Markdown
    // link early. `normalizePublicWebPageUrl` already turns angle brackets themselves into escaped URL characters.
    return `[${WORKSHOP_PRESENTATION_TITLE}](<${presentationUrl}>)`;
}

/**
 * A workshop-level presentation in the shared material-card format.
 *
 * Note: It deliberately has no unlock date or membership flag. Those rules apply only to ordinary content blocks,
 *       while the presentation is available to every participant as soon as the room is open.
 */
export function WorkshopPresentationMaterial({ presentationUrl }: WorkshopPresentationMaterialProps) {
    const presentationMaterial: WorkshopMaterialCardContent = {
        id: WORKSHOP_PRESENTATION_MATERIAL_ID,
        title: WORKSHOP_PRESENTATION_TITLE,
        bodyMarkdown: createWorkshopPresentationMarkdown(presentationUrl),
        isFollowUp: false,
        isPaidMembersOnly: false,
    };

    return (
        <WorkshopMaterialCard
            contentBlock={presentationMaterial}
            callToActionLabel={WORKSHOP_PRESENTATION_CALL_TO_ACTION_LABEL}
            ariaLabel="Prezentace workshopu"
        />
    );
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content lib/exports/svgChartExport.ts | Select-Object -First 380; $package = Get-Content -Raw package.json | ConvertFrom-Json; $package.dependencies.psobject.Properties | ForEach-Object { "$($_.Name)=$($_.Value)" }; $package.devDependencies.psobject.Properties | ForEach-Object { "$($_.Name)=$($_.Value)" }' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 359ms:
/**
 * Turning a chart which is already drawn on the page into a file
 *
 * Note: The chart is an SVG element, so the picture is taken from the very element the reader is looking at instead of
 *       drawing it a second time. What the three pictures have in common is therefore one serialization, which the
 *       raster and the printable file both start from.
 */

const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

/**
 * A picture is exported with a background, because a chart drawn in dark ink on nothing at all is unreadable in a
 * viewer which shows transparency as black.
 */
const DEFAULT_EXPORT_BACKGROUND_COLOR = '#ffffff';

/**
 * The exported picture carries the typeface it is read in, because a file which left the page has no stylesheet to
 * inherit one from and would otherwise be written in whatever a viewer falls back to.
 */
const EXPORT_FONT_FAMILY = 'system-ui, -apple-system, "Segoe UI", sans-serif';

/**
 * How much sharper the raster picture is than the chart on the screen, so that it stays readable when it is enlarged
 */
const PNG_EXPORT_SCALE = 2;

const JPEG_EXPORT_QUALITY = 0.95;

/**
 * The printable page, in the points a PDF measures everything in, and the margin the picture keeps from its edges
 */
const PDF_PAGE_WIDTH_POINTS = 842;
const PDF_PAGE_HEIGHT_POINTS = 595;
const PDF_PAGE_MARGIN_POINTS = 24;

export type SvgChartExportOptions = {
    /**
     * The name the picture carries inside itself, which a reader of the file sees instead of a bare drawing
     */
    readonly title: string;

    readonly backgroundColor?: string;
};

type ChartPictureSize = {
    readonly width: number;
    readonly height: number;
};

function getSvgElementSize(svgElement: SVGSVGElement): ChartPictureSize {
    const { width, height } = svgElement.getBoundingClientRect();

    return {
        width: Math.max(1, Math.round(width || svgElement.clientWidth)),
        height: Math.max(1, Math.round(height || svgElement.clientHeight)),
    };
}

/**
 * Copy the drawn chart into a standalone document: named, sized in absolute numbers and standing on a background of
 * its own, so that it opens the same way in a browser, in a vector editor and in a document
 */
export function serializeChartAsSvg(svgElement: SVGSVGElement, options: SvgChartExportOptions): string {
    const { width, height } = getSvgElementSize(svgElement);
    const exportedElement = svgElement.cloneNode(true) as SVGSVGElement;

    exportedElement.setAttribute('width', String(width));
    exportedElement.setAttribute('height', String(height));
    exportedElement.setAttribute('viewBox', `0 0 ${width} ${height}`);
    exportedElement.setAttribute('font-family', EXPORT_FONT_FAMILY);

    // Note: On the page the chart fills whatever it is given, which an exported file has nothing to be measured
    //       against, so it is given the size it was drawn at.
    exportedElement.style.width = `${width}px`;
    exportedElement.style.height = `${height}px`;

    const titleElement = document.createElementNS(SVG_NAMESPACE, 'title');
    titleElement.textContent = options.title;

    const backgroundElement = document.createElementNS(SVG_NAMESPACE, 'rect');
    backgroundElement.setAttribute('x', '0');
    backgroundElement.setAttribute('y', '0');
    backgroundElement.setAttribute('width', String(width));
    backgroundElement.setAttribute('height', String(height));
    backgroundElement.setAttribute('fill', options.backgroundColor ?? DEFAULT_EXPORT_BACKGROUND_COLOR);

    exportedElement.insertBefore(backgroundElement, exportedElement.firstChild);
    exportedElement.insertBefore(titleElement, exportedElement.firstChild);

    // Note: The namespace is named only when the writer did not name it by itself, because an `xmlns` written twice is
    //       not a document a strict reader opens at all.
    const serializedChart = new XMLSerializer().serializeToString(exportedElement);
    const namespacedChart = serializedChart.includes('xmlns=')
        ? serializedChart
        : serializedChart.replace('<svg', `<svg xmlns="${SVG_NAMESPACE}"`);

    return `<?xml version="1.0" encoding="UTF-8"?>\n${namespacedChart}`;
}

/**
 * Draw the serialized chart onto a canvas, which is what both raster pictures are taken from
 *
 * Note: The document is handed over as a data address rather than as a temporary file, because a canvas which loaded a
 *       picture from anywhere else refuses to give its content back.
 */
async function drawChartOnCanvas(
    svgElement: SVGSVGElement,
    options: SvgChartExportOptions,
): Promise<HTMLCanvasElement> {
    const { width, height } = getSvgElementSize(svgElement);
    const serializedChart = serializeChartAsSvg(svgElement, options);
    const chartImage = new Image();
    chartImage.width = width;
    chartImage.height = height;

    await new Promise<void>((resolve, reject) => {
        chartImage.addEventListener('load', () => resolve());
        chartImage.addEventListener('error', () => reject(new Error('Graf se nepodařilo vykreslit do obrázku')));
        chartImage.src = `data:image/svg+xml;charset=utf-8,${encodeURIComponent(serializedChart)}`;
    });

    const canvasElement = document.createElement('canvas');
    canvasElement.width = width * PNG_EXPORT_SCALE;
    canvasElement.height = height * PNG_EXPORT_SCALE;

    const canvasContext = canvasElement.getContext('2d');
    if (canvasContext === null) {
        throw new Error('Graf se nepodařilo vykreslit do obrázku');
    }

    canvasContext.fillStyle = options.backgroundColor ?? DEFAULT_EXPORT_BACKGROUND_COLOR;
    canvasContext.fillRect(0, 0, canvasElement.width, canvasElement.height);
    canvasContext.drawImage(chartImage, 0, 0, canvasElement.width, canvasElement.height);

    return canvasElement;
}

async function readCanvasAsBlob(canvasElement: HTMLCanvasElement, mimeType: string, quality?: number): Promise<Blob> {
    return new Promise<Blob>((resolve, reject) => {
        canvasElement.toBlob(
            (blob) => (blob === null ? reject(new Error('Obrázek grafu se nepodařilo uložit')) : resolve(blob)),
            mimeType,
            quality,
        );
    });
}

export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
    return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
}

/**
 * Where one part of the written document begins, counted in bytes from its very beginning
 */
function getByteLength(chunks: readonly Uint8Array[]): number {
    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
}

function encodeAsciiText(text: string): Uint8Array {
    return new TextEncoder().encode(text);
}

function formatCrossReferenceEntry(objectOffset: number): string {
    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
}

/**
 * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
 *
 * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
 *       altogether, and a bracket in a name would end the name in the middle of itself.
 */
function formatPdfTextString(text: string): string {
    const characterCodes = ['FEFF'];

    for (const character of text) {
        const codePoint = character.codePointAt(0) ?? 0;

        if (codePoint > 0xffff) {
            const surrogateOffset = codePoint - 0x10000;
            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
        } else {
            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
        }
    }

    return `<${characterCodes.join('').toUpperCase()}>`;
}

/**
 * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
 */
function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
    readonly width: number;
    readonly height: number;
    readonly offsetX: number;
    readonly offsetY: number;
} {
    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
    const width = pictureSize.width * scale;
    const height = pictureSize.height * scale;

    return {
        width,
        height,
        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
    };
}

/**
 * Write the smallest possible document which shows one picture on one page
 *
 * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
 *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
 */
export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
    const placement = getPdfPicturePlacement(pictureSize);
    const contentStream =
        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;

    const objectBodies: readonly string[] = [
        '<< /Type /Catalog /Pages 2 0 R >>',
        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
    ];

    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
    const objectOffsets: number[] = [];

    objectBodies.forEach((objectBody, objectIndex) => {
        objectOffsets.push(getByteLength(chunks));
        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
    });

    objectOffsets.push(getByteLength(chunks));
    chunks.push(
        encodeAsciiText(
            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
                'stream\n',
        ),
        pictureBytes,
        encodeAsciiText('\nendstream\nendobj\n'),
    );

    objectOffsets.push(getByteLength(chunks));
    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));

    const crossReferenceOffset = getByteLength(chunks);
    chunks.push(
        encodeAsciiText(
            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
                objectOffsets.map(formatCrossReferenceEntry).join('') +
                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
        ),
    );

    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
}

export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
    const canvasElement = await drawChartOnCanvas(svgElement, options);
    const pictureBlob = await readCanvasAsBlob(canvasElement, 'image/jpeg', JPEG_EXPORT_QUALITY);
    const pictureBytes = new Uint8Array(await pictureBlob.arrayBuffer());

    return createSinglePicturePdf(
        pictureBytes,
        { width: canvasElement.width, height: canvasElement.height },
        options.title,
    );
}
@hookform/resolvers=^5.0.1
@radix-ui/react-accordion=^1.2.3
@radix-ui/react-alert-dialog=^1.1.6
@radix-ui/react-aspect-ratio=^1.1.2
@radix-ui/react-avatar=^1.1.3
@radix-ui/react-checkbox=^1.1.4
@radix-ui/react-collapsible=^1.1.3
@radix-ui/react-context-menu=^2.2.6
@radix-ui/react-dialog=^1.1.6
@radix-ui/react-dropdown-menu=^2.1.6
@radix-ui/react-hover-card=^1.1.6
@radix-ui/react-label=^2.1.2
@radix-ui/react-menubar=^1.1.6
@radix-ui/react-navigation-menu=^1.2.5
@radix-ui/react-popover=^1.1.6
@radix-ui/react-progress=^1.1.2
@radix-ui/react-radio-group=^1.2.3
@radix-ui/react-scroll-area=^1.2.3
@radix-ui/react-select=^2.1.6
@radix-ui/react-separator=^1.1.2
@radix-ui/react-slider=^1.2.3
@radix-ui/react-slot=^1.1.2
@radix-ui/react-switch=^1.1.3
@radix-ui/react-tabs=^1.1.3
@radix-ui/react-toast=^1.2.6
@radix-ui/react-toggle=^1.1.2
@radix-ui/react-toggle-group=^1.1.2
@radix-ui/react-tooltip=^1.1.8
@supabase/ssr=^0.7.0
@supabase/supabase-js=^2.57.2
class-variance-authority=^0.7.1
clsx=^2.1.1
cmdk=^1.1.1
date-fns=^3.6.0
dotenv=^16.4.7
embla-carousel-react=^8.5.2
framer-motion=^12.6.3
input-otp=^1.4.2
js-yaml=^4.1.0
leaflet=^1.9.4
logrocket=^12.1.1
lucide-react=^0.487.0
moment=^2.30.1
next=15.2.6
next-themes=^0.4.6
pg=^8.16.3
react=^18.3.1
react-day-picker=^8.10.1
react-dom=^18.3.1
react-hook-form=^7.55.0
react-qr-code=^2.0.18
react-resizable-panels=^2.1.7
react-use-measure=^2.1.7
recharts=^2.15.1
sharp=^0.34.4
sonner=^2.0.3
spacetrim=^0.11.60
stripe=^22.6.0
tailwind-merge=^3.1.0
tw-animate-css=^1.2.5
vaul=^1.1.2
zod=^3.24.2
@babel/eslint-parser=^7.27.1
@eslint/eslintrc=^3
@playwright/test=^1.62.1
@testing-library/jest-dom=^6.6.3
@testing-library/react=^16.3.0
@total-typescript/ts-reset=^0.6.1
@types/js-yaml=^4.0.9
@types/lodash=^4.17.20
@types/node=^20
@types/pg=^8.15.5
@types/react=^19
@types/react-dom=^19
autoprefixer=^10.4.21
eslint=^9
eslint-config-next=15.2.4
jsdom=^26.0.0
postcss=^8.5.3
ptbk=0.114.0-38
raw-loader=^4.0.2
tailwindcss=^3.4.17
tailwindcss-animate=^1.0.7
tsx=^4.19.4
typescript=^5
vitest=^3.1.1

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"contentBlocks:|nextContentUnlockAt|paidMembersOnlyContentPreviews|loadWorkshopPublic|WorkshopPublicState|map.*Content|isPaidMembersOnly\" lib/workshops/workshopDatabase.ts app/api/workshops -g '*.ts' -g '*.tsx'; Get-Content lib/workshops/workshopDatabase.ts | Select-Object -Skip 2140 -First 270; Get-Content -Raw lib/workshops/workshopDate.ts; Get-Content -Raw lib/events/event.ts; Get-Content -Raw businesses/online-workshop/onlineWorkshopTerms.ts" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 384ms:
lib/workshops/workshopDatabase.ts:72:    WorkshopPublicState,
lib/workshops/workshopDatabase.ts:510:export function mapWorkshopContentRow(row: WorkshopContentRow, linkClickCount = 0): WorkshopContentBlock {
lib/workshops/workshopDatabase.ts:519:        isPaidMembersOnly: row.is_paid_members_only,
lib/workshops/workshopDatabase.ts:1175:type LoadedWorkshopPublicState = {
lib/workshops/workshopDatabase.ts:1176:    readonly state: WorkshopPublicState | null;
lib/workshops/workshopDatabase.ts:2027:): Promise<{ readonly contentBlocks: readonly WorkshopContentBlock[] | null; readonly errorMessage: string | null }> {
lib/workshops/workshopDatabase.ts:2039:        return { contentBlocks: null, errorMessage: firstError.message };
lib/workshops/workshopDatabase.ts:2052:        contentBlocks: ((contentResult.data ?? []) as WorkshopContentRow[]).map((contentBlock) =>
lib/workshops/workshopDatabase.ts:2053:            mapWorkshopContentRow(contentBlock, linkClickCountByContentBlockId.get(contentBlock.id) ?? 0),
lib/workshops/workshopDatabase.ts:2208:export async function loadWorkshopPublicState(
lib/workshops/workshopDatabase.ts:2213:): Promise<LoadedWorkshopPublicState> {
lib/workshops/workshopDatabase.ts:2366:        .map(mapWorkshopContentRow);
lib/workshops/workshopDatabase.ts:2369:    const { readableContentBlocks, paidMembersOnlyContentPreviews } = selectWorkshopContentForMember(rawContentBlocks, {
lib/workshops/workshopDatabase.ts:2398:    const materializedContentBlocks = materializedContentResults.map((materializedContentResult) => ({
lib/workshops/workshopDatabase.ts:2475:            contentBlocks: materializedContentBlocks,
lib/workshops/workshopDatabase.ts:2476:            nextContentUnlockAt: (nextUnlockResult.data?.unlock_at as string | undefined) ?? null,
lib/workshops/workshopDatabase.ts:2477:            paidMembersOnlyContentPreviews,
lib/workshops/workshopDatabase.ts:2656:            contentBlocks: ((contentResult.data ?? []) as WorkshopContentRow[]).map((contentBlock) =>
lib/workshops/workshopDatabase.ts:2657:                mapWorkshopContentRow(contentBlock, linkClickCountByContentBlockId.get(contentBlock.id) ?? 0),
app/api/workshops\[workshopSlug]\state\route.ts:1:import { loadWorkshopPublicState } from '@/lib/workshops/workshopDatabase';
app/api/workshops\[workshopSlug]\state\route.ts:19:    const { state, errorMessage } = await loadWorkshopPublicState(
app/api/workshops\[workshopSlug]\connect\route.ts:7:    loadWorkshopPublicState,
app/api/workshops\[workshopSlug]\connect\route.ts:59:    const { state, errorMessage } = await loadWorkshopPublicState(
 *
 * Note: Only a message the whole room sees can hold the top of the chat, so a pin which lost its approval is left out.
 */
function withPinnedWorkshopCommentRow(
    commentRows: readonly WorkshopCommentRow[],
    pinnedCommentRow: WorkshopCommentRow | null,
): readonly WorkshopCommentRow[] {
    if (
        pinnedCommentRow === null ||
        pinnedCommentRow.status !== 'approved' ||
        commentRows.some(({ id }) => id === pinnedCommentRow.id)
    ) {
        return commentRows;
    }

    return [...commentRows, pinnedCommentRow];
}

/**
 * Pins one message on top of a chat or releases the top of that chat again
 *
 * Note: The workshop remembers a single pin, so pinning a message releases the previously pinned one in the same write.
 * Note: Releasing the top only clears this very message, so it never drops a pin which another tab set meanwhile.
 */
export async function updatePinnedWorkshopComment(
    supabase: SupabaseClient,
    workshopId: string,
    commentId: string,
    isPinned: boolean,
): Promise<{ readonly errorMessage: string | null }> {
    const pinnedCommentQuery = supabase
        .from(WORKSHOP_TABLE_NAME)
        .update({ pinned_comment_id: isPinned ? commentId : null })
        .eq('id', workshopId);

    const { error } = await (isPinned ? pinnedCommentQuery : pinnedCommentQuery.eq('pinned_comment_id', commentId));
    if (error) {
        console.error('Failed to change the pinned comment of a workshop:', error.message);
        return { errorMessage: 'Pinned comment could not be changed' };
    }

    return { errorMessage: null };
}

/**
 * Chooses the one comment the host is answering over the stage, or leaves the stage without a question.
 *
 * Note: The database checks that a selected comment belongs to this room. The stage keeps no copied text: the selected
 *       comment remains the one source of the question in both chat and live presentation.
 */
export async function updateWorkshopStageComment(
    supabase: SupabaseClient,
    workshopId: string,
    stageCommentId: string | null,
): Promise<{ readonly errorMessage: string | null }> {
    const { error } = await supabase
        .from(WORKSHOP_TABLE_NAME)
        .update({ stage_comment_id: stageCommentId })
        .eq('id', workshopId);
    if (error) {
        console.error('Failed to change the comment on a workshop stage:', error.message);
        return { errorMessage: 'Stage comment could not be changed' };
    }

    return { errorMessage: null };
}

export async function loadWorkshopPublicState(
    supabase: SupabaseClient,
    workshopRow: WorkshopRow,
    participant: WorkshopParticipant,
    commentSort: WorkshopCommentSort,
): Promise<LoadedWorkshopPublicState> {
    const contentVisibilityCutoff = new Date().toISOString();
    const workshop = mapWorkshopRow(workshopRow);
    const isWorkshopPast = workshopRow.room_kind === 'workshop' && getWorkshopPhase(workshop) === 'past';

    // Note: The reactions which flew over the stage recently are replayed for somebody entering the room. A room
    //       without that panel therefore does not load them, exactly as it does not count them.
    const isReactionsPanelOffered = isWorkshopPanelOffered(
        workshopRow.room_kind,
        workshopRow.disabled_panels,
        'reactions',
    );
    const roomCapabilities = getWorkshopKindCapabilities(workshopRow.room_kind);
    const isStageOffered = roomCapabilities.isStageOffered;

    // Note: The membership decides which materials a member sees, so the room asks about it before it assembles what
    //       is visible. A room which does not offer the membership has nothing to unlock, and an answer which does
    //       not arrive keeps the paid materials hidden rather than letting them escape.
    let isPaidMember = false;
    if (roomCapabilities.isMembershipOffered) {
        const { membership, errorMessage: membershipErrorMessage } = await loadCommunityMembershipByEmail(
            supabase,
            participant.email,
        );
        if (membershipErrorMessage !== null) {
            console.error('Failed to load the community membership of a workshop participant:', membershipErrorMessage);
        }
        isPaidMember = membership !== null && isPaidCommunityMembershipStatus(membership.status);
    }

    // Note: A moderator is shown every message which waits for a decision, because making that decision is exactly
    //       what they are in the room for. Everybody else only ever sees the messages they wrote themselves.
    const isModerationOffered = isWorkshopParticipantModerating(participant);
    const pendingCommentsQuery = supabase
        .from(WORKSHOP_COMMENT_TABLE_NAME)
        .select(WORKSHOP_COMMENT_COLUMNS)
        .eq('workshop_id', workshopRow.id)
        .eq('status', 'pending')
        .order('created_at', { ascending: false })
        .limit(MAXIMAL_VISIBLE_PENDING_COMMENT_COUNT);
    const commentsQuery = supabase
        .from(WORKSHOP_COMMENT_TABLE_NAME)
        .select(WORKSHOP_COMMENT_COLUMNS)
        .eq('workshop_id', workshopRow.id)
        .eq('status', 'approved')
        .order('created_at', { ascending: false })
        .limit(MAXIMAL_VISIBLE_COMMENT_COUNT);
    // Note: A member who does not pay for the membership is never told that a material of the paid ones unlocks next,
    //       because that countdown would promise them something a purchase alone cannot open.
    const nextContentUnlockBaseQuery = supabase
        .from(WORKSHOP_CONTENT_TABLE_NAME)
        .select('unlock_at')
        .eq('workshop_id', workshopRow.id)
        .eq('is_published', true)
        .gt('unlock_at', contentVisibilityCutoff);
    const nextContentUnlockQuery = isPaidMember
        ? nextContentUnlockBaseQuery
        : nextContentUnlockBaseQuery.eq('is_paid_members_only', false);
    // Note: The materials are read as a paid member would read them, whoever is reading the room. Which of them reach
    //       this member and which are only named to them is then decided once, so the room cannot hide a material it
    //       says nothing about or name one it also hands over.
    const visibleContentQuery = supabase
        .from(WORKSHOP_CONTENT_TABLE_NAME)
        .select(WORKSHOP_CONTENT_COLUMNS)
        .eq('workshop_id', workshopRow.id)
        .eq('is_published', true)
        .lte('unlock_at', contentVisibilityCutoff)
        .order('sort_order', { ascending: true })
        .order('unlock_at', { ascending: true });
    const futureFollowUpContentQuery = supabase
        .from(WORKSHOP_CONTENT_TABLE_NAME)
        .select(WORKSHOP_CONTENT_COLUMNS)
        .eq('workshop_id', workshopRow.id)
        .eq('is_published', true)
        .eq('is_follow_up', true)
        .gt('unlock_at', contentVisibilityCutoff);

    const [
        contentResult,
        nextUnlockResult,
        futureFollowUpContentResult,
        feedbackResult,
        commentsResult,
        pendingCommentsResult,
        reactionsResult,
        reactionCountsResult,
        watchingParticipantCount,
        pinnedCommentRow,
        stageCommentResult,
        pollsResult,
    ] = await Promise.all([
        visibleContentQuery,
        (isWorkshopPast ? nextContentUnlockQuery.eq('is_follow_up', false) : nextContentUnlockQuery)
            .order('unlock_at', { ascending: true })
            .limit(1)
            .maybeSingle(),
        // A selected follow-up remains a normal material before the end, including its scheduled unlock. Once the
        // wrap-up starts it must be available for the stage link even if its ordinary unlock time is later; unrelated
        // scheduled materials keep their own timing.
        isWorkshopPast ? futureFollowUpContentQuery.maybeSingle() : Promise.resolve({ data: null, error: null }),
        workshopRow.room_kind === 'workshop'
            ? supabase
                  .from(WORKSHOP_FEEDBACK_TABLE_NAME)
                  .select(WORKSHOP_FEEDBACK_COLUMNS)
                  .eq('workshop_id', workshopRow.id)
                  .eq('participant_id', participant.id)
                  .maybeSingle()
            : Promise.resolve({ data: null, error: null }),
        commentsQuery,
        isModerationOffered ? pendingCommentsQuery : pendingCommentsQuery.eq('participant_id', participant.id),
        isReactionsPanelOffered
            ? supabase
                  .from(WORKSHOP_REACTION_TABLE_NAME)
                  .select('id, emoji, created_at')
                  .eq('workshop_id', workshopRow.id)
                  .order('created_at', { ascending: false })
                  .limit(MAXIMAL_RECENT_REACTION_COUNT)
            : Promise.resolve({ data: [], error: null }),
        loadWorkshopReactionCounts(supabase, workshopRow),
        countWatchingWorkshopParticipants(supabase, workshopRow),
        loadPinnedWorkshopCommentRowOrNull(supabase, workshopRow),
        isStageOffered && workshopRow.stage_comment_id !== null
            ? loadWorkshopCommentReference(supabase, workshopRow.id, workshopRow.stage_comment_id)
            : Promise.resolve({ comment: null, errorMessage: null }),
        loadWorkshopPolls(supabase, workshopRow, participant.email),
    ]);

    const stateQueryError =
        contentResult.error ??
        nextUnlockResult.error ??
        futureFollowUpContentResult.error ??
        feedbackResult.error ??
        commentsResult.error ??
        pendingCommentsResult.error ??
        reactionsResult.error;
    const errorMessage =
        stateQueryError?.message ??
        reactionCountsResult.errorMessage ??
        stageCommentResult.errorMessage ??
        pollsResult.errorMessage;
    if (errorMessage !== null) {
        return { state: null, errorMessage };
    }

    const rawContentBlocks = [
        ...((contentResult.data ?? []) as WorkshopContentRow[]),
        ...(futureFollowUpContentResult.data === null ? [] : [futureFollowUpContentResult.data as WorkshopContentRow]),
    ]
        .sort(
            (firstContentBlock, secondContentBlock) =>
                firstContentBlock.sort_order - secondContentBlock.sort_order ||
                Date.parse(firstContentBlock.unlock_at) - Date.parse(secondContentBlock.unlock_at),
        )
        .map(mapWorkshopContentRow);
    // Only what this member may read is prepared for them; a withheld material never has its links materialized,
    // because nothing of it but its title is going to leave the server.
    const { readableContentBlocks, paidMembersOnlyContentPreviews } = selectWorkshopContentForMember(rawContentBlocks, {
        isPaidMember,
        isMembershipOffered: roomCapabilities.isMembershipOffered,
    });
    // The recording of an ended workshop is decided here for the very same reason: a member who has not unlocked it
    // receives the teaser published for it instead of the stream, rather than the room merely not playing what it
    // still handed over.
    const { readableVideo, paidMembersOnlyVideo } = selectWorkshopVideoForMember(workshop, {
        isWorkshopPast,
        isPaidMember,
        isMembershipOffered: roomCapabilities.isMembershipOffered,
    });
    const materializedContentResults = await Promise.all(
        readableContentBlocks.map(async (contentBlock) => ({
            contentBlock,
            ...(await materializeWorkshopMaterialShortLinks(supabase, {
                workshopSlug: workshopRow.slug,
                workshopKind: workshopRow.room_kind,
                contentBlockId: contentBlock.id,
                bodyMarkdown: contentBlock.bodyMarkdown,
            })),
        })),
    );
    const materializationErrorMessage = materializedContentResults.find(
        (materializedContentResult) => materializedContentResult.errorMessage !== null,
    )?.errorMessage;
    if (materializationErrorMessage !== null && materializationErrorMessage !== undefined) {
        return { state: null, errorMessage: materializationErrorMessage };
    }
    const materializedContentBlocks = materializedContentResults.map((materializedContentResult) => ({
        ...materializedContentResult.contentBlock,
        bodyMarkdown: materializedContentResult.bodyMarkdown ?? materializedContentResult.contentBlock.bodyMarkdown,
    }));

    const commentRows = withPinnedWorkshopCommentRow(
        [
            ...((commentsResult.data ?? []) as WorkshopCommentRow[]),
            ...((pendingCommentsResult.data ?? []) as WorkshopCommentRow[]),
        ],
        pinnedCommentRow,
    );
    const commentIds = commentRows.map(({ id }) => id);
import { DEFAULT_WORKSHOP_DURATION_MINUTES } from '@/lib/workshops/workshopConstants';
import { getWorkshopExpectedEndsAtMilliseconds } from '@/lib/workshops/workshopPhase';

const CZECH_LOCALE = 'cs-CZ';
const PRAGUE_TIME_ZONE = 'Europe/Prague';
const MILLISECONDS_PER_MINUTE = 60 * 1000;

const CZECH_WORKSHOP_DATE_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    weekday: 'long',
    day: 'numeric',
    month: 'numeric',
    year: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

const CZECH_WORKSHOP_TIME_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    hour: '2-digit',
    minute: '2-digit',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * Formats an occurrence date in the timezone in which Czech workshops take place, independently of the server's
 * timezone.
 */
export function formatCzechWorkshopDate(startsAt: string): string {
    return CZECH_WORKSHOP_DATE_FORMAT.format(new Date(startsAt));
}

/**
 * The exact moment one occurrence begins at, for example `čtvrtek 20. 8. 2026 19:00`
 *
 * Note: This is how a term is named where it has to be told apart from another term held on the very same day, which
 *       is what identifies a term a registration was written for before the terms had addresses of their own.
 */
export function formatCzechWorkshopMoment(startsAt: string): string {
    return `${formatCzechWorkshopDate(startsAt)} ${formatCzechWorkshopTime(startsAt)}`;
}

const CZECH_WORKSHOP_SHORT_DATE_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    dateStyle: 'short',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * Names an occurrence date as shortly as a label beside a title can, in the same Prague time as its full date.
 */
export function formatCzechWorkshopShortDate(startsAt: string): string {
    return CZECH_WORKSHOP_SHORT_DATE_FORMAT.format(new Date(startsAt));
}

const CZECH_WORKSHOP_DAY_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    day: 'numeric',
    month: 'numeric',
    year: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The day one occurrence falls on, for example `4. 9. 2026`
 *
 * Note: This is how a term a visitor has to put in their calendar is named, so it says its year in full rather than
 *       shortening it the way a compact label beside a title does.
 */
export function formatCzechWorkshopDay(startsAt: string): string {
    return CZECH_WORKSHOP_DAY_FORMAT.format(new Date(startsAt));
}

const CZECH_WORKSHOP_DAY_AND_MONTH_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    day: 'numeric',
    month: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The day one occurrence falls on without its year, for example `4. 9.`
 *
 * Note: This is how a term is named where several terms are listed beside each other and the year they share would
 *       only be repeated.
 */
export function formatCzechWorkshopDayAndMonth(startsAt: string): string {
    return CZECH_WORKSHOP_DAY_AND_MONTH_FORMAT.format(new Date(startsAt));
}

const PRAGUE_CALENDAR_DATE_FORMAT = new Intl.DateTimeFormat('en-CA', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The calendar day one occurrence falls on in Prague, written as `2026-09-04`
 *
 * Note: This is the machine-readable day of a term rather than a label for a visitor, which is what identifies a term
 *       by the day it is held on.
 */
export function formatPragueCalendarDate(startsAt: string): string {
    return PRAGUE_CALENDAR_DATE_FORMAT.format(new Date(startsAt));
}

/**
 * The time span one occurrence runs for in Prague time, for example `10:00–16:00`
 */
export function formatCzechWorkshopTimeRange(startsAt: string, endsAt: string | null): string {
    const startTime = formatCzechWorkshopTime(startsAt);
    return endsAt === null ? startTime : `${startTime}–${formatCzechWorkshopTime(endsAt)}`;
}

/**
 * Formats the start time of a Czech workshop in Prague time.
 */
export function formatCzechWorkshopTime(startsAt: string): string {
    return CZECH_WORKSHOP_TIME_FORMAT.format(new Date(startsAt));
}

/**
 * Describes how long a workshop is expected to take. A workshop whose end is still left open is announced with the
 * same usual length as its calendar invitation, because a visitor is told how long to set aside before anybody can
 * know when the workshop will really be ended.
 */
export function formatCzechWorkshopDuration(startsAt: string, endsAt: string | null): string {
    const startsAtMilliseconds = Date.parse(startsAt);
    const durationMinutes = Number.isFinite(startsAtMilliseconds)
        ? Math.round(
              (getWorkshopExpectedEndsAtMilliseconds({ startsAt, endsAt }) - startsAtMilliseconds) /
                  MILLISECONDS_PER_MINUTE,
          )
        : DEFAULT_WORKSHOP_DURATION_MINUTES;

    return `${durationMinutes} minut`;
}

import { isEventLocationKind, type EventLocationKind } from '@/lib/events/eventLocation';
import { isEventType, type EventType } from '@/lib/events/eventTypes';

/**
 * What one term of a public event is, beside the moment it happens at
 *
 * Note: Only a room which happens at a time is an event. A permanent room such as the community has none of this,
 *       which is why every occurrence carries it and every other room carries `null` instead.
 */
export type EventDetails = {
    /**
     * The kind of event this term is, which decides the landing page it is listed on
     */
    readonly type: EventType;
    readonly locationKind: EventLocationKind;

    /**
     * Where a term is held, which only a term held somewhere has
     */
    readonly locationLabel: string;

    /**
     * The price of one seat, where zero is a free event
     */
    readonly priceCzk: number;

    /**
     * How many people fit into this term, or `null` when nobody has to be turned away
     */
    readonly maximumParticipantCount: number | null;
};

/**
 * What a term of an event is before anybody describes it: a free online term of the workshop everybody starts with
 */
export const DEFAULT_EVENT_DETAILS: EventDetails = {
    type: 'online-workshop',
    locationKind: 'online',
    locationLabel: '',
    priceCzk: 0,
    maximumParticipantCount: null,
};

/**
 * The stored event fields of one room, as far as they can be trusted
 *
 * Note: A room which is not an event has no event fields at all, and a stored kind of event or place which the
 *       application does not know is deliberately read as no event rather than as a made-up one, so nothing lists a
 *       term it could not describe.
 */
export function createEventDetailsOrNull(values: {
    readonly type: string | null;
    readonly locationKind: string | null;
    readonly locationLabel: string;
    readonly priceCzk: number | null;
    readonly maximumParticipantCount: number | null;
}): EventDetails | null {
    const { type, locationKind, locationLabel, priceCzk, maximumParticipantCount } = values;

    if (type === null || locationKind === null || priceCzk === null) {
        return null;
    }

    if (!isEventType(type) || !isEventLocationKind(locationKind)) {
        console.error(`Unknown event type "${type}" or event location "${locationKind}" was read from the database.`);
        return null;
    }

    return { type, locationKind, locationLabel, priceCzk, maximumParticipantCount };
}

import type { WorkshopConnectionDetails } from '@/businesses/online-workshop/participant/WorkshopConnectionForm';
import {
    formatCzechWorkshopDate,
    formatCzechWorkshopDuration,
    formatCzechWorkshopTime,
} from '@/lib/workshops/workshopDate';
import type { WorkshopOccurrenceTiming } from '@/lib/workshops/workshopPhase';
import type { WorkshopSummary } from '@/lib/workshops/workshopTypes';

/**
 * The one line every offered term of the online workshop adds about itself: how long it takes
 *
 * Note: A visitor choosing a term on the landing page and a participant choosing one in the room read the very same
 *       sentence about it, so a term never promises two different lengths.
 */
export function createOnlineWorkshopTermNoteText(workshop: WorkshopOccurrenceTiming): string {
    return `${formatCzechWorkshopDuration(workshop.startsAt, workshop.endsAt)} + Q&A`;
}

/**
 * What the door of one term of the online workshop says about that very term
 *
 * Note: Every term is described by the words its own administration wrote, so the waiting room says what the term a
 *       participant picked is about rather than what the event as a whole is about.
 */
export function createOnlineWorkshopConnectionDetails(workshop: WorkshopSummary): WorkshopConnectionDetails {
    return {
        title: workshop.title,
        description: workshop.description,
        dateLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTime(workshop.startsAt)}`,
        durationLabel: formatCzechWorkshopDuration(workshop.startsAt, workshop.endsAt),
    };
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files lib/exports components businesses app | rg \"(Export|export|Download|download).*test|.*\\.test\\.tsx"'$" | Select-Object -First 200; rg -n --glob '"'"'!node_modules'"' --glob '"'!'"\\.next' --glob '"'!prompts/**'"' \"renderChartAsPdfBlob|createSinglePicturePdf|URL\\.createObjectURL|createElement\\('a'\\)|\\.download\\s*=|Sta(h|)nout|Stáhnout\" app businesses components lib tests | Select-Object -First 400; rg -n \"export type WorkshopSummary|type WorkshopSummary\" lib/workshops/workshopTypes.ts; Get-Content lib/workshops/workshopTypes.ts | Select-Object -First 115; rg -n \"WorkshopContent\" businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom.test.tsx" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 420ms:
components\workshops\WorkshopReactionStream.test.tsx
components\workshops\WorkshopLinksPanel.test.tsx
components\workshop-comment-markdown.test.tsx
components\url-shortener.test.tsx
components\shortener\ShortcodeLinkAdmin.test.tsx
components\footer.test.tsx
components\admin\DiscountCodeForm.test.tsx
lib/exports\svgChartExport.test.ts
app\api\contacts\export\[formatId]\route.test.ts
businesses\workshop-admin\WorkshopSettingsForm.test.tsx
businesses\workshop-admin\WorkshopSelectorCardList.test.tsx
businesses\workshop-admin\WorkshopRegistrationContacts.test.tsx
businesses\workshop-admin\WorkshopPollAdmin.test.tsx
businesses\workshop-admin\WorkshopParticipantTimeline.test.tsx
businesses\workshop-admin\WorkshopOverviewChartRangeControls.test.tsx
businesses\workshop-admin\WorkshopOverviewChart.test.tsx
businesses\workshop-admin\WorkshopEventLinks.test.tsx
businesses\workshop-admin\WorkshopCommentModeration.test.tsx
businesses\workshop-admin\WorkshopArtificialComment.test.tsx
businesses\workshop-admin\CreateWorkshopForm.test.tsx
businesses\ai-supervize-mini\AiSupervizeMiniRegistrationForm.test.tsx
businesses\community\CommunityParticipantPage.test.tsx
businesses\online-workshop\OnlineWorkshopRegistrationForm.test.tsx
businesses\online-workshop\participant\OnlineWorkshopSelectedTermRoom.test.tsx
businesses\online-workshop\participant\OnlineWorkshopParticipantPage.test.tsx
businesses\online-workshop\participant\WorkshopChat.test.tsx
businesses\online-workshop\participant\WorkshopCalendarInvitation.test.tsx
businesses\online-workshop\participant\useWorkshopParticipant.test.tsx
businesses\online-workshop\participant\WorkshopPolls.test.tsx
businesses\online-workshop\participant\WorkshopParticipantBadge.test.tsx
businesses\online-workshop\participant\WorkshopFeedback.test.tsx
businesses\online-workshop\participant\WorkshopContent.test.tsx
businesses\online-workshop\participant\WorkshopReactions.test.tsx
businesses\online-workshop\participant\WorkshopWatchingBadge.test.tsx
businesses\online-workshop\participant\WorkshopStage.test.tsx
businesses\community\membership\CommunityMembershipModal.test.tsx
businesses\community\membership\CommunityMembershipLivePreview.test.tsx
businesses\community\membership\CommunityMembershipBadge.test.tsx
businesses\community\membership\CommunityMembershipActivitySection.test.tsx
businesses\community\membership\CommunityMembershipPage.test.tsx
businesses\community\membership\CommunityMembershipRegistrationForm.test.tsx
businesses\community\membership\CommunityMembershipWebinarArchive.test.tsx
businesses\ai-ta-krajta\AiTaKrajtaEpisodeCard.test.tsx
businesses\ai-ta-krajta\AiTaKrajtaPeopleSection.test.tsx
businesses\ai-ta-krajta\useAiTaKrajtaOrderedPeople.test.tsx
components\url-shortener.tsx:235:        const downloadLink = document.createElement('a');
components\url-shortener.tsx:237:        downloadLink.download = `qrcode-${createdLink.shortcode}.png`;
lib\workshops\workshopSchemas.test.ts:317:                bodyMarkdown: '[Stáhnout materiály](https://example.com)',
businesses\workshop-admin\WorkshopOverviewExportButtons.tsx:6:import { renderChartAsPdfBlob, renderChartAsPngBlob, serializeChartAsSvg } from '@/lib/exports/svgChartExport';
businesses\workshop-admin\WorkshopOverviewExportButtons.tsx:74:                    : await renderChartAsPdfBlob(chartElement, exportOptions);
components\calendar\AddToCalendarButtons.tsx:99:                    Stáhnout .ics
businesses\online-workshop\participant\WorkshopContent.test.tsx:232:            bodyMarkdown: '[Stáhnout podklady](https://ptbk.io/material-def456)',
businesses\online-workshop\participant\WorkshopCalendarInvitation.test.tsx:72:        const icalendarLink = screen.getByRole('link', { name: /Stáhnout \.ics/ });
lib\exports\svgChartExport.ts:221:export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
lib\exports\svgChartExport.ts:271:export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
lib\exports\svgChartExport.ts:276:    return createSinglePicturePdf(
lib\exports\svgChartExport.test.ts:2:import { createSinglePicturePdf } from './svgChartExport';
lib\exports\svgChartExport.test.ts:15:describe('createSinglePicturePdf', () => {
lib\exports\svgChartExport.test.ts:18:            createSinglePicturePdf(PICTURE_BYTES, { width: 800, height: 400 }, 'Přehled workshopu'),
lib\exports\svgChartExport.test.ts:29:            createSinglePicturePdf(PICTURE_BYTES, { width: 800, height: 400 }, 'Přehled'),
lib\exports\svgChartExport.test.ts:44:            createSinglePicturePdf(PICTURE_BYTES, { width: 100, height: 100 }, 'Př (2026)'),
lib\exports\svgChartExport.test.ts:54:            createSinglePicturePdf(PICTURE_BYTES, { width: 1000, height: 500 }, 'Graf'),
lib\downloadBlobFile.ts:10:    const fileUrl = URL.createObjectURL(blob);
lib\downloadBlobFile.ts:11:    const linkElement = document.createElement('a');
lib\downloadBlobFile.ts:13:    linkElement.download = fileName;
businesses\ai-ta-krajta\AiTaKrajtaBrandingPage.tsx:98:                                        Stáhnout soubory
businesses\ai-ta-krajta\AiTaKrajtaBrandingPage.tsx:157:                                                Stáhnout
35:export type WorkshopSummary = {
import type { AdminContactJoin } from '@/lib/admin/adminContactJoin';
import type { CommunityMembershipStatus } from '@/lib/community-membership/communityMembershipTypes';
import type { EventDetails } from '@/lib/events/event';
import type { GithubCommit } from '@/lib/github/githubCommitFeed';
import type { WorkshopPanelKey } from '@/lib/workshops/workshopPanels';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';

/**
 * A live room is normally one workshop occurrence. The community uses the same resilient room infrastructure, but
 * remains a single, separately administered room across all occurrences.
 */
export const WORKSHOP_KIND_VALUES = ['workshop', 'community', 'project'] as const;

export type WorkshopKind = (typeof WORKSHOP_KIND_VALUES)[number];

export function isWorkshopKind(value: string): value is WorkshopKind {
    return WORKSHOP_KIND_VALUES.includes(value as WorkshopKind);
}

/**
 * The lifecycle of something a participant submits for moderation. Chat messages and community projects deliberately
 * use these same states, so trust and moderator privileges never approve one kind of submission differently from the
 * other.
 */
export const WORKSHOP_SUBMISSION_STATUS_VALUES = ['pending', 'approved', 'rejected'] as const;

export type WorkshopSubmissionStatus = (typeof WORKSHOP_SUBMISSION_STATUS_VALUES)[number];

/**
 * A chat message keeps this alias because it is the public type used by the existing room API.
 */
export type WorkshopCommentStatus = WorkshopSubmissionStatus;
export type WorkshopCommentSort = 'recent' | 'upvotes';

export type WorkshopSummary = {
    readonly id: string;
    readonly kind: WorkshopKind;
    readonly slug: string;
    readonly title: string;

    /**
     * What this very term is about, in the words an administrator wrote about it
     *
     * Note: Terms of one event each have a subject of their own, so a list of them says what every single one is
     *       about rather than describing them all by the event they belong to. The room and the list read the very
     *       same words, so a term is never described in two different ways.
     */
    readonly description: string;
    readonly startsAt: string;
    readonly endsAt: string | null;
    readonly isPublished: boolean;

    /**
     * The event this room is a term of, or `null` for a permanent room which is no event at all
     *
     * Note: Every kind of event is administered in the very same place and stored in the very same table, so a page
     *       listing terms only ever asks which kind of event it wants to list.
     */
    readonly event: EventDetails | null;
};

/**
 * One occurrence as the administration lists it, together with the audience it gathered
 *
 * Note: The participant count deliberately stays out of `WorkshopSummary`, so no public list of terms exposes how
 *       many people registered for them.
 */
export type WorkshopAdminSummary = WorkshopSummary & {
    readonly participantCount: number;

    /**
     * How many people registered for this term on the landing page of its event, or `null` for a room which is no term
     * of an event and therefore has no registration form at all
     *
     * Note: This is a different audience from `participantCount`, which counts the people who really entered the room.
     *       Somebody can register and never come, and somebody can be handed a link into a room they never registered
     *       for, so neither number can be derived from the other.
     */
    readonly registeredParticipantCount: number | null;
};

export type WorkshopDetails = WorkshopSummary & {
    /** Number of artificial viewers added to the live watching badge by an administrator. */
    readonly artificialWatchingParticipantCount?: number;
    readonly youtubeVideoId: string | null;

    /**
     * Where paid members begin an ended workshop's recording, in seconds from the beginning of its stream.
     *
     * Note: This has no effect on the countdown or the live stream. It only removes the waiting-room portion from the
     *       replay which the membership unlocks after the workshop ends.
     */
    readonly recordingStartOffsetSeconds: number;

    /**
     * The teaser of that stream which stands in for it once the workshop is over, or `null` while none was published
     *
     * Note: The recording of an ended workshop is unlocked by the paid membership, and this is the snippet of it which
     *       an administrator lets everybody else watch instead. It is deliberately a second video rather than a part
     *       of the first one, so nothing of the recording itself has to reach a member who has not unlocked it.
     */
    readonly previewYoutubeVideoId: string | null;

    /**
     * A public PDF, presentation file, or Markdown page which belongs to this workshop, or `null` while none was
     * published. It is a workshop setting rather than an ordinary content block, so every participant can open it
     * without waiting for a scheduled unlock or buying a membership.
     */
    readonly presentationUrl: string | null;

    /**
     * The project this workshop is about, or `null` while no project is connected to it
     *
     * Note: This is the whole connection — the repository, its branch selection, and the address the project runs at
     *       — so the administration sets, changes, and unsets it as one thing. What has been committed in it is
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx:8:    WorkshopContentPreview,
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx:151:const PAID_MEMBERS_ONLY_CONTENT_PREVIEWS: readonly WorkshopContentPreview[] = [
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx:229:    paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[] = [],

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw lib/downloadBlobFile.ts; Get-Content -Raw lib/exports/svgChartExport.test.ts; Get-Content -Raw businesses/workshop-admin/WorkshopOverviewExportButtons.tsx; Get-Content businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx | Select-Object -First 520; Get-Content businesses/online-workshop/participant/WorkshopContent.test.tsx | Select-Object -First 360' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 406ms:
type DownloadBlobFileOptions = {
    readonly fileName: string;
    readonly blob: Blob;
};

/**
 * Offer a file generated in the browser to the user as a download
 */
export function downloadBlobFile({ fileName, blob }: DownloadBlobFileOptions): void {
    const fileUrl = URL.createObjectURL(blob);
    const linkElement = document.createElement('a');
    linkElement.href = fileUrl;
    linkElement.download = fileName;

    // Note: Firefox only follows the click when the link is a part of the document
    document.body.appendChild(linkElement);
    linkElement.click();
    document.body.removeChild(linkElement);

    URL.revokeObjectURL(fileUrl);
}

import { describe, expect, it } from 'vitest';
import { createSinglePicturePdf } from './svgChartExport';

/**
 * Bytes which a reader takes for a compressed picture, standing in for one the browser really made
 */
const PICTURE_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);

async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
        .map((byte) => String.fromCharCode(byte))
        .join('');
}

describe('createSinglePicturePdf', () => {
    it('writes a document a reader can open', async () => {
        const pdfText = await readPdfAsLatin1Text(
            createSinglePicturePdf(PICTURE_BYTES, { width: 800, height: 400 }, 'Přehled workshopu'),
        );

        expect(pdfText.startsWith('%PDF-1.4')).toBe(true);
        expect(pdfText.endsWith('%%EOF\n')).toBe(true);
        expect(pdfText).toContain('/Filter /DCTDecode');
        expect(pdfText).toContain(`/Length ${PICTURE_BYTES.length}`);
    });

    it('points at every object it lists, so that a reader never looks for one in the wrong place', async () => {
        const pdfText = await readPdfAsLatin1Text(
            createSinglePicturePdf(PICTURE_BYTES, { width: 800, height: 400 }, 'Přehled'),
        );

        const crossReferenceOffset = Number(/startxref\n(\d+)\n/.exec(pdfText)?.[1]);
        expect(pdfText.startsWith('xref', crossReferenceOffset)).toBe(true);

        const objectOffsets = Array.from(pdfText.matchAll(/^(\d{10}) 00000 n $/gm)).map((match) => Number(match[1]));
        expect(objectOffsets).toHaveLength(6);
        objectOffsets.forEach((objectOffset, objectIndex) => {
            expect(pdfText.startsWith(`${objectIndex + 1} 0 obj`, objectOffset)).toBe(true);
        });
    });

    it('writes a name with diacritics, or with a bracket in it, as the numbers of its letters', async () => {
        const pdfText = await readPdfAsLatin1Text(
            createSinglePicturePdf(PICTURE_BYTES, { width: 100, height: 100 }, 'Př (2026)'),
        );

        // Note: The mark of UTF-16, then `P`, `ř`, a space and `(2026)`, none of which can end the name early
        expect(pdfText).toContain('/Title <FEFF005001590020002800320030003200360029>');
        expect(pdfText).not.toContain('/Title (');
    });

    it('fits the picture onto the page without stretching it out of shape', async () => {
        const pdfText = await readPdfAsLatin1Text(
            createSinglePicturePdf(PICTURE_BYTES, { width: 1000, height: 500 }, 'Graf'),
        );

        const [, pictureWidth, pictureHeight] = /\n(\d+\.\d+) 0 0 (\d+\.\d+) /.exec(pdfText) ?? [];
        expect(Number(pictureWidth) / Number(pictureHeight)).toBeCloseTo(2, 5);
        expect(Number(pictureWidth)).toBeLessThanOrEqual(842 - 2 * 24);
        expect(Number(pictureHeight)).toBeLessThanOrEqual(595 - 2 * 24);
    });
});

'use client';

import { Button } from '@/components/ui/button';
import { downloadBlobFile } from '@/lib/downloadBlobFile';
import { downloadTextFile } from '@/lib/downloadTextFile';
import { renderChartAsPdfBlob, renderChartAsPngBlob, serializeChartAsSvg } from '@/lib/exports/svgChartExport';
import type { WorkshopOverviewSeriesDescriptor } from '@/lib/workshops/workshopOverviewSeries';
import { serializeWorkshopOverviewSeriesAsCsv } from '@/lib/workshops/workshopOverviewCsv';
import type { WorkshopOverviewSeriesPoint } from '@/lib/workshops/workshopOverviewSeriesPoints';
import { Download } from 'lucide-react';
import { useState } from 'react';

/**
 * The files one graph can be taken away as, in the order of how often an administrator asks for them
 */
const WORKSHOP_OVERVIEW_EXPORT_FORMATS = ['CSV', 'SVG', 'PNG', 'PDF'] as const;

type WorkshopOverviewExportFormat = (typeof WORKSHOP_OVERVIEW_EXPORT_FORMATS)[number];

type WorkshopOverviewExportButtonsProps = {
    readonly workshopSlug: string;
    readonly points: readonly WorkshopOverviewSeriesPoint[];
    readonly descriptors: readonly WorkshopOverviewSeriesDescriptor[];
    readonly getChartElement: () => SVGSVGElement | null;
};

/**
 * Take the graph away as a picture or as the numbers it is drawn from
 *
 * Note: Every file says exactly what is on the screen, the zoom, the switched on lines and the written metrics
 *       included, so a shared link and a downloaded file always show the same thing.
 */
export function WorkshopOverviewExportButtons({
    workshopSlug,
    points,
    descriptors,
    getChartElement,
}: WorkshopOverviewExportButtonsProps) {
    const [errorMessage, setErrorMessage] = useState<string | null>(null);

    const exportGraph = async (exportFormat: WorkshopOverviewExportFormat) => {
        const fileName = `${workshopSlug}-graf.${exportFormat.toLowerCase()}`;
        const exportOptions = { title: `Přehled workshopu ${workshopSlug}` };

        try {
            setErrorMessage(null);

            if (exportFormat === 'CSV') {
                downloadTextFile({
                    fileName,
                    mimeType: 'text/csv;charset=utf-8',
                    content: serializeWorkshopOverviewSeriesAsCsv(points, descriptors),
                });
                return;
            }

            const chartElement = getChartElement();
            if (chartElement === null) {
                throw new Error('Graf ještě není vykreslený');
            }

            if (exportFormat === 'SVG') {
                downloadTextFile({
                    fileName,
                    mimeType: 'image/svg+xml;charset=utf-8',
                    content: serializeChartAsSvg(chartElement, exportOptions),
                });
                return;
            }

            const blob =
                exportFormat === 'PNG'
                    ? await renderChartAsPngBlob(chartElement, exportOptions)
                    : await renderChartAsPdfBlob(chartElement, exportOptions);

            downloadBlobFile({ fileName, blob });
        } catch (error) {
            setErrorMessage((error as Error).message);
        }
    };

    return (
        <div className="flex flex-col items-end gap-1">
            <div className="flex flex-wrap items-center gap-1.5">
                {WORKSHOP_OVERVIEW_EXPORT_FORMATS.map((exportFormat) => (
                    <Button
                        key={exportFormat}
                        type="button"
                        variant="outline"
                        size="sm"
                        onClick={() => void exportGraph(exportFormat)}
                    >
                        <Download className="mr-1.5 h-4 w-4" /> {exportFormat}
                    </Button>
                ))}
            </div>
            {errorMessage !== null && <p className="text-xs text-rose-600">{errorMessage}</p>}
        </div>
    );
}

/**
 * @vitest-environment jsdom
 */

import type { CommunityMembershipRoomState } from '@/lib/community-membership/communityMembershipTypes';
import { DEFAULT_EVENT_DETAILS } from '@/lib/events/event';
import type {
    WorkshopContentPreview,
    WorkshopDetails,
    WorkshopPaidMembersVideo,
    WorkshopPoll,
    WorkshopPublicState,
} from '@/lib/workshops/workshopTypes';
import { cleanup, fireEvent, render, screen, within } from '@testing-library/react';
import type { InputHTMLAttributes, ReactNode } from 'react';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';

/**
 * The room as far as this page is concerned: a loaded state and the actions it offers
 */
const participantMocks = vi.hoisted(() => ({ controller: null as unknown }));

vi.mock('@/businesses/online-workshop/participant/useWorkshopParticipant', () => ({
    useWorkshopParticipant: () => participantMocks.controller,
}));

/**
 * What the room was told about the project of the workshop, which is read from GitHub rather than from the room
 */
const repositoryProgressMocks = vi.hoisted(() => ({
    controller: {
        progress: null as unknown,
        isProgressRead: true,
        newCommitShas: new Set<string>(),
    },
}));

vi.mock('@/businesses/online-workshop/participant/useWorkshopRepositoryProgress', () => ({
    useWorkshopRepositoryProgress: () => repositoryProgressMocks.controller,
}));

vi.mock('@/components/promptbook-qr-code', () => ({
    PromptbookQrCode: ({ value }: { readonly value: string }) => (
        <span data-testid="workshop-material-qr-code" data-value={value} />
    ),
}));

const fetchCommunityMembership = vi.fn<(workshopSlug: string) => Promise<CommunityMembershipRoomState>>();

vi.mock('@/businesses/community/membership/communityMembershipRoomApi', () => ({
    fetchCommunityMembership: (workshopSlug: string) => fetchCommunityMembership(workshopSlug),
    confirmCommunityMembershipCheckout: vi.fn(),
    openCommunityMembershipSubscriptionPortal: vi.fn(),
    scheduleCommunityMembershipCancellation: vi.fn(),
    reactivateCommunityMembership: vi.fn(),
    startCommunityMembershipPurchase: vi.fn(),
}));

// The checkbox of the design system measures itself, which the test document cannot do.
vi.mock('@/components/ui/checkbox', () => ({
    Checkbox: ({
        checked,
        onCheckedChange,
        ...props
    }: InputHTMLAttributes<HTMLInputElement> & {
        checked?: boolean;
        onCheckedChange?: (isChecked: boolean) => void;
    }) => (
        <input
            {...props}
            type="checkbox"
            checked={checked}
            onChange={(event) => onCheckedChange?.(event.currentTarget.checked)}
        />
    ),
}));

import {
    OnlineWorkshopParticipantPage,
    type WorkshopNavigationDetails,
} from '@/businesses/online-workshop/participant/OnlineWorkshopParticipantPage';

const WORKSHOP: WorkshopDetails = {
    id: '5a7eb2ad-2583-4e98-9640-50bc773b5fde',
    kind: 'workshop',
    event: DEFAULT_EVENT_DETAILS,
    slug: 'produkcni-kod-2026-08-21',
    title: 'Produkční kód s AI agenty',
    description: 'Online workshop s Pavolem Hejným a Jiřím Jahnem.',
    startsAt: '2026-08-21T19:00:00+02:00',
    endsAt: '2026-08-21T20:30:00+02:00',
    youtubeVideoId: 'dQw4w9WgXcQ',
    recordingStartOffsetSeconds: 0,
    previewYoutubeVideoId: null,
    presentationUrl: null,
    repository: null,
    isPublished: true,
    allowedReactions: ['👍'],
    disabledPanels: [],
    createdAt: '2026-08-01T10:00:00+02:00',
    updatedAt: '2026-08-01T10:00:00+02:00',
};

/**
 * Note: The community deliberately keeps a stored stream and a stored start, so these tests prove that the kind of
 *       the room takes the stage and the countdown away rather than an empty setting doing it by accident.
 */
const COMMUNITY: WorkshopDetails = {
    ...WORKSHOP,
    id: '0d6b0f1c-9b0a-4b7e-9c02-6f2f7a3f5f31',
    kind: 'community',
    slug: 'komunita',
    title: 'Komunita Promptbooku',
};

/**
 * A project discussion is the one room which is nobody's membership surface, because it is opened from the community
 * which already showed it.
 */
const PROJECT_DISCUSSION: WorkshopDetails = {
    ...WORKSHOP,
    id: '9b4c1f7e-24a8-4de6-9a1f-0f3f9c5a71d2',
    kind: 'project',
    slug: 'projekt-diskuze',
    title: 'Projekt komunity',
};

const FREE_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'none',
    monthlyPriceCzk: null,
    currentPeriodEndsAt: null,
    isCancellationScheduled: false,
    isPurchaseOffered: true,
    isSubscriptionManagementOffered: false,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const PAID_MEMBERSHIP: CommunityMembershipRoomState = {
    ...FREE_MEMBERSHIP,
    status: 'active',
    monthlyPriceCzk: 199,
    currentPeriodEndsAt: '2026-09-30T10:00:00.000Z',
    isPurchaseOffered: false,
    isSubscriptionManagementOffered: true,
};

/**
 * The paid materials of a room as far as a member who has not paid is told about them
 */
const PAID_MEMBERS_ONLY_CONTENT_PREVIEWS: readonly WorkshopContentPreview[] = [
    { id: 'paid-material-1', title: 'Bonusové podklady' },
];

/**
 * An occurrence which is already over by the moment the room is rendered at, so its stage is the closing wrap-up
 *
 * Note: Its stream is deliberately absent, exactly as the server hands the room to a member whose membership does not
 *       unlock the recording of it.
 */
const ENDED_WORKSHOP_WITHOUT_ITS_RECORDING: WorkshopDetails = {
    ...WORKSHOP,
    endsAt: '2026-08-21T19:10:00+02:00',
    youtubeVideoId: null,
};

/**
 * A term which is about a project, together with the branch it follows and where that project runs
 */
const WORKSHOP_ABOUT_A_PROJECT: WorkshopDetails = {
    ...WORKSHOP,
    repository: {
        owner: 'hejny',
        name: 'promptbook',
        branch: 'main',
        deploymentUrl: 'https://workshop.example/app',
    },
};

const WORKSHOP_WITH_PRESENTATION: WorkshopDetails = {
    ...WORKSHOP,
    presentationUrl: 'https://files.example.com/(production-ai-workshop).pptx',
};

const ATTACHED_COMMUNITY_POLL: WorkshopPoll = {
    id: 'poll-id',
    question: 'Co si z workshopu odnášíte?',
    isClosed: false,
    isVisible: true,
    createdAt: '2026-08-21T19:00:00+02:00',
    updatedAt: '2026-08-21T19:00:00+02:00',
    options: [
        { id: 'option-1', label: 'Praktické tipy', sortOrder: 0, voteCount: 7, isVotedByParticipant: false },
        { id: 'option-2', label: 'Nové nápady', sortOrder: 1, voteCount: 4, isVotedByParticipant: false },
    ],
    attachedWorkshops: [],
};

/**
 * The workshops a permanent room such as the community leads to
 */
const WORKSHOP_NAVIGATION: WorkshopNavigationDetails = {
    workshops: [
        {
            id: WORKSHOP.id,
            kind: WORKSHOP.kind,
            event: WORKSHOP.event,
            slug: WORKSHOP.slug,
            title: WORKSHOP.title,
            description: WORKSHOP.description,
            startsAt: WORKSHOP.startsAt,
            endsAt: WORKSHOP.endsAt,
            isPublished: WORKSHOP.isPublished,
        },
    ],
    title: 'Workshopy Promptbooku',
    description: 'Vyberte si workshop.',
    emptyMessage: 'Žádný workshop není dostupný.',
    locale: 'cs-CZ',
    timeZone: 'Europe/Prague',
};

function renderParticipantRoom(
    workshop: WorkshopDetails,
    workshopNavigation?: WorkshopNavigationDetails,
    isUsingCachedState = false,
    participantHeaderSupplement?: ReactNode,
    polls: readonly WorkshopPoll[] = [],
    paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[] = [],
    paidMembersOnlyVideo: WorkshopPaidMembersVideo | null = null,
) {
    const state: WorkshopPublicState = {
        serverTime: '2026-08-21T19:30:00+02:00',
        workshop,
        participant: {
            id: 'participant-id',
            fullname: 'Jana Nováková',
            email: 'jana@example.com',
            connectedAt: '2026-08-21T19:20:00+02:00',
            isInteractionBanned: false,
            isTrusted: true,
            isModerator: false,
        },
        watchingParticipantCount: 3,
        contentBlocks: [],
        nextContentUnlockAt: null,
        paidMembersOnlyContentPreviews,
        paidMembersOnlyVideo,
        feedback: null,
        comments: [],
        stageComment: null,
        recentReactions: [],
        reactionCounts: [],
        polls,
    };

    participantMocks.controller = {
        state,
        commentSort: 'recent',
        isCheckingConnection: false,
        isConnectionRequired: false,
        isRefreshing: false,
        isUsingCachedState,
        errorMessage: isUsingCachedState
            ? 'Spojení s workshopem je dočasně nedostupné. Zobrazuje se naposledy uložená verze.'
            : null,
        subscribeToReactions: () => () => undefined,
        newlyUnlockedContentBlockIds: new Set<string>(),
        connect: async () => true,
        changeFullname: async () => true,
        refresh: async () => true,
        changeCommentSort: () => undefined,
        submitComment: async () => true,
        upvoteComment: async () => undefined,
        voteOnPoll: async () => true,
        moderateComment: async () => true,
        moderateAuthor: async () => true,
        react: async () => undefined,
        saveFeedback: async () => true,
    };

    // Note: A member returning to a permanent room opens it without any parameters at all, so the room is rendered
    //       without them here and every identity it shows can only have come from the loaded room itself.
    return render(
        <OnlineWorkshopParticipantPage
            workshopSlug={workshop.slug}
            connectionDetails={{
                title: workshop.title,
                description: workshop.description,
                dateLabel: 'Kdykoli online',
                durationLabel: 'Stálý přístup',
            }}
            calendarDetails={null}
            initialEmail=""
            initialFullname=""
            participantHeaderSupplement={participantHeaderSupplement}
            workshopNavigation={workshopNavigation}
        />,
    );
}

/**
 * The cards of the material list of the room, in the order the member reading it comes to them
 */
function getMaterialCards(): readonly Element[] {
    const materialsSection = screen.getByRole('heading', { name: 'Materiály z workshopu' }).closest('section');

    return Array.from(materialsSection?.querySelectorAll('article') ?? []);
}

/**
 * Where among those cards the way into the permanent community is offered
 */
function getCommunityMaterialIndex(materialCards: readonly Element[]): number {
    const communityLink = screen.getByRole('link', { name: /Vstoupit do komunity/ });

    return materialCards.findIndex((materialCard) => materialCard.contains(communityLink));
}

beforeEach(() => {
    window.history.replaceState({}, '', '/cs/online-workshop/participant');
    fetchCommunityMembership.mockResolvedValue(FREE_MEMBERSHIP);
    repositoryProgressMocks.controller = { progress: null, isProgressRead: true, newCommitShas: new Set<string>() };
});

afterEach(() => {
    cleanup();
    vi.clearAllMocks();
});

describe('online workshop participant room', () => {
    it('gathers a workshop occurrence around its stage, its reactions, and its watching count', () => {
        const { container } = renderParticipantRoom(WORKSHOP);

        expect(container.querySelector('iframe')).not.toBeNull();
        expect(screen.queryByRole('button', { name: /Reagovat/ })).not.toBeNull();
        expect(screen.queryByText('Sledují 3 lidé')).not.toBeNull();
        expect(screen.getByRole('status', { name: 'Připojeno k serveru' })).not.toBeNull();
    });

    it('leaves a permanent room without the stage, the reactions, and the watching count of a live occurrence', () => {
        const { container } = renderParticipantRoom(COMMUNITY);

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.queryByRole('button', { name: /Reagovat/ })).toBeNull();
        expect(screen.queryByText('Sledují 3 lidé')).toBeNull();
    });

    it('shows the project a workshop is about as a special material, together with what has been committed in it', async () => {
        repositoryProgressMocks.controller = {
            progress: {
                // Keep the fields from the former response shape here to ensure the panel only presents commits.
                details: {
                    description: 'A/B testing landing page for Promptbook',
                    defaultBranch: 'main',
                    primaryLanguage: 'TypeScript',
                    starCount: 1,
                },
                commits: [
                    {
                        sha: '6dcb09b5b57875f334f61aebed695e2e4193db5e',
                        message: 'Přidat panel repozitáře',
                        authorName: 'Pavol Hejný',
                        committedAt: '2026-08-21T19:25:00+02:00',
                    },
                ],
                commitCountSinceStart: 2,
                isCommitCountSinceStartComplete: true,
            },
            isProgressRead: true,
            newCommitShas: new Set(['6dcb09b5b57875f334f61aebed695e2e4193db5e']),
        };

        renderParticipantRoom(WORKSHOP_ABOUT_A_PROJECT);

        await screen.findByRole('button', { name: 'Free členství. Otevřít možnosti členství' });

        expect(screen.getByText('hejny/promptbook')).not.toBeNull();
        expect(screen.getByText('Přidat panel repozitáře')).not.toBeNull();
        expect(screen.getByText('Nový')).not.toBeNull();
        expect(screen.getByRole('link', { name: /Živá aplikace/ }).getAttribute('href')).toBe(
            'https://workshop.example/app',
        );

        const repositoryPanel = screen.getByLabelText('Projekt workshopu');
        const materialsSection = screen.getByRole('heading', { name: 'Materiály z workshopu' }).closest('section');

        expect(materialsSection?.contains(repositoryPanel)).toBe(true);
        expect(repositoryPanel.tagName).toBe('ARTICLE');
        expect(repositoryPanel.textContent).not.toContain('A/B testing landing page for Promptbook');
        expect(repositoryPanel.textContent).not.toContain('Od začátku workshopu');
        expect(repositoryPanel.textContent).not.toContain('main');
        expect(repositoryPanel.textContent).not.toContain('TypeScript');
        expect(within(repositoryPanel).queryByText('1')).toBeNull();
    });

    it('keeps the repository material available to a paying participant', async () => {
        fetchCommunityMembership.mockResolvedValue(PAID_MEMBERSHIP);
        renderParticipantRoom(WORKSHOP_ABOUT_A_PROJECT);

        await screen.findByRole('button', { name: 'Placené členství. Otevřít stav členství' });
        expect(screen.getByLabelText('Projekt workshopu')).not.toBeNull();
    });

    it('shows the workshop presentation as a shared material even when no ordinary material has unlocked', async () => {
        renderParticipantRoom(WORKSHOP_WITH_PRESENTATION);

        const presentationMaterial = await screen.findByLabelText('Prezentace workshopu');
        const materialsSection = screen.getByRole('heading', { name: 'Materiály z workshopu' }).closest('section');
        const presentationLink = await screen.findByRole('link', { name: /Otevřít prezentaci/ });

        expect(materialsSection?.contains(presentationMaterial)).toBe(true);
        expect(presentationLink.getAttribute('href')).toBe(WORKSHOP_WITH_PRESENTATION.presentationUrl);
        expect(presentationLink.getAttribute('target')).toBe('_blank');
        expect(presentationLink.getAttribute('rel')).toBe('noopener noreferrer');
    });

    it('keeps the presentation material available to a paying participant', async () => {
        fetchCommunityMembership.mockResolvedValue(PAID_MEMBERSHIP);
        renderParticipantRoom(WORKSHOP_WITH_PRESENTATION);

        await screen.findByRole('button', { name: 'Placené členství. Otevřít stav členství' });
        expect(screen.getByLabelText('Prezentace workshopu')).not.toBeNull();
    });

    it('shows nothing about a project in a room which is about none, and in a room which cannot be about one', () => {
        renderParticipantRoom(WORKSHOP);
        expect(screen.queryByLabelText('Projekt workshopu')).toBeNull();

        cleanup();

        renderParticipantRoom({ ...COMMUNITY, repository: WORKSHOP_ABOUT_A_PROJECT.repository });
        expect(screen.queryByLabelText('Projekt workshopu')).toBeNull();
    });

    it('does not put a presentation into a room kind which cannot offer one', () => {
        renderParticipantRoom({ ...COMMUNITY, presentationUrl: WORKSHOP_WITH_PRESENTATION.presentationUrl });

        expect(screen.queryByLabelText('Prezentace workshopu')).toBeNull();
    });

    it('lets a member vote on a visible community poll attached to a workshop', () => {
        renderParticipantRoom(WORKSHOP, undefined, false, undefined, [ATTACHED_COMMUNITY_POLL]);

        expect(screen.getByText('Co si z workshopu odnášíte?')).not.toBeNull();
        expect(screen.getByText('7 · 64 %')).not.toBeNull();
        expect(screen.getByRole('button', { name: /Praktické tipy/ }).hasAttribute('disabled')).toBe(false);
    });

    it('leads from a permanent room to a workshop with the identity the room already verified', () => {
        renderParticipantRoom(COMMUNITY, WORKSHOP_NAVIGATION);

        expect(screen.getByRole('link', { name: new RegExp(WORKSHOP.title) }).getAttribute('href')).toBe(
            `/cs/online-workshop/participant?workshop=${WORKSHOP.slug}&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1`,
        );
    });

    it('leads from a workshop into the permanent community with that very same identity', () => {
        renderParticipantRoom(WORKSHOP);

        const communityLink = screen.getByRole('link', { name: /Vstoupit do komunity/ });
        const materialsSection = screen.getByRole('heading', { name: 'Materiály z workshopu' }).closest('section');

        expect(communityLink.getAttribute('href')).toBe(
            '/cs/komunita?email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
        );
        expect(materialsSection?.contains(communityLink)).toBe(true);
        expect(communityLink.closest('article')).not.toBeNull();
    });

    it('keeps the community material available to a paying member as well', async () => {
        fetchCommunityMembership.mockResolvedValue(PAID_MEMBERSHIP);
        renderParticipantRoom(WORKSHOP);

        await screen.findByRole('button', { name: 'Placené členství. Otevřít stav členství' });
        expect(screen.getByRole('link', { name: /Vstoupit do komunity/ })).not.toBeNull();
    });

    it('invites a member who does not pay into the community before the other materials of the room', async () => {
        renderParticipantRoom(WORKSHOP_WITH_PRESENTATION);

        await screen.findByRole('button', { name: 'Free členství. Otevřít možnosti členství' });
        const materialCards = getMaterialCards();

        expect(getCommunityMaterialIndex(materialCards)).toBe(0);
        expect(materialCards.indexOf(screen.getByLabelText('Prezentace workshopu'))).toBe(materialCards.length - 1);
    });

    it('leaves the community at the end of the materials of a paying member', async () => {
        fetchCommunityMembership.mockResolvedValue(PAID_MEMBERSHIP);
        renderParticipantRoom(WORKSHOP_WITH_PRESENTATION);

        await screen.findByRole('button', { name: 'Placené členství. Otevřít stav členství' });
        const materialCards = getMaterialCards();

        expect(getCommunityMaterialIndex(materialCards)).toBe(materialCards.length - 1);
        expect(materialCards.indexOf(screen.getByLabelText('Prezentace workshopu'))).toBe(0);
    });

    it('invites nobody into the community from the community itself, nor from a project discussion inside it', () => {
        renderParticipantRoom(COMMUNITY);
        expect(screen.queryByRole('link', { name: /Vstoupit do komunity/ })).toBeNull();

        cleanup();

        renderParticipantRoom(PROJECT_DISCUSSION);
        expect(screen.queryByRole('link', { name: /Vstoupit do komunity/ })).toBeNull();
    });

    it('keeps the chat and the materials of the shared room in a permanent room', () => {
        renderParticipantRoom(COMMUNITY);

        expect(screen.queryByText(COMMUNITY.title)).not.toBeNull();
        expect(screen.queryByRole('textbox')).not.toBeNull();
    });

    it('places room-specific membership information beside the participant badge', () => {
        renderParticipantRoom(COMMUNITY, undefined, false, <span>Free členství</span>);

        expect(screen.getByText('Free členství')).toBeTruthy();
/**
 * @vitest-environment jsdom
 */

import { WorkshopContent } from '@/businesses/online-workshop/participant/WorkshopContent';
import { WorkshopPresentationMaterial } from '@/businesses/online-workshop/participant/WorkshopPresentationMaterial';
import type { CommunityMembershipRoomState } from '@/lib/community-membership/communityMembershipTypes';
import type { WorkshopSpecialMaterial } from '@/lib/workshops/workshopSpecialMaterials';
import type { WorkshopContentBlock, WorkshopContentPreview } from '@/lib/workshops/workshopTypes';
import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
import { afterEach, describe, expect, it, vi } from 'vitest';

/**
 * The room as far as the material list is concerned: the membership it already loaded and the modal it can open
 */
const membershipRoomMock = vi.hoisted(() => ({
    membershipRoom: null as null | {
        membership: CommunityMembershipRoomState | null;
        openMembershipModal: () => void;
    },
}));

vi.mock('@/businesses/community/membership/CommunityMembershipRoomProvider', () => ({
    useCommunityMembershipRoom: () => membershipRoomMock.membershipRoom,
}));

vi.mock('@/components/markdown-content', () => ({
    MarkdownContent: ({ content, className }: { readonly content: string; readonly className?: string }) => {
        const markdownLinks = Array.from(content.matchAll(/\[([^\]]+)\]\((?:<([^>]+)>|([^)]+))\)/g));

        return (
            <div data-testid="markdown-content" className={className}>
                {markdownLinks.map((markdownLink) => {
                    const href = markdownLink[2] ?? markdownLink[3];
                    return (
                        <a key={href} href={href}>
                        {markdownLink[1]}
                    </a>
                    );
                })}
            </div>
        );
    },
}));

vi.mock('@/components/promptbook-qr-code', () => ({
    PromptbookQrCode: ({
        value,
        size,
        className,
    }: {
        readonly value: string;
        readonly size?: number;
        readonly className?: string;
    }) => <span data-testid="workshop-material-qr-code" data-value={value} data-size={size} className={className} />,
}));

const CONTENT_BLOCK: WorkshopContentBlock = {
    id: 'material-1',
    title: '',
    bodyMarkdown: '[Zjistit více](https://ptbk.io/material-abc123)',
    unlockAt: '2026-08-20T19:00:00.000Z',
    sortOrder: 0,
    isPublished: true,
    isFollowUp: false,
    isPaidMembersOnly: false,
    createdAt: '2026-08-20T18:00:00.000Z',
    updatedAt: '2026-08-20T18:00:00.000Z',
    linkClickCount: 0,
};

const FREE_PURCHASABLE_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'none',
    monthlyPriceCzk: null,
    currentPeriodEndsAt: null,
    isCancellationScheduled: false,
    isPurchaseOffered: true,
    isSubscriptionManagementOffered: false,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const PAID_MEMBERSHIP: CommunityMembershipRoomState = {
    ...FREE_PURCHASABLE_MEMBERSHIP,
    status: 'active',
    monthlyPriceCzk: 199,
    currentPeriodEndsAt: '2026-09-30T10:00:00.000Z',
    isPurchaseOffered: false,
    isSubscriptionManagementOffered: true,
};

const PAID_MEMBERS_ONLY_CONTENT_PREVIEWS: readonly WorkshopContentPreview[] = [
    { id: 'paid-material-1', title: 'Bonusové podklady' },
];

/**
 * The material list of a room which has an ordinary material, a card placed by the membership, and one placed after it
 */
const TITLED_CONTENT_BLOCK: WorkshopContentBlock = { ...CONTENT_BLOCK, title: 'Podklady z workshopu' };

const COMMUNITY_INVITATION: WorkshopSpecialMaterial = {
    id: 'community',
    content: <article aria-label="Komunita Promptbooku">Komunita Promptbooku</article>,
    placement: 'before-materials-until-paid',
};

const PRESENTATION_SPECIAL_MATERIAL: WorkshopSpecialMaterial = {
    id: 'presentation',
    content: <WorkshopPresentationMaterial presentationUrl="https://files.example.com/prezentace.pdf" />,
};

/**
 * Every card of the material list, in the order it is read in, named the way the member reading it is told
 */
function getMaterialOrder(container: HTMLElement): readonly string[] {
    return Array.from(container.querySelectorAll('article')).map(
        (materialCard) => materialCard.getAttribute('aria-label') ?? materialCard.querySelector('h3')?.textContent ?? '',
    );
}

function renderWorkshopContent(
    contentBlocks: readonly WorkshopContentBlock[],
    paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[] = [],
    specialMaterials: readonly WorkshopSpecialMaterial[] = [],
) {
    return render(
        <WorkshopContent
            contentBlocks={contentBlocks}
            nextContentUnlockAt={null}
            newlyUnlockedContentBlockIds={new Set()}
            paidMembersOnlyContentPreviews={paidMembersOnlyContentPreviews}
            specialMaterials={specialMaterials}
        />,
    );
}

afterEach(() => {
    cleanup();
    membershipRoomMock.membershipRoom = null;
});

describe('workshop materials', () => {
    it('keeps a special material in the material list even when no ordinary material is unlocked', () => {
        renderWorkshopContent(
            [],
            [],
            [
            {
                id: 'community',
                content: <article aria-label="Komunita Promptbooku">Komunita Promptbooku</article>,
            },
            ],
        );

        expect(screen.getByRole('heading', { name: 'Materiály z workshopu' })).not.toBeNull();
        expect(screen.getByRole('article', { name: 'Komunita Promptbooku' })).not.toBeNull();
    });

    it('opens the material list of a member who does not pay with the invitation into the community', () => {
        membershipRoomMock.membershipRoom = { membership: FREE_PURCHASABLE_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = renderWorkshopContent(
            [TITLED_CONTENT_BLOCK],
            [],
            [PRESENTATION_SPECIAL_MATERIAL, COMMUNITY_INVITATION],
        );

        expect(getMaterialOrder(container)).toEqual([
            'Komunita Promptbooku',
            'Podklady z workshopu',
            'Prezentace workshopu',
        ]);
    });

    it('closes the material list of a paying member with that very same invitation', () => {
        membershipRoomMock.membershipRoom = { membership: PAID_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = renderWorkshopContent(
            [TITLED_CONTENT_BLOCK],
            [],
            [PRESENTATION_SPECIAL_MATERIAL, COMMUNITY_INVITATION],
        );

        expect(getMaterialOrder(container)).toEqual([
            'Podklady z workshopu',
            'Prezentace workshopu',
            'Komunita Promptbooku',
        ]);
    });

    it('invites into the community first while the membership of the member is still unknown', () => {
        const { container } = renderWorkshopContent([TITLED_CONTENT_BLOCK], [], [COMMUNITY_INVITATION]);

        expect(getMaterialOrder(container)).toEqual(['Komunita Promptbooku', 'Podklady z workshopu']);
    });

    it('uses the ordinary material card, primary action, and QR code for a workshop presentation', async () => {
        const presentationUrl = 'https://files.example.com/(ai-agents).pptx';
        renderWorkshopContent(
            [],
            [],
            [
                {
                    id: 'presentation',
                    content: <WorkshopPresentationMaterial presentationUrl={presentationUrl} />,
                },
            ],
        );

        expect(screen.getByLabelText('Prezentace workshopu')).not.toBeNull();
        expect(screen.getByRole('heading', { name: 'Prezentace' })).not.toBeNull();
        expect(screen.getByRole('link', { name: 'Otevřít prezentaci: Prezentace' }).getAttribute('href')).toBe(
            presentationUrl,
        );
        expect((await screen.findByTestId('workshop-material-qr-code')).getAttribute('data-value')).toBe(
            presentationUrl,
        );
    });

    it('offers a prominent short-link call to action when a material has one link', async () => {
        renderWorkshopContent([CONTENT_BLOCK]);

        const callToAction = await screen.findByRole('link', { name: /Otevřít materiál: Zjistit více/ });

        expect(callToAction.getAttribute('href')).toBe('https://ptbk.io/material-abc123');
        expect(callToAction.getAttribute('target')).toBe('_blank');
        expect(callToAction.getAttribute('rel')).toBe('noopener noreferrer');
    });

    it('shows each material short link as a desktop-only QR code for opening on a phone', async () => {
        const secondContentBlock: WorkshopContentBlock = {
            ...CONTENT_BLOCK,
            id: 'material-2',
            bodyMarkdown: '[Stáhnout podklady](https://ptbk.io/material-def456)',
        };
        renderWorkshopContent([CONTENT_BLOCK, secondContentBlock]);

        const qrCodes = await screen.findAllByTestId('workshop-material-qr-code');

        expect(qrCodes.map((qrCode) => qrCode.getAttribute('data-value'))).toEqual([
            'https://ptbk.io/material-abc123',
            'https://ptbk.io/material-def456',
        ]);
        expect(qrCodes.every((qrCode) => qrCode.getAttribute('data-size') === '144')).toBe(true);
        expect(screen.getAllByLabelText('QR kódy materiálů')).toHaveLength(2);
        expect(
            screen.getAllByLabelText('QR kódy materiálů').every((qrCodes) => qrCodes.className.includes('hidden')),
        ).toBe(true);
        expect(
            screen.getAllByLabelText('QR kódy materiálů').every((qrCodes) => qrCodes.className.includes('lg:flex')),
        ).toBe(true);
    });

    it('uses the QR renderer quiet zone without an extra frame or redundant phone prompt', async () => {
        renderWorkshopContent([CONTENT_BLOCK]);

        const qrCode = await screen.findByTestId('workshop-material-qr-code');
        const qrCodeFigure = screen.getByLabelText('QR kód materiálu: Zjistit více');

        expect(qrCode.parentElement).toBe(qrCodeFigure);
        expect(qrCode.className).toContain('mx-auto');
        expect(qrCode.className).not.toMatch(/(?:^|\s)p-\S+/);
        expect(screen.queryByText('Otevřít v telefonu')).toBeNull();
    });

    it('keeps multiple material links as light underlined links without a call to action', async () => {
        const contentBlockWithMultipleLinks: WorkshopContentBlock = {
            ...CONTENT_BLOCK,
            bodyMarkdown: '[První materiál](https://example.com/one) a [druhý materiál](https://example.com/two)',
        };
        const { container } = renderWorkshopContent([contentBlockWithMultipleLinks]);

        await waitFor(() => expect(container.querySelectorAll('a')).toHaveLength(2));

        expect(screen.queryByRole('link', { name: /Otevřít materiál/ })).toBeNull();
        expect(screen.getByTestId('markdown-content').className).toContain('[--chat-md-link-color:#f1f5f9]');
    });

    it('keeps every link of a multi-link material available through its own QR code', async () => {
        const contentBlockWithMultipleLinks: WorkshopContentBlock = {
            ...CONTENT_BLOCK,
            bodyMarkdown:
                '[První materiál](https://ptbk.io/material-one) a [druhý materiál](https://ptbk.io/material-two)',
        };
        renderWorkshopContent([contentBlockWithMultipleLinks]);

        const qrCodes = await screen.findAllByTestId('workshop-material-qr-code');

        expect(qrCodes.map((qrCode) => qrCode.getAttribute('data-value'))).toEqual([
            'https://ptbk.io/material-one',
            'https://ptbk.io/material-two',
        ]);
        expect(screen.getByLabelText('QR kód materiálu: První materiál')).not.toBeNull();
        expect(screen.getByLabelText('QR kód materiálu: druhý materiál')).not.toBeNull();
    });

    it('marks the selected follow-up material while it stays in the ordinary material list', () => {
        renderWorkshopContent([{ ...CONTENT_BLOCK, isFollowUp: true, title: 'Další krok' }]);

        expect(screen.getByText('Navazující materiál')).not.toBeNull();
        expect(screen.getByRole('heading', { name: 'Další krok' })).not.toBeNull();
    });

    it('marks a material which only paid members may see while it stays in the list of a member who paid', () => {
        renderWorkshopContent([{ ...CONTENT_BLOCK, isPaidMembersOnly: true, title: 'Bonusové podklady' }]);

        expect(screen.getByText('Pro placené členy')).not.toBeNull();
        expect(screen.getByRole('heading', { name: 'Bonusové podklady' })).not.toBeNull();
    });

    it('says where the paid materials are and offers the membership which unlocks them to a member who has not paid', () => {
        const openMembershipModal = vi.fn();
        membershipRoomMock.membershipRoom = { membership: FREE_PURCHASABLE_MEMBERSHIP, openMembershipModal };
        renderWorkshopContent([CONTENT_BLOCK], PAID_MEMBERS_ONLY_CONTENT_PREVIEWS);

        expect(screen.getByText('Materiály pro placené členy')).not.toBeNull();
        fireEvent.click(screen.getByRole('button', { name: /Koupit placené členství/ }));

        expect(openMembershipModal).toHaveBeenCalledOnce();
    });

    it('names every hidden paid material as a teaser of what the membership unlocks', () => {
        membershipRoomMock.membershipRoom = { membership: FREE_PURCHASABLE_MEMBERSHIP, openMembershipModal: vi.fn() };
        renderWorkshopContent(
            [CONTENT_BLOCK],
            [...PAID_MEMBERS_ONLY_CONTENT_PREVIEWS, { id: 'paid-material-2', title: 'Nahrávka workshopu' }],
        );

        const contentPreviewTitles = Array.from(
            screen.getByLabelText('Náhled materiálů pro placené členy').querySelectorAll('li'),
        ).map((listItem) => listItem.textContent);

        expect(contentPreviewTitles).toEqual(['Bonusové podklady', 'Nahrávka workshopu']);
        expect(screen.getByText('Co odemknete')).not.toBeNull();
    });

    it('keeps saying where the paid materials are when none of them has a title to tease with', () => {
        membershipRoomMock.membershipRoom = { membership: FREE_PURCHASABLE_MEMBERSHIP, openMembershipModal: vi.fn() };
        renderWorkshopContent([], [{ id: 'paid-material-1', title: '' }]);

        expect(screen.getByText('Materiály pro placené členy')).not.toBeNull();
        expect(screen.queryByLabelText('Náhled materiálů pro placené členy')).toBeNull();
    });

    it('keeps saying where the paid materials are even when nothing else is unlocked yet', () => {
        membershipRoomMock.membershipRoom = { membership: FREE_PURCHASABLE_MEMBERSHIP, openMembershipModal: vi.fn() };
        renderWorkshopContent([], PAID_MEMBERS_ONLY_CONTENT_PREVIEWS);

        expect(screen.getByText('Materiály pro placené členy')).not.toBeNull();
    });

    it('shows no paid-materials notice while the membership is still unknown or cannot be bought', () => {
        renderWorkshopContent([CONTENT_BLOCK], PAID_MEMBERS_ONLY_CONTENT_PREVIEWS);
        expect(screen.queryByText('Materiály pro placené členy')).toBeNull();
        expect(screen.queryByText('Bonusové podklady')).toBeNull();

        membershipRoomMock.membershipRoom = {
            membership: { ...FREE_PURCHASABLE_MEMBERSHIP, isPurchaseOffered: false },
            openMembershipModal: vi.fn(),
        };
        renderWorkshopContent([CONTENT_BLOCK], PAID_MEMBERS_ONLY_CONTENT_PREVIEWS);
        expect(screen.queryByText('Materiály pro placené členy')).toBeNull();

codex
I found no PDF dependency in the app, but there is already a small in-browser PDF writer used for workshop analytics and a shared browser download helper. I’ll extend that existing export path for paginated, Unicode-safe wrap-ups rather than adding a package or duplicating a download mechanism.
apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\pdfPictures.ts
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\svgChartExport.ts
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..7e3e59bc9509e749dc2df995012df3244857a48a
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -151,121 +151,21 @@
 }
 
 /**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
-}
-
-/**
  * Write the smallest possible document which shows one picture on one page
  *
  * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
-    );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..7e3e59bc9509e749dc2df995012df3244857a48a
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -151,121 +151,21 @@
 }
 
 /**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
-}
-
-/**
  * Write the smallest possible document which shows one picture on one page
  *
  * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
-    );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content lib/exports/svgChartExport.ts | Select-Object -First 45; Get-Content lib/exports/svgChartExport.ts | Select-Object -Skip 165 -First 90; Get-Content -Raw lib/exports/pdfPictures.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 420ms:
/**
 * Turning a chart which is already drawn on the page into a file
 *
 * Note: The chart is an SVG element, so the picture is taken from the very element the reader is looking at instead of
 *       drawing it a second time. What the three pictures have in common is therefore one serialization, which the
 *       raster and the printable file both start from.
 */

const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

/**
 * A picture is exported with a background, because a chart drawn in dark ink on nothing at all is unreadable in a
 * viewer which shows transparency as black.
 */
const DEFAULT_EXPORT_BACKGROUND_COLOR = '#ffffff';

/**
 * The exported picture carries the typeface it is read in, because a file which left the page has no stylesheet to
 * inherit one from and would otherwise be written in whatever a viewer falls back to.
 */
const EXPORT_FONT_FAMILY = 'system-ui, -apple-system, "Segoe UI", sans-serif';

/**
 * How much sharper the raster picture is than the chart on the screen, so that it stays readable when it is enlarged
 */
const PNG_EXPORT_SCALE = 2;

const JPEG_EXPORT_QUALITY = 0.95;

/**
 * The printable page, in the points a PDF measures everything in, and the margin the picture keeps from its edges
 */
const PDF_PAGE_WIDTH_POINTS = 842;
const PDF_PAGE_HEIGHT_POINTS = 595;
const PDF_PAGE_MARGIN_POINTS = 24;

export type SvgChartExportOptions = {
    /**
     * The name the picture carries inside itself, which a reader of the file sees instead of a bare drawing
     */
    readonly title: string;

    readonly backgroundColor?: string;
};

        },
        title,
    );
}

export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
    const canvasElement = await drawChartOnCanvas(svgElement, options);
    const pictureBlob = await readCanvasAsBlob(canvasElement, 'image/jpeg', JPEG_EXPORT_QUALITY);
    const pictureBytes = new Uint8Array(await pictureBlob.arrayBuffer());

    return createSinglePicturePdf(
        pictureBytes,
        { width: canvasElement.width, height: canvasElement.height },
        options.title,
    );
}
/**
 * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
 *
 * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
 * pixels without embedding a second font or relying on a reader's local font collection.
 */

export type PdfPicture = {
    readonly bytes: Uint8Array;
    readonly width: number;
    readonly height: number;
};

export type PdfPageSize = {
    readonly width: number;
    readonly height: number;
    readonly margin: number;
};

/**
 * Where one part of the written document begins, counted in bytes from its very beginning
 */
function getByteLength(chunks: readonly Uint8Array[]): number {
    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
}

function encodeAsciiText(text: string): Uint8Array {
    return new TextEncoder().encode(text);
}

function formatCrossReferenceEntry(objectOffset: number): string {
    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
}

/**
 * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
 *
 * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
 * which makes it read the same in every PDF reader without making the document embed a font.
 */
function formatPdfTextString(text: string): string {
    const characterCodes = ['FEFF'];

    for (const character of text) {
        const codePoint = character.codePointAt(0) ?? 0;

        if (codePoint > 0xffff) {
            const surrogateOffset = codePoint - 0x10000;
            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
        } else {
            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
        }
    }

    return `<${characterCodes.join('').toUpperCase()}>`;
}

function getPdfPicturePlacement(
    picture: Pick<PdfPicture, 'width' | 'height'>,
    pageSize: PdfPageSize,
): {
    readonly width: number;
    readonly height: number;
    readonly offsetX: number;
    readonly offsetY: number;
} {
    const availableWidth = pageSize.width - 2 * pageSize.margin;
    const availableHeight = pageSize.height - 2 * pageSize.margin;
    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
    const width = picture.width * scale;
    const height = picture.height * scale;

    return {
        width,
        height,
        offsetX: (pageSize.width - width) / 2,
        offsetY: (pageSize.height - height) / 2,
    };
}

function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
    const placement = getPdfPicturePlacement(picture, pageSize);

    return (
        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
    );
}

function getPageObjectNumber(pageIndex: number): number {
    return 3 + pageIndex * 3;
}

function appendPdfObject(
    chunks: Uint8Array[],
    objectOffsets: number[],
    objectNumber: number,
    objectBodyChunks: readonly Uint8Array[],
): void {
    objectOffsets.push(getByteLength(chunks));
    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
}

function appendPdfTextObject(
    chunks: Uint8Array[],
    objectOffsets: number[],
    objectNumber: number,
    objectBody: string,
): void {
    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
}

/**
 * Write one or more JPEG pictures as a printable PDF document.
 *
 * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
 * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
 */
export function createPicturePdf(
    pictures: readonly PdfPicture[],
    pageSize: PdfPageSize,
    title: string,
): Blob {
    if (pictures.length === 0) {
        throw new Error('PDF musí obsahovat alespoň jednu stránku');
    }

    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
    const objectOffsets: number[] = [];

    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
    appendPdfTextObject(
        chunks,
        objectOffsets,
        2,
        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
            `/Count ${pictures.length} >>`,
    );

    pictures.forEach((picture, pageIndex) => {
        const pageObjectNumber = getPageObjectNumber(pageIndex);
        const contentObjectNumber = pageObjectNumber + 1;
        const pictureObjectNumber = pageObjectNumber + 2;
        const contentStream = createPageContentStream(picture, pageSize);

        appendPdfTextObject(
            chunks,
            objectOffsets,
            pageObjectNumber,
            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
        );
        appendPdfTextObject(
            chunks,
            objectOffsets,
            contentObjectNumber,
            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
        );
        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
            encodeAsciiText(
                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
            ),
            picture.bytes,
            encodeAsciiText('\nendstream'),
        ]);
    });

    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);

    const crossReferenceOffset = getByteLength(chunks);
    chunks.push(
        encodeAsciiText(
            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
                objectOffsets.map(formatCrossReferenceEntry).join('') +
                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
        ),
    );

    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
}


diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..7e3e59bc9509e749dc2df995012df3244857a48a
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -151,121 +151,21 @@
 }
 
 /**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
-}
-
-/**
  * Write the smallest possible document which shows one picture on one page
  *
  * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
-    );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\svgChartExport.ts
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopWrapUpDocument.ts
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\workshopWrapUpPdf.ts
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6b7f28f5874ae219b3baa7ecf1bda1739d6fae85
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,364 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly documentTitle: string;
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(documentTitle: string): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage(renderState.documentTitle);
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        documentTitle: document.title,
+        pages: [createWrapUpPdfPage(document.title)],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6b7f28f5874ae219b3baa7ecf1bda1739d6fae85
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,364 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly documentTitle: string;
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(documentTitle: string): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage(renderState.documentTitle);
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        documentTitle: document.title,
+        pages: [createWrapUpPdfPage(document.title)],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw tsconfig.json; npx eslint lib/exports/pdfPictures.ts lib/exports/svgChartExport.ts lib/workshops/workshopWrapUpDocument.ts lib/exports/workshopWrapUpPdf.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 2717ms:
{
  "compilerOptions": {
    "target": "es5",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./*"
      ],
      "@books/*": [
        "../books/*.book"
      ]
      // <- Note: [📖] Allow books to be imported
      // TODO: [🧵]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "next-env.d.ts",
    "out/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "out/dev/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}


Oops! Something went wrong! :(

ESLint: 9.39.0

ESLint couldn't find an eslint.config.(js|mjs|cjs) file.

From ESLint v9.0.0, the default configuration file is now eslint.config.js.
If you are using a .eslintrc.* file, please follow the migration guide
to update your configuration file to the new format:

https://eslint.org/docs/latest/use/configure/migration-guide

If you still have problems after following the migration guide, please stop by
https://eslint.org/chat/help to chat with the team.


diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6b7f28f5874ae219b3baa7ecf1bda1739d6fae85
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,364 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly documentTitle: string;
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(documentTitle: string): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage(renderState.documentTitle);
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        documentTitle: document.title,
+        pages: [createWrapUpPdfPage(document.title)],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\workshopWrapUpPdf.ts
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\OnlineWorkshopParticipantPage.tsx
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopStage.tsx
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUp.tsx
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx; Get-Content businesses/online-workshop/participant/WorkshopStage.tsx | Select-Object -First 100; Get-Content businesses/online-workshop/participant/WorkshopStage.tsx | Select-Object -Skip 135 -First 50; Get-Content lib/exports/svgChartExport.ts | Select-Object -First 18; git diff --stat; git diff -- lib/exports/svgChartExport.ts lib/exports/pdfPictures.ts lib/workshops/workshopWrapUpDocument.ts lib/exports/workshopWrapUpPdf.ts businesses/online-workshop/participant/WorkshopWrapUp.tsx businesses/online-workshop/participant/WorkshopStage.tsx businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 384ms:
'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly workshop: Pick<
        WorkshopDetails,
        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
    >;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    workshop,
    contentBlocks,
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}

'use client';

import { WorkshopWrapUp } from '@/businesses/online-workshop/participant/WorkshopWrapUp';
import { WorkshopStageComment } from '@/businesses/online-workshop/participant/WorkshopStageComment';
import { WorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/WorkshopRepositoryCommitNotification';
import { useWorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/useWorkshopRepositoryCommitNotification';
import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import { useWorkshopReactionStream } from '@/components/workshops/useWorkshopReactionStream';
import { WorkshopReactionStream } from '@/components/workshops/WorkshopReactionStream';
import { trackGoogleAnalyticsEvent } from '@/lib/tracking/track-google-analytics-event';
import { createYoutubeEmbedUrl } from '@/lib/youtube/youtubeEmbed';
import { keepYoutubeVideoSubtitlesHidden, unmuteYoutubeVideo } from '@/lib/youtube/youtubePlayerCommands';
import { getWorkshopPhase } from '@/lib/workshops/workshopPhase';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type {
    WorkshopCommentReference,
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { motion, useReducedMotion } from 'framer-motion';
import { ArrowDownLeft, ArrowLeft, Maximize, Play, Radio, Volume2 } from 'lucide-react';
import { useEffect, useMemo, useRef, useState } from 'react';

const CLOCK_TICK_MILLISECONDS = 1000;

type WorkshopStageProps = {
    readonly workshop: WorkshopDetails;
    readonly serverTime: string;

    /**
     * Where the reactions of the room come from
     *
     * Note: The stage keeps the flying reactions itself, so a busy room re-renders nothing but the stage they fly over.
     */
    readonly subscribeToReactions: SubscribeToWorkshopReactions;

    /** The project connected to this workshop, if there is one */
    readonly repository?: WorkshopRepository | null;

    /** Offers the stage commits found by the repository monitor or its polling fallback */
    readonly subscribeToRepositoryCommits?: SubscribeToWorkshopRepositoryCommits;

    /**
     * All of these values are supplied by the participant room in production. Defaults keep this low-level stage usable
     * in isolation while it is still responsible for choosing the correct temporal phase.
     */
    readonly feedback?: WorkshopFeedback | null;
    readonly contentBlocks?: readonly WorkshopContentBlock[];
    readonly followUpContentBlock?: WorkshopContentBlock | null;
    readonly stageComment?: WorkshopCommentReference | null;

    /**
     * The recording which this member has not unlocked, or `null` while nothing of the video is withheld from them
     *
     * Note: The server decides this together with the video it hands over, so the stage plays whatever video it was
     *       given and offers the membership exactly when it was given none.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;
    readonly onSaveFeedback?: (values: WorkshopFeedbackValues) => Promise<boolean>;
};

function getRemainingSegments(remainingMilliseconds: number) {
    const remainingSeconds = Math.max(0, Math.floor(remainingMilliseconds / 1000));
    return [
        { label: 'dní', value: Math.floor(remainingSeconds / 86400) },
        { label: 'hodin', value: Math.floor((remainingSeconds % 86400) / 3600) },
        { label: 'minut', value: Math.floor((remainingSeconds % 3600) / 60) },
        { label: 'sekund', value: remainingSeconds % 60 },
    ];
}

function requestVideoFullscreen(videoFrame: HTMLIFrameElement | null): void {
    const requestFullscreen = videoFrame?.requestFullscreen;
    if (requestFullscreen === undefined || videoFrame === null) {
        return;
    }

    void requestFullscreen.call(videoFrame).catch(() => undefined);
}

const refuseStandaloneFeedbackSave = async (): Promise<boolean> => false;

export function WorkshopStage({
    workshop,
    serverTime,
    subscribeToReactions,
    repository = null,
    subscribeToRepositoryCommits,
    feedback = null,
    contentBlocks = [],
    followUpContentBlock = null,
    stageComment = null,
    paidMembersOnlyVideo = null,
    onSaveFeedback = refuseStandaloneFeedbackSave,
}: WorkshopStageProps) {
    const isReducedMotionPreferred = useReducedMotion() === true;
        );
        return () => window.clearInterval(intervalId);
    }, [serverClockOffset]);

    useEffect(() => setIsVideoUnmuted(false), [workshop.youtubeVideoId]);

    useEffect(() => {
        if (workshop.youtubeVideoId === null || !isWorkshopOngoing) {
            return;
        }

        return keepYoutubeVideoSubtitlesHidden(videoFrameReference.current);
    }, [workshop.youtubeVideoId, isWorkshopOngoing]);

    const countdownSegments = getRemainingSegments(remainingMilliseconds);
    const handleVideoUnmute = () => {
        unmuteYoutubeVideo(videoFrameReference.current);
        setIsVideoUnmuted(true);
        trackGoogleAnalyticsEvent('workshop_video_unmuted', { workshop_slug: workshop.slug });
    };
    const handleVideoFullscreen = () => requestVideoFullscreen(videoFrameReference.current);

    return (
        <section className="relative overflow-hidden rounded-2xl border border-white/10 bg-[#081a24] shadow-2xl">
            {isWorkshopPast && isVideoRewatchShown && workshop.youtubeVideoId !== null ? (
                <div>
                    <div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 px-5 py-3">
                        <span className="inline-flex items-center gap-2 text-sm font-bold text-white">
                            <Play className="h-4 w-4 text-amber-300" aria-hidden="true" /> Video z workshopu
                        </span>
                        <button
                            type="button"
                            onClick={() => setIsVideoRewatchShown(false)}
                            className="inline-flex items-center gap-2 rounded-full border border-white/20 bg-slate-950/60 px-3 py-1.5 text-xs font-semibold text-slate-200 transition hover:border-cyan-200/70 hover:bg-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200"
                        >
                            <ArrowLeft className="h-3.5 w-3.5" aria-hidden="true" /> Zpět na závěrečné shrnutí
                        </button>
                    </div>
                    <div className="relative aspect-video">
                        <iframe
                            className="absolute inset-0 h-full w-full"
                            src={createYoutubeEmbedUrl(workshop.youtubeVideoId, {
                                isAutoplayed: true,
                                isInlinePlayback: true,
                                isRelatedVideoEnabled: false,
                                isControlsVisible: true,
                                isJavaScriptApiEnabled: false,
                                startAtSeconds: workshop.recordingStartOffsetSeconds,
                            })}
                            title={workshop.title}
/**
 * Turning a chart which is already drawn on the page into a file
 *
 * Note: The chart is an SVG element, so the picture is taken from the very element the reader is looking at instead of
 *       drawing it a second time. What the three pictures have in common is therefore one serialization, which the
 *       raster and the printable file both start from.
 */

import { createPicturePdf } from '@/lib/exports/pdfPictures';

const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

/**
 * A picture is exported with a background, because a chart drawn in dark ink on nothing at all is unreadable in a
 * viewer which shows transparency as black.
 */
const DEFAULT_EXPORT_BACKGROUND_COLOR = '#ffffff';

 businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx |   1 +
 businesses/online-workshop/participant/WorkshopStage.tsx                 |   4 ++
 businesses/online-workshop/participant/WorkshopWrapUp.tsx                |  11 ++++
 lib/exports/svgChartExport.ts                                            | 118 ++++------------------------------------
 prompts/2026-09-0240-wrap-up-pdf.md                                      |   2 +-
 5 files changed, 27 insertions(+), 109 deletions(-)
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e017054..3df3571 100644
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@ export function OnlineWorkshopParticipantPage({
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83..eca1968 100644
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@ type WorkshopStageProps = {
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@ export function WorkshopStage({
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@ export function WorkshopStage({
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938f..dec8363 100644
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@ type WorkshopWrapUpProps = {
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@ export function WorkshopWrapUp({
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d01..1a43fc2 100644
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -150,68 +152,6 @@ export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: S
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
 }
 
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
-}
-
 /**
  * Write the smallest possible document which shows one picture on one page
  *
@@ -219,53 +159,15 @@ function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
-    );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx tsc --noEmit' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 10547ms:
lib/exports/workshopWrapUpPdf.ts(218,61): error TS2552: Cannot find name 'PDF_SECTION_TITLE_TEXT_STYLE'. Did you mean 'SECTION_TITLE_TEXT_STYLE'?

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..29d1ed2bc536bd4f4a3cfe55a04ca9c0349df9c8
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, PDF_SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\workshopWrapUpPdf.ts
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

2026-09-16T07:23:47.179028Z ERROR codex_core::tools::router: error=apply_patch verification failed: invalid patch: multiple operations target C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopWrapUpDocument.test.ts
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopWrapUpDocument.test.ts
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\pdfPictures.test.ts
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopStage.test.tsx
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..caed3316e8ed8475e8c28ce5ed8f520027189d79
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,89 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..caed3316e8ed8475e8c28ce5ed8f520027189d79
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,89 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..02b963464702c9345f5d5f61b1b77a56a46dd778
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..02b963464702c9345f5d5f61b1b77a56a46dd778
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx tsc --noEmit' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 8849ms:
businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx(87,50): error TS2339: Property 'toHaveTextContent' does not exist on type 'Assertion<HTMLElement>'.
businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx(88,36): error TS2339: Property 'toHaveAttribute' does not exist on type 'Assertion<HTMLElement>'.

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..02b963464702c9345f5d5f61b1b77a56a46dd778
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "toHaveTextContent|toHaveAttribute|@testing-library/jest-dom" businesses/online-workshop/participant/*.test.tsx | Select-Object -First 100; Get-Content -Raw vitest.config.ts 2>$null; rg -n "setupFiles|jest-dom" . --glob '"'"'!node_modules'"' --glob '"'!'"\\.next' --glob '"'!prompts/**'"' | Select-Object -First 100" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 372ms:
rg: businesses/online-workshop/participant/*.test.tsx: IO error for operation on businesses/online-workshop/participant/*.test.tsx: The filename, directory name, or volume label syntax is incorrect. (os error 123)
import path from 'node:path';
import { defineConfig } from 'vitest/config';

export default defineConfig({
    // Note: The same JSX runtime Next.js compiles the components with, so that a component does not need to import
    //       React just to be renderable in a test.
    esbuild: {
        jsx: 'automatic',
    },
    resolve: {
        alias: {
            '@': path.resolve(__dirname),
        },
    },
    test: {
        exclude: ['node_modules/**', '.next/**', '.tmp/**', 'out/**', 'tests/e2e/**'],
    },
});

.\package.json:95:        "@testing-library/jest-dom": "^6.6.3",
.\package-lock.json:78:                "@testing-library/jest-dom": "^6.6.3",
.\package-lock.json:17917:        "node_modules/@testing-library/jest-dom": {
.\package-lock.json:17919:            "resolved": "https://registry.npmjs.org/@testing-library/jest-dom/-/jest-dom-6.9.1.tgz",
.\package-lock.json:17937:        "node_modules/@testing-library/jest-dom/node_modules/dom-accessibility-api": {

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..02b963464702c9345f5d5f61b1b77a56a46dd778
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect(await screen.findByRole('alert')).toHaveTextContent('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton).not.toHaveAttribute('disabled');
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..3c21c621427ca0a8ecd94563db6c273ac8035563
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..3c21c621427ca0a8ecd94563db6c273ac8035563
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx tsc --noEmit; git status --short' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 11420ms:
 M businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
 M businesses/online-workshop/participant/WorkshopStage.test.tsx
 M businesses/online-workshop/participant/WorkshopStage.tsx
 M businesses/online-workshop/participant/WorkshopWrapUp.tsx
 M lib/exports/svgChartExport.ts
 M prompts/2026-09-0240-wrap-up-pdf.md
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
?? lib/exports/pdfPictures.test.ts
?? lib/exports/pdfPictures.ts
?? lib/exports/workshopWrapUpPdf.ts
?? lib/workshops/workshopWrapUpDocument.test.ts
?? lib/workshops/workshopWrapUpDocument.ts

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..3c21c621427ca0a8ecd94563db6c273ac8035563
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

codex
The implementation now keeps the export entirely participant-side: it receives the same already-authorized workshop state that renders the closing room, derives the summary/takeaways/material list from it, renders a paginated A4 PDF locally, and uses the app’s existing download utility. I’ve also added focused tests for the data boundary, download behavior, and multi-page PDF writer.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx vitest run lib/exports/svgChartExport.test.ts lib/exports/pdfPictures.test.ts lib/workshops/workshopWrapUpDocument.test.ts businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx businesses/online-workshop/participant/WorkshopStage.test.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 8269ms:

 RUN  v3.2.4 C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron

 ✓ lib/exports/svgChartExport.test.ts (4 tests) 11ms
 ❯ lib/exports/pdfPictures.test.ts (2 tests | 1 failed) 31ms
   × picture PDF export > keeps every supplied picture on its own PDF page 28ms
     → expected '%PDF-1.4\n1 0 obj\n<< /Type /Catalog …' to contain '/Title <FEFF005300680072006E007500740…'
   ✓ picture PDF export > rejects a document without a page instead of writing an invalid file 1ms
 ❯ lib/workshops/workshopWrapUpDocument.test.ts (4 tests | 1 failed) 22ms
   × workshop wrap-up document > uses the selected workshop and the same readable materials as the participant room 18ms
     → expected [ 'Postup pro nasazení', …(5) ] to deeply equal [ 'Postup pro nasazení', …(3) ]
   ✓ workshop wrap-up document > does not invent a hidden material when only the readable content blocks reach the document factory 2ms
   ✓ workshop wrap-up document > turns authored Markdown into readable document text while keeping link addresses separately 0ms
   ✓ workshop wrap-up document > uses the stable workshop slug in the PDF file name 0ms
 ❯ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx (2 tests | 1 failed) 156ms
   ✓ workshop wrap-up PDF download > creates and downloads a PDF from the workshop and the materials already visible to this attendee 125ms
   × workshop wrap-up PDF download > keeps the button usable and explains a failed PDF render 30ms
     → Found multiple elements with the role "button" and name "Stáhnout shrnutí workshopu (PDF)"

Here are the matching elements:

Ignored nodes: comments, script, style
[36m<button[39m
  [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
  [33mtype[39m=[32m"button"[39m
[36m>[39m
  [36m<svg[39m
    [33maria-hidden[39m=[32m"true"[39m
    [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
    [33mfill[39m=[32m"none"[39m
    [33mheight[39m=[32m"24"[39m
    [33mstroke[39m=[32m"currentColor"[39m
    [33mstroke-linecap[39m=[32m"round"[39m
    [33mstroke-linejoin[39m=[32m"round"[39m
    [33mstroke-width[39m=[32m"2"[39m
    [33mviewBox[39m=[32m"0 0 24 24"[39m
    [33mwidth[39m=[32m"24"[39m
    [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
  [36m>[39m
    [36m<path[39m
      [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
    [36m/>[39m
    [36m<polyline[39m
      [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
    [36m/>[39m
    [36m<line[39m
      [33mx1[39m=[32m"12"[39m
      [33mx2[39m=[32m"12"[39m
      [33my1[39m=[32m"15"[39m
      [33my2[39m=[32m"3"[39m
    [36m/>[39m
  [36m</svg>[39m
  [0mStáhnout shrnutí workshopu (PDF)[0m
[36m</button>[39m

Ignored nodes: comments, script, style
[36m<button[39m
  [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
  [33mtype[39m=[32m"button"[39m
[36m>[39m
  [36m<svg[39m
    [33maria-hidden[39m=[32m"true"[39m
    [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
    [33mfill[39m=[32m"none"[39m
    [33mheight[39m=[32m"24"[39m
    [33mstroke[39m=[32m"currentColor"[39m
    [33mstroke-linecap[39m=[32m"round"[39m
    [33mstroke-linejoin[39m=[32m"round"[39m
    [33mstroke-width[39m=[32m"2"[39m
    [33mviewBox[39m=[32m"0 0 24 24"[39m
    [33mwidth[39m=[32m"24"[39m
    [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
  [36m>[39m
    [36m<path[39m
      [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
    [36m/>[39m
    [36m<polyline[39m
      [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
    [36m/>[39m
    [36m<line[39m
      [33mx1[39m=[32m"12"[39m
      [33mx2[39m=[32m"12"[39m
      [33my1[39m=[32m"15"[39m
      [33my2[39m=[32m"3"[39m
    [36m/>[39m
  [36m</svg>[39m
  [0mStáhnout shrnutí workshopu (PDF)[0m
[36m</button>[39m

(If this is intentional, then use the `*AllBy*` variant of the query (like `queryAllByText`, `getAllByText`, or `findAllByText`)).

Ignored nodes: comments, script, style
[36m<body>[39m
  [36m<div>[39m
    [36m<div[39m
      [33mclass[39m=[32m"mt-5"[39m
    [36m>[39m
      [36m<button[39m
        [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
        [33mtype[39m=[32m"button"[39m
      [36m>[39m
        [36m<svg[39m
          [33maria-hidden[39m=[32m"true"[39m
          [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
          [33mfill[39m=[32m"none"[39m
          [33mheight[39m=[32m"24"[39m
          [33mstroke[39m=[32m"currentColor"[39m
          [33mstroke-linecap[39m=[32m"round"[39m
          [33mstroke-linejoin[39m=[32m"round"[39m
          [33mstroke-width[39m=[32m"2"[39m
          [33mviewBox[39m=[32m"0 0 24 24"[39m
          [33mwidth[39m=[32m"24"[39m
          [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
        [36m>[39m
          [36m<path[39m
            [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
          [36m/>[39m
          [36m<polyline[39m
            [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
          [36m/>[39m
          [36m<line[39m
            [33mx1[39m=[32m"12"[39m
            [33mx2[39m=[32m"12"[39m
            [33my1[39m=[32m"15"[39m
            [33my2[39m=[32m"3"[39m
          [36m/>[39m
        [36m</svg>[39m
        [0mStáhnout shrnutí workshopu (PDF)[0m
      [36m</button>[39m
    [36m</div>[39m
  [36m</div>[39m
  [36m<div>[39m
    [36m<div[39m
      [33mclass[39m=[32m"mt-5"[39m
    [36m>[39m
      [36m<button[39m
        [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
        [33mtype[39m=[32m"button"[39m
      [36m>[39m
        [36m<svg[39m
          [33maria-hidden[39m=[32m"true"[39m
          [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
          [33mfill[39m=[32m"none"[39m
          [33mheight[39m=[32m"24"[39m
          [33mstroke[39m=[32m"currentColor"[39m
          [33mstroke-linecap[39m=[32m"round"[39m
          [33mstroke-linejoin[39m=[32m"round"[39m
          [33mstroke-width[39m=[32m"2"[39m
          [33mviewBox[39m=[32m"0 0 24 24"[39m
          [33mwidth[39m=[32m"24"[39m
          [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
        [36m>[39m
          [36m<path[39m
            [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
          [36m/>[39m
          [36m<polyline[39m
            [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
          [36m/>[39m
          [36m<line[39m
            [33mx1[39m=[32m"12"[39m
            [33mx2[39m=[32m"12"[39m
            [33my1[39m=[32m"15"[39m
            [33my2[39m=[32m"3"[39m
          [36m/>[39m
        [36m</svg>[39m
        [0mStáhnout shrnutí workshopu (PDF)[0m
      [36m</button>[39m
    [36m</div>[39m
  [36m</div>[39m
[36m</body>[39m
 ✓ businesses/online-workshop/participant/WorkshopStage.test.tsx (14 tests) 444ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 3 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  lib/exports/pdfPictures.test.ts > picture PDF export > keeps every supplied picture on its own PDF page
AssertionError: expected '%PDF-1.4\n1 0 obj\n<< /Type /Catalog …' to contain '/Title <FEFF005300680072006E007500740…'

- Expected
+ Received

- /Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>
+ %PDF-1.4
+ 1 0 obj
+ << /Type /Catalog /Pages 2 0 R >>
+ endobj
+ 2 0 obj
+ << /Type /Pages /Kids [3 0 R 6 0 R] /Count 2 >>
+ endobj
+ 3 0 obj
+ << /Type /Page /Parent 2 0 R /MediaBox [0 0 595 842] /Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>
+ endobj
+ 4 0 obj
+ << /Length 43 >>
+ stream
+ q
+ 595.00 0 0 841.64 0.00 0.18 cm
+ /Im0 Do
+ Q
+ endstream
+ endobj
+ 5 0 obj
+ << /Type /XObject /Subtype /Image /Width 1240 /Height 1754 /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length 12 >>
+ stream
+ ÿØÿà JFIFÿÙ
+ endstream
+ endobj
+ 6 0 obj
+ << /Type /Page /Parent 2 0 R /MediaBox [0 0 595 842] /Resources << /XObject << /Im0 8 0 R >> >> /Contents 7 0 R >>
+ endobj
+ 7 0 obj
+ << /Length 43 >>
+ stream
+ q
+ 595.00 0 0 841.64 0.00 0.18 cm
+ /Im0 Do
+ Q
+ endstream
+ endobj
+ 8 0 obj
+ << /Type /XObject /Subtype /Image /Width 1240 /Height 1754 /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length 12 >>
+ stream
+ ÿØÿà JFIFÿÙ
+ endstream
+ endobj
+ 9 0 obj
+ << /Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075> >>
+ endobj
+ xref
+ 0 10
+ 0000000000 65535 f 
+ 0000000009 00000 n 
+ 0000000058 00000 n 
+ 0000000121 00000 n 
+ 0000000251 00000 n 
+ 0000000343 00000 n 
+ 0000000523 00000 n 
+ 0000000653 00000 n 
+ 0000000745 00000 n 
+ 0000000925 00000 n 
+ trailer
+ << /Size 10 /Root 1 0 R /Info 9 0 R >>
+ startxref
+ 1028
+ %%EOF
+

 ❯ lib/exports/pdfPictures.test.ts:29:25
     27|         expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
     28|         expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLeng…
     29|         expect(pdfText).toContain('/Title <FEFF005300680072006E0075007…
       |                         ^
     30|     });
     31| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/3]⎯

 FAIL  lib/workshops/workshopWrapUpDocument.test.ts > workshop wrap-up document > uses the selected workshop and the same readable materials as the participant room
AssertionError: expected [ 'Postup pro nasazení', …(5) ] to deeply equal [ 'Postup pro nasazení', …(3) ]

- Expected
+ Received

  [
    "Postup pro nasazení",
    "Nejdůležitější kroky",
    "Napište ověřitelný plán.",
    "Nechte agenta spustit testy.",
+   "Praktický online workshop o psaní produkčního kódu s AI agenty.",
+   "Produkční kód s AI agenty",
  ]

 ❯ lib/workshops/workshopWrapUpDocument.test.ts:47:39
     45|         expect(document.dateAndTimeLabel).toContain('19:00–20:30');
     46|         expect(document.summary).toBe(WORKSHOP.description);
     47|         expect(document.keyTakeaways).toEqual([
       |                                       ^
     48|             'Postup pro nasazení',
     49|             'Nejdůležitější kroky',

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯

 FAIL  businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx > workshop wrap-up PDF download > keeps the button usable and explains a failed PDF render
TestingLibraryElementError: Found multiple elements with the role "button" and name "Stáhnout shrnutí workshopu (PDF)"

Here are the matching elements:

Ignored nodes: comments, script, style
[36m<button[39m
  [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
  [33mtype[39m=[32m"button"[39m
[36m>[39m
  [36m<svg[39m
    [33maria-hidden[39m=[32m"true"[39m
    [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
    [33mfill[39m=[32m"none"[39m
    [33mheight[39m=[32m"24"[39m
    [33mstroke[39m=[32m"currentColor"[39m
    [33mstroke-linecap[39m=[32m"round"[39m
    [33mstroke-linejoin[39m=[32m"round"[39m
    [33mstroke-width[39m=[32m"2"[39m
    [33mviewBox[39m=[32m"0 0 24 24"[39m
    [33mwidth[39m=[32m"24"[39m
    [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
  [36m>[39m
    [36m<path[39m
      [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
    [36m/>[39m
    [36m<polyline[39m
      [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
    [36m/>[39m
    [36m<line[39m
      [33mx1[39m=[32m"12"[39m
      [33mx2[39m=[32m"12"[39m
      [33my1[39m=[32m"15"[39m
      [33my2[39m=[32m"3"[39m
    [36m/>[39m
  [36m</svg>[39m
  [0mStáhnout shrnutí workshopu (PDF)[0m
[36m</button>[39m

Ignored nodes: comments, script, style
[36m<button[39m
  [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
  [33mtype[39m=[32m"button"[39m
[36m>[39m
  [36m<svg[39m
    [33maria-hidden[39m=[32m"true"[39m
    [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
    [33mfill[39m=[32m"none"[39m
    [33mheight[39m=[32m"24"[39m
    [33mstroke[39m=[32m"currentColor"[39m
    [33mstroke-linecap[39m=[32m"round"[39m
    [33mstroke-linejoin[39m=[32m"round"[39m
    [33mstroke-width[39m=[32m"2"[39m
    [33mviewBox[39m=[32m"0 0 24 24"[39m
    [33mwidth[39m=[32m"24"[39m
    [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
  [36m>[39m
    [36m<path[39m
      [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
    [36m/>[39m
    [36m<polyline[39m
      [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
    [36m/>[39m
    [36m<line[39m
      [33mx1[39m=[32m"12"[39m
      [33mx2[39m=[32m"12"[39m
      [33my1[39m=[32m"15"[39m
      [33my2[39m=[32m"3"[39m
    [36m/>[39m
  [36m</svg>[39m
  [0mStáhnout shrnutí workshopu (PDF)[0m
[36m</button>[39m

(If this is intentional, then use the `*AllBy*` variant of the query (like `queryAllByText`, `getAllByText`, or `findAllByText`)).

Ignored nodes: comments, script, style
[36m<body>[39m
  [36m<div>[39m
    [36m<div[39m
      [33mclass[39m=[32m"mt-5"[39m
    [36m>[39m
      [36m<button[39m
        [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
        [33mtype[39m=[32m"button"[39m
      [36m>[39m
        [36m<svg[39m
          [33maria-hidden[39m=[32m"true"[39m
          [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
          [33mfill[39m=[32m"none"[39m
          [33mheight[39m=[32m"24"[39m
          [33mstroke[39m=[32m"currentColor"[39m
          [33mstroke-linecap[39m=[32m"round"[39m
          [33mstroke-linejoin[39m=[32m"round"[39m
          [33mstroke-width[39m=[32m"2"[39m
          [33mviewBox[39m=[32m"0 0 24 24"[39m
          [33mwidth[39m=[32m"24"[39m
          [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
        [36m>[39m
          [36m<path[39m
            [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
          [36m/>[39m
          [36m<polyline[39m
            [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
          [36m/>[39m
          [36m<line[39m
            [33mx1[39m=[32m"12"[39m
            [33mx2[39m=[32m"12"[39m
            [33my1[39m=[32m"15"[39m
            [33my2[39m=[32m"3"[39m
          [36m/>[39m
        [36m</svg>[39m
        [0mStáhnout shrnutí workshopu (PDF)[0m
      [36m</button>[39m
    [36m</div>[39m
  [36m</div>[39m
  [36m<div>[39m
    [36m<div[39m
      [33mclass[39m=[32m"mt-5"[39m
    [36m>[39m
      [36m<button[39m
        [33mclass[39m=[32m"inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"[39m
        [33mtype[39m=[32m"button"[39m
      [36m>[39m
        [36m<svg[39m
          [33maria-hidden[39m=[32m"true"[39m
          [33mclass[39m=[32m"lucide lucide-download h-4 w-4"[39m
          [33mfill[39m=[32m"none"[39m
          [33mheight[39m=[32m"24"[39m
          [33mstroke[39m=[32m"currentColor"[39m
          [33mstroke-linecap[39m=[32m"round"[39m
          [33mstroke-linejoin[39m=[32m"round"[39m
          [33mstroke-width[39m=[32m"2"[39m
          [33mviewBox[39m=[32m"0 0 24 24"[39m
          [33mwidth[39m=[32m"24"[39m
          [33mxmlns[39m=[32m"http://www.w3.org/2000/svg"[39m
        [36m>[39m
          [36m<path[39m
            [33md[39m=[32m"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"[39m
          [36m/>[39m
          [36m<polyline[39m
            [33mpoints[39m=[32m"7 10 12 15 17 10"[39m
          [36m/>[39m
          [36m<line[39m
            [33mx1[39m=[32m"12"[39m
            [33mx2[39m=[32m"12"[39m
            [33my1[39m=[32m"15"[39m
            [33my2[39m=[32m"3"[39m
          [36m/>[39m
        [36m</svg>[39m
        [0mStáhnout shrnutí workshopu (PDF)[0m
      [36m</button>[39m
    [36m</div>[39m
  [36m</div>[39m
[36m</body>[39m
 ❯ Object.getElementError node_modules/@testing-library/dom/dist/config.js:37:19
 ❯ getElementError node_modules/@testing-library/dom/dist/query-helpers.js:20:35
 ❯ getMultipleElementsFoundError node_modules/@testing-library/dom/dist/query-helpers.js:23:10
 ❯ node_modules/@testing-library/dom/dist/query-helpers.js:55:13
 ❯ node_modules/@testing-library/dom/dist/query-helpers.js:95:19
 ❯ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx:84:39
     82|         render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentB…
     83| 
     84|         const downloadButton = screen.getByRole('button', { name: 'Stá…
       |                                       ^
     85|         fireEvent.click(downloadButton);
     86| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯


 Test Files  3 failed | 2 passed (5)
      Tests  3 failed | 23 passed (26)
   Start at  09:25:55
   Duration  5.67s (transform 1.22s, setup 0ms, collect 4.67s, tests 663ms, environment 3.35s, prepare 1.34s)


diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..3c21c621427ca0a8ecd94563db6c273ac8035563
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,90 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..4e46a41249a866bee5da3376b4d428d58aa19732
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F007000750>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0321a0efed4e25bd9ab345b3d608807e70090492
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const keyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+        workshop.description.trim(),
+        workshop.title.trim(),
+    ]);
+
+    return keyTakeaways;
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\exports\pdfPictures.test.ts
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopWrapUpDocument.ts
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx vitest run lib/exports/svgChartExport.test.ts lib/exports/pdfPictures.test.ts lib/workshops/workshopWrapUpDocument.test.ts businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx businesses/online-workshop/participant/WorkshopStage.test.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 7687ms:

 RUN  v3.2.4 C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron

 ✓ lib/exports/pdfPictures.test.ts (2 tests) 8ms
 ✓ lib/exports/svgChartExport.test.ts (4 tests) 10ms
 ✓ lib/workshops/workshopWrapUpDocument.test.ts (4 tests) 10ms
 ✓ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx (2 tests) 170ms
 ✓ businesses/online-workshop/participant/WorkshopStage.test.tsx (14 tests) 444ms

 Test Files  5 passed (5)
      Tests  26 passed (26)
   Start at  09:26:25
   Duration  5.51s (transform 1.49s, setup 0ms, collect 4.68s, tests 644ms, environment 3.07s, prepare 1.82s)


diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\AGENTS.md
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\changelog\_current-preversion.md
diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

codex
The focused tests now pass. The finished-only behavior is preserved by the existing recorded-end phase rule; the new button is rendered only inside that phase’s wrap-up, while its data factory deliberately sees only materials the current attendee can already read. I’ve documented that behavior in both the preversion changelog and the repository route notes.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --check; npm run build' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process node -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,Path,StartTime; if (Test-Path .next/BUILD_ID) { Get-Content .next/BUILD_ID }' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 473ms:

   Id ProcessName Path                                                 StartTime
   -- ----------- ----                                                 ---------
24584 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/15/2026 10:18:53 AM
27424 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/16/2026 9:27:00 AM
27448 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/16/2026 8:37:51 AM
28608 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/15/2026 10:18:54 AM
36852 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/16/2026 8:37:50 AM
43284 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/16/2026 9:14:21 AM
47752 node        C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 9/16/2026 9:27:00 AM


diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-CimInstance Win32_Process -Filter \"name = 'node.exe'\" | Select-Object ProcessId,CommandLine | Format-Table -Wrap" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 534ms:

ProcessId CommandLine
--------- -----------
    24584 C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
          C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npx-cli.js ptbk coder verify --order
          from-latest --ignore Refactor --commit --auto-pull --auto-push
    28608 "node"   "C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\.bin\\..\@promptbook
          \cli\bin\promptbook-cli.js" coder verify --order from-latest --ignore Refactor --commit --auto-pull
          --auto-push
    36852 C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
          C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npx-cli.js ptbk coder run --harness
          openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md
          --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push
    27448 "node"   "C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\.bin\\..\@promptbook
          \cli\bin\promptbook-cli.js" coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max
          --agent agents/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before
          yes-and-fix --auto-pull --auto-push
    43284 C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
          C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/@openai/codex/bin/codex.js -c
          forced_login_method=chatgpt -c model_reasoning_effort=max --ask-for-approval never exec --model
          gpt-5.6-terra --local-provider none --sandbox danger-full-access -C
          C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron --skip-git-repo-check
    27424 "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe"
          C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npm-cli.js run build
    47752 "node"
          "C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\.bin\\..\next\dist\bin\next"
          build


diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

codex
The production build is still running; the targeted tests and TypeScript check are already green. I’m waiting for the build result, then I’ll do a final diff and behavior review.
 exited 1 in 49610ms:

> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
Failed to compile.

node:dns/promises
Module build failed: UnhandledSchemeError: Reading from "node:dns/promises" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Hook.CALL_ASYNC_DELEGATE [as _callAsync] (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:6378)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494

Import trace for requested module:
node:dns/promises
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx

node:net
Module build failed: UnhandledSchemeError: Reading from "node:net" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494
    at NormalModule.needBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:414126)

Import trace for requested module:
node:net
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx


> Build failed because of webpack errors

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Wait-Process -Id 27424 -Timeout 45 -ErrorAction SilentlyContinue; Get-CimInstance Win32_Process -Filter "ProcessId = 27424" | Select-Object ProcessId,CommandLine | Format-Table -Wrap; if (Test-Path .next/BUILD_ID) { Get-Content .next/BUILD_ID }' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 630ms:
diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run build' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

 exited 1 in 36529ms:

> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
Failed to compile.

node:dns/promises
Module build failed: UnhandledSchemeError: Reading from "node:dns/promises" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Hook.CALL_ASYNC_DELEGATE [as _callAsync] (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:6378)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494

Import trace for requested module:
node:dns/promises
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx

node:net
Module build failed: UnhandledSchemeError: Reading from "node:net" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494
    at NormalModule.needBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:414160)

Import trace for requested module:
node:net
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx


> Build failed because of webpack errors

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

codex
The production build exposed a client/server boundary issue in my first pass: the PDF data helper imported the server-side material-link persistence module. I’m separating the already-existing pure Markdown URL parsing into a client-safe utility, so the PDF still reuses the same parsing rule without bundling database/network code into the participant room.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"from '@/lib/workshops/workshopMaterialLinks'|from \\\"@/lib/workshops/workshopMaterialLinks\\\"\" . --glob '"'!node_modules'"' --glob '"'!'"\\.next' --glob '"'!prompts/**'"'; rg -n \""'^export (async )?function|''^export type" lib/workshops/workshopMaterialLinks.ts; Get-Content lib/workshops/workshopMaterialLinks.ts | Select-Object -First 370' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 335ms:
ParserError: 
Line |
   2 |  rg -n "from '@/lib/workshops/workshopMaterialLinks'|from \"@/lib/work …
     |                                                             ~
     | Unrecognized token in source text.

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'workshopMaterialLinks' . --glob '"'!node_modules'"' --glob '"'!'"\\.next' --glob '"'!prompts/**'"'; rg -n '"'^export (async )?function|''^export type'"' lib/workshops/workshopMaterialLinks.ts; Get-Content lib/workshops/workshopMaterialLinks.ts | Select-Object -First 390" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 400ms:
.\app\api\workshops\[workshopSlug]\comments\route.ts:11:import { materializeWorkshopCommentShortLinks } from '@/lib/workshops/workshopMaterialLinks';
.\app\api\admin\workshops\[workshopId]\content\[contentId]\route.ts:12:import { ensureWorkshopMaterialShortLinks } from '@/lib/workshops/workshopMaterialLinks';
.\app\api\admin\workshops\[workshopId]\content\route.ts:12:import { ensureWorkshopMaterialShortLinks } from '@/lib/workshops/workshopMaterialLinks';
.\app\api\admin\workshops\[workshopId]\comments\route.ts:5:import { ensureWorkshopCommentShortLinks } from '@/lib/workshops/workshopMaterialLinks';
.\lib\workshops\workshopWrapUpDocument.ts:1:import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
.\lib\workshops\workshopMaterialLinks.test.ts:9:} from '@/lib/workshops/workshopMaterialLinks';
.\lib\workshops\workshopDatabase.ts:34:import { materializeWorkshopMaterialShortLinks } from '@/lib/workshops/workshopMaterialLinks';
.\lib\workshops\workshopDatabase.ts:35:import { materializeWorkshopCommentShortLinks } from '@/lib/workshops/workshopMaterialLinks';
53:export type WorkshopShortcodeLinkPresentation = {
347:export function createWorkshopShortcodeLinkTrackingUrl(
369:export function createWorkshopMaterialTrackingUrl(
382:export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
392:export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
447:export function replaceWorkshopShortcodeLinkDestinations(
471:export function replaceWorkshopMaterialLinkDestinations(
478:export function getWorkshopShortcodeLinkSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
482:export function getWorkshopMaterialShortcodeSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
676:export async function materializeWorkshopMaterialShortLinks(
698:export async function materializeWorkshopCommentShortLinks(
720:export async function ensureWorkshopMaterialShortLinks(
738:export async function ensureWorkshopCommentShortLinks(
import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';
const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
        }

        const destination = markdown.slice(destinationStart, destinationEnd);
        if (destination !== '') {
            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
        }
    }

    return ranges;
}

function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const openingAnchorPattern = /<a\b[^>]*>/gi;
    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;

    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
        const openingAnchor = openingAnchorMatch[0];
        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
        if (isInsideCode(markdown, openingAnchorIndex)) {
            continue;
        }

        const hrefMatch = hrefPattern.exec(openingAnchor);
        if (hrefMatch === null) {
            continue;
        }

        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
        if (!destination) {
            continue;
        }

        const hrefText = hrefMatch[0];
        const destinationIndexInHref = hrefText.lastIndexOf(destination);
        if (destinationIndexInHref === -1) {
            continue;
        }

        const hrefIndexInAnchor = hrefMatch.index;
        ranges.push({
            destination,
            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;

    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
        const destination = autolinkMatch[1];
        const matchIndex = autolinkMatch.index ?? 0;
        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
            ranges.push({
                destination,
                start: matchIndex,
                end: matchIndex + autolinkMatch[0].length,
                isTitleRequired: true,
            });
        }
    }

    return ranges;
}

/**
 * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
 * bare HTTP(S) URL too. Materialization must therefore match that rendered
 * anchor as well as explicit Markdown and HTML links.
 */
function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
        const leadingWhitespace = bareUrlMatch[1] ?? '';
        const rawDestination = bareUrlMatch[2];
        const matchIndex = bareUrlMatch.index ?? 0;
        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
        const start = matchIndex + leadingWhitespace.length;

        if (destination !== '' && !isInsideCode(markdown, start)) {
            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
        }
    }

    return ranges;
}

function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;

    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
        const destination = definitionMatch[1] ?? definitionMatch[2];
        if (!destination) {
            continue;
        }

        const matchIndex = definitionMatch.index ?? 0;
        if (isInsideCode(markdown, matchIndex)) {
            continue;
        }

        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
        ranges.push({
            destination,
            start: matchIndex + destinationIndexInDefinition,
            end: matchIndex + destinationIndexInDefinition + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content lib/workshops/workshopMaterialLinks.ts | Select-Object -Skip 365 -First 160; Get-Content lib/workshops/workshopMaterialLinks.test.ts | Select-Object -First 420' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 326ms:
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    try {
        return await fetchPublicWebPageTitle(trackableUrl);
    } catch {
        // A remote page may reject our bounded metadata request, but that must
        // never prevent a room from handing out its already-safe short link.
        return getWorkshopShortcodeLinkFallbackTitle(destinationUrl);
    }
}

/**
 * Replaces a source address with its persisted short URL. An authored Markdown,
 * HTML, or reference label remains its author's wording; raw URLs and
 * autolinks receive the fetched page title in ordinary Markdown link syntax.
 */
export function replaceWorkshopShortcodeLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    let replacedMarkdown = bodyMarkdown;
    const ranges = collectWorkshopMaterialLinkRanges(bodyMarkdown);

    for (const range of [...ranges].reverse()) {
        const shortcodeLink = shortcodeLinkByDestination.get(range.destination);
        if (shortcodeLink !== undefined) {
            const replacement =
                typeof shortcodeLink === 'string'
                    ? shortcodeLink
                    : range.isTitleRequired
                      ? createMarkdownShortcodeLink(shortcodeLink.title, shortcodeLink.shortUrl)
                      : shortcodeLink.shortUrl;
            replacedMarkdown =
                replacedMarkdown.slice(0, range.start) + replacement + replacedMarkdown.slice(range.end);
        }
    }

    return replacedMarkdown;
}

export function replaceWorkshopMaterialLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    return replaceWorkshopShortcodeLinkDestinations(bodyMarkdown, shortcodeLinkByDestination);
}

export function getWorkshopShortcodeLinkSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return workshopKind === 'workshop' ? 'online-workshop' : 'community';
}

export function getWorkshopMaterialShortcodeSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return getWorkshopShortcodeLinkSourceApp(workshopKind);
}

function getShortcodeLinkId(value: number | string): number | null {
    const shortcodeLinkId = Number(value);

    return Number.isSafeInteger(shortcodeLinkId) && shortcodeLinkId > 0 ? shortcodeLinkId : null;
}

async function loadWorkshopShortcodeLinks(
    supabase: SupabaseClient,
    linkOwner: WorkshopShortcodeLinkOwner,
): Promise<LoadedWorkshopShortcodeLinks> {
    const { data: mappingData, error: mappingError } = await supabase
        .from(linkOwner.mappingTableName)
        .select('destination_url, destination_title, shortcode_link_id')
        .eq(linkOwner.mappingOwnerColumnName, linkOwner.id);
    if (mappingError) {
        return { shortcodeLinkByDestination: null, errorMessage: mappingError.message };
    }

    const mappings = (mappingData ?? []) as WorkshopShortcodeLinkMappingRow[];
    const shortcodeLinkIds = Array.from(
        new Set(
            mappings
                .map((mapping) => getShortcodeLinkId(mapping.shortcode_link_id))
                .filter((shortcodeLinkId): shortcodeLinkId is number => shortcodeLinkId !== null),
        ),
    );
    if (shortcodeLinkIds.length === 0) {
        return { shortcodeLinkByDestination: new Map(), errorMessage: null };
    }

    const { data: shortcodeLinkData, error: shortcodeLinkError } = await supabase
        .from(SHORTCODE_LINK_TABLE_NAME)
        .select('id, shortcode')
        .in('id', shortcodeLinkIds);
    if (shortcodeLinkError) {
        return { shortcodeLinkByDestination: null, errorMessage: shortcodeLinkError.message };
    }

    const shortcodeById = new Map<number, string>(
        ((shortcodeLinkData ?? []) as ShortcodeLinkReferenceRow[])
import {
    createWorkshopMaterialTrackingUrl,
    getWorkshopMaterialLinkDestinations,
    getWorkshopMaterialShortcodeSourceApp,
    materializeWorkshopCommentShortLinks,
    materializeWorkshopMaterialShortLinks,
    replaceWorkshopMaterialLinkDestinations,
    type WorkshopShortcodeLinkPresentation,
} from '@/lib/workshops/workshopMaterialLinks';
import type { SupabaseClient } from '@supabase/supabase-js';
import { afterEach, describe, expect, it, vi } from 'vitest';

const { createAdHocShortcodeLinkMock, fetchPublicWebPageTitleMock } = vi.hoisted(() => ({
    createAdHocShortcodeLinkMock: vi.fn(),
    fetchPublicWebPageTitleMock: vi.fn(),
}));

vi.mock('@/lib/shortener/shortcodeLinkAdHoc', () => ({
    createAdHocShortcodeLink: createAdHocShortcodeLinkMock,
}));

vi.mock('@/lib/network/publicWebPagePreview', () => ({
    fetchPublicWebPageTitle: fetchPublicWebPageTitleMock,
}));

afterEach(() => {
    createAdHocShortcodeLinkMock.mockReset();
    fetchPublicWebPageTitleMock.mockReset();
});

describe('workshop material tracking links', () => {
    it('adds stable workshop UTM parameters without losing existing query parameters', () => {
        const trackingUrl = createWorkshopMaterialTrackingUrl(
            'https://example.com/material?download=1&utm_source=old-source',
            'online-workshop-2026-08-20',
            'content-123',
        );

        const parsedUrl = new URL(trackingUrl);
        expect(parsedUrl.searchParams.get('download')).toBe('1');
        expect(parsedUrl.searchParams.get('utm_source')).toBe('promptbook');
        expect(parsedUrl.searchParams.get('utm_medium')).toBe('workshop');
        expect(parsedUrl.searchParams.get('utm_campaign')).toBe('online-workshop-2026-08-20');
        expect(parsedUrl.searchParams.get('utm_content')).toBe('content-123');
    });

    it('does not rewrite in-page anchors or unsupported protocols', () => {
        expect(createWorkshopMaterialTrackingUrl('#slides', 'workshop', 'content')).toBe('#slides');
        expect(createWorkshopMaterialTrackingUrl('mailto:hello@example.com', 'workshop', 'content')).toBe(
            'mailto:hello@example.com',
        );
    });

    it('finds every ordinary material link while leaving images, e-mail links, and code samples alone', () => {
        const materialMarkdown = [
            '[Inline](https://example.com/inline)',
            '<https://example.com/autolink>',
            '<a href="https://example.com/html">HTML</a>',
            '[reference]: https://example.com/reference',
            'Bare URL https://example.com/bare.',
            '![Diagram](https://example.com/image.png)',
            '[Email](mailto:hello@example.com)',
            '`[Example](https://example.com/code)`',
        ].join('\n\n');

        expect(getWorkshopMaterialLinkDestinations(materialMarkdown)).toEqual([
            'https://example.com/inline',
            'https://example.com/autolink',
            'https://example.com/html',
            'https://example.com/reference',
            'https://example.com/bare',
        ]);
    });

    it('uses the fetched title in Markdown when a raw URL becomes a short link', () => {
        const materialMarkdown =
            '[Inline](https://example.com/inline) and <a href="https://example.com/html">HTML</a> with https://example.com/bare and ![image](https://example.com/image.png)';
        const materialWithShortLinks = replaceWorkshopMaterialLinkDestinations(
            materialMarkdown,
            new Map<string, string | WorkshopShortcodeLinkPresentation>([
                ['https://example.com/inline', 'https://ptbk.io/inline123'],
                ['https://example.com/html', 'https://ptbk.io/html123'],
                [
                    'https://example.com/bare',
                    { shortUrl: 'https://ptbk.io/bare123', title: 'Příručka [pro účastníky]' },
                ],
            ]),
        );

        expect(materialWithShortLinks).toBe(
            '[Inline](https://ptbk.io/inline123) and <a href="https://ptbk.io/html123">HTML</a> with [Příručka \\[pro účastníky\\]](https://ptbk.io/bare123) and ![image](https://example.com/image.png)',
        );
    });

    it('turns a Markdown autolink into one title-backed short link', () => {
        expect(
            replaceWorkshopMaterialLinkDestinations(
                '<https://example.com/autolink>',
                new Map<string, WorkshopShortcodeLinkPresentation>([
                    [
                        'https://example.com/autolink',
                        { shortUrl: 'https://ptbk.io/autolink123', title: 'Veřejná dokumentace' },
                    ],
                ]),
            ),
        ).toBe('[Veřejná dokumentace](https://ptbk.io/autolink123)');
    });

    it('labels automatic material links by the app which created them', () => {
        expect(getWorkshopMaterialShortcodeSourceApp('workshop')).toBe('online-workshop');
        expect(getWorkshopMaterialShortcodeSourceApp('community')).toBe('community');
        expect(getWorkshopMaterialShortcodeSourceApp('project')).toBe('community');
    });

    it('creates and returns an ad hoc short link instead of exposing a material destination', async () => {
        let mappings: readonly { readonly destination_url: string; readonly shortcode_link_id: number }[] = [];
        const mappingUpsert = vi.fn(async (values: {
            readonly destination_url: string;
            readonly shortcode_link_id: number;
        }) => {
            mappings = [
                {
                    destination_url: values.destination_url,
                    shortcode_link_id: values.shortcode_link_id,
                },
            ];
            return { error: null };
        });
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_content_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    upsert: mappingUpsert,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 44, shortcode: 'material-44' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        createAdHocShortcodeLinkMock.mockResolvedValue({
            shortcodeLink: {
                id: 44,
                createdAt: '2026-08-24T10:00:00.000Z',
                shortcode: 'material-44',
                urls: ['https://example.com/material'],
                note: null,
                landingPage: null,
                isAdHoc: true,
                sourceApp: 'online-workshop',
            },
            errorMessage: null,
        });

        const materializedLink = await materializeWorkshopMaterialShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'production-ai-2026-08-24',
                workshopKind: 'workshop',
                contentBlockId: 'content-44',
                bodyMarkdown: '[Otevřít materiál](https://example.com/material?download=1)',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: '[Otevřít materiál](https://ptbk.io/material-44)',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).toHaveBeenCalledWith(expect.anything(), {
            urls: [
                'https://example.com/material?download=1&utm_source=promptbook&utm_medium=workshop&utm_campaign=production-ai-2026-08-24&utm_content=content-44',
            ],
            note: 'Ad hoc material link for production-ai-2026-08-24',
            sourceApp: 'online-workshop',
        });
        expect(mappingUpsert).toHaveBeenCalledWith(
            {
                content_block_id: 'content-44',
                destination_url: 'https://example.com/material?download=1',
                shortcode_link_id: 44,
            },
            { onConflict: 'content_block_id,destination_url', ignoreDuplicates: true },
        );
    });

    it('reuses the persisted material short-link path for an artificial or moderator chat message', async () => {
        let mappings: readonly {
            readonly destination_url: string;
            readonly destination_title?: string;
            readonly shortcode_link_id: number;
        }[] = [];
        const mappingUpsert = vi.fn(async (values: {
            readonly destination_url: string;
            readonly destination_title?: string;
            readonly shortcode_link_id: number;
        }) => {
            mappings = [
                {
                    destination_url: values.destination_url,
                    ...(values.destination_title === undefined ? {} : { destination_title: values.destination_title }),
                    shortcode_link_id: values.shortcode_link_id,
                },
            ];
            return { error: null };
        });
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_comment_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    upsert: mappingUpsert,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 45, shortcode: 'comment-45' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        createAdHocShortcodeLinkMock.mockResolvedValue({
            shortcodeLink: {
                id: 45,
                createdAt: '2026-08-25T10:00:00.000Z',
                shortcode: 'comment-45',
                urls: ['https://example.com/guide'],
                note: null,
                landingPage: null,
                isAdHoc: true,
                sourceApp: 'online-workshop',
            },
            errorMessage: null,
        });
        fetchPublicWebPageTitleMock.mockResolvedValue('Průvodce AI agenty');

        const materializedLink = await materializeWorkshopCommentShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'production-ai-2026-08-25',
                workshopKind: 'workshop',
                commentId: 'comment-45',
                bodyMarkdown: 'Podívejte se na https://example.com/guide.',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: 'Podívejte se na [Průvodce AI agenty](https://ptbk.io/comment-45).',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).toHaveBeenCalledWith(expect.anything(), {
            urls: [
                'https://example.com/guide?utm_source=promptbook&utm_medium=workshop&utm_campaign=production-ai-2026-08-25&utm_content=comment-45',
            ],
            note: 'Ad hoc chat link for production-ai-2026-08-25',
            sourceApp: 'online-workshop',
        });
        expect(mappingUpsert).toHaveBeenCalledWith(
            {
                comment_id: 'comment-45',
                destination_url: 'https://example.com/guide',
                destination_title: 'Průvodce AI agenty',
                shortcode_link_id: 45,
            },
            { onConflict: 'comment_id,destination_url', ignoreDuplicates: true },
        );
        expect(fetchPublicWebPageTitleMock).toHaveBeenCalledWith('https://example.com/guide');
    });

    it('backfills a title for an existing community short link without making a second short link', async () => {
        let mappings: readonly {
            readonly destination_url: string;
            readonly destination_title: string | null;
            readonly shortcode_link_id: number;
        }[] = [
            {
                destination_url: 'https://example.com/community-guide',
                destination_title: null,
                shortcode_link_id: 46,
            },
        ];
        const updateDestination = vi.fn(async (_columnName: string, destination: string) => {
            mappings = mappings.map((mapping) =>
                mapping.destination_url === destination ? { ...mapping, destination_title: 'Průvodce komunitou' } : mapping,
            );
            return { error: null };
        });
        const updateOwner = vi.fn(() => ({ eq: updateDestination }));
        const mappingUpdate = vi.fn(() => ({ eq: updateOwner }));
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_content_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    update: mappingUpdate,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 46, shortcode: 'community-46' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        fetchPublicWebPageTitleMock.mockResolvedValue('Průvodce komunitou');

        const materializedLink = await materializeWorkshopMaterialShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'komunita',
                workshopKind: 'community',
                contentBlockId: 'content-46',
                bodyMarkdown: 'Začněte zde: https://example.com/community-guide',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: 'Začněte zde: [Průvodce komunitou](https://ptbk.io/community-46)',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).not.toHaveBeenCalled();
        expect(mappingUpdate).toHaveBeenCalledWith({ destination_title: 'Průvodce komunitou' });
        expect(updateOwner).toHaveBeenCalledWith('content_block_id', 'content-46');
        expect(updateDestination).toHaveBeenCalledWith('destination_url', 'https://example.com/community-guide');
        expect(fetchPublicWebPageTitleMock).toHaveBeenCalledWith('https://example.com/community-guide');
    });
});

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopMaterialLinkSyntax.ts
diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopMaterialLinkSyntax.ts b/lib/workshops/workshopMaterialLinkSyntax.ts
new file mode 100644
index 0000000000000000000000000000000000000000..18d8d419646119d4b6c44fd89e23c28c9baadd0b
--- /dev/null
+++ b/lib/workshops/workshopMaterialLinkSyntax.ts
@@ -0,0 +1,317 @@
+/**
+ * The client-safe Markdown link syntax shared by the material renderer, public room exports, and server-side short
+ * link materialization. It intentionally has no network or database imports.
+ */
+
+const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
+const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
+const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
+const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
+const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
+
+export type WorkshopMaterialLinkRange = {
+    readonly destination: string;
+    readonly start: number;
+    readonly end: number;
+    readonly isTitleRequired: boolean;
+};
+
+function getWorkshopMaterialLinkBaseUrl(): string {
+    return WORKSHOP_MATERIAL_LINK_BASE_URL;
+}
+
+function isEscaped(text: string, index: number): boolean {
+    let precedingBackslashCount = 0;
+
+    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
+        precedingBackslashCount++;
+    }
+
+    return precedingBackslashCount % 2 === 1;
+}
+
+function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
+    const precedingText = markdown.slice(0, position);
+    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
+
+    return fenceCount % 2 === 1;
+}
+
+function isWithinInlineCode(markdown: string, position: number): boolean {
+    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
+    const precedingLineText = markdown.slice(lineStart, position);
+    let unescapedBacktickCount = 0;
+
+    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
+        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
+            unescapedBacktickCount++;
+        }
+    }
+
+    return unescapedBacktickCount % 2 === 1;
+}
+
+function isInsideCode(markdown: string, position: number): boolean {
+    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
+}
+
+function findUnescapedCharacter(text: string, character: string, start: number): number {
+    for (let cursor = start; cursor < text.length; cursor++) {
+        if (text[cursor] === character && !isEscaped(text, cursor)) {
+            return cursor;
+        }
+    }
+
+    return -1;
+}
+
+function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+
+    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
+        if (
+            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
+            isEscaped(markdown, openingBracket) ||
+            isInsideCode(markdown, openingBracket)
+        ) {
+            continue;
+        }
+
+        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
+        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
+            continue;
+        }
+
+        let destinationStart = closingBracket + 2;
+        while (/\s/.test(markdown[destinationStart] ?? '')) {
+            destinationStart++;
+        }
+
+        if (markdown[destinationStart] === '<') {
+            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
+            if (closingAngleBracket === -1) {
+                continue;
+            }
+
+            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
+            if (destination !== '') {
+                ranges.push({
+                    destination,
+                    start: destinationStart + 1,
+                    end: closingAngleBracket,
+                    isTitleRequired: false,
+                });
+            }
+            continue;
+        }
+
+        let parenthesisDepth = 0;
+        let destinationEnd = destinationStart;
+        for (; destinationEnd < markdown.length; destinationEnd++) {
+            const character = markdown[destinationEnd];
+            if (isEscaped(markdown, destinationEnd)) {
+                continue;
+            }
+            if (character === '(') {
+                parenthesisDepth++;
+                continue;
+            }
+            if (character === ')') {
+                if (parenthesisDepth === 0) {
+                    break;
+                }
+                parenthesisDepth--;
+                continue;
+            }
+            if (parenthesisDepth === 0 && /\s/.test(character)) {
+                break;
+            }
+        }
+
+        const destination = markdown.slice(destinationStart, destinationEnd);
+        if (destination !== '') {
+            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
+        }
+    }
+
+    return ranges;
+}
+
+function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const openingAnchorPattern = /<a\b[^>]*>/gi;
+    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
+
+    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
+        const openingAnchor = openingAnchorMatch[0];
+        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
+        if (isInsideCode(markdown, openingAnchorIndex)) {
+            continue;
+        }
+
+        const hrefMatch = hrefPattern.exec(openingAnchor);
+        if (hrefMatch === null) {
+            continue;
+        }
+
+        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
+        if (!destination) {
+            continue;
+        }
+
+        const hrefText = hrefMatch[0];
+        const destinationIndexInHref = hrefText.lastIndexOf(destination);
+        if (destinationIndexInHref === -1) {
+            continue;
+        }
+
+        const hrefIndexInAnchor = hrefMatch.index;
+        ranges.push({
+            destination,
+            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
+            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
+            isTitleRequired: false,
+        });
+    }
+
+    return ranges;
+}
+
+function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
+
+    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
+        const destination = autolinkMatch[1];
+        const matchIndex = autolinkMatch.index ?? 0;
+        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
+            ranges.push({
+                destination,
+                start: matchIndex,
+                end: matchIndex + autolinkMatch[0].length,
+                isTitleRequired: true,
+            });
+        }
+    }
+
+    return ranges;
+}
+
+/**
+ * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a bare HTTP(S) URL too. Link parsing must
+ * therefore match that rendered anchor as well as explicit Markdown and HTML links.
+ */
+function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+
+    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
+        const leadingWhitespace = bareUrlMatch[1] ?? '';
+        const rawDestination = bareUrlMatch[2];
+        const matchIndex = bareUrlMatch.index ?? 0;
+        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
+        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
+        const start = matchIndex + leadingWhitespace.length;
+
+        if (destination !== '' && !isInsideCode(markdown, start)) {
+            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
+        }
+    }
+
+    return ranges;
+}
+
+function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
+
+    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
+        const destination = definitionMatch[1] ?? definitionMatch[2];
+        if (!destination) {
+            continue;
+        }
+
+        const matchIndex = definitionMatch.index ?? 0;
+        if (isInsideCode(markdown, matchIndex)) {
+            continue;
+        }
+
+        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
+        ranges.push({
+            destination,
+            start: matchIndex + destinationIndexInDefinition,
+            end: matchIndex + destinationIndexInDefinition + destination.length,
+            isTitleRequired: false,
+        });
+    }
+
+    return ranges;
+}
+
+export function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const sortedRanges = [
+        ...collectMarkdownInlineLinkRanges(markdown),
+        ...collectHtmlLinkRanges(markdown),
+        ...collectAutolinkRanges(markdown),
+        ...collectReferenceDefinitionRanges(markdown),
+        ...collectBareUrlRanges(markdown),
+    ].sort(
+        (firstRange, secondRange) =>
+            firstRange.start - secondRange.start ||
+            firstRange.end - secondRange.end ||
+            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
+    );
+
+    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
+    for (const range of sortedRanges) {
+        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
+        if (precedingRange === undefined || range.start >= precedingRange.end) {
+            nonOverlappingRanges.push(range);
+        }
+    }
+
+    return nonOverlappingRanges;
+}
+
+export function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
+    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
+        return null;
+    }
+
+    try {
+        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
+
+        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
+    } catch {
+        return null;
+    }
+}
+
+/**
+ * Lists the HTTP(S) destinations written in a material, regardless of whether they use inline Markdown, HTML,
+ * autolinks, or reference definitions. Images, anchors, e-mail links, and code samples are deliberately not click
+ * links.
+ */
+export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
+    return Array.from(
+        new Set(
+            collectWorkshopMaterialLinkRanges(bodyMarkdown)
+                .map((range) => range.destination)
+                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
+        ),
+    );
+}
+
+export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
+    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
+}
+
+export function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
+    return Array.from(
+        new Set(
+            collectWorkshopMaterialLinkRanges(bodyMarkdown)
+                .filter((range) => range.isTitleRequired)
+                .map((range) => range.destination)
+                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
+        ),
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}

diff --git a/AGENTS.md b/AGENTS.md
index 82dbe5f40d85e1c96e75df832910606fd9773760..f3da6d5646a350c7984fd418436e5456fdfc2f28
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -23,7 +23,10 @@
   offers every published term as the same term cards the landing page registers
   with: running and upcoming ones first, finished ones behind a disclosure.
   Picking one changes the room being connected to and the `workshop` parameter,
-  without losing the name and e-mail already typed.
+  without losing the name and e-mail already typed. Once a term has its recorded
+  end, its participant wrap-up offers a locally generated PDF using only the
+  attendee-visible description, key takeaways and shared materials; it creates
+  no administrative setting or database record.
 - `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
   merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
   the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e0170541c8a7c189c696154e13b925d32f83f34e..3df357167cb1b216b8349686fcd61b12988e8e65
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 15321f44618338b60737351db6376c2f29d948a1..5feaafd53adecc605329755a16d19b4231797be5
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -371,6 +371,7 @@
         expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
             '#workshop-material-follow-up-material',
         );
+        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
         expect(reactionSource.listenerCount()).toBe(1);
     });
 
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83dca54c5b23944010cfe9525cee7ea2a66..eca196884fe222704d9d6c85c77ead7d01359edb
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938feef40ead9c44e3fa838e5e725233b6893..dec83630084e63d372c88fa0b3e8df4e24cb4b3b
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..499dfc98dcb756f9f7091b62d715bf71ba2a8a32
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
@@ -0,0 +1,94 @@
+/**
+ * @vitest-environment jsdom
+ */
+
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
+import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
+
+const pdfExportMocks = vi.hoisted(() => ({
+    createWorkshopWrapUpPdfBlob: vi.fn(),
+    downloadBlobFile: vi.fn(),
+}));
+
+vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
+    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
+}));
+
+vi.mock('@/lib/downloadBlobFile', () => ({
+    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
+}));
+
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický workshop s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: null,
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Podklady z workshopu',
+    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: false,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+beforeEach(() => {
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
+    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
+    pdfExportMocks.downloadBlobFile.mockReset();
+});
+
+afterEach(() => {
+    cleanup();
+});
+
+describe('workshop wrap-up PDF download', () => {
+    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
+
+        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
+
+        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
+        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
+            expect.objectContaining({
+                title: WORKSHOP.title,
+                summary: WORKSHOP.description,
+                materials: [
+                    expect.objectContaining({
+                        title: CONTENT_BLOCK.title,
+                        urls: ['https://ptbk.io/material-123'],
+                    }),
+                ],
+            }),
+        );
+        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
+            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
+        );
+    });
+
+    it('keeps the button usable and explains a failed PDF render', async () => {
+        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
+        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
+
+        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
+        fireEvent.click(downloadButton);
+
+        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
+        expect(downloadButton.hasAttribute('disabled')).toBe(false);
+    });
+});
diff --git a/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..46a4c3fa959d7c113b6c80ac5970dad8b9383d0c
--- /dev/null
+++ b/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
@@ -0,0 +1,71 @@
+'use client';
+
+import { downloadBlobFile } from '@/lib/downloadBlobFile';
+import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { Download, LoaderCircle } from 'lucide-react';
+import { useState } from 'react';
+
+type WorkshopWrapUpPdfDownloadProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+
+    /** The same participant-visible materials rendered in the room below the stage. */
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+/**
+ * Lets an attendee take the information already visible in a completed room away as one printable document.
+ *
+ * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
+ * helper starts the file download, so this button cannot introduce a second material source or download path.
+ */
+export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
+    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
+    const [errorMessage, setErrorMessage] = useState<string | null>(null);
+
+    const downloadWorkshopWrapUp = async () => {
+        setIsDownloadInProgress(true);
+        setErrorMessage(null);
+
+        try {
+            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
+            const blob = await createWorkshopWrapUpPdfBlob(document);
+
+            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
+        } catch {
+            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
+        } finally {
+            setIsDownloadInProgress(false);
+        }
+    };
+
+    return (
+        <div className="mt-5">
+            <button
+                type="button"
+                onClick={() => void downloadWorkshopWrapUp()}
+                disabled={isDownloadInProgress}
+                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
+            >
+                {isDownloadInProgress ? (
+                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
+                ) : (
+                    <Download className="h-4 w-4" aria-hidden="true" />
+                )}
+                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
+            </button>
+            {errorMessage !== null && (
+                <p role="alert" className="mt-2 text-sm text-rose-200">
+                    {errorMessage}
+                </p>
+            )}
+        </div>
+    );
+}
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf98d2cd2925f23914f9a8ead2213d4754f..98810726d3a5421be04fc190d70df87b36c0c928
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
diff --git a/lib/exports/pdfPictures.test.ts b/lib/exports/pdfPictures.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f1cf8cb5c923461ed7b9b000574c756f41bf8012
--- /dev/null
+++ b/lib/exports/pdfPictures.test.ts
@@ -0,0 +1,35 @@
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+import { describe, expect, it } from 'vitest';
+
+const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
+const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };
+
+async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
+    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
+        .map((byte) => String.fromCharCode(byte))
+        .join('');
+}
+
+describe('picture PDF export', () => {
+    it('keeps every supplied picture on its own PDF page', async () => {
+        const pdfText = await readPdfAsLatin1Text(
+            createPicturePdf(
+                [
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
+                ],
+                PDF_PAGE_SIZE,
+                'Shrnutí workshopu',
+            ),
+        );
+
+        expect(pdfText).toContain('/Count 2');
+        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
+        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
+        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
+    });
+
+    it('rejects a document without a page instead of writing an invalid file', () => {
+        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
+    });
+});
diff --git a/lib/exports/pdfPictures.ts b/lib/exports/pdfPictures.ts
new file mode 100644
index 0000000000000000000000000000000000000000..16dd80ef9853e176efbb618f4b6b67a3fa0045b3
--- /dev/null
+++ b/lib/exports/pdfPictures.ts
@@ -0,0 +1,185 @@
+/**
+ * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
+ *
+ * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
+ * pixels without embedding a second font or relying on a reader's local font collection.
+ */
+
+export type PdfPicture = {
+    readonly bytes: Uint8Array;
+    readonly width: number;
+    readonly height: number;
+};
+
+export type PdfPageSize = {
+    readonly width: number;
+    readonly height: number;
+    readonly margin: number;
+};
+
+/**
+ * Where one part of the written document begins, counted in bytes from its very beginning
+ */
+function getByteLength(chunks: readonly Uint8Array[]): number {
+    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
+}
+
+function encodeAsciiText(text: string): Uint8Array {
+    return new TextEncoder().encode(text);
+}
+
+function formatCrossReferenceEntry(objectOffset: number): string {
+    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
+}
+
+/**
+ * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
+ *
+ * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
+ * which makes it read the same in every PDF reader without making the document embed a font.
+ */
+function formatPdfTextString(text: string): string {
+    const characterCodes = ['FEFF'];
+
+    for (const character of text) {
+        const codePoint = character.codePointAt(0) ?? 0;
+
+        if (codePoint > 0xffff) {
+            const surrogateOffset = codePoint - 0x10000;
+            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
+            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
+        } else {
+            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
+        }
+    }
+
+    return `<${characterCodes.join('').toUpperCase()}>`;
+}
+
+function getPdfPicturePlacement(
+    picture: Pick<PdfPicture, 'width' | 'height'>,
+    pageSize: PdfPageSize,
+): {
+    readonly width: number;
+    readonly height: number;
+    readonly offsetX: number;
+    readonly offsetY: number;
+} {
+    const availableWidth = pageSize.width - 2 * pageSize.margin;
+    const availableHeight = pageSize.height - 2 * pageSize.margin;
+    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
+    const width = picture.width * scale;
+    const height = picture.height * scale;
+
+    return {
+        width,
+        height,
+        offsetX: (pageSize.width - width) / 2,
+        offsetY: (pageSize.height - height) / 2,
+    };
+}
+
+function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
+    const placement = getPdfPicturePlacement(picture, pageSize);
+
+    return (
+        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
+        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
+    );
+}
+
+function getPageObjectNumber(pageIndex: number): number {
+    return 3 + pageIndex * 3;
+}
+
+function appendPdfObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBodyChunks: readonly Uint8Array[],
+): void {
+    objectOffsets.push(getByteLength(chunks));
+    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
+}
+
+function appendPdfTextObject(
+    chunks: Uint8Array[],
+    objectOffsets: number[],
+    objectNumber: number,
+    objectBody: string,
+): void {
+    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
+}
+
+/**
+ * Write one or more JPEG pictures as a printable PDF document.
+ *
+ * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
+ * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
+ */
+export function createPicturePdf(
+    pictures: readonly PdfPicture[],
+    pageSize: PdfPageSize,
+    title: string,
+): Blob {
+    if (pictures.length === 0) {
+        throw new Error('PDF musí obsahovat alespoň jednu stránku');
+    }
+
+    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
+    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
+    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
+    const objectOffsets: number[] = [];
+
+    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
+    appendPdfTextObject(
+        chunks,
+        objectOffsets,
+        2,
+        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
+            `/Count ${pictures.length} >>`,
+    );
+
+    pictures.forEach((picture, pageIndex) => {
+        const pageObjectNumber = getPageObjectNumber(pageIndex);
+        const contentObjectNumber = pageObjectNumber + 1;
+        const pictureObjectNumber = pageObjectNumber + 2;
+        const contentStream = createPageContentStream(picture, pageSize);
+
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            pageObjectNumber,
+            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
+                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
+        );
+        appendPdfTextObject(
+            chunks,
+            objectOffsets,
+            contentObjectNumber,
+            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
+        );
+        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
+            encodeAsciiText(
+                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
+                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
+            ),
+            picture.bytes,
+            encodeAsciiText('\nendstream'),
+        ]);
+    });
+
+    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);
+
+    const crossReferenceOffset = getByteLength(chunks);
+    chunks.push(
+        encodeAsciiText(
+            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
+                objectOffsets.map(formatCrossReferenceEntry).join('') +
+                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
+                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
+        ),
+    );
+
+    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
+}
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d0128f2e3487d28dad07c99b380213ff0e1e..1a43fc2135d76b984256f0c3afac471ed426b1b3
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -148,68 +150,6 @@
 
 export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
-}
-
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
 }
 
 /**
@@ -219,53 +159,15 @@
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
-    );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
diff --git a/lib/exports/workshopWrapUpPdf.ts b/lib/exports/workshopWrapUpPdf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c9b9081b33c9bd67c179d7fdf0bed96b33c39a88
--- /dev/null
+++ b/lib/exports/workshopWrapUpPdf.ts
@@ -0,0 +1,362 @@
+import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
+import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';
+
+const PDF_PAGE_WIDTH_PIXELS = 1240;
+const PDF_PAGE_HEIGHT_PIXELS = 1754;
+const PDF_PAGE_MARGIN_PIXELS = 88;
+const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
+const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
+const PDF_PAGE_WIDTH_POINTS = 595;
+const PDF_PAGE_HEIGHT_POINTS = 842;
+const PDF_JPEG_QUALITY = 0.92;
+const PDF_BRAND_COLOR = '#0b4f60';
+const PDF_ACCENT_COLOR = '#0ea5b7';
+const PDF_TEXT_COLOR = '#102a36';
+const PDF_MUTED_TEXT_COLOR = '#526873';
+const PDF_BACKGROUND_COLOR = '#f7fbfc';
+const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
+const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
+const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
+const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
+const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
+const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
+const PDF_BODY_LINE_HEIGHT = 42;
+const PDF_METADATA_LINE_HEIGHT = 34;
+const PDF_PARAGRAPH_GAP_PIXELS = 20;
+const PDF_SECTION_GAP_PIXELS = 38;
+const PDF_LIST_INDENT_PIXELS = 38;
+const PDF_MATERIAL_GAP_PIXELS = 26;
+
+type WrapUpPdfPage = {
+    readonly canvas: HTMLCanvasElement;
+    readonly context: CanvasRenderingContext2D;
+    contentPositionY: number;
+};
+
+type WrapUpPdfRenderState = {
+    readonly pages: WrapUpPdfPage[];
+};
+
+type PdfTextStyle = {
+    readonly color: string;
+    readonly font: string;
+    readonly lineHeight: number;
+};
+
+const BODY_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_BODY_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_BODY_BOLD_FONT,
+    lineHeight: PDF_BODY_LINE_HEIGHT,
+};
+
+const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_BRAND_COLOR,
+    font: PDF_SECTION_TITLE_FONT,
+    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
+};
+
+const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_TEXT_COLOR,
+    font: PDF_DOCUMENT_TITLE_FONT,
+    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
+};
+
+const METADATA_TEXT_STYLE: PdfTextStyle = {
+    color: PDF_MUTED_TEXT_COLOR,
+    font: PDF_METADATA_FONT,
+    lineHeight: PDF_METADATA_LINE_HEIGHT,
+};
+
+function getPdfContentWidth(): number {
+    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
+}
+
+function createWrapUpPdfPage(): WrapUpPdfPage {
+    const canvas = document.createElement('canvas');
+    canvas.width = PDF_PAGE_WIDTH_PIXELS;
+    canvas.height = PDF_PAGE_HEIGHT_PIXELS;
+
+    const context = canvas.getContext('2d');
+    if (context === null) {
+        throw new Error('Shrnutí se nepodařilo připravit k tisku');
+    }
+
+    context.fillStyle = PDF_BACKGROUND_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
+    context.fillStyle = PDF_ACCENT_COLOR;
+    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
+    context.fillStyle = PDF_BRAND_COLOR;
+    context.font = PDF_METADATA_FONT;
+    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
+    context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    context.font = PDF_FOOTER_FONT;
+    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
+    context.strokeStyle = '#c9e7eb';
+    context.lineWidth = 2;
+    context.beginPath();
+    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
+    context.stroke();
+
+    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
+}
+
+function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const currentPage = renderState.pages[renderState.pages.length - 1];
+    if (currentPage === undefined) {
+        throw new Error('Shrnutí nemá stránku k vykreslení');
+    }
+
+    return currentPage;
+}
+
+function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
+    const nextPage = createWrapUpPdfPage();
+    renderState.pages.push(nextPage);
+    return nextPage;
+}
+
+function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
+    const currentPage = getCurrentPage(renderState);
+    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;
+
+    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
+}
+
+function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
+    const wordParts: string[] = [];
+    let currentWordPart = '';
+
+    for (const character of word) {
+        const nextWordPart = currentWordPart + character;
+        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
+            wordParts.push(currentWordPart);
+            currentWordPart = character;
+        } else {
+            currentWordPart = nextWordPart;
+        }
+    }
+
+    if (currentWordPart !== '') {
+        wordParts.push(currentWordPart);
+    }
+
+    return wordParts;
+}
+
+function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
+    const words = line.trim().split(/\s+/).filter((word) => word !== '');
+    const lines: string[] = [];
+    let currentLine = '';
+
+    for (const word of words) {
+        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
+        if (context.measureText(nextLine).width <= maximumWidth) {
+            currentLine = nextLine;
+            continue;
+        }
+
+        if (currentLine !== '') {
+            lines.push(currentLine);
+            currentLine = '';
+        }
+
+        const wordParts = splitLongPdfWord(context, word, maximumWidth);
+        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
+        lines.push(...wordParts.slice(0, -1));
+        currentLine = finalWordPart;
+    }
+
+    if (currentLine !== '') {
+        lines.push(currentLine);
+    }
+
+    return lines;
+}
+
+function getWrappedPdfLines(
+    context: CanvasRenderingContext2D,
+    text: string,
+    maximumWidth: number,
+): readonly string[] {
+    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
+}
+
+function drawPdfTextLines(
+    renderState: WrapUpPdfRenderState,
+    lines: readonly string[],
+    textStyle: PdfTextStyle,
+    offsetX = PDF_PAGE_MARGIN_PIXELS,
+): void {
+    for (const line of lines) {
+        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
+        currentPage.context.font = textStyle.font;
+        currentPage.context.fillStyle = textStyle.color;
+
+        if (line !== '') {
+            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
+        }
+        currentPage.contentPositionY += textStyle.lineHeight;
+    }
+}
+
+function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
+    const currentPage = ensurePdfVerticalSpace(renderState, gap);
+    currentPage.contentPositionY += gap;
+}
+
+function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
+    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
+    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
+    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
+    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = textStyle.font;
+    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
+    drawPdfTextLines(renderState, lines, textStyle);
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
+    for (const item of items) {
+        const currentPage = getCurrentPage(renderState);
+        currentPage.context.font = BODY_TEXT_STYLE.font;
+        const lines = getWrappedPdfLines(
+            currentPage.context,
+            item,
+            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
+        );
+        const firstLine = lines[0] ?? '';
+        const remainingLines = lines.slice(1);
+        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
+        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
+        bulletPage.context.font = PDF_BODY_BOLD_FONT;
+        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
+        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
+    }
+
+    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
+}
+
+function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
+    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
+}
+
+function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
+    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
+    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);
+
+    if (material.body !== '') {
+        addPdfParagraph(renderState, material.body);
+    }
+
+    if (material.urls.length > 0) {
+        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
+        addPdfBulletList(renderState, material.urls);
+    }
+}
+
+function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    const currentPage = getCurrentPage(renderState);
+    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
+    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
+    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
+    addPdfVerticalGap(renderState, 8);
+    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
+}
+
+function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
+    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
+    addPdfParagraph(renderState, document.summary);
+
+    addPdfSectionTitle(renderState, 'Klíčové poznatky');
+    if (document.keyTakeaways.length === 0) {
+        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
+    } else {
+        addPdfBulletList(renderState, document.keyTakeaways);
+    }
+
+    addPdfSectionTitle(renderState, 'Sdílené materiály');
+    if (document.materials.length === 0) {
+        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
+    } else {
+        document.materials.forEach((material) => addPdfMaterial(renderState, material));
+    }
+}
+
+function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
+    page.context.strokeStyle = '#c9e7eb';
+    page.context.lineWidth = 2;
+    page.context.beginPath();
+    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
+    page.context.stroke();
+    page.context.font = PDF_FOOTER_FONT;
+    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
+    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'right';
+    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
+    page.context.textAlign = 'left';
+}
+
+function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
+    const renderState: WrapUpPdfRenderState = {
+        pages: [createWrapUpPdfPage()],
+    };
+
+    addPdfDocumentTitle(renderState, document);
+    addPdfDocumentSections(renderState, document);
+    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));
+
+    return renderState.pages;
+}
+
+async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
+    const blob = await new Promise<Blob>((resolve, reject) => {
+        page.canvas.toBlob(
+            (canvasBlob) =>
+                canvasBlob === null
+                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
+                    : resolve(canvasBlob),
+            'image/jpeg',
+            PDF_JPEG_QUALITY,
+        );
+    });
+
+    return {
+        bytes: new Uint8Array(await blob.arrayBuffer()),
+        width: page.canvas.width,
+        height: page.canvas.height,
+    };
+}
+
+/**
+ * Renders the participant-safe wrap-up into an A4 PDF in the browser.
+ *
+ * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
+ * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
+ */
+export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
+    const pages = renderWorkshopWrapUpDocument(document);
+    const pictures = await Promise.all(pages.map(createPdfPicture));
+
+    return createPicturePdf(
+        pictures,
+        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
+        document.title,
+    );
+}
diff --git a/lib/workshops/workshopMaterialLinkSyntax.ts b/lib/workshops/workshopMaterialLinkSyntax.ts
new file mode 100644
index 0000000000000000000000000000000000000000..18d8d419646119d4b6c44fd89e23c28c9baadd0b
--- /dev/null
+++ b/lib/workshops/workshopMaterialLinkSyntax.ts
@@ -0,0 +1,317 @@
+/**
+ * The client-safe Markdown link syntax shared by the material renderer, public room exports, and server-side short
+ * link materialization. It intentionally has no network or database imports.
+ */
+
+const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
+const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
+const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
+const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
+const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
+
+export type WorkshopMaterialLinkRange = {
+    readonly destination: string;
+    readonly start: number;
+    readonly end: number;
+    readonly isTitleRequired: boolean;
+};
+
+function getWorkshopMaterialLinkBaseUrl(): string {
+    return WORKSHOP_MATERIAL_LINK_BASE_URL;
+}
+
+function isEscaped(text: string, index: number): boolean {
+    let precedingBackslashCount = 0;
+
+    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
+        precedingBackslashCount++;
+    }
+
+    return precedingBackslashCount % 2 === 1;
+}
+
+function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
+    const precedingText = markdown.slice(0, position);
+    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
+
+    return fenceCount % 2 === 1;
+}
+
+function isWithinInlineCode(markdown: string, position: number): boolean {
+    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
+    const precedingLineText = markdown.slice(lineStart, position);
+    let unescapedBacktickCount = 0;
+
+    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
+        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
+            unescapedBacktickCount++;
+        }
+    }
+
+    return unescapedBacktickCount % 2 === 1;
+}
+
+function isInsideCode(markdown: string, position: number): boolean {
+    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
+}
+
+function findUnescapedCharacter(text: string, character: string, start: number): number {
+    for (let cursor = start; cursor < text.length; cursor++) {
+        if (text[cursor] === character && !isEscaped(text, cursor)) {
+            return cursor;
+        }
+    }
+
+    return -1;
+}
+
+function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+
+    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
+        if (
+            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
+            isEscaped(markdown, openingBracket) ||
+            isInsideCode(markdown, openingBracket)
+        ) {
+            continue;
+        }
+
+        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
+        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
+            continue;
+        }
+
+        let destinationStart = closingBracket + 2;
+        while (/\s/.test(markdown[destinationStart] ?? '')) {
+            destinationStart++;
+        }
+
+        if (markdown[destinationStart] === '<') {
+            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
+            if (closingAngleBracket === -1) {
+                continue;
+            }
+
+            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
+            if (destination !== '') {
+                ranges.push({
+                    destination,
+                    start: destinationStart + 1,
+                    end: closingAngleBracket,
+                    isTitleRequired: false,
+                });
+            }
+            continue;
+        }
+
+        let parenthesisDepth = 0;
+        let destinationEnd = destinationStart;
+        for (; destinationEnd < markdown.length; destinationEnd++) {
+            const character = markdown[destinationEnd];
+            if (isEscaped(markdown, destinationEnd)) {
+                continue;
+            }
+            if (character === '(') {
+                parenthesisDepth++;
+                continue;
+            }
+            if (character === ')') {
+                if (parenthesisDepth === 0) {
+                    break;
+                }
+                parenthesisDepth--;
+                continue;
+            }
+            if (parenthesisDepth === 0 && /\s/.test(character)) {
+                break;
+            }
+        }
+
+        const destination = markdown.slice(destinationStart, destinationEnd);
+        if (destination !== '') {
+            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
+        }
+    }
+
+    return ranges;
+}
+
+function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const openingAnchorPattern = /<a\b[^>]*>/gi;
+    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
+
+    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
+        const openingAnchor = openingAnchorMatch[0];
+        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
+        if (isInsideCode(markdown, openingAnchorIndex)) {
+            continue;
+        }
+
+        const hrefMatch = hrefPattern.exec(openingAnchor);
+        if (hrefMatch === null) {
+            continue;
+        }
+
+        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
+        if (!destination) {
+            continue;
+        }
+
+        const hrefText = hrefMatch[0];
+        const destinationIndexInHref = hrefText.lastIndexOf(destination);
+        if (destinationIndexInHref === -1) {
+            continue;
+        }
+
+        const hrefIndexInAnchor = hrefMatch.index;
+        ranges.push({
+            destination,
+            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
+            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
+            isTitleRequired: false,
+        });
+    }
+
+    return ranges;
+}
+
+function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
+
+    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
+        const destination = autolinkMatch[1];
+        const matchIndex = autolinkMatch.index ?? 0;
+        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
+            ranges.push({
+                destination,
+                start: matchIndex,
+                end: matchIndex + autolinkMatch[0].length,
+                isTitleRequired: true,
+            });
+        }
+    }
+
+    return ranges;
+}
+
+/**
+ * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a bare HTTP(S) URL too. Link parsing must
+ * therefore match that rendered anchor as well as explicit Markdown and HTML links.
+ */
+function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+
+    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
+        const leadingWhitespace = bareUrlMatch[1] ?? '';
+        const rawDestination = bareUrlMatch[2];
+        const matchIndex = bareUrlMatch.index ?? 0;
+        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
+        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
+        const start = matchIndex + leadingWhitespace.length;
+
+        if (destination !== '' && !isInsideCode(markdown, start)) {
+            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
+        }
+    }
+
+    return ranges;
+}
+
+function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const ranges: WorkshopMaterialLinkRange[] = [];
+    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
+
+    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
+        const destination = definitionMatch[1] ?? definitionMatch[2];
+        if (!destination) {
+            continue;
+        }
+
+        const matchIndex = definitionMatch.index ?? 0;
+        if (isInsideCode(markdown, matchIndex)) {
+            continue;
+        }
+
+        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
+        ranges.push({
+            destination,
+            start: matchIndex + destinationIndexInDefinition,
+            end: matchIndex + destinationIndexInDefinition + destination.length,
+            isTitleRequired: false,
+        });
+    }
+
+    return ranges;
+}
+
+export function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
+    const sortedRanges = [
+        ...collectMarkdownInlineLinkRanges(markdown),
+        ...collectHtmlLinkRanges(markdown),
+        ...collectAutolinkRanges(markdown),
+        ...collectReferenceDefinitionRanges(markdown),
+        ...collectBareUrlRanges(markdown),
+    ].sort(
+        (firstRange, secondRange) =>
+            firstRange.start - secondRange.start ||
+            firstRange.end - secondRange.end ||
+            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
+    );
+
+    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
+    for (const range of sortedRanges) {
+        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
+        if (precedingRange === undefined || range.start >= precedingRange.end) {
+            nonOverlappingRanges.push(range);
+        }
+    }
+
+    return nonOverlappingRanges;
+}
+
+export function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
+    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
+        return null;
+    }
+
+    try {
+        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
+
+        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
+    } catch {
+        return null;
+    }
+}
+
+/**
+ * Lists the HTTP(S) destinations written in a material, regardless of whether they use inline Markdown, HTML,
+ * autolinks, or reference definitions. Images, anchors, e-mail links, and code samples are deliberately not click
+ * links.
+ */
+export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
+    return Array.from(
+        new Set(
+            collectWorkshopMaterialLinkRanges(bodyMarkdown)
+                .map((range) => range.destination)
+                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
+        ),
+    );
+}
+
+export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
+    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
+}
+
+export function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
+    return Array.from(
+        new Set(
+            collectWorkshopMaterialLinkRanges(bodyMarkdown)
+                .filter((range) => range.isTitleRequired)
+                .map((range) => range.destination)
+                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
+        ),
+    );
+}
diff --git a/lib/workshops/workshopWrapUpDocument.test.ts b/lib/workshops/workshopWrapUpDocument.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..9be898dcf9bb09affe391e6e3cce6b6c227e538e
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.test.ts
@@ -0,0 +1,90 @@
+import {
+    createWorkshopWrapUpDocument,
+    createWorkshopWrapUpPdfFileName,
+    extractWorkshopWrapUpPlainText,
+} from '@/lib/workshops/workshopWrapUpDocument';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+import { describe, expect, it } from 'vitest';
+
+const WORKSHOP: Pick<
+    WorkshopDetails,
+    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+> = {
+    slug: 'produkcni-kod-2026-08-21',
+    title: 'Produkční kód s AI agenty',
+    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
+    startsAt: '2026-08-21T19:00:00+02:00',
+    endsAt: '2026-08-21T20:30:00+02:00',
+    presentationUrl: 'https://files.example.com/prezentace.pdf',
+};
+
+const CONTENT_BLOCK: WorkshopContentBlock = {
+    id: 'material-1',
+    title: 'Postup pro nasazení',
+    bodyMarkdown: `## Nejdůležitější kroky
+- Napište ověřitelný plán.
+- Nechte agenta spustit testy.
+
+[Podklady](https://ptbk.io/material-123)`,
+    unlockAt: '2026-08-21T19:00:00+02:00',
+    sortOrder: 0,
+    isPublished: true,
+    isFollowUp: true,
+    isPaidMembersOnly: false,
+    createdAt: '2026-08-21T18:00:00+02:00',
+    updatedAt: '2026-08-21T18:00:00+02:00',
+    linkClickCount: 0,
+};
+
+describe('workshop wrap-up document', () => {
+    it('uses the selected workshop and the same readable materials as the participant room', () => {
+        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
+
+        expect(document.title).toBe(WORKSHOP.title);
+        expect(document.dateAndTimeLabel).toContain('21.');
+        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
+        expect(document.summary).toBe(WORKSHOP.description);
+        expect(document.keyTakeaways).toEqual([
+            'Postup pro nasazení',
+            'Nejdůležitější kroky',
+            'Napište ověřitelný plán.',
+            'Nechte agenta spustit testy.',
+        ]);
+        expect(document.materials).toEqual([
+            {
+                title: 'Postup pro nasazení',
+                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
+                urls: ['https://ptbk.io/material-123'],
+                isFollowUp: true,
+            },
+            {
+                title: 'Prezentace',
+                body: '',
+                urls: [WORKSHOP.presentationUrl],
+                isFollowUp: false,
+            },
+        ]);
+    });
+
+    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
+        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
+        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
+        const document = createWorkshopWrapUpDocument({
+            workshop: { ...WORKSHOP, presentationUrl: null },
+            contentBlocks: [visibleContentBlock],
+        });
+
+        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
+        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
+    });
+
+    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
+        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
+            'Důležité čtení & praxe',
+        );
+    });
+
+    it('uses the stable workshop slug in the PDF file name', () => {
+        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
+    });
+});
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f6debcac189c2029bec08dfa431fda693203dc93
--- /dev/null
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -0,0 +1,180 @@
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
+import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
+
+const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
+const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
+const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
+const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
+const HTML_TAG_PATTERN = /<[^>]*>/g;
+const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
+const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
+const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
+const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
+const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
+    '&amp;': '&',
+    '&gt;': '>',
+    '&lt;': '<',
+    '&nbsp;': ' ',
+    '&quot;': '"',
+};
+
+export type WorkshopWrapUpMaterial = {
+    readonly title: string;
+    readonly body: string;
+    readonly urls: readonly string[];
+    readonly isFollowUp: boolean;
+};
+
+/**
+ * The participant-safe data which a completed workshop hands over as a PDF.
+ *
+ * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
+ * therefore never export the body or URL of a paid material which the room itself withheld from them.
+ */
+export type WorkshopWrapUpDocument = {
+    readonly title: string;
+    readonly dateAndTimeLabel: string;
+    readonly summary: string;
+    readonly keyTakeaways: readonly string[];
+    readonly materials: readonly WorkshopWrapUpMaterial[];
+};
+
+type WorkshopWrapUpDocumentInput = {
+    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
+};
+
+function decodeHtmlEntities(text: string): string {
+    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
+}
+
+function removeMarkdownLinePrefix(line: string): string {
+    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
+}
+
+/**
+ * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
+ *
+ * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
+ * link parser, so a reader gets both the explanation and an address they can open.
+ */
+export function extractWorkshopWrapUpPlainText(markdown: string): string {
+    return decodeHtmlEntities(
+        markdown
+            .replace(MARKDOWN_LINK_PATTERN, '$1')
+            .replace(HTML_TAG_PATTERN, ' ')
+            .split(/\r?\n/)
+            .map(removeMarkdownLinePrefix)
+            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
+            .filter((line) => line !== '')
+            .join('\n'),
+    );
+}
+
+function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
+    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
+}
+
+function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
+    return {
+        title: getWorkshopWrapUpMaterialTitle(contentBlock),
+        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
+        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
+        isFollowUp: contentBlock.isFollowUp,
+    };
+}
+
+function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
+    return {
+        title: PRESENTATION_MATERIAL_TITLE,
+        body: '',
+        urls: [presentationUrl],
+        isFollowUp: false,
+    };
+}
+
+function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
+    const authoredTakeaways = contentBlock.bodyMarkdown
+        .split(/\r?\n/)
+        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
+        .map(removeMarkdownLinePrefix)
+        .map((line) => extractWorkshopWrapUpPlainText(line))
+        .filter((line) => line !== '');
+
+    return [
+        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
+        ...authoredTakeaways,
+    ];
+}
+
+function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
+    const normalizedValues = new Set<string>();
+    const keyTakeaways: string[] = [];
+
+    for (const value of values) {
+        const trimmedValue = value.trim();
+        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
+        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);
+
+        if (isNewValue) {
+            normalizedValues.add(normalizedValue);
+            keyTakeaways.push(trimmedValue);
+        }
+
+        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
+            break;
+        }
+    }
+
+    return keyTakeaways;
+}
+
+function getWorkshopWrapUpKeyTakeaways(
+    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
+    contentBlocks: readonly WorkshopContentBlock[],
+): readonly string[] {
+    const materialKeyTakeaways = getUniqueKeyTakeaways([
+        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
+        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
+    ]);
+
+    return materialKeyTakeaways.length > 0
+        ? materialKeyTakeaways
+        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
+}
+
+/**
+ * Builds the data of the wrap-up from fields which the participant room already owns.
+ *
+ * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
+ * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
+ * that the existing room state made readable.
+ */
+export function createWorkshopWrapUpDocument({
+    workshop,
+    contentBlocks,
+}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
+    const materials = [
+        ...contentBlocks.map(createWorkshopWrapUpMaterial),
+        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
+    ];
+
+    return {
+        title: workshop.title,
+        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
+            workshop.startsAt,
+            workshop.endsAt,
+        )}`,
+        summary: workshop.description.trim() || workshop.title,
+        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
+        materials,
+    };
+}
+
+/**
+ * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
+ */
+export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
+    return `${workshopSlug}-shrnuti-workshopu.pdf`;
+}
fatal runtime error: I/O error: operation failed to complete synchronously, aborting

=== runner shell finished at 2026-09-16T07:30:26.410Z ===
Status: failed with exit code 127

=== runner shell started at 2026-09-16T07:40:26.444Z ===
Script path: /c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/.promptbook/coder-prompts/2026-09-0240-wrap-up-pdf.sh

--- raw input ---
if [ -n "${PTBK_AGENTS_SERVER_ENV_FILE:-}" ] && [ -f "${PTBK_AGENTS_SERVER_ENV_FILE}" ]; then
set -a
source "${PTBK_AGENTS_SERVER_ENV_FILE}"
set +a
elif [ -f .env ]; then
set -a
source .env
set +a
fi

CODEX_LOGIN_STATUS="$(
    unset OPENAI_API_KEY OPENAI_BASE_URL CODEX_API_KEY
    # 'codex login status' prints the "Logged in using ChatGPT" line to stderr, so merge stderr into stdout (2>&1) to capture it
    codex login status 2>&1 || true
)"
case "$CODEX_LOGIN_STATUS" in
    *"Logged in using ChatGPT"*)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=1
        ;;
    *)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=0
        ;;
esac

CODEX_LOGIN_METHOD=chatgpt
CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=chatgpt)
unset CODEX_API_KEY
if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" != "1" ] &&
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" = "1" ] &&
    [ -n "${OPENAI_API_KEY:-}" ]; then
    CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=api)
    CODEX_LOGIN_METHOD=api
    CODEX_API_KEY="${OPENAI_API_KEY}"
    export CODEX_API_KEY
fi

if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" = "1" ] ||
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" != "1" ] ||
    [ -z "${OPENAI_API_KEY:-}" ]; then
unset OPENAI_API_KEY
unset OPENAI_BASE_URL
fi

printf '%s %s\n' 'ptbk-codex-login-method:' "${CODEX_LOGIN_METHOD}"

codex \
    "${CODEX_LOGIN_METHOD_ARGUMENTS[@]}" \
    -c model_reasoning_effort="max" \
    --ask-for-approval never \
    exec --model gpt-5.6-terra \
    --local-provider none \
    --sandbox danger-full-access \
    -C /c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron \
    --skip-git-repo-check \
    <<'CODEX_PROMPT'

## Your Task

After the workshop has ended, allow to download a wrap-up PDF 

- The wrap-up PDF should include a summary of the workshop, key takeaways, and any materials shared during the session.
- Do not add anything into the admin or database. The wrap-up PDF should be generated from information already there. 
- You are working with page `/cs/online-workshop/participant?workshop=`
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

## Your Behavior

You are Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

## Context

### Context

This repository contains Promptbook landing pages for different businesses,
use cases, and audiences. Keep these rules current when behavior changes.

#### Public routes

- `/` redirects to `/cs` or `/en` using `Accept-Language`.
- `/cs` is the Czech homepage and source of truth for homepage structure and copy.
- `/en` is its English localization.
- `/pro-mesta`, `/pro-firmy`, `/for-agro`, `/for-industry`, `/ai-supervize`,
  `/hackathon-factory`, and `/pavol` are specialized landing pages. `/pavol`
  redirects to `/cs/pavol` or `/en/pavol`; the localized pages are Pavol Hejny's
  personal pages.
- `/ai-supervize-mini` is the Czech one-day AI Supervize page. Published terms,
  prices, capacities, places, FAQs, registration, and participant information
  come from `/admin/workshops`; with no published term it shows a notice.
  `/skoleni` redirects there.
- `/cs/online-workshop` lists free 60-minute online workshops about writing
  production code with AI agents. Each term has its own subject and description,
  but all use one registration form. `/cs/online-workshop/dekujeme` is the
  full-load conversion page; `/participant` is the live room. Its waiting room
  offers every published term as the same term cards the landing page registers
  with: running and upcoming ones first, finished ones behind a disclosure.
  Picking one changes the room being connected to and the `workshop` parameter,
  without losing the name and e-mail already typed.
- `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
  merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
  the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
  `application/rss+xml`.
  Each episode lists every credited person by name, and every person any source
  credits has a card in `aiTaKrajtaPeople.ts`, which a test keeps in step, so a
  credited person never misses their portrait and the filter cannot lose them.
  It keeps exact episode counts but labels subscriptions and listening hours as
  estimates. The fixed mini-player, newest-episode header button, person/search/
  episode/play/archive/collaboration filters, and section hash are shareable as
  query/hash state; the snake game is local state. Person clicks filter episodes.
  Everyone in the roster shows a normalized, transparent PNG portrait. The source
  crop is recorded by `scripts/_cutAiTaKrajtaPeoplePortraits.mjs`, from the
  episode cover the show itself published them on or from the picture they publish
  of themselves when no cover carries them; the final alpha PNG is named by the
  roster and a test verifies its format and transparent pixels. Portrait frames
  supply subtle, stable neutral-gradient variations and a restrained hover cue,
  so photographed backgrounds never compete with the page. The one people list
  is drawn anew
  for every visit, weighted by how many episodes of the archive name each
  person, so somebody heard often comes up high far more often than someone in
  one díl without the list ever becoming a ranking. The draw is local state and
  is made in the browser; the page itself is built in the order the draw leans
  towards, from the most often named person to the least often named one, so the
  browser hydrates into the list it was sent.
  Collaboration submissions use `/admin/contacts`. Its tab icon is the page's
  own snake drawing in `/ai-ta-krajta/logo.svg` and `.png`; SVG corners are
  rounded and transparent, while the raster fills its square. That drawing is
  traced off the cover artwork of the show and recorded once in
  `businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.ts`, together with the
  measurements of the animal along its own length. The snake of the minigame
  starts in exactly that shape, in the frame the still logo occupied, and only
  then eases into the proportions, colours and speed of a game snake.
  A seven-cell Promptbook coder terminal floats in the bottom-right corner and
  clears the player and cookie controls. It types `$ ptbk`, then animates an
  ASCII octopus in response to pointer, focus, scrolling, and the snake terrarium.
  It stays still for reduced motion and pauses its clock in hidden tabs. The
  badge and the shared footer credit link to `https://coder.ptbk.io/`.
- `/ai-ta-krajta/media-kit` and `/ai-ta-krajta/branding` are the two pages beside
  the podcast. They are named once, in `AI_TA_KRAJTA_SUBPAGES`, which the footer
  lists and which they point at each other through, so no link can name a page
  differently than the page names itself. Both wear the same compact header,
  section heading and footer as each other. The brand kit hands out the logo the
  site already serves at `/ai-ta-krajta/logo.svg`, `.png` and the cover artwork,
  publishes no colour outside `AI_TA_KRAJTA_COLORS`, and says how the name is
  written; it asks for nothing and sends visitors to the media-kit form.
- `/cs/komunita` is the permanent Czech community room. It has chat, polls,
  projects, materials, and published terms, but no schedule, stage, or live
  updates. Terms show event kind, format/place, price, and status. A term with a
  live room links there; otherwise it links to its landing page. The calendar
  opens on the member's month, can filter by day, and uses the same terms and
  statuses as the cards. Empty days cannot be selected; an empty month is only
  selected when the member's month has no terms. The room offers Google Calendar
  and `webcal:` subscriptions.
- `/cs/komunita/projects` is the full project gallery, ordered by upvotes.
  `/cs/komunita/projects/<project_id>` shows scraped project details and a
  moderated discussion. Project-room sessions derive from the community session;
  the author moderates their own discussion.
- `/cs/komunita/clenstvi` offers free community access and the 199 Kč monthly
  membership. Webinars remain free; paid access adds recordings, archive,
  materials, extra content, priority questions, and Discord/community features.
  The page is prefilled from `fullname` and `email`, supports community discount
  codes, has no new annual plan or trial, and allows cancellation.
- `/cs/komunita/calendar.ics` publishes the same terms by stable slug, so moved
  or renamed terms update existing subscriptions. It contains only event terms
  and public destinations, never subscriber identity.
- `/admin/workshops` manages terms of every event kind, participants, comments,
  reactions, content, attached community polls, and settings. Every term says both
  of its audiences: the people registered on the landing page of its event, and
  the people who entered its room. A room which is no term of an event says
  nothing about registrations at all. The registration block opens the exact
  filtered `/admin/contacts` list and its shared CSV, vCard, and Book exports;
  it never creates a second contact table or serializer in workshop administration.
- `/admin/community` manages the permanent community, including polls, project
  moderation, participants, memberships, payments, and room analytics.
- `/admin/shortener` manages public short links, QR/UTM output, destinations,
  notes, search/filter/sort state, and private click history. Links are served
  by `/[shortcode]`; `/shortener` redirects to the admin page.
- `/admin/login` authenticates the single `admin` account with `ADMIN_PASSWORD`
  and a signed session cookie. All `/admin/*` pages and APIs require
  `requireAdminSignedIn`. `/admin` is the post-login dashboard.
- Other public/legal routes include `/contact`, `/data-deletion`, `/privacy`,
  `/terms`, `/dekujeme`, `/branding`, and the routes under `/k`, `/old`, and
  `/test`.

#### Shared community and workshop behavior

- Rooms lead to each other in both directions, through one identity hand-off. The
  community lists the terms and opens the room of each; an invited workshop room
  places its community link beside its materials as a special material for every
  participant, regardless of paid membership. Where a special material sits is
  decided once, in `lib/workshops/workshopSpecialMaterials.ts`: a card says where
  among the ordinary materials it belongs, and the invitation opens the material
  list of a participant who does not pay while it closes the list of a paying
  member. A membership which is not loaded yet leaves it where a participant who
  does not pay reads it. Both carry the connected member's
  name and email on, and an incomplete identity carries nothing rather than half
  of it. Which rooms invite is answered by
  `lib/workshops/workshopKindCapabilities.ts`; the community and project
  discussions do not. The invitation names the community's sections by the names
  the community itself uses.
- Community polls attached to workshops are shared. A normalized email gives a
  member one vote across the community and all attached workshops. Workshops
  may display and accept votes, but the community owns administration.
- The participant room owns the membership badge and popup. Community and live
  workshop rooms use the same membership for the connecting email, and checkout
  returns to the room where it started. Membership is offered only by room kinds
  listed in `lib/workshops/workshopKindCapabilities.ts`; project discussions do
  not offer it.
- Members can open Stripe Customer Portal from the popup. In-app cancellation
  stops only the next renewal, preserves access through the paid period, appears
  in the badge, and can be reversed before that period ends.
- A full-price-for-the-whole-term discount is a voucher: atomically consume it,
  create membership immediately, ask for no card, and create no Stripe renewal.
  A limited-month discount still opens checkout because the regular price returns.
  Stripe webhooks update completed, cancelled, failed, and late payments. With no
  Stripe key, hide membership; with test keys, identify the test payment gate.
- Community projects use a URL-first metadata wizard. Ordinary submissions await
  moderation; trusted members and moderators are approved immediately. Pending
  projects remain visible to their author and moderators.
- A live workshop room has countdown, YouTube stage, reactions, watching count,
  moderated chat, timed materials, and attached poll aggregates. An open-ended
  term runs until its recorded end; its stage does not end automatically. Admins
  can select, replace, clear, or create the displayed comment through the same
  private realtime channel used by reactions.
- A term can be about a project. The connection is one value — GitHub repository,
  the default branch, and one or more branch patterns such as `main`, `client-*`,
  `feature/*`, or `*` for all branches, plus an optional deployment URL — so it is
  set, changed, and unset at once and neither branch selection nor deployment
  outlives its repository. Room kinds which offer it are named in
  `lib/workshops/workshopKindCapabilities.ts`. The room shows the repository as a
  special material beside its ordinary materials for every participant, with its
  links and newest commits, marking those which arrive while a participant watches;
  a newly found commit is broadcast to active rooms and appears on the stage for
  ten seconds. A selection resolving to multiple branches is shown as a commit
  graph. Commits come from the public commit feed or keyless API, pooled once per
  server and cached through the revalidation window; an unreadable feed still
  leaves the room naming its project.
- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
  GitHub Markdown page. The room renders it beside ordinary materials through the
  shared material card, primary action, and QR code, for every participant without
  making it timed or membership-gated content.
- Paid-only materials are decided on the server in one pass. Members receive
  unlocked material; others receive only the published titles as an offer. An
  untitled item is not named, items are not named before their unlock time, and a
  room without membership hides paid-only items without naming them.
- After a workshop ends, its recording is server-gated to members. Others receive
  the published teaser, or a generic offer when no teaser exists; a term without
  a recording offers nothing.
- Trusted participants remain invisible and their messages are auto-approved.
  Moderators see pending messages, can approve/reject/correct/pin them, and can
  trust or silence authors. Workshop and community moderators are appointed in
  `/admin/workshops`; project authors moderate their own discussions.
- The room records whether each open browser is actively or passively attended
  from pointer, typing, scroll, and touch activity. Admin analytics distinguish
  active computer users from merely open tabs with dashed audience lines.
- The community's compact gallery shows its five highest-upvoted approved projects
  and links to the full gallery. Community landing content is read from the live
  room: expose only anonymous totals, approved messages, and shared projects;
  show member first names only. If the room cannot be read, show less content,
  not an error. Landing-page reactions are estimates based on recent room data.

#### Administration and data rules

- Event kinds are defined once in `lib/events/eventTypes.ts`; adding one should
  not require database migration or page-specific duplication. Terms ask for kind,
  online/place format, price, and capacity. Each kind also names where its landing
  page records registrations, which is the `placeName` of the contacts it gathers.
- Which term a registration belongs to is decided once, in
  `lib/workshops/workshopRegistrations.ts`. A term is recognised by its slug, by
  its Prague day, and by the moment it begins at, so registrations written before
  terms had slugs keep counting. Registration forms write the term through the
  same line prefixes that rule reads back. Contacts are still gathered, recorded
  and shown only by `/admin/contacts`; counting reads nothing but their notes.
  The registration actions in workshop administration carry that same term filter
  into the contacts list and its exports.
- Workshop polls are read-only in workshop administration. Creating a new event
  publishes it by default; duplicating an event keeps it published unless the
  source is unpublished. Duplicating a workshop preserves its connections to
  the already existing attached polls; it creates no polls, options, votes, or room history. Stage settings contain
  the live stream and recording teaser, presentation settings the public presentation
  address, and project settings the repository the term is about, written as an
  address or as `owner/name`, together with its branch patterns. Deleting a workshop is a soft deletion: its room
  data and attached
  community polls stay stored, but the term leaves normal public and administrative
  lists and no longer holds its slug for a replacement. An end may be empty;
  admins can record, adjust, clear, and reopen it. Overview analytics are zoomable and share their
  room, section, lines, reaction, zoom, and keyword metrics through query params.
- Community administration is the workshop dashboard restricted to the
  `community` room kind: no room picker, schedule, stage, reactions, or address.
  Its membership view filters Stripe lifecycle, prices, discounts, identifiers,
  dates, test/live mode, and links to the exact Stripe checkout/subscription.
  Community analytics cover all measured time and member details show seen times.
- Shortener search, provenance filters, sorting, and selected click history live
  in GET parameters. Editing changes only shortcode, destinations, note, and
  landing page; deletion also deletes recorded clicks.

#### Database and verification

- Put database changes in `migrations/*.sql`. Startup and
  `npm run migrate-database` run `_initialize.sql`, then migrations in filename
  order, recording immutable filenames and checksums in `public."Migration"`.
  Changed or missing migrations fail, as do queries requiring an unapplied one.
- `npm run test-types` must build before `tsc` to refresh `.next/types`.
- `npm run test-e2e` uses the Next.js development server; first-request compile
  headroom belongs in `playwright.config.ts`, not individual tests. E2E tests are
  independent and each archives one recording in `tests/e2e/videos/`, retaining
  only recent runs.

CODEX_PROMPT

--- raw output ---
.env: line 27: app: command not found
ptbk-codex-login-method: chatgpt
Reading prompt from stdin...
OpenAI Codex v0.154.0
--------
workdir: C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: max
reasoning summaries: none
session id: 01a0a929-0edb-7f23-90d9-13d5f2a95af0
--------
user

## Your Task

After the workshop has ended, allow to download a wrap-up PDF 

- The wrap-up PDF should include a summary of the workshop, key takeaways, and any materials shared during the session.
- Do not add anything into the admin or database. The wrap-up PDF should be generated from information already there. 
- You are working with page `/cs/online-workshop/participant?workshop=`
- Keep in mind the DRY _(don't repeat yourself)_ principle.
- Do a analysis of the current functionality before you start implementing.
- Add the changes into the [changelog](./changelog/_current-preversion.md)

## Your Behavior

You are Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

## Context

### Context

This repository contains Promptbook landing pages for different businesses,
use cases, and audiences. Keep these rules current when behavior changes.

#### Public routes

- `/` redirects to `/cs` or `/en` using `Accept-Language`.
- `/cs` is the Czech homepage and source of truth for homepage structure and copy.
- `/en` is its English localization.
- `/pro-mesta`, `/pro-firmy`, `/for-agro`, `/for-industry`, `/ai-supervize`,
  `/hackathon-factory`, and `/pavol` are specialized landing pages. `/pavol`
  redirects to `/cs/pavol` or `/en/pavol`; the localized pages are Pavol Hejny's
  personal pages.
- `/ai-supervize-mini` is the Czech one-day AI Supervize page. Published terms,
  prices, capacities, places, FAQs, registration, and participant information
  come from `/admin/workshops`; with no published term it shows a notice.
  `/skoleni` redirects there.
- `/cs/online-workshop` lists free 60-minute online workshops about writing
  production code with AI agents. Each term has its own subject and description,
  but all use one registration form. `/cs/online-workshop/dekujeme` is the
  full-load conversion page; `/participant` is the live room. Its waiting room
  offers every published term as the same term cards the landing page registers
  with: running and upcoming ones first, finished ones behind a disclosure.
  Picking one changes the room being connected to and the `workshop` parameter,
  without losing the name and e-mail already typed.
- `/ai-ta-krajta` reads episodes hourly from podcast RSS and YouTube feeds and
  merges their host rosters with `businesses/ai-ta-krajta/aiTaKrajtaEpisodes.json`. Its shared platform list exposes
  the publisher's direct RSS feed for custom podcast applications, and page metadata advertises it as
  `application/rss+xml`.
  Each episode lists every credited person by name, and every person any source
  credits has a card in `aiTaKrajtaPeople.ts`, which a test keeps in step, so a
  credited person never misses their portrait and the filter cannot lose them.
  It keeps exact episode counts but labels subscriptions and listening hours as
  estimates. The fixed mini-player, newest-episode header button, person/search/
  episode/play/archive/collaboration filters, and section hash are shareable as
  query/hash state; the snake game is local state. Person clicks filter episodes.
  Everyone in the roster shows a normalized, transparent PNG portrait. The source
  crop is recorded by `scripts/_cutAiTaKrajtaPeoplePortraits.mjs`, from the
  episode cover the show itself published them on or from the picture they publish
  of themselves when no cover carries them; the final alpha PNG is named by the
  roster and a test verifies its format and transparent pixels. Portrait frames
  supply subtle, stable neutral-gradient variations and a restrained hover cue,
  so photographed backgrounds never compete with the page. The one people list
  is drawn anew
  for every visit, weighted by how many episodes of the archive name each
  person, so somebody heard often comes up high far more often than someone in
  one díl without the list ever becoming a ranking. The draw is local state and
  is made in the browser; the page itself is built in the order the draw leans
  towards, from the most often named person to the least often named one, so the
  browser hydrates into the list it was sent.
  Collaboration submissions use `/admin/contacts`. Its tab icon is the page's
  own snake drawing in `/ai-ta-krajta/logo.svg` and `.png`; SVG corners are
  rounded and transparent, while the raster fills its square. That drawing is
  traced off the cover artwork of the show and recorded once in
  `businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.ts`, together with the
  measurements of the animal along its own length. The snake of the minigame
  starts in exactly that shape, in the frame the still logo occupied, and only
  then eases into the proportions, colours and speed of a game snake.
  A seven-cell Promptbook coder terminal floats in the bottom-right corner and
  clears the player and cookie controls. It types `$ ptbk`, then animates an
  ASCII octopus in response to pointer, focus, scrolling, and the snake terrarium.
  It stays still for reduced motion and pauses its clock in hidden tabs. The
  badge and the shared footer credit link to `https://coder.ptbk.io/`.
- `/ai-ta-krajta/media-kit` and `/ai-ta-krajta/branding` are the two pages beside
  the podcast. They are named once, in `AI_TA_KRAJTA_SUBPAGES`, which the footer
  lists and which they point at each other through, so no link can name a page
  differently than the page names itself. Both wear the same compact header,
  section heading and footer as each other. The brand kit hands out the logo the
  site already serves at `/ai-ta-krajta/logo.svg`, `.png` and the cover artwork,
  publishes no colour outside `AI_TA_KRAJTA_COLORS`, and says how the name is
  written; it asks for nothing and sends visitors to the media-kit form.
- `/cs/komunita` is the permanent Czech community room. It has chat, polls,
  projects, materials, and published terms, but no schedule, stage, or live
  updates. Terms show event kind, format/place, price, and status. A term with a
  live room links there; otherwise it links to its landing page. The calendar
  opens on the member's month, can filter by day, and uses the same terms and
  statuses as the cards. Empty days cannot be selected; an empty month is only
  selected when the member's month has no terms. The room offers Google Calendar
  and `webcal:` subscriptions.
- `/cs/komunita/projects` is the full project gallery, ordered by upvotes.
  `/cs/komunita/projects/<project_id>` shows scraped project details and a
  moderated discussion. Project-room sessions derive from the community session;
  the author moderates their own discussion.
- `/cs/komunita/clenstvi` offers free community access and the 199 Kč monthly
  membership. Webinars remain free; paid access adds recordings, archive,
  materials, extra content, priority questions, and Discord/community features.
  The page is prefilled from `fullname` and `email`, supports community discount
  codes, has no new annual plan or trial, and allows cancellation.
- `/cs/komunita/calendar.ics` publishes the same terms by stable slug, so moved
  or renamed terms update existing subscriptions. It contains only event terms
  and public destinations, never subscriber identity.
- `/admin/workshops` manages terms of every event kind, participants, comments,
  reactions, content, attached community polls, and settings. Every term says both
  of its audiences: the people registered on the landing page of its event, and
  the people who entered its room. A room which is no term of an event says
  nothing about registrations at all. The registration block opens the exact
  filtered `/admin/contacts` list and its shared CSV, vCard, and Book exports;
  it never creates a second contact table or serializer in workshop administration.
- `/admin/community` manages the permanent community, including polls, project
  moderation, participants, memberships, payments, and room analytics.
- `/admin/shortener` manages public short links, QR/UTM output, destinations,
  notes, search/filter/sort state, and private click history. Links are served
  by `/[shortcode]`; `/shortener` redirects to the admin page.
- `/admin/login` authenticates the single `admin` account with `ADMIN_PASSWORD`
  and a signed session cookie. All `/admin/*` pages and APIs require
  `requireAdminSignedIn`. `/admin` is the post-login dashboard.
- Other public/legal routes include `/contact`, `/data-deletion`, `/privacy`,
  `/terms`, `/dekujeme`, `/branding`, and the routes under `/k`, `/old`, and
  `/test`.

#### Shared community and workshop behavior

- Rooms lead to each other in both directions, through one identity hand-off. The
  community lists the terms and opens the room of each; an invited workshop room
  places its community link beside its materials as a special material for every
  participant, regardless of paid membership. Where a special material sits is
  decided once, in `lib/workshops/workshopSpecialMaterials.ts`: a card says where
  among the ordinary materials it belongs, and the invitation opens the material
  list of a participant who does not pay while it closes the list of a paying
  member. A membership which is not loaded yet leaves it where a participant who
  does not pay reads it. Both carry the connected member's
  name and email on, and an incomplete identity carries nothing rather than half
  of it. Which rooms invite is answered by
  `lib/workshops/workshopKindCapabilities.ts`; the community and project
  discussions do not. The invitation names the community's sections by the names
  the community itself uses.
- Community polls attached to workshops are shared. A normalized email gives a
  member one vote across the community and all attached workshops. Workshops
  may display and accept votes, but the community owns administration.
- The participant room owns the membership badge and popup. Community and live
  workshop rooms use the same membership for the connecting email, and checkout
  returns to the room where it started. Membership is offered only by room kinds
  listed in `lib/workshops/workshopKindCapabilities.ts`; project discussions do
  not offer it.
- Members can open Stripe Customer Portal from the popup. In-app cancellation
  stops only the next renewal, preserves access through the paid period, appears
  in the badge, and can be reversed before that period ends.
- A full-price-for-the-whole-term discount is a voucher: atomically consume it,
  create membership immediately, ask for no card, and create no Stripe renewal.
  A limited-month discount still opens checkout because the regular price returns.
  Stripe webhooks update completed, cancelled, failed, and late payments. With no
  Stripe key, hide membership; with test keys, identify the test payment gate.
- Community projects use a URL-first metadata wizard. Ordinary submissions await
  moderation; trusted members and moderators are approved immediately. Pending
  projects remain visible to their author and moderators.
- A live workshop room has countdown, YouTube stage, reactions, watching count,
  moderated chat, timed materials, and attached poll aggregates. An open-ended
  term runs until its recorded end; its stage does not end automatically. Admins
  can select, replace, clear, or create the displayed comment through the same
  private realtime channel used by reactions.
- A term can be about a project. The connection is one value — GitHub repository,
  the default branch, and one or more branch patterns such as `main`, `client-*`,
  `feature/*`, or `*` for all branches, plus an optional deployment URL — so it is
  set, changed, and unset at once and neither branch selection nor deployment
  outlives its repository. Room kinds which offer it are named in
  `lib/workshops/workshopKindCapabilities.ts`. The room shows the repository as a
  special material beside its ordinary materials for every participant, with its
  links and newest commits, marking those which arrive while a participant watches;
  a newly found commit is broadcast to active rooms and appears on the stage for
  ten seconds. A selection resolving to multiple branches is shown as a commit
  graph. Commits come from the public commit feed or keyless API, pooled once per
  server and cached through the revalidation window; an unreadable feed still
  leaves the room naming its project.
- A workshop can carry one public presentation URL for a PDF, PowerPoint file, or
  GitHub Markdown page. The room renders it beside ordinary materials through the
  shared material card, primary action, and QR code, for every participant without
  making it timed or membership-gated content.
- Paid-only materials are decided on the server in one pass. Members receive
  unlocked material; others receive only the published titles as an offer. An
  untitled item is not named, items are not named before their unlock time, and a
  room without membership hides paid-only items without naming them.
- After a workshop ends, its recording is server-gated to members. Others receive
  the published teaser, or a generic offer when no teaser exists; a term without
  a recording offers nothing.
- Trusted participants remain invisible and their messages are auto-approved.
  Moderators see pending messages, can approve/reject/correct/pin them, and can
  trust or silence authors. Workshop and community moderators are appointed in
  `/admin/workshops`; project authors moderate their own discussions.
- The room records whether each open browser is actively or passively attended
  from pointer, typing, scroll, and touch activity. Admin analytics distinguish
  active computer users from merely open tabs with dashed audience lines.
- The community's compact gallery shows its five highest-upvoted approved projects
  and links to the full gallery. Community landing content is read from the live
  room: expose only anonymous totals, approved messages, and shared projects;
  show member first names only. If the room cannot be read, show less content,
  not an error. Landing-page reactions are estimates based on recent room data.

#### Administration and data rules

- Event kinds are defined once in `lib/events/eventTypes.ts`; adding one should
  not require database migration or page-specific duplication. Terms ask for kind,
  online/place format, price, and capacity. Each kind also names where its landing
  page records registrations, which is the `placeName` of the contacts it gathers.
- Which term a registration belongs to is decided once, in
  `lib/workshops/workshopRegistrations.ts`. A term is recognised by its slug, by
  its Prague day, and by the moment it begins at, so registrations written before
  terms had slugs keep counting. Registration forms write the term through the
  same line prefixes that rule reads back. Contacts are still gathered, recorded
  and shown only by `/admin/contacts`; counting reads nothing but their notes.
  The registration actions in workshop administration carry that same term filter
  into the contacts list and its exports.
- Workshop polls are read-only in workshop administration. Creating a new event
  publishes it by default; duplicating an event keeps it published unless the
  source is unpublished. Duplicating a workshop preserves its connections to
  the already existing attached polls; it creates no polls, options, votes, or room history. Stage settings contain
  the live stream and recording teaser, presentation settings the public presentation
  address, and project settings the repository the term is about, written as an
  address or as `owner/name`, together with its branch patterns. Deleting a workshop is a soft deletion: its room
  data and attached
  community polls stay stored, but the term leaves normal public and administrative
  lists and no longer holds its slug for a replacement. An end may be empty;
  admins can record, adjust, clear, and reopen it. Overview analytics are zoomable and share their
  room, section, lines, reaction, zoom, and keyword metrics through query params.
- Community administration is the workshop dashboard restricted to the
  `community` room kind: no room picker, schedule, stage, reactions, or address.
  Its membership view filters Stripe lifecycle, prices, discounts, identifiers,
  dates, test/live mode, and links to the exact Stripe checkout/subscription.
  Community analytics cover all measured time and member details show seen times.
- Shortener search, provenance filters, sorting, and selected click history live
  in GET parameters. Editing changes only shortcode, destinations, note, and
  landing page; deletion also deletes recorded clicks.

#### Database and verification

- Put database changes in `migrations/*.sql`. Startup and
  `npm run migrate-database` run `_initialize.sql`, then migrations in filename
  order, recording immutable filenames and checksums in `public."Migration"`.
  Changed or missing migrations fail, as do queries requiring an unapplied one.
- `npm run test-types` must build before `tsc` to refresh `.next/types`.
- `npm run test-e2e` uses the Next.js development server; first-request compile
  headroom belongs in `playwright.config.ts`, not individual tests. E2E tests are
  independent and each archives one recording in `tests/e2e/videos/`, retaining
  only recent runs.


codex
I’ll first trace how the participant room decides that a term has ended and how it already exposes workshop descriptions and materials. Then I’ll add a client-side PDF wrap-up that reuses those same attendee-visible data paths, with no admin or database changes.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git status --short; rg -n --glob '"'!node_modules'"' \"participant|recorded end|ended|hasEnded|workshop.*end|workshopEnd|materials\" app lib components tests changelog 2>"'$null' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 348ms:
 M AGENTS.md
 M businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
 M businesses/online-workshop/participant/WorkshopStage.test.tsx
 M businesses/online-workshop/participant/WorkshopStage.tsx
 M businesses/online-workshop/participant/WorkshopWrapUp.tsx
 M changelog/_current-preversion.md
 M lib/exports/svgChartExport.ts
 M prompts/2026-09-0240-wrap-up-pdf.md
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
?? lib/exports/pdfPictures.test.ts
?? lib/exports/pdfPictures.ts
?? lib/exports/workshopWrapUpPdf.ts
?? lib/workshops/workshopMaterialLinkSyntax.ts
?? lib/workshops/workshopWrapUpDocument.test.ts
?? lib/workshops/workshopWrapUpDocument.ts
changelog\_current-preversion.md:3:- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
changelog\_current-preversion.md:4:  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
changelog\_current-preversion.md:10:  ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
changelog\_current-preversion.md:14:- Placed the community material of `/cs/online-workshop/participant?workshop=` by what the member reading it has: a
changelog\_current-preversion.md:15:  participant who does not pay for the membership is invited into the community before the materials of the workshop,
changelog\_current-preversion.md:18:  card which shares the material list now says where among the ordinary materials it belongs, so one rule places them
changelog\_current-preversion.md:22:  Markdown page, and `/cs/online-workshop/participant?workshop=` shows it beside ordinary materials with the same
changelog\_current-preversion.md:24:  to free and paid participants alike without becoming timed or membership-gated content.
changelog\_current-preversion.md:26:- Moved a connected GitHub repository in `/cs/online-workshop/participant` into the materials list as a special
changelog\_current-preversion.md:27:  material. Its existing repository, deployment, and live-commit links now stay with ordinary workshop materials,
changelog\_current-preversion.md:29:  participants without turning the repository into timed or membership-gated content.
changelog\_current-preversion.md:31:- Moved the community hand-off in `/cs/online-workshop/participant` into the materials list as a dedicated
changelog\_current-preversion.md:33:  available to free and paid members alike, while carrying the participant identity through the existing shared
changelog\_current-preversion.md:35:  ordinary materials without becoming timed or membership-gated workshop content.
changelog\_current-preversion.md:42:- Let `/admin/workshops` reopen a workshop by clearing its recorded end, and give an open-ended workshop quick choices to end now, one hour after its start, or two hours after its start; each choice uses the existing live update, so `/cs/online-workshop/participant` immediately keeps or replaces the stream accordingly.
changelog\_current-preversion.md:58:  the community or scrolling past its materials. A return from Stripe opens that modal as well, preserving the clear
changelog\_current-preversion.md:67:  participant history, so the two administration views stay directly connected without pretending one room session is
changelog\_current-preversion.md:70:- Show every visible community poll attached to an occurrence inside that occurrence's participant room, including its
changelog\_current-preversion.md:97:- Made the material QR codes in the workshop and community rooms compact and square. Their shared renderer now owns
changelog\_current-preversion.md:104:  view, yet keeps the native scrollbar as a fallback. The participant, contact, discount-code and short-link tables
changelog\_current-preversion.md:105:  all use it, and their useful identity column — the participant or contact name, a discount code, or a shortcode —
changelog\_current-preversion.md:153:- Let the host put a question on the live stage from `/admin/workshops`. In the `Komentáře` section, any attendee
changelog\_current-preversion.md:154:  question which has not been rejected can now be sent above the stream of `/cs/online-workshop/participant`, replaced
changelog\_current-preversion.md:163:  `/cs/online-workshop/participant` now watches whether somebody is at their computer or phone at all — whether they
changelog\_current-preversion.md:165:  measured minute of a room is either actively or passively attended. Somebody who is watching without moving a hand
changelog\_current-preversion.md:169:  counted the audience now also remembers how that minute was attended.
changelog\_current-preversion.md:176:  participant is listed with is now named `Čas v místnosti` throughout the administration and its exports, because it
changelog\_current-preversion.md:179:- Let a workshop have no end at all. A term whose end is left empty in `/admin/workshops` now runs from its start for
changelog\_current-preversion.md:180:  as long as it takes: `/cs/online-workshop/participant` keeps its stream on the stage and its room open indefinitely
changelog\_current-preversion.md:181:  instead of declaring the workshop over an hour after it started, which is what an unwritten end used to mean. The
changelog\_current-preversion.md:182:  settings of such a running term offer an `Ukončit workshop` button which writes the current moment as its end, so
changelog\_current-preversion.md:183:  the room replaces the stage with the closing wrap-up and its feedback the moment the workshop really ends, and the
changelog\_current-preversion.md:197:  the schedule, the capacity and the price, the registration form and the participant information page all read the
changelog\_current-preversion.md:211:  existing membership page with the benefits of the paid membership, while the shared participant-room header now
changelog\_current-preversion.md:216:  webinar recordings and their archive, practical materials, additional content, priority questions and the existing
changelog\_current-preversion.md:248:  the workshop links and above the materials, ordered by upvotes. Its home view shows the five strongest projects and a
changelog\_current-preversion.md:268:- Let moderators and administration-created artificial messages of `/cs/online-workshop/participant` and `/cs/komunita` carry active links while keeping every normal participant's URLs inert text. Eligible links now follow the same persisted ad hoc `ptbk.io` shortener path as materials, including their UTM source record and shortener redirect click measurement; the safe chat renderer opens only those already-shortened URLs in a new tab, so a raw destination cannot become an untracked active link.
changelog\_current-preversion.md:276:- Put a branded QR code beside every linked material on the desktop layouts of `/cs/online-workshop/participant` and `/cs/komunita`. Each code carries the persisted public short URL prepared when `/admin/workshops` saves the material, so scanning it on a phone follows the exact same redirect and click tracking as opening, copying, or forwarding the material link; mobile layouts keep the panel out of the way. The shortener and the shared participant room now both use one client-only Promptbook QR renderer instead of loading separate implementations.
changelog\_current-preversion.md:278:- Routed every HTTP(S) material link of `/cs/online-workshop/participant` and `/cs/komunita` through a persisted ad hoc `ptbk.io` short link, including ordinary Markdown, HTML, reference, autolink and bare-URL forms. The participant room no longer posts browser click events: opening, copying or forwarding the same short address is measured only by the shortener redirect, and `/admin/workshops` now derives material totals and its activity graph from those redirect records. Per-participant material-click figures were removed because a shareable public link cannot honestly identify the workshop participant who opened it. The shortener now records whether a link was manually created or ad hoc and which application created it, and `/admin/shortener` can display, filter and sort by both properties.
changelog\_current-preversion.md:280:- Made `/cs/online-workshop/participant` survive a temporary overloaded or unavailable backend after it has been opened once: every successful room snapshot is now kept in the browser under its own workshop slug, for no longer than the participant session, and a failed refresh leaves the stream, countdown, chat and materials on the last known version instead of replacing the room with an error. A small shared status dot in the upper-right corner now quietly changes from green “Připojeno” to amber while the saved version is shown, keeps the detailed explanation in its tooltip and accessible label, and lets a participant retry immediately; it also serves `/cs/komunita` through the common participant room. The room keeps trying with a jittered exponential backoff rather than sending a whole audience back every half minute, and pauses nonessential presence reports until the service answers again; an authoritative missing-room or missing-session answer removes the private saved copy. The `state-changed` broadcasts already sent for every content and setting change in `/admin/workshops` make the next successful refresh replace that copy, so no second cache or migration is needed there.
changelog\_current-preversion.md:282:- Added a small service worker for the online-workshop funnel: the landing page, confirmation and room prepare the already loaded Next.js assets, and every canonical participant document (`?workshop=…`) is saved network-first under that exact workshop selection. When the application server answers with an overload error or cannot be reached, a visitor who has opened that term before receives their local application shell and its matching room snapshot; prefilled e-mail/name URLs and legacy links without a selected workshop are deliberately never reused as a cached page.
changelog\_current-preversion.md:284:- Added the third, post-workshop stage to `/cs/online-workshop/participant`: when an occurrence ends, the live video is replaced with a thank-you wrap-up, a progressively saved star review (low scores ask what to improve first; high scores ask what helped first), and a direct link to the chosen follow-up material. Chat and reactions remain available throughout. One ordinary Markdown material can now be marked as the follow-up; it is highlighted in the material list before the end and is enforced as a single selection per workshop in the database.
changelog\_current-preversion.md:286:- Added the private `Zpětná vazba` section to `/admin/workshops`, with each rating, partial written response, and its linked Contact context. The same feedback joins the existing contact projection, so `/admin/contacts` and its CSV, vCard, and Book exports show a person's workshop feedback beside their Contact records and attendance history without duplicating source data.
changelog\_current-preversion.md:290:- Added Playwright end-to-end coverage for the public site without touching `/admin`: the smoke test opens the public landing and information routes, while real `@example.com` submissions cover the footer newsletter, generic and business lead dialogs, qualification flow, podcast subscription, Pavol contact form, online workshop registration, AI Supervize Mini registration and its future-term interest form. The public workshop-room connection is covered too when a published room is available. `npm run test-e2e` runs headlessly, `npm run test-e2e:headed` opens the browser, both terminal shortcuts are available, and every run archives timestamped `.webm` recordings under the git-ignored `tests/e2e/videos/` directory. `npm run delete-test-data` is the separate, deliberate garbage collector: it removes `@example.com` Contact and workshop-participant records through `DATABASE_URL`, letting database cascades remove their dependent workshop activity without deleting data at the end of a test run.
changelog\_current-preversion.md:300:- Replaced the stack of coloured bars in the "Přehled" of `/admin/workshops` and `/admin/community` with a real graph of the workshop in time: the audience is drawn across the hour the way a video draws its viewers, next to the messages, reactions, votes and clicks on the materials, all against one axis so that no two scales can invent a relation which is not in the data. Hovering anywhere reads out every line at that moment, dragging over the graph zooms into that span and the wheel zooms back out, and the graph opens exactly on the time of the workshop, from where it can be opened up to everything which happened before it started and after it ended.
changelog\_current-preversion.md:305:- Started remembering who was watching a live room and when, as one row per participant and minute of the presence the room already reports, which is what the audience of the graph is counted from. A workshop which was held before this says so instead of drawing a room nobody watched.
changelog\_current-preversion.md:306:- Made the workshop links of `/cs/komunita` carry the member who clicks them: a room now tells the participant reading it their own e-mail address, so every link it offers arrives at `/cs/online-workshop/participant` with `email` and `fullname` filled in and the workshop welcomes a community member without asking them to type either again. Until now only somebody who had just typed their e-mail into the connection form was handed on, and a member returning to the community on nothing but their session cookie was sent to an empty form — which the calendar invitation of a workshop room, built from the very same identity, was sending to an empty form as well.
changelog\_current-preversion.md:307:- Took the subtitles off the stream of `/cs/online-workshop/participant`: asking the embed address not to turn them on was never enough for a participant whose own YouTube account keeps them on, or for a live broadcast which writes them by itself, so the room now unloads the subtitle module of the player, and keeps asking for the first few seconds because a player which has just appeared on the page is not listening yet.
changelog\_current-preversion.md:308:- Fixed the participants of `/admin/workshops` and `/admin/community` still answering `column reference "fullname" is ambiguous` instead of listing anybody, which took their CSV and vCard exports down with them again: naming the row source of every ordering column mended the migration files but not a database which still held an older body of the paging function, so that function now settles the conflict for the whole of itself at once and drops every signature it ever had, leaving no older copy of it to be called instead.
changelog\_current-preversion.md:310:- Gave the live rooms moderators next to their invisible trusted participants: a moderator of `/cs/online-workshop/participant` or `/cs/komunita` is announced by a badge on their messages and in the header of the room, is shown every message which waits for a decision instead of only their own, and approves, rejects, corrects, or pins any message straight from the chat, while a trusted participant stays invisible with nothing but their own messages being approved as they write them.
changelog\_current-preversion.md:311:- Let that moderator also trust the author of a message or take their interactions away, from the very same message, so a helpful participant stops waiting for moderation and a disruptive one stops reaching the room without anybody leaving the chat.
changelog\_current-preversion.md:312:- Kept the appointment of a moderator with `/admin/workshops` alone, where an administrator makes and unmakes both trusted participants and moderators, filters the audience by either of them, and exports who moderates; a moderator of a room can hand on trust but never their own moderation, and cannot reach a fellow moderator whom a ban would dismiss, which both the room and the administration read from one description of what each moderating role may do.
changelog\_current-preversion.md:313:- Added an AI-context `.book` export to `/admin/contacts`, using the exact same filtered and sorted contact selection as CSV and vCard while preserving every source Contact record, the normalized identity, and each workshop attendance with its join/last-seen times, active duration, comments, reactions, material-link clicks, votes, and moderation state.
changelog\_current-preversion.md:317:- Fixed the participants of `/admin/workshops` answering `column reference "fullname" is ambiguous` instead of listing anybody, which took their CSV and vCard exports down with them: the paging function now says which row source each of its ordering columns comes from, so the database no longer has to choose between a column and the returned value of the very same name.
changelog\_current-preversion.md:318:- Made `/cs/komunita` the permanent room it is: no stage, no countdown towards a start it never had, no participant reactions and no watching count, and no realtime channel or fast poll behind them, so the community catches up calmly instead of pretending to be a live occurrence.
changelog\_current-preversion.md:320:- Described every room kind in one place — whether it is the only one of its kind, keeps a fixed URL, has a schedule, a stage, and live updates — so the participant room, its administration, the routes, and the data loading all ask the same question instead of naming the community, and a room which does not show a live panel neither counts nor broadcasts for it.
changelog\_current-preversion.md:321:- Replaced the workshop selector of `/admin/workshops` with a card for every occurrence, saying when the term is, whether it is upcoming, ongoing or already over, and how many participants it gathered; a running room leads the list, the prepared terms follow it, and the history closes it.
changelog\_current-preversion.md:323:- Added the Czech-only `/cs/komunita` member room with the workshop waiting-room flow, GET-prefilled name and e-mail, moderated chat, timed materials, and identity-prefilled links to every published workshop.
changelog\_current-preversion.md:324:- Added `/admin/community`, reusing the workshop moderation, participants, content, analytics, and settings dashboard while keeping its stable community URL read-only.
changelog\_current-preversion.md:326:- Extended the live-room data model with a database-enforced singleton community kind, keeping it separate from workshop terms and their public/admin lists without duplicating the secure participant-room infrastructure.
changelog\_current-preversion.md:327:- Rebuilt `/admin/workshops` around focused overview, participant, comment, reaction, content, and settings sections; large audiences now have server-side filtering, sorting, paging, per-person activity histories, a workshop-wide activity timeline, and CSV exports for every section plus filtered participant vCards.
changelog\_current-preversion.md:329:- Carried the selected workshop as `?workshop=<slug>` through registration confirmation, direct room links, and calendar invitations; `/cs/online-workshop/participant` and `/cs/online-workshop/dekujeme` now resolve it, while old links without it use the most recent published workshop.
changelog\_current-preversion.md:346:- Joined the private contact and workshop-participant views by normalized e-mail, including case-insensitive and `+tag` aliases: `/admin/contacts` now groups duplicate Contact rows and shows workshop attendance, while workshop participant lists, timelines, CSV exports, and vCards include the matching Contact details such as phone numbers without changing stored data.
changelog\_current-preversion.md:352:- Added the reusable `/cs/online-workshop/participant` live room with participant sessions, a server-synchronized countdown and YouTube stream, timed Markdown materials, moderated chat, upvotes, animated reactions, an RLS-secured Supabase model, and workshop administration from the new `/admin` dashboard.
changelog\_current-preversion.md:353:- Offered the workshop to the calendar of a participant from the `/cs/online-workshop/participant` room as long as it has not started, as a `Karel <> Pavol - Produkční kód s AI agenty` event built from the live workshop details and carrying a prefilled link back into the room, and moved the registration confirmation onto the very same shared calendar code.
changelog\_current-preversion.md:354:- Let a participant of the `/cs/online-workshop/participant` room rename themselves straight from the room header, which also renames the author of the comments they already wrote, and validates the new name by the very same rule as the connection form.
changelog\_current-preversion.md:355:- Let a participant of the `/cs/online-workshop/participant` room answer a message of the chat, so a question and its answers are read as one conversation which the newest-first order lifts by its newest answer, while an answer is moderated, voted, and rate limited exactly like any other message and the moderation of it shows the question it answers.
changelog\_current-preversion.md:356:- Gave every reaction of the `/cs/online-workshop/participant` room a celebration of its own: a thumbs-up hops, a heart beats its way up an S-curve, applause claps just above the edge, fire flickers up a draught, an idea glows and drifts, laughter rolls away, `</>` climbs line by line behind a blinking cursor, sparkles twinkle, a snake slithers, eyes dart around, a party popper throws confetti and a firework explodes, while any other emoji still flies with the animation the room had before and any text an admin sends flies as a readable chip instead of a huge word.
changelog\_current-preversion.md:357:- Composed those animations in one registry out of a flight, a flourish and a decoration, which the stylesheet of the room answers with keyframes the browser runs on the compositor, so a new reaction is one entry and a full stage costs no JavaScript, keeps at most two dozen reactions in the air however many participants react at once, and stops travelling altogether for anyone who asked the system for less motion.
changelog\_current-preversion.md:359:- Kept every participant action in the online workshop attributable to its server-side timestamp: joining the room, comments, likes, reactions, and material-link clicks are recorded for the administration without duplicating the existing event records.
tests\e2e\public-submissions.spec.ts:280:test('connects a public online-workshop participant', async ({ page }) => {
tests\e2e\public-submissions.spec.ts:281:    await page.goto('/cs/online-workshop/participant');
tests\e2e\public-submissions.spec.ts:283:    const fullname = page.locator('#participant-fullname');
tests\e2e\public-submissions.spec.ts:287:            'The configured database has no published workshop room available for participant registration.',
tests\e2e\public-submissions.spec.ts:293:    await page.locator('#participant-email').fill(createE2eTestEmail('workshop-participant'));
app\sitemap.test.ts:11:        expect(entries.map((entry) => entry.url)).not.toContain('https://ptbk.io/cs/online-workshop/participant');
components\legal\PersonalDataConsentNote.tsx:16: * @param children sentence appended after the note, such as what the visitor receives next
components\workshops\workshopReactionAnimations.css:11: * workshop with thousands of participants. `will-change` is deliberately not used: a running transform animation is
components\workshops\workshopReactionAnimations.css:890: * The safety net of a participant who asked the operating system for less motion. The room already resolves such a
components\workshops\WorkshopLinksPanel.tsx:4:import { WorkshopCalendarMonth } from '@/components/workshops/WorkshopCalendarMonth';
components\workshops\WorkshopLinksPanel.tsx:34:    readonly participantIdentity: WorkshopParticipantIdentity;
components\workshops\WorkshopLinksPanel.tsx:56: * A list of the terms of every kind of event, which preserves the already verified identity of the participant in each
components\workshops\WorkshopLinksPanel.tsx:65:    participantIdentity,
components\workshops\WorkshopLinksPanel.tsx:78:        () => createEventListings({ workshops, participantIdentity, currentTimeMilliseconds, timeZone }),
components\workshops\WorkshopLinksPanel.tsx:79:        [workshops, participantIdentity, currentTimeMilliseconds, timeZone],
components\workshops\WorkshopLinksPanel.test.tsx:63:            participantIdentity={PARTICIPANT_IDENTITY}
components\workshops\WorkshopLinksPanel.test.tsx:150:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
components\workshops\WorkshopFlyingReaction.tsx:19:     * Whether the participant asked the operating system to move as little as possible
components\workshops\WorkshopFlyingReaction.tsx:29: *       them a big workshop sends at once.
components\events\EventTermOptionCard.tsx:10: * participant connects to
components\workshops\WorkshopCalendarMonth.tsx:3:import { WorkshopCalendarDay } from '@/components/workshops/WorkshopCalendarDay';
components\workshops\useWorkshopReactionStream.ts:9: * Note: A workshop of thousands of participants sends far more than that, so the oldest reactions make room for the
components\workshops\useWorkshopReactionStream.ts:10: *       newest ones. The stage stays readable and, more importantly, the browser of every single participant animates
components\workshops\useWorkshopReactionStream.ts:47: *       participants clap at once therefore re-renders the stage a few times per second instead of a hundred times.
components\promptbook-qr-code.tsx:18: * shortener and participant rooms use exactly the same QR-code implementation and canvas layout.
components\analytics.tsx:15: * participant room initializes Google Analytics without a page view and sends
components\branding-components-demo.tsx:62:                        participants={SAMPLE_PARTICIPANTS}
components\mocked-chat-section.tsx:18:    const participants = useMemo(
components\mocked-chat-section.tsx:20:            conversation.participants
components\mocked-chat-section.tsx:21:                .filter((participant: any) => participant.name !== 'user')
components\mocked-chat-section.tsx:22:                .map((participant: any) => ({
components\mocked-chat-section.tsx:23:                    ...participant,
components\mocked-chat-section.tsx:24:                    name: participant.name,
components\mocked-chat-section.tsx:25:                    isMe: participant.isMe,
components\mocked-chat-section.tsx:26:                    fullname: participant.fullname || participant.name,
components\mocked-chat-section.tsx:27:                    color: participant.color || '#6B7280',
components\mocked-chat-section.tsx:28:                    avatarSrc: participant.avatar! /* || generatePlaceholderAgentProfileImageUrl(participant.name)*/,
components\mocked-chat-section.tsx:30:        [conversation.participants],
components\mocked-chat-section.tsx:57:                participants={participants}
app\cs\online-workshop\participant\page.tsx:2:import { OnlineWorkshopSelectedTermRoom } from '@/businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom';
app\cs\online-workshop\participant\page.tsx:24:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\online-workshop\participant\page.tsx:44:            initialEmail={participantIdentity.email}
app\cs\online-workshop\participant\page.tsx:45:            initialFullname={participantIdentity.fullname}
lib\community\communityPreviewValues.ts:40:        isAuthorModerator: row.participant_id !== null && moderatorParticipantIds.has(row.participant_id),
lib\community\communityPreviewValues.test.ts:67:                participant_id: 'participant-id',
lib\community\communityPreviewValues.test.ts:72:            new Set(['participant-id']),
lib\community\communityPreviewValues.test.ts:84:                participant_id: null,
lib\community\communityPreviewValues.test.ts:89:            new Set(['participant-id']),
lib\community\communityPreviewValues.test.ts:99:                participant_id: null,
app\cs\online-workshop\dekujeme\page.tsx:21:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\online-workshop\dekujeme\page.tsx:33:    return <OnlineWorkshopThankYouPage workshop={workshop} participantIdentity={participantIdentity} />;
lib\community\communityPreviewDatabase.ts:18:    readonly participant_id: string | null;
lib\community\communityPreviewDatabase.ts:66: * Note: A message the administration created without a participant is deliberately left out, so this total says what
lib\community\communityPreviewDatabase.ts:148:        .select('id, participant_id, author_name, body, created_at')
lib\community\communityPreviewDatabase.ts:172:    participantIds: readonly string[],
lib\community\communityPreviewDatabase.ts:174:    if (participantIds.length === 0) {
lib\community\communityPreviewDatabase.ts:183:        .in('id', participantIds);
lib\community\communityPreview.ts:99:            .map((discussionRow) => discussionRow.participant_id)
lib\community\communityPreview.ts:100:            .filter((participantId): participantId is string => participantId !== null),
lib\calendar\create-calendar-links.test.ts:15:    url: 'https://ptbk.io/cs/online-workshop/participant?email=karel%40firma.cz&fullname=Karel+Nov%C3%A1k',
components\admin\AdminContactDetails.tsx:30:                    <li key={workshopParticipation.participantId} className="border-l-2 border-cyan-200 pl-2">
app\cs\komunita\page.tsx:31:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\komunita\page.tsx:40:            initialEmail={participantIdentity.email}
app\cs\komunita\page.tsx:41:            initialFullname={participantIdentity.fullname}
lib\metadata\structured-data.ts:196: * for the private participant room.
app\cs\komunita\clenstvi\page.tsx:19:    const participantIdentity = readWorkshopParticipantIdentity(
app\cs\komunita\clenstvi\page.tsx:35:            initialFullname={participantIdentity.fullname}
app\cs\komunita\clenstvi\page.tsx:36:            initialEmail={participantIdentity.email}
app\cs\komunita\calendar.ics\route.test.ts:95:            'URL:https://ptbk.io/cs/online-workshop/participant?workshop=production-ai-2026-09-10',
app\branding\page.tsx:103:                                    presentations, event materials, partner pages, and articles.
lib\legal\termsAndConditionsDocuments.tsx:192:                    'The membership page describes the free community and the current paid membership. Live online AI webinars remain free; paid membership primarily gives access to recordings, materials, and additional content.',
lib\legal\termsAndConditionsDocuments.tsx:218:                    'The content of the website, the workshop materials, texts, graphics, and software are protected by copyright and belong to the operator or to its partners.',
lib\legal\termsAndConditionsDocuments.tsx:219:                    'You may use the materials you receive from us for your own needs and for the needs of your company. You must not distribute, sell, or build your own training on them without our written consent.',
lib\admin\adminContactJoin.ts:5: * One workshop attendance that belongs to an admin contact identity.
lib\admin\adminContactJoin.ts:7: * Note: This is a read model assembled only for the administration. The workshop participant and Contact rows stay
lib\admin\adminContactJoin.ts:11:    readonly participantId: string;
lib\admin\adminContactJoin.ts:16:    readonly workshopEndsAt: string | null;
lib\admin\adminContactJoin.ts:30: * One post-workshop review in the same private identity projection as Contact rows and workshop attendances.
lib\admin\adminContactJoin.ts:32: * The feedback remains stored against its workshop participant. This is only the admin read model which lets a
lib\admin\adminContactJoin.ts:41:    readonly workshopEndsAt: string | null;
lib\admin\adminContactJoin.ts:42:    readonly participantId: string;
lib\admin\adminContactJoin.ts:132: * Group Contact rows and workshop attendances by their normalized e-mail identity.
lib\admin\adminContactJoin.ts:333: * A complete, readable representation of all workshop attendances in a group for exports.
lib\admin\adminContactJoin.ts:346:                `Workshop ends at: ${workshopParticipation.workshopEndsAt ?? ''}`,
lib\admin\adminContactJoin.ts:347:                `Participant ID: ${workshopParticipation.participantId}`,
lib\admin\adminContactJoin.ts:377:                `Participant ID: ${workshopFeedback.participantId}`,
lib\legal\privacyPolicyDocuments.tsx:169:                    'Data about an event registration: the chosen date, the number of participants, the format, and any discount code.',
lib\legal\privacyPolicyDocuments.tsx:179:                    'To register you for a workshop or another event, issue an invoice, and send you the materials - based on the performance of the contract and our legal obligations.',
lib\admin\adminContactJoin.test.ts:40:    participantId: 'participant-1',
lib\admin\adminContactJoin.test.ts:45:    workshopEndsAt: '2026-08-20T18:30:00.000Z',
lib\admin\adminContactJoin.test.ts:64:    workshopEndsAt: WORKSHOP_PARTICIPATION.workshopEndsAt,
lib\admin\adminContactJoin.test.ts:65:    participantId: WORKSHOP_PARTICIPATION.participantId,
lib\admin\adminContactJoin.test.ts:84:    it('groups duplicate Contact rows and workshop attendance into one person without losing the source records', () => {
lib\admin\adminContactJoin.test.ts:127:            [{ ...WORKSHOP_PARTICIPATION, participantId: 'participant-without-email', email: '' }],
lib\admin\adminContactJoin.test.ts:134:    it('includes workshop attendance in both grouped contact export formats', () => {
lib\admin\adminContactDatabase.ts:23:    'id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at';
lib\admin\adminContactDatabase.ts:48:    readonly participant_id: string;
lib\admin\adminContactDatabase.ts:59:    readonly participant_id: string;
lib\admin\adminContactDatabase.ts:79:     * Whether this screen needs the complete cross-workshop attendance history, rather than only Contact-table fields.
lib\admin\adminContactDatabase.ts:94:function createWorkshopParticipantActivityTotalsKey(workshopId: string, participantId: string): string {
lib\admin\adminContactDatabase.ts:95:    return `${workshopId}:${participantId}`;
lib\admin\adminContactDatabase.ts:99:    participantRow: AdminContactWorkshopParticipantRow,
lib\admin\adminContactDatabase.ts:103:    const workshopRow = workshopById.get(participantRow.workshop_id);
lib\admin\adminContactDatabase.ts:109:        createWorkshopParticipantActivityTotalsKey(participantRow.workshop_id, participantRow.id),
lib\admin\adminContactDatabase.ts:113:        participantId: participantRow.id,
lib\admin\adminContactDatabase.ts:118:        workshopEndsAt: workshopRow.ends_at,
lib\admin\adminContactDatabase.ts:119:        fullname: participantRow.fullname,
lib\admin\adminContactDatabase.ts:120:        email: participantRow.email,
lib\admin\adminContactDatabase.ts:121:        connectedAt: participantRow.connected_at,
lib\admin\adminContactDatabase.ts:122:        lastSeenAt: participantRow.last_seen_at,
lib\admin\adminContactDatabase.ts:123:        activeDurationSeconds: participantRow.active_duration_seconds,
lib\admin\adminContactDatabase.ts:127:        isInteractionBanned: participantRow.is_interaction_banned,
lib\admin\adminContactDatabase.ts:128:        isTrusted: participantRow.is_trusted,
lib\admin\adminContactDatabase.ts:135:    participantById: ReadonlyMap<string, AdminContactWorkshopParticipantRow>,
lib\admin\adminContactDatabase.ts:138:    const participantRow = participantById.get(feedbackRow.participant_id);
lib\admin\adminContactDatabase.ts:139:    if (workshopRow === undefined || participantRow === undefined) {
lib\admin\adminContactDatabase.ts:149:        workshopEndsAt: workshopRow.ends_at,
lib\admin\adminContactDatabase.ts:150:        participantId: participantRow.id,
lib\admin\adminContactDatabase.ts:151:        fullname: participantRow.fullname,
lib\admin\adminContactDatabase.ts:152:        email: participantRow.email,
lib\admin\adminContactDatabase.ts:163: * Read the already-aggregated activity of every workshop participant in one stable, paged query.
lib\admin\adminContactDatabase.ts:165: * The workshop participant page uses the very same database aggregate for one workshop. This multi-workshop variant
lib\admin\adminContactDatabase.ts:182:                .rpc('get_workshop_participant_activity_totals_for_workshops', {
lib\admin\adminContactDatabase.ts:185:                .order('workshop_id', { ascending: true })
lib\admin\adminContactDatabase.ts:186:                .order('participant_id', { ascending: true })
lib\admin\adminContactDatabase.ts:188:        '`get_workshop_participant_activity_totals_for_workshops`',
lib\admin\adminContactDatabase.ts:195: * Read the workshop source rows needed by the admin-only contact projection once, then compose both attendance and
lib\admin\adminContactDatabase.ts:212:    const [workshopRowsResult, participantRowsResult, feedbackRowsResult] = await Promise.all([
lib\admin\adminContactDatabase.ts:245:        workshopRowsResult.errorMessage ?? participantRowsResult.errorMessage ?? feedbackRowsResult.errorMessage;
lib\admin\adminContactDatabase.ts:246:    if (workshopRowsResult.rows === null || participantRowsResult.rows === null || feedbackRowsResult.rows === null) {
lib\admin\adminContactDatabase.ts:253:    const participantById = new Map<string, AdminContactWorkshopParticipantRow>(
lib\admin\adminContactDatabase.ts:254:        participantRowsResult.rows.map((participantRow) => [participantRow.id, participantRow] as const),
lib\admin\adminContactDatabase.ts:272:                        activityTotals.participant_id,
lib\admin\adminContactDatabase.ts:279:        ? participantRowsResult.rows
lib\admin\adminContactDatabase.ts:280:              .map((participantRow) =>
lib\admin\adminContactDatabase.ts:281:                  mapAdminWorkshopParticipation(participantRow, workshopById, activityTotalsByParticipationKey),
lib\admin\adminContactDatabase.ts:289:        .map((feedbackRow) => mapAdminWorkshopFeedback(feedbackRow, workshopById, participantById))
app\api\workshops\[workshopSlug]\state\route.ts:22:        authenticatedRequest.participant,
lib\admin\adminContactDatabase.test.ts:68:        const participantQuery = createPagedQuery([
lib\admin\adminContactDatabase.test.ts:70:                id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:84:                participant_id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:94:                participant_id: 'participant-1',
lib\admin\adminContactDatabase.test.ts:110:                return participantQuery;
lib\admin\adminContactDatabase.test.ts:133:        expect(rpc).toHaveBeenCalledWith('get_workshop_participant_activity_totals_for_workshops', {
app\api\workshops\[workshopSlug]\repository\route.ts:12: * How far the project of this workshop has come, read for the participants who are in its room
app\api\workshops\[workshopSlug]\repository\route.ts:15: *       delays nothing but the panel which is about GitHub. Every participant reads one and the same server-side
lib\conversations-data.ts:19:    participants: ConversationParticipant[];
app\api\workshops\[workshopSlug]\reactions\route.ts:45:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\reactions\route.ts:57:        authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\presence\route.ts:30:    const { error } = await authenticatedRequest.supabase.rpc('record_workshop_participant_presence', {
app\api\workshops\[workshopSlug]\presence\route.ts:32:        target_participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\presence\route.ts:37:        console.error('Failed to record workshop participant presence:', error.message);
lib\language\pageLanguage.test.ts:15:        expect(getLanguageFromPathname('/cs/online-workshop/participant')).toBe('cs');
lib\language\pageLanguage.test.ts:16:        expect(getLanguageFromPathname('/ai-supervize-mini/participant')).toBe('cs');
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:44:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:52:        authenticatedRequest.participant,
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:61:            return NextResponse.json({ error: 'Poll has already ended' }, { status: 409 });
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.ts:71:        authenticatedRequest.participant.email,
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:53:const PARTICIPANT = { id: 'participant-id', email: 'jana@example.com', isInteractionBanned: false };
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:82:describe('workshop-attached community poll voting endpoint', () => {
app\api\workshops\[workshopSlug]\polls\[pollId]\votes\route.test.ts:97:            participant: PARTICIPANT,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:17:    readonly params: Promise<{ readonly workshopSlug: string; readonly participantId: string }>;
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:21: * Trusts one participant of the room or takes their interactions away, straight from the chat
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:38:    const { workshopSlug, participantId } = await context.params;
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:52:        participantId,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:55:        console.error('Failed to read a moderated workshop participant:', moderatedParticipant.errorMessage);
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:58:    if (moderatedParticipant.participant === null) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:61:    if (!isWorkshopParticipantModeratedBy('moderator', moderatedParticipant.participant)) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:68:    const { participant, errorMessage } = await updateModeratedWorkshopParticipant(
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:71:        participantId,
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:75:        console.error('Failed to moderate a workshop participant from the room:', errorMessage);
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:78:    if (participant === null) {
app\api\workshops\[workshopSlug]\participants\[participantId]\route.ts:86:    return NextResponse.json(participant, { headers: { 'Cache-Control': 'no-store' } });
app\api\workshops\[workshopSlug]\participant\route.ts:33:    // A banned participant keeps the name they were moderated under, because renaming would also rewrite the author
app\api\workshops\[workshopSlug]\participant\route.ts:35:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\participant\route.ts:43:        authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\participant\route.ts:50:    // Nobody else needs a fresh snapshot when the new name stayed inside the room of the renamed participant.
app\api\workshops\[workshopSlug]\participant\route.ts:57:    const response = NextResponse.json({ participant: renameResult.participant });
app\api\workshops\[workshopSlug]\participant\route.ts:63: * Signs the participant out of this room, which is the way back out of a room they connected to themselves.
app\api\workshops\[workshopSlug]\participant\route.ts:65: * Note: A participant who may not interact is signed out like anybody else. Their ban holds their name and their
app\api\workshops\[workshopSlug]\participant\route.ts:84:            authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\participant\route.ts:87:            // The cookie is deliberately left alone, so a participant told that signing out failed is really still in.
lib\community-membership\communityMembershipTypes.ts:79:    readonly participantId: string;
lib\github\githubCommitApi.ts:60: * Reads the small part of one GitHub REST commit response which the participant room can safely show
lib\contacts\serializeContactsAsBook.ts:33: * Every CONTACT keeps all source Contact records, workshop attendance, and feedback, rather than merely the merged
lib\contacts\serializeContactsAsBook.ts:42:        `NOTE These are the contacts exported from the ${SITE_NAME} contacts administration. Each CONTACT contains all known contact records, workshop attendance activity, and workshop feedback at the time of export.`,
lib\community-membership\communityMembershipSubscription.test.ts:44:    it('does not offer a reactivation after Stripe has already ended the subscription', () => {
lib\contacts\serializeContactsAsBook.test.ts:25:    participantId: 'participant-1',
lib\contacts\serializeContactsAsBook.test.ts:30:    workshopEndsAt: '2026-08-20T18:30:00.000Z',
lib\github\fetchGithubRepository.ts:139: * repository has many active branches. Each URL is still cached by `fetchCachedText`, so participants do not multiply
lib\community-membership\communityMembershipStatus.ts:8: *       membership somebody is paying for, while everything the gate has given up on is a membership which ended.
lib\community-membership\communityMembershipRedemption.ts:74:        requestedByParticipantId: member.participantId,
lib\exports\workshopWrapUpPdf.ts:294:    if (document.materials.length === 0) {
lib\exports\workshopWrapUpPdf.ts:297:        document.materials.forEach((material) => addPdfMaterial(renderState, material));
lib\exports\workshopWrapUpPdf.ts:348: * Renders the participant-safe wrap-up into an A4 PDF in the browser.
lib\exports\workshopWrapUpPdf.ts:350: * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
lib\exports\workshopWrapUpPdf.ts:351: * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
lib\community-membership\communityMembershipRedemption.test.ts:26:    participantId: '00000000-0000-0000-0000-000000000001',
lib\community-membership\communityMembershipRedemption.test.ts:70:            requestedByParticipantId: MEMBER.participantId,
lib\community-membership\communityMembershipDatabase.ts:211: * Loads all paid-membership states needed by one page of community participants in one query. The membership is keyed
lib\community-membership\communityMembershipDatabase.ts:235:            errorMessage: reportSupabaseError('the membership statuses of community participants', error),
lib\community-membership\communityMembershipDatabase.ts:312:        requested_by_participant_id: values.requestedByParticipantId,
lib\community-membership\communityMembershipCheckout.ts:52:    participantId: 'communityParticipantId',
lib\community-membership\communityMembershipCheckout.ts:101:        [COMMUNITY_MEMBERSHIP_METADATA_KEYS.participantId]: member.participantId,
lib\community-membership\communityMembershipCheckout.ts:163:        client_reference_id: member.participantId,
lib\community-membership\communityMembershipCheckout.test.ts:15:    participantId: '00000000-0000-0000-0000-000000000001',
lib\community-membership\communityMembershipCheckout.test.ts:128:const WORKSHOP_ROOM_URL = 'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty';
lib\community-membership\communityMembershipCheckout.test.ts:142:            'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty' +
lib\community-membership\communityMembershipCheckout.test.ts:146:            'https://ptbk.io/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty&membership=cancelled',
lib\contacts\contacts.test.ts:233:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
lib\contacts\contacts.test.ts:238:                userNote: JSON.stringify({ selectedDateId: '2026-08-26', participantCount: 3 }),
lib\contacts\contacts.test.ts:248:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
lib\contacts\contacts.test.ts:253:                userNote: JSON.stringify({ selectedDateId: 'online-workshop-2026-09-02', participantCount: 1 }),
lib\contacts\contacts.test.ts:277:                                participantCount: 1,
lib\community-membership\communityMembershipAdminQuery.ts:103: * administration, so this list never competes with the participant list or overview graph for a shared-link value.
lib\contacts\contactColumnDefinitions.ts:79: * Width of the admin-only workshop attendance column, which is derived from another source rather than Contact fields.
lib\community-membership\communityMembershipAdminLinks.ts:6:export type CommunityMembershipAdminTab = 'participants' | 'memberships';
lib\community-membership\communityMembershipAdminLinks.ts:9: * Builds the two-way handoff between a community participant and their durable membership. A participant is a room
lib\community-membership\communityMembershipAdminLinks.test.ts:8:    it('uses the durable normalized e-mail to open a membership from its community participant', () => {
lib\community-membership\communityMembershipAdminLinks.test.ts:14:    it('opens the participant side without inventing a member target when one was not selected', () => {
lib\community-membership\communityMembershipAdminLinks.test.ts:15:        expect(createCommunityMembershipAdminPath('participants')).toBe('/admin/community?tab=participants');
app\api\workshops\[workshopSlug]\membership\route.ts:30:        authenticatedRequest.participant.email,
lib\events\eventTypes.ts:48:    readonly participantPath: string | null;
lib\events\eventTypes.ts:63:        participantPath: ONLINE_WORKSHOP_PARTICIPANT_PATH,
lib\events\eventTypes.ts:70:        participantPath: null,
app\api\workshops\[workshopSlug]\membership\portal\route.ts:44:    const { participant, supabase, workshopRow } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\portal\route.ts:45:    const membershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:52:    maximum_participant_count: null,
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:66:    return { participant: { email: 'jana@example.com' }, supabase: SUPABASE, workshopRow };
app\api\workshops\[workshopSlug]\membership\portal\route.test.ts:149:            return_url: 'https://promptbook.studio/cs/online-workshop/participant?workshop=produkcni-kod-s-ai-agenty',
lib\events\eventListing.ts:45:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\events\eventListing.ts:66:    participantIdentity,
lib\events\eventListing.ts:71:        const link = createEventLinkOrNull(workshop, participantIdentity);
lib\events\eventListing.test.ts:54:        participantIdentity: PARTICIPANT_IDENTITY,
lib\events\eventListing.test.ts:72:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
lib\events\eventLinks.ts:21:    participantIdentity: WorkshopParticipantIdentity,
lib\events\eventLinks.ts:27:    const { participantPath, landingPagePath } = getEventTypeDefinition(workshop.event.type);
lib\events\eventLinks.ts:29:    return participantPath === null
lib\events\eventLinks.ts:31:        : createWorkshopRoomLink(participantPath, participantIdentity, workshop.slug);
lib\community-projects\communityProjectTypes.ts:11: * `pending`, while the shared participant-submission policy may auto-approve a trusted user or a moderator.
lib\community-projects\communityProjectTypes.ts:37: * participant identity; members only need the display name of its author and their own vote. A pending card reaches
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:162:            requestedByParticipantId: member.participantId,
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:207:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:208:    const currentMembership = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\checkout\route.ts:232:        member: { participantId: participant.id, fullname: participant.fullname, email: participant.email },
lib\events\eventCalendar.ts:8:import { getWorkshopCalendarEndsAt } from '@/lib/workshops/workshopCalendar';
lib\events\eventCalendar.ts:36:export function createPublicEventCalendarEventOrNull(workshop: WorkshopSummary): CalendarEvent | null {
lib\community-projects\communityProjectDatabase.ts:17:    'id, author_community_participant_id, discussion_workshop_id, url, title, description, preview_image_url, status, upvote_count, downvote_count, created_at';
lib\community-projects\communityProjectDatabase.ts:23:    readonly author_community_participant_id: string;
lib\community-projects\communityProjectDatabase.ts:75:        (project.author_community_participant_id === communityParticipant.id ||
lib\community-projects\communityProjectDatabase.ts:100:            query = query.eq('author_community_participant_id', authorCommunityParticipantId);
lib\community-projects\communityProjectDatabase.ts:187:            .eq('community_participant_id', communityParticipantId)
lib\community-projects\communityProjectDatabase.ts:239:            projectRows.map((projectRow) => projectRow.author_community_participant_id),
lib\community-projects\communityProjectDatabase.ts:271:                    authorName: authorsResult.authorNameById.get(projectRow.author_community_participant_id) ?? 'Člen komunity',
lib\community-projects\communityProjectDatabase.ts:285: * Lists approved cards by their positive vote total. A participant receives their own pending project as well, while
lib\community-projects\communityProjectService.ts:40:    readonly author_community_participant_id: string;
lib\community-projects\communityProjectService.ts:61:    readonly discussion_participant_id: string;
lib\community-projects\communityProjectService.ts:70:    'not-found' | 'member-invalid' | 'community-participant-missing' | 'discussion-participant-invalid';
lib\community-projects\communityProjectService.ts:75:    'key-share': 'FOR KEY SHARE OF community_participant',
lib\community-projects\communityProjectService.ts:76:    update: 'FOR UPDATE OF community_participant',
lib\community-projects\communityProjectService.ts:157:                community_participant.id,
lib\community-projects\communityProjectService.ts:158:                community_participant.fullname,
lib\community-projects\communityProjectService.ts:159:                community_participant.email,
lib\community-projects\communityProjectService.ts:160:                community_participant.is_interaction_banned,
lib\community-projects\communityProjectService.ts:161:                community_participant.is_trusted,
lib\community-projects\communityProjectService.ts:162:                community_participant.is_moderator
lib\community-projects\communityProjectService.ts:163:            FROM ${WORKSHOP_PARTICIPANT_TABLE_REFERENCE} AS community_participant
lib\community-projects\communityProjectService.ts:165:                ON community_room.id = community_participant.workshop_id
lib\community-projects\communityProjectService.ts:166:            WHERE community_participant.id = $1
lib\community-projects\communityProjectService.ts:198:                community_project.author_community_participant_id
lib\community-projects\communityProjectService.ts:265:              AND community_participant_id = $2
lib\community-projects\communityProjectService.ts:288:            WHERE community_project_vote.community_participant_id = $1
lib\community-projects\communityProjectService.ts:305:            SELECT discussion_participant_id
lib\community-projects\communityProjectService.ts:308:              AND community_participant_id = $2
lib\community-projects\communityProjectService.ts:385:                community_participant_id,
lib\community-projects\communityProjectService.ts:386:                discussion_participant_id,
lib\community-projects\communityProjectService.ts:458:                author_community_participant_id,
lib\community-projects\communityProjectService.ts:520: * two browser tabs cannot create two discussion participants for the same member.
lib\community-projects\communityProjectService.ts:529:    const isProjectAuthor = projectDiscussion.author_community_participant_id === communityParticipantId;
lib\community-projects\communityProjectService.ts:535:            existingMapping.discussion_participant_id,
lib\community-projects\communityProjectService.ts:540:                'discussion-participant-invalid',
lib\community-projects\communityProjectService.ts:625:        throw new CommunityProjectMutationError('community-participant-missing', 'COMMUNITY_PROJECT_MEMBER_NOT_FOUND');
lib\community-projects\communityProjectService.ts:635:            WHERE community_participant_id = $1;
lib\community-projects\communityProjectService.ts:662:        throw new CommunityProjectMutationError('community-participant-missing', 'COMMUNITY_PROJECT_MEMBER_NOT_FOUND');
lib\community-projects\communityProjectService.ts:669:    readonly participantId: string | null;
lib\community-projects\communityProjectService.ts:674:        const participantId = await runDatabaseTransaction(COMMUNITY_PROJECT_OPERATION_NAME, (transaction) =>
lib\community-projects\communityProjectService.ts:678:        return { participantId, errorMessage: null, isParticipantMissing: false };
lib\community-projects\communityProjectService.ts:681:            error instanceof CommunityProjectMutationError && error.kind === 'community-participant-missing';
lib\community-projects\communityProjectService.ts:682:        return { participantId: null, errorMessage: getMutationErrorMessage(error), isParticipantMissing };
lib\community-projects\communityProjectService.ts:707:                    community_participant_id,
lib\community-projects\communityProjectService.ts:721:                  AND community_participant_id = $2;
lib\community-projects\communityProjectService.ts:732:                  AND community_participant_id = $2;
lib\community-projects\communityProjectConstants.ts:3:export const COMMUNITY_PROJECT_DISCUSSION_PARTICIPANT_TABLE_NAME = 'community_project_discussion_participants';
app\api\workshops\[workshopSlug]\membership\checkout\confirmation\route.ts:52:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\checkout\confirmation\route.ts:62:    if (checkoutMemberEmail !== normalizeCommunityMemberEmail(participant.email)) {
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:60:    const { participant, supabase } = authenticatedRequest;
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:61:    const membershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\cancellation\route.ts:87:    const refreshedMembershipResult = await loadCommunityMembershipByEmail(supabase, participant.email);
app\api\workshops\[workshopSlug]\membership\cancellation\route.test.ts:50:    participant: { email: 'jana@example.com' },
app\api\workshops\[workshopSlug]\feedback\route.ts:16: * Persists one step of the participant's post-workshop feedback.
app\api\workshops\[workshopSlug]\feedback\route.ts:18: * A score initializes the one row for a participant. The optional text fields deliberately update that row afterwards,
app\api\workshops\[workshopSlug]\feedback\route.ts:19: * allowing each answer to survive independently if the participant does not continue through the whole form.
app\api\workshops\[workshopSlug]\feedback\route.ts:46:        return NextResponse.json({ error: 'Feedback is available after the workshop ends' }, { status: 403 });
app\api\workshops\[workshopSlug]\feedback\route.ts:56:                  .eq('participant_id', authenticatedRequest.participant.id)
app\api\workshops\[workshopSlug]\feedback\route.ts:62:                          participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\feedback\route.ts:65:                      { onConflict: 'workshop_id,participant_id' },
app\api\workshops\[workshopSlug]\feedback\route.ts:67:    const { data, error } = await feedbackQuery.select('id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at').maybeSingle();
lib\workshops\workshopAdminSections.ts:9:    'participants',
lib\supabase\reportSupabaseError.test.ts:28:        expect(reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR)).toBe(
lib\supabase\reportSupabaseError.test.ts:36:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR);
lib\supabase\reportSupabaseError.test.ts:39:        expect(report).toContain('`get_workshop_admin_participant_page`');
lib\supabase\reportSupabaseError.test.ts:48:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR);
lib\supabase\reportSupabaseError.test.ts:67:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR, {
lib\supabase\reportSupabaseError.test.ts:81:        reportSupabaseError('`get_workshop_admin_participant_page`', AMBIGUOUS_COLUMN_ERROR, {
lib\workshops\workshopAdminParticipantQuery.ts:92: * Reads the participant list view out of an API or export URL.
lib\workshops\workshopAdminParticipantQuery.ts:125: * Writes the filters and sort in a form shared by the paged API and participant exports.
app\api\workshops\[workshopSlug]\connect\route.ts:58:    // participant authentication request when many attendees join at once.
app\api\workshops\[workshopSlug]\connect\route.ts:62:        connection.participant,
app\api\workshops\[workshopSlug]\connect\route.ts:67:        // endpoint without creating a duplicate participant row.
lib\tracking\trackingExclusions.ts:17: * Admin URLs carry a bearer token and the participant URL can carry personal
lib\tracking\trackingExclusions.ts:25: * The participant room sends only explicit anonymized interaction events to
lib\workshops\workshopAdminParticipantQuery.test.ts:8:describe('workshop admin participant query', () => {
lib\tracking\trackingExclusions.test.ts:10:    it('excludes admin and workshop participant pages', () => {
lib\tracking\trackingExclusions.test.ts:13:        expect(isThirdPartyTrackingAllowed('/cs/online-workshop/participant')).toBe(false);
lib\tracking\trackingExclusions.test.ts:17:    it('allows anonymized Google Analytics events from the participant room without a page view', () => {
lib\tracking\trackingExclusions.test.ts:19:        expect(isGoogleAnalyticsAllowed('/cs/online-workshop/participant')).toBe(true);
lib\tracking\trackingExclusions.test.ts:20:        expect(isGoogleAnalyticsPageViewAllowed('/cs/online-workshop/participant')).toBe(false);
lib\workshops\workshopAdminExports.ts:21:    'participants',
lib\workshops\workshopAdminExports.ts:22:    'participants-vcard',
lib\workshops\workshopAdminExports.ts:35:    readonly participantFullname: string | null;
lib\workshops\workshopAdminExports.ts:36:    readonly participantEmail: string | null;
lib\workshops\workshopAdminExports.ts:130:export function serializeWorkshopAdminParticipantsAsCsv(participants: readonly WorkshopAdminParticipant[]): string {
lib\workshops\workshopAdminExports.ts:131:    return serializeRowsAsCsv(participants, [
lib\workshops\workshopAdminExports.ts:132:            { header: 'Jméno', getValue: (participant) => participant.fullname },
lib\workshops\workshopAdminExports.ts:133:            { header: 'E-mail', getValue: (participant) => participant.email },
lib\workshops\workshopAdminExports.ts:134:            { header: 'Registrace', getValue: (participant) => participant.connectedAt },
lib\workshops\workshopAdminExports.ts:135:            { header: 'Naposledy aktivní', getValue: (participant) => participant.lastSeenAt },
lib\workshops\workshopAdminExports.ts:136:            { header: 'Čas v místnosti (s)', getValue: (participant) => participant.activeDurationSeconds },
lib\workshops\workshopAdminExports.ts:137:            { header: 'Komentáře', getValue: (participant) => participant.commentCount },
lib\workshops\workshopAdminExports.ts:138:            { header: 'Reakce', getValue: (participant) => participant.reactionCount },
lib\workshops\workshopAdminExports.ts:139:            { header: 'Hlasy', getValue: (participant) => participant.upvoteCount },
lib\workshops\workshopAdminExports.ts:140:            { header: 'Důvěryhodný', getValue: (participant) => formatBoolean(participant.isTrusted) },
lib\workshops\workshopAdminExports.ts:141:            { header: 'Moderátor', getValue: (participant) => formatBoolean(participant.isModerator) },
lib\workshops\workshopAdminExports.ts:142:            { header: 'Interakce zakázány', getValue: (participant) => formatBoolean(participant.isInteractionBanned) },
lib\workshops\workshopAdminExports.ts:145:                getValue: (participant) => getAdminContactPhoneNumbers(participant.contactGroup).join(', '),
lib\workshops\workshopAdminExports.ts:149:                getValue: (participant) => formatAdminContactRecords(participant.contactGroup),
lib\workshops\workshopAdminExports.ts:153:                getValue: (participant) => formatAdminWorkshopParticipations(participant.contactGroup),
lib\workshops\workshopAdminExports.ts:160:    participants: readonly WorkshopAdminParticipant[],
lib\workshops\workshopAdminExports.ts:163:        participants.map((participant) => {
lib\workshops\workshopAdminExports.ts:164:            const phoneNumbers = getAdminContactPhoneNumbers(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:165:            const contactRecords = formatAdminContactRecords(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:166:            const workshopParticipations = formatAdminWorkshopParticipations(participant.contactGroup);
lib\workshops\workshopAdminExports.ts:169:                uid: `workshop-participant-${participant.id}`,
lib\workshops\workshopAdminExports.ts:170:                fullname: participant.fullname,
lib\workshops\workshopAdminExports.ts:171:                email: participant.email,
lib\workshops\workshopAdminExports.ts:175:                    `Registrace: ${participant.connectedAt}`,
lib\workshops\workshopAdminExports.ts:176:                    `Čas v místnosti: ${participant.activeDurationSeconds} s`,
lib\workshops\workshopAdminExports.ts:182:                revision: participant.lastSeenAt,
lib\workshops\workshopAdminExports.ts:192:            { header: 'ID účastníka', getValue: (comment) => comment.participantId },
lib\workshops\workshopAdminExports.ts:210:            { header: 'Jméno účastníka', getValue: (reaction) => reaction.participantFullname },
lib\workshops\workshopAdminExports.ts:211:            { header: 'E-mail účastníka', getValue: (reaction) => reaction.participantEmail },
lib\workshops\workshopAdminExports.ts:235:            { header: 'Nově připojení', getValue: (point) => point.participantCount },
lib\workshops\workshopAdminExports.ts:243:                    point.participantCount +
lib\workshops\workshopAdminExports.ts:263:    const fileExtension = exportKind === 'participants-vcard' ? 'vcf' : 'csv';
lib\workshops\workshopAdminExports.ts:271:        readonly participants?: readonly WorkshopAdminParticipant[];
lib\workshops\workshopAdminExports.ts:285:        case 'participants':
lib\workshops\workshopAdminExports.ts:287:                content: serializeWorkshopAdminParticipantsAsCsv(options.participants ?? []),
lib\workshops\workshopAdminExports.ts:291:        case 'participants-vcard':
lib\workshops\workshopAdminExports.ts:293:                content: serializeWorkshopAdminParticipantsAsVcard(options.workshop, options.participants ?? []),
lib\community-projects\communityProjectService.test.ts:15:const COMMUNITY_PARTICIPANT_QUERY_FRAGMENT = 'FROM public.workshop_participants AS community_participant';
lib\community-projects\communityProjectService.test.ts:112:        const participantInsert = findQueryCall(queryCalls, 'INSERT INTO public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:113:        expect(participantInsert.values[2]).toBe('Ada Lovelace');
lib\community-projects\communityProjectService.test.ts:114:        expect(participantInsert.values[3]).toBe('ada@example.com');
lib\community-projects\communityProjectService.test.ts:115:        expect(participantInsert.values[4]).toMatch(/^[a-f0-9]{64}$/);
lib\community-projects\communityProjectService.test.ts:116:        expect(participantInsert.values[5]).toBe(true);
lib\community-projects\communityProjectService.test.ts:120:            'INSERT INTO public.community_project_discussion_participants',
lib\community-projects\communityProjectService.test.ts:124:        expect(discussionMappingInsert.values[2]).toBe(participantInsert.values[0]);
lib\community-projects\communityProjectService.test.ts:151:    it('refuses to create a project for a participant outside the permanent community', async () => {
lib\community-projects\communityProjectService.test.ts:180:                        author_community_participant_id: communityParticipantId,
lib\community-projects\communityProjectService.test.ts:184:            if (queryText.includes('SELECT discussion_participant_id')) {
lib\community-projects\communityProjectService.test.ts:185:                return [{ discussion_participant_id: discussionParticipantId }];
lib\community-projects\communityProjectService.test.ts:202:        const participantUpdate = findQueryCall(queryCalls, 'UPDATE public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:203:        expect(participantUpdate.values).toEqual([
lib\community-projects\communityProjectService.test.ts:211:        expect(queryCalls.some(({ queryText }) => queryText.includes('INSERT INTO public.workshop_participants'))).toBe(
lib\community-projects\communityProjectService.test.ts:234:                        author_community_participant_id: '55555555-5555-4555-8555-555555555555',
lib\community-projects\communityProjectService.test.ts:247:        const participantInsert = findQueryCall(queryCalls, 'INSERT INTO public.workshop_participants');
lib\community-projects\communityProjectService.test.ts:250:            'INSERT INTO public.community_project_discussion_participants',
lib\community-projects\communityProjectService.test.ts:253:        expect(connection.discussionParticipantId).toBe(participantInsert.values[0]);
lib\community-projects\communityProjectService.test.ts:254:        expect(participantInsert.values[5]).toBe(false);
lib\community-projects\communityProjectService.test.ts:325:    it('reconciles every vote before deleting a community participant', async () => {
lib\community-projects\communityProjectService.test.ts:339:            if (queryText.includes('DELETE FROM public.workshop_participants')) {
lib\community-projects\communityProjectService.test.ts:350:        expect(findQueryCall(queryCalls, 'FOR UPDATE OF community_participant')).toBeDefined();
lib\community-projects\communityProjectService.test.ts:363:        expect(findQueryCall(queryCalls, 'DELETE FROM public.workshop_participants').values).toEqual([
lib\workshops\workshopCalendar.test.ts:7:} from '@/lib/workshops/workshopCalendar';
lib\workshops\workshopCalendar.test.ts:18:const PARTICIPANT_PATH = '/cs/online-workshop/participant';
lib\workshops\workshopCalendar.test.ts:20:describe('workshop calendar event', () => {
lib\workshops\workshopCalendar.test.ts:21:    it('names the event as a meeting of the participant with the host', () => {
lib\workshops\workshopCalendar.test.ts:27:    it('leaves out an unknown participant instead of naming an empty side of the meeting', () => {
lib\workshops\workshopCalendar.test.ts:34:    it('points the event to the participant room with the details of the participant prefilled', () => {
lib\workshops\workshopCalendar.test.ts:38:            participantIdentity: { email: 'karel@firma.cz', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:39:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopCalendar.test.ts:52:    it('falls back to the plain participant room when the identity cannot prefill the connection form', () => {
lib\workshops\workshopCalendar.test.ts:56:            participantIdentity: { email: '', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:57:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopCalendar.test.ts:64:    it('gives a workshop without an end its usual length', () => {
lib\workshops\workshopCalendar.test.ts:68:            participantIdentity: { email: 'karel@firma.cz', fullname: 'Karel Novák' },
lib\workshops\workshopCalendar.test.ts:69:            participantPath: PARTICIPANT_PATH,
lib\workshops\workshopAttendance.ts:8: * it sends and the graph which draws it all mean the same thing by an active participant.
lib\workshops\workshopAttendance.ts:15: *       moving a hand for half a minute never makes an attentive participant passive. It is short enough that somebody
lib\workshops\workshopAttendance.ts:37: * Was this participant at their computer, as far as the room could tell at this moment?
lib\workshops\workshopAttendance.test.ts:5:} from '@/lib/workshops/workshopAttendance';
lib\workshops\workshopAttendance.test.ts:20:    it('counts a room which has been open without a single interaction as passively attended', () => {
lib\workshops\workshopAttendance.test.ts:26:    it('counts a room nobody has ever interacted with as passively attended', () => {
lib\workshops\workshopAdminExports.test.ts:34:    id: 'participant-id',
lib\workshops\workshopAdminExports.test.ts:73:                participantId: PARTICIPANT.id,
lib\workshops\workshopAdminExports.test.ts:78:                workshopEndsAt: WORKSHOP.endsAt,
lib\workshops\workshopAdminExports.test.ts:99:            'participants',
lib\workshops\workshopAdminExports.test.ts:100:            'participants-vcard',
lib\workshops\workshopAdminExports.test.ts:108:    it('writes the full participant activity summary as a spreadsheet-friendly CSV', () => {
lib\workshops\workshopAdminExports.test.ts:117:    it('writes a useful contact card for each filtered participant', () => {
lib\workshops\workshopAdminExports.test.ts:123:        expect(vcard).toContain('UID:workshop-participant-participant-id');
lib\workshops\workshopAdminExports.test.ts:127:    it('joins Contact-table values and workshop history into participant CSV and vCard exports', () => {
lib\workshops\workshopAdminExports.test.ts:182:    it('uses the matching MIME type and filename for participant vCards', () => {
lib\workshops\workshopAdminExports.test.ts:183:        const exportFile = createWorkshopAdminExportFile('participants-vcard', {
lib\workshops\workshopAdminExports.test.ts:185:            participants: [PARTICIPANT],
lib\workshops\workshopAdminExports.test.ts:190:        expect(buildWorkshopAdminExportFileName(WORKSHOP, 'participants-vcard')).toBe(
lib\workshops\workshopAdminExports.test.ts:191:            'production-code-with-agents-participants-vcard.vcf',
lib\workshops\workshopChatInteractivity.test.ts:5:    it('offers writing and voting in a chat which belongs to the participants', () => {
lib\workshops\workshopChatInteractivity.test.ts:15:    it('leaves the form to a banned participant, so nothing tells them about their ban', () => {
lib\workshops\workshopChatInteractivity.test.ts:42:    it('keeps a moderator moderating a chat which the participants of the room cannot write into', () => {
app\api\workshops\[workshopSlug]\comments\[commentId]\upvotes\route.ts:33:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\comments\[commentId]\upvotes\route.ts:56:        participant_id: authenticatedRequest.participant.id,
lib\workshops\workshopCalendar.ts:19: * Note: A workshop loaded from the database satisfies this shape without carrying any participant data.
lib\workshops\workshopCalendar.ts:41:     * Details of the participant the event is built for, which may still be incomplete
lib\workshops\workshopCalendar.ts:43:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\workshops\workshopCalendar.ts:46:     * Site-relative path of the participant room of the workshop
lib\workshops\workshopCalendar.ts:48:    readonly participantPath: string;
lib\workshops\workshopCalendar.ts:52: * Names the event as a meeting of the participant with the host, so that both recognize it among the rest of their day
lib\workshops\workshopCalendar.ts:59:    participantFullname: string,
lib\workshops\workshopCalendar.ts:61:    const meetingTitle = [getFirstName(participantFullname), getFirstName(hostFullname)]
lib\workshops\workshopCalendar.ts:69: * Moment a workshop is expected to end, taken from its usual length whenever its end is still left open
lib\workshops\workshopCalendar.ts:71: * Note: A calendar entry has to last for something, so an occurrence which will run until it is ended is written into
lib\workshops\workshopCalendar.ts:72: *       the calendar of a participant with the length such a workshop usually takes.
lib\workshops\workshopCalendar.ts:79: * Turns a workshop into the event which a participant can put into their calendar
lib\workshops\workshopCalendar.ts:81: * Note: The event points to the participant room with the details of the participant prefilled, so that the link inside
lib\workshops\workshopCalendar.ts:88:    participantIdentity,
lib\workshops\workshopCalendar.ts:89:    participantPath,
lib\workshops\workshopCalendar.ts:91:    const participantLink = createWorkshopParticipantLink(participantPath, participantIdentity, occurrence.slug);
lib\workshops\workshopCalendar.ts:95:        title: createWorkshopCalendarEventTitle(occurrence.title, hostFullname, participantIdentity.fullname),
lib\workshops\workshopCalendar.ts:99:        url: createAbsoluteUrl(participantLink ?? createWorkshopSelectionPath(participantPath, occurrence.slug)),
lib\workshops\workshopCalendar.ts:111: * Whether a workshop is still ahead, which is the only time when putting it into a calendar is worth offering
lib\workshops\workshopCalendar.ts:114: * @param currentTime moment to compare against, as an ISO 8601 string - the server time keeps every participant in sync
lib\workshops\workshopChatInteractivity.ts:2: * What the chat still offers to one participant
lib\workshops\workshopChatInteractivity.ts:4: * Note: Both the switch of the whole panel and the ban of a single participant end here, so every part of the chat
lib\workshops\workshopChatInteractivity.ts:29:     * Whether this participant moderates the room, see `isWorkshopParticipantModerating`
lib\workshops\workshopChatInteractivity.ts:35: * Note: A banned participant keeps the form, because they must not learn about their ban. Their messages are rejected
lib\workshops\workshopChatInteractivity.ts:37: * Note: A switched-off chat is switched off for the participants of the room. A moderator keeps moderating it, exactly
app\api\workshops\[workshopSlug]\comments\route.ts:97:    const commentStatus = getWorkshopParticipantSubmissionStatus(authenticatedRequest.participant);
app\api\workshops\[workshopSlug]\comments\route.ts:103:            participant_id: authenticatedRequest.participant.id,
app\api\workshops\[workshopSlug]\comments\route.ts:105:            author_name: authenticatedRequest.participant.fullname,
app\api\workshops\[workshopSlug]\comments\route.ts:120:    const { participant } = authenticatedRequest;
app\api\workshops\[workshopSlug]\comments\route.ts:122:    if (areWorkshopCommentLinksEnabled({ isArtificial: false, isAuthorModerator: participant.isModerator })) {
app\api\workshops\[workshopSlug]\comments\route.ts:138:    // The participant who just wrote this message is the only author it can have, so it is described from them.
app\api\workshops\[workshopSlug]\comments\route.ts:141:        authorByParticipantId: new Map([[participant.id, createWorkshopCommentAuthor(participant)]]),
app\api\workshops\[workshopSlug]\comments\route.ts:142:        isModerationOffered: isWorkshopParticipantModerating(participant),
app\api\workshops\komunita\projects\[projectId]\vote\route.ts:27:    const interactionBanResponse = getWorkshopInteractionBanResponseOrNull(authenticatedRequest.participant);
app\api\workshops\komunita\projects\[projectId]\vote\route.ts:43:    const savedVote = await setCommunityProjectVote(projectId, authenticatedRequest.participant.id, parsedResult.data.vote);
app\api\workshops\komunita\projects\[projectId]\route.ts:33:    if (!isWorkshopParticipantModerating(authenticatedRequest.participant)) {
app\api\workshops\komunita\projects\[projectId]\connect\route.ts:23: * community cookies to unrelated workshop endpoints.
app\api\workshops\komunita\projects\[projectId]\connect\route.ts:41:    const connectedDiscussion = await connectCommunityProjectDiscussion(projectId, authenticatedRequest.participant.id);
app\api\workshops\komunita\projects\route.ts:43:        authenticatedRequest.participant,
app\api\workshops\komunita\projects\route.ts:54:            isModerationOffered: isWorkshopParticipantModerating(authenticatedRequest.participant),
app\api\workshops\komunita\projects\route.ts:95:        communityParticipantId: authenticatedRequest.participant.id,
app\api\workshops\komunita\projects\route.ts:109:        authenticatedRequest.participant,
app\api\ai-supervize-mini\registration\route.ts:62:    const participantCount = readParticipantCount(body.participantCount);
app\api\ai-supervize-mini\registration\route.ts:73:        participantCount === null ||
app\api\ai-supervize-mini\registration\route.ts:88:        participantCount,
app\api\ai-supervize-mini\registration\route.ts:138:    if (maximumParticipantCount !== null && registrationRequest.participantCount > maximumParticipantCount) {
app\api\ai-supervize-mini\registration\route.ts:165:        registrationRequest.participantCount,
app\api\ai-supervize-mini\registration\route.ts:239:            registrationRequest.participantCount,
app\api\ai-supervize-mini\registration\route.ts:244:            registrationRequest.participantCount,
app\api\ai-supervize-mini\registration\route.test.ts:88:            participantCount: 2,
app\api\ai-supervize-mini\registration\route.test.ts:170:            participantCount: 2,
app\api\ai-supervize-mini\registration\route.test.ts:180:        const response = await POST(createRegistrationRequest({ participantCount: 9 }));
app\api\contacts\export\[formatId]\route.test.ts:127:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
app\api\contacts\export\[formatId]\route.test.ts:133:                userNote: JSON.stringify({ selectedDateId: 'online-workshop-2026-09-02', participantCount: 1 }),
app\api\contacts\export\[formatId]\route.test.ts:139:                userNote: JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 }),
lib\workshops\workshopClientState.test.ts:123:    it('uses server time so an incorrect participant clock cannot delay content', () => {
lib\workshops\workshopDate.ts:23: * Formats an occurrence date in the timezone in which Czech workshops take place, independently of the server's
lib\workshops\workshopDate.ts:118: * Describes how long a workshop is expected to take. A workshop whose end is still left open is announced with the
lib\workshops\workshopDate.ts:120: * know when the workshop will really be ended.
lib\workshops\workshopDatabase.ts:125:    readonly maximum_participant_count: number | null;
lib\workshops\workshopDatabase.ts:126:    readonly artificial_watching_participant_count?: number;
lib\workshops\workshopDatabase.ts:129:     * The keys of the panels this workshop switched off for its participants
lib\workshops\workshopDatabase.ts:160:    | 'maximum_participant_count'
lib\workshops\workshopDatabase.ts:168:    'id, room_kind, slug, title, description, starts_at, ends_at, is_published, event_type, location_kind, location_label, price_czk, maximum_participant_count';
lib\workshops\workshopDatabase.ts:190: * The one record a participant progressively fills after a workshop.
lib\workshops\workshopDatabase.ts:195:    readonly participant_id: string;
lib\workshops\workshopDatabase.ts:205:    'id, workshop_id, participant_id, rating, what_was_good, what_was_bad, note, created_at, updated_at';
lib\workshops\workshopDatabase.ts:211:    readonly participant_id: string | null;
lib\workshops\workshopDatabase.ts:226:    'id, participant_id, parent_comment_id, author_name, body, status, upvote_count, artificial_upvote_count, is_artificial, created_at';
lib\workshops\workshopDatabase.ts:279: * Everything the administration lists about one participant, so a new participant field is selected everywhere at once
lib\workshops\workshopDatabase.ts:287:const WORKSHOP_ADMIN_PARTICIPANT_PAGE_FUNCTION_NAME = 'get_workshop_admin_participant_page';
lib\workshops\workshopDatabase.ts:295:    readonly participant_id: string;
lib\workshops\workshopDatabase.ts:308:    readonly participant_count: number | string;
lib\workshops\workshopDatabase.ts:323:    readonly watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:324:    readonly actively_watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:325:    readonly passively_watching_participant_count: number | string;
lib\workshops\workshopDatabase.ts:326:    readonly participant_count: number | string;
lib\workshops\workshopDatabase.ts:343:    readonly participant_id: string | null;
lib\workshops\workshopDatabase.ts:410: * otherwise complete participant timeline or a large reaction export.
lib\workshops\workshopDatabase.ts:451:        // to a participant browser.
lib\workshops\workshopDatabase.ts:463:        artificialWatchingParticipantCount: row.artificial_watching_participant_count ?? 0,
lib\workshops\workshopDatabase.ts:484:            maximumParticipantCount: row.maximum_participant_count,
lib\workshops\workshopDatabase.ts:495:    participantCount: number,
lib\workshops\workshopDatabase.ts:502:        participantCount,
lib\workshops\workshopDatabase.ts:539:    participantById: ReadonlyMap<string, WorkshopFeedbackParticipantRow>,
lib\workshops\workshopDatabase.ts:541:    const participant = participantById.get(row.participant_id);
lib\workshops\workshopDatabase.ts:542:    if (participant === undefined) {
lib\workshops\workshopDatabase.ts:548:        participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:549:        fullname: participant.fullname,
lib\workshops\workshopDatabase.ts:550:        email: participant.email,
lib\workshops\workshopDatabase.ts:560:    const author = row.participant_id === null ? undefined : roomContext.authorByParticipantId.get(row.participant_id);
lib\workshops\workshopDatabase.ts:580:        participantId: row.id,
lib\workshops\workshopDatabase.ts:588: * The author of a message which the very participant who wrote it just sent, so it needs no second read of them
lib\workshops\workshopDatabase.ts:590:export function createWorkshopCommentAuthor(participant: WorkshopParticipant): WorkshopCommentAuthor {
lib\workshops\workshopDatabase.ts:592:        participantId: participant.id,
lib\workshops\workshopDatabase.ts:593:        isTrusted: participant.isTrusted,
lib\workshops\workshopDatabase.ts:594:        isInteractionBanned: participant.isInteractionBanned,
lib\workshops\workshopDatabase.ts:595:        isModerator: participant.isModerator,
lib\workshops\workshopDatabase.ts:602: * Note: An artificial message of the administration has no author at all, and a deleted participant leaves their
lib\workshops\workshopDatabase.ts:613:        .map((commentRow) => commentRow.participant_id)
lib\workshops\workshopDatabase.ts:614:        .filter((participantId): participantId is string => participantId !== null);
lib\workshops\workshopDatabase.ts:696:        workshopRow.ends_at === null ? currentMilliseconds : Date.parse(workshopRow.ends_at);
lib\workshops\workshopDatabase.ts:721:    const endsAtMilliseconds = workshopRow.ends_at === null ? Date.now() : Date.parse(workshopRow.ends_at);
lib\workshops\workshopDatabase.ts:732:        watchingParticipantCount: getNonNegativeWholeNumber(row.watching_participant_count),
lib\workshops\workshopDatabase.ts:733:        activelyWatchingParticipantCount: getNonNegativeWholeNumber(row.actively_watching_participant_count),
lib\workshops\workshopDatabase.ts:734:        passivelyWatchingParticipantCount: getNonNegativeWholeNumber(row.passively_watching_participant_count),
lib\workshops\workshopDatabase.ts:735:        participantCount: getNonNegativeWholeNumber(row.participant_count),
lib\workshops\workshopDatabase.ts:818:type WorkshopPollVoteErrorKind = 'not-found' | 'closed' | 'invalid-participant' | 'database';
lib\workshops\workshopDatabase.ts:831:    WORKSHOP_POLL_PARTICIPANT_INVALID: 'invalid-participant',
lib\workshops\workshopDatabase.ts:832:    WORKSHOP_POLL_VOTER_EMAIL_INVALID: 'invalid-participant',
lib\workshops\workshopDatabase.ts:844:    participant: Pick<WorkshopParticipant, 'id' | 'email'>,
lib\workshops\workshopDatabase.ts:852:        target_participant_id: participant.id,
lib\workshops\workshopDatabase.ts:853:        target_voter_email: normalizeWorkshopParticipantEmail(participant.email),
lib\workshops\workshopDatabase.ts:870: *       room correct when several participants react at once, when a room reconnects, or when its own broadcast comes
lib\workshops\workshopDatabase.ts:876:    participantId: string | null,
lib\workshops\workshopDatabase.ts:884:        target_participant_id: participantId,
lib\workshops\workshopDatabase.ts:1111:    const { data, error } = await supabase.rpc('get_workshop_participant_counts', { target_room_kind: workshopKind });
lib\workshops\workshopDatabase.ts:1113:        console.error('Failed to count the participants of the listed workshops:', error.message);
lib\workshops\workshopDatabase.ts:1119:            (participantCounts) =>
lib\workshops\workshopDatabase.ts:1121:                    participantCounts.workshop_id,
lib\workshops\workshopDatabase.ts:1122:                    getNonNegativeWholeNumber(participantCounts.participant_count),
lib\workshops\workshopDatabase.ts:1139:    const [workshopsResult, participantCountByWorkshopId, registeredParticipantCountByTermId] = await Promise.all([
lib\workshops\workshopDatabase.ts:1167:                participantCountByWorkshopId.get(workshopRow.id) ?? 0,
lib\workshops\workshopDatabase.ts:1181: * Counts the participants whose browser talked to the room within the watching window
lib\workshops\workshopDatabase.ts:1186: *       participant would otherwise count the whole audience for nobody to read.
lib\workshops\workshopDatabase.ts:1205:        console.error('Failed to count the participants watching a workshop:', error.message);
lib\workshops\workshopDatabase.ts:1209:    return (count ?? 0) + Math.max(0, workshopRow.artificial_watching_participant_count ?? 0);
lib\workshops\workshopDatabase.ts:1488: * participant-specific lookup contains only that participant's own selection, so no room response can infer who
lib\workshops\workshopDatabase.ts:1576: * Loads one page of participants together with the activity totals used for its sortable columns.
lib\workshops\workshopDatabase.ts:1578: * Note: The database applies the filters and sort before it limits the result, so a large workshop never needs to send
lib\workshops\workshopDatabase.ts:1579: * every participant to the browser just to display one page.
lib\workshops\workshopDatabase.ts:1586:    const participantPageParameters = {
lib\workshops\workshopDatabase.ts:1601:        participantPageParameters,
lib\workshops\workshopDatabase.ts:1609:                participantPageParameters,
lib\workshops\workshopDatabase.ts:1614:    const participantRows = (data ?? []) as WorkshopAdminParticipantPageRow[];
lib\workshops\workshopDatabase.ts:1617:            participants: participantRows.map(mapWorkshopAdminParticipantPageRow),
lib\workshops\workshopDatabase.ts:1618:            totalCount: getNonNegativeWholeNumber(participantRows[0]?.total_count),
lib\workshops\workshopDatabase.ts:1625: * Loads the exact actions of one participant from their existing source records.
lib\workshops\workshopDatabase.ts:1627: * Note: Registration and last activity are participant fields rather than action rows, so they are added as two
lib\workshops\workshopDatabase.ts:1630: * aggregate instead of this participant-attributed timeline.
lib\workshops\workshopDatabase.ts:1635:    participantId: string,
lib\workshops\workshopDatabase.ts:1637:    const [participantResult, commentsResult, reactionsResult, upvotesResult] = await Promise.all([
lib\workshops\workshopDatabase.ts:1642:            .eq('id', participantId)
lib\workshops\workshopDatabase.ts:1649:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1659:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1669:                .eq('participant_id', participantId)
lib\workshops\workshopDatabase.ts:1677:        participantResult.error?.message ??
lib\workshops\workshopDatabase.ts:1686:    if (participantResult.data === null) {
lib\workshops\workshopDatabase.ts:1690:    const participant = participantResult.data as WorkshopAdminParticipantRow;
lib\workshops\workshopDatabase.ts:1713:        { kind: 'joined' as const, id: `joined-${participant.id}`, occurredAt: participant.connected_at },
lib\workshops\workshopDatabase.ts:1714:        { kind: 'last-seen' as const, id: `last-seen-${participant.id}`, occurredAt: participant.last_seen_at },
lib\workshops\workshopDatabase.ts:1746:            participant: mapWorkshopAdminParticipantRow(participant, {
lib\workshops\workshopDatabase.ts:1747:                participant_id: participant.id,
lib\workshops\workshopDatabase.ts:1847: * Loads every participant which matches the administration filter for a file export.
lib\workshops\workshopDatabase.ts:1849: * Note: The export keeps the exact same filter and sort as the participant table, while intentionally ignoring its
lib\workshops\workshopDatabase.ts:1857:    readonly participants: readonly WorkshopAdminParticipant[] | null;
lib\workshops\workshopDatabase.ts:1867:        return { participants: null, errorMessage: firstPageResult.errorMessage };
lib\workshops\workshopDatabase.ts:1870:    const participants = [...firstPageResult.page.participants];
lib\workshops\workshopDatabase.ts:1878:            return { participants: null, errorMessage: pageResult.errorMessage };
lib\workshops\workshopDatabase.ts:1881:        participants.push(...pageResult.page.participants);
lib\workshops\workshopDatabase.ts:1884:    return { participants, errorMessage: null };
lib\workshops\workshopDatabase.ts:1918:            participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:1929: * Reads enough participant identity to make exported reactions understandable, including artificial reactions whose
lib\workshops\workshopDatabase.ts:1930: * participant is intentionally absent.
lib\workshops\workshopDatabase.ts:1935:    participantIds: readonly string[],
lib\workshops\workshopDatabase.ts:1941:        participantIds,
lib\workshops\workshopDatabase.ts:1954:        identityByParticipantId: new Map(rows.map((participant) => [participant.id, participant] as const)),
lib\workshops\workshopDatabase.ts:1971:              readonly participantFullname: string | null;
lib\workshops\workshopDatabase.ts:1972:              readonly participantEmail: string | null;
lib\workshops\workshopDatabase.ts:1982:            .select('id, participant_id, emoji, created_at, is_artificial')
lib\workshops\workshopDatabase.ts:1992:    const participantIds = rows
lib\workshops\workshopDatabase.ts:1993:        .map((reaction) => reaction.participant_id)
lib\workshops\workshopDatabase.ts:1994:        .filter((participantId): participantId is string => participantId !== null);
lib\workshops\workshopDatabase.ts:1998:        participantIds,
lib\workshops\workshopDatabase.ts:2006:            const participant =
lib\workshops\workshopDatabase.ts:2007:                reaction.participant_id === null ? undefined : identityByParticipantId.get(reaction.participant_id);
lib\workshops\workshopDatabase.ts:2012:                participantFullname: participant?.fullname ?? null,
lib\workshops\workshopDatabase.ts:2013:                participantEmail: participant?.email ?? null,
lib\workshops\workshopDatabase.ts:2062: * Feedback and participants stay separate source records, just as comments and reactions do. The small identity read
lib\workshops\workshopDatabase.ts:2063: * keeps this list attributable without ever exposing an e-mail address through the participant-facing state route.
lib\workshops\workshopDatabase.ts:2084:    const participantResult = await loadWorkshopRowsByIds<WorkshopFeedbackParticipantRow>(
lib\workshops\workshopDatabase.ts:2085:        feedbackResult.rows.map((feedback) => feedback.participant_id),
lib\workshops\workshopDatabase.ts:2086:        (participantIds) =>
lib\workshops\workshopDatabase.ts:2091:                .in('id', participantIds),
lib\workshops\workshopDatabase.ts:2093:    if (participantResult.rows === null) {
lib\workshops\workshopDatabase.ts:2094:        return { feedbacks: null, errorMessage: participantResult.errorMessage };
lib\workshops\workshopDatabase.ts:2097:    const participantById = new Map<string, WorkshopFeedbackParticipantRow>(
lib\workshops\workshopDatabase.ts:2098:        participantResult.rows.map((participant) => [participant.id, participant] as const),
lib\workshops\workshopDatabase.ts:2102:            .map((feedback) => mapWorkshopAdminFeedbackRow(feedback, participantById))
lib\workshops\workshopDatabase.ts:2211:    participant: WorkshopParticipant,
lib\workshops\workshopDatabase.ts:2228:    // Note: The membership decides which materials a member sees, so the room asks about it before it assembles what
lib\workshops\workshopDatabase.ts:2230:    //       not arrive keeps the paid materials hidden rather than letting them escape.
lib\workshops\workshopDatabase.ts:2235:            participant.email,
lib\workshops\workshopDatabase.ts:2238:            console.error('Failed to load the community membership of a workshop participant:', membershipErrorMessage);
lib\workshops\workshopDatabase.ts:2245:    const isModerationOffered = isWorkshopParticipantModerating(participant);
lib\workshops\workshopDatabase.ts:2271:    // Note: The materials are read as a paid member would read them, whoever is reading the room. Which of them reach
lib\workshops\workshopDatabase.ts:2311:        // scheduled materials keep their own timing.
lib\workshops\workshopDatabase.ts:2318:                  .eq('participant_id', participant.id)
lib\workshops\workshopDatabase.ts:2322:        isModerationOffered ? pendingCommentsQuery : pendingCommentsQuery.eq('participant_id', participant.id),
lib\workshops\workshopDatabase.ts:2337:        loadWorkshopPolls(supabase, workshopRow, participant.email),
lib\workshops\workshopDatabase.ts:2373:    // The recording of an ended workshop is decided here for the very same reason: a member who has not unlocked it
lib\workshops\workshopDatabase.ts:2418:            .eq('participant_id', participant.id)
lib\workshops\workshopDatabase.ts:2437:                commentRow.participant_id === null ? undefined : authorByParticipantId.get(commentRow.participant_id);
lib\workshops\workshopDatabase.ts:2473:            participant,
lib\workshops\workshopDatabase.ts:2555:        participantCountResult,
lib\workshops\workshopDatabase.ts:2609:        participantCountResult.error ??
lib\workshops\workshopDatabase.ts:2637:        participantId: row.participant_id,
lib\workshops\workshopDatabase.ts:2664:            participants: [],
lib\workshops\workshopDatabase.ts:2665:            participantCount: participantCountResult.count ?? 0,
lib\workshops\workshopConstants.ts:4:export const WORKSHOP_PARTICIPANT_TABLE_NAME = 'workshop_participants';
lib\workshops\workshopConstants.ts:58: * Note: The commits are read through one cached server-side answer however many participants have the room open, so
lib\workshops\workshopConstants.ts:59: *       the room can follow the project without turning every participant into a GitHub client.
lib\workshops\workshopConstants.ts:79: * How long after their last request a participant still counts as watching the workshop
lib\workshops\workshopConstants.ts:82: *       so this window stays above both to never drop an attentive participant out of the count.
lib\workshops\workshopConstants.ts:87: * How long one presence sample of a participant lasts, which is what the audience of the administration graph is
lib\workshops\workshopKindCapabilities.ts:30:     * Whether a room gathers its participants around a shared stage with a video stream
lib\workshops\workshopKindCapabilities.ts:40:    /** Whether a room can give every participant a presentation beside its ordinary materials. */
lib\workshops\workshopKindCapabilities.ts:65:     * Whether a room leads its participants on into the permanent community, which is where everything outlasting one
lib\workshops\workshopKindCapabilities.ts:75:     * Whether a participant can end their own session in this room and be offered its connection form again, which
lib\workshops\workshopKindCapabilities.ts:78:     * Note: Only a room a participant connected to themselves is left this way. The session of a project discussion is
lib\workshops\workshopKindCapabilities.test.ts:89:    it('lets a participant sign out of the rooms they connected to themselves', () => {
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:9:    readonly params: Promise<{ readonly workshopId: string; readonly participantId: string }>;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:13: * Serves the full event history only after an administrator opens a participant's detail.
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:21:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:27:    const [participantTimelineResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:28:        loadWorkshopAdminParticipantTimeline(workshopData.supabase, workshopData.workshopRow, participantId),
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:31:    const { timeline, errorMessage } = participantTimelineResult;
app\api\admin\workshops\[workshopId]\participants\[participantId]\timeline\route.ts:47:            participant: joinAdminContactGroup(timeline.participant, contactGroupsResult.groups ?? []),
lib\workshops\workshopMaterialLinks.ts:695: * ad hoc-shortcode path as materials. A normal participant never calls this:
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:12:    readonly params: Promise<{ readonly workshopId: string; readonly participantId: string }>;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:27:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:34:    const { participant, errorMessage } = await updateModeratedWorkshopParticipant(
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:37:        participantId,
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:43:    if (participant === null) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:48:    return NextResponse.json(participant);
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:57:    const { workshopId, participantId } = await context.params;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:65:        const deletedCommunityParticipant = await deleteCommunityParticipantWithProjectVotes(participantId);
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:66:        if (deletedCommunityParticipant.participantId === null) {
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:69:                    'Failed to delete a community participant and reconcile their project votes:',
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:84:        deletedParticipantId = deletedCommunityParticipant.participantId;
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:89:            .eq('id', participantId)
app\api\admin\workshops\[workshopId]\participants\[participantId]\route.ts:104:    return NextResponse.json({ participantId: deletedParticipantId });
app\api\admin\workshops\[workshopId]\participants\route.ts:34:    const [participantPageResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\participants\route.ts:41:    const { page, errorMessage } = participantPageResult;
app\api\admin\workshops\[workshopId]\participants\route.ts:56:              page.participants.map((participant) => participant.email),
app\api\admin\workshops\[workshopId]\participants\route.ts:66:            participants: page.participants.map((participant) => {
app\api\admin\workshops\[workshopId]\participants\route.ts:67:                const participantWithContactGroup = joinAdminContactGroup(participant, contactGroupsResult.groups ?? []);
app\api\admin\workshops\[workshopId]\participants\route.ts:71:                          ...participantWithContactGroup,
app\api\admin\workshops\[workshopId]\participants\route.ts:74:                              participant.email,
app\api\admin\workshops\[workshopId]\participants\route.ts:77:                    : participantWithContactGroup;
app\api\admin\workshops\[workshopId]\participants\route.test.ts:46:    id: 'participant-1',
app\api\admin\workshops\[workshopId]\participants\route.test.ts:63:    return new NextRequest('https://promptbook.studio/api/admin/workshops/community-id/participants');
app\api\admin\workshops\[workshopId]\participants\route.test.ts:66:describe('admin community participants membership projection', () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:77:        joinAdminContactGroupMock.mockImplementation((participant) => ({ ...participant, contactGroup: null }));
app\api\admin\workshops\[workshopId]\participants\route.test.ts:80:            page: { participants: [PARTICIPANT], totalCount: 1 },
app\api\admin\workshops\[workshopId]\participants\route.test.ts:85:    it('adds the membership state to each community participant in one batched membership lookup', async () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:98:        const responseBody = (await response.json()) as { readonly participants: unknown };
app\api\admin\workshops\[workshopId]\participants\route.test.ts:99:        expect(responseBody.participants).toEqual([
app\api\admin\workshops\[workshopId]\participants\route.test.ts:106:    it('does not fetch paid membership information for an ordinary workshop participant list', async () => {
app\api\admin\workshops\[workshopId]\participants\route.test.ts:118:    it('rejects a request before querying either participant or payment data when the administrator is not signed in', async () => {
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:56:        case 'participants':
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:57:        case 'participants-vcard': {
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:59:            const [participantsResult, contactGroupsResult] = await Promise.all([
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:66:            const { participants, errorMessage } = participantsResult;
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:67:            if (participants === null) {
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:76:                participants: participants.map((participant) =>
app\api\admin\workshops\[workshopId]\exports\[exportKind]\route.ts:77:                    joinAdminContactGroup(participant, contactGroupsResult.groups ?? []),
app\api\admin\workshops\[workshopId]\feedback\route.test.ts:34:    participantId: 'participant-1',
lib\workshops\workshopModeration.test.ts:14:        id: 'participant-1',
lib\workshops\workshopMigration.test.ts:44:    'migrations/2026-08-0800-workshop-admin-participant-page-ordering.sql',
lib\workshops\workshopMigration.test.ts:51:    'migrations/2026-08-1100-workshop-admin-participant-page-variable-conflict.sql',
lib\workshops\workshopMigration.test.ts:140:    'migrations/2026-08-3200-workshop-active-and-passive-attendance.sql',
lib\workshops\workshopMigration.test.ts:178:const PARTICIPANT_PAGE_ROW_SOURCE = 'participant_page';
lib\workshops\workshopMigration.test.ts:182:    'workshop_participants',
lib\workshops\workshopMigration.test.ts:189: * Names the database column behind one sortable column of the participant administration.
lib\workshops\workshopMigration.test.ts:208:    it('records each participant action with a server timestamp and identity', () => {
lib\workshops\workshopMigration.test.ts:209:        expect(MIGRATION_SQL).toMatch(/workshop_participants[\s\S]*connected_at timestamptz NOT NULL DEFAULT now\(\)/);
lib\workshops\workshopMigration.test.ts:212:            /workshop_comment_upvotes[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:215:            /workshop_reactions[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:218:            /workshop_content_link_clicks[\s\S]*participant_id uuid NOT NULL[\s\S]*created_at timestamptz NOT NULL DEFAULT now\(\)/,
lib\workshops\workshopMigration.test.ts:231:            'CONSTRAINT workshop_comment_upvotes_one_per_participant UNIQUE (comment_id, participant_id)',
lib\workshops\workshopMigration.test.ts:233:        expect(MIGRATION_SQL).toContain('workshop_comment_upvotes_participant_fk');
lib\workshops\workshopMigration.test.ts:248:    it('marks artificial actions and participant interaction bans in the follow-up migration', () => {
lib\workshops\workshopMigration.test.ts:252:        expect(MODERATION_MIGRATION_SQL).toContain('ALTER COLUMN participant_id DROP NOT NULL');
lib\workshops\workshopMigration.test.ts:261:        expect(ANALYTICS_MIGRATION_SQL).toContain('record_workshop_participant_presence');
lib\workshops\workshopMigration.test.ts:262:        expect(ANALYTICS_MIGRATION_SQL).toContain('get_workshop_participant_activity_totals');
lib\workshops\workshopMigration.test.ts:265:    it('indexes the participants seen recently so the watching count stays cheap', () => {
lib\workshops\workshopMigration.test.ts:267:            'CREATE INDEX IF NOT EXISTS workshop_participants_watching_idx',
lib\workshops\workshopMigration.test.ts:270:            'public.workshop_participants (workshop_id, last_seen_at DESC)',
lib\workshops\workshopMigration.test.ts:338:        expect(REACTION_COUNT_MIGRATION_SQL).toContain('target_participant_id IS NULL');
lib\workshops\workshopMigration.test.ts:364:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('get_workshop_admin_participant_page');
lib\workshops\workshopMigration.test.ts:368:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_admin_filter_idx');
lib\workshops\workshopMigration.test.ts:369:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_fullname_trigram_idx');
lib\workshops\workshopMigration.test.ts:370:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_participants_email_trigram_idx');
lib\workshops\workshopMigration.test.ts:371:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_comments_participant_timeline_idx');
lib\workshops\workshopMigration.test.ts:372:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain('workshop_reactions_participant_timeline_idx');
lib\workshops\workshopMigration.test.ts:377:            'CREATE OR REPLACE FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:396:        expect(ADMIN_ANALYTICS_MIGRATION_SQL).toContain("'participant'::text AS event_kind");
lib\workshops\workshopMigration.test.ts:408:        expect(MODERATOR_MIGRATION_SQL).toContain('CREATE INDEX IF NOT EXISTS workshop_participants_moderator_idx');
lib\workshops\workshopMigration.test.ts:412:    it('returns and filters the moderator in the paged participant administration, without a second overload', () => {
lib\workshops\workshopMigration.test.ts:415:        expect(MODERATOR_MIGRATION_SQL).toContain('DROP FUNCTION IF EXISTS public.get_workshop_admin_participant_page');
lib\workshops\workshopMigration.test.ts:418:            'AND (target_is_moderator IS NULL OR workshop_participant.is_moderator = target_is_moderator)',
lib\workshops\workshopMigration.test.ts:420:        expect(MODERATOR_MIGRATION_SQL).toContain('workshop_participant.is_moderator,');
lib\workshops\workshopMigration.test.ts:430:    it('settles the ambiguous participant ordering for the whole function, whatever the database still holds', () => {
lib\workshops\workshopMigration.test.ts:435:            'CREATE FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:441:                `DROP FUNCTION IF EXISTS public.get_workshop_admin_participant_page( ${functionSignature} );`,
lib\workshops\workshopMigration.test.ts:453:            'GRANT EXECUTE ON FUNCTION public.get_workshop_admin_participant_page',
lib\workshops\workshopMigration.test.ts:477:            'CONSTRAINT workshop_poll_votes_one_per_participant UNIQUE (poll_id, participant_id)',
lib\workshops\workshopMigration.test.ts:552:            'REFERENCES public.workshop_participants(id) ON DELETE SET NULL',
lib\workshops\workshopMigration.test.ts:593:            'CREATE TABLE IF NOT EXISTS public.community_project_discussion_participants',
lib\workshops\workshopMigration.test.ts:595:        expect(COMMUNITY_PROJECT_MIGRATION_SQL).toContain('PRIMARY KEY (project_id, community_participant_id)');
lib\workshops\workshopMigration.test.ts:628:            'DROP CONSTRAINT IF EXISTS community_project_votes_community_participant_id_fkey',
lib\workshops\workshopMigration.test.ts:631:            'REFERENCES public.workshop_participants(id) ON DELETE RESTRICT',
lib\workshops\workshopMigration.test.ts:648:            'CONSTRAINT workshop_feedback_one_per_participant UNIQUE (workshop_id, participant_id)',
lib\workshops\workshopMigration.test.ts:650:        expect(WRAP_UP_AND_FEEDBACK_MIGRATION_SQL).toContain('workshop_feedback_participant_fk');
lib\workshops\workshopMigration.test.ts:675:    it('routes material analytics through persisted ad hoc short links rather than a participant browser event', () => {
lib\workshops\workshopMigration.test.ts:780:    it('remembers how every measured minute of a room was attended', () => {
lib\workshops\workshopMigration.test.ts:785:            'DROP FUNCTION IF EXISTS public.record_workshop_participant_presence(uuid, uuid, integer);',
lib\workshops\workshopMigration.test.ts:789:            'ON CONFLICT (workshop_id, bucket_starts_at, participant_id) DO UPDATE',
lib\workshops\workshopMigration.test.ts:791:        expect(ATTENDANCE_MIGRATION_SQL).toContain('NOT workshop_participant_presence_samples.is_actively_attending');
lib\workshops\workshopMigration.test.ts:793:            'GRANT EXECUTE ON FUNCTION public.record_workshop_participant_presence(uuid, uuid, integer, boolean) TO service_role;',
lib\workshops\workshopMigration.test.ts:799:        expect(ATTENDANCE_MIGRATION_SQL).toContain('actively_watching_participant_count bigint');
lib\workshops\workshopMigration.test.ts:800:        expect(ATTENDANCE_MIGRATION_SQL).toContain('passively_watching_participant_count bigint');
lib\workshops\workshopModeration.ts:30:     * Whether this role has the messages of a participant approved as they are written from now on
lib\workshops\workshopModeration.ts:35:     * Whether this role takes the interactions of a participant away
lib\workshops\workshopModeration.ts:82: * Whether a participant moderates the room they are connected to
lib\workshops\workshopModeration.ts:89:export function isWorkshopParticipantModerating(participant: WorkshopParticipant): boolean {
lib\workshops\workshopModeration.ts:90:    return participant.isModerator && !participant.isInteractionBanned;
lib\workshops\workshopModeration.ts:94: * Whether this moderating role may change the moderation state of one participant at all
lib\workshops\workshopModeration.ts:102:    participant: { readonly isModerator: boolean },
lib\workshops\workshopModeration.ts:104:    return !participant.isModerator || getWorkshopModerationCapabilities(moderationRole).isModeratorAppointmentOffered;
lib\workshops\workshopModeration.ts:127: * The written participant fields which this moderating role may not change
app\api\admin\workshops\[workshopId]\content\route.ts:63:        // The source Markdown was persisted safely. The participant-state load
lib\workshops\workshopOverviewSeriesPoints.test.ts:27:        participantCount: 0,
lib\workshops\workshopOverviewSeriesPoints.test.ts:91:    it('reaches everything which happened before the workshop started and after it ended', () => {
lib\workshops\workshopOverviewSeriesPoints.test.ts:93:            createTimelinePoint('2026-08-20T08:00:00.000Z', { participantCount: 3 }),
lib\workshops\workshopOverviewSeriesPoints.ts:185:            joinedParticipants: point.participantCount,
lib\workshops\workshopOverviewSeriesPoints.ts:257: * started and everything which happened after it ended
lib\workshops\workshopPaidMembersContent.test.ts:29:describe('the materials one member of a room is given', () => {
lib\workshops\workshopPaidMembersContent.test.ts:71:    it('keeps the order the materials were read in', () => {
lib\workshops\workshopPaidMembersContent.ts:4: * What decides whether the materials reserved for paid members reach the member reading the room
lib\workshops\workshopPaidMembersContent.ts:19: * The materials of a room as one member is given them: the ones they may read, and the ones they are merely told about
lib\workshops\workshopPaidMembersContent.ts:31: * Divides the materials a paid member would read into the ones this member receives and the ones which are only named
lib\workshops\workshopPaidMembersVideo.test.ts:15:    it('plays the stream to everybody while the workshop has not ended', () => {
lib\workshops\workshopPaidMembersVideo.test.ts:23:    it('keeps the recording of an ended workshop for the member whose membership unlocks it', () => {
lib\workshops\workshopPaidMembersVideo.test.ts:57:    it('offers nothing for an ended workshop which carries no recording at all', () => {
lib\workshops\workshopPanels.ts:5: * One part of the participant room which an admin can take away from the participants of a single workshop
lib\workshops\workshopPanels.ts:29: * Every switchable panel of the participant room, in the order the administration offers them
lib\workshops\workshopPanels.ts:82: * Whether the room still offers a panel to its participants
lib\workshops\workshopParticipantInteraction.ts:11:    participant: Pick<WorkshopParticipant, 'isInteractionBanned' | 'isTrusted' | 'isModerator'>,
lib\workshops\workshopParticipantInteraction.ts:13:    if (participant.isInteractionBanned) {
lib\workshops\workshopParticipantInteraction.ts:17:    return participant.isTrusted || participant.isModerator ? 'approved' : 'pending';
lib\workshops\workshopParticipantInteraction.ts:20:export function getWorkshopInteractionBanResponseOrNull(participant: WorkshopParticipant): NextResponse | null {
lib\workshops\workshopParticipantInteraction.ts:21:    return participant.isInteractionBanned
lib\workshops\workshopParticipantInteraction.ts:30: * Refuses an action of a participant which the room does not offer anymore
lib\workshops\workshopParticipantInteraction.ts:34: * Note: Only participants are held back here. The administration writes through its own routes and therefore keeps
lib\workshops\workshopParticipantFullname.ts:4: * Cleans a participant name the same way it is stored, so that a form never validates something else than the server
lib\workshops\workshopParticipantFullname.ts:11: * The single rule for a participant name, shared by the connection form, the rename form and the API schemas
lib\workshops\workshopParticipantEmail.ts:2: * The one form of a participant address used to decide durable identity across rooms.
lib\workshops\workshopPaidMembersVideo.ts:59: * Note: An occurrence which carries no stream has nothing a purchase could unlock, so its ended room offers nothing
lib\workshops\workshopReactionAnimations.ts:14:     * What the participant sent, an emoji or any other short text
lib\workshops\workshopReactionAnimations.ts:109:     * The reactions which are celebrated this way, already in the form participants send them
lib\workshops\workshopReactionAnimations.ts:325:const EMOJI_START_PATTERN = new RegExp('^\\p{Extended_Pictographic}', 'u');
lib\workshops\workshopReactionAnimations.ts:329: * The reaction as the registry knows it, no matter how the keyboard of the participant spelled it
lib\workshops\workshopReactionAnimations.ts:374: * The same reaction told without any travel, for a participant who asked the system for less motion
lib\workshops\workshopReactionAnimations.ts:444: *       recognize what a participant sent, not to be listed twice in the administration.
lib\workshops\workshopReactionAnimations.test.ts:97:    it('recognizes the same reaction however the keyboard of the participant spelled it', () => {
lib\workshops\workshopReactionAnimations.test.ts:124:    it('leaves a reaction where it was sent when the participant asked for less motion', () => {
lib\workshops\workshopPublic.ts:37: * Note: This is what a room offers a participant to choose from, so it deliberately says more than the landing page
lib\workshops\workshopPublic.ts:38: *       of the same event does. A term is worth entering while it runs and long after it ended, while only a term
lib\workshops\workshopPublic.ts:78: * Loads the one published community room. Its data model is shared with workshops so participants get the same
lib\workshops\workshopPhase.ts:17: * Note: An occurrence without an end has not ended. It runs for as long as it takes and is only over once its end is
lib\workshops\workshopPhase.ts:55: * Moment an occurrence is expected to end, taken from the usual length of a workshop while its end is left open
lib\workshops\workshopPhase.ts:58: *       this expectation is for. It deliberately never decides whether an occurrence is over — only a recorded end
lib\workshops\workshopPhase.ts:108: * Note: The history is ranked by its negated date, so the term which ended last leads it while the future is led by
lib\workshops\workshopPhase.test.ts:46:    it('keeps a workshop whose end is left open running until an end is recorded', () => {
lib\workshops\workshopPhase.test.ts:55:    it('ends a workshop as soon as the administration records the moment it was ended', () => {
lib\workshops\workshopPhase.test.ts:56:        const endedWorkshop: WorkshopOccurrenceTiming = {
lib\workshops\workshopPhase.test.ts:61:        expect(isWorkshopEndOpen(endedWorkshop)).toBe(false);
lib\workshops\workshopPhase.test.ts:62:        expect(getWorkshopPhase(endedWorkshop, Date.parse('2026-08-21T20:11:00+02:00'))).toBe('ongoing');
lib\workshops\workshopPhase.test.ts:63:        expect(getWorkshopPhase(endedWorkshop, Date.parse('2026-08-21T20:13:00+02:00'))).toBe('past');
lib\workshops\workshopPhase.test.ts:66:    it('expects an open end to take as long as a workshop usually does, without ending it', () => {
lib\workshops\workshopPhase.test.ts:83:    it('lists what runs now first, then what starts soonest, and finally what ended last', () => {
lib\workshops\workshopWrapUpDocument.ts:30: * The participant-safe data which a completed workshop hands over as a PDF.
lib\workshops\workshopWrapUpDocument.ts:32: * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
lib\workshops\workshopWrapUpDocument.ts:40:    readonly materials: readonly WorkshopWrapUpMaterial[];
lib\workshops\workshopWrapUpDocument.ts:44:    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
lib\workshops\workshopWrapUpDocument.ts:148: * Builds the data of the wrap-up from fields which the participant room already owns.
lib\workshops\workshopWrapUpDocument.ts:158:    const materials = [
lib\workshops\workshopWrapUpDocument.ts:167:            workshop.endsAt,
lib\workshops\workshopWrapUpDocument.ts:171:        materials,
lib\workshops\workshopPollSharing.test.ts:31:    maximum_participant_count: null,
lib\workshops\workshopPollSharing.test.ts:170:    it('passes the verified e-mail and current participant to the one shared vote procedure', async () => {
lib\workshops\workshopPollSharing.test.ts:177:            { id: 'workshop-participant-id', email: MEMBER_EMAIL },
lib\workshops\workshopPollSharing.test.ts:187:            target_participant_id: 'workshop-participant-id',
lib\workshops\workshopWrapUpDocument.test.ts:40:    it('uses the selected workshop and the same readable materials as the participant room', () => {
lib\workshops\workshopWrapUpDocument.test.ts:53:        expect(document.materials).toEqual([
lib\workshops\workshopWrapUpDocument.test.ts:77:        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
lib\workshops\workshopWrapUpDocument.test.ts:78:        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
lib\workshops\workshopValues.ts:49:        maximum_participant_count: values.maximumParticipantCount,
lib\workshops\workshopValues.ts:50:        artificial_watching_participant_count: values.artificialWatchingParticipantCount,
lib\workshops\workshopValues.ts:75:            : { maximum_participant_count: values.maximumParticipantCount }),
lib\workshops\workshopValues.ts:78:            : { artificial_watching_participant_count: values.artificialWatchingParticipantCount }),
lib\workshops\workshopValues.ts:119: * Database names for the feedback fields which may be independently saved by the participant.
lib\workshops\workshopValues.ts:157: * The written moderation state of one participant, whether the administration or a moderator of the room wrote it
lib\workshops\workshopValues.ts:169:        participant_id: null,
lib\workshops\workshopParticipantRename.ts:11:    readonly participant: WorkshopParticipant;
lib\workshops\workshopParticipantRename.ts:24: * Renames a participant everywhere their name is shown: in the room header and on every comment they wrote.
lib\workshops\workshopParticipantRename.ts:28: * Note: The comments are renamed after the participant, because a participant renamed with stale comments still leads
lib\workshops\workshopParticipantRename.ts:29: *       to the name they asked for, while renamed comments of a participant who kept their old name would not.
lib\workshops\workshopParticipantRename.ts:34:    participantId: string,
lib\workshops\workshopParticipantRename.ts:37:    const { data: participantRow, error: participantError } = await supabase
lib\workshops\workshopParticipantRename.ts:40:        .eq('id', participantId)
lib\workshops\workshopParticipantRename.ts:45:    if (participantError || participantRow === null) {
lib\workshops\workshopParticipantRename.ts:47:            'Failed to rename a workshop participant:',
lib\workshops\workshopParticipantRename.ts:48:            participantError?.message ?? 'No participant returned',
lib\workshops\workshopParticipantRename.ts:53:    const participant = mapWorkshopParticipantRow(participantRow as WorkshopParticipantRow);
lib\workshops\workshopParticipantRename.ts:58:        .eq('participant_id', participantId)
lib\workshops\workshopParticipantRename.ts:63:        // The participant already carries their new name, so the rename is reported as done and only the older
lib\workshops\workshopParticipantRename.ts:65:        console.error('Failed to rename the comments of a workshop participant:', commentsError.message);
lib\workshops\workshopParticipantRename.ts:66:        return { participant, isVisibleCommentRenamed: false };
lib\workshops\workshopParticipantRename.ts:70:        participant,
lib\workshops\workshopTypes.ts:21: * The lifecycle of something a participant submits for moderation. Chat messages and community projects deliberately
lib\workshops\workshopTypes.ts:65: * Note: The participant count deliberately stays out of `WorkshopSummary`, so no public list of terms exposes how
lib\workshops\workshopTypes.ts:69:    readonly participantCount: number;
lib\workshops\workshopTypes.ts:75:     * Note: This is a different audience from `participantCount`, which counts the people who really entered the room.
lib\workshops\workshopTypes.ts:88:     * Where paid members begin an ended workshop's recording, in seconds from the beginning of its stream.
lib\workshops\workshopTypes.ts:91:     *       replay which the membership unlocks after the workshop ends.
lib\workshops\workshopTypes.ts:98:     * Note: The recording of an ended workshop is unlocked by the paid membership, and this is the snippet of it which
lib\workshops\workshopTypes.ts:106:     * published. It is a workshop setting rather than an ordinary content block, so every participant can open it
lib\workshops\workshopTypes.ts:136:     * Contact address this participant connected with
lib\workshops\workshopTypes.ts:138:     * Note: A room only ever describes the very participant reading it, so this address never reaches anybody else. It
lib\workshops\workshopTypes.ts:146:     * Whether the messages of this participant are approved as they are written
lib\workshops\workshopTypes.ts:148:     * Note: Trust stays invisible in the room. Nothing but the approval of their own messages tells a participant or
lib\workshops\workshopTypes.ts:154:     * Whether this participant moderates the room, which the room says with a badge
lib\workshops\workshopTypes.ts:168:     * Present when the participant is listed inside the community administration. A community membership belongs to
lib\workshops\workshopTypes.ts:169:     * their e-mail address rather than to this particular room session, so ordinary workshop participant data does
lib\workshops\workshopTypes.ts:176: * One server-paged slice of participants, keeping large workshops responsive in the administration.
lib\workshops\workshopTypes.ts:179:    readonly participants: readonly WorkshopAdminParticipant[];
lib\workshops\workshopTypes.ts:184: * One timestamped activity attributable to a participant.
lib\workshops\workshopTypes.ts:218:    readonly participant: WorkshopAdminParticipant;
lib\workshops\workshopTypes.ts:240:     * Note: How that is told apart is decided by `workshopAttendance`, and a bucket which was measured before the
lib\workshops\workshopTypes.ts:249:    readonly participantCount: number;
lib\workshops\workshopTypes.ts:334: * A participant's progressively saved reflection after one workshop.
lib\workshops\workshopTypes.ts:349: * The admin-only form of feedback, attributable to its participant and joinable to the private contact projection.
lib\workshops\workshopTypes.ts:354:        readonly participantId: string;
lib\workshops\workshopTypes.ts:362: * Note: Only a moderator receives this, so an ordinary participant never learns which invisible moderation state the
lib\workshops\workshopTypes.ts:366:    readonly participantId: string;
lib\workshops\workshopTypes.ts:387:     * Whether the administration created this message without a participant
lib\workshops\workshopTypes.ts:428: * Note: This counts reaction actions, rather than distinct people. A participant who reacts twice therefore adds two
lib\workshops\workshopTypes.ts:437: * One answer a member can choose in a poll. The room only receives aggregate counts and whether its own participant
lib\workshops\workshopTypes.ts:488:    readonly participant: WorkshopParticipant;
lib\workshops\workshopTypes.ts:491:     * How many participants had the room open recently, including the one this state was loaded for
lib\workshops\workshopTypes.ts:498:     * The materials which only paid members may see, named for a member who is not one of them
lib\workshops\workshopTypes.ts:500:     * Note: The materials themselves stay out of `contentBlocks`, so this list is what lets the room say where the
lib\workshops\workshopTypes.ts:501:     *       paid materials are, what they are called and how to unlock them. It is empty for a member who already paid
lib\workshops\workshopTypes.ts:542:    readonly participantId: string | null;
lib\workshops\workshopTypes.ts:571:    readonly participants: readonly WorkshopAdminParticipant[];
lib\workshops\workshopTypes.ts:572:    readonly participantCount: number;
lib\workshops\workshopSubmissionStatus.ts:4: * Recognizes the shared moderation lifecycle of a participant submission without tying project routes to chat-only
lib\workshops\workshopParticipantModeration.ts:10: * The moderation state of one participant after it was written
lib\workshops\workshopParticipantModeration.ts:13:    readonly participantId: string;
lib\workshops\workshopParticipantModeration.ts:30:        participantId: row.id,
lib\workshops\workshopParticipantModeration.ts:38: * Reads the moderation state one participant of one room carries right now
lib\workshops\workshopParticipantModeration.ts:46:    participantId: string,
lib\workshops\workshopParticipantModeration.ts:47:): Promise<{ readonly participant: ModeratedWorkshopParticipant | null; readonly errorMessage: string | null }> {
lib\workshops\workshopParticipantModeration.ts:51:        .eq('id', participantId)
lib\workshops\workshopParticipantModeration.ts:56:        return { participant: null, errorMessage: error.message };
lib\workshops\workshopParticipantModeration.ts:60:        participant: data === null ? null : mapModeratedWorkshopParticipantRow(data as ModeratedWorkshopParticipantRow),
lib\workshops\workshopParticipantModeration.ts:66: * Writes the moderation state of one participant of one room and reads it back
lib\workshops\workshopParticipantModeration.ts:70: * Note: The participant is looked for inside its own workshop, so no request can moderate somebody of another room.
lib\workshops\workshopParticipantModeration.ts:75:    participantId: string,
lib\workshops\workshopParticipantModeration.ts:77:): Promise<{ readonly participant: ModeratedWorkshopParticipant | null; readonly errorMessage: string | null }> {
lib\workshops\workshopParticipantModeration.ts:81:        .eq('id', participantId)
lib\workshops\workshopParticipantModeration.ts:87:        return { participant: null, errorMessage: error.message };
lib\workshops\workshopParticipantModeration.ts:91:        participant: data === null ? null : mapModeratedWorkshopParticipantRow(data as ModeratedWorkshopParticipantRow),
lib\workshops\workshopSpecialMaterials.ts:4: * Where a card which shares the material list belongs among the ordinary materials
lib\workshops\workshopSpecialMaterials.ts:11:     * After the ordinary materials, which is where a card belongs that adds to what the room already handed over
lib\workshops\workshopSpecialMaterials.ts:13:    | 'after-materials'
lib\workshops\workshopSpecialMaterials.ts:16:     * Before the ordinary materials while the member reading them does not pay for the community membership, and after
lib\workshops\workshopSpecialMaterials.ts:19:    | 'before-materials-until-paid';
lib\workshops\workshopSpecialMaterials.ts:22: * A participant-facing card which belongs in the material list without being an administrator-managed material.
lib\workshops\workshopSpecialMaterials.ts:26: *       material list still gives every participant the same predictable place to find them.
lib\workshops\workshopSpecialMaterials.ts:33:     * Where this card belongs in the material list, which is after the ordinary materials unless it says otherwise
lib\workshops\workshopSpecialMaterials.ts:49: * The special materials of a room divided around its ordinary materials, each group in the order it was supplied in
lib\workshops\workshopSpecialMaterials.ts:57: * Divides the special materials of a room into the ones which open its material list and the ones which close it.
lib\workshops\workshopSpecialMaterials.ts:70:        specialMaterial.placement === 'before-materials-until-paid' && !isPaidMember;
lib\workshops\workshopRegistrations.ts:7: *       who entered the live room are counted from the room's own participants instead.
lib\workshops\workshopRegistrations.ts:18:    readonly participantCount: number;
lib\workshops\workshopRegistrations.ts:43:const REGISTRATION_PARTICIPANT_COUNT_FIELD_NAME = 'participantCount';
lib\workshops\workshopRegistrations.ts:115:    const participantCount = registrationPayload[REGISTRATION_PARTICIPANT_COUNT_FIELD_NAME];
lib\workshops\workshopRegistrations.ts:120:        typeof participantCount !== 'number' ||
lib\workshops\workshopRegistrations.ts:121:        !Number.isSafeInteger(participantCount) ||
lib\workshops\workshopRegistrations.ts:122:        participantCount < 1
lib\workshops\workshopRegistrations.ts:127:    return { termId, participantCount };
lib\workshops\workshopRegistrations.ts:156:            return { termId, participantCount: SINGLE_PARTICIPANT_COUNT };
lib\workshops\workshopRegistrations.ts:229:            registeredParticipantCount + registration.participantCount,
lib\workshops\workshopSpecialMaterials.test.ts:12:    placement: 'before-materials-until-paid',
lib\workshops\workshopSpecialMaterials.test.ts:21:describe('placement of the special materials of a room', () => {
lib\workshops\workshopSpecialMaterials.test.ts:42:    it('leaves a card which says nothing about its placement after the ordinary materials of either member', () => {
lib\workshops\workshopRegistrations.test.ts:53:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 4 }),
lib\workshops\workshopRegistrations.test.ts:55:        ).toEqual({ termId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 4 });
lib\workshops\workshopRegistrations.test.ts:63:            JSON.stringify({ selectedDateId: '2026-09-09', participantCount: 2 }),
lib\workshops\workshopRegistrations.test.ts:66:        expect(readWorkshopRegistration(contactNote)).toEqual({ termId: '2026-09-09', participantCount: 2 });
lib\workshops\workshopRegistrations.test.ts:72:            participantCount: 1,
lib\workshops\workshopRegistrations.test.ts:79:            participantCount: 1,
lib\workshops\workshopRegistrations.test.ts:103:            JSON.stringify({ workshop: 'AI Supervize Mini', leadType: 'Interested, but cannot attend' }),
lib\workshops\workshopRegistrations.test.ts:113:                JSON.stringify({ selectedDateId: ONLINE_WORKSHOP_TERM.slug, participantCount: 0 }),
lib\workshops\workshopRegistrations.test.ts:126:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 3 }),
lib\workshops\workshopRegistrations.test.ts:138:            readWorkshopRegistration(JSON.stringify({ selectedDateId: '2026-09-09', participantCount: 4 })),
lib\workshops\workshopRegistrations.test.ts:140:                JSON.stringify({ selectedDateId: AI_SUPERVIZE_MINI_TERM.slug, participantCount: 1 }),
lib\workshops\workshopRegistrations.test.ts:176:                { termId: ONLINE_WORKSHOP_TERM.slug, participantCount: 1 },
lib\workshops\workshopRegistrations.test.ts:181:            isWorkshopRegistrationForTerm({ termId: '2026-08-26', participantCount: 1 }, ONLINE_WORKSHOP_TERM),
lib\workshops\workshopRegistrations.test.ts:185:                { termId: 'středa 26. 8. 2026 19:00', participantCount: 1 },
lib\workshops\workshopRegistrations.test.ts:191:                { termId: 'online-workshop-2026-09-02', participantCount: 1 },
lib\workshops\workshopSession.ts:25: * Columns every query needs to describe a participant to themselves
lib\workshops\workshopSession.ts:27: * Note: These are only ever read for the participant making the request, so their own contact address is among them.
lib\workshops\workshopSession.ts:70: * Takes the session of one room out of the browser, which is what really signs a participant out of it: the cookie is
lib\workshops\workshopSession.ts:98:): Promise<{ readonly participant: WorkshopParticipant; readonly sessionToken: string } | null> {
lib\workshops\workshopSession.ts:114:        console.error('Failed to connect a workshop participant:', error?.message ?? 'No participant returned');
lib\workshops\workshopSession.ts:118:    return { participant: mapWorkshopParticipantRow(data as WorkshopParticipantRow), sessionToken };
lib\workshops\workshopSession.ts:122: * Ends the room session of one participant, leaving everything they did in that room exactly where it is.
lib\workshops\workshopSession.ts:124: * Note: A participant row is the author of every message, reaction and measured minute they left behind, so signing
lib\workshops\workshopSession.ts:126: *       every participant rather than none at all, the token is replaced by a fresh one which no browser was ever
lib\workshops\workshopSession.ts:132:    participantId: string,
lib\workshops\workshopSession.ts:137:        .eq('id', participantId)
lib\workshops\workshopSession.ts:141:        console.error('Failed to end the session of a workshop participant:', error.message);
lib\workshops\workshopSession.ts:168:        console.error('Failed to authenticate a workshop participant:', error.message);
lib\workshops\workshopSession.ts:182:        console.error('Failed to update workshop participant activity:', activityError.message);
lib\workshops\workshopSession.test.ts:4:describe('workshop participant sessions', () => {
lib\workshops\workshopSession.test.ts:14:        const sessionToken = 'participant-session-token';
lib\workshops\workshopRegistrationTiming.test.ts:12:describe('workshop direct participant links', () => {
lib\workshops\workshopRegistrationTiming.test.ts:38:    it('carries valid participant details to the thank-you path only inside the direct-link window', () => {
lib\workshops\workshopRegistrationTiming.test.ts:39:        const participantIdentity = { email: 'jana@example.com', fullname: 'Jana Nováková' } as const;
lib\workshops\workshopRegistrationTiming.test.ts:46:                participantIdentity,
lib\workshops\workshopRegistrationTiming.test.ts:57:                participantIdentity,
lib\workshops\workshopSchemas.ts:48:    .refine(isWorkshopParticipantFullnameValid, 'A participant name is required');
lib\workshops\workshopSchemas.ts:317: * Every change a moderating role can make to one participant of a room
lib\workshops\workshopSchemas.ts:329:        message: 'At least one participant field is required',
lib\workshops\workshopSchemas.ts:335: * Note: A room which does not say how it was attended is taken to have been attended passively, so a page which was
lib\workshops\workshopSchemas.ts:475: * A participant can first submit a rating and then update each optional answer on its own request.
lib\workshops\workshopRepositoryMonitor.ts:122:    // Progress is newest-first. Sending oldest-first means that a participant who receives several commits in one
lib\workshops\workshopSchemas.test.ts:39:    it('normalizes participant identity before it is stored', () => {
lib\workshops\workshopSchemas.test.ts:46:    it('renames a participant under the very same rule the connection form uses', () => {
lib\workshops\workshopSchemas.test.ts:80:        expect(workshopCommentUpdateSchema.safeParse({ status: 'pending' }).success).toBe(false);
lib\workshops\workshopSchemas.test.ts:327:    it('writes which materials only paid members may see and refuses a content request without that answer', () => {
lib\workshops\workshopSchemas.test.ts:442:    it('validates artificial workshop actions independently from participant actions', () => {
lib\workshops\workshopSchemas.test.ts:466:    it('accepts trusted participant changes and bounded active-time reports', () => {
lib\workshops\workshopSchemas.test.ts:469:        expect(workshopPresenceSchema.parse({ activeDurationSeconds: 30, isActivelyAttending: true })).toEqual({
lib\workshops\workshopSchemas.test.ts:476:    it('takes a heartbeat which says nothing about its attendance as a passively attended room', () => {
lib\workshops\workshopRequest.ts:16:    readonly participant: WorkshopParticipant;
lib\workshops\workshopRequest.ts:33:    const participant = await authenticateWorkshopParticipant(supabase, request, workshopRow.id, workshopRow.slug);
lib\workshops\workshopRequest.ts:34:    if (participant === null) {
lib\workshops\workshopRequest.ts:38:    return { supabase, workshopRow, participant };
lib\workshops\workshopRequest.ts:55:    return isWorkshopParticipantModerating(authenticatedRequest.participant)
lib\workshops\workshopRegistrationTiming.ts:39:    readonly participantIdentity: WorkshopParticipantIdentity;
lib\workshops\workshopRegistrationTiming.ts:44: * Carries participant details to a registration confirmation page only when the direct-room-link window is open.
lib\workshops\workshopRegistrationTiming.ts:50:    participantIdentity,
lib\workshops\workshopRegistrationTiming.ts:57:    const searchParameters = createWorkshopParticipantSearchParameters(participantIdentity);
lib\workshops\workshopRequest.test.ts:10:        id: 'participant-1',
lib\workshops\workshopRequest.test.ts:20:describe('workshop participant interaction bans', () => {
lib\workshops\workshopRequest.test.ts:21:    it('allows watching participants and refuses chat interactions for banned participants', async () => {
lib\workshops\workshopParticipantLink.test.ts:11:    it('keeps the selected occurrence in a participant link together with the prefilled identity', () => {
lib\workshops\workshopParticipantLink.test.ts:14:                '/cs/online-workshop/participant',
lib\workshops\workshopParticipantLink.test.ts:19:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10&email=jana%40example.com&fullname=Jana+Nov%C3%A1kov%C3%A1',
lib\workshops\workshopParticipantLink.test.ts:23:    it('keeps the selected occurrence even without a prefilled participant identity', () => {
lib\workshops\workshopParticipantLink.test.ts:24:        expect(createWorkshopSelectionPath('/cs/online-workshop/participant', WORKSHOP_SLUG)).toBe(
lib\workshops\workshopParticipantLink.test.ts:25:            '/cs/online-workshop/participant?workshop=production-ai-2026-09-10',
lib\workshops\workshopParticipantLink.test.ts:29:                '/cs/online-workshop/participant',
lib\workshops\workshopParticipantLink.ts:26: * Reads participant details from URL search parameters using the same limits as the participant connection form.
lib\workshops\workshopParticipantLink.ts:72: * Creates the query parameters used to prefill a workshop participant connection form.
lib\workshops\workshopParticipantLink.ts:74: * Invalid or incomplete values do not produce a link. The participant room still validates its form server-side, but
lib\workshops\workshopParticipantLink.ts:99: * Note: This is what leads a participant into the one permanent room of the application, whose address is decided once
lib\workshops\workshopParticipantLink.ts:110: * Creates a site-relative participant URL with prefilled connection details.
lib\workshops\workshopParticipantLink.ts:113:    participantPath: string,
lib\workshops\workshopParticipantLink.ts:122:    return createWorkshopSelectionPath(participantPath, workshopSlug, searchParameters);
lib\workshops\workshopParticipantLink.ts:132:    participantPath: string,
lib\workshops\workshopParticipantLink.ts:137:        createWorkshopParticipantLink(participantPath, identity, workshopSlug) ??
lib\workshops\workshopParticipantLink.ts:138:        createWorkshopSelectionPath(participantPath, workshopSlug)

codex
The workspace already contains uncommitted files and edits specifically named for this feature, including a PDF document builder and tests. I’ll treat those as existing work—not overwrite them—and audit the implementation against the requested behavior before making only the necessary corrections.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --stat; git diff -- businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx businesses/online-workshop/participant/WorkshopStage.tsx businesses/online-workshop/participant/WorkshopWrapUp.tsx changelog/_current-preversion.md; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx; Get-Content -Raw lib/workshops/workshopWrapUpDocument.ts; Get-Content -Raw lib/exports/workshopWrapUpPdf.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 518ms:
 AGENTS.md                                                                |   5 +-
 businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx |   1 +
 businesses/online-workshop/participant/WorkshopStage.test.tsx            |   1 +
 businesses/online-workshop/participant/WorkshopStage.tsx                 |   4 ++
 businesses/online-workshop/participant/WorkshopWrapUp.tsx                |  11 ++++
 changelog/_current-preversion.md                                         |   6 ++
 lib/exports/svgChartExport.ts                                            | 118 ++++------------------------------------
 prompts/2026-09-0240-wrap-up-pdf.md                                      |   2 +-
 8 files changed, 38 insertions(+), 110 deletions(-)
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e017054..3df3571 100644
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@ export function OnlineWorkshopParticipantPage({
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83..eca1968 100644
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@ type WorkshopStageProps = {
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@ export function WorkshopStage({
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@ export function WorkshopStage({
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938f..dec8363 100644
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@ type WorkshopWrapUpProps = {
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@ export function WorkshopWrapUp({
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf..9881072 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are
'use client';

import { downloadBlobFile } from '@/lib/downloadBlobFile';
import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
import {
    createWorkshopWrapUpDocument,
    createWorkshopWrapUpPdfFileName,
} from '@/lib/workshops/workshopWrapUpDocument';
import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
import { Download, LoaderCircle } from 'lucide-react';
import { useState } from 'react';

type WorkshopWrapUpPdfDownloadProps = {
    readonly workshop: Pick<
        WorkshopDetails,
        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
    >;

    /** The same participant-visible materials rendered in the room below the stage. */
    readonly contentBlocks: readonly WorkshopContentBlock[];
};

/**
 * Lets an attendee take the information already visible in a completed room away as one printable document.
 *
 * Note: This owns only the browser interaction. The document factory decides its contents and the shared download
 * helper starts the file download, so this button cannot introduce a second material source or download path.
 */
export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
    const [isDownloadInProgress, setIsDownloadInProgress] = useState(false);
    const [errorMessage, setErrorMessage] = useState<string | null>(null);

    const downloadWorkshopWrapUp = async () => {
        setIsDownloadInProgress(true);
        setErrorMessage(null);

        try {
            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
            const blob = await createWorkshopWrapUpPdfBlob(document);

            downloadBlobFile({ fileName: createWorkshopWrapUpPdfFileName(workshop.slug), blob });
        } catch {
            setErrorMessage('Shrnutí se nepodařilo stáhnout. Zkuste to prosím znovu.');
        } finally {
            setIsDownloadInProgress(false);
        }
    };

    return (
        <div className="mt-5">
            <button
                type="button"
                onClick={() => void downloadWorkshopWrapUp()}
                disabled={isDownloadInProgress}
                className="inline-flex items-center gap-2 rounded-full border border-cyan-200/60 bg-cyan-300/10 px-5 py-2.5 text-sm font-bold text-cyan-100 shadow-lg shadow-cyan-300/10 transition hover:border-cyan-100 hover:bg-cyan-300/20 disabled:cursor-wait disabled:opacity-70 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
            >
                {isDownloadInProgress ? (
                    <LoaderCircle className="h-4 w-4 animate-spin" aria-hidden="true" />
                ) : (
                    <Download className="h-4 w-4" aria-hidden="true" />
                )}
                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
            </button>
            {errorMessage !== null && (
                <p role="alert" className="mt-2 text-sm text-rose-200">
                    {errorMessage}
                </p>
            )}
        </div>
    );
}

import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';

const MAXIMAL_KEY_TAKEAWAY_COUNT = 8;
const PRESENTATION_MATERIAL_TITLE = 'Prezentace';
const UNTITLED_MATERIAL_TITLE = 'Materiál z workshopu';
const MARKDOWN_LINK_PATTERN = /!?\[([^\]]*)\]\((?:<[^>]*>|[^)]*)\)/g;
const HTML_TAG_PATTERN = /<[^>]*>/g;
const MARKDOWN_DECORATION_PATTERN = /[`*_~]/g;
const MARKDOWN_PREFIX_PATTERN = /^\s{0,3}(?:#{1,6}\s+|>\s?|[-*+]\s+|\d+[.)]\s+)/;
const MARKDOWN_HEADING_PATTERN = /^\s{0,3}#{1,6}\s+/;
const MARKDOWN_LIST_ITEM_PATTERN = /^\s{0,3}(?:[-*+]\s+|\d+[.)]\s+)/;
const HTML_ENTITY_BY_TEXT: Readonly<Record<string, string>> = {
    '&amp;': '&',
    '&gt;': '>',
    '&lt;': '<',
    '&nbsp;': ' ',
    '&quot;': '"',
};

export type WorkshopWrapUpMaterial = {
    readonly title: string;
    readonly body: string;
    readonly urls: readonly string[];
    readonly isFollowUp: boolean;
};

/**
 * The participant-safe data which a completed workshop hands over as a PDF.
 *
 * Note: Its materials come from the same already-filtered room state as the visible material cards. A member can
 * therefore never export the body or URL of a paid material which the room itself withheld from them.
 */
export type WorkshopWrapUpDocument = {
    readonly title: string;
    readonly dateAndTimeLabel: string;
    readonly summary: string;
    readonly keyTakeaways: readonly string[];
    readonly materials: readonly WorkshopWrapUpMaterial[];
};

type WorkshopWrapUpDocumentInput = {
    readonly workshop: Pick<WorkshopDetails, 'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'>;
    readonly contentBlocks: readonly WorkshopContentBlock[];
};

function decodeHtmlEntities(text: string): string {
    return text.replace(/&(amp|gt|lt|nbsp|quot);/g, (entity) => HTML_ENTITY_BY_TEXT[entity] ?? entity);
}

function removeMarkdownLinePrefix(line: string): string {
    return line.replace(MARKDOWN_PREFIX_PATTERN, '');
}

/**
 * Makes authored Markdown readable in a document without trying to become a second Markdown renderer.
 *
 * Note: Links keep their authored label here and their public URLs are supplied separately from the shared material
 * link parser, so a reader gets both the explanation and an address they can open.
 */
export function extractWorkshopWrapUpPlainText(markdown: string): string {
    return decodeHtmlEntities(
        markdown
            .replace(MARKDOWN_LINK_PATTERN, '$1')
            .replace(HTML_TAG_PATTERN, ' ')
            .split(/\r?\n/)
            .map(removeMarkdownLinePrefix)
            .map((line) => line.replace(MARKDOWN_DECORATION_PATTERN, '').replace(/\s+/g, ' ').trim())
            .filter((line) => line !== '')
            .join('\n'),
    );
}

function getWorkshopWrapUpMaterialTitle(contentBlock: Pick<WorkshopContentBlock, 'title'>): string {
    return contentBlock.title.trim() || UNTITLED_MATERIAL_TITLE;
}

function createWorkshopWrapUpMaterial(contentBlock: WorkshopContentBlock): WorkshopWrapUpMaterial {
    return {
        title: getWorkshopWrapUpMaterialTitle(contentBlock),
        body: extractWorkshopWrapUpPlainText(contentBlock.bodyMarkdown),
        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
        isFollowUp: contentBlock.isFollowUp,
    };
}

function createWorkshopPresentationMaterial(presentationUrl: string): WorkshopWrapUpMaterial {
    return {
        title: PRESENTATION_MATERIAL_TITLE,
        body: '',
        urls: [presentationUrl],
        isFollowUp: false,
    };
}

function getMaterialKeyTakeaways(contentBlock: WorkshopContentBlock): readonly string[] {
    const authoredTakeaways = contentBlock.bodyMarkdown
        .split(/\r?\n/)
        .filter((line) => MARKDOWN_HEADING_PATTERN.test(line) || MARKDOWN_LIST_ITEM_PATTERN.test(line))
        .map(removeMarkdownLinePrefix)
        .map((line) => extractWorkshopWrapUpPlainText(line))
        .filter((line) => line !== '');

    return [
        ...(contentBlock.isFollowUp ? [getWorkshopWrapUpMaterialTitle(contentBlock)] : []),
        ...authoredTakeaways,
    ];
}

function getUniqueKeyTakeaways(values: readonly string[]): readonly string[] {
    const normalizedValues = new Set<string>();
    const keyTakeaways: string[] = [];

    for (const value of values) {
        const trimmedValue = value.trim();
        const normalizedValue = trimmedValue.toLocaleLowerCase('cs-CZ');
        const isNewValue = trimmedValue !== '' && !normalizedValues.has(normalizedValue);

        if (isNewValue) {
            normalizedValues.add(normalizedValue);
            keyTakeaways.push(trimmedValue);
        }

        if (keyTakeaways.length === MAXIMAL_KEY_TAKEAWAY_COUNT) {
            break;
        }
    }

    return keyTakeaways;
}

function getWorkshopWrapUpKeyTakeaways(
    workshop: Pick<WorkshopDetails, 'title' | 'description'>,
    contentBlocks: readonly WorkshopContentBlock[],
): readonly string[] {
    const materialKeyTakeaways = getUniqueKeyTakeaways([
        ...contentBlocks.flatMap(getMaterialKeyTakeaways),
        ...contentBlocks.map(getWorkshopWrapUpMaterialTitle),
    ]);

    return materialKeyTakeaways.length > 0
        ? materialKeyTakeaways
        : getUniqueKeyTakeaways([workshop.description.trim(), workshop.title.trim()]);
}

/**
 * Builds the data of the wrap-up from fields which the participant room already owns.
 *
 * Note: It does not infer new advice or call a service. The summary is the workshop's authored description, key
 * takeaways are headings, list items and titles already shared with the attendee, and every listed material is one
 * that the existing room state made readable.
 */
export function createWorkshopWrapUpDocument({
    workshop,
    contentBlocks,
}: WorkshopWrapUpDocumentInput): WorkshopWrapUpDocument {
    const materials = [
        ...contentBlocks.map(createWorkshopWrapUpMaterial),
        ...(workshop.presentationUrl === null ? [] : [createWorkshopPresentationMaterial(workshop.presentationUrl)]),
    ];

    return {
        title: workshop.title,
        dateAndTimeLabel: `${formatCzechWorkshopDate(workshop.startsAt)} · ${formatCzechWorkshopTimeRange(
            workshop.startsAt,
            workshop.endsAt,
        )}`,
        summary: workshop.description.trim() || workshop.title,
        keyTakeaways: getWorkshopWrapUpKeyTakeaways(workshop, contentBlocks),
        materials,
    };
}

/**
 * Keeps the browser download name stable and safe while making every selected occurrence easy to recognize later.
 */
export function createWorkshopWrapUpPdfFileName(workshopSlug: string): string {
    return `${workshopSlug}-shrnuti-workshopu.pdf`;
}

import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
import type { WorkshopWrapUpDocument, WorkshopWrapUpMaterial } from '@/lib/workshops/workshopWrapUpDocument';

const PDF_PAGE_WIDTH_PIXELS = 1240;
const PDF_PAGE_HEIGHT_PIXELS = 1754;
const PDF_PAGE_MARGIN_PIXELS = 88;
const PDF_PAGE_HEADER_BOTTOM_PIXELS = 156;
const PDF_PAGE_FOOTER_TOP_PIXELS = 1650;
const PDF_PAGE_WIDTH_POINTS = 595;
const PDF_PAGE_HEIGHT_POINTS = 842;
const PDF_JPEG_QUALITY = 0.92;
const PDF_BRAND_COLOR = '#0b4f60';
const PDF_ACCENT_COLOR = '#0ea5b7';
const PDF_TEXT_COLOR = '#102a36';
const PDF_MUTED_TEXT_COLOR = '#526873';
const PDF_BACKGROUND_COLOR = '#f7fbfc';
const PDF_SECTION_TITLE_FONT = '700 32px "Segoe UI", Arial, sans-serif';
const PDF_DOCUMENT_TITLE_FONT = '700 56px "Segoe UI", Arial, sans-serif';
const PDF_BODY_FONT = '400 28px "Segoe UI", Arial, sans-serif';
const PDF_BODY_BOLD_FONT = '700 28px "Segoe UI", Arial, sans-serif';
const PDF_METADATA_FONT = '600 24px "Segoe UI", Arial, sans-serif';
const PDF_FOOTER_FONT = '400 21px "Segoe UI", Arial, sans-serif';
const PDF_DOCUMENT_TITLE_LINE_HEIGHT = 66;
const PDF_SECTION_TITLE_LINE_HEIGHT = 42;
const PDF_BODY_LINE_HEIGHT = 42;
const PDF_METADATA_LINE_HEIGHT = 34;
const PDF_PARAGRAPH_GAP_PIXELS = 20;
const PDF_SECTION_GAP_PIXELS = 38;
const PDF_LIST_INDENT_PIXELS = 38;
const PDF_MATERIAL_GAP_PIXELS = 26;

type WrapUpPdfPage = {
    readonly canvas: HTMLCanvasElement;
    readonly context: CanvasRenderingContext2D;
    contentPositionY: number;
};

type WrapUpPdfRenderState = {
    readonly pages: WrapUpPdfPage[];
};

type PdfTextStyle = {
    readonly color: string;
    readonly font: string;
    readonly lineHeight: number;
};

const BODY_TEXT_STYLE: PdfTextStyle = {
    color: PDF_TEXT_COLOR,
    font: PDF_BODY_FONT,
    lineHeight: PDF_BODY_LINE_HEIGHT,
};

const MATERIAL_TITLE_TEXT_STYLE: PdfTextStyle = {
    color: PDF_BRAND_COLOR,
    font: PDF_BODY_BOLD_FONT,
    lineHeight: PDF_BODY_LINE_HEIGHT,
};

const SECTION_TITLE_TEXT_STYLE: PdfTextStyle = {
    color: PDF_BRAND_COLOR,
    font: PDF_SECTION_TITLE_FONT,
    lineHeight: PDF_SECTION_TITLE_LINE_HEIGHT,
};

const DOCUMENT_TITLE_TEXT_STYLE: PdfTextStyle = {
    color: PDF_TEXT_COLOR,
    font: PDF_DOCUMENT_TITLE_FONT,
    lineHeight: PDF_DOCUMENT_TITLE_LINE_HEIGHT,
};

const METADATA_TEXT_STYLE: PdfTextStyle = {
    color: PDF_MUTED_TEXT_COLOR,
    font: PDF_METADATA_FONT,
    lineHeight: PDF_METADATA_LINE_HEIGHT,
};

function getPdfContentWidth(): number {
    return PDF_PAGE_WIDTH_PIXELS - 2 * PDF_PAGE_MARGIN_PIXELS;
}

function createWrapUpPdfPage(): WrapUpPdfPage {
    const canvas = document.createElement('canvas');
    canvas.width = PDF_PAGE_WIDTH_PIXELS;
    canvas.height = PDF_PAGE_HEIGHT_PIXELS;

    const context = canvas.getContext('2d');
    if (context === null) {
        throw new Error('Shrnutí se nepodařilo připravit k tisku');
    }

    context.fillStyle = PDF_BACKGROUND_COLOR;
    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, PDF_PAGE_HEIGHT_PIXELS);
    context.fillStyle = PDF_ACCENT_COLOR;
    context.fillRect(0, 0, PDF_PAGE_WIDTH_PIXELS, 14);
    context.fillStyle = PDF_BRAND_COLOR;
    context.font = PDF_METADATA_FONT;
    context.fillText('PROMPTBOOK', PDF_PAGE_MARGIN_PIXELS, 73);
    context.fillStyle = PDF_MUTED_TEXT_COLOR;
    context.font = PDF_FOOTER_FONT;
    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
    context.strokeStyle = '#c9e7eb';
    context.lineWidth = 2;
    context.beginPath();
    context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
    context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_HEADER_BOTTOM_PIXELS - 1);
    context.stroke();

    return { canvas, context, contentPositionY: PDF_PAGE_HEADER_BOTTOM_PIXELS + 52 };
}

function getCurrentPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
    const currentPage = renderState.pages[renderState.pages.length - 1];
    if (currentPage === undefined) {
        throw new Error('Shrnutí nemá stránku k vykreslení');
    }

    return currentPage;
}

function addWrapUpPdfPage(renderState: WrapUpPdfRenderState): WrapUpPdfPage {
    const nextPage = createWrapUpPdfPage();
    renderState.pages.push(nextPage);
    return nextPage;
}

function ensurePdfVerticalSpace(renderState: WrapUpPdfRenderState, requiredHeight: number): WrapUpPdfPage {
    const currentPage = getCurrentPage(renderState);
    const isNewPageNeeded = currentPage.contentPositionY + requiredHeight > PDF_PAGE_FOOTER_TOP_PIXELS;

    return isNewPageNeeded ? addWrapUpPdfPage(renderState) : currentPage;
}

function splitLongPdfWord(context: CanvasRenderingContext2D, word: string, maximumWidth: number): readonly string[] {
    const wordParts: string[] = [];
    let currentWordPart = '';

    for (const character of word) {
        const nextWordPart = currentWordPart + character;
        if (currentWordPart !== '' && context.measureText(nextWordPart).width > maximumWidth) {
            wordParts.push(currentWordPart);
            currentWordPart = character;
        } else {
            currentWordPart = nextWordPart;
        }
    }

    if (currentWordPart !== '') {
        wordParts.push(currentWordPart);
    }

    return wordParts;
}

function wrapPdfLine(context: CanvasRenderingContext2D, line: string, maximumWidth: number): readonly string[] {
    const words = line.trim().split(/\s+/).filter((word) => word !== '');
    const lines: string[] = [];
    let currentLine = '';

    for (const word of words) {
        const nextLine = currentLine === '' ? word : `${currentLine} ${word}`;
        if (context.measureText(nextLine).width <= maximumWidth) {
            currentLine = nextLine;
            continue;
        }

        if (currentLine !== '') {
            lines.push(currentLine);
            currentLine = '';
        }

        const wordParts = splitLongPdfWord(context, word, maximumWidth);
        const finalWordPart = wordParts[wordParts.length - 1] ?? '';
        lines.push(...wordParts.slice(0, -1));
        currentLine = finalWordPart;
    }

    if (currentLine !== '') {
        lines.push(currentLine);
    }

    return lines;
}

function getWrappedPdfLines(
    context: CanvasRenderingContext2D,
    text: string,
    maximumWidth: number,
): readonly string[] {
    return text.split('\n').flatMap((line) => (line.trim() === '' ? [''] : wrapPdfLine(context, line, maximumWidth)));
}

function drawPdfTextLines(
    renderState: WrapUpPdfRenderState,
    lines: readonly string[],
    textStyle: PdfTextStyle,
    offsetX = PDF_PAGE_MARGIN_PIXELS,
): void {
    for (const line of lines) {
        const currentPage = ensurePdfVerticalSpace(renderState, textStyle.lineHeight);
        currentPage.context.font = textStyle.font;
        currentPage.context.fillStyle = textStyle.color;

        if (line !== '') {
            currentPage.context.fillText(line, offsetX, currentPage.contentPositionY);
        }
        currentPage.contentPositionY += textStyle.lineHeight;
    }
}

function addPdfVerticalGap(renderState: WrapUpPdfRenderState, gap: number): void {
    const currentPage = ensurePdfVerticalSpace(renderState, gap);
    currentPage.contentPositionY += gap;
}

function addPdfSectionTitle(renderState: WrapUpPdfRenderState, title: string): void {
    addPdfVerticalGap(renderState, PDF_SECTION_GAP_PIXELS);
    const currentPage = ensurePdfVerticalSpace(renderState, SECTION_TITLE_TEXT_STYLE.lineHeight + PDF_PARAGRAPH_GAP_PIXELS);
    currentPage.context.font = SECTION_TITLE_TEXT_STYLE.font;
    const lines = getWrappedPdfLines(currentPage.context, title, getPdfContentWidth());
    drawPdfTextLines(renderState, lines, SECTION_TITLE_TEXT_STYLE);
    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
}

function addPdfParagraph(renderState: WrapUpPdfRenderState, text: string, textStyle = BODY_TEXT_STYLE): void {
    const currentPage = getCurrentPage(renderState);
    currentPage.context.font = textStyle.font;
    const lines = getWrappedPdfLines(currentPage.context, text, getPdfContentWidth());
    drawPdfTextLines(renderState, lines, textStyle);
    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
}

function addPdfBulletList(renderState: WrapUpPdfRenderState, items: readonly string[]): void {
    for (const item of items) {
        const currentPage = getCurrentPage(renderState);
        currentPage.context.font = BODY_TEXT_STYLE.font;
        const lines = getWrappedPdfLines(
            currentPage.context,
            item,
            getPdfContentWidth() - PDF_LIST_INDENT_PIXELS,
        );
        const firstLine = lines[0] ?? '';
        const remainingLines = lines.slice(1);
        const bulletPage = ensurePdfVerticalSpace(renderState, BODY_TEXT_STYLE.lineHeight);
        bulletPage.context.fillStyle = PDF_ACCENT_COLOR;
        bulletPage.context.font = PDF_BODY_BOLD_FONT;
        bulletPage.context.fillText('•', PDF_PAGE_MARGIN_PIXELS, bulletPage.contentPositionY);
        drawPdfTextLines(renderState, [firstLine], BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
        drawPdfTextLines(renderState, remainingLines, BODY_TEXT_STYLE, PDF_PAGE_MARGIN_PIXELS + PDF_LIST_INDENT_PIXELS);
    }

    addPdfVerticalGap(renderState, PDF_PARAGRAPH_GAP_PIXELS);
}

function getMaterialLabel(material: WorkshopWrapUpMaterial): string {
    return material.isFollowUp ? `Navazující materiál: ${material.title}` : material.title;
}

function addPdfMaterial(renderState: WrapUpPdfRenderState, material: WorkshopWrapUpMaterial): void {
    addPdfVerticalGap(renderState, PDF_MATERIAL_GAP_PIXELS);
    addPdfParagraph(renderState, getMaterialLabel(material), MATERIAL_TITLE_TEXT_STYLE);

    if (material.body !== '') {
        addPdfParagraph(renderState, material.body);
    }

    if (material.urls.length > 0) {
        addPdfParagraph(renderState, material.urls.length === 1 ? 'Odkaz na materiál:' : 'Odkazy na materiály:', METADATA_TEXT_STYLE);
        addPdfBulletList(renderState, material.urls);
    }
}

function addPdfDocumentTitle(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
    const currentPage = getCurrentPage(renderState);
    currentPage.context.font = DOCUMENT_TITLE_TEXT_STYLE.font;
    const titleLines = getWrappedPdfLines(currentPage.context, document.title, getPdfContentWidth());
    drawPdfTextLines(renderState, titleLines, DOCUMENT_TITLE_TEXT_STYLE);
    addPdfVerticalGap(renderState, 8);
    addPdfParagraph(renderState, document.dateAndTimeLabel, METADATA_TEXT_STYLE);
}

function addPdfDocumentSections(renderState: WrapUpPdfRenderState, document: WorkshopWrapUpDocument): void {
    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
    addPdfParagraph(renderState, document.summary);

    addPdfSectionTitle(renderState, 'Klíčové poznatky');
    if (document.keyTakeaways.length === 0) {
        addPdfParagraph(renderState, 'V materiálech nebyly samostatné poznatky uvedeny.');
    } else {
        addPdfBulletList(renderState, document.keyTakeaways);
    }

    addPdfSectionTitle(renderState, 'Sdílené materiály');
    if (document.materials.length === 0) {
        addPdfParagraph(renderState, 'Během workshopu nebyly sdíleny žádné další materiály.');
    } else {
        document.materials.forEach((material) => addPdfMaterial(renderState, material));
    }
}

function drawPdfPageFooter(page: WrapUpPdfPage, pageIndex: number, pageCount: number): void {
    page.context.strokeStyle = '#c9e7eb';
    page.context.lineWidth = 2;
    page.context.beginPath();
    page.context.moveTo(PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
    page.context.lineTo(PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS - 24);
    page.context.stroke();
    page.context.font = PDF_FOOTER_FONT;
    page.context.fillStyle = PDF_MUTED_TEXT_COLOR;
    page.context.fillText('promptbook.studio', PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
    page.context.textAlign = 'right';
    page.context.fillText(`Strana ${pageIndex + 1} z ${pageCount}`, PDF_PAGE_WIDTH_PIXELS - PDF_PAGE_MARGIN_PIXELS, PDF_PAGE_FOOTER_TOP_PIXELS + 16);
    page.context.textAlign = 'left';
}

function renderWorkshopWrapUpDocument(document: WorkshopWrapUpDocument): readonly WrapUpPdfPage[] {
    const renderState: WrapUpPdfRenderState = {
        pages: [createWrapUpPdfPage()],
    };

    addPdfDocumentTitle(renderState, document);
    addPdfDocumentSections(renderState, document);
    renderState.pages.forEach((page, pageIndex) => drawPdfPageFooter(page, pageIndex, renderState.pages.length));

    return renderState.pages;
}

async function createPdfPicture(page: WrapUpPdfPage): Promise<PdfPicture> {
    const blob = await new Promise<Blob>((resolve, reject) => {
        page.canvas.toBlob(
            (canvasBlob) =>
                canvasBlob === null
                    ? reject(new Error('Shrnutí se nepodařilo uložit jako PDF'))
                    : resolve(canvasBlob),
            'image/jpeg',
            PDF_JPEG_QUALITY,
        );
    });

    return {
        bytes: new Uint8Array(await blob.arrayBuffer()),
        width: page.canvas.width,
        height: page.canvas.height,
    };
}

/**
 * Renders the participant-safe wrap-up into an A4 PDF in the browser.
 *
 * Note: The PDF is deliberately generated only after a person asks for it. No participant content leaves the room,
 * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.
 */
export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
    const pages = renderWorkshopWrapUpDocument(document);
    const pictures = await Promise.all(pages.map(createPdfPicture));

    return createPicturePdf(
        pictures,
        { width: PDF_PAGE_WIDTH_POINTS, height: PDF_PAGE_HEIGHT_POINTS, margin: 0 },
        document.title,
    );
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx; Get-Content -Raw lib/workshops/workshopWrapUpDocument.test.ts; Get-Content -Raw lib/exports/pdfPictures.ts; Get-Content -Raw lib/exports/pdfPictures.test.ts; Get-Content -Raw businesses/online-workshop/participant/WorkshopStage.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx; Get-Content -Raw lib/workshops/workshopTypes.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 278ms:
/**
 * @vitest-environment jsdom
 */

import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';

const pdfExportMocks = vi.hoisted(() => ({
    createWorkshopWrapUpPdfBlob: vi.fn(),
    downloadBlobFile: vi.fn(),
}));

vi.mock('@/lib/exports/workshopWrapUpPdf', () => ({
    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
}));

vi.mock('@/lib/downloadBlobFile', () => ({
    downloadBlobFile: (options: unknown) => pdfExportMocks.downloadBlobFile(options),
}));

import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';

const WORKSHOP: Pick<
    WorkshopDetails,
    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
> = {
    slug: 'produkcni-kod-2026-08-21',
    title: 'Produkční kód s AI agenty',
    description: 'Praktický workshop s AI agenty.',
    startsAt: '2026-08-21T19:00:00+02:00',
    endsAt: '2026-08-21T20:30:00+02:00',
    presentationUrl: null,
};

const CONTENT_BLOCK: WorkshopContentBlock = {
    id: 'material-1',
    title: 'Podklady z workshopu',
    bodyMarkdown: '[Otevřít podklady](https://ptbk.io/material-123)',
    unlockAt: '2026-08-21T19:00:00+02:00',
    sortOrder: 0,
    isPublished: true,
    isFollowUp: false,
    isPaidMembersOnly: false,
    createdAt: '2026-08-21T18:00:00+02:00',
    updatedAt: '2026-08-21T18:00:00+02:00',
    linkClickCount: 0,
};

beforeEach(() => {
    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
    pdfExportMocks.downloadBlobFile.mockReset();
});

afterEach(() => {
    cleanup();
});

describe('workshop wrap-up PDF download', () => {
    it('creates and downloads a PDF from the workshop and the materials already visible to this attendee', async () => {
        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);

        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));

        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
            expect.objectContaining({
                title: WORKSHOP.title,
                summary: WORKSHOP.description,
                materials: [
                    expect.objectContaining({
                        title: CONTENT_BLOCK.title,
                        urls: ['https://ptbk.io/material-123'],
                    }),
                ],
            }),
        );
        expect(pdfExportMocks.downloadBlobFile).toHaveBeenCalledWith(
            expect.objectContaining({ fileName: 'produkcni-kod-2026-08-21-shrnuti-workshopu.pdf' }),
        );
    });

    it('keeps the button usable and explains a failed PDF render', async () => {
        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);

        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
        fireEvent.click(downloadButton);

        expect((await screen.findByRole('alert')).textContent).toContain('Shrnutí se nepodařilo stáhnout');
        expect(downloadButton.hasAttribute('disabled')).toBe(false);
    });
});

import {
    createWorkshopWrapUpDocument,
    createWorkshopWrapUpPdfFileName,
    extractWorkshopWrapUpPlainText,
} from '@/lib/workshops/workshopWrapUpDocument';
import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
import { describe, expect, it } from 'vitest';

const WORKSHOP: Pick<
    WorkshopDetails,
    'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
> = {
    slug: 'produkcni-kod-2026-08-21',
    title: 'Produkční kód s AI agenty',
    description: 'Praktický online workshop o psaní produkčního kódu s AI agenty.',
    startsAt: '2026-08-21T19:00:00+02:00',
    endsAt: '2026-08-21T20:30:00+02:00',
    presentationUrl: 'https://files.example.com/prezentace.pdf',
};

const CONTENT_BLOCK: WorkshopContentBlock = {
    id: 'material-1',
    title: 'Postup pro nasazení',
    bodyMarkdown: `## Nejdůležitější kroky
- Napište ověřitelný plán.
- Nechte agenta spustit testy.

[Podklady](https://ptbk.io/material-123)`,
    unlockAt: '2026-08-21T19:00:00+02:00',
    sortOrder: 0,
    isPublished: true,
    isFollowUp: true,
    isPaidMembersOnly: false,
    createdAt: '2026-08-21T18:00:00+02:00',
    updatedAt: '2026-08-21T18:00:00+02:00',
    linkClickCount: 0,
};

describe('workshop wrap-up document', () => {
    it('uses the selected workshop and the same readable materials as the participant room', () => {
        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });

        expect(document.title).toBe(WORKSHOP.title);
        expect(document.dateAndTimeLabel).toContain('21.');
        expect(document.dateAndTimeLabel).toContain('19:00–20:30');
        expect(document.summary).toBe(WORKSHOP.description);
        expect(document.keyTakeaways).toEqual([
            'Postup pro nasazení',
            'Nejdůležitější kroky',
            'Napište ověřitelný plán.',
            'Nechte agenta spustit testy.',
        ]);
        expect(document.materials).toEqual([
            {
                title: 'Postup pro nasazení',
                body: 'Nejdůležitější kroky\nNapište ověřitelný plán.\nNechte agenta spustit testy.\nPodklady',
                urls: ['https://ptbk.io/material-123'],
                isFollowUp: true,
            },
            {
                title: 'Prezentace',
                body: '',
                urls: [WORKSHOP.presentationUrl],
                isFollowUp: false,
            },
        ]);
    });

    it('does not invent a hidden material when only the readable content blocks reach the document factory', () => {
        const visibleContentBlock = { ...CONTENT_BLOCK, title: 'Viditelné podklady' };
        const hiddenContentBlock = { ...CONTENT_BLOCK, id: 'hidden-material', title: 'Pouze pro členy' };
        const document = createWorkshopWrapUpDocument({
            workshop: { ...WORKSHOP, presentationUrl: null },
            contentBlocks: [visibleContentBlock],
        });

        expect(document.materials.map((material) => material.title)).toEqual(['Viditelné podklady']);
        expect(document.materials.map((material) => material.title)).not.toContain(hiddenContentBlock.title);
    });

    it('turns authored Markdown into readable document text while keeping link addresses separately', () => {
        expect(extractWorkshopWrapUpPlainText('**Důležité** [čtení](https://example.com) &amp; <em>praxe</em>')).toBe(
            'Důležité čtení & praxe',
        );
    });

    it('uses the stable workshop slug in the PDF file name', () => {
        expect(createWorkshopWrapUpPdfFileName(WORKSHOP.slug)).toBe('produkcni-kod-2026-08-21-shrnuti-workshopu.pdf');
    });
});

/**
 * The smallest reusable PDF writer needed for browser-generated documents made from JPEG pictures.
 *
 * Note: Canvas is responsible for drawing Unicode text before it reaches this writer. A PDF can then carry those
 * pixels without embedding a second font or relying on a reader's local font collection.
 */

export type PdfPicture = {
    readonly bytes: Uint8Array;
    readonly width: number;
    readonly height: number;
};

export type PdfPageSize = {
    readonly width: number;
    readonly height: number;
    readonly margin: number;
};

/**
 * Where one part of the written document begins, counted in bytes from its very beginning
 */
function getByteLength(chunks: readonly Uint8Array[]): number {
    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
}

function encodeAsciiText(text: string): Uint8Array {
    return new TextEncoder().encode(text);
}

function formatCrossReferenceEntry(objectOffset: number): string {
    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
}

/**
 * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16.
 *
 * Note: This is used for metadata instead of visible page text. Visible Czech text is already drawn into a picture,
 * which makes it read the same in every PDF reader without making the document embed a font.
 */
function formatPdfTextString(text: string): string {
    const characterCodes = ['FEFF'];

    for (const character of text) {
        const codePoint = character.codePointAt(0) ?? 0;

        if (codePoint > 0xffff) {
            const surrogateOffset = codePoint - 0x10000;
            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
        } else {
            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
        }
    }

    return `<${characterCodes.join('').toUpperCase()}>`;
}

function getPdfPicturePlacement(
    picture: Pick<PdfPicture, 'width' | 'height'>,
    pageSize: PdfPageSize,
): {
    readonly width: number;
    readonly height: number;
    readonly offsetX: number;
    readonly offsetY: number;
} {
    const availableWidth = pageSize.width - 2 * pageSize.margin;
    const availableHeight = pageSize.height - 2 * pageSize.margin;
    const scale = Math.min(availableWidth / picture.width, availableHeight / picture.height);
    const width = picture.width * scale;
    const height = picture.height * scale;

    return {
        width,
        height,
        offsetX: (pageSize.width - width) / 2,
        offsetY: (pageSize.height - height) / 2,
    };
}

function createPageContentStream(picture: Pick<PdfPicture, 'width' | 'height'>, pageSize: PdfPageSize): string {
    const placement = getPdfPicturePlacement(picture, pageSize);

    return (
        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`
    );
}

function getPageObjectNumber(pageIndex: number): number {
    return 3 + pageIndex * 3;
}

function appendPdfObject(
    chunks: Uint8Array[],
    objectOffsets: number[],
    objectNumber: number,
    objectBodyChunks: readonly Uint8Array[],
): void {
    objectOffsets.push(getByteLength(chunks));
    chunks.push(encodeAsciiText(`${objectNumber} 0 obj\n`), ...objectBodyChunks, encodeAsciiText('\nendobj\n'));
}

function appendPdfTextObject(
    chunks: Uint8Array[],
    objectOffsets: number[],
    objectNumber: number,
    objectBody: string,
): void {
    appendPdfObject(chunks, objectOffsets, objectNumber, [encodeAsciiText(objectBody)]);
}

/**
 * Write one or more JPEG pictures as a printable PDF document.
 *
 * Note: A page gets one picture of its own, which keeps a long document readable instead of shrinking it to fit one
 * page. The same primitive serves image-only analytics exports and text documents drawn on a canvas.
 */
export function createPicturePdf(
    pictures: readonly PdfPicture[],
    pageSize: PdfPageSize,
    title: string,
): Blob {
    if (pictures.length === 0) {
        throw new Error('PDF musí obsahovat alespoň jednu stránku');
    }

    const pageObjectNumbers = pictures.map((_, pageIndex) => getPageObjectNumber(pageIndex));
    const informationObjectNumber = getPageObjectNumber(pictures.length - 1) + 3;
    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
    const objectOffsets: number[] = [];

    appendPdfTextObject(chunks, objectOffsets, 1, '<< /Type /Catalog /Pages 2 0 R >>');
    appendPdfTextObject(
        chunks,
        objectOffsets,
        2,
        `<< /Type /Pages /Kids [${pageObjectNumbers.map((pageObjectNumber) => `${pageObjectNumber} 0 R`).join(' ')}] ` +
            `/Count ${pictures.length} >>`,
    );

    pictures.forEach((picture, pageIndex) => {
        const pageObjectNumber = getPageObjectNumber(pageIndex);
        const contentObjectNumber = pageObjectNumber + 1;
        const pictureObjectNumber = pageObjectNumber + 2;
        const contentStream = createPageContentStream(picture, pageSize);

        appendPdfTextObject(
            chunks,
            objectOffsets,
            pageObjectNumber,
            `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageSize.width} ${pageSize.height}] ` +
                `/Resources << /XObject << /Im0 ${pictureObjectNumber} 0 R >> >> /Contents ${contentObjectNumber} 0 R >>`,
        );
        appendPdfTextObject(
            chunks,
            objectOffsets,
            contentObjectNumber,
            `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
        );
        appendPdfObject(chunks, objectOffsets, pictureObjectNumber, [
            encodeAsciiText(
                `<< /Type /XObject /Subtype /Image /Width ${picture.width} /Height ${picture.height} ` +
                    `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${picture.bytes.length} >>\nstream\n`,
            ),
            picture.bytes,
            encodeAsciiText('\nendstream'),
        ]);
    });

    appendPdfTextObject(chunks, objectOffsets, informationObjectNumber, `<< /Title ${formatPdfTextString(title)} >>`);

    const crossReferenceOffset = getByteLength(chunks);
    chunks.push(
        encodeAsciiText(
            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
                objectOffsets.map(formatCrossReferenceEntry).join('') +
                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info ${informationObjectNumber} 0 R >>\n` +
                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
        ),
    );

    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
}

import { createPicturePdf } from '@/lib/exports/pdfPictures';
import { describe, expect, it } from 'vitest';

const JPEG_BYTES = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0xff, 0xd9]);
const PDF_PAGE_SIZE = { width: 595, height: 842, margin: 0 };

async function readPdfAsLatin1Text(pdf: Blob): Promise<string> {
    return Array.from(new Uint8Array(await pdf.arrayBuffer()))
        .map((byte) => String.fromCharCode(byte))
        .join('');
}

describe('picture PDF export', () => {
    it('keeps every supplied picture on its own PDF page', async () => {
        const pdfText = await readPdfAsLatin1Text(
            createPicturePdf(
                [
                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
                    { bytes: JPEG_BYTES, width: 1240, height: 1754 },
                ],
                PDF_PAGE_SIZE,
                'Shrnutí workshopu',
            ),
        );

        expect(pdfText).toContain('/Count 2');
        expect(pdfText).toContain('/Kids [3 0 R 6 0 R]');
        expect((pdfText.match(/\/Subtype \/Image/g) ?? [])).toHaveLength(2);
        expect(pdfText).toContain('/Title <FEFF005300680072006E0075007400ED00200077006F0072006B00730068006F00700075>');
    });

    it('rejects a document without a page instead of writing an invalid file', () => {
        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
    });
});

'use client';

import { WorkshopWrapUp } from '@/businesses/online-workshop/participant/WorkshopWrapUp';
import { WorkshopStageComment } from '@/businesses/online-workshop/participant/WorkshopStageComment';
import { WorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/WorkshopRepositoryCommitNotification';
import { useWorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/useWorkshopRepositoryCommitNotification';
import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import { useWorkshopReactionStream } from '@/components/workshops/useWorkshopReactionStream';
import { WorkshopReactionStream } from '@/components/workshops/WorkshopReactionStream';
import { trackGoogleAnalyticsEvent } from '@/lib/tracking/track-google-analytics-event';
import { createYoutubeEmbedUrl } from '@/lib/youtube/youtubeEmbed';
import { keepYoutubeVideoSubtitlesHidden, unmuteYoutubeVideo } from '@/lib/youtube/youtubePlayerCommands';
import { getWorkshopPhase } from '@/lib/workshops/workshopPhase';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type {
    WorkshopCommentReference,
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { motion, useReducedMotion } from 'framer-motion';
import { ArrowDownLeft, ArrowLeft, Maximize, Play, Radio, Volume2 } from 'lucide-react';
import { useEffect, useMemo, useRef, useState } from 'react';

const CLOCK_TICK_MILLISECONDS = 1000;

type WorkshopStageProps = {
    readonly workshop: WorkshopDetails;
    readonly serverTime: string;

    /**
     * Where the reactions of the room come from
     *
     * Note: The stage keeps the flying reactions itself, so a busy room re-renders nothing but the stage they fly over.
     */
    readonly subscribeToReactions: SubscribeToWorkshopReactions;

    /** The project connected to this workshop, if there is one */
    readonly repository?: WorkshopRepository | null;

    /** Offers the stage commits found by the repository monitor or its polling fallback */
    readonly subscribeToRepositoryCommits?: SubscribeToWorkshopRepositoryCommits;

    /**
     * All of these values are supplied by the participant room in production. Defaults keep this low-level stage usable
     * in isolation while it is still responsible for choosing the correct temporal phase.
     */
    readonly feedback?: WorkshopFeedback | null;
    readonly contentBlocks?: readonly WorkshopContentBlock[];
    readonly followUpContentBlock?: WorkshopContentBlock | null;
    readonly stageComment?: WorkshopCommentReference | null;

    /**
     * The recording which this member has not unlocked, or `null` while nothing of the video is withheld from them
     *
     * Note: The server decides this together with the video it hands over, so the stage plays whatever video it was
     *       given and offers the membership exactly when it was given none.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;
    readonly onSaveFeedback?: (values: WorkshopFeedbackValues) => Promise<boolean>;
};

function getRemainingSegments(remainingMilliseconds: number) {
    const remainingSeconds = Math.max(0, Math.floor(remainingMilliseconds / 1000));
    return [
        { label: 'dní', value: Math.floor(remainingSeconds / 86400) },
        { label: 'hodin', value: Math.floor((remainingSeconds % 86400) / 3600) },
        { label: 'minut', value: Math.floor((remainingSeconds % 3600) / 60) },
        { label: 'sekund', value: remainingSeconds % 60 },
    ];
}

function requestVideoFullscreen(videoFrame: HTMLIFrameElement | null): void {
    const requestFullscreen = videoFrame?.requestFullscreen;
    if (requestFullscreen === undefined || videoFrame === null) {
        return;
    }

    void requestFullscreen.call(videoFrame).catch(() => undefined);
}

const refuseStandaloneFeedbackSave = async (): Promise<boolean> => false;

export function WorkshopStage({
    workshop,
    serverTime,
    subscribeToReactions,
    repository = null,
    subscribeToRepositoryCommits,
    feedback = null,
    contentBlocks = [],
    followUpContentBlock = null,
    stageComment = null,
    paidMembersOnlyVideo = null,
    onSaveFeedback = refuseStandaloneFeedbackSave,
}: WorkshopStageProps) {
    const isReducedMotionPreferred = useReducedMotion() === true;
    const serverClockOffset = useMemo(() => Date.parse(serverTime) - Date.now(), [serverTime]);
    const [currentTime, setCurrentTime] = useState(() => Date.now() + serverClockOffset);
    const [isVideoUnmuted, setIsVideoUnmuted] = useState(false);
    const videoFrameReference = useRef<HTMLIFrameElement>(null);
    const { flyingReactions, launchReaction } = useWorkshopReactionStream();

    const phase = getWorkshopPhase(workshop, currentTime);
    const isWorkshopOngoing = phase === 'ongoing';
    const isWorkshopPast = phase === 'past';
    const remainingMilliseconds = Date.parse(workshop.startsAt) - currentTime;
    const newRepositoryCommit = useWorkshopRepositoryCommitNotification({
        workshopSlug: workshop.slug,
        isWorkshopOngoing,
        isEnabled: repository !== null,
        subscribeToRepositoryCommits,
    });

    // Note: Once the workshop is over, the room only holds its video for the members whose membership pays for it, and
    //       the server is what decides that. The wrap-up therefore keeps its feedback for everybody and gains either
    //       the button which plays the video again or the offer of the membership which unlocks it.
    const isVideoRewatchOffered = isWorkshopPast && workshop.youtubeVideoId !== null;
    const [isVideoRewatchShown, setIsVideoRewatchShown] = useState(false);
    useEffect(() => {
        if (!isVideoRewatchOffered) {
            setIsVideoRewatchShown(false);
        }
    }, [isVideoRewatchOffered]);

    useEffect(() => subscribeToReactions(launchReaction), [launchReaction, subscribeToReactions]);

    useEffect(() => {
        setCurrentTime(Date.now() + serverClockOffset);
        const intervalId = window.setInterval(
            () => setCurrentTime(Date.now() + serverClockOffset),
            CLOCK_TICK_MILLISECONDS,
        );
        return () => window.clearInterval(intervalId);
    }, [serverClockOffset]);

    useEffect(() => setIsVideoUnmuted(false), [workshop.youtubeVideoId]);

    useEffect(() => {
        if (workshop.youtubeVideoId === null || !isWorkshopOngoing) {
            return;
        }

        return keepYoutubeVideoSubtitlesHidden(videoFrameReference.current);
    }, [workshop.youtubeVideoId, isWorkshopOngoing]);

    const countdownSegments = getRemainingSegments(remainingMilliseconds);
    const handleVideoUnmute = () => {
        unmuteYoutubeVideo(videoFrameReference.current);
        setIsVideoUnmuted(true);
        trackGoogleAnalyticsEvent('workshop_video_unmuted', { workshop_slug: workshop.slug });
    };
    const handleVideoFullscreen = () => requestVideoFullscreen(videoFrameReference.current);

    return (
        <section className="relative overflow-hidden rounded-2xl border border-white/10 bg-[#081a24] shadow-2xl">
            {isWorkshopPast && isVideoRewatchShown && workshop.youtubeVideoId !== null ? (
                <div>
                    <div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 px-5 py-3">
                        <span className="inline-flex items-center gap-2 text-sm font-bold text-white">
                            <Play className="h-4 w-4 text-amber-300" aria-hidden="true" /> Video z workshopu
                        </span>
                        <button
                            type="button"
                            onClick={() => setIsVideoRewatchShown(false)}
                            className="inline-flex items-center gap-2 rounded-full border border-white/20 bg-slate-950/60 px-3 py-1.5 text-xs font-semibold text-slate-200 transition hover:border-cyan-200/70 hover:bg-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200"
                        >
                            <ArrowLeft className="h-3.5 w-3.5" aria-hidden="true" /> Zpět na závěrečné shrnutí
                        </button>
                    </div>
                    <div className="relative aspect-video">
                        <iframe
                            className="absolute inset-0 h-full w-full"
                            src={createYoutubeEmbedUrl(workshop.youtubeVideoId, {
                                isAutoplayed: true,
                                isInlinePlayback: true,
                                isRelatedVideoEnabled: false,
                                isControlsVisible: true,
                                isJavaScriptApiEnabled: false,
                                startAtSeconds: workshop.recordingStartOffsetSeconds,
                            })}
                            title={workshop.title}
                            allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                            referrerPolicy="strict-origin-when-cross-origin"
                            allowFullScreen
                        />
                    </div>
                </div>
            ) : isWorkshopPast ? (
                <WorkshopWrapUp
                    workshop={workshop}
                    contentBlocks={contentBlocks}
                    feedback={feedback}
                    followUpContentBlock={followUpContentBlock}
                    paidMembersOnlyVideo={paidMembersOnlyVideo}
                    onSaveFeedback={onSaveFeedback}
                    onRewatchVideo={isVideoRewatchOffered ? () => setIsVideoRewatchShown(true) : undefined}
                />
            ) : (
                <div
                    className={`relative min-w-0 w-full max-w-full aspect-video ${isWorkshopOngoing ? 'min-h-[220px] sm:min-h-[260px]' : 'min-h-[280px] sm:min-h-[260px]'}`}
                >
                    {isWorkshopOngoing && workshop.youtubeVideoId ? (
                        <iframe
                            ref={videoFrameReference}
                            className="absolute inset-0 h-full w-full"
                            src={createYoutubeEmbedUrl(workshop.youtubeVideoId, {
                                isAutoplayed: true,
                                isMuted: true,
                                isInlinePlayback: true,
                                isRelatedVideoEnabled: false,
                                isControlsVisible: false,
                                isCaptionsEnabled: false,
                                isJavaScriptApiEnabled: true,
                            })}
                            title={workshop.title}
                            allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                            referrerPolicy="strict-origin-when-cross-origin"
                            allowFullScreen
                        />
                    ) : isWorkshopOngoing ? (
                        <div className="absolute inset-0 flex flex-col items-center justify-center bg-[radial-gradient(circle_at_center,rgba(48,168,189,.24),transparent_52%)] px-8 text-center">
                            <Radio className="h-11 w-11 animate-pulse text-cyan-300" />
                            <h2 className="mt-5 text-2xl font-bold text-white">Stream právě připravujeme</h2>
                            <p className="mt-2 max-w-md text-sm text-slate-400">
                                Video se zde objeví automaticky, jakmile administrátor vloží YouTube stream.
                            </p>
                        </div>
                    ) : (
                        <div className="absolute inset-0 flex flex-col items-center justify-center bg-[radial-gradient(circle_at_50%_35%,rgba(122,235,255,.16),transparent_42%)] px-4 py-5 text-center sm:px-5">
                            <span className="inline-flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/10 px-3 py-1 text-[11px] font-semibold uppercase leading-5 tracking-[0.16em] text-cyan-200 sm:text-xs sm:tracking-[0.18em]">
                                <span className="h-2 w-2 animate-pulse rounded-full bg-cyan-300" /> Začínáme za
                            </span>
                            <div className="mt-5 grid w-full max-w-[19rem] grid-cols-2 gap-2 sm:mt-7 sm:w-auto sm:max-w-none sm:grid-cols-4 sm:gap-4">
                                {countdownSegments.map((segment) => (
                                    <div
                                        key={segment.label}
                                        className="min-w-0 rounded-xl border border-white/10 bg-white/5 px-2 py-2.5 text-center sm:min-w-[82px] sm:px-4 sm:py-4"
                                    >
                                        <div className="font-mono text-3xl font-bold tabular-nums text-white sm:text-4xl">
                                            {String(segment.value).padStart(2, '0')}
                                        </div>
                                        <div className="mt-1 text-[10px] uppercase tracking-wider text-slate-500 sm:text-xs">
                                            {segment.label}
                                        </div>
                                    </div>
                                ))}
                            </div>
                            <p className="mt-4 w-full max-w-[25rem] px-2 text-sm leading-6 text-slate-400 sm:mt-6">
                                Stránku nemusíte obnovovat. Stream se spustí automaticky.
                            </p>
                        </div>
                    )}
                </div>
            )}

            <WorkshopReactionStream reactions={flyingReactions} />
            {isWorkshopOngoing && newRepositoryCommit !== null && repository !== null && (
                <WorkshopRepositoryCommitNotification
                    key={newRepositoryCommit.sha}
                    repository={repository}
                    commit={newRepositoryCommit}
                />
            )}
            {isWorkshopOngoing && <WorkshopStageComment stageComment={stageComment} />}

            {isWorkshopOngoing && workshop.youtubeVideoId && (
                <button
                    type="button"
                    onClick={handleVideoFullscreen}
                    aria-label="Přehrát video na celé obrazovce"
                    className="absolute right-3 top-3 z-20 inline-flex items-center gap-2 rounded-full border border-white/20 bg-slate-950/90 px-3 py-2 text-xs font-semibold text-white shadow-lg transition hover:border-cyan-200/70 hover:bg-slate-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 sm:right-5 sm:top-5"
                >
                    <Maximize className="h-4 w-4" aria-hidden="true" />
                    <span className="hidden sm:inline">Celá obrazovka</span>
                </button>
            )}

            {isWorkshopOngoing && workshop.youtubeVideoId && !isVideoUnmuted && (
                <motion.div
                    initial={isReducedMotionPreferred ? false : { opacity: 0, y: 12 }}
                    animate={{ opacity: 1, y: 0 }}
                    className="absolute bottom-3 left-3 right-3 z-20 rounded-2xl border border-cyan-200/40 bg-slate-950/95 p-3 shadow-2xl backdrop-blur sm:bottom-5 sm:left-6 sm:right-auto sm:max-w-xs sm:p-4"
                >
                    <div className="flex items-start gap-3">
                        <ArrowDownLeft className="mt-1 h-8 w-8 shrink-0 animate-bounce text-cyan-300" aria-hidden="true" />
                        <div>
                            <p className="text-sm font-bold text-white">Zapněte si zvuk</p>
                            <p className="mt-1 text-xs leading-5 text-slate-300">
                                Klikněte na tlačítko níže – stream se kvůli automatickému spuštění otevírá ztlumený.
                            </p>
                            <button
                                type="button"
                                onClick={handleVideoUnmute}
                                className="mt-3 inline-flex items-center gap-2 rounded-full bg-cyan-300 px-4 py-2 text-sm font-bold text-slate-950 transition hover:bg-cyan-200"
                            >
                                <Volume2 className="h-4 w-4" /> Zapnout zvuk
                            </button>
                        </div>
                    </div>
                </motion.div>
            )}
        </section>
    );
}

'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly workshop: Pick<
        WorkshopDetails,
        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
    >;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    workshop,
    contentBlocks,
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}

import type { AdminContactJoin } from '@/lib/admin/adminContactJoin';
import type { CommunityMembershipStatus } from '@/lib/community-membership/communityMembershipTypes';
import type { EventDetails } from '@/lib/events/event';
import type { GithubCommit } from '@/lib/github/githubCommitFeed';
import type { WorkshopPanelKey } from '@/lib/workshops/workshopPanels';
import type { WorkshopRepository } from '@/lib/workshops/workshopRepository';

/**
 * A live room is normally one workshop occurrence. The community uses the same resilient room infrastructure, but
 * remains a single, separately administered room across all occurrences.
 */
export const WORKSHOP_KIND_VALUES = ['workshop', 'community', 'project'] as const;

export type WorkshopKind = (typeof WORKSHOP_KIND_VALUES)[number];

export function isWorkshopKind(value: string): value is WorkshopKind {
    return WORKSHOP_KIND_VALUES.includes(value as WorkshopKind);
}

/**
 * The lifecycle of something a participant submits for moderation. Chat messages and community projects deliberately
 * use these same states, so trust and moderator privileges never approve one kind of submission differently from the
 * other.
 */
export const WORKSHOP_SUBMISSION_STATUS_VALUES = ['pending', 'approved', 'rejected'] as const;

export type WorkshopSubmissionStatus = (typeof WORKSHOP_SUBMISSION_STATUS_VALUES)[number];

/**
 * A chat message keeps this alias because it is the public type used by the existing room API.
 */
export type WorkshopCommentStatus = WorkshopSubmissionStatus;
export type WorkshopCommentSort = 'recent' | 'upvotes';

export type WorkshopSummary = {
    readonly id: string;
    readonly kind: WorkshopKind;
    readonly slug: string;
    readonly title: string;

    /**
     * What this very term is about, in the words an administrator wrote about it
     *
     * Note: Terms of one event each have a subject of their own, so a list of them says what every single one is
     *       about rather than describing them all by the event they belong to. The room and the list read the very
     *       same words, so a term is never described in two different ways.
     */
    readonly description: string;
    readonly startsAt: string;
    readonly endsAt: string | null;
    readonly isPublished: boolean;

    /**
     * The event this room is a term of, or `null` for a permanent room which is no event at all
     *
     * Note: Every kind of event is administered in the very same place and stored in the very same table, so a page
     *       listing terms only ever asks which kind of event it wants to list.
     */
    readonly event: EventDetails | null;
};

/**
 * One occurrence as the administration lists it, together with the audience it gathered
 *
 * Note: The participant count deliberately stays out of `WorkshopSummary`, so no public list of terms exposes how
 *       many people registered for them.
 */
export type WorkshopAdminSummary = WorkshopSummary & {
    readonly participantCount: number;

    /**
     * How many people registered for this term on the landing page of its event, or `null` for a room which is no term
     * of an event and therefore has no registration form at all
     *
     * Note: This is a different audience from `participantCount`, which counts the people who really entered the room.
     *       Somebody can register and never come, and somebody can be handed a link into a room they never registered
     *       for, so neither number can be derived from the other.
     */
    readonly registeredParticipantCount: number | null;
};

export type WorkshopDetails = WorkshopSummary & {
    /** Number of artificial viewers added to the live watching badge by an administrator. */
    readonly artificialWatchingParticipantCount?: number;
    readonly youtubeVideoId: string | null;

    /**
     * Where paid members begin an ended workshop's recording, in seconds from the beginning of its stream.
     *
     * Note: This has no effect on the countdown or the live stream. It only removes the waiting-room portion from the
     *       replay which the membership unlocks after the workshop ends.
     */
    readonly recordingStartOffsetSeconds: number;

    /**
     * The teaser of that stream which stands in for it once the workshop is over, or `null` while none was published
     *
     * Note: The recording of an ended workshop is unlocked by the paid membership, and this is the snippet of it which
     *       an administrator lets everybody else watch instead. It is deliberately a second video rather than a part
     *       of the first one, so nothing of the recording itself has to reach a member who has not unlocked it.
     */
    readonly previewYoutubeVideoId: string | null;

    /**
     * A public PDF, presentation file, or Markdown page which belongs to this workshop, or `null` while none was
     * published. It is a workshop setting rather than an ordinary content block, so every participant can open it
     * without waiting for a scheduled unlock or buying a membership.
     */
    readonly presentationUrl: string | null;

    /**
     * The project this workshop is about, or `null` while no project is connected to it
     *
     * Note: This is the whole connection — the repository, its branch selection, and the address the project runs at
     *       — so the administration sets, changes, and unsets it as one thing. What has been committed in it is
     *       deliberately not part of it: that is read from GitHub, see `fetchWorkshopRepositoryProgress`.
     */
    readonly repository: WorkshopRepository | null;
    readonly allowedReactions: readonly string[];

    /**
     * The panels of the room an admin switched off for this workshop
     *
     * Note: Everything not listed here is offered, so a panel added later starts switched on for every workshop.
     */
    readonly disabledPanels: readonly WorkshopPanelKey[];
    readonly createdAt: string;
    readonly updatedAt: string;
};

export type WorkshopParticipant = {
    readonly id: string;
    readonly fullname: string;

    /**
     * Contact address this participant connected with
     *
     * Note: A room only ever describes the very participant reading it, so this address never reaches anybody else. It
     *       is what lets a room hand a verified identity on, for example into the link leading to another room.
     */
    readonly email: string;
    readonly connectedAt: string;
    readonly isInteractionBanned: boolean;

    /**
     * Whether the messages of this participant are approved as they are written
     *
     * Note: Trust stays invisible in the room. Nothing but the approval of their own messages tells a participant or
     *       anybody else that they were trusted.
     */
    readonly isTrusted: boolean;

    /**
     * Whether this participant moderates the room, which the room says with a badge
     */
    readonly isModerator: boolean;
};

export type WorkshopAdminParticipant = WorkshopParticipant &
    AdminContactJoin & {
    readonly lastSeenAt: string;
    readonly activeDurationSeconds: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly upvoteCount: number;

    /**
     * Present when the participant is listed inside the community administration. A community membership belongs to
     * their e-mail address rather than to this particular room session, so ordinary workshop participant data does
     * not fetch or expose it.
     */
    readonly communityMembershipStatus?: CommunityMembershipStatus;
};

/**
 * One server-paged slice of participants, keeping large workshops responsive in the administration.
 */
export type WorkshopAdminParticipantPage = {
    readonly participants: readonly WorkshopAdminParticipant[];
    readonly totalCount: number;
};

/**
 * One timestamped activity attributable to a participant.
 *
 * Note: These are assembled from the existing audited source records. They deliberately do not introduce a second
 * event store which could disagree with comments, reactions, or votes.
 */
export type WorkshopParticipantTimelineEvent =
    | {
          readonly kind: 'joined' | 'last-seen';
          readonly id: string;
          readonly occurredAt: string;
      }
    | {
          readonly kind: 'comment';
          readonly id: string;
          readonly occurredAt: string;
          readonly body: string;
          readonly status: WorkshopCommentStatus;
      }
    | {
          readonly kind: 'reaction';
          readonly id: string;
          readonly occurredAt: string;
          readonly emoji: string;
      }
    | {
          readonly kind: 'upvote';
          readonly id: string;
          readonly occurredAt: string;
          readonly commentId: string;
          readonly commentAuthorName: string | null;
          readonly commentBody: string | null;
      };

export type WorkshopAdminParticipantTimeline = {
    readonly participant: WorkshopAdminParticipant;
    readonly events: readonly WorkshopParticipantTimelineEvent[];
};

/**
 * Activity totals inside one compact bucket of the workshop-wide timeline.
 */
export type WorkshopAdminTimelinePoint = {
    readonly startsAt: string;

    /**
     * How many people had the room open during this bucket, which is the audience rather than an action
     *
     * Note: This is counted from the presence the room reports while it is open, so a workshop which was held before
     *       the room started reporting it has an audience of nobody however many people really watched it.
     */
    readonly watchingParticipantCount: number;

    /**
     * How many of the watching people were really at their computer during this bucket, and how many only had the room
     * open, which together are the whole audience of it
     *
     * Note: How that is told apart is decided by `workshopAttendance`, and a bucket which was measured before the
     *       attendance of a room was measured at all is passive throughout.
     */
    readonly activelyWatchingParticipantCount: number;
    readonly passivelyWatchingParticipantCount: number;

    /**
     * How many people registered into the room during this bucket
     */
    readonly participantCount: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly upvoteCount: number;
    readonly linkClickCount: number;

    /**
     * How many times each reaction was sent during this bucket, so a graph can draw one of them alone
     */
    readonly reactionCountsByEmoji: Readonly<Record<string, number>>;
};

/**
 * One message with the moment it was written, which is everything a metric counting words in the chat needs
 *
 * Note: The administration counts the matches of a regular expression in the browser, so that an expression which is
 *       still being typed answers immediately and never reaches the database.
 */
export type WorkshopAdminCommentSample = {
    readonly occurredAt: string;
    readonly body: string;
};

export type WorkshopAdminAnalytics = {
    readonly timelineStartsAt: string;
    readonly timelineEndsAt: string;
    readonly bucketDurationSeconds: number;
    readonly timeline: readonly WorkshopAdminTimelinePoint[];
    readonly reactionCounts: readonly WorkshopReactionCount[];
    readonly commentSamples: readonly WorkshopAdminCommentSample[];

    /**
     * Whether every message of the room could be sampled, or the oldest ones had to be left out of a very busy room
     */
    readonly isCommentSampleComplete: boolean;
};

export type WorkshopContentBlock = {
    readonly id: string;
    readonly title: string;
    readonly bodyMarkdown: string;
    readonly unlockAt: string;
    readonly sortOrder: number;
    readonly isPublished: boolean;

    /**
     * The one ordinary material selected to lead the post-workshop follow-up.
     */
    readonly isFollowUp: boolean;

    /**
     * Whether this material is reserved for the members who pay for the community membership
     *
     * Note: A room which offers the membership keeps it out of the state of everybody else and only says that it is
     *       there, so the material itself never reaches a member who has not unlocked it.
     */
    readonly isPaidMembersOnly: boolean;
    readonly createdAt: string;
    readonly updatedAt: string;
    readonly linkClickCount: number;
};

/**
 * As much of a withheld material as the member who may not read it is told
 *
 * Note: Nothing but the title of such a material ever leaves the server, so the room names what is waiting behind the
 *       membership without handing over any of it.
 */
export type WorkshopContentPreview = {
    readonly id: string;
    readonly title: string;
};

/**
 * As much of a withheld recording as the member who may not play it is given
 *
 * Note: The recording itself never leaves the server for such a member. Only the teaser an administrator published for
 *       it does, which is what the room shows on the closing stage instead, and which is `null` while none was
 *       published at all.
 */
export type WorkshopPaidMembersVideo = {
    readonly previewYoutubeVideoId: string | null;
};

/**
 * A participant's progressively saved reflection after one workshop.
 *
 * The score creates the record; every written answer is optional and can then arrive independently, so somebody who
 * only answers the first question is still represented faithfully.
 */
export type WorkshopFeedback = {
    readonly rating: number;
    readonly whatWasGood: string | null;
    readonly whatWasBad: string | null;
    readonly note: string | null;
    readonly createdAt: string;
    readonly updatedAt: string;
};

/**
 * The admin-only form of feedback, attributable to its participant and joinable to the private contact projection.
 */
export type WorkshopAdminFeedback = WorkshopFeedback &
    AdminContactJoin & {
        readonly id: string;
        readonly participantId: string;
        readonly fullname: string;
        readonly email: string;
    };

/**
 * Who wrote a message, as far as somebody moderating the room may know them
 *
 * Note: Only a moderator receives this, so an ordinary participant never learns which invisible moderation state the
 *       author of a message carries, nor the identity behind their name.
 */
export type WorkshopCommentAuthor = {
    readonly participantId: string;
    readonly isTrusted: boolean;
    readonly isInteractionBanned: boolean;
    readonly isModerator: boolean;
};

export type WorkshopComment = {
    readonly id: string;
    readonly authorName: string;
    readonly body: string;
    readonly status: WorkshopCommentStatus;
    readonly upvoteCount: number;
    readonly isUpvotedByParticipant: boolean;
    readonly createdAt: string;

    /**
     * Whether a moderator of the room wrote this message, which the whole room sees on it
     */
    readonly isAuthorModerator: boolean;

    /**
     * Whether the administration created this message without a participant
     */
    readonly isArtificial: boolean;

    /**
     * The author as a moderator of the room may act on them, or `null` for everybody else
     */
    readonly moderatedAuthor: WorkshopCommentAuthor | null;

    /**
     * The comment this one answers, or `null` when it opens its own thread
     */
    readonly parentCommentId: string | null;

    /**
     * Whether an admin pinned this message to the top of the chat
     *
     * Note: A room has at most one pinned message, because the pin is remembered by the workshop itself.
     */
    readonly isPinned: boolean;
};

/**
 * A comment together with the answers it received
 *
 * Note: The chat is exactly one level deep, so a reply never carries replies of its own.
 */
export type WorkshopCommentThread = {
    readonly comment: WorkshopComment;
    readonly replies: readonly WorkshopComment[];
};

export type WorkshopReaction = {
    readonly id: string;
    readonly emoji: string;
    readonly createdAt: string;
};

/**
 * The number of times one exact reaction has been sent in a workshop
 *
 * Note: This counts reaction actions, rather than distinct people. A participant who reacts twice therefore adds two
 *       to the total, exactly as the room celebrated two reactions.
 */
export type WorkshopReactionCount = {
    readonly emoji: string;
    readonly count: number;
};

/**
 * One answer a member can choose in a poll. The room only receives aggregate counts and whether its own participant
 * chose this option, never the identities behind any other vote.
 */
export type WorkshopPollOption = {
    readonly id: string;
    readonly label: string;
    readonly sortOrder: number;
    readonly voteCount: number;
    readonly isVotedByParticipant: boolean;
};

/**
 * The administrative view additionally separates the member votes from the explicitly seeded aggregate. The member
 * room deliberately keeps receiving only `WorkshopPollOption`, so an artificial starting count does not expose its
 * origin to the people taking part in the poll.
 */
export type WorkshopAdminPollOption = WorkshopPollOption & {
    readonly realVoteCount: number;
    readonly artificialVoteCount: number;
};

/**
 * A community question prepared by an administrator. Poll infrastructure is shared with the room model, while the
 * room-kind capability decides which kinds offer it.
 */
export type WorkshopPoll = {
    readonly id: string;
    readonly question: string;
    readonly isClosed: boolean;
    readonly isVisible: boolean;
    readonly createdAt: string;
    readonly updatedAt: string;
    readonly options: readonly WorkshopPollOption[];

    /**
     * The workshop occurrences this poll is about
     *
     * Note: A poll keeps belonging to the community which administers it. An attached occurrence is the subject of the
     *       question, which is what lets its room receive the poll and what lets the administration of that occurrence
     *       see the question asked about it. Members are only ever told about published occurrences.
     */
    readonly attachedWorkshops: readonly WorkshopSummary[];
};

export type WorkshopAdminPoll = Omit<WorkshopPoll, 'options'> & {
    readonly options: readonly WorkshopAdminPollOption[];
};

export type WorkshopPublicState = {
    readonly serverTime: string;
    readonly workshop: WorkshopDetails;
    readonly participant: WorkshopParticipant;

    /**
     * How many participants had the room open recently, including the one this state was loaded for
     */
    readonly watchingParticipantCount: number;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly nextContentUnlockAt: string | null;

    /**
     * The materials which only paid members may see, named for a member who is not one of them
     *
     * Note: The materials themselves stay out of `contentBlocks`, so this list is what lets the room say where the
     *       paid materials are, what they are called and how to unlock them. It is empty for a member who already paid
     *       and in a room which offers no membership at all.
     */
    readonly paidMembersOnlyContentPreviews: readonly WorkshopContentPreview[];

    /**
     * The recording which the paid membership would unlock for this member, or `null` while nothing of the video is
     * being withheld from them
     *
     * Note: The stream of the occurrence stays out of `workshop` while this is set, so the room cannot play a recording
     *       it also offers to sell. It is `null` for everybody who may watch it — during the workshop itself, for a
     *       member who paid, and in a room which offers no membership — and for an occurrence which carries no video
     *       at all, because there would be nothing for a purchase to unlock.
     */
    readonly paidMembersOnlyVideo: WorkshopPaidMembersVideo | null;
    readonly feedback: WorkshopFeedback | null;
    readonly comments: readonly WorkshopComment[];

    /**
     * The one question the host selected for the shared stage, or `null` while no question is being answered
     *
     * Note: This is a reference to the ordinary comment which supplied the question. Keeping that comment as the
     *       source of truth lets an attendee and the host read the same words without copying a second message body
     *       into a stage-only record.
     */
    readonly stageComment: WorkshopCommentReference | null;
    readonly recentReactions: readonly WorkshopReaction[];
    readonly reactionCounts: readonly WorkshopReactionCount[];
    readonly polls: readonly WorkshopPoll[];
};

/**
 * As much of an answered comment as the moderation of a reply needs to judge it
 */
export type WorkshopCommentReference = {
    readonly id: string;
    readonly authorName: string;
    readonly body: string;
};

export type WorkshopAdminComment = Omit<WorkshopComment, 'isUpvotedByParticipant'> & {
    readonly participantId: string | null;
    readonly realUpvoteCount: number;
    readonly artificialUpvoteCount: number;
    readonly parentComment: WorkshopCommentReference | null;
};

export type WorkshopAdminSnapshot = {
    readonly workshop: WorkshopDetails;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly polls: readonly WorkshopAdminPoll[];

    /**
     * The community polls asked about this room, which a room administering polls of its own never has
     *
     * Note: These are read-only here. They are administered where they belong, in the administration of the community
     *       which owns them, rather than being editable from two places at once.
     */
    readonly attachedPolls: readonly WorkshopAdminPoll[];
    readonly comments: readonly WorkshopAdminComment[];

    /**
     * The message pinned on top of the chat, whatever moderation state the administration is listing
     */
    readonly pinnedComment: WorkshopCommentReference | null;

    /**
     * The question currently shown over the shared stage, if the room has one
     */
    readonly stageComment: WorkshopCommentReference | null;
    readonly participants: readonly WorkshopAdminParticipant[];
    readonly participantCount: number;
    readonly commentCount: number;
    readonly reactionCount: number;
    readonly artificialReactionCount: number;
};

export type WorkshopRealtimeEvent =
    | { readonly kind: 'state-changed' }
    | { readonly kind: 'reaction'; readonly reaction: WorkshopReaction; readonly reactionCount: number }
    | { readonly kind: 'upvote'; readonly commentId: string; readonly upvoteCount: number }
    | { readonly kind: 'stage-comment'; readonly stageComment: WorkshopCommentReference | null }
    | { readonly kind: 'repository-commit'; readonly commit: GithubCommit };


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n -C 4 "WorkshopWrapUp|isWorkshopPast|contentBlocks|endsAt" businesses/online-workshop/participant/WorkshopStage.tsx businesses/online-workshop/participant/WorkshopStage.test.tsx businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx lib/workshops/workshopPhase.ts lib/workshops/workshopDate.ts lib/workshops/workshopMaterialLinks.ts lib/workshops/workshopMaterialLinkSyntax.ts; Get-Content -Raw lib/workshops/workshopPhase.ts; Get-Content -Raw lib/workshops/workshopDate.ts; Get-Content -Raw lib/workshops/workshopMaterialLinks.ts; Get-Content -Raw lib/workshops/workshopMaterialLinkSyntax.ts; Get-Content -Raw lib/downloadBlobFile.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 301ms:
businesses/online-workshop/participant/WorkshopStage.tsx-1-'use client';
businesses/online-workshop/participant/WorkshopStage.tsx-2-
businesses/online-workshop/participant/WorkshopStage.tsx:3:import { WorkshopWrapUp } from '@/businesses/online-workshop/participant/WorkshopWrapUp';
businesses/online-workshop/participant/WorkshopStage.tsx-4-import { WorkshopStageComment } from '@/businesses/online-workshop/participant/WorkshopStageComment';
businesses/online-workshop/participant/WorkshopStage.tsx-5-import { WorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/WorkshopRepositoryCommitNotification';
businesses/online-workshop/participant/WorkshopStage.tsx-6-import { useWorkshopRepositoryCommitNotification } from '@/businesses/online-workshop/participant/useWorkshopRepositoryCommitNotification';
businesses/online-workshop/participant/WorkshopStage.tsx-7-import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
--
businesses/online-workshop/participant/WorkshopStage.tsx-48-     * All of these values are supplied by the participant room in production. Defaults keep this low-level stage usable
businesses/online-workshop/participant/WorkshopStage.tsx-49-     * in isolation while it is still responsible for choosing the correct temporal phase.
businesses/online-workshop/participant/WorkshopStage.tsx-50-     */
businesses/online-workshop/participant/WorkshopStage.tsx-51-    readonly feedback?: WorkshopFeedback | null;
businesses/online-workshop/participant/WorkshopStage.tsx:52:    readonly contentBlocks?: readonly WorkshopContentBlock[];
businesses/online-workshop/participant/WorkshopStage.tsx-53-    readonly followUpContentBlock?: WorkshopContentBlock | null;
businesses/online-workshop/participant/WorkshopStage.tsx-54-    readonly stageComment?: WorkshopCommentReference | null;
businesses/online-workshop/participant/WorkshopStage.tsx-55-
businesses/online-workshop/participant/WorkshopStage.tsx-56-    /**
--
businesses/online-workshop/participant/WorkshopStage.tsx-90-    subscribeToReactions,
businesses/online-workshop/participant/WorkshopStage.tsx-91-    repository = null,
businesses/online-workshop/participant/WorkshopStage.tsx-92-    subscribeToRepositoryCommits,
businesses/online-workshop/participant/WorkshopStage.tsx-93-    feedback = null,
businesses/online-workshop/participant/WorkshopStage.tsx:94:    contentBlocks = [],
businesses/online-workshop/participant/WorkshopStage.tsx-95-    followUpContentBlock = null,
businesses/online-workshop/participant/WorkshopStage.tsx-96-    stageComment = null,
businesses/online-workshop/participant/WorkshopStage.tsx-97-    paidMembersOnlyVideo = null,
businesses/online-workshop/participant/WorkshopStage.tsx-98-    onSaveFeedback = refuseStandaloneFeedbackSave,
--
businesses/online-workshop/participant/WorkshopStage.tsx-105-    const { flyingReactions, launchReaction } = useWorkshopReactionStream();
businesses/online-workshop/participant/WorkshopStage.tsx-106-
businesses/online-workshop/participant/WorkshopStage.tsx-107-    const phase = getWorkshopPhase(workshop, currentTime);
businesses/online-workshop/participant/WorkshopStage.tsx-108-    const isWorkshopOngoing = phase === 'ongoing';
businesses/online-workshop/participant/WorkshopStage.tsx:109:    const isWorkshopPast = phase === 'past';
businesses/online-workshop/participant/WorkshopStage.tsx-110-    const remainingMilliseconds = Date.parse(workshop.startsAt) - currentTime;
businesses/online-workshop/participant/WorkshopStage.tsx-111-    const newRepositoryCommit = useWorkshopRepositoryCommitNotification({
businesses/online-workshop/participant/WorkshopStage.tsx-112-        workshopSlug: workshop.slug,
businesses/online-workshop/participant/WorkshopStage.tsx-113-        isWorkshopOngoing,
--
businesses/online-workshop/participant/WorkshopStage.tsx-117-
businesses/online-workshop/participant/WorkshopStage.tsx-118-    // Note: Once the workshop is over, the room only holds its video for the members whose membership pays for it, and
businesses/online-workshop/participant/WorkshopStage.tsx-119-    //       the server is what decides that. The wrap-up therefore keeps its feedback for everybody and gains either
businesses/online-workshop/participant/WorkshopStage.tsx-120-    //       the button which plays the video again or the offer of the membership which unlocks it.
businesses/online-workshop/participant/WorkshopStage.tsx:121:    const isVideoRewatchOffered = isWorkshopPast && workshop.youtubeVideoId !== null;
businesses/online-workshop/participant/WorkshopStage.tsx-122-    const [isVideoRewatchShown, setIsVideoRewatchShown] = useState(false);
businesses/online-workshop/participant/WorkshopStage.tsx-123-    useEffect(() => {
businesses/online-workshop/participant/WorkshopStage.tsx-124-        if (!isVideoRewatchOffered) {
businesses/online-workshop/participant/WorkshopStage.tsx-125-            setIsVideoRewatchShown(false);
--
businesses/online-workshop/participant/WorkshopStage.tsx-156-    const handleVideoFullscreen = () => requestVideoFullscreen(videoFrameReference.current);
businesses/online-workshop/participant/WorkshopStage.tsx-157-
businesses/online-workshop/participant/WorkshopStage.tsx-158-    return (
businesses/online-workshop/participant/WorkshopStage.tsx-159-        <section className="relative overflow-hidden rounded-2xl border border-white/10 bg-[#081a24] shadow-2xl">
businesses/online-workshop/participant/WorkshopStage.tsx:160:            {isWorkshopPast && isVideoRewatchShown && workshop.youtubeVideoId !== null ? (
businesses/online-workshop/participant/WorkshopStage.tsx-161-                <div>
businesses/online-workshop/participant/WorkshopStage.tsx-162-                    <div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/10 px-5 py-3">
businesses/online-workshop/participant/WorkshopStage.tsx-163-                        <span className="inline-flex items-center gap-2 text-sm font-bold text-white">
businesses/online-workshop/participant/WorkshopStage.tsx-164-                            <Play className="h-4 w-4 text-amber-300" aria-hidden="true" /> Video z workshopu
--
businesses/online-workshop/participant/WorkshopStage.tsx-188-                            allowFullScreen
businesses/online-workshop/participant/WorkshopStage.tsx-189-                        />
businesses/online-workshop/participant/WorkshopStage.tsx-190-                    </div>
businesses/online-workshop/participant/WorkshopStage.tsx-191-                </div>
businesses/online-workshop/participant/WorkshopStage.tsx:192:            ) : isWorkshopPast ? (
businesses/online-workshop/participant/WorkshopStage.tsx:193:                <WorkshopWrapUp
businesses/online-workshop/participant/WorkshopStage.tsx-194-                    workshop={workshop}
businesses/online-workshop/participant/WorkshopStage.tsx:195:                    contentBlocks={contentBlocks}
businesses/online-workshop/participant/WorkshopStage.tsx-196-                    feedback={feedback}
businesses/online-workshop/participant/WorkshopStage.tsx-197-                    followUpContentBlock={followUpContentBlock}
businesses/online-workshop/participant/WorkshopStage.tsx-198-                    paidMembersOnlyVideo={paidMembersOnlyVideo}
businesses/online-workshop/participant/WorkshopStage.tsx-199-                    onSaveFeedback={onSaveFeedback}
--
lib/workshops/workshopPhase.ts-18- *       recorded, which is what the administration does when the workshop is really finished.
lib/workshops/workshopPhase.ts-19- */
lib/workshops/workshopPhase.ts-20-export type WorkshopOccurrenceTiming = {
lib/workshops/workshopPhase.ts-21-    readonly startsAt: string;
lib/workshops/workshopPhase.ts:22:    readonly endsAt: string | null;
lib/workshops/workshopPhase.ts-23-};
lib/workshops/workshopPhase.ts-24-
lib/workshops/workshopPhase.ts-25-/**
lib/workshops/workshopPhase.ts-26- * The order in which an administrator needs the phases: what runs right now, then what is being prepared, and
--
lib/workshops/workshopPhase.ts-38- * Note: This is the single rule for whether an occurrence has an end at all, so nothing decides on its own that an
lib/workshops/workshopPhase.ts-39- *       unwritten or nonsensical end means the workshop is over.
lib/workshops/workshopPhase.ts-40- */
lib/workshops/workshopPhase.ts-41-export function getWorkshopRecordedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number | null {
lib/workshops/workshopPhase.ts:42:    const endsAtMilliseconds = occurrence.endsAt === null ? Number.NaN : Date.parse(occurrence.endsAt);
lib/workshops/workshopPhase.ts-43-
lib/workshops/workshopPhase.ts:44:    return endsAtMilliseconds > Date.parse(occurrence.startsAt) ? endsAtMilliseconds : null;
lib/workshops/workshopPhase.ts-45-}
lib/workshops/workshopPhase.ts-46-
lib/workshops/workshopPhase.ts-47-/**
lib/workshops/workshopPhase.ts-48- * Whether an occurrence still has no end, so it runs until the administration ends it
--
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-230-    const { state } = controller;
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-231-    const roomCapabilities = getWorkshopKindCapabilities(state.workshop.kind);
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-232-    const isWorkshopPollVisible = isWorkshopPollVisibleInRoom(state.workshop.kind);
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-233-    const isModerating = isWorkshopParticipantModerating(state.participant);
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx:234:    const followUpContentBlock = state.contentBlocks.find((contentBlock) => contentBlock.isFollowUp) ?? null;
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-235-    const connectedRepository = roomCapabilities.isRepositoryOffered ? state.workshop.repository : null;
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-236-    const presentationUrl = roomCapabilities.isPresentationOffered ? state.workshop.presentationUrl : null;
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-237-    const specialMaterials: readonly WorkshopSpecialMaterial[] = [
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-238-        ...(presentationUrl === null
--
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-349-                            subscribeToReactions={controller.subscribeToReactions}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-350-                            repository={state.workshop.repository}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-351-                            subscribeToRepositoryCommits={subscribeToRepositoryCommits}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-352-                            feedback={state.feedback}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx:353:                            contentBlocks={state.contentBlocks}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-354-                            followUpContentBlock={followUpContentBlock}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-355-                            stageComment={state.stageComment}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-356-                            paidMembersOnlyVideo={state.paidMembersOnlyVideo}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-357-                            onSaveFeedback={controller.saveFeedback}
--
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-413-
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-414-                {isMaterialsShown && (
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-415-                    <div className="min-w-0 lg:col-start-1 lg:row-start-2">
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-416-                        <WorkshopContent
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx:417:                            contentBlocks={state.contentBlocks}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-418-                            nextContentUnlockAt={state.nextContentUnlockAt}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-419-                            newlyUnlockedContentBlockIds={controller.newlyUnlockedContentBlockIds}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-420-                            paidMembersOnlyContentPreviews={state.paidMembersOnlyContentPreviews}
businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx-421-                            specialMaterials={specialMaterials}
--
lib/workshops/workshopDate.ts-101-
lib/workshops/workshopDate.ts-102-/**
lib/workshops/workshopDate.ts-103- * The time span one occurrence runs for in Prague time, for example `10:00–16:00`
lib/workshops/workshopDate.ts-104- */
lib/workshops/workshopDate.ts:105:export function formatCzechWorkshopTimeRange(startsAt: string, endsAt: string | null): string {
lib/workshops/workshopDate.ts-106-    const startTime = formatCzechWorkshopTime(startsAt);
lib/workshops/workshopDate.ts:107:    return endsAt === null ? startTime : `${startTime}–${formatCzechWorkshopTime(endsAt)}`;
lib/workshops/workshopDate.ts-108-}
lib/workshops/workshopDate.ts-109-
lib/workshops/workshopDate.ts-110-/**
lib/workshops/workshopDate.ts-111- * Formats the start time of a Czech workshop in Prague time.
--
lib/workshops/workshopDate.ts-118- * Describes how long a workshop is expected to take. A workshop whose end is still left open is announced with the
lib/workshops/workshopDate.ts-119- * same usual length as its calendar invitation, because a visitor is told how long to set aside before anybody can
lib/workshops/workshopDate.ts-120- * know when the workshop will really be ended.
lib/workshops/workshopDate.ts-121- */
lib/workshops/workshopDate.ts:122:export function formatCzechWorkshopDuration(startsAt: string, endsAt: string | null): string {
lib/workshops/workshopDate.ts-123-    const startsAtMilliseconds = Date.parse(startsAt);
lib/workshops/workshopDate.ts-124-    const durationMinutes = Number.isFinite(startsAtMilliseconds)
lib/workshops/workshopDate.ts-125-        ? Math.round(
lib/workshops/workshopDate.ts:126:              (getWorkshopExpectedEndsAtMilliseconds({ startsAt, endsAt }) - startsAtMilliseconds) /
lib/workshops/workshopDate.ts-127-                  MILLISECONDS_PER_MINUTE,
lib/workshops/workshopDate.ts-128-          )
lib/workshops/workshopDate.ts-129-        : DEFAULT_WORKSHOP_DURATION_MINUTES;
lib/workshops/workshopDate.ts-130-
--
businesses/online-workshop/participant/WorkshopStage.test.tsx-56-    slug: 'online-workshop-2026-08-20',
businesses/online-workshop/participant/WorkshopStage.test.tsx-57-    title: 'Produkční kód s AI agenty',
businesses/online-workshop/participant/WorkshopStage.test.tsx-58-    description: 'Online workshop s Pavolem Hejným a Jiřím Jahnem.',
businesses/online-workshop/participant/WorkshopStage.test.tsx-59-    startsAt: '2026-08-20T19:00:00+02:00',
businesses/online-workshop/participant/WorkshopStage.test.tsx:60:    endsAt: '2026-08-20T20:30:00+02:00',
businesses/online-workshop/participant/WorkshopStage.test.tsx-61-    youtubeVideoId: null,
businesses/online-workshop/participant/WorkshopStage.test.tsx-62-    recordingStartOffsetSeconds: 0,
businesses/online-workshop/participant/WorkshopStage.test.tsx-63-    previewYoutubeVideoId: null,
businesses/online-workshop/participant/WorkshopStage.test.tsx-64-    presentationUrl: null,
--
businesses/online-workshop/participant/WorkshopStage.test.tsx-77-};
businesses/online-workshop/participant/WorkshopStage.test.tsx-78-
businesses/online-workshop/participant/WorkshopStage.test.tsx-79-const OPEN_ENDED_WORKSHOP_WITH_VIDEO: WorkshopDetails = {
businesses/online-workshop/participant/WorkshopStage.test.tsx-80-    ...WORKSHOP_WITH_VIDEO,
businesses/online-workshop/participant/WorkshopStage.test.tsx:81:    endsAt: null,
businesses/online-workshop/participant/WorkshopStage.test.tsx-82-};
businesses/online-workshop/participant/WorkshopStage.test.tsx-83-
businesses/online-workshop/participant/WorkshopStage.test.tsx-84-/**
businesses/online-workshop/participant/WorkshopStage.test.tsx-85- * The teaser an administrator published for the recording of a workshop
--
businesses/online-workshop/participant/WorkshopStage.test.tsx-340-    it('wraps a workshop without an end up as soon as the administration records its end', () => {
businesses/online-workshop/participant/WorkshopStage.test.tsx-341-        const reactionSource = createReactionSource();
businesses/online-workshop/participant/WorkshopStage.test.tsx-342-        const { container } = render(
businesses/online-workshop/participant/WorkshopStage.test.tsx-343-            <WorkshopStage
businesses/online-workshop/participant/WorkshopStage.test.tsx:344:                workshop={{ ...OPEN_ENDED_WORKSHOP_WITH_VIDEO, endsAt: '2026-08-20T21:12:00+02:00' }}
businesses/online-workshop/participant/WorkshopStage.test.tsx-345-                serverTime="2026-08-20T21:13:00+02:00"
businesses/online-workshop/participant/WorkshopStage.test.tsx-346-                subscribeToReactions={reactionSource.subscribeToReactions}
businesses/online-workshop/participant/WorkshopStage.test.tsx-347-                followUpContentBlock={FOLLOW_UP_CONTENT}
businesses/online-workshop/participant/WorkshopStage.test.tsx-348-                onSaveFeedback={async () => true}
import { DEFAULT_WORKSHOP_DURATION_MINUTES } from '@/lib/workshops/workshopConstants';

const MILLISECONDS_PER_MINUTE = 60 * 1000;

/**
 * Where one occurrence currently stands in time
 */
export const WORKSHOP_PHASE_VALUES = ['ongoing', 'upcoming', 'past'] as const;

export type WorkshopPhase = (typeof WORKSHOP_PHASE_VALUES)[number];

/**
 * As much of a workshop as it takes to decide when it happens
 *
 * Note: Both a summary and the full details of a workshop satisfy this shape, so nothing has to be loaded just to
 *       place an occurrence in time.
 * Note: An occurrence without an end has not ended. It runs for as long as it takes and is only over once its end is
 *       recorded, which is what the administration does when the workshop is really finished.
 */
export type WorkshopOccurrenceTiming = {
    readonly startsAt: string;
    readonly endsAt: string | null;
};

/**
 * The order in which an administrator needs the phases: what runs right now, then what is being prepared, and
 * finally the history.
 */
const WORKSHOP_PHASE_ORDER: Readonly<Record<WorkshopPhase, number>> = {
    ongoing: 0,
    upcoming: 1,
    past: 2,
};

/**
 * Moment an occurrence was really given as its end, or `null` while that end is left open
 *
 * Note: This is the single rule for whether an occurrence has an end at all, so nothing decides on its own that an
 *       unwritten or nonsensical end means the workshop is over.
 */
export function getWorkshopRecordedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number | null {
    const endsAtMilliseconds = occurrence.endsAt === null ? Number.NaN : Date.parse(occurrence.endsAt);

    return endsAtMilliseconds > Date.parse(occurrence.startsAt) ? endsAtMilliseconds : null;
}

/**
 * Whether an occurrence still has no end, so it runs until the administration ends it
 */
export function isWorkshopEndOpen(occurrence: WorkshopOccurrenceTiming): boolean {
    return getWorkshopRecordedEndsAtMilliseconds(occurrence) === null;
}

/**
 * Moment an occurrence is expected to end, taken from the usual length of a workshop while its end is left open
 *
 * Note: A calendar invitation and a duration label have to name a length before an occurrence has one, which is what
 *       this expectation is for. It deliberately never decides whether an occurrence is over — only a recorded end
 *       does that.
 */
export function getWorkshopExpectedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number {
    return (
        getWorkshopRecordedEndsAtMilliseconds(occurrence) ??
        Date.parse(occurrence.startsAt) + DEFAULT_WORKSHOP_DURATION_MINUTES * MILLISECONDS_PER_MINUTE
    );
}

/**
 * Decides whether an occurrence is still ahead, running right now, or already over
 *
 * Note: An occurrence whose end is open never becomes past by itself. It keeps running — and keeps its stage on —
 *       until an administrator records the end of it.
 *
 * @param currentTimeMilliseconds moment to compare against, so a list places every occurrence against the same instant
 */
export function getWorkshopPhase(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds = Date.now(),
): WorkshopPhase {
    if (Date.parse(occurrence.startsAt) > currentTimeMilliseconds) {
        return 'upcoming';
    }

    const recordedEndsAtMilliseconds = getWorkshopRecordedEndsAtMilliseconds(occurrence);
    return recordedEndsAtMilliseconds === null || recordedEndsAtMilliseconds > currentTimeMilliseconds
        ? 'ongoing'
        : 'past';
}

/**
 * The one phase which speaks for a group of occurrences, which is the most pressing among them
 *
 * Note: A day of a calendar carries whatever terms fall on it, and it can only be coloured by one of them. It is
 *       coloured by the very same ranking which puts a running term on top of a list, so a day where something is
 *       happening right now never looks like history.
 */
export function getMostProminentWorkshopPhase(phases: readonly WorkshopPhase[]): WorkshopPhase {
    return phases.reduce(
        (mostProminentPhase, phase) =>
            WORKSHOP_PHASE_ORDER[phase] < WORKSHOP_PHASE_ORDER[mostProminentPhase] ? phase : mostProminentPhase,
        'past',
    );
}

/**
 * The two keys which order one occurrence among the others
 *
 * Note: The history is ranked by its negated date, so the term which ended last leads it while the future is led by
 *       the term which starts next. The most relevant occurrence of every group therefore stays on top of it.
 */
function getWorkshopPhaseOrder(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds: number,
): { readonly phaseRank: number; readonly dateRank: number } {
    const phase = getWorkshopPhase(occurrence, currentTimeMilliseconds);
    const startsAtMilliseconds = Date.parse(occurrence.startsAt);
    const dateRank = Number.isFinite(startsAtMilliseconds) ? startsAtMilliseconds : 0;

    return { phaseRank: WORKSHOP_PHASE_ORDER[phase], dateRank: phase === 'past' ? -dateRank : dateRank };
}

/**
 * Orders occurrences by their phase first and by their date within every phase
 *
 * @param currentTimeMilliseconds moment which decides the phases, so one list is never sorted against a moving instant
 */
export function sortWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): readonly TWorkshop[] {
    return workshops
        .map((workshop) => ({ workshop, order: getWorkshopPhaseOrder(workshop, currentTimeMilliseconds) }))
        .sort(
            (firstEntry, secondEntry) =>
                firstEntry.order.phaseRank - secondEntry.order.phaseRank ||
                firstEntry.order.dateRank - secondEntry.order.dateRank,
        )
        .map(({ workshop }) => workshop);
}

/**
 * Splits occurrences into the same ordered phases which administration and public listings use.
 *
 * Keeping the ordering and the grouping together means a picker can progressively disclose an archive without
 * teaching a second place which workshop belongs before, during, or after the current moment.
 */
export function groupWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): Readonly<Record<WorkshopPhase, readonly TWorkshop[]>> {
    const workshopsByPhase: Record<WorkshopPhase, TWorkshop[]> = {
        ongoing: [],
        upcoming: [],
        past: [],
    };

    for (const workshop of sortWorkshopsByPhase(workshops, currentTimeMilliseconds)) {
        workshopsByPhase[getWorkshopPhase(workshop, currentTimeMilliseconds)].push(workshop);
    }

    return workshopsByPhase;
}

import { DEFAULT_WORKSHOP_DURATION_MINUTES } from '@/lib/workshops/workshopConstants';
import { getWorkshopExpectedEndsAtMilliseconds } from '@/lib/workshops/workshopPhase';

const CZECH_LOCALE = 'cs-CZ';
const PRAGUE_TIME_ZONE = 'Europe/Prague';
const MILLISECONDS_PER_MINUTE = 60 * 1000;

const CZECH_WORKSHOP_DATE_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    weekday: 'long',
    day: 'numeric',
    month: 'numeric',
    year: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

const CZECH_WORKSHOP_TIME_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    hour: '2-digit',
    minute: '2-digit',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * Formats an occurrence date in the timezone in which Czech workshops take place, independently of the server's
 * timezone.
 */
export function formatCzechWorkshopDate(startsAt: string): string {
    return CZECH_WORKSHOP_DATE_FORMAT.format(new Date(startsAt));
}

/**
 * The exact moment one occurrence begins at, for example `čtvrtek 20. 8. 2026 19:00`
 *
 * Note: This is how a term is named where it has to be told apart from another term held on the very same day, which
 *       is what identifies a term a registration was written for before the terms had addresses of their own.
 */
export function formatCzechWorkshopMoment(startsAt: string): string {
    return `${formatCzechWorkshopDate(startsAt)} ${formatCzechWorkshopTime(startsAt)}`;
}

const CZECH_WORKSHOP_SHORT_DATE_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    dateStyle: 'short',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * Names an occurrence date as shortly as a label beside a title can, in the same Prague time as its full date.
 */
export function formatCzechWorkshopShortDate(startsAt: string): string {
    return CZECH_WORKSHOP_SHORT_DATE_FORMAT.format(new Date(startsAt));
}

const CZECH_WORKSHOP_DAY_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    day: 'numeric',
    month: 'numeric',
    year: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The day one occurrence falls on, for example `4. 9. 2026`
 *
 * Note: This is how a term a visitor has to put in their calendar is named, so it says its year in full rather than
 *       shortening it the way a compact label beside a title does.
 */
export function formatCzechWorkshopDay(startsAt: string): string {
    return CZECH_WORKSHOP_DAY_FORMAT.format(new Date(startsAt));
}

const CZECH_WORKSHOP_DAY_AND_MONTH_FORMAT = new Intl.DateTimeFormat(CZECH_LOCALE, {
    day: 'numeric',
    month: 'numeric',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The day one occurrence falls on without its year, for example `4. 9.`
 *
 * Note: This is how a term is named where several terms are listed beside each other and the year they share would
 *       only be repeated.
 */
export function formatCzechWorkshopDayAndMonth(startsAt: string): string {
    return CZECH_WORKSHOP_DAY_AND_MONTH_FORMAT.format(new Date(startsAt));
}

const PRAGUE_CALENDAR_DATE_FORMAT = new Intl.DateTimeFormat('en-CA', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    timeZone: PRAGUE_TIME_ZONE,
});

/**
 * The calendar day one occurrence falls on in Prague, written as `2026-09-04`
 *
 * Note: This is the machine-readable day of a term rather than a label for a visitor, which is what identifies a term
 *       by the day it is held on.
 */
export function formatPragueCalendarDate(startsAt: string): string {
    return PRAGUE_CALENDAR_DATE_FORMAT.format(new Date(startsAt));
}

/**
 * The time span one occurrence runs for in Prague time, for example `10:00–16:00`
 */
export function formatCzechWorkshopTimeRange(startsAt: string, endsAt: string | null): string {
    const startTime = formatCzechWorkshopTime(startsAt);
    return endsAt === null ? startTime : `${startTime}–${formatCzechWorkshopTime(endsAt)}`;
}

/**
 * Formats the start time of a Czech workshop in Prague time.
 */
export function formatCzechWorkshopTime(startsAt: string): string {
    return CZECH_WORKSHOP_TIME_FORMAT.format(new Date(startsAt));
}

/**
 * Describes how long a workshop is expected to take. A workshop whose end is still left open is announced with the
 * same usual length as its calendar invitation, because a visitor is told how long to set aside before anybody can
 * know when the workshop will really be ended.
 */
export function formatCzechWorkshopDuration(startsAt: string, endsAt: string | null): string {
    const startsAtMilliseconds = Date.parse(startsAt);
    const durationMinutes = Number.isFinite(startsAtMilliseconds)
        ? Math.round(
              (getWorkshopExpectedEndsAtMilliseconds({ startsAt, endsAt }) - startsAtMilliseconds) /
                  MILLISECONDS_PER_MINUTE,
          )
        : DEFAULT_WORKSHOP_DURATION_MINUTES;

    return `${durationMinutes} minut`;
}

import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';
const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
        }

        const destination = markdown.slice(destinationStart, destinationEnd);
        if (destination !== '') {
            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
        }
    }

    return ranges;
}

function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const openingAnchorPattern = /<a\b[^>]*>/gi;
    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;

    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
        const openingAnchor = openingAnchorMatch[0];
        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
        if (isInsideCode(markdown, openingAnchorIndex)) {
            continue;
        }

        const hrefMatch = hrefPattern.exec(openingAnchor);
        if (hrefMatch === null) {
            continue;
        }

        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
        if (!destination) {
            continue;
        }

        const hrefText = hrefMatch[0];
        const destinationIndexInHref = hrefText.lastIndexOf(destination);
        if (destinationIndexInHref === -1) {
            continue;
        }

        const hrefIndexInAnchor = hrefMatch.index;
        ranges.push({
            destination,
            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;

    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
        const destination = autolinkMatch[1];
        const matchIndex = autolinkMatch.index ?? 0;
        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
            ranges.push({
                destination,
                start: matchIndex,
                end: matchIndex + autolinkMatch[0].length,
                isTitleRequired: true,
            });
        }
    }

    return ranges;
}

/**
 * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
 * bare HTTP(S) URL too. Materialization must therefore match that rendered
 * anchor as well as explicit Markdown and HTML links.
 */
function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
        const leadingWhitespace = bareUrlMatch[1] ?? '';
        const rawDestination = bareUrlMatch[2];
        const matchIndex = bareUrlMatch.index ?? 0;
        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
        const start = matchIndex + leadingWhitespace.length;

        if (destination !== '' && !isInsideCode(markdown, start)) {
            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
        }
    }

    return ranges;
}

function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;

    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
        const destination = definitionMatch[1] ?? definitionMatch[2];
        if (!destination) {
            continue;
        }

        const matchIndex = definitionMatch.index ?? 0;
        if (isInsideCode(markdown, matchIndex)) {
            continue;
        }

        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
        ranges.push({
            destination,
            start: matchIndex + destinationIndexInDefinition,
            end: matchIndex + destinationIndexInDefinition + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    try {
        return await fetchPublicWebPageTitle(trackableUrl);
    } catch {
        // A remote page may reject our bounded metadata request, but that must
        // never prevent a room from handing out its already-safe short link.
        return getWorkshopShortcodeLinkFallbackTitle(destinationUrl);
    }
}

/**
 * Replaces a source address with its persisted short URL. An authored Markdown,
 * HTML, or reference label remains its author's wording; raw URLs and
 * autolinks receive the fetched page title in ordinary Markdown link syntax.
 */
export function replaceWorkshopShortcodeLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    let replacedMarkdown = bodyMarkdown;
    const ranges = collectWorkshopMaterialLinkRanges(bodyMarkdown);

    for (const range of [...ranges].reverse()) {
        const shortcodeLink = shortcodeLinkByDestination.get(range.destination);
        if (shortcodeLink !== undefined) {
            const replacement =
                typeof shortcodeLink === 'string'
                    ? shortcodeLink
                    : range.isTitleRequired
                      ? createMarkdownShortcodeLink(shortcodeLink.title, shortcodeLink.shortUrl)
                      : shortcodeLink.shortUrl;
            replacedMarkdown =
                replacedMarkdown.slice(0, range.start) + replacement + replacedMarkdown.slice(range.end);
        }
    }

    return replacedMarkdown;
}

export function replaceWorkshopMaterialLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    return replaceWorkshopShortcodeLinkDestinations(bodyMarkdown, shortcodeLinkByDestination);
}

export function getWorkshopShortcodeLinkSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return workshopKind === 'workshop' ? 'online-workshop' : 'community';
}

export function getWorkshopMaterialShortcodeSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
    return getWorkshopShortcodeLinkSourceApp(workshopKind);
}

function getShortcodeLinkId(value: number | string): number | null {
    const shortcodeLinkId = Number(value);

    return Number.isSafeInteger(shortcodeLinkId) && shortcodeLinkId > 0 ? shortcodeLinkId : null;
}

async function loadWorkshopShortcodeLinks(
    supabase: SupabaseClient,
    linkOwner: WorkshopShortcodeLinkOwner,
): Promise<LoadedWorkshopShortcodeLinks> {
    const { data: mappingData, error: mappingError } = await supabase
        .from(linkOwner.mappingTableName)
        .select('destination_url, destination_title, shortcode_link_id')
        .eq(linkOwner.mappingOwnerColumnName, linkOwner.id);
    if (mappingError) {
        return { shortcodeLinkByDestination: null, errorMessage: mappingError.message };
    }

    const mappings = (mappingData ?? []) as WorkshopShortcodeLinkMappingRow[];
    const shortcodeLinkIds = Array.from(
        new Set(
            mappings
                .map((mapping) => getShortcodeLinkId(mapping.shortcode_link_id))
                .filter((shortcodeLinkId): shortcodeLinkId is number => shortcodeLinkId !== null),
        ),
    );
    if (shortcodeLinkIds.length === 0) {
        return { shortcodeLinkByDestination: new Map(), errorMessage: null };
    }

    const { data: shortcodeLinkData, error: shortcodeLinkError } = await supabase
        .from(SHORTCODE_LINK_TABLE_NAME)
        .select('id, shortcode')
        .in('id', shortcodeLinkIds);
    if (shortcodeLinkError) {
        return { shortcodeLinkByDestination: null, errorMessage: shortcodeLinkError.message };
    }

    const shortcodeById = new Map<number, string>(
        ((shortcodeLinkData ?? []) as ShortcodeLinkReferenceRow[])
            .map((shortcodeLink) => {
                const shortcodeLinkId = getShortcodeLinkId(shortcodeLink.id);

                return shortcodeLinkId === null ? null : ([shortcodeLinkId, shortcodeLink.shortcode] as const);
            })
            .filter((shortcodeLink): shortcodeLink is readonly [number, string] => shortcodeLink !== null),
    );
    const shortcodeLinkByDestination = new Map<string, LoadedWorkshopShortcodeLink>();

    for (const mapping of mappings) {
        const shortcodeLinkId = getShortcodeLinkId(mapping.shortcode_link_id);
        const shortcode = shortcodeLinkId === null ? undefined : shortcodeById.get(shortcodeLinkId);
        if (shortcode !== undefined) {
            const storedTitle = getStoredWorkshopShortcodeLinkTitle(mapping.destination_title);
            shortcodeLinkByDestination.set(mapping.destination_url, {
                shortUrl: createPublicShortcodeLinkUrl(shortcode),
                title: storedTitle ?? getWorkshopShortcodeLinkFallbackTitle(mapping.destination_url),
                isTitleStored: storedTitle !== null,
            });
        }
    }

    return { shortcodeLinkByDestination, errorMessage: null };
}

async function persistWorkshopShortcodeLinkTitle(
    supabase: SupabaseClient,
    linkOwner: WorkshopShortcodeLinkOwner,
    destination: string,
    title: string,
): Promise<string | null> {
    const { error } = await supabase
        .from(linkOwner.mappingTableName)
        .update({ destination_title: title })
        .eq(linkOwner.mappingOwnerColumnName, linkOwner.id)
        .eq('destination_url', destination);

    return error?.message ?? null;
}

/**
 * Makes sure every trackable URL of one persisted workshop record has one ad
 * hoc short link, then returns a copy of its Markdown with public short URLs.
 * The original text remains in its source table, so a changed destination or a
 * deleted shortcode can be safely prepared again.
 */
async function materializeWorkshopShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly bodyMarkdown: string;
    },
    linkOwner: WorkshopShortcodeLinkOwner,
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    const destinations = getWorkshopShortcodeLinkDestinations(context.bodyMarkdown);
    if (destinations.length === 0) {
        return { bodyMarkdown: context.bodyMarkdown, errorMessage: null };
    }
    const destinationsRequiringTitle = new Set(getWorkshopShortcodeLinkDestinationsRequiringTitle(context.bodyMarkdown));

    const loadedShortcodeLinks = await loadWorkshopShortcodeLinks(supabase, linkOwner);
    if (loadedShortcodeLinks.shortcodeLinkByDestination === null) {
        return { bodyMarkdown: null, errorMessage: loadedShortcodeLinks.errorMessage };
    }

    const missingDestinations = destinations.filter(
        (destination) => !loadedShortcodeLinks.shortcodeLinkByDestination.has(destination),
    );
    for (const destination of missingDestinations) {
        const destinationTitle = destinationsRequiringTitle.has(destination)
            ? await resolveWorkshopShortcodeLinkTitle(destination)
            : null;
        const trackedDestination = createWorkshopShortcodeLinkTrackingUrl(
            destination,
            context.workshopSlug,
            linkOwner.id,
        );
        const createdShortcodeLink = await createAdHocShortcodeLink(supabase, {
            urls: [trackedDestination],
            note: linkOwner.note,
            sourceApp: getWorkshopShortcodeLinkSourceApp(context.workshopKind),
        });
        if (createdShortcodeLink.shortcodeLink === null) {
            return { bodyMarkdown: null, errorMessage: createdShortcodeLink.errorMessage };
        }

        // A room can be opened by many people at once. Upsert lets the source
        // record retain whichever equivalent shortcode reached the mapping
        // first, and a reload below makes every concurrent response use it.
        const { error: mappingError } = await supabase
            .from(linkOwner.mappingTableName)
            .upsert(
                {
                    [linkOwner.mappingOwnerColumnName]: linkOwner.id,
                    destination_url: destination,
                    shortcode_link_id: createdShortcodeLink.shortcodeLink.id,
                    ...(destinationTitle === null ? {} : { destination_title: destinationTitle }),
                },
                { onConflict: `${linkOwner.mappingOwnerColumnName},destination_url`, ignoreDuplicates: true },
            );
        if (mappingError) {
            return { bodyMarkdown: null, errorMessage: mappingError.message };
        }
    }

    const resolvedShortcodeLinks =
        missingDestinations.length === 0
            ? loadedShortcodeLinks
            : await loadWorkshopShortcodeLinks(supabase, linkOwner);
    const resolvedShortcodeLinkByDestination = resolvedShortcodeLinks.shortcodeLinkByDestination;
    if (resolvedShortcodeLinkByDestination === null) {
        return { bodyMarkdown: null, errorMessage: resolvedShortcodeLinks.errorMessage };
    }

    const destinationsWithoutStoredTitle = Array.from(destinationsRequiringTitle).filter(
        (destination) => !resolvedShortcodeLinkByDestination.get(destination)?.isTitleStored,
    );
    for (const destination of destinationsWithoutStoredTitle) {
        const titleErrorMessage = await persistWorkshopShortcodeLinkTitle(
            supabase,
            linkOwner,
            destination,
            await resolveWorkshopShortcodeLinkTitle(destination),
        );
        if (titleErrorMessage !== null) {
            return { bodyMarkdown: null, errorMessage: titleErrorMessage };
        }
    }

    let shortcodeLinkByDestination = resolvedShortcodeLinkByDestination;
    if (destinationsWithoutStoredTitle.length > 0) {
        const reloadedShortcodeLinks = await loadWorkshopShortcodeLinks(supabase, linkOwner);
        const reloadedShortcodeLinkByDestination = reloadedShortcodeLinks.shortcodeLinkByDestination;
        if (reloadedShortcodeLinkByDestination === null) {
            return { bodyMarkdown: null, errorMessage: reloadedShortcodeLinks.errorMessage };
        }
        shortcodeLinkByDestination = reloadedShortcodeLinkByDestination;
    }

    return {
        bodyMarkdown: replaceWorkshopShortcodeLinkDestinations(context.bodyMarkdown, shortcodeLinkByDestination),
        errorMessage: null,
    };
}

/**
 * Makes sure every trackable URL in one material has one ad hoc short link,
 * then returns a copy of its Markdown which contains the public short URLs.
 */
export async function materializeWorkshopMaterialShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly contentBlockId: string;
        readonly bodyMarkdown: string;
    },
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    return materializeWorkshopShortLinks(supabase, context, {
        id: context.contentBlockId,
        mappingTableName: WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
        mappingOwnerColumnName: 'content_block_id',
        note: `Ad hoc material link for ${context.workshopSlug}`,
    });
}

/**
 * Materializes the links of an eligible chat message through the same persisted
 * ad hoc-shortcode path as materials. A normal participant never calls this:
 * their links deliberately stay inert text in the room.
 */
export async function materializeWorkshopCommentShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly commentId: string;
        readonly bodyMarkdown: string;
    },
): Promise<{ readonly bodyMarkdown: string | null; readonly errorMessage: string | null }> {
    return materializeWorkshopShortLinks(supabase, context, {
        id: context.commentId,
        mappingTableName: WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
        mappingOwnerColumnName: 'comment_id',
        note: `Ad hoc chat link for ${context.workshopSlug}`,
    });
}

/**
 * Prepares a material as soon as an administrator saves it. Participant-facing
 * reads call the same materializer as a recovery path for links which existed
 * before this migration or whose first preparation was interrupted.
 */
export async function ensureWorkshopMaterialShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly contentBlockId: string;
        readonly bodyMarkdown: string;
    },
): Promise<string | null> {
    const { errorMessage } = await materializeWorkshopMaterialShortLinks(supabase, context);

    return errorMessage;
}

/**
 * Prepares a moderator or artificial message as soon as it is written. Public
 * state loading repeats this safely for old messages and interrupted writes.
 */
export async function ensureWorkshopCommentShortLinks(
    supabase: SupabaseClient,
    context: {
        readonly workshopSlug: string;
        readonly workshopKind: WorkshopKind;
        readonly commentId: string;
        readonly bodyMarkdown: string;
    },
): Promise<string | null> {
    const { errorMessage } = await materializeWorkshopCommentShortLinks(supabase, context);

    return errorMessage;
}

/**
 * The client-safe Markdown link syntax shared by the material renderer, public room exports, and server-side short
 * link materialization. It intentionally has no network or database imports.
 */

const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

export type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
        }

        const destination = markdown.slice(destinationStart, destinationEnd);
        if (destination !== '') {
            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
        }
    }

    return ranges;
}

function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const openingAnchorPattern = /<a\b[^>]*>/gi;
    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;

    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
        const openingAnchor = openingAnchorMatch[0];
        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
        if (isInsideCode(markdown, openingAnchorIndex)) {
            continue;
        }

        const hrefMatch = hrefPattern.exec(openingAnchor);
        if (hrefMatch === null) {
            continue;
        }

        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
        if (!destination) {
            continue;
        }

        const hrefText = hrefMatch[0];
        const destinationIndexInHref = hrefText.lastIndexOf(destination);
        if (destinationIndexInHref === -1) {
            continue;
        }

        const hrefIndexInAnchor = hrefMatch.index;
        ranges.push({
            destination,
            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;

    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
        const destination = autolinkMatch[1];
        const matchIndex = autolinkMatch.index ?? 0;
        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
            ranges.push({
                destination,
                start: matchIndex,
                end: matchIndex + autolinkMatch[0].length,
                isTitleRequired: true,
            });
        }
    }

    return ranges;
}

/**
 * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a bare HTTP(S) URL too. Link parsing must
 * therefore match that rendered anchor as well as explicit Markdown and HTML links.
 */
function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
        const leadingWhitespace = bareUrlMatch[1] ?? '';
        const rawDestination = bareUrlMatch[2];
        const matchIndex = bareUrlMatch.index ?? 0;
        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
        const start = matchIndex + leadingWhitespace.length;

        if (destination !== '' && !isInsideCode(markdown, start)) {
            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
        }
    }

    return ranges;
}

function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;

    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
        const destination = definitionMatch[1] ?? definitionMatch[2];
        if (!destination) {
            continue;
        }

        const matchIndex = definitionMatch.index ?? 0;
        if (isInsideCode(markdown, matchIndex)) {
            continue;
        }

        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
        ranges.push({
            destination,
            start: matchIndex + destinationIndexInDefinition,
            end: matchIndex + destinationIndexInDefinition + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

export function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

export function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether they use inline Markdown, HTML,
 * autolinks, or reference definitions. Images, anchors, e-mail links, and code samples are deliberately not click
 * links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

export function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

type DownloadBlobFileOptions = {
    readonly fileName: string;
    readonly blob: Blob;
};

/**
 * Offer a file generated in the browser to the user as a download
 */
export function downloadBlobFile({ fileName, blob }: DownloadBlobFileOptions): void {
    const fileUrl = URL.createObjectURL(blob);
    const linkElement = document.createElement('a');
    linkElement.href = fileUrl;
    linkElement.download = fileName;

    // Note: Firefox only follows the click when the link is a part of the document
    document.body.appendChild(linkElement);
    linkElement.click();
    document.body.removeChild(linkElement);

    URL.revokeObjectURL(fileUrl);
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content -Raw package.json; Get-Content -Raw lib/workshops/workshopPhase.ts; Get-Content -Raw businesses/online-workshop/participant/WorkshopStage.test.tsx; Get-Content -Raw businesses/online-workshop/participant/WorkshopWrapUp.tsx' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 270ms:
{
    "name": "promptbook-landing-page",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "npx kill-port 4009 && next dev -p 4009",
        "build": "next build",
        "start": "next start",
        "lint": "next lint",
        "test": "vitest run",
        "test-e2e": "playwright test",
        "test-e2e:headed": "playwright test --headed",
        "test-types": "npm run build && tsc",
        "migrate-database": "tsx scripts/migrate-database.ts",
        "backup-database": "tsx scripts/backup-database.ts",
        "backup-database:verify": "tsx scripts/backup-database-verify.ts",
        "delete-test-data": "tsx scripts/delete-test-data.ts",
        "test-for-ptbk-coder": "npm run lint && npx kill-port 4009 && npm run test-types && npm run test-e2e && npm run delete-test-data",
        "export": "next build",
        "coder:generate-boilerplates": "npx ptbk coder generate-boilerplates --template ./prompts/templates/common.md",
        "coder:run": "npx ptbk coder run --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-push --auto-pull",
        "coder:run:openai": "npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-push --auto-pull",
        "coder:find-refactor-candidates": "npx ptbk coder find-refactor-candidates",
        "coder:verify": "npx ptbk coder verify --order from-latest --commit --auto-pull --auto-push",
        "coder:add": "npx ptbk coder add --template ./prompts/templates/common.md"
    },
    "dependencies": {
        "@hookform/resolvers": "^5.0.1",
        "@radix-ui/react-accordion": "^1.2.3",
        "@radix-ui/react-alert-dialog": "^1.1.6",
        "@radix-ui/react-aspect-ratio": "^1.1.2",
        "@radix-ui/react-avatar": "^1.1.3",
        "@radix-ui/react-checkbox": "^1.1.4",
        "@radix-ui/react-collapsible": "^1.1.3",
        "@radix-ui/react-context-menu": "^2.2.6",
        "@radix-ui/react-dialog": "^1.1.6",
        "@radix-ui/react-dropdown-menu": "^2.1.6",
        "@radix-ui/react-hover-card": "^1.1.6",
        "@radix-ui/react-label": "^2.1.2",
        "@radix-ui/react-menubar": "^1.1.6",
        "@radix-ui/react-navigation-menu": "^1.2.5",
        "@radix-ui/react-popover": "^1.1.6",
        "@radix-ui/react-progress": "^1.1.2",
        "@radix-ui/react-radio-group": "^1.2.3",
        "@radix-ui/react-scroll-area": "^1.2.3",
        "@radix-ui/react-select": "^2.1.6",
        "@radix-ui/react-separator": "^1.1.2",
        "@radix-ui/react-slider": "^1.2.3",
        "@radix-ui/react-slot": "^1.1.2",
        "@radix-ui/react-switch": "^1.1.3",
        "@radix-ui/react-tabs": "^1.1.3",
        "@radix-ui/react-toast": "^1.2.6",
        "@radix-ui/react-toggle": "^1.1.2",
        "@radix-ui/react-toggle-group": "^1.1.2",
        "@radix-ui/react-tooltip": "^1.1.8",
        "@supabase/ssr": "^0.7.0",
        "@supabase/supabase-js": "^2.57.2",
        "class-variance-authority": "^0.7.1",
        "clsx": "^2.1.1",
        "cmdk": "^1.1.1",
        "date-fns": "^3.6.0",
        "dotenv": "^16.4.7",
        "embla-carousel-react": "^8.5.2",
        "framer-motion": "^12.6.3",
        "input-otp": "^1.4.2",
        "js-yaml": "^4.1.0",
        "leaflet": "^1.9.4",
        "logrocket": "^12.1.1",
        "lucide-react": "^0.487.0",
        "moment": "^2.30.1",
        "next": "15.2.6",
        "next-themes": "^0.4.6",
        "pg": "^8.16.3",
        "react": "^18.3.1",
        "react-day-picker": "^8.10.1",
        "react-dom": "^18.3.1",
        "react-hook-form": "^7.55.0",
        "react-qr-code": "^2.0.18",
        "react-resizable-panels": "^2.1.7",
        "react-use-measure": "^2.1.7",
        "recharts": "^2.15.1",
        "sharp": "^0.34.4",
        "sonner": "^2.0.3",
        "spacetrim": "^0.11.60",
        "stripe": "^22.6.0",
        "tailwind-merge": "^3.1.0",
        "tw-animate-css": "^1.2.5",
        "vaul": "^1.1.2",
        "zod": "^3.24.2"
    },
    "devDependencies": {
        "@babel/eslint-parser": "^7.27.1",
        "@eslint/eslintrc": "^3",
        "@playwright/test": "^1.62.1",
        "@testing-library/jest-dom": "^6.6.3",
        "@testing-library/react": "^16.3.0",
        "@total-typescript/ts-reset": "^0.6.1",
        "@types/js-yaml": "^4.0.9",
        "@types/lodash": "^4.17.20",
        "@types/node": "^20",
        "@types/pg": "^8.15.5",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "autoprefixer": "^10.4.21",
        "eslint": "^9",
        "eslint-config-next": "15.2.4",
        "jsdom": "^26.0.0",
        "postcss": "^8.5.3",
        "ptbk": "0.114.0-38",
        "raw-loader": "^4.0.2",
        "tailwindcss": "^3.4.17",
        "tailwindcss-animate": "^1.0.7",
        "tsx": "^4.19.4",
        "typescript": "^5",
        "vitest": "^3.1.1"
    }
}

import { DEFAULT_WORKSHOP_DURATION_MINUTES } from '@/lib/workshops/workshopConstants';

const MILLISECONDS_PER_MINUTE = 60 * 1000;

/**
 * Where one occurrence currently stands in time
 */
export const WORKSHOP_PHASE_VALUES = ['ongoing', 'upcoming', 'past'] as const;

export type WorkshopPhase = (typeof WORKSHOP_PHASE_VALUES)[number];

/**
 * As much of a workshop as it takes to decide when it happens
 *
 * Note: Both a summary and the full details of a workshop satisfy this shape, so nothing has to be loaded just to
 *       place an occurrence in time.
 * Note: An occurrence without an end has not ended. It runs for as long as it takes and is only over once its end is
 *       recorded, which is what the administration does when the workshop is really finished.
 */
export type WorkshopOccurrenceTiming = {
    readonly startsAt: string;
    readonly endsAt: string | null;
};

/**
 * The order in which an administrator needs the phases: what runs right now, then what is being prepared, and
 * finally the history.
 */
const WORKSHOP_PHASE_ORDER: Readonly<Record<WorkshopPhase, number>> = {
    ongoing: 0,
    upcoming: 1,
    past: 2,
};

/**
 * Moment an occurrence was really given as its end, or `null` while that end is left open
 *
 * Note: This is the single rule for whether an occurrence has an end at all, so nothing decides on its own that an
 *       unwritten or nonsensical end means the workshop is over.
 */
export function getWorkshopRecordedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number | null {
    const endsAtMilliseconds = occurrence.endsAt === null ? Number.NaN : Date.parse(occurrence.endsAt);

    return endsAtMilliseconds > Date.parse(occurrence.startsAt) ? endsAtMilliseconds : null;
}

/**
 * Whether an occurrence still has no end, so it runs until the administration ends it
 */
export function isWorkshopEndOpen(occurrence: WorkshopOccurrenceTiming): boolean {
    return getWorkshopRecordedEndsAtMilliseconds(occurrence) === null;
}

/**
 * Moment an occurrence is expected to end, taken from the usual length of a workshop while its end is left open
 *
 * Note: A calendar invitation and a duration label have to name a length before an occurrence has one, which is what
 *       this expectation is for. It deliberately never decides whether an occurrence is over — only a recorded end
 *       does that.
 */
export function getWorkshopExpectedEndsAtMilliseconds(occurrence: WorkshopOccurrenceTiming): number {
    return (
        getWorkshopRecordedEndsAtMilliseconds(occurrence) ??
        Date.parse(occurrence.startsAt) + DEFAULT_WORKSHOP_DURATION_MINUTES * MILLISECONDS_PER_MINUTE
    );
}

/**
 * Decides whether an occurrence is still ahead, running right now, or already over
 *
 * Note: An occurrence whose end is open never becomes past by itself. It keeps running — and keeps its stage on —
 *       until an administrator records the end of it.
 *
 * @param currentTimeMilliseconds moment to compare against, so a list places every occurrence against the same instant
 */
export function getWorkshopPhase(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds = Date.now(),
): WorkshopPhase {
    if (Date.parse(occurrence.startsAt) > currentTimeMilliseconds) {
        return 'upcoming';
    }

    const recordedEndsAtMilliseconds = getWorkshopRecordedEndsAtMilliseconds(occurrence);
    return recordedEndsAtMilliseconds === null || recordedEndsAtMilliseconds > currentTimeMilliseconds
        ? 'ongoing'
        : 'past';
}

/**
 * The one phase which speaks for a group of occurrences, which is the most pressing among them
 *
 * Note: A day of a calendar carries whatever terms fall on it, and it can only be coloured by one of them. It is
 *       coloured by the very same ranking which puts a running term on top of a list, so a day where something is
 *       happening right now never looks like history.
 */
export function getMostProminentWorkshopPhase(phases: readonly WorkshopPhase[]): WorkshopPhase {
    return phases.reduce(
        (mostProminentPhase, phase) =>
            WORKSHOP_PHASE_ORDER[phase] < WORKSHOP_PHASE_ORDER[mostProminentPhase] ? phase : mostProminentPhase,
        'past',
    );
}

/**
 * The two keys which order one occurrence among the others
 *
 * Note: The history is ranked by its negated date, so the term which ended last leads it while the future is led by
 *       the term which starts next. The most relevant occurrence of every group therefore stays on top of it.
 */
function getWorkshopPhaseOrder(
    occurrence: WorkshopOccurrenceTiming,
    currentTimeMilliseconds: number,
): { readonly phaseRank: number; readonly dateRank: number } {
    const phase = getWorkshopPhase(occurrence, currentTimeMilliseconds);
    const startsAtMilliseconds = Date.parse(occurrence.startsAt);
    const dateRank = Number.isFinite(startsAtMilliseconds) ? startsAtMilliseconds : 0;

    return { phaseRank: WORKSHOP_PHASE_ORDER[phase], dateRank: phase === 'past' ? -dateRank : dateRank };
}

/**
 * Orders occurrences by their phase first and by their date within every phase
 *
 * @param currentTimeMilliseconds moment which decides the phases, so one list is never sorted against a moving instant
 */
export function sortWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): readonly TWorkshop[] {
    return workshops
        .map((workshop) => ({ workshop, order: getWorkshopPhaseOrder(workshop, currentTimeMilliseconds) }))
        .sort(
            (firstEntry, secondEntry) =>
                firstEntry.order.phaseRank - secondEntry.order.phaseRank ||
                firstEntry.order.dateRank - secondEntry.order.dateRank,
        )
        .map(({ workshop }) => workshop);
}

/**
 * Splits occurrences into the same ordered phases which administration and public listings use.
 *
 * Keeping the ordering and the grouping together means a picker can progressively disclose an archive without
 * teaching a second place which workshop belongs before, during, or after the current moment.
 */
export function groupWorkshopsByPhase<TWorkshop extends WorkshopOccurrenceTiming>(
    workshops: readonly TWorkshop[],
    currentTimeMilliseconds = Date.now(),
): Readonly<Record<WorkshopPhase, readonly TWorkshop[]>> {
    const workshopsByPhase: Record<WorkshopPhase, TWorkshop[]> = {
        ongoing: [],
        upcoming: [],
        past: [],
    };

    for (const workshop of sortWorkshopsByPhase(workshops, currentTimeMilliseconds)) {
        workshopsByPhase[getWorkshopPhase(workshop, currentTimeMilliseconds)].push(workshop);
    }

    return workshopsByPhase;
}

/**
 * @vitest-environment jsdom
 */

import type { SubscribeToWorkshopReactions } from '@/businesses/online-workshop/participant/useWorkshopReactionAnimations';
import { WorkshopStage } from '@/businesses/online-workshop/participant/WorkshopStage';
import type { CommunityMembershipRoomState } from '@/lib/community-membership/communityMembershipTypes';
import { DEFAULT_EVENT_DETAILS } from '@/lib/events/event';
import type { GithubCommit } from '@/lib/github/githubCommitFeed';
import type { FlyingWorkshopReaction } from '@/lib/workshops/workshopReactionAnimations';
import type { SubscribeToWorkshopRepositoryCommits } from '@/lib/workshops/workshopRepositoryProgress';
import type { WorkshopCommentReference, WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
import { act, cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
import { afterEach, describe, expect, it, vi } from 'vitest';

/**
 * The room as far as the stage is concerned: the membership it already loaded for the member watching it
 */
const membershipRoomMock = vi.hoisted(() => ({
    membershipRoom: null as null | {
        membership: CommunityMembershipRoomState | null;
        openMembershipModal: () => void;
    },
}));

vi.mock('@/businesses/community/membership/CommunityMembershipRoomProvider', () => ({
    useCommunityMembershipRoom: () => membershipRoomMock.membershipRoom,
}));

const PAID_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'active',
    monthlyPriceCzk: 199,
    currentPeriodEndsAt: '2026-09-30T10:00:00.000Z',
    isCancellationScheduled: false,
    isPurchaseOffered: false,
    isSubscriptionManagementOffered: true,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const FREE_MEMBERSHIP: CommunityMembershipRoomState = {
    status: 'none',
    monthlyPriceCzk: null,
    currentPeriodEndsAt: null,
    isCancellationScheduled: false,
    isPurchaseOffered: true,
    isSubscriptionManagementOffered: false,
    isCoveredByDiscountCode: false,
    isPaymentInTestMode: false,
};

const WORKSHOP: WorkshopDetails = {
    id: '5a7eb2ad-2583-4e98-9640-50bc773b5fde',
    kind: 'workshop',
    event: DEFAULT_EVENT_DETAILS,
    slug: 'online-workshop-2026-08-20',
    title: 'Produkční kód s AI agenty',
    description: 'Online workshop s Pavolem Hejným a Jiřím Jahnem.',
    startsAt: '2026-08-20T19:00:00+02:00',
    endsAt: '2026-08-20T20:30:00+02:00',
    youtubeVideoId: null,
    recordingStartOffsetSeconds: 0,
    previewYoutubeVideoId: null,
    presentationUrl: null,
    repository: null,
    isPublished: true,
    allowedReactions: ['👍'],
    disabledPanels: [],
    createdAt: '2026-08-01T10:00:00+02:00',
    updatedAt: '2026-08-01T10:00:00+02:00',
};

const WORKSHOP_WITH_VIDEO: WorkshopDetails = {
    ...WORKSHOP,
    youtubeVideoId: 'dQw4w9WgXcQ',
    recordingStartOffsetSeconds: 75,
};

const OPEN_ENDED_WORKSHOP_WITH_VIDEO: WorkshopDetails = {
    ...WORKSHOP_WITH_VIDEO,
    endsAt: null,
};

/**
 * The teaser an administrator published for the recording of a workshop
 */
const PREVIEW_YOUTUBE_VIDEO_ID = 'M7lc1UVf-VE';

/**
 * The ended workshop as the server hands it to a member whose membership does not unlock its recording: the stream is
 * gone from the room itself and only what is offered to them comes with it.
 */
const WORKSHOP_WITHOUT_ITS_RECORDING: WorkshopDetails = {
    ...WORKSHOP_WITH_VIDEO,
    youtubeVideoId: null,
};

const FOLLOW_UP_CONTENT: WorkshopContentBlock = {
    id: 'follow-up-material',
    title: 'Materiály pro další krok',
    bodyMarkdown: '[Otevřít](https://example.com/materialy)',
    unlockAt: '2026-08-20T19:00:00+02:00',
    sortOrder: 0,
    isPublished: true,
    isFollowUp: true,
    isPaidMembersOnly: false,
    createdAt: '2026-08-20T18:00:00+02:00',
    updatedAt: '2026-08-20T18:00:00+02:00',
    linkClickCount: 0,
};

const STAGE_COMMENT: WorkshopCommentReference = {
    id: 'stage-question',
    authorName: 'Jana Nováková',
    body: 'Jak poznám, že agent opravdu běží v produkci?',
};

/**
 * The room as far as the stage is concerned: something which tells it about a reaction
 */
function createReactionSource() {
    const listeners = new Set<(reaction: FlyingWorkshopReaction) => void>();
    const subscribeToReactions: SubscribeToWorkshopReactions = (listener) => {
        listeners.add(listener);
        return () => {
            listeners.delete(listener);
        };
    };

    return {
        subscribeToReactions,
        listenerCount: () => listeners.size,
        sendReaction: (reaction: FlyingWorkshopReaction) =>
            act(() => {
                listeners.forEach((listener) => listener(reaction));
            }),
    };
}

function createRepositoryCommitSource() {
    const listeners = new Set<(commit: GithubCommit) => void>();
    const subscribeToRepositoryCommits: SubscribeToWorkshopRepositoryCommits = (listener) => {
        listeners.add(listener);
        return () => {
            listeners.delete(listener);
        };
    };

    return {
        subscribeToRepositoryCommits,
        sendCommit: (commit: GithubCommit) =>
            act(() => {
                listeners.forEach((listener) => listener(commit));
            }),
    };
}

afterEach(() => {
    cleanup();
    membershipRoomMock.membershipRoom = null;
});

describe('workshop stage', () => {
    it('sends a reaction of the room over the stage', async () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(reactionSource.listenerCount()).toBe(1);
        await reactionSource.sendReaction({ flightId: 'first', reactionText: '🎉' });
        await waitFor(() => expect(container.querySelectorAll('.workshop-reaction')).toHaveLength(1));
        expect(container.querySelector('.workshop-reaction')?.className).toContain('workshop-reaction-flight--launch');
    });

    it('shows a new repository commit on the stage for ten seconds', () => {
        vi.useFakeTimers();
        try {
            const reactionSource = createReactionSource();
            const repositoryCommitSource = createRepositoryCommitSource();
            const commit: GithubCommit = {
                sha: '6dcb09b5b57875f334f61aebed695e2e4193db5b',
                message: 'Přidat oznámení commitů',
                authorName: 'Pavol Hejný',
                committedAt: '2026-08-20T19:09:00.000Z',
            };

            render(
                <WorkshopStage
                    workshop={WORKSHOP}
                    serverTime="2026-08-20T19:10:00+02:00"
                    subscribeToReactions={reactionSource.subscribeToReactions}
                    repository={{ owner: 'hejny', name: 'promptbook', branch: 'main', deploymentUrl: null }}
                    subscribeToRepositoryCommits={repositoryCommitSource.subscribeToRepositoryCommits}
                />,
            );

            repositoryCommitSource.sendCommit(commit);
            expect(screen.getByRole('status').textContent).toContain('Nový commit na projektu');
            expect(screen.getByText(commit.message)).not.toBeNull();

            act(() => vi.advanceTimersByTime(9_999));
            expect(screen.getByText(commit.message)).not.toBeNull();

            act(() => vi.advanceTimersByTime(1));
            expect(screen.queryByText(commit.message)).toBeNull();
        } finally {
            vi.useRealTimers();
        }
    });

    it('shows the question the host selected over the stream', () => {
        const reactionSource = createReactionSource();
        render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                stageComment={STAGE_COMMENT}
            />,
        );

        expect(screen.getByRole('status').textContent).toContain('Otázka na stage');
        expect(screen.getByRole('status').textContent).toContain(STAGE_COMMENT.authorName);
        expect(screen.getByRole('status').textContent).toContain(STAGE_COMMENT.body);
    });

    it('stops listening once the room leaves the stage', () => {
        const reactionSource = createReactionSource();
        const { unmount } = render(
            <WorkshopStage
                workshop={WORKSHOP}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        unmount();
        expect(reactionSource.listenerCount()).toBe(0);
    });

    it('offers the active video in fullscreen mode', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );
        const videoFrame = container.querySelector('iframe');
        const requestFullscreen = vi.fn().mockResolvedValue(undefined);

        expect(videoFrame).not.toBeNull();
        Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });

        expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
        expect(videoFrame?.allowFullscreen).toBe(true);

        fireEvent.click(screen.getByRole('button', { name: 'Přehrát video na celé obrazovce' }));

        expect(requestFullscreen).toHaveBeenCalledOnce();
    });

    it('takes the subtitles away from the video it plays', () => {
        const reactionSource = createReactionSource();
        const postMessage = vi.fn();
        const contentWindowSpy = vi
            .spyOn(HTMLIFrameElement.prototype, 'contentWindow', 'get')
            .mockReturnValue({ postMessage } as unknown as Window);

        render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(postMessage.mock.calls.map(([message]) => JSON.parse(message as string))).toEqual([
            { event: 'command', func: 'unloadModule', args: ['captions'] },
            { event: 'command', func: 'unloadModule', args: ['cc'] },
        ]);

        contentWindowSpy.mockRestore();
    });

    it('keeps the configured replay offset out of the live stream', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T19:10:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(container.querySelector('iframe')?.getAttribute('src')).not.toContain('start=75');
    });

    it('says nothing to a player of a workshop which has not started', () => {
        const reactionSource = createReactionSource();
        const postMessage = vi.fn();
        const contentWindowSpy = vi
            .spyOn(HTMLIFrameElement.prototype, 'contentWindow', 'get')
            .mockReturnValue({ postMessage } as unknown as Window);

        render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T18:50:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
            />,
        );

        expect(postMessage).not.toHaveBeenCalled();

        contentWindowSpy.mockRestore();
    });

    it('keeps the video of a workshop without an end on the stage however long it runs', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={OPEN_ENDED_WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-21T09:00:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
            />,
        );

        expect(container.querySelector('iframe')).not.toBeNull();
        expect(screen.queryByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).toBeNull();
    });

    it('wraps a workshop without an end up as soon as the administration records its end', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={{ ...OPEN_ENDED_WORKSHOP_WITH_VIDEO, endsAt: '2026-08-20T21:12:00+02:00' }}
                serverTime="2026-08-20T21:13:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
    });

    it('replaces the video with the wrap-up while reactions keep their stage stream', () => {
        const reactionSource = createReactionSource();
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                feedback={null}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
        expect(screen.getByRole('link', { name: /Materiály pro další krok/ }).getAttribute('href')).toBe(
            '#workshop-material-follow-up-material',
        );
        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
        expect(reactionSource.listenerCount()).toBe(1);
    });

    it('keeps the wrap-up for a member who has not paid and offers them the withheld video instead of playing it', () => {
        const reactionSource = createReactionSource();
        membershipRoomMock.membershipRoom = { membership: FREE_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITHOUT_ITS_RECORDING}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                paidMembersOnlyVideo={{ previewYoutubeVideoId: null }}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
        expect(screen.queryByRole('button', { name: /Přehrát video znovu/ })).toBeNull();
        expect(screen.getByText('Záznam workshopu je pro placené členy')).not.toBeNull();
    });

    it('plays the published teaser of the withheld video and opens the membership which unlocks the whole of it', () => {
        const reactionSource = createReactionSource();
        const openMembershipModal = vi.fn();
        membershipRoomMock.membershipRoom = { membership: FREE_MEMBERSHIP, openMembershipModal };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITHOUT_ITS_RECORDING}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                paidMembersOnlyVideo={{ previewYoutubeVideoId: PREVIEW_YOUTUBE_VIDEO_ID }}
                onSaveFeedback={async () => true}
            />,
        );
        const previewFrame = container.querySelector('iframe');

        expect(previewFrame?.getAttribute('src')).toContain(
            `https://www.youtube-nocookie.com/embed/${PREVIEW_YOUTUBE_VIDEO_ID}`,
        );
        expect(previewFrame?.getAttribute('src')).toContain('autoplay=0');
        expect(screen.getByText('Ukázka ze záznamu')).not.toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Koupit placené členství/ }));

        expect(openMembershipModal).toHaveBeenCalledOnce();
    });

    it('keeps the wrap-up of a paid member and lets them play the video of the ended workshop again', () => {
        const reactionSource = createReactionSource();
        membershipRoomMock.membershipRoom = { membership: PAID_MEMBERSHIP, openMembershipModal: vi.fn() };
        const { container } = render(
            <WorkshopStage
                workshop={WORKSHOP_WITH_VIDEO}
                serverTime="2026-08-20T20:31:00+02:00"
                subscribeToReactions={reactionSource.subscribeToReactions}
                followUpContentBlock={FOLLOW_UP_CONTENT}
                onSaveFeedback={async () => true}
            />,
        );

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Přehrát video znovu/ }));

        const rewatchFrame = container.querySelector('iframe');
        expect(rewatchFrame).not.toBeNull();
        expect(rewatchFrame?.getAttribute('src')).toContain('https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ');
        expect(rewatchFrame?.getAttribute('src')).toContain('controls=1');
        expect(rewatchFrame?.getAttribute('src')).toContain('start=75');
        expect(screen.queryByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).toBeNull();

        fireEvent.click(screen.getByRole('button', { name: /Zpět na závěrečné shrnutí/ }));

        expect(container.querySelector('iframe')).toBeNull();
        expect(screen.getByRole('heading', { name: 'Děkujeme, že jste byli u toho!' })).not.toBeNull();
    });
});

'use client';

import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
import type {
    WorkshopContentBlock,
    WorkshopDetails,
    WorkshopFeedback as WorkshopFeedbackValue,
    WorkshopPaidMembersVideo,
} from '@/lib/workshops/workshopTypes';
import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';

type WorkshopWrapUpProps = {
    readonly workshop: Pick<
        WorkshopDetails,
        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
    >;
    readonly contentBlocks: readonly WorkshopContentBlock[];
    readonly feedback: WorkshopFeedbackValue | null;
    readonly followUpContentBlock: WorkshopContentBlock | null;
    readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;

    /**
     * The recording which the membership of this member has not unlocked, or `null` while nothing of it is withheld
     *
     * Note: Exactly one of these two is ever given: a member who may play the recording is offered the button which
     *       plays it, and a member who may not is offered the teaser of it and the membership which unlocks it.
     */
    readonly paidMembersOnlyVideo?: WorkshopPaidMembersVideo | null;

    /**
     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
     * same screen for everybody and only gains the button which plays the video again.
     */
    readonly onRewatchVideo?: () => void;
};

/**
 * The third stage of a workshop. The follow-up is still rendered by the shared material list below; this screen links
 * directly to that same record so the stage does not invent a second material model or a second tracking path.
 */
export function WorkshopWrapUp({
    workshop,
    contentBlocks,
    feedback,
    followUpContentBlock,
    paidMembersOnlyVideo = null,
    onSaveFeedback,
    onRewatchVideo,
}: WorkshopWrapUpProps) {
    return (
        <div className="relative px-5 py-7 sm:px-8 sm:py-10">
            <div className="mx-auto max-w-2xl">
                <div className="flex items-center gap-2 text-cyan-200">
                    <PartyPopper className="h-5 w-5" aria-hidden="true" />
                    <span className="text-xs font-bold uppercase tracking-[0.16em]">Workshop je u konce</span>
                </div>
                <h2 className="mt-4 text-3xl font-bold tracking-tight text-white sm:text-4xl">
                    Děkujeme, že jste byli u toho!
                </h2>
                <p className="mt-3 max-w-xl text-sm leading-7 text-slate-300 sm:text-base">
                    Budeme rádi za krátkou zpětnou vazbu. Zabere jen chvilku a pomůže nám připravit další setkání ještě
                    lépe.
                </p>

                {onRewatchVideo !== undefined && (
                    <button
                        type="button"
                        onClick={onRewatchVideo}
                        className="mt-5 inline-flex items-center gap-2 rounded-full bg-amber-300 px-5 py-2.5 text-sm font-bold text-slate-950 shadow-lg shadow-amber-300/10 transition hover:bg-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-200 focus-visible:ring-offset-2 focus-visible:ring-offset-[#081a24]"
                    >
                        <Play className="h-4 w-4" aria-hidden="true" /> Přehrát video znovu
                    </button>
                )}

                {paidMembersOnlyVideo !== null && (
                    <div className="mt-5">
                        <WorkshopPaidMembersVideoNotice paidMembersOnlyVideo={paidMembersOnlyVideo} />
                    </div>
                )}

                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />

                <div className="mt-6">
                    <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                </div>

                {followUpContentBlock !== null && (
                    <a
                        href={`#workshop-material-${followUpContentBlock.id}`}
                        className="mt-5 flex items-start gap-3 rounded-2xl border border-amber-200/25 bg-amber-300/[0.08] p-4 text-left transition hover:border-amber-200/60 hover:bg-amber-300/[0.13]"
                    >
                        <BookOpenText className="mt-0.5 h-5 w-5 shrink-0 text-amber-200" aria-hidden="true" />
                        <span className="min-w-0 flex-1">
                            <span className="block text-xs font-bold uppercase tracking-[0.14em] text-amber-200">
                                Navazující materiál
                            </span>
                            <span className="mt-1 block text-sm font-bold text-white">
                                {followUpContentBlock.title || 'Otevřít doporučený materiál z workshopu'}
                            </span>
                        </span>
                        <ArrowDown className="mt-2 h-4 w-4 shrink-0 text-amber-100" aria-hidden="true" />
                    </a>
                )}
            </div>
        </div>
    );
}


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git diff -- lib/exports/svgChartExport.ts; Get-Content -Raw lib/exports/svgChartExport.ts; rg -n --glob '"'!node_modules'"' \"createPicturePdf|createWorkshopWrapUpPdfBlob|WorkshopWrapUpPdfDownload|createWorkshopWrapUpDocument\" ." in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 347ms:
diff --git a/lib/exports/svgChartExport.ts b/lib/exports/svgChartExport.ts
index db95d01..1a43fc2 100644
--- a/lib/exports/svgChartExport.ts
+++ b/lib/exports/svgChartExport.ts
@@ -6,6 +6,8 @@
  *       raster and the printable file both start from.
  */
 
+import { createPicturePdf } from '@/lib/exports/pdfPictures';
+
 const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
 
 /**
@@ -150,68 +152,6 @@ export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: S
     return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
 }
 
-/**
- * Where one part of the written document begins, counted in bytes from its very beginning
- */
-function getByteLength(chunks: readonly Uint8Array[]): number {
-    return chunks.reduce((totalLength, chunk) => totalLength + chunk.length, 0);
-}
-
-function encodeAsciiText(text: string): Uint8Array {
-    return new TextEncoder().encode(text);
-}
-
-function formatCrossReferenceEntry(objectOffset: number): string {
-    return `${objectOffset.toString().padStart(10, '0')} 00000 n \n`;
-}
-
-/**
- * Write a text of a document as the numbers of its letters, announced by the mark which says they are UTF-16
- *
- * Note: A reader which was handed the plain letters would read a Czech name written in them as a different word
- *       altogether, and a bracket in a name would end the name in the middle of itself.
- */
-function formatPdfTextString(text: string): string {
-    const characterCodes = ['FEFF'];
-
-    for (const character of text) {
-        const codePoint = character.codePointAt(0) ?? 0;
-
-        if (codePoint > 0xffff) {
-            const surrogateOffset = codePoint - 0x10000;
-            characterCodes.push((0xd800 + (surrogateOffset >> 10)).toString(16).padStart(4, '0'));
-            characterCodes.push((0xdc00 + (surrogateOffset & 0x3ff)).toString(16).padStart(4, '0'));
-        } else {
-            characterCodes.push(codePoint.toString(16).padStart(4, '0'));
-        }
-    }
-
-    return `<${characterCodes.join('').toUpperCase()}>`;
-}
-
-/**
- * How large the picture is on the printed page, and where it sits, so that it keeps its shape inside the margins
- */
-function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
-    readonly width: number;
-    readonly height: number;
-    readonly offsetX: number;
-    readonly offsetY: number;
-} {
-    const availableWidth = PDF_PAGE_WIDTH_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const availableHeight = PDF_PAGE_HEIGHT_POINTS - 2 * PDF_PAGE_MARGIN_POINTS;
-    const scale = Math.min(availableWidth / pictureSize.width, availableHeight / pictureSize.height);
-    const width = pictureSize.width * scale;
-    const height = pictureSize.height * scale;
-
-    return {
-        width,
-        height,
-        offsetX: (PDF_PAGE_WIDTH_POINTS - width) / 2,
-        offsetY: (PDF_PAGE_HEIGHT_POINTS - height) / 2,
-    };
-}
-
 /**
  * Write the smallest possible document which shows one picture on one page
  *
@@ -219,53 +159,15 @@ function getPdfPicturePlacement(pictureSize: ChartPictureSize): {
  *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
  */
 export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
-    const placement = getPdfPicturePlacement(pictureSize);
-    const contentStream =
-        `q\n${placement.width.toFixed(2)} 0 0 ${placement.height.toFixed(2)} ` +
-        `${placement.offsetX.toFixed(2)} ${placement.offsetY.toFixed(2)} cm\n/Im0 Do\nQ\n`;
-
-    const objectBodies: readonly string[] = [
-        '<< /Type /Catalog /Pages 2 0 R >>',
-        '<< /Type /Pages /Kids [3 0 R] /Count 1 >>',
-        `<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PDF_PAGE_WIDTH_POINTS} ${PDF_PAGE_HEIGHT_POINTS}] ` +
-            '/Resources << /XObject << /Im0 5 0 R >> >> /Contents 4 0 R >>',
-        `<< /Length ${encodeAsciiText(contentStream).length} >>\nstream\n${contentStream}endstream`,
-    ];
-
-    const chunks: Uint8Array[] = [encodeAsciiText('%PDF-1.4\n')];
-    const objectOffsets: number[] = [];
-
-    objectBodies.forEach((objectBody, objectIndex) => {
-        objectOffsets.push(getByteLength(chunks));
-        chunks.push(encodeAsciiText(`${objectIndex + 1} 0 obj\n${objectBody}\nendobj\n`));
-    });
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(
-        encodeAsciiText(
-            '5 0 obj\n<< /Type /XObject /Subtype /Image ' +
-                `/Width ${pictureSize.width} /Height ${pictureSize.height} ` +
-                `/ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${pictureBytes.length} >>\n` +
-                'stream\n',
-        ),
-        pictureBytes,
-        encodeAsciiText('\nendstream\nendobj\n'),
+    return createPicturePdf(
+        [{ bytes: pictureBytes, ...pictureSize }],
+        {
+            width: PDF_PAGE_WIDTH_POINTS,
+            height: PDF_PAGE_HEIGHT_POINTS,
+            margin: PDF_PAGE_MARGIN_POINTS,
+        },
+        title,
     );
-
-    objectOffsets.push(getByteLength(chunks));
-    chunks.push(encodeAsciiText(`6 0 obj\n<< /Title ${formatPdfTextString(title)} >>\nendobj\n`));
-
-    const crossReferenceOffset = getByteLength(chunks);
-    chunks.push(
-        encodeAsciiText(
-            `xref\n0 ${objectOffsets.length + 1}\n0000000000 65535 f \n` +
-                objectOffsets.map(formatCrossReferenceEntry).join('') +
-                `trailer\n<< /Size ${objectOffsets.length + 1} /Root 1 0 R /Info 6 0 R >>\n` +
-                `startxref\n${crossReferenceOffset}\n%%EOF\n`,
-        ),
-    );
-
-    return new Blob(chunks as BlobPart[], { type: 'application/pdf' });
 }
 
 export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
/**
 * Turning a chart which is already drawn on the page into a file
 *
 * Note: The chart is an SVG element, so the picture is taken from the very element the reader is looking at instead of
 *       drawing it a second time. What the three pictures have in common is therefore one serialization, which the
 *       raster and the printable file both start from.
 */

import { createPicturePdf } from '@/lib/exports/pdfPictures';

const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

/**
 * A picture is exported with a background, because a chart drawn in dark ink on nothing at all is unreadable in a
 * viewer which shows transparency as black.
 */
const DEFAULT_EXPORT_BACKGROUND_COLOR = '#ffffff';

/**
 * The exported picture carries the typeface it is read in, because a file which left the page has no stylesheet to
 * inherit one from and would otherwise be written in whatever a viewer falls back to.
 */
const EXPORT_FONT_FAMILY = 'system-ui, -apple-system, "Segoe UI", sans-serif';

/**
 * How much sharper the raster picture is than the chart on the screen, so that it stays readable when it is enlarged
 */
const PNG_EXPORT_SCALE = 2;

const JPEG_EXPORT_QUALITY = 0.95;

/**
 * The printable page, in the points a PDF measures everything in, and the margin the picture keeps from its edges
 */
const PDF_PAGE_WIDTH_POINTS = 842;
const PDF_PAGE_HEIGHT_POINTS = 595;
const PDF_PAGE_MARGIN_POINTS = 24;

export type SvgChartExportOptions = {
    /**
     * The name the picture carries inside itself, which a reader of the file sees instead of a bare drawing
     */
    readonly title: string;

    readonly backgroundColor?: string;
};

type ChartPictureSize = {
    readonly width: number;
    readonly height: number;
};

function getSvgElementSize(svgElement: SVGSVGElement): ChartPictureSize {
    const { width, height } = svgElement.getBoundingClientRect();

    return {
        width: Math.max(1, Math.round(width || svgElement.clientWidth)),
        height: Math.max(1, Math.round(height || svgElement.clientHeight)),
    };
}

/**
 * Copy the drawn chart into a standalone document: named, sized in absolute numbers and standing on a background of
 * its own, so that it opens the same way in a browser, in a vector editor and in a document
 */
export function serializeChartAsSvg(svgElement: SVGSVGElement, options: SvgChartExportOptions): string {
    const { width, height } = getSvgElementSize(svgElement);
    const exportedElement = svgElement.cloneNode(true) as SVGSVGElement;

    exportedElement.setAttribute('width', String(width));
    exportedElement.setAttribute('height', String(height));
    exportedElement.setAttribute('viewBox', `0 0 ${width} ${height}`);
    exportedElement.setAttribute('font-family', EXPORT_FONT_FAMILY);

    // Note: On the page the chart fills whatever it is given, which an exported file has nothing to be measured
    //       against, so it is given the size it was drawn at.
    exportedElement.style.width = `${width}px`;
    exportedElement.style.height = `${height}px`;

    const titleElement = document.createElementNS(SVG_NAMESPACE, 'title');
    titleElement.textContent = options.title;

    const backgroundElement = document.createElementNS(SVG_NAMESPACE, 'rect');
    backgroundElement.setAttribute('x', '0');
    backgroundElement.setAttribute('y', '0');
    backgroundElement.setAttribute('width', String(width));
    backgroundElement.setAttribute('height', String(height));
    backgroundElement.setAttribute('fill', options.backgroundColor ?? DEFAULT_EXPORT_BACKGROUND_COLOR);

    exportedElement.insertBefore(backgroundElement, exportedElement.firstChild);
    exportedElement.insertBefore(titleElement, exportedElement.firstChild);

    // Note: The namespace is named only when the writer did not name it by itself, because an `xmlns` written twice is
    //       not a document a strict reader opens at all.
    const serializedChart = new XMLSerializer().serializeToString(exportedElement);
    const namespacedChart = serializedChart.includes('xmlns=')
        ? serializedChart
        : serializedChart.replace('<svg', `<svg xmlns="${SVG_NAMESPACE}"`);

    return `<?xml version="1.0" encoding="UTF-8"?>\n${namespacedChart}`;
}

/**
 * Draw the serialized chart onto a canvas, which is what both raster pictures are taken from
 *
 * Note: The document is handed over as a data address rather than as a temporary file, because a canvas which loaded a
 *       picture from anywhere else refuses to give its content back.
 */
async function drawChartOnCanvas(
    svgElement: SVGSVGElement,
    options: SvgChartExportOptions,
): Promise<HTMLCanvasElement> {
    const { width, height } = getSvgElementSize(svgElement);
    const serializedChart = serializeChartAsSvg(svgElement, options);
    const chartImage = new Image();
    chartImage.width = width;
    chartImage.height = height;

    await new Promise<void>((resolve, reject) => {
        chartImage.addEventListener('load', () => resolve());
        chartImage.addEventListener('error', () => reject(new Error('Graf se nepodařilo vykreslit do obrázku')));
        chartImage.src = `data:image/svg+xml;charset=utf-8,${encodeURIComponent(serializedChart)}`;
    });

    const canvasElement = document.createElement('canvas');
    canvasElement.width = width * PNG_EXPORT_SCALE;
    canvasElement.height = height * PNG_EXPORT_SCALE;

    const canvasContext = canvasElement.getContext('2d');
    if (canvasContext === null) {
        throw new Error('Graf se nepodařilo vykreslit do obrázku');
    }

    canvasContext.fillStyle = options.backgroundColor ?? DEFAULT_EXPORT_BACKGROUND_COLOR;
    canvasContext.fillRect(0, 0, canvasElement.width, canvasElement.height);
    canvasContext.drawImage(chartImage, 0, 0, canvasElement.width, canvasElement.height);

    return canvasElement;
}

async function readCanvasAsBlob(canvasElement: HTMLCanvasElement, mimeType: string, quality?: number): Promise<Blob> {
    return new Promise<Blob>((resolve, reject) => {
        canvasElement.toBlob(
            (blob) => (blob === null ? reject(new Error('Obrázek grafu se nepodařilo uložit')) : resolve(blob)),
            mimeType,
            quality,
        );
    });
}

export async function renderChartAsPngBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
    return readCanvasAsBlob(await drawChartOnCanvas(svgElement, options), 'image/png');
}

/**
 * Write the smallest possible document which shows one picture on one page
 *
 * Note: The picture is carried as the very bytes a browser compressed it into, which a PDF reader understands as
 *       `DCTDecode`. Nothing else has to be encoded, so a printable file costs no library at all.
 */
export function createSinglePicturePdf(pictureBytes: Uint8Array, pictureSize: ChartPictureSize, title: string): Blob {
    return createPicturePdf(
        [{ bytes: pictureBytes, ...pictureSize }],
        {
            width: PDF_PAGE_WIDTH_POINTS,
            height: PDF_PAGE_HEIGHT_POINTS,
            margin: PDF_PAGE_MARGIN_POINTS,
        },
        title,
    );
}

export async function renderChartAsPdfBlob(svgElement: SVGSVGElement, options: SvgChartExportOptions): Promise<Blob> {
    const canvasElement = await drawChartOnCanvas(svgElement, options);
    const pictureBlob = await readCanvasAsBlob(canvasElement, 'image/jpeg', JPEG_EXPORT_QUALITY);
    const pictureBytes = new Uint8Array(await pictureBlob.arrayBuffer());

    return createSinglePicturePdf(
        pictureBytes,
        { width: canvasElement.width, height: canvasElement.height },
        options.title,
    );
}

.\lib\workshops\workshopWrapUpDocument.ts:154:export function createWorkshopWrapUpDocument({
.\lib\workshops\workshopWrapUpDocument.test.ts:2:    createWorkshopWrapUpDocument,
.\lib\workshops\workshopWrapUpDocument.test.ts:41:        const document = createWorkshopWrapUpDocument({ workshop: WORKSHOP, contentBlocks: [CONTENT_BLOCK] });
.\lib\workshops\workshopWrapUpDocument.test.ts:72:        const document = createWorkshopWrapUpDocument({
.\lib\exports\workshopWrapUpPdf.ts:1:import { createPicturePdf, type PdfPicture } from '@/lib/exports/pdfPictures';
.\lib\exports\workshopWrapUpPdf.ts:353:export async function createWorkshopWrapUpPdfBlob(document: WorkshopWrapUpDocument): Promise<Blob> {
.\lib\exports\workshopWrapUpPdf.ts:357:    return createPicturePdf(
.\lib\exports\svgChartExport.ts:9:import { createPicturePdf } from '@/lib/exports/pdfPictures';
.\lib\exports\svgChartExport.ts:162:    return createPicturePdf(
.\lib\exports\pdfPictures.ts:120:export function createPicturePdf(
.\lib\exports\pdfPictures.test.ts:1:import { createPicturePdf } from '@/lib/exports/pdfPictures';
.\lib\exports\pdfPictures.test.ts:16:            createPicturePdf(
.\lib\exports\pdfPictures.test.ts:33:        expect(() => createPicturePdf([], PDF_PAGE_SIZE, 'Prázdné shrnutí')).toThrow('alespoň jednu stránku');
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:4:import { createWorkshopWrapUpPdfBlob } from '@/lib/exports/workshopWrapUpPdf';
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:6:    createWorkshopWrapUpDocument,
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:13:type WorkshopWrapUpPdfDownloadProps = {
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:29:export function WorkshopWrapUpPdfDownload({ workshop, contentBlocks }: WorkshopWrapUpPdfDownloadProps) {
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:38:            const document = createWorkshopWrapUpDocument({ workshop, contentBlocks });
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.tsx:39:            const blob = await createWorkshopWrapUpPdfBlob(document);
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:10:    createWorkshopWrapUpPdfBlob: vi.fn(),
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:15:    createWorkshopWrapUpPdfBlob: (document: unknown) => pdfExportMocks.createWorkshopWrapUpPdfBlob(document),
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:22:import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:51:    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockReset();
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:52:    pdfExportMocks.createWorkshopWrapUpPdfBlob.mockResolvedValue(new Blob(['PDF'], { type: 'application/pdf' }));
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:62:        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[CONTENT_BLOCK]} />);
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:66:        await waitFor(() => expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledOnce());
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:67:        expect(pdfExportMocks.createWorkshopWrapUpPdfBlob).toHaveBeenCalledWith(
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:85:        pdfExportMocks.createWorkshopWrapUpPdfBlob.mockRejectedValueOnce(new Error('canvas is unavailable'));
.\businesses\online-workshop\participant\WorkshopWrapUpPdfDownload.test.tsx:86:        render(<WorkshopWrapUpPdfDownload workshop={WORKSHOP} contentBlocks={[]} />);
.\businesses\online-workshop\participant\WorkshopWrapUp.tsx:5:import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
.\businesses\online-workshop\participant\WorkshopWrapUp.tsx:84:                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />

codex
The audit confirms the intended integration point: the stage enters its existing wrap-up only when `getWorkshopPhase` reports `past`, which requires a valid recorded end. The room state already supplies only the materials unlocked for the current attendee, so the PDF can safely consume that exact list; the public presentation is a separate already-visible workshop field.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test -- businesses/online-workshop/participant/WorkshopStage.test.tsx businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx lib/workshops/workshopWrapUpDocument.test.ts lib/exports/pdfPictures.test.ts lib/exports/svgChartExport.test.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 6909ms:

> promptbook-landing-page@0.1.0 test
> vitest run businesses/online-workshop/participant/WorkshopStage.test.tsx businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx lib/workshops/workshopWrapUpDocument.test.ts lib/exports/pdfPictures.test.ts lib/exports/svgChartExport.test.ts


 RUN  v3.2.4 C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron

 ✓ lib/exports/pdfPictures.test.ts (2 tests) 8ms
 ✓ lib/exports/svgChartExport.test.ts (4 tests) 11ms
 ✓ lib/workshops/workshopWrapUpDocument.test.ts (4 tests) 10ms
 ✓ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx (2 tests) 146ms
 ✓ businesses/online-workshop/participant/WorkshopStage.test.tsx (14 tests) 410ms

 Test Files  5 passed (5)
      Tests  26 passed (26)
   Start at  09:41:39
   Duration  5.20s (transform 1.21s, setup 0ms, collect 4.41s, tests 584ms, environment 2.96s, prepare 1.09s)


codex
The focused tests pass. I’m doing the full type/build verification next, then I’ll add only any missing regression coverage or corrections I find; I won’t alter the unrelated uncommitted work in the tree.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-types' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 35302ms:

> promptbook-landing-page@0.1.0 test-types
> npm run build && tsc


> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
Failed to compile.

node:dns/promises
Module build failed: UnhandledSchemeError: Reading from "node:dns/promises" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Hook.CALL_ASYNC_DELEGATE [as _callAsync] (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:6378)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494

Import trace for requested module:
node:dns/promises
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx

node:net
Module build failed: UnhandledSchemeError: Reading from "node:net" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494
    at NormalModule.needBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:414160)

Import trace for requested module:
node:net
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx


> Build failed because of webpack errors

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process node -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime,Path; Get-ChildItem .next -ErrorAction SilentlyContinue | Select-Object Name,LastWriteTime' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 610ms:

   Id ProcessName StartTime             Path
   -- ----------- ---------             ----
24584 node        9/15/2026 10:18:53 AM C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
27448 node        9/16/2026 8:37:51 AM  C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
28608 node        9/15/2026 10:18:54 AM C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
30564 node        9/16/2026 9:40:34 AM  C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
36852 node        9/16/2026 8:37:50 AM  C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe
                                        
                                        
                                        
                                        
                                        
                                        
                                        
                                        
                                        
                                        


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-CimInstance Win32_Process -Filter \"Name = 'node.exe'\" | Select-Object ProcessId,ParentProcessId,CommandLine | Format-List" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 440ms:

ProcessId       : 24584
ParentProcessId : 35604
CommandLine     : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 
                  C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npx-cli.js ptbk coder verify 
                  --order from-latest --ignore Refactor --commit --auto-pull --auto-push

ProcessId       : 28608
ParentProcessId : 24556
CommandLine     : "node"   "C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\.bin\\..\@pr
                  omptbook\cli\bin\promptbook-cli.js" coder verify --order from-latest --ignore Refactor --commit 
                  --auto-pull --auto-push

ProcessId       : 36852
ParentProcessId : 54256
CommandLine     : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 
                  C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npx-cli.js ptbk coder run --harness 
                  openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context 
                  AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push

ProcessId       : 27448
ParentProcessId : 29348
CommandLine     : "node"   "C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\.bin\\..\@pr
                  omptbook\cli\bin\promptbook-cli.js" coder run --harness openai-codex --model gpt-5.6-terra 
                  --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run 
                  test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push

ProcessId       : 30564
ParentProcessId : 25976
CommandLine     : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 
                  C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/@openai/codex/bin/codex.js -c 
                  forced_login_method=chatgpt -c model_reasoning_effort=max --ask-for-approval never exec --model 
                  gpt-5.6-terra --local-provider none --sandbox danger-full-access -C 
                  C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron --skip-git-repo-check


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run build' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 exited 1 in 34887ms:

> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
Failed to compile.

node:dns/promises
Module build failed: UnhandledSchemeError: Reading from "node:dns/promises" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Hook.CALL_ASYNC_DELEGATE [as _callAsync] (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:6378)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494

Import trace for requested module:
node:dns/promises
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx

node:net
Module build failed: UnhandledSchemeError: Reading from "node:net" is not handled by plugins (Unhandled scheme).
Webpack supports "data:" and "file:" URIs by default.
You may need an additional plugin to handle "node:" URIs.
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408376
    at Hook.eval [as callAsync] (eval at create (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:14:9224), <anonymous>:6:1)
    at Object.processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408301)
    at processResource (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:5308)
    at iteratePitchingLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:4667)
    at runLoaders (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\loader-runner\LoaderRunner.js:1:8590)
    at NormalModule._doBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:408163)
    at NormalModule.build (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:410176)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:82494
    at NormalModule.needBuild (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\next\dist\compiled\webpack\bundle5.js:29:414160)

Import trace for requested module:
node:net
./lib/network/publicWebPagePreview.ts
./lib/workshops/workshopMaterialLinks.ts
./lib/workshops/workshopWrapUpDocument.ts
./businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
./businesses/online-workshop/participant/WorkshopWrapUp.tsx
./businesses/online-workshop/participant/WorkshopStage.tsx
./businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx


> Build failed because of webpack errors

codex
The production build exposed one real issue: the new client-side document builder imported the server-side material-link module, which pulls Node networking APIs into the browser bundle. I’ll switch it to the existing client-safe link-syntax module so the PDF still uses the same parser without crossing the server/client boundary.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content lib/workshops/workshopMaterialLinks.ts -TotalCount 180; rg -n \"getWorkshopMaterialLinkDestinations|workshopMaterialLinkSyntax\" --glob '"'!node_modules'"' lib businesses tests" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 426ms:
import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';
const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
lib\workshops\workshopWrapUpDocument.ts:1:import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
lib\workshops\workshopWrapUpDocument.ts:83:        urls: getWorkshopMaterialLinkDestinations(contentBlock.bodyMarkdown),
lib\workshops\workshopMaterialLinkSyntax.ts:304:export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
lib\workshops\workshopMaterialLinks.ts:392:export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
lib\workshops\workshopMaterialLinks.test.ts:3:    getWorkshopMaterialLinkDestinations,
lib\workshops\workshopMaterialLinks.test.ts:66:        expect(getWorkshopMaterialLinkDestinations(materialMarkdown)).toEqual([

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "''^(export )?(function|const|type|interface) " lib/workshops/workshopMaterialLinks.ts lib/workshops/workshopMaterialLinkSyntax.ts; Get-Content lib/workshops/workshopMaterialLinks.ts | Select-Object -Skip 300 -First 135; Get-Content lib/workshops/workshopMaterialLinkSyntax.ts | Select-Object -Skip 250 -First 85; Get-Content -Raw lib/workshops/workshopMaterialLinks.test.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 293ms:
lib/workshops/workshopMaterialLinks.ts:15:const WORKSHOP_UTM_SOURCE = 'promptbook';
lib/workshops/workshopMaterialLinks.ts:16:const WORKSHOP_UTM_MEDIUM = 'workshop';
lib/workshops/workshopMaterialLinks.ts:17:const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
lib/workshops/workshopMaterialLinks.ts:18:const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
lib/workshops/workshopMaterialLinks.ts:19:const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
lib/workshops/workshopMaterialLinks.ts:20:const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
lib/workshops/workshopMaterialLinks.ts:21:const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
lib/workshops/workshopMaterialLinks.ts:23:type WorkshopMaterialLinkRange = {
lib/workshops/workshopMaterialLinks.ts:30:type WorkshopShortcodeLinkMappingRow = {
lib/workshops/workshopMaterialLinks.ts:41:type WorkshopShortcodeLinkOwner = {
lib/workshops/workshopMaterialLinks.ts:48:type ShortcodeLinkReferenceRow = {
lib/workshops/workshopMaterialLinks.ts:53:export type WorkshopShortcodeLinkPresentation = {
lib/workshops/workshopMaterialLinks.ts:58:type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;
lib/workshops/workshopMaterialLinks.ts:60:type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
lib/workshops/workshopMaterialLinks.ts:64:type LoadedWorkshopShortcodeLinks =
lib/workshops/workshopMaterialLinks.ts:71:function getWorkshopMaterialLinkBaseUrl(): string {
lib/workshops/workshopMaterialLinks.ts:75:function isEscaped(text: string, index: number): boolean {
lib/workshops/workshopMaterialLinks.ts:85:function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinks.ts:92:function isWithinInlineCode(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinks.ts:106:function isInsideCode(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinks.ts:110:function findUnescapedCharacter(text: string, character: string, start: number): number {
lib/workshops/workshopMaterialLinks.ts:120:function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:192:function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:232:function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:257:function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:276:function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:303:function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinks.ts:328:function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
lib/workshops/workshopMaterialLinks.ts:347:export function createWorkshopShortcodeLinkTrackingUrl(
lib/workshops/workshopMaterialLinks.ts:369:export function createWorkshopMaterialTrackingUrl(
lib/workshops/workshopMaterialLinks.ts:382:export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
lib/workshops/workshopMaterialLinks.ts:392:export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
lib/workshops/workshopMaterialLinks.ts:396:function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
lib/workshops/workshopMaterialLinks.ts:407:function escapeMarkdownLinkTitle(title: string): string {
lib/workshops/workshopMaterialLinks.ts:411:function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
lib/workshops/workshopMaterialLinks.ts:415:function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
lib/workshops/workshopMaterialLinks.ts:421:function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
lib/workshops/workshopMaterialLinks.ts:447:export function replaceWorkshopShortcodeLinkDestinations(
lib/workshops/workshopMaterialLinks.ts:471:export function replaceWorkshopMaterialLinkDestinations(
lib/workshops/workshopMaterialLinks.ts:478:export function getWorkshopShortcodeLinkSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
lib/workshops/workshopMaterialLinks.ts:482:export function getWorkshopMaterialShortcodeSourceApp(workshopKind: WorkshopKind): ShortcodeLinkSourceApp {
lib/workshops/workshopMaterialLinks.ts:486:function getShortcodeLinkId(value: number | string): number | null {
lib/workshops/workshopMaterialLinkSyntax.ts:6:const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
lib/workshops/workshopMaterialLinkSyntax.ts:7:const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
lib/workshops/workshopMaterialLinkSyntax.ts:8:const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
lib/workshops/workshopMaterialLinkSyntax.ts:9:const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
lib/workshops/workshopMaterialLinkSyntax.ts:10:const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
lib/workshops/workshopMaterialLinkSyntax.ts:12:export type WorkshopMaterialLinkRange = {
lib/workshops/workshopMaterialLinkSyntax.ts:19:function getWorkshopMaterialLinkBaseUrl(): string {
lib/workshops/workshopMaterialLinkSyntax.ts:23:function isEscaped(text: string, index: number): boolean {
lib/workshops/workshopMaterialLinkSyntax.ts:33:function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinkSyntax.ts:40:function isWithinInlineCode(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinkSyntax.ts:54:function isInsideCode(markdown: string, position: number): boolean {
lib/workshops/workshopMaterialLinkSyntax.ts:58:function findUnescapedCharacter(text: string, character: string, start: number): number {
lib/workshops/workshopMaterialLinkSyntax.ts:68:function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:140:function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:180:function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:204:function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:223:function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:250:export function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
lib/workshops/workshopMaterialLinkSyntax.ts:275:export function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
lib/workshops/workshopMaterialLinkSyntax.ts:294:export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
lib/workshops/workshopMaterialLinkSyntax.ts:304:export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
lib/workshops/workshopMaterialLinkSyntax.ts:308:export function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
}

function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    try {
        return await fetchPublicWebPageTitle(trackableUrl);
    } catch {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

export function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether they use inline Markdown, HTML,
 * autolinks, or reference definitions. Images, anchors, e-mail links, and code samples are deliberately not click
 * links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

export function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}
import {
    createWorkshopMaterialTrackingUrl,
    getWorkshopMaterialLinkDestinations,
    getWorkshopMaterialShortcodeSourceApp,
    materializeWorkshopCommentShortLinks,
    materializeWorkshopMaterialShortLinks,
    replaceWorkshopMaterialLinkDestinations,
    type WorkshopShortcodeLinkPresentation,
} from '@/lib/workshops/workshopMaterialLinks';
import type { SupabaseClient } from '@supabase/supabase-js';
import { afterEach, describe, expect, it, vi } from 'vitest';

const { createAdHocShortcodeLinkMock, fetchPublicWebPageTitleMock } = vi.hoisted(() => ({
    createAdHocShortcodeLinkMock: vi.fn(),
    fetchPublicWebPageTitleMock: vi.fn(),
}));

vi.mock('@/lib/shortener/shortcodeLinkAdHoc', () => ({
    createAdHocShortcodeLink: createAdHocShortcodeLinkMock,
}));

vi.mock('@/lib/network/publicWebPagePreview', () => ({
    fetchPublicWebPageTitle: fetchPublicWebPageTitleMock,
}));

afterEach(() => {
    createAdHocShortcodeLinkMock.mockReset();
    fetchPublicWebPageTitleMock.mockReset();
});

describe('workshop material tracking links', () => {
    it('adds stable workshop UTM parameters without losing existing query parameters', () => {
        const trackingUrl = createWorkshopMaterialTrackingUrl(
            'https://example.com/material?download=1&utm_source=old-source',
            'online-workshop-2026-08-20',
            'content-123',
        );

        const parsedUrl = new URL(trackingUrl);
        expect(parsedUrl.searchParams.get('download')).toBe('1');
        expect(parsedUrl.searchParams.get('utm_source')).toBe('promptbook');
        expect(parsedUrl.searchParams.get('utm_medium')).toBe('workshop');
        expect(parsedUrl.searchParams.get('utm_campaign')).toBe('online-workshop-2026-08-20');
        expect(parsedUrl.searchParams.get('utm_content')).toBe('content-123');
    });

    it('does not rewrite in-page anchors or unsupported protocols', () => {
        expect(createWorkshopMaterialTrackingUrl('#slides', 'workshop', 'content')).toBe('#slides');
        expect(createWorkshopMaterialTrackingUrl('mailto:hello@example.com', 'workshop', 'content')).toBe(
            'mailto:hello@example.com',
        );
    });

    it('finds every ordinary material link while leaving images, e-mail links, and code samples alone', () => {
        const materialMarkdown = [
            '[Inline](https://example.com/inline)',
            '<https://example.com/autolink>',
            '<a href="https://example.com/html">HTML</a>',
            '[reference]: https://example.com/reference',
            'Bare URL https://example.com/bare.',
            '![Diagram](https://example.com/image.png)',
            '[Email](mailto:hello@example.com)',
            '`[Example](https://example.com/code)`',
        ].join('\n\n');

        expect(getWorkshopMaterialLinkDestinations(materialMarkdown)).toEqual([
            'https://example.com/inline',
            'https://example.com/autolink',
            'https://example.com/html',
            'https://example.com/reference',
            'https://example.com/bare',
        ]);
    });

    it('uses the fetched title in Markdown when a raw URL becomes a short link', () => {
        const materialMarkdown =
            '[Inline](https://example.com/inline) and <a href="https://example.com/html">HTML</a> with https://example.com/bare and ![image](https://example.com/image.png)';
        const materialWithShortLinks = replaceWorkshopMaterialLinkDestinations(
            materialMarkdown,
            new Map<string, string | WorkshopShortcodeLinkPresentation>([
                ['https://example.com/inline', 'https://ptbk.io/inline123'],
                ['https://example.com/html', 'https://ptbk.io/html123'],
                [
                    'https://example.com/bare',
                    { shortUrl: 'https://ptbk.io/bare123', title: 'Příručka [pro účastníky]' },
                ],
            ]),
        );

        expect(materialWithShortLinks).toBe(
            '[Inline](https://ptbk.io/inline123) and <a href="https://ptbk.io/html123">HTML</a> with [Příručka \\[pro účastníky\\]](https://ptbk.io/bare123) and ![image](https://example.com/image.png)',
        );
    });

    it('turns a Markdown autolink into one title-backed short link', () => {
        expect(
            replaceWorkshopMaterialLinkDestinations(
                '<https://example.com/autolink>',
                new Map<string, WorkshopShortcodeLinkPresentation>([
                    [
                        'https://example.com/autolink',
                        { shortUrl: 'https://ptbk.io/autolink123', title: 'Veřejná dokumentace' },
                    ],
                ]),
            ),
        ).toBe('[Veřejná dokumentace](https://ptbk.io/autolink123)');
    });

    it('labels automatic material links by the app which created them', () => {
        expect(getWorkshopMaterialShortcodeSourceApp('workshop')).toBe('online-workshop');
        expect(getWorkshopMaterialShortcodeSourceApp('community')).toBe('community');
        expect(getWorkshopMaterialShortcodeSourceApp('project')).toBe('community');
    });

    it('creates and returns an ad hoc short link instead of exposing a material destination', async () => {
        let mappings: readonly { readonly destination_url: string; readonly shortcode_link_id: number }[] = [];
        const mappingUpsert = vi.fn(async (values: {
            readonly destination_url: string;
            readonly shortcode_link_id: number;
        }) => {
            mappings = [
                {
                    destination_url: values.destination_url,
                    shortcode_link_id: values.shortcode_link_id,
                },
            ];
            return { error: null };
        });
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_content_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    upsert: mappingUpsert,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 44, shortcode: 'material-44' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        createAdHocShortcodeLinkMock.mockResolvedValue({
            shortcodeLink: {
                id: 44,
                createdAt: '2026-08-24T10:00:00.000Z',
                shortcode: 'material-44',
                urls: ['https://example.com/material'],
                note: null,
                landingPage: null,
                isAdHoc: true,
                sourceApp: 'online-workshop',
            },
            errorMessage: null,
        });

        const materializedLink = await materializeWorkshopMaterialShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'production-ai-2026-08-24',
                workshopKind: 'workshop',
                contentBlockId: 'content-44',
                bodyMarkdown: '[Otevřít materiál](https://example.com/material?download=1)',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: '[Otevřít materiál](https://ptbk.io/material-44)',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).toHaveBeenCalledWith(expect.anything(), {
            urls: [
                'https://example.com/material?download=1&utm_source=promptbook&utm_medium=workshop&utm_campaign=production-ai-2026-08-24&utm_content=content-44',
            ],
            note: 'Ad hoc material link for production-ai-2026-08-24',
            sourceApp: 'online-workshop',
        });
        expect(mappingUpsert).toHaveBeenCalledWith(
            {
                content_block_id: 'content-44',
                destination_url: 'https://example.com/material?download=1',
                shortcode_link_id: 44,
            },
            { onConflict: 'content_block_id,destination_url', ignoreDuplicates: true },
        );
    });

    it('reuses the persisted material short-link path for an artificial or moderator chat message', async () => {
        let mappings: readonly {
            readonly destination_url: string;
            readonly destination_title?: string;
            readonly shortcode_link_id: number;
        }[] = [];
        const mappingUpsert = vi.fn(async (values: {
            readonly destination_url: string;
            readonly destination_title?: string;
            readonly shortcode_link_id: number;
        }) => {
            mappings = [
                {
                    destination_url: values.destination_url,
                    ...(values.destination_title === undefined ? {} : { destination_title: values.destination_title }),
                    shortcode_link_id: values.shortcode_link_id,
                },
            ];
            return { error: null };
        });
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_comment_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    upsert: mappingUpsert,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 45, shortcode: 'comment-45' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        createAdHocShortcodeLinkMock.mockResolvedValue({
            shortcodeLink: {
                id: 45,
                createdAt: '2026-08-25T10:00:00.000Z',
                shortcode: 'comment-45',
                urls: ['https://example.com/guide'],
                note: null,
                landingPage: null,
                isAdHoc: true,
                sourceApp: 'online-workshop',
            },
            errorMessage: null,
        });
        fetchPublicWebPageTitleMock.mockResolvedValue('Průvodce AI agenty');

        const materializedLink = await materializeWorkshopCommentShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'production-ai-2026-08-25',
                workshopKind: 'workshop',
                commentId: 'comment-45',
                bodyMarkdown: 'Podívejte se na https://example.com/guide.',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: 'Podívejte se na [Průvodce AI agenty](https://ptbk.io/comment-45).',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).toHaveBeenCalledWith(expect.anything(), {
            urls: [
                'https://example.com/guide?utm_source=promptbook&utm_medium=workshop&utm_campaign=production-ai-2026-08-25&utm_content=comment-45',
            ],
            note: 'Ad hoc chat link for production-ai-2026-08-25',
            sourceApp: 'online-workshop',
        });
        expect(mappingUpsert).toHaveBeenCalledWith(
            {
                comment_id: 'comment-45',
                destination_url: 'https://example.com/guide',
                destination_title: 'Průvodce AI agenty',
                shortcode_link_id: 45,
            },
            { onConflict: 'comment_id,destination_url', ignoreDuplicates: true },
        );
        expect(fetchPublicWebPageTitleMock).toHaveBeenCalledWith('https://example.com/guide');
    });

    it('backfills a title for an existing community short link without making a second short link', async () => {
        let mappings: readonly {
            readonly destination_url: string;
            readonly destination_title: string | null;
            readonly shortcode_link_id: number;
        }[] = [
            {
                destination_url: 'https://example.com/community-guide',
                destination_title: null,
                shortcode_link_id: 46,
            },
        ];
        const updateDestination = vi.fn(async (_columnName: string, destination: string) => {
            mappings = mappings.map((mapping) =>
                mapping.destination_url === destination ? { ...mapping, destination_title: 'Průvodce komunitou' } : mapping,
            );
            return { error: null };
        });
        const updateOwner = vi.fn(() => ({ eq: updateDestination }));
        const mappingUpdate = vi.fn(() => ({ eq: updateOwner }));
        const from = vi.fn((tableName: string) => {
            if (tableName === 'workshop_content_shortcode_links') {
                return {
                    select: vi.fn(() => ({ eq: vi.fn(async () => ({ data: mappings, error: null })) })),
                    update: mappingUpdate,
                };
            }

            if (tableName === 'ShortcodeLink') {
                return {
                    select: vi.fn(() => ({
                        in: vi.fn(async () => ({ data: [{ id: 46, shortcode: 'community-46' }], error: null })),
                    })),
                };
            }

            throw new Error(`Unexpected table ${tableName}`);
        });
        fetchPublicWebPageTitleMock.mockResolvedValue('Průvodce komunitou');

        const materializedLink = await materializeWorkshopMaterialShortLinks(
            { from } as unknown as SupabaseClient,
            {
                workshopSlug: 'komunita',
                workshopKind: 'community',
                contentBlockId: 'content-46',
                bodyMarkdown: 'Začněte zde: https://example.com/community-guide',
            },
        );

        expect(materializedLink).toEqual({
            bodyMarkdown: 'Začněte zde: [Průvodce komunitou](https://ptbk.io/community-46)',
            errorMessage: null,
        });
        expect(createAdHocShortcodeLinkMock).not.toHaveBeenCalled();
        expect(mappingUpdate).toHaveBeenCalledWith({ destination_title: 'Průvodce komunitou' });
        expect(updateOwner).toHaveBeenCalledWith('content_block_id', 'content-46');
        expect(updateDestination).toHaveBeenCalledWith('destination_url', 'https://example.com/community-guide');
        expect(fetchPublicWebPageTitleMock).toHaveBeenCalledWith('https://example.com/community-guide');
    });
});


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content lib/workshops/workshopMaterialLinks.ts | Select-Object -First 430' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 299ms:
import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';
const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;

type WorkshopMaterialLinkRange = {
    readonly destination: string;
    readonly start: number;
    readonly end: number;
    readonly isTitleRequired: boolean;
};

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

function getWorkshopMaterialLinkBaseUrl(): string {
    return WORKSHOP_MATERIAL_LINK_BASE_URL;
}

function isEscaped(text: string, index: number): boolean {
    let precedingBackslashCount = 0;

    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
        precedingBackslashCount++;
    }

    return precedingBackslashCount % 2 === 1;
}

function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
    const precedingText = markdown.slice(0, position);
    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;

    return fenceCount % 2 === 1;
}

function isWithinInlineCode(markdown: string, position: number): boolean {
    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
    const precedingLineText = markdown.slice(lineStart, position);
    let unescapedBacktickCount = 0;

    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
            unescapedBacktickCount++;
        }
    }

    return unescapedBacktickCount % 2 === 1;
}

function isInsideCode(markdown: string, position: number): boolean {
    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
}

function findUnescapedCharacter(text: string, character: string, start: number): number {
    for (let cursor = start; cursor < text.length; cursor++) {
        if (text[cursor] === character && !isEscaped(text, cursor)) {
            return cursor;
        }
    }

    return -1;
}

function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
        if (
            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
            isEscaped(markdown, openingBracket) ||
            isInsideCode(markdown, openingBracket)
        ) {
            continue;
        }

        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
            continue;
        }

        let destinationStart = closingBracket + 2;
        while (/\s/.test(markdown[destinationStart] ?? '')) {
            destinationStart++;
        }

        if (markdown[destinationStart] === '<') {
            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
            if (closingAngleBracket === -1) {
                continue;
            }

            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
            if (destination !== '') {
                ranges.push({
                    destination,
                    start: destinationStart + 1,
                    end: closingAngleBracket,
                    isTitleRequired: false,
                });
            }
            continue;
        }

        let parenthesisDepth = 0;
        let destinationEnd = destinationStart;
        for (; destinationEnd < markdown.length; destinationEnd++) {
            const character = markdown[destinationEnd];
            if (isEscaped(markdown, destinationEnd)) {
                continue;
            }
            if (character === '(') {
                parenthesisDepth++;
                continue;
            }
            if (character === ')') {
                if (parenthesisDepth === 0) {
                    break;
                }
                parenthesisDepth--;
                continue;
            }
            if (parenthesisDepth === 0 && /\s/.test(character)) {
                break;
            }
        }

        const destination = markdown.slice(destinationStart, destinationEnd);
        if (destination !== '') {
            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
        }
    }

    return ranges;
}

function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const openingAnchorPattern = /<a\b[^>]*>/gi;
    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;

    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
        const openingAnchor = openingAnchorMatch[0];
        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
        if (isInsideCode(markdown, openingAnchorIndex)) {
            continue;
        }

        const hrefMatch = hrefPattern.exec(openingAnchor);
        if (hrefMatch === null) {
            continue;
        }

        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
        if (!destination) {
            continue;
        }

        const hrefText = hrefMatch[0];
        const destinationIndexInHref = hrefText.lastIndexOf(destination);
        if (destinationIndexInHref === -1) {
            continue;
        }

        const hrefIndexInAnchor = hrefMatch.index;
        ranges.push({
            destination,
            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;

    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
        const destination = autolinkMatch[1];
        const matchIndex = autolinkMatch.index ?? 0;
        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
            ranges.push({
                destination,
                start: matchIndex,
                end: matchIndex + autolinkMatch[0].length,
                isTitleRequired: true,
            });
        }
    }

    return ranges;
}

/**
 * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
 * bare HTTP(S) URL too. Materialization must therefore match that rendered
 * anchor as well as explicit Markdown and HTML links.
 */
function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];

    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
        const leadingWhitespace = bareUrlMatch[1] ?? '';
        const rawDestination = bareUrlMatch[2];
        const matchIndex = bareUrlMatch.index ?? 0;
        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
        const start = matchIndex + leadingWhitespace.length;

        if (destination !== '' && !isInsideCode(markdown, start)) {
            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
        }
    }

    return ranges;
}

function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const ranges: WorkshopMaterialLinkRange[] = [];
    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;

    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
        const destination = definitionMatch[1] ?? definitionMatch[2];
        if (!destination) {
            continue;
        }

        const matchIndex = definitionMatch.index ?? 0;
        if (isInsideCode(markdown, matchIndex)) {
            continue;
        }

        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
        ranges.push({
            destination,
            start: matchIndex + destinationIndexInDefinition,
            end: matchIndex + destinationIndexInDefinition + destination.length,
            isTitleRequired: false,
        });
    }

    return ranges;
}

function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
    const sortedRanges = [
        ...collectMarkdownInlineLinkRanges(markdown),
        ...collectHtmlLinkRanges(markdown),
        ...collectAutolinkRanges(markdown),
        ...collectReferenceDefinitionRanges(markdown),
        ...collectBareUrlRanges(markdown),
    ].sort(
        (firstRange, secondRange) =>
            firstRange.start - secondRange.start ||
            firstRange.end - secondRange.end ||
            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
    );

    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
    for (const range of sortedRanges) {
        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
        if (precedingRange === undefined || range.start >= precedingRange.end) {
            nonOverlappingRanges.push(range);
        }
    }

    return nonOverlappingRanges;
}

function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
        return null;
    }

    try {
        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());

        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
    } catch {
        return null;
    }
}

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

/**
 * Lists the HTTP(S) destinations written in a material, regardless of whether
 * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
 * anchors, e-mail links, and code samples are deliberately not click links.
 */
export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
}

function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
    return Array.from(
        new Set(
            collectWorkshopMaterialLinkRanges(bodyMarkdown)
                .filter((range) => range.isTitleRequired)
                .map((range) => range.destination)
                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
        ),
    );
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopWrapUpDocument.ts
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short; git diff -- lib/workshops/workshopWrapUpDocument.ts; git diff --check' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 335ms:
 M AGENTS.md
 M businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
 M businesses/online-workshop/participant/WorkshopStage.test.tsx
 M businesses/online-workshop/participant/WorkshopStage.tsx
 M businesses/online-workshop/participant/WorkshopWrapUp.tsx
 M changelog/_current-preversion.md
 M lib/exports/svgChartExport.ts
 M prompts/2026-09-0240-wrap-up-pdf.md
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
?? lib/exports/pdfPictures.test.ts
?? lib/exports/pdfPictures.ts
?? lib/exports/workshopWrapUpPdf.ts
?? lib/workshops/workshopMaterialLinkSyntax.ts
?? lib/workshops/workshopWrapUpDocument.test.ts
?? lib/workshops/workshopWrapUpDocument.ts

diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopMaterialLinks.ts
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..6a33b797fd7bc3b382783493927eac14df77c67b
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,24 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+    type WorkshopMaterialLinkRange,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +67,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
 
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
-
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +101,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..6a33b797fd7bc3b382783493927eac14df77c67b
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,24 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+    type WorkshopMaterialLinkRange,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +67,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
 
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
-
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +101,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content lib/workshops/workshopMaterialLinks.ts -TotalCount 160; rg -n "collectWorkshopMaterialLinkRanges|getTrackableWorkshopMaterialUrl|getWorkshopShortcodeLinkDestinations|getWorkshopMaterialLinkDestinations|WorkshopMaterialLinkRange" lib/workshops/workshopMaterialLinks.ts; git diff --check' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 374ms:
import { createAdHocShortcodeLink } from '@/lib/shortener/shortcodeLinkAdHoc';
import {
    createPublicShortcodeLinkUrl,
    type ShortcodeLinkSourceApp,
} from '@/lib/shortener/shortcodeLink';
import { SHORTCODE_LINK_TABLE_NAME } from '@/lib/shortener/shortcodeLinkConstants';
import { fetchPublicWebPageTitle } from '@/lib/network/publicWebPagePreview';
import {
    WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
    WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
} from '@/lib/workshops/workshopConstants';
import {
    collectWorkshopMaterialLinkRanges,
    getTrackableWorkshopMaterialUrl,
    getWorkshopShortcodeLinkDestinations,
    getWorkshopShortcodeLinkDestinationsRequiringTitle,
    type WorkshopMaterialLinkRange,
} from '@/lib/workshops/workshopMaterialLinkSyntax';
import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
import type { SupabaseClient } from '@supabase/supabase-js';

export {
    getWorkshopMaterialLinkDestinations,
    getWorkshopShortcodeLinkDestinations,
} from '@/lib/workshops/workshopMaterialLinkSyntax';

const WORKSHOP_UTM_SOURCE = 'promptbook';
const WORKSHOP_UTM_MEDIUM = 'workshop';

type WorkshopShortcodeLinkMappingRow = {
    readonly destination_url: string;
    readonly destination_title?: string | null;
    readonly shortcode_link_id: number | string;
};

/**
 * One persisted source record whose URLs are handed out as public short links.
 * Materials and eligible chat messages differ only by this durable mapping;
 * parsing, UTM values, collision handling, and URL replacement stay shared.
 */
type WorkshopShortcodeLinkOwner = {
    readonly id: string;
    readonly mappingTableName: string;
    readonly mappingOwnerColumnName: string;
    readonly note: string;
};

type ShortcodeLinkReferenceRow = {
    readonly id: number | string;
    readonly shortcode: string;
};

export type WorkshopShortcodeLinkPresentation = {
    readonly shortUrl: string;
    readonly title: string;
};

type WorkshopShortcodeLinkReplacement = string | WorkshopShortcodeLinkPresentation;

type LoadedWorkshopShortcodeLink = WorkshopShortcodeLinkPresentation & {
    readonly isTitleStored: boolean;
};

type LoadedWorkshopShortcodeLinks =
    | {
          readonly shortcodeLinkByDestination: ReadonlyMap<string, LoadedWorkshopShortcodeLink>;
          readonly errorMessage: null;
      }
    | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };

/**
 * Keeps the source address of a workshop-owned link useful to its destination's
 * analytics. The public address is later handed out as a short link; this is
 * only the destination stored behind that short link.
 */
export function createWorkshopShortcodeLinkTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    sourceRecordId: string,
): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    const parsedUrl = new URL(trackableUrl);
    parsedUrl.searchParams.set('utm_source', WORKSHOP_UTM_SOURCE);
    parsedUrl.searchParams.set('utm_medium', WORKSHOP_UTM_MEDIUM);
    parsedUrl.searchParams.set('utm_campaign', workshopSlug);
    parsedUrl.searchParams.set('utm_content', sourceRecordId);
    return parsedUrl.toString();
}

/**
 * The material-specific name remains available to callers which describe a
 * content block. Chat messages use the same tracking URL factory above.
 */
export function createWorkshopMaterialTrackingUrl(
    destinationUrl: string,
    workshopSlug: string,
    contentBlockId: string,
): string {
    return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
}

function escapeMarkdownLinkTitle(title: string): string {
    return title.replace(/[\\\[\]]/g, '\\$&');
}

function createMarkdownShortcodeLink(title: string, shortUrl: string): string {
    return `[${escapeMarkdownLinkTitle(title)}](${shortUrl})`;
}

function getWorkshopShortcodeLinkFallbackTitle(destinationUrl: string): string {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);

    return trackableUrl === null ? destinationUrl : new URL(trackableUrl).hostname;
}

function getStoredWorkshopShortcodeLinkTitle(value: string | null | undefined): string | null {
    const title = value?.trim() ?? '';

    return title === '' ? null : title;
}

async function resolveWorkshopShortcodeLinkTitle(destinationUrl: string): Promise<string> {
    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
    if (trackableUrl === null) {
        return destinationUrl;
    }

    try {
        return await fetchPublicWebPageTitle(trackableUrl);
    } catch {
        // A remote page may reject our bounded metadata request, but that must
        // never prevent a room from handing out its already-safe short link.
        return getWorkshopShortcodeLinkFallbackTitle(destinationUrl);
    }
}

/**
 * Replaces a source address with its persisted short URL. An authored Markdown,
 * HTML, or reference label remains its author's wording; raw URLs and
 * autolinks receive the fetched page title in ordinary Markdown link syntax.
 */
export function replaceWorkshopShortcodeLinkDestinations(
    bodyMarkdown: string,
    shortcodeLinkByDestination: ReadonlyMap<string, WorkshopShortcodeLinkReplacement>,
): string {
    let replacedMarkdown = bodyMarkdown;
    const ranges = collectWorkshopMaterialLinkRanges(bodyMarkdown);

    for (const range of [...ranges].reverse()) {
        const shortcodeLink = shortcodeLinkByDestination.get(range.destination);
        if (shortcodeLink !== undefined) {
            const replacement =
                typeof shortcodeLink === 'string'
                    ? shortcodeLink
                    : range.isTitleRequired
                      ? createMarkdownShortcodeLink(shortcodeLink.title, shortcodeLink.shortUrl)
13:    collectWorkshopMaterialLinkRanges,
14:    getTrackableWorkshopMaterialUrl,
15:    getWorkshopShortcodeLinkDestinations,
16:    getWorkshopShortcodeLinkDestinationsRequiringTitle,
17:    type WorkshopMaterialLinkRange,
23:    getWorkshopMaterialLinkDestinations,
24:    getWorkshopShortcodeLinkDestinations,
81:    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
115:    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
127:    const trackableUrl = getTrackableWorkshopMaterialUrl(destinationUrl);
151:    const ranges = collectWorkshopMaterialLinkRanges(bodyMarkdown);
280:    const destinations = getWorkshopShortcodeLinkDestinations(context.bodyMarkdown);
284:    const destinationsRequiringTitle = new Set(getWorkshopShortcodeLinkDestinationsRequiringTitle(context.bodyMarkdown));

diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..6a33b797fd7bc3b382783493927eac14df77c67b
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,24 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+    type WorkshopMaterialLinkRange,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +67,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
 
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
-
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +101,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\workshops\workshopMaterialLinks.ts
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test -- businesses/online-workshop/participant/WorkshopStage.test.tsx businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx lib/workshops/workshopWrapUpDocument.test.ts lib/workshops/workshopMaterialLinks.test.ts lib/exports/pdfPictures.test.ts lib/exports/svgChartExport.test.ts' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 6475ms:

> promptbook-landing-page@0.1.0 test
> vitest run businesses/online-workshop/participant/WorkshopStage.test.tsx businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx lib/workshops/workshopWrapUpDocument.test.ts lib/workshops/workshopMaterialLinks.test.ts lib/exports/pdfPictures.test.ts lib/exports/svgChartExport.test.ts


 RUN  v3.2.4 C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron

 ✓ lib/exports/svgChartExport.test.ts (4 tests) 10ms
 ✓ lib/workshops/workshopMaterialLinks.test.ts (9 tests) 23ms
 ✓ lib/workshops/workshopWrapUpDocument.test.ts (4 tests) 11ms
 ✓ lib/exports/pdfPictures.test.ts (2 tests) 8ms
 ✓ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx (2 tests) 143ms
 ✓ businesses/online-workshop/participant/WorkshopStage.test.tsx (14 tests) 421ms

 Test Files  6 passed (6)
      Tests  35 passed (35)
   Start at  09:46:30
   Duration  4.85s (transform 1.11s, setup 0ms, collect 3.28s, tests 616ms, environment 2.87s, prepare 1.43s)


diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run build' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

 succeeded in 75980ms:

> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
 ✓ Compiled successfully
   Skipping validation of types
   Skipping linting
   Collecting page data ...
   Generating static pages (0/78) ...
BenefitsSection rendered
IntegrationsSection rendered
BenefitsSection rendered
   Generating static pages (19/78) 
BenefitsSection rendered
IntegrationsSection rendered
   Generating static pages (38/78) 
BenefitsSection rendered
IntegrationsSection rendered
BenefitsSection rendered
IntegrationsSection rendered
   Generating static pages (58/78) 
 ✓ Generating static pages (78/78)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                                  Size  First Load JS  Revalidate  Expire
┌ ƒ /                                                                                       377 B         102 kB
├ ○ /_not-found                                                                             377 B         102 kB
├ ƒ /[shortcode]                                                                          2.31 kB         142 kB
├ ƒ /admin                                                                                  175 B         105 kB
├ ƒ /admin/community                                                                      7.73 kB         325 kB
├ ƒ /admin/contacts                                                                         37 kB         196 kB
├ ƒ /admin/discount-codes                                                                 9.86 kB         119 kB
├ ƒ /admin/login                                                                            377 B         102 kB
├ ƒ /admin/shortener                                                                      64.6 kB         174 kB
├ ƒ /admin/workshops                                                                        215 B         318 kB
├ ○ /ai-supervize                                                                           16 kB         239 kB
├ ƒ /ai-supervize-mini                                                                    17.1 kB         231 kB
├ ○ /ai-supervize-mini/opengraph-image                                                      377 B         102 kB
├ ƒ /ai-supervize-mini/participant                                                        2.51 kB         139 kB
├ ○ /ai-supervize/opengraph-image                                                           377 B         102 kB
├ ○ /ai-ta-krajta                                                                         18.9 kB         146 kB          1h      1y
├ ○ /ai-ta-krajta/branding                                                                  204 B         121 kB
├ ○ /ai-ta-krajta/logo.png                                                                  377 B         102 kB
├ ○ /ai-ta-krajta/logo.svg                                                                  377 B         102 kB
├ ○ /ai-ta-krajta/manifest.webmanifest                                                      377 B         102 kB
├ ○ /ai-ta-krajta/media-kit                                                               3.63 kB         125 kB
├ ○ /ai-ta-krajta/opengraph-image                                                           377 B         102 kB
├ ƒ /api/admin/community/memberships                                                        377 B         102 kB
├ ƒ /api/admin/community/projects                                                           377 B         102 kB
├ ƒ /api/admin/community/projects/[projectId]                                               377 B         102 kB
├ ƒ /api/admin/discount-codes                                                               377 B         102 kB
├ ƒ /api/admin/discount-codes/[discountCodeId]                                              377 B         102 kB
├ ƒ /api/admin/session                                                                      377 B         102 kB
├ ƒ /api/admin/session/sign-out                                                             377 B         102 kB
├ ƒ /api/admin/shortener                                                                    377 B         102 kB
├ ƒ /api/admin/shortener/[shortcodeLinkId]                                                  377 B         102 kB
├ ƒ /api/admin/shortener/[shortcodeLinkId]/clicks                                           377 B         102 kB
├ ƒ /api/admin/workshops                                                                    377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]                                                       377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/analytics                                             377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/artificial-reactions                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments                                              377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments/[commentId]                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments/[commentId]/artificial-upvotes               377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/content                                               377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/content/[contentId]                                   377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/exports/[exportKind]                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/feedback                                              377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants                                          377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants/[participantId]                          377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants/[participantId]/timeline                 377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls                                                 377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls/[pollId]                                        377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls/[pollId]/options/[optionId]/artificial-votes    377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/reactions                                             377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/stage-comment                                         377 B         102 kB
├ ƒ /api/ai-supervize-mini/registration                                                     377 B         102 kB
├ ƒ /api/ai-ta-krajta/episodes/search                                                       377 B         102 kB
├ ƒ /api/community/membership/registration                                                  377 B         102 kB
├ ƒ /api/contacts                                                                           377 B         102 kB
├ ƒ /api/contacts/export/[formatId]                                                         377 B         102 kB
├ ƒ /api/discount-codes/validate                                                            377 B         102 kB
├ ƒ /api/stripe/webhook                                                                     377 B         102 kB
├ ƒ /api/track-click                                                                        377 B         102 kB
├ ƒ /api/waitlist                                                                           377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments/[commentId]                                      377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments/[commentId]/upvotes                              377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/connect                                                   377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/feedback                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership                                                377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/cancellation                                   377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/checkout                                       377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/checkout/confirmation                          377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/portal                                         377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/participant                                               377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/participants/[participantId]                              377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/polls/[pollId]/votes                                      377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/presence                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/reactions                                                 377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/repository                                                377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/state                                                     377 B         102 kB
├ ƒ /api/workshops/komunita/projects                                                        377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]                                            377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]/connect                                    377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]/vote                                       377 B         102 kB
├ ƒ /api/workshops/komunita/projects/preview                                                377 B         102 kB
├ ○ /branding                                                                             4.75 kB         141 kB
├ ○ /contact                                                                              2.54 kB         163 kB
├ ○ /cs                                                                                     169 B         239 kB
├ ƒ /cs/komunita                                                                            232 B         300 kB
├ ƒ /cs/komunita/calendar.ics                                                               377 B         102 kB
├ ƒ /cs/komunita/clenstvi                                                                 13.2 kB         154 kB
├ ○ /cs/komunita/clenstvi/opengraph-image                                                   377 B         102 kB
├ ○ /cs/komunita/opengraph-image                                                            377 B         102 kB
├ ƒ /cs/komunita/projects                                                                 2.84 kB         139 kB
├ ƒ /cs/komunita/projects/[projectId]                                                     3.02 kB         297 kB
├ ○ /cs/obchodni-podminky                                                                 2.52 kB         139 kB
├ ○ /cs/ochrana-osobnich-udaju                                                            2.52 kB         139 kB
├ ƒ /cs/online-workshop                                                                   13.1 kB         244 kB
├ ƒ /cs/online-workshop/dekujeme                                                          4.01 kB         152 kB
├ ○ /cs/online-workshop/opengraph-image                                                     377 B         102 kB
├ ƒ /cs/online-workshop/participant                                                       2.73 kB         297 kB
├ ○ /cs/online-workshop/participant/opengraph-image                                         377 B         102 kB
├ ○ /cs/opengraph-image                                                                     377 B         102 kB
├ ○ /cs/pavol                                                                               158 B         203 kB
├ ○ /cs/pavol/opengraph-image                                                               377 B         102 kB
├ ○ /data-deletion                                                                        2.51 kB         139 kB
├ ○ /dekujeme                                                                             3.71 kB         151 kB
├ ○ /en                                                                                     169 B         239 kB
├ ○ /en/opengraph-image                                                                     377 B         102 kB
├ ○ /en/pavol                                                                               158 B         203 kB
├ ○ /en/pavol/opengraph-image                                                               377 B         102 kB
├ ○ /en/privacy-policy                                                                    2.52 kB         139 kB
├ ○ /en/terms-and-conditions                                                              2.52 kB         139 kB
├ ○ /for-agro                                                                             7.31 kB         268 kB
├ ○ /for-agro/opengraph-image                                                               377 B         102 kB
├ ○ /for-industry                                                                         7.92 kB         269 kB
├ ○ /for-industry/opengraph-image                                                           377 B         102 kB
├ ○ /hackathon-factory                                                                      15 kB         252 kB
├ ○ /hackathon-factory/opengraph-image                                                      377 B         102 kB
├ ƒ /k/[...shortFileUrlParts]                                                               377 B         102 kB
├ ○ /manifest.webmanifest                                                                   377 B         102 kB
├ ○ /old                                                                                  20.5 kB         245 kB
├ ○ /opengraph-image                                                                        377 B         102 kB
├ ƒ /pavol                                                                                  377 B         102 kB
├ ƒ /privacy                                                                                377 B         102 kB
├ ƒ /pro-firmy                                                                              377 B         102 kB
├ ○ /pro-mesta                                                                            6.39 kB         267 kB
├ ○ /pro-mesta/opengraph-image                                                              377 B         102 kB
├ ○ /robots.txt                                                                             377 B         102 kB
├ ○ /shortener                                                                              377 B         102 kB
├ ○ /sitemap.xml                                                                            377 B         102 kB
├ ƒ /skoleni                                                                                377 B         102 kB
├ ƒ /terms                                                                                  377 B         102 kB
└ ○ /test/hopko                                                                           4.62 kB         106 kB
+ First Load JS shared by all                                                              102 kB
  ├ chunks/1684-6c8ee981b3c4cfc9.js                                                       45.8 kB
  ├ chunks/4bd1b696-56d776b9b579ad04.js                                                   53.3 kB
  └ other shared chunks (total)                                                           2.53 kB


ƒ Middleware                                                                              30.9 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand


diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

codex
The browser/server boundary is now clean: the PDF uses a client-safe shared material-link parser, while the server-side short-link workflow reuses that same parser instead of maintaining a duplicate. The production build now succeeds; I’m completing the compiler check and reviewing the final diff.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx tsc' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 13314ms:
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short; git diff --stat; git diff --check; git diff -- lib/workshops/workshopMaterialLinks.ts; git diff -- businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx businesses/online-workshop/participant/WorkshopStage.tsx businesses/online-workshop/participant/WorkshopWrapUp.tsx changelog/_current-preversion.md' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 561ms:
 M AGENTS.md
 M businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
 M businesses/online-workshop/participant/WorkshopStage.test.tsx
 M businesses/online-workshop/participant/WorkshopStage.tsx
 M businesses/online-workshop/participant/WorkshopWrapUp.tsx
 M changelog/_current-preversion.md
 M lib/exports/svgChartExport.ts
 M lib/workshops/workshopMaterialLinks.ts
 M prompts/2026-09-0240-wrap-up-pdf.md
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
?? lib/exports/pdfPictures.test.ts
?? lib/exports/pdfPictures.ts
?? lib/exports/workshopWrapUpPdf.ts
?? lib/workshops/workshopMaterialLinkSyntax.ts
?? lib/workshops/workshopWrapUpDocument.test.ts
?? lib/workshops/workshopWrapUpDocument.ts
 AGENTS.md                                                                |   5 +-
 businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx |   1 +
 businesses/online-workshop/participant/WorkshopStage.test.tsx            |   1 +
 businesses/online-workshop/participant/WorkshopStage.tsx                 |   4 +
 businesses/online-workshop/participant/WorkshopWrapUp.tsx                |  11 ++
 changelog/_current-preversion.md                                         |   6 +
 lib/exports/svgChartExport.ts                                            | 118 ++-------------
 lib/workshops/workshopMaterialLinks.ts                                   | 324 ++--------------------------------------
 prompts/2026-09-0240-wrap-up-pdf.md                                      |   2 +-
 9 files changed, 49 insertions(+), 423 deletions(-)
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa85..ed2b071 100644
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,23 +9,22 @@ import {
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
-
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
 
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
@@ -68,277 +67,6 @@ type LoadedWorkshopShortcodeLinks =
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
 
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
-
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -374,36 +102,6 @@ export function createWorkshopMaterialTrackingUrl(
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
 }
 
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
 function escapeMarkdownLinkTitle(title: string): string {
     return title.replace(/[\\\[\]]/g, '\\$&');
 }
diff --git a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
index e017054..3df3571 100644
--- a/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
+++ b/businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
@@ -350,6 +350,7 @@ export function OnlineWorkshopParticipantPage({
                             repository={state.workshop.repository}
                             subscribeToRepositoryCommits={subscribeToRepositoryCommits}
                             feedback={state.feedback}
+                            contentBlocks={state.contentBlocks}
                             followUpContentBlock={followUpContentBlock}
                             stageComment={state.stageComment}
                             paidMembersOnlyVideo={state.paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopStage.tsx b/businesses/online-workshop/participant/WorkshopStage.tsx
index 1b88e83..eca1968 100644
--- a/businesses/online-workshop/participant/WorkshopStage.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.tsx
@@ -49,6 +49,7 @@ type WorkshopStageProps = {
      * in isolation while it is still responsible for choosing the correct temporal phase.
      */
     readonly feedback?: WorkshopFeedback | null;
+    readonly contentBlocks?: readonly WorkshopContentBlock[];
     readonly followUpContentBlock?: WorkshopContentBlock | null;
     readonly stageComment?: WorkshopCommentReference | null;
 
@@ -90,6 +91,7 @@ export function WorkshopStage({
     repository = null,
     subscribeToRepositoryCommits,
     feedback = null,
+    contentBlocks = [],
     followUpContentBlock = null,
     stageComment = null,
     paidMembersOnlyVideo = null,
@@ -189,6 +191,8 @@ export function WorkshopStage({
                 </div>
             ) : isWorkshopPast ? (
                 <WorkshopWrapUp
+                    workshop={workshop}
+                    contentBlocks={contentBlocks}
                     feedback={feedback}
                     followUpContentBlock={followUpContentBlock}
                     paidMembersOnlyVideo={paidMembersOnlyVideo}
diff --git a/businesses/online-workshop/participant/WorkshopWrapUp.tsx b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
index 8ba938f..dec8363 100644
--- a/businesses/online-workshop/participant/WorkshopWrapUp.tsx
+++ b/businesses/online-workshop/participant/WorkshopWrapUp.tsx
@@ -2,15 +2,22 @@
 
 import { WorkshopFeedback } from '@/businesses/online-workshop/participant/WorkshopFeedback';
 import { WorkshopPaidMembersVideoNotice } from '@/businesses/online-workshop/participant/WorkshopPaidMembersVideoNotice';
+import { WorkshopWrapUpPdfDownload } from '@/businesses/online-workshop/participant/WorkshopWrapUpPdfDownload';
 import type { WorkshopFeedbackValues } from '@/businesses/online-workshop/participant/workshopParticipantApi';
 import type {
     WorkshopContentBlock,
+    WorkshopDetails,
     WorkshopFeedback as WorkshopFeedbackValue,
     WorkshopPaidMembersVideo,
 } from '@/lib/workshops/workshopTypes';
 import { ArrowDown, BookOpenText, PartyPopper, Play } from 'lucide-react';
 
 type WorkshopWrapUpProps = {
+    readonly workshop: Pick<
+        WorkshopDetails,
+        'slug' | 'title' | 'description' | 'startsAt' | 'endsAt' | 'presentationUrl'
+    >;
+    readonly contentBlocks: readonly WorkshopContentBlock[];
     readonly feedback: WorkshopFeedbackValue | null;
     readonly followUpContentBlock: WorkshopContentBlock | null;
     readonly onSaveFeedback: (values: WorkshopFeedbackValues) => Promise<boolean>;
@@ -35,6 +42,8 @@ type WorkshopWrapUpProps = {
  * directly to that same record so the stage does not invent a second material model or a second tracking path.
  */
 export function WorkshopWrapUp({
+    workshop,
+    contentBlocks,
     feedback,
     followUpContentBlock,
     paidMembersOnlyVideo = null,
@@ -72,6 +81,8 @@ export function WorkshopWrapUp({
                     </div>
                 )}
 
+                <WorkshopWrapUpPdfDownload workshop={workshop} contentBlocks={contentBlocks} />
+
                 <div className="mt-6">
                     <WorkshopFeedback feedback={feedback} onSave={onSaveFeedback} />
                 </div>
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 1b75dcf..9881072 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,5 +1,11 @@
 # Current preversion
 
+- After an online workshop has its recorded end, `/cs/online-workshop/participant?workshop=` now offers its attendee a
+  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
+  authored in material titles, headings and lists, and material text/links the room already unlocked for that person;
+  the public presentation is included too. The PDF is created locally only when requested, so it adds no admin setting,
+  database record, or new source of material access.
+
 - Added a configurable start offset in seconds to the stage settings of `/admin/workshops`. After an online workshop
   ends, `/cs/online-workshop/participant?workshop=` now opens the paid member replay at that position, so the same
   uploaded stream can skip its empty waiting room without a second trimmed upload. The countdown and live stream are

diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

apply patch
patch: completed
C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\businesses\online-workshop\participant\WorkshopStage.test.tsx
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

codex
I also added a regression assertion that the download is absent while the workshop is still live. The end-state path, attendee-safe materials, PDF construction, and the existing server-side material-link workflow now share one parser; I’m running the full unit suite after that refactor.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test' in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

 succeeded in 71821ms:

> promptbook-landing-page@0.1.0 test
> vitest run


 RUN  v3.2.4 C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron

 ✓ businesses/workshop-admin/WorkshopPollAdmin.test.tsx (8 tests) 1260ms
   ✓ community poll administration > sends a trimmed question, choices, and default settings through the shared admin callback  312ms
   ✓ community poll administration > edits question and options, then deletes a poll only after confirmation  337ms
 ✓ components/workshops/WorkshopLinksPanel.test.tsx (9 tests) 1656ms
   ✓ workshop links panel > opens on the calendar of the month a member is in  482ms
   ✓ workshop links panel > colours every day of the calendar by where the term held on it stands in time  309ms
   ✓ workshop links panel > narrows the listed terms to the chosen day and back to the whole month  305ms
 ✓ components/shortener/ShortcodeLinkAdmin.test.tsx (3 tests) 1649ms
   ✓ shortcode link administration > filters and sorts links by their immutable creation provenance  946ms
   ✓ shortcode link administration > opens one click history and saves the history and list filters into the URL  597ms
 ✓ businesses/community/membership/CommunityMembershipModal.test.tsx (15 tests) 3335ms
   ✓ community membership modal > opens the in-room offer for its monthly price from the free membership badge  486ms
   ✓ community membership modal > asks for no card at all when a code covers the whole membership for as long as it lasts  493ms
   ✓ community membership modal > keeps asking for a card when a code covers the membership for its first months only  481ms
   ✓ community membership modal > names no test card for a membership which never reaches the gate  434ms
   ✓ community membership modal > hands over the membership a voucher covers without sending the member anywhere  475ms
 ✓ businesses/online-workshop/participant/WorkshopChat.test.tsx (14 tests) 1347ms
 ✓ businesses/workshop-admin/WorkshopCommentModeration.test.tsx (7 tests) 1084ms
   ✓ workshop comment moderation > saves the corrected text of a message which is already in the chat  400ms
 ✓ businesses/workshop-admin/WorkshopSettingsForm.test.tsx (21 tests) 2247ms
   ✓ workshop settings form > offers every quick end choice for a running workshop whose end is still open  359ms
   ✓ workshop settings form > offers to reopen a workshop with a defined end and does not end an upcoming workshop right now  340ms
 ✓ businesses/workshop-admin/WorkshopSelectorCardList.test.tsx (9 tests) 669ms
   ✓ workshop selector card list > leads with the running workshop and prepared terms, while keeping the archive available on demand  329ms
 ✓ businesses/online-workshop/participant/WorkshopParticipantBadge.test.tsx (12 tests) 718ms
 ✓ businesses/online-workshop/participant/OnlineWorkshopSelectedTermRoom.test.tsx (5 tests) 663ms
   ✓ online workshop waiting room > offers every workshop which runs now or is still ahead, and keeps the finished ones one click away  372ms
 ✓ businesses/community/membership/CommunityMembershipPage.test.tsx (2 tests) 572ms
   ✓ community membership page > presents free live webinars and one 199 Kč monthly paid option  488ms
 ✓ businesses/online-workshop/participant/OnlineWorkshopParticipantPage.test.tsx (26 tests) 9121ms
   ✓ online workshop participant room > gathers a workshop occurrence around its stage, its reactions, and its watching count  412ms
   ✓ online workshop participant room > shows the workshop presentation as a shared material even when no ordinary material has unlocked  2342ms
   ✓ online workshop participant room > keeps the presentation material available to a paying participant  541ms
   ✓ online workshop participant room > keeps the community material available to a paying member as well  323ms
   ✓ online workshop participant room > invites a member who does not pay into the community before the other materials of the room  487ms
   ✓ online workshop participant room > leaves the community at the end of the materials of a paying member  556ms
   ✓ online workshop participant room > says in a workshop occurrence which membership its member has and offers the paid one there  572ms
   ✓ online workshop participant room > lets a paying member manage the very same membership from a workshop occurrence  510ms
   ✓ online workshop participant room > names the paid materials and opens the membership offer for a member who has not paid  451ms
   ✓ online workshop participant room > teases the withheld recording of an ended workshop and opens the membership which unlocks it  465ms
   ✓ online workshop participant room > shows no paid-materials notice to a member whose membership already unlocked them, nor in a room with none  751ms
 ✓ businesses/workshop-admin/WorkshopOverviewChart.test.tsx (7 tests) 740ms
   ✓ WorkshopOverviewChart > draws one line for every switched on series, in the colour that series always has  373ms
 ✓ businesses/community/membership/CommunityMembershipBadge.test.tsx (5 tests) 624ms
   ✓ community membership badge > identifies the free tier and opens the offer in a modal in the very same room  422ms
 ✓ businesses/ai-supervize-mini/AiSupervizeMiniRegistrationForm.test.tsx (3 tests) 522ms
 ✓ businesses/online-workshop/participant/WorkshopPolls.test.tsx (4 tests) 376ms
 ✓ components/url-shortener.test.tsx (4 tests) 736ms
   ✓ url shortener result > shows the shortened link itself instead of the destination it leads to  411ms
 ✓ businesses/online-workshop/participant/WorkshopContent.test.tsx (17 tests) 874ms
 ✓ businesses/online-workshop/participant/WorkshopFeedback.test.tsx (4 tests) 486ms
 ✓ components/admin/DiscountCodeForm.test.tsx (1 test) 367ms
   ✓ discount-code form > defaults subscription discounts to permanent and saves the selected temporary month count  366ms
 ✓ businesses/online-workshop/participant/WorkshopReactions.test.tsx (2 tests) 311ms
 ✓ businesses/community/membership/CommunityMembershipRegistrationForm.test.tsx (2 tests) 344ms
 ✓ businesses/workshop-admin/WorkshopParticipantTimeline.test.tsx (2 tests) 375ms
   ✓ workshop participant timeline > keeps a live workshop attendance interval visible and shows the recorded actions  314ms
 ✓ businesses/online-workshop/participant/WorkshopStage.test.tsx (14 tests) 1063ms
 ✓ businesses/workshop-admin/WorkshopArtificialComment.test.tsx (2 tests) 236ms
 ✓ businesses/online-workshop/participant/useWorkshopParticipant.test.tsx (5 tests) 397ms
 ✓ businesses/workshop-admin/WorkshopOverviewChartRangeControls.test.tsx (3 tests) 494ms
   ✓ workshop overview chart range controls > offers labelled zoom and move buttons that change the graph range  338ms
 ✓ businesses/online-workshop/OnlineWorkshopRegistrationForm.test.tsx (3 tests) 574ms
   ✓ Online workshop registration form > uses one form to choose between terms without clearing contact details  519ms
 ✓ businesses/workshop-admin/CreateWorkshopForm.test.tsx (4 tests) 582ms
   ✓ create workshop form > opens a selected workshop with its publication state and creates it through the normal callback  389ms
 ✓ businesses/online-workshop/participant/WorkshopCalendarInvitation.test.tsx (3 tests) 321ms
 ✓ businesses/ai-ta-krajta/AiTaKrajtaEpisodeCard.test.tsx (6 tests) 341ms
 ✓ businesses/workshop-admin/WorkshopEventLinks.test.tsx (5 tests) 250ms
 ✓ businesses/community/membership/CommunityMembershipActivitySection.test.tsx (4 tests) 324ms
 ✓ businesses/workshop-admin/WorkshopRegistrationContacts.test.tsx (2 tests) 317ms
   ✓ workshop registration contacts > opens and exports the same term-filtered contacts view  313ms
 ✓ components/footer.test.tsx (1 test) 227ms
 ✓ components/workshop-comment-markdown.test.tsx (3 tests) 184ms
 ✓ businesses/ai-ta-krajta/AiTaKrajtaPeopleSection.test.tsx (1 test) 185ms
 ✓ businesses/online-workshop/participant/WorkshopWatchingBadge.test.tsx (2 tests) 95ms
 ✓ businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx (2 tests) 291ms
 ✓ businesses/community/membership/CommunityMembershipWebinarArchive.test.tsx (3 tests) 101ms
 ✓ hooks/useUrlSynchronizedViewState.test.tsx (1 test) 49ms
 ✓ components/workshops/WorkshopReactionStream.test.tsx (5 tests) 111ms
 ✓ businesses/community/membership/CommunityMembershipLivePreview.test.tsx (3 tests) 121ms
 ✓ businesses/ai-ta-krajta/useAiTaKrajtaOrderedPeople.test.tsx (3 tests) 60ms
 ✓ hooks/useSynchronizedHorizontalScroll.test.tsx (1 test) 51ms
 ✓ lib/youtube/youtubePlayerCommands.test.ts (5 tests) 15ms
 ✓ businesses/online-workshop/participant/workshopParticipantStateCache.test.ts (7 tests) 15ms
 ✓ businesses/community/CommunityParticipantPage.test.tsx (3 tests) 64ms
 ✓ hooks/usePodcastPlaybackProgress.test.tsx (4 tests) 36ms
 ✓ lib/database/postgresCli.test.ts (1 test) 216ms
 ✓ lib/database/backupDatabase.test.ts (12 tests) 172ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaMarkArtwork.test.ts (3 tests) 192ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaPeople.test.ts (52 tests) 1603ms
 ✓ lib/calendar/calendarMonth.test.ts (7 tests) 41ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaIcon.test.ts (5 tests) 43ms
 ✓ lib/workshops/workshopReactionAnimationStyle.test.ts (7 tests) 66ms
 ✓ lib/podcast/podcastPlaybackProgressStorage.test.ts (5 tests) 12ms
 ✓ lib/events/eventListing.test.ts (6 tests) 40ms
stderr | app/api/contacts/export/[formatId]/route.test.ts > the export of the contacts served in a new tab > says that the contacts cannot be reached without the service role key
⚠️ The contacts cannot be reached, set SUPABASE_SERVICE_ROLE_KEY - the "Contact" table is closed by row level security

 ✓ app/api/contacts/export/[formatId]/route.test.ts (12 tests) 60ms
stderr | lib/contacts/contacts.test.ts > sortContacts > keeps a contact whose date cannot be read at the end in both directions
Deprecation warning: value provided is not in a recognized RFC2822 or ISO format. moment construction falls back to js Date(), which is not reliable across all browsers and versions. Non RFC2822/ISO date formats are discouraged. Please refer to http://momentjs.com/guides/#/warnings/js-date/ for more info.
Arguments: 
[0] _isAMomentObject: true, _isUTC: false, _useUTC: false, _l: undefined, _i: nobody knows when, _f: undefined, _strict: undefined, _locale: [object Object]
Error: 
    at Function.createFromInputFallback (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:324:25)
    at configFromString (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:2613:19)
    at configFromInput (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:3056:13)
    at prepareConfig (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:3039:13)
    at createFromConfig (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:3006:44)
    at createLocalOrUTC (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:3100:16)
    at createLocal (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:3104:16)
    at hooks (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\node_modules\moment\moment.js:16:29)
    at parseContactDate (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\contacts\contactDates.ts:57:25)
    at getContactSortValue (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\contacts\contactValues.ts:100:16)

 ✓ lib/contacts/contacts.test.ts (68 tests) 98ms
 ✓ app/cs/komunita/calendar.ics/route.test.ts (4 tests) 42ms
stderr | app/api/ai-supervize-mini/registration/route.test.ts > AI Supervize Mini registration endpoint > does not store a registration when its submitted discount cannot be verified
Failed to validate AI Supervize Mini registration discount code: Database not configured

 ✓ app/api/ai-supervize-mini/registration/route.test.ts (5 tests) 34ms
stderr | app/api/admin/shortener/[shortcodeLinkId]/route.test.ts > one admin shortcode link > reports a shortcode taken by another link as a conflict
⚠️ The database refused the editing of a short link: duplicate key value
   Error code: 23505

stderr | app/api/admin/shortener/[shortcodeLinkId]/route.test.ts > one admin shortcode link > says that a short link still referenced elsewhere could not be removed
⚠️ The database refused the deletion of a short link: violates foreign key constraint
   Error code: 23503
 ✓ app/api/admin/shortener/[shortcodeLinkId]/route.test.ts (10 tests) 44ms

 ✓ businesses/online-workshop/participant/workshopWatchingCountLabel.test.ts (4 tests) 30ms
stderr | app/api/waitlist/route.test.ts > gathering a contact from a public form > says that the contact was not saved when the service role key is missing
⚠️ The contacts cannot be reached, set SUPABASE_SERVICE_ROLE_KEY - the "Contact" table is closed by row level security

 ✓ app/api/waitlist/route.test.ts (12 tests) 38ms
stderr | app/api/admin/shortener/route.test.ts > admin shortcode link collection > reports an unavailable service-role database instead of falling back to the public client
⚠️ The short links cannot be written, set SUPABASE_SERVICE_ROLE_KEY - only the service role may write the "ShortcodeLink" table

stderr | app/api/admin/shortener/route.test.ts > admin shortcode link collection > allows an administrator to choose another preview when the shortcode already exists
⚠️ The database refused the creation of a short link: duplicate key value
   Error code: 23505

 ✓ app/api/admin/shortener/route.test.ts (7 tests) 37ms
 ✓ lib/database/migrateDatabase.test.ts (5 tests) 34ms
stderr | lib/network/fetchCachedText.test.ts > fetchCachedText > gives the caller nothing when a publisher answers with an error
https://publisher.example/feed.xml answered with the status 503

stderr | lib/network/fetchCachedText.test.ts > fetchCachedText > gives the caller nothing when a publisher cannot be reached at all
https://publisher.example/feed.xml could not be read Error: ECONNREFUSED
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.test.ts:82:19
    at mockCall (file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/spy/dist/index.js:96:15)
    at spy (file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/tinyspy/dist/index.js:47:80)
    at fetchCachedText (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.ts:49:32)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.test.ts:85:22
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:155:11
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:752:26
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:1897:20
    at new Promise (<anonymous>)
    at runWithTimeout (file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:1863:10)

stderr | lib/network/fetchCachedText.test.ts > fetchCachedText > gives the caller nothing when the bound cancels a publisher which stayed silent
https://publisher.example/feed.xml could not be read DOMException [TimeoutError]: The operation was aborted due to timeout
    at new DOMException (node:internal/per_context/domexception:76:18)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.test.ts:90:19
    at mockCall (file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/spy/dist/index.js:96:15)
    at spy (file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/tinyspy/dist/index.js:47:80)
    at fetchCachedText (C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.ts:49:32)
    at C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron\lib\network\fetchCachedText.test.ts:93:22
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:155:11
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:752:26
    at file:///C:/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/node_modules/@vitest/runner/dist/chunk-hooks.js:1897:20
    at new Promise (<anonymous>)

 ✓ lib/network/fetchCachedText.test.ts (7 tests) 26ms
 ✓ lib/workshops/workshopSchemas.test.ts (30 tests) 47ms
 ✓ lib/workshops/workshopReactionAnimations.test.ts (13 tests) 29ms
 ✓ lib/github/githubCommitFeed.test.ts (3 tests) 27ms
 ✓ lib/workshops/fetchWorkshopRepositoryProgress.test.ts (2 tests) 27ms
 ✓ app/api/admin/workshops/route.test.ts (2 tests) 26ms
 ✓ app/api/workshops/[workshopSlug]/membership/portal/route.test.ts (6 tests) 24ms
stderr | app/api/contacts/route.test.ts > contact mutations > says that the contacts cannot be reached when updating without the service role key
⚠️ The contacts cannot be reached, set SUPABASE_SERVICE_ROLE_KEY - the "Contact" table is closed by row level security

stderr | app/api/contacts/route.test.ts > contact mutations > says that the contacts cannot be reached when deleting without the service role key
⚠️ The contacts cannot be reached, set SUPABASE_SERVICE_ROLE_KEY - the "Contact" table is closed by row level security

 ✓ app/api/contacts/route.test.ts (9 tests) 33ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaSnakeSimulation.test.ts (9 tests) 31ms
 ✓ app/api/admin/session/route.test.ts (7 tests) 27ms
 ✓ lib/api/api.test.ts (16 tests) 24ms
 ✓ app/api/community/membership/registration/route.test.ts (7 tests) 27ms
 ✓ lib/workshops/workshopMigration.test.ts (48 tests) 32ms
 ✓ lib/github/githubCommitApi.test.ts (3 tests) 26ms
 ✓ app/api/workshops/[workshopSlug]/polls/[pollId]/votes/route.test.ts (3 tests) 22ms
 ✓ lib/workshops/workshopRegistrations.test.ts (14 tests) 11ms
 ✓ lib/workshops/workshopMaterialLinks.test.ts (9 tests) 27ms
 ✓ app/api/admin/workshops/[workshopId]/route.test.ts (4 tests) 25ms
 ✓ app/api/workshops/[workshopSlug]/membership/cancellation/route.test.ts (6 tests) 24ms
 ✓ lib/workshops/workshopRepositoryProgress.test.ts (3 tests) 22ms
 ✓ app/api/admin/shortener/[shortcodeLinkId]/clicks/route.test.ts (3 tests) 22ms
 ✓ app/api/admin/workshops/[workshopId]/stage-comment/route.test.ts (3 tests) 24ms
 ✓ lib/admin/adminContactJoin.test.ts (4 tests) 20ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaEpisodePeople.test.ts (16 tests) 17ms
 ✓ app/api/admin/workshops/[workshopId]/participants/route.test.ts (3 tests) 20ms
 ✓ lib/admin/adminApiGuard.test.ts (5 tests) 22ms
 ✓ app/api/admin/workshops/[workshopId]/feedback/route.test.ts (2 tests) 26ms
 ✓ lib/community-projects/communityProjectService.test.ts (10 tests) 19ms
 ✓ app/api/ai-ta-krajta/episodes/search/route.test.ts (2 tests) 16ms
 ✓ app/api/admin/community/memberships/route.test.ts (2 tests) 18ms
 ✓ lib/discounts/discountCodeSchema.test.ts (5 tests) 17ms
 ✓ lib/workshops/workshopRepositoryMonitor.test.ts (2 tests) 17ms
 ✓ lib/workshops/workshopOverviewSeriesPoints.test.ts (16 tests) 15ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaSnakeBody.test.ts (5 tests) 13ms
 ✓ lib/github/githubRepository.test.ts (11 tests) 16ms
 ✓ lib/admin/adminContactDatabase.test.ts (1 test) 14ms
 ✓ lib/community-membership/communityMembershipCheckout.test.ts (6 tests) 17ms
 ✓ lib/workshops/workshopPollSharing.test.ts (5 tests) 15ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaPeopleOrder.test.ts (7 tests) 14ms
 ✓ lib/calendar/create-calendar-links.test.ts (6 tests) 12ms
 ✓ businesses/workshop-admin/workshopCreateDraft.test.ts (7 tests) 12ms
 ✓ lib/community-membership/communityMembershipRedemption.test.ts (5 tests) 12ms
 ✓ businesses/community/membership/communityMembershipPrice.test.ts (7 tests) 12ms
 ✓ lib/workshops/workshopAdminViewState.test.ts (12 tests) 11ms
 ✓ lib/workshops/workshopValues.test.ts (12 tests) 13ms
 ✓ lib/admin/adminSession.test.ts (10 tests) 19ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaInternalEpisodes.test.ts (19 tests) 11ms
 ✓ lib/workshops/workshopWrapUpDocument.test.ts (4 tests) 11ms
 ✓ lib/supabase/reportSupabaseError.test.ts (6 tests) 9ms
 ✓ lib/shortener/shortcodeLandingPageMetadata.test.ts (3 tests) 10ms
 ✓ lib/supabase/loadAllSupabaseRows.test.ts (3 tests) 10ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaPublicPlatformStatistics.test.ts (11 tests) 10ms
 ✓ lib/exports/svgChartExport.test.ts (4 tests) 11ms
 ✓ lib/workshops/workshopPanels.test.ts (8 tests) 10ms
 ✓ lib/podcast/mergePodcastEpisodes.test.ts (9 tests) 10ms
 ✓ lib/podcast/parsePodcastRssFeed.test.ts (12 tests) 11ms
 ✓ lib/workshops/workshopRequest.test.ts (3 tests) 15ms
 ✓ lib/workshops/workshopAdminExports.test.ts (7 tests) 12ms
 ✓ lib/workshops/workshopCalendar.test.ts (7 tests) 13ms
 ✓ lib/workshops/workshopPhase.test.ts (9 tests) 9ms
 ✓ lib/workshops/workshopKindCapabilities.test.ts (13 tests) 9ms
 ✓ lib/podcast/podcastPlaybackProgress.test.ts (17 tests) 10ms
 ✓ lib/discounts/discountCode.test.ts (7 tests) 9ms
 ✓ lib/workshops/workshopCommentThreads.test.ts (7 tests) 9ms
 ✓ lib/community/communityPreviewValues.test.ts (10 tests) 9ms
 ✓ lib/random/shuffleByWeight.test.ts (6 tests) 10ms
 ✓ businesses/ai-supervize-mini/workshopRegistration.test.ts (5 tests) 9ms
 ✓ lib/workshops/workshopModeration.test.ts (6 tests) 7ms
 ✓ lib/youtube/youtubeChannelFeed.test.ts (9 tests) 9ms
 ✓ lib/discounts/discountCodeDatabase.test.ts (1 test) 8ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaMetadata.test.ts (3 tests) 8ms
 ✓ lib/workshops/workshopActiveStatusQuery.test.ts (3 tests) 10ms
stderr | lib/workshops/workshopRepository.test.ts > the stored project of a workshop > is no project when what is stored names no repository, whatever else is stored beside it
Unknown GitHub repository "not a repository" was read from the database.
 ✓ lib/workshops/workshopRepository.test.ts (5 tests) 8ms

 ✓ lib/community-projects/communityProjectPreview.test.ts (3 tests) 8ms
 ✓ lib/workshops/workshopOverviewRangeNavigation.test.ts (4 tests) 7ms
 ✓ lib/exports/pdfPictures.test.ts (2 tests) 8ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaViewState.test.ts (7 tests) 8ms
 ✓ lib/workshops/workshopClientState.test.ts (8 tests) 8ms
 ✓ lib/e2e/inMemorySupabase.test.ts (1 test) 8ms
 ✓ lib/community-membership/communityMembershipSubscription.test.ts (5 tests) 8ms
 ✓ lib/community-membership/communityMembershipRoomState.test.ts (9 tests) 8ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaBrandAssets.test.ts (6 tests) 8ms
 ✓ lib/metadata/create-page-metadata.test.ts (4 tests) 8ms
 ✓ lib/workshops/workshopAdminParticipantQuery.test.ts (3 tests) 9ms
 ✓ lib/youtube/youtube.test.ts (7 tests) 7ms
 ✓ lib/workshops/workshopAttendance.test.ts (6 tests) 7ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaSnakeLogoPose.test.ts (3 tests) 7ms
 ✓ lib/workshops/workshopPaidMembersVideo.test.ts (7 tests) 8ms
 ✓ lib/workshops/workshopSession.test.ts (2 tests) 7ms
 ✓ businesses/ai-ta-krajta/config.test.ts (1 test) 7ms
 ✓ businesses/online-workshop/onlineWorkshopMetadata.test.ts (1 test) 7ms
 ✓ lib/payments/stripeConfiguration.test.ts (5 tests) 7ms
 ✓ lib/workshops/workshopRepositoryGraph.test.ts (2 tests) 6ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaEpisodeHosts.test.ts (4 tests) 6ms
 ✓ lib/workshops/workshopPaidMembersContent.test.ts (5 tests) 6ms
 ✓ businesses/workshop-admin/workshopRepositoryDraft.test.ts (3 tests) 7ms
 ✓ lib/workshops/workshopSpecialMaterials.test.ts (4 tests) 6ms
 ✓ businesses/community/membership/communityMembershipMetadata.test.ts (3 tests) 6ms
 ✓ businesses/community/membership/communityMembershipCheckoutReturn.test.ts (4 tests) 6ms
 ✓ lib/community-membership/communityMembershipAdminQuery.test.ts (3 tests) 6ms
 ✓ lib/language/pageLanguage.test.ts (5 tests) 6ms
 ✓ app/sitemap.test.ts (2 tests) 6ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaMediaKitContent.test.ts (4 tests) 6ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaAudienceStatistics.test.ts (3 tests) 6ms
 ✓ lib/discounts/discountPlaces.test.ts (3 tests) 6ms
 ✓ lib/workshops/workshopChatInteractivity.test.ts (4 tests) 6ms
 ✓ lib/community-membership/communityMembershipAdminMigration.test.ts (2 tests) 5ms
 ✓ lib/contacts/serializeContactsAsBook.test.ts (1 test) 5ms
 ✓ lib/discounts/useDiscountCodeValidation.test.ts (5 tests) 5ms
 ✓ lib/workshops/workshopParticipantLink.test.ts (3 tests) 5ms
 ✓ lib/tracking/trackingExclusions.test.ts (3 tests) 7ms
 ✓ lib/workshops/workshopRegistrationTiming.test.ts (3 tests) 6ms
 ✓ businesses/ai-ta-krajta/aiTaKrajtaEpisodeTranscriptSearch.test.ts (2 tests) 5ms
 ✓ lib/community-membership/communityMembershipStatus.test.ts (4 tests) 5ms
 ✓ lib/api/createRequestSiteUrl.test.ts (2 tests) 5ms
 ✓ lib/community-membership/communityMembershipStripeDashboard.test.ts (3 tests) 4ms
 ✓ businesses/workshop-admin/workshopAdminSnapshot.test.ts (3 tests) 5ms
 ✓ lib/payments/stripeAmount.test.ts (3 tests) 5ms
 ✓ lib/community-membership/communityMembershipAdminLinks.test.ts (3 tests) 5ms
 ✓ lib/discounts/discountCodeWildcardMigration.test.ts (1 test) 4ms
 ✓ lib/workshops/workshopPollValues.test.ts (2 tests) 4ms
 ✓ lib/discounts/discountCodeSubscriptionDurationMigration.test.ts (1 test) 4ms
 ✓ lib/discounts/discountCodeAnyWildcardMigration.test.ts (1 test) 4ms
 ✓ lib/community-membership/communityMembershipCancellationMigration.test.ts (1 test) 4ms

 Test Files  191 passed (191)
      Tests  1243 passed (1243)
   Start at  09:48:44
   Duration  70.10s (transform 21.53s, setup 0ms, collect 84.11s, tests 40.94s, environment 94.64s, prepare 43.41s)


diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

codex
Verification is green: the full suite passes (191 files / 1,243 tests), as do the production build and TypeScript check. I’m doing one final workspace/diff check now to confirm the changelog entry is present and that no database or admin artifacts were introduced.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git status --short; git diff --check; rg -n \"wrap-up PDF|wrap-up|Shrnutí workshopu|Stáhnout shrnutí\" changelog/_current-preversion.md businesses/online-workshop/participant lib/workshops lib/exports; rg -n \"migrations|admin\" --glob '"'!node_modules'"' lib/workshops/workshopWrapUpDocument.ts lib/exports/workshopWrapUpPdf.ts businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx" in C:\Users\me\work\promptbook-experiments-and-landing-pages\aldaron
 succeeded in 370ms:
 M AGENTS.md
 M businesses/online-workshop/participant/OnlineWorkshopParticipantPage.tsx
 M businesses/online-workshop/participant/WorkshopStage.test.tsx
 M businesses/online-workshop/participant/WorkshopStage.tsx
 M businesses/online-workshop/participant/WorkshopWrapUp.tsx
 M changelog/_current-preversion.md
 M lib/exports/svgChartExport.ts
 M lib/workshops/workshopMaterialLinks.ts
 M prompts/2026-09-0240-wrap-up-pdf.md
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.test.tsx
?? businesses/online-workshop/participant/WorkshopWrapUpPdfDownload.tsx
?? lib/exports/pdfPictures.test.ts
?? lib/exports/pdfPictures.ts
?? lib/exports/workshopWrapUpPdf.ts
?? lib/workshops/workshopMaterialLinkSyntax.ts
?? lib/workshops/workshopWrapUpDocument.test.ts
?? lib/workshops/workshopWrapUpDocument.ts
changelog/_current-preversion.md:4:  downloadable, paginated wrap-up PDF. It uses the same participant-visible workshop description, takeaways already
changelog/_current-preversion.md:183:  the room replaces the stage with the closing wrap-up and its feedback the moment the workshop really ends, and the
changelog/_current-preversion.md:284:- Added the third, post-workshop stage to `/cs/online-workshop/participant`: when an occurrence ends, the live video is replaced with a thank-you wrap-up, a progressively saved star review (low scores ask what to improve first; high scores ask what helped first), and a direct link to the chosen follow-up material. Chat and reactions remain available throughout. One ordinary Markdown material can now be marked as the follow-up; it is highlighted in the material list before the end and is enforced as a single selection per workshop in the database.
lib/exports\workshopWrapUpPdf.ts:101:    context.fillText('Shrnutí workshopu', PDF_PAGE_MARGIN_PIXELS, 110);
lib/exports\workshopWrapUpPdf.ts:283:    addPdfSectionTitle(renderState, 'Shrnutí workshopu');
lib/exports\workshopWrapUpPdf.ts:348: * Renders the participant-safe wrap-up into an A4 PDF in the browser.
lib/exports\pdfPictures.test.ts:22:                'Shrnutí workshopu',
businesses/online-workshop/participant\OnlineWorkshopParticipantPage.test.tsx:156: * An occurrence which is already over by the moment the room is rendered at, so its stage is the closing wrap-up
lib/workshops\workshopDatabase.ts:2310:        // wrap-up starts it must be available for the stage link even if its ordinary unlock time is later; unrelated
lib/workshops\workshopMigration.test.ts:59:    'migrations/2026-08-1600-online-workshop-wrap-up-and-feedback.sql',
businesses/online-workshop/participant\WorkshopFeedback.test.tsx:11:describe('workshop wrap-up feedback', () => {
businesses/online-workshop/participant\WorkshopWrapUpPdfDownload.tsx:62:                {isDownloadInProgress ? 'Připravuji PDF…' : 'Stáhnout shrnutí workshopu (PDF)'}
businesses/online-workshop/participant\WorkshopWrapUpPdfDownload.test.tsx:60:describe('workshop wrap-up PDF download', () => {
businesses/online-workshop/participant\WorkshopWrapUpPdfDownload.test.tsx:64:        fireEvent.click(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' }));
businesses/online-workshop/participant\WorkshopWrapUpPdfDownload.test.tsx:88:        const downloadButton = screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' });
businesses/online-workshop/participant\WorkshopWrapUp.tsx:34:     * Offered only to the members whose membership unlocks the video of the ended workshop, so the wrap-up stays the
businesses/online-workshop/participant\WorkshopStage.tsx:119:    //       the server is what decides that. The wrap-up therefore keeps its feedback for everybody and gains either
businesses/online-workshop/participant\WorkshopStage.test.tsx:259:        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
businesses/online-workshop/participant\WorkshopStage.test.tsx:357:    it('replaces the video with the wrap-up while reactions keep their stage stream', () => {
businesses/online-workshop/participant\WorkshopStage.test.tsx:375:        expect(screen.getByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).not.toBeNull();
businesses/online-workshop/participant\WorkshopStage.test.tsx:379:    it('keeps the wrap-up for a member who has not paid and offers them the withheld video instead of playing it', () => {
businesses/online-workshop/participant\WorkshopStage.test.tsx:426:    it('keeps the wrap-up of a paid member and lets them play the video of the ended workshop again', () => {
lib/workshops\workshopWrapUpDocument.ts:148: * Builds the data of the wrap-up from fields which the participant room already owns.
lib/workshops\workshopWrapUpDocument.test.ts:39:describe('workshop wrap-up document', () => {
lib/exports/workshopWrapUpPdf.ts:351: * no administrative setting is needed, and a long list of materials simply continues on its next canvas page.

diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

codex
Implemented the post-workshop PDF wrap-up.

- The existing recorded-end phase now gates the download button; it is absent while live.
- PDF includes the workshop description, derived key takeaways, attendee-visible materials/links, and presentation.
- It is generated locally on demand—no admin UI, database fields, or migration added.
- Shared material-link parsing was extracted so browser PDF code stays client-safe and DRY.
- Updated `changelog/_current-preversion.md`.

Verified with:

- `npm test` — 191 files, 1,243 tests passed
- `npm run build` — passed
- `npx tsc` — passed
diff --git a/businesses/online-workshop/participant/WorkshopStage.test.tsx b/businesses/online-workshop/participant/WorkshopStage.test.tsx
index 5feaafd53adecc605329755a16d19b4231797be5..2f9946d91d6e960cb8252110c871eff975fb12fe
--- a/businesses/online-workshop/participant/WorkshopStage.test.tsx
+++ b/businesses/online-workshop/participant/WorkshopStage.test.tsx
@@ -256,6 +256,7 @@
         const requestFullscreen = vi.fn().mockResolvedValue(undefined);
 
         expect(videoFrame).not.toBeNull();
+        expect(screen.queryByRole('button', { name: 'Stáhnout shrnutí workshopu (PDF)' })).toBeNull();
         Object.defineProperty(videoFrame, 'requestFullscreen', { value: requestFullscreen });
 
         expect(videoFrame?.getAttribute('allow')).toContain('fullscreen');
diff --git a/lib/workshops/workshopMaterialLinks.ts b/lib/workshops/workshopMaterialLinks.ts
index f60aa853e385f89333496b9ebc64c015be8e3428..ed2b071c4da8a8ea8b26fb815aec3f41becaea17
--- a/lib/workshops/workshopMaterialLinks.ts
+++ b/lib/workshops/workshopMaterialLinks.ts
@@ -9,24 +9,23 @@
     WORKSHOP_COMMENT_SHORTCODE_LINK_TABLE_NAME,
     WORKSHOP_CONTENT_SHORTCODE_LINK_TABLE_NAME,
 } from '@/lib/workshops/workshopConstants';
+import {
+    collectWorkshopMaterialLinkRanges,
+    getTrackableWorkshopMaterialUrl,
+    getWorkshopShortcodeLinkDestinations,
+    getWorkshopShortcodeLinkDestinationsRequiringTitle,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
 import type { WorkshopKind } from '@/lib/workshops/workshopTypes';
 import type { SupabaseClient } from '@supabase/supabase-js';
 
+export {
+    getWorkshopMaterialLinkDestinations,
+    getWorkshopShortcodeLinkDestinations,
+} from '@/lib/workshops/workshopMaterialLinkSyntax';
+
 const WORKSHOP_UTM_SOURCE = 'promptbook';
 const WORKSHOP_UTM_MEDIUM = 'workshop';
-const WORKSHOP_MATERIAL_HASH_LINK_PREFIX = '#';
-const WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS = new Set(['http:', 'https:']);
-const WORKSHOP_MATERIAL_LINK_BASE_URL = 'https://www.promptbook.studio';
-const WORKSHOP_MATERIAL_BARE_URL_PATTERN = /(^|\s)(https?:\/\/[^\s<>()\[\]"']+)/gm;
-const WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN = /[.,;:!?]+$/;
 
-type WorkshopMaterialLinkRange = {
-    readonly destination: string;
-    readonly start: number;
-    readonly end: number;
-    readonly isTitleRequired: boolean;
-};
-
 type WorkshopShortcodeLinkMappingRow = {
     readonly destination_url: string;
     readonly destination_title?: string | null;
@@ -67,278 +66,7 @@
           readonly errorMessage: null;
       }
     | { readonly shortcodeLinkByDestination: null; readonly errorMessage: string };
-
-function getWorkshopMaterialLinkBaseUrl(): string {
-    return WORKSHOP_MATERIAL_LINK_BASE_URL;
-}
-
-function isEscaped(text: string, index: number): boolean {
-    let precedingBackslashCount = 0;
-
-    for (let cursor = index - 1; cursor >= 0 && text[cursor] === '\\'; cursor--) {
-        precedingBackslashCount++;
-    }
-
-    return precedingBackslashCount % 2 === 1;
-}
-
-function isWithinFencedCodeBlock(markdown: string, position: number): boolean {
-    const precedingText = markdown.slice(0, position);
-    const fenceCount = Array.from(precedingText.matchAll(/^ {0,3}(?:`{3,}|~{3,})/gm)).length;
-
-    return fenceCount % 2 === 1;
-}
-
-function isWithinInlineCode(markdown: string, position: number): boolean {
-    const lineStart = markdown.lastIndexOf('\n', position - 1) + 1;
-    const precedingLineText = markdown.slice(lineStart, position);
-    let unescapedBacktickCount = 0;
-
-    for (let cursor = 0; cursor < precedingLineText.length; cursor++) {
-        if (precedingLineText[cursor] === '`' && !isEscaped(precedingLineText, cursor)) {
-            unescapedBacktickCount++;
-        }
-    }
-
-    return unescapedBacktickCount % 2 === 1;
-}
-
-function isInsideCode(markdown: string, position: number): boolean {
-    return isWithinFencedCodeBlock(markdown, position) || isWithinInlineCode(markdown, position);
-}
-
-function findUnescapedCharacter(text: string, character: string, start: number): number {
-    for (let cursor = start; cursor < text.length; cursor++) {
-        if (text[cursor] === character && !isEscaped(text, cursor)) {
-            return cursor;
-        }
-    }
-
-    return -1;
-}
-
-function collectMarkdownInlineLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (let openingBracket = markdown.indexOf('['); openingBracket !== -1; openingBracket = markdown.indexOf('[', openingBracket + 1)) {
-        if (
-            (openingBracket > 0 && markdown[openingBracket - 1] === '!') ||
-            isEscaped(markdown, openingBracket) ||
-            isInsideCode(markdown, openingBracket)
-        ) {
-            continue;
-        }
-
-        const closingBracket = findUnescapedCharacter(markdown, ']', openingBracket + 1);
-        if (closingBracket === -1 || markdown[closingBracket + 1] !== '(') {
-            continue;
-        }
-
-        let destinationStart = closingBracket + 2;
-        while (/\s/.test(markdown[destinationStart] ?? '')) {
-            destinationStart++;
-        }
-
-        if (markdown[destinationStart] === '<') {
-            const closingAngleBracket = findUnescapedCharacter(markdown, '>', destinationStart + 1);
-            if (closingAngleBracket === -1) {
-                continue;
-            }
-
-            const destination = markdown.slice(destinationStart + 1, closingAngleBracket);
-            if (destination !== '') {
-                ranges.push({
-                    destination,
-                    start: destinationStart + 1,
-                    end: closingAngleBracket,
-                    isTitleRequired: false,
-                });
-            }
-            continue;
-        }
-
-        let parenthesisDepth = 0;
-        let destinationEnd = destinationStart;
-        for (; destinationEnd < markdown.length; destinationEnd++) {
-            const character = markdown[destinationEnd];
-            if (isEscaped(markdown, destinationEnd)) {
-                continue;
-            }
-            if (character === '(') {
-                parenthesisDepth++;
-                continue;
-            }
-            if (character === ')') {
-                if (parenthesisDepth === 0) {
-                    break;
-                }
-                parenthesisDepth--;
-                continue;
-            }
-            if (parenthesisDepth === 0 && /\s/.test(character)) {
-                break;
-            }
-        }
-
-        const destination = markdown.slice(destinationStart, destinationEnd);
-        if (destination !== '') {
-            ranges.push({ destination, start: destinationStart, end: destinationEnd, isTitleRequired: false });
-        }
-    }
-
-    return ranges;
-}
-
-function collectHtmlLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const openingAnchorPattern = /<a\b[^>]*>/gi;
-    const hrefPattern = /\bhref\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+))/i;
-
-    for (const openingAnchorMatch of Array.from(markdown.matchAll(openingAnchorPattern))) {
-        const openingAnchor = openingAnchorMatch[0];
-        const openingAnchorIndex = openingAnchorMatch.index ?? 0;
-        if (isInsideCode(markdown, openingAnchorIndex)) {
-            continue;
-        }
-
-        const hrefMatch = hrefPattern.exec(openingAnchor);
-        if (hrefMatch === null) {
-            continue;
-        }
-
-        const destination = hrefMatch[1] ?? hrefMatch[2] ?? hrefMatch[3];
-        if (!destination) {
-            continue;
-        }
-
-        const hrefText = hrefMatch[0];
-        const destinationIndexInHref = hrefText.lastIndexOf(destination);
-        if (destinationIndexInHref === -1) {
-            continue;
-        }
-
-        const hrefIndexInAnchor = hrefMatch.index;
-        ranges.push({
-            destination,
-            start: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref,
-            end: openingAnchorIndex + hrefIndexInAnchor + destinationIndexInHref + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectAutolinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const autolinkPattern = /<(https?:\/\/[^<>\s]+)>/gi;
-
-    for (const autolinkMatch of Array.from(markdown.matchAll(autolinkPattern))) {
-        const destination = autolinkMatch[1];
-        const matchIndex = autolinkMatch.index ?? 0;
-        if (destination !== undefined && !isInsideCode(markdown, matchIndex)) {
-            ranges.push({
-                destination,
-                start: matchIndex,
-                end: matchIndex + autolinkMatch[0].length,
-                isTitleRequired: true,
-            });
-        }
-    }
-
-    return ranges;
-}
-
-/**
- * The shared Markdown renderer applies its `simplifiedAutoLink` rule to a
- * bare HTTP(S) URL too. Materialization must therefore match that rendered
- * anchor as well as explicit Markdown and HTML links.
- */
-function collectBareUrlRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-
-    for (const bareUrlMatch of Array.from(markdown.matchAll(WORKSHOP_MATERIAL_BARE_URL_PATTERN))) {
-        const leadingWhitespace = bareUrlMatch[1] ?? '';
-        const rawDestination = bareUrlMatch[2];
-        const matchIndex = bareUrlMatch.index ?? 0;
-        const trailingPunctuation = rawDestination.match(WORKSHOP_MATERIAL_BARE_URL_TRAILING_PUNCTUATION_PATTERN)?.[0] ?? '';
-        const destination = rawDestination.slice(0, rawDestination.length - trailingPunctuation.length);
-        const start = matchIndex + leadingWhitespace.length;
-
-        if (destination !== '' && !isInsideCode(markdown, start)) {
-            ranges.push({ destination, start, end: start + destination.length, isTitleRequired: true });
-        }
-    }
-
-    return ranges;
-}
-
-function collectReferenceDefinitionRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const ranges: WorkshopMaterialLinkRange[] = [];
-    const referenceDefinitionPattern = /^ {0,3}\[[^\]\n]+\]:\s*(?:<([^>\n]+)>|(\S+))/gm;
-
-    for (const definitionMatch of Array.from(markdown.matchAll(referenceDefinitionPattern))) {
-        const destination = definitionMatch[1] ?? definitionMatch[2];
-        if (!destination) {
-            continue;
-        }
-
-        const matchIndex = definitionMatch.index ?? 0;
-        if (isInsideCode(markdown, matchIndex)) {
-            continue;
-        }
 
-        const destinationIndexInDefinition = definitionMatch[0].lastIndexOf(destination);
-        ranges.push({
-            destination,
-            start: matchIndex + destinationIndexInDefinition,
-            end: matchIndex + destinationIndexInDefinition + destination.length,
-            isTitleRequired: false,
-        });
-    }
-
-    return ranges;
-}
-
-function collectWorkshopMaterialLinkRanges(markdown: string): readonly WorkshopMaterialLinkRange[] {
-    const sortedRanges = [
-        ...collectMarkdownInlineLinkRanges(markdown),
-        ...collectHtmlLinkRanges(markdown),
-        ...collectAutolinkRanges(markdown),
-        ...collectReferenceDefinitionRanges(markdown),
-        ...collectBareUrlRanges(markdown),
-    ].sort(
-        (firstRange, secondRange) =>
-            firstRange.start - secondRange.start ||
-            firstRange.end - secondRange.end ||
-            Number(firstRange.isTitleRequired) - Number(secondRange.isTitleRequired),
-    );
-
-    const nonOverlappingRanges: WorkshopMaterialLinkRange[] = [];
-    for (const range of sortedRanges) {
-        const precedingRange = nonOverlappingRanges[nonOverlappingRanges.length - 1];
-        if (precedingRange === undefined || range.start >= precedingRange.end) {
-            nonOverlappingRanges.push(range);
-        }
-    }
-
-    return nonOverlappingRanges;
-}
-
-function getTrackableWorkshopMaterialUrl(destinationUrl: string): string | null {
-    if (!destinationUrl || destinationUrl.startsWith(WORKSHOP_MATERIAL_HASH_LINK_PREFIX)) {
-        return null;
-    }
-
-    try {
-        const parsedUrl = new URL(destinationUrl, getWorkshopMaterialLinkBaseUrl());
-
-        return WORKSHOP_MATERIAL_ALLOWED_PROTOCOLS.has(parsedUrl.protocol) ? parsedUrl.toString() : null;
-    } catch {
-        return null;
-    }
-}
-
 /**
  * Keeps the source address of a workshop-owned link useful to its destination's
  * analytics. The public address is later handed out as a short link; this is
@@ -372,36 +100,6 @@
     contentBlockId: string,
 ): string {
     return createWorkshopShortcodeLinkTrackingUrl(destinationUrl, workshopSlug, contentBlockId);
-}
-
-/**
- * Lists the HTTP(S) destinations written in a material, regardless of whether
- * they use inline Markdown, HTML, autolinks, or reference definitions. Images,
- * anchors, e-mail links, and code samples are deliberately not click links.
- */
-export function getWorkshopShortcodeLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
-}
-
-export function getWorkshopMaterialLinkDestinations(bodyMarkdown: string): readonly string[] {
-    return getWorkshopShortcodeLinkDestinations(bodyMarkdown);
-}
-
-function getWorkshopShortcodeLinkDestinationsRequiringTitle(bodyMarkdown: string): readonly string[] {
-    return Array.from(
-        new Set(
-            collectWorkshopMaterialLinkRanges(bodyMarkdown)
-                .filter((range) => range.isTitleRequired)
-                .map((range) => range.destination)
-                .filter((destination) => getTrackableWorkshopMaterialUrl(destination) !== null),
-        ),
-    );
 }
 
 function escapeMarkdownLinkTitle(title: string): string {
diff --git a/lib/workshops/workshopWrapUpDocument.ts b/lib/workshops/workshopWrapUpDocument.ts
index f6debcac189c2029bec08dfa431fda693203dc93..7e655233def12ca059ea4d0ce521fdaffb63b27d
--- a/lib/workshops/workshopWrapUpDocument.ts
+++ b/lib/workshops/workshopWrapUpDocument.ts
@@ -1,4 +1,4 @@
-import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinks';
+import { getWorkshopMaterialLinkDestinations } from '@/lib/workshops/workshopMaterialLinkSyntax';
 import { formatCzechWorkshopDate, formatCzechWorkshopTimeRange } from '@/lib/workshops/workshopDate';
 import type { WorkshopContentBlock, WorkshopDetails } from '@/lib/workshops/workshopTypes';
 

tokens used
142,989
Implemented the post-workshop PDF wrap-up.

- The existing recorded-end phase now gates the download button; it is absent while live.
- PDF includes the workshop description, derived key takeaways, attendee-visible materials/links, and presentation.
- It is generated locally on demand—no admin UI, database fields, or migration added.
- Shared material-link parsing was extracted so browser PDF code stays client-safe and DRY.
- Updated `changelog/_current-preversion.md`.

Verified with:

- `npm test` — 191 files, 1,243 tests passed
- `npm run build` — passed
- `npx tsc` — passed

=== runner shell finished at 2026-09-16T07:50:31.853Z ===
Status: succeeded

=== test shell started at 2026-09-16T07:50:31.882Z ===
Script path: /c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron/.promptbook/coder-prompts/2026-09-0240-wrap-up-pdf.test.sh

--- raw input ---
cd "/c/Users/me/work/promptbook-experiments-and-landing-pages/aldaron"
npm run test-for-ptbk-coder

--- raw output ---

> promptbook-landing-page@0.1.0 test-for-ptbk-coder
> npm run lint && npx kill-port 4009 && npm run test-types && npm run test-e2e && npm run delete-test-data


> promptbook-landing-page@0.1.0 lint
> next lint

✔ No ESLint warnings or errors
Process on port 4009 killed

> promptbook-landing-page@0.1.0 test-types
> npm run build && tsc


> promptbook-landing-page@0.1.0 build
> next build

   ▲ Next.js 15.2.6
   - Environments: .env

   Creating an optimized production build ...
 ✓ Compiled successfully
   Skipping validation of types
   Skipping linting
   Collecting page data ...
   Generating static pages (0/78) ...
   Generating static pages (19/78) 
BenefitsSection rendered
IntegrationsSection rendered
   Generating static pages (38/78) 
BenefitsSection rendered
BenefitsSection rendered
IntegrationsSection rendered
BenefitsSection rendered
IntegrationsSection rendered
BenefitsSection rendered
IntegrationsSection rendered
   Generating static pages (58/78) 
 ✓ Generating static pages (78/78)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                                  Size  First Load JS  Revalidate  Expire
┌ ƒ /                                                                                       377 B         102 kB
├ ○ /_not-found                                                                             377 B         102 kB
├ ƒ /[shortcode]                                                                          2.31 kB         142 kB
├ ƒ /admin                                                                                  175 B         105 kB
├ ƒ /admin/community                                                                      7.73 kB         325 kB
├ ƒ /admin/contacts                                                                         37 kB         196 kB
├ ƒ /admin/discount-codes                                                                 9.86 kB         119 kB
├ ƒ /admin/login                                                                            377 B         102 kB
├ ƒ /admin/shortener                                                                      64.6 kB         174 kB
├ ƒ /admin/workshops                                                                        215 B         318 kB
├ ○ /ai-supervize                                                                           16 kB         239 kB
├ ƒ /ai-supervize-mini                                                                    17.1 kB         231 kB
├ ○ /ai-supervize-mini/opengraph-image                                                      377 B         102 kB
├ ƒ /ai-supervize-mini/participant                                                        2.51 kB         139 kB
├ ○ /ai-supervize/opengraph-image                                                           377 B         102 kB
├ ○ /ai-ta-krajta                                                                         18.9 kB         146 kB          1h      1y
├ ○ /ai-ta-krajta/branding                                                                  204 B         121 kB
├ ○ /ai-ta-krajta/logo.png                                                                  377 B         102 kB
├ ○ /ai-ta-krajta/logo.svg                                                                  377 B         102 kB
├ ○ /ai-ta-krajta/manifest.webmanifest                                                      377 B         102 kB
├ ○ /ai-ta-krajta/media-kit                                                               3.63 kB         125 kB
├ ○ /ai-ta-krajta/opengraph-image                                                           377 B         102 kB
├ ƒ /api/admin/community/memberships                                                        377 B         102 kB
├ ƒ /api/admin/community/projects                                                           377 B         102 kB
├ ƒ /api/admin/community/projects/[projectId]                                               377 B         102 kB
├ ƒ /api/admin/discount-codes                                                               377 B         102 kB
├ ƒ /api/admin/discount-codes/[discountCodeId]                                              377 B         102 kB
├ ƒ /api/admin/session                                                                      377 B         102 kB
├ ƒ /api/admin/session/sign-out                                                             377 B         102 kB
├ ƒ /api/admin/shortener                                                                    377 B         102 kB
├ ƒ /api/admin/shortener/[shortcodeLinkId]                                                  377 B         102 kB
├ ƒ /api/admin/shortener/[shortcodeLinkId]/clicks                                           377 B         102 kB
├ ƒ /api/admin/workshops                                                                    377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]                                                       377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/analytics                                             377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/artificial-reactions                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments                                              377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments/[commentId]                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/comments/[commentId]/artificial-upvotes               377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/content                                               377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/content/[contentId]                                   377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/exports/[exportKind]                                  377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/feedback                                              377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants                                          377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants/[participantId]                          377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/participants/[participantId]/timeline                 377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls                                                 377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls/[pollId]                                        377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/polls/[pollId]/options/[optionId]/artificial-votes    377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/reactions                                             377 B         102 kB
├ ƒ /api/admin/workshops/[workshopId]/stage-comment                                         377 B         102 kB
├ ƒ /api/ai-supervize-mini/registration                                                     377 B         102 kB
├ ƒ /api/ai-ta-krajta/episodes/search                                                       377 B         102 kB
├ ƒ /api/community/membership/registration                                                  377 B         102 kB
├ ƒ /api/contacts                                                                           377 B         102 kB
├ ƒ /api/contacts/export/[formatId]                                                         377 B         102 kB
├ ƒ /api/discount-codes/validate                                                            377 B         102 kB
├ ƒ /api/stripe/webhook                                                                     377 B         102 kB
├ ƒ /api/track-click                                                                        377 B         102 kB
├ ƒ /api/waitlist                                                                           377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments/[commentId]                                      377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/comments/[commentId]/upvotes                              377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/connect                                                   377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/feedback                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership                                                377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/cancellation                                   377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/checkout                                       377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/checkout/confirmation                          377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/membership/portal                                         377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/participant                                               377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/participants/[participantId]                              377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/polls/[pollId]/votes                                      377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/presence                                                  377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/reactions                                                 377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/repository                                                377 B         102 kB
├ ƒ /api/workshops/[workshopSlug]/state                                                     377 B         102 kB
├ ƒ /api/workshops/komunita/projects                                                        377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]                                            377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]/connect                                    377 B         102 kB
├ ƒ /api/workshops/komunita/projects/[projectId]/vote                                       377 B         102 kB
├ ƒ /api/workshops/komunita/projects/preview                                                377 B         102 kB
├ ○ /branding                                                                             4.75 kB         141 kB
├ ○ /contact                                                                              2.54 kB         163 kB
├ ○ /cs                                                                                     169 B         239 kB
├ ƒ /cs/komunita                                                                            232 B         300 kB
├ ƒ /cs/komunita/calendar.ics                                                               377 B         102 kB
├ ƒ /cs/komunita/clenstvi                                                                 13.2 kB         154 kB
├ ○ /cs/komunita/clenstvi/opengraph-image                                                   377 B         102 kB
├ ○ /cs/komunita/opengraph-image                                                            377 B         102 kB
├ ƒ /cs/komunita/projects                                                                 2.84 kB         139 kB
├ ƒ /cs/komunita/projects/[projectId]                                                     3.02 kB         297 kB
├ ○ /cs/obchodni-podminky                                                                 2.52 kB         139 kB
├ ○ /cs/ochrana-osobnich-udaju                                                            2.52 kB         139 kB
├ ƒ /cs/online-workshop                                                                   13.1 kB         244 kB
├ ƒ /cs/online-workshop/dekujeme                                                          4.01 kB         152 kB
├ ○ /cs/online-workshop/opengraph-image                                                     377 B         102 kB
├ ƒ /cs/online-workshop/participant                                                       2.73 kB         297 kB
├ ○ /cs/online-workshop/participant/opengraph-image                                         377 B         102 kB
├ ○ /cs/opengraph-image                                                                     377 B         102 kB
├ ○ /cs/pavol                                                                               158 B         203 kB
├ ○ /cs/pavol/opengraph-image                                                               377 B         102 kB
├ ○ /data-deletion                                                                        2.51 kB         139 kB
├ ○ /dekujeme                                                                             3.71 kB         151 kB
├ ○ /en                                                                                     169 B         239 kB
├ ○ /en/opengraph-image                                                                     377 B         102 kB
├ ○ /en/pavol                                                                               158 B         203 kB
├ ○ /en/pavol/opengraph-image                                                               377 B         102 kB
├ ○ /en/privacy-policy                                                                    2.52 kB         139 kB
├ ○ /en/terms-and-conditions                                                              2.52 kB         139 kB
├ ○ /for-agro                                                                             7.31 kB         268 kB
├ ○ /for-agro/opengraph-image                                                               377 B         102 kB
├ ○ /for-industry                                                                         7.92 kB         269 kB
├ ○ /for-industry/opengraph-image                                                           377 B         102 kB
├ ○ /hackathon-factory                                                                      15 kB         252 kB
├ ○ /hackathon-factory/opengraph-image                                                      377 B         102 kB
├ ƒ /k/[...shortFileUrlParts]                                                               377 B         102 kB
├ ○ /manifest.webmanifest                                                                   377 B         102 kB
├ ○ /old                                                                                  20.5 kB         245 kB
├ ○ /opengraph-image                                                                        377 B         102 kB
├ ƒ /pavol                                                                                  377 B         102 kB
├ ƒ /privacy                                                                                377 B         102 kB
├ ƒ /pro-firmy                                                                              377 B         102 kB
├ ○ /pro-mesta                                                                            6.39 kB         267 kB
├ ○ /pro-mesta/opengraph-image                                                              377 B         102 kB
├ ○ /robots.txt                                                                             377 B         102 kB
├ ○ /shortener                                                                              377 B         102 kB
├ ○ /sitemap.xml                                                                            377 B         102 kB
├ ƒ /skoleni                                                                                377 B         102 kB
├ ƒ /terms                                                                                  377 B         102 kB
└ ○ /test/hopko                                                                           4.62 kB         106 kB
+ First Load JS shared by all                                                              102 kB
  ├ chunks/1684-6c8ee981b3c4cfc9.js                                                       45.8 kB
  ├ chunks/4bd1b696-56d776b9b579ad04.js                                                   53.3 kB
  └ other shared chunks (total)                                                           2.53 kB


ƒ Middleware                                                                              30.9 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand


> promptbook-landing-page@0.1.0 test-e2e
> playwright test

[WebServer] Database migrations skipped: DATABASE_URL is not configured.

Running 47 tests using 1 worker

  ✓   1 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs (10.5s)
  ✓   2 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /en (3.6s)
  ✓   3 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /pro-mesta (6.2s)
  ✓   4 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /for-agro (4.9s)
  ✓   5 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /for-industry (4.3s)
  ✓   6 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /ai-supervize (6.1s)
  ✓   7 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /ai-supervize-mini (9.3s)
  ✓   8 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /ai-ta-krajta (6.2s)
  ✓   9 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /ai-ta-krajta/media-kit (4.5s)
  ✓  10 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /ai-ta-krajta/branding (4.7s)
  ✓  11 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /hackathon-factory (5.2s)
  ✓  12 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs/online-workshop (7.2s)
  ✓  13 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs/komunita/clenstvi (11.3s)
  ✓  14 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs/pavol (5.8s)
  ✓  15 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /en/pavol (4.7s)
  ✓  16 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs/ochrana-osobnich-udaju (4.2s)
  ✓  17 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /en/privacy-policy (5.0s)
  ✓  18 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /cs/obchodni-podminky (4.3s)
  ✓  19 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /en/terms-and-conditions (4.9s)
  ✓  20 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /contact (4.7s)
  ✓  21 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /branding (6.9s)
  ✓  22 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /data-deletion (7.6s)
  ✓  23 tests\e2e\public-pages.spec.ts:134:9 › public landing and information page loads: /old (16.4s)
  ✓  24 tests\e2e\public-pages.spec.ts:139:5 › AI ta Krajta collaboration section deep-links to its media kit (9.8s)
  ✓  25 tests\e2e\public-pages.spec.ts:150:5 › AI ta Krajta owns its metadata, icon and installable manifest and credits Promptbook coder (13.8s)
  ✓  26 tests\e2e\public-pages.spec.ts:246:5 › AI ta Krajta searches complete transcripts on the server without sending them to the browser (6.3s)
  ✓  27 tests\e2e\public-pages.spec.ts:264:9 › public address redirects to its page: / (36ms)
  ✓  28 tests\e2e\public-pages.spec.ts:264:9 › public address redirects to its page: /pavol (3.3s)
  ✓  29 tests\e2e\public-pages.spec.ts:264:9 › public address redirects to its page: /privacy (2.6s)
  ✓  30 tests\e2e\public-pages.spec.ts:264:9 › public address redirects to its page: /terms (2.4s)
  ✓  31 tests\e2e\public-pages.spec.ts:264:9 › public address redirects to its page: /skoleni (2.5s)
  ✓  32 tests\e2e\public-pages.spec.ts:278:5 › /skoleni carries a discount code to the workshop registration (118ms)
  ✓  33 tests\e2e\public-submissions.spec.ts:13:5 › submits the shared footer newsletter form (19.8s)
  ✓  34 tests\e2e\public-submissions.spec.ts:27:5 › submits the reusable get-started lead dialog (7.2s)
  ✓  35 tests\e2e\public-submissions.spec.ts:41:5 › submits the business lead dialog (4.5s)
  ✓  36 tests\e2e\public-submissions.spec.ts:55:5 › submits the homepage qualification lead flow (31.8s)
  ✓  37 tests\e2e\public-submissions.spec.ts:80:5 › submits Pavol’s personal contact form (6.7s)
  ✓  38 tests\e2e\public-submissions.spec.ts:96:5 › submits a published online-workshop registration (24.7s)
  ✓  39 tests\e2e\public-submissions.spec.ts:116:5 › personalizes and submits the 199 Kč Promptbook paid community membership (12.3s)
  ✓  40 tests\e2e\public-submissions.spec.ts:143:5 › submits an available AI Supervize Mini workshop registration (12.1s)
  ✓  41 tests\e2e\public-submissions.spec.ts:171:5 › submits the AI Supervize Mini future-term interest form (4.6s)
  ✓  42 tests\e2e\public-submissions.spec.ts:196:5 › submits the AI ta Krajta collaboration form (4.8s)
  ✓  43 tests\e2e\public-submissions.spec.ts:211:5 › plays the newest AI ta Krajta episode from the header (4.0s)
  ✓  44 tests\e2e\public-submissions.spec.ts:220:5 › resumes a half-played AI ta Krajta episode where its listener left it (6.4s)
  ✓  45 tests\e2e\public-submissions.spec.ts:254:5 › starts the AI ta Krajta minigame from its snake and keeps it local to the page (3.6s)
  ✓  46 tests\e2e\public-submissions.spec.ts:268:5 › filters the AI ta Krajta archive by a person and keeps its destination in the hash (4.4s)
  -  47 tests\e2e\public-submissions.spec.ts:280:5 › connects a public online-workshop participant
Saved 41 E2E video(s) to tests/e2e/videos/.
Removed the E2E video(s) of 1 outdated run(s) from tests/e2e/videos/.

  1 skipped
  46 passed (6.5m)

> promptbook-landing-page@0.1.0 delete-test-data
> tsx scripts/delete-test-data.ts

Deleted 10 Contact row(s) and 0 workshop participant row(s) with an @example.com e-mail address.

=== test shell finished at 2026-09-16T07:58:43.385Z ===
Status: succeeded
```
